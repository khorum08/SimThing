# Normalize all text files to LF in the repo and on checkout, regardless of
# core.autocrlf. The ClauseThing tests compare emitted JSON byte-for-byte
# against goldens in crates/simthing-clausething/tests/goldens/, so fixtures
# and goldens must check out with LF bytes on every platform.
* text=auto eol=lf
