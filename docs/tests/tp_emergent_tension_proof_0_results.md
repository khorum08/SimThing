# TP-EMERGENT-TENSION-PROOF-0 — evidence

## Claim

Canonical multi-tick GPU runs on `scenarios/terran_pirate_galaxy.clause` through ordinary `open_from_spec + step_once` accrete production from an authored output coefficient, suppress local flow through an authored `flow_coupling` on distinct location hosts, and diverge sealed **construction** crossings (manufacturing need on produced hulls, `event_kind = 92`) when only owner policy-weight silo scalars change. Clause-source coefficient=`0` and coupling-omitted controls bite. RF-5 expansion/minerals regressions remain green via source variants. Owner OVL is **pending**.

## Identity SHAs

- Branch: `coder/tp-emergent-tension-proof-0`
- PR: https://github.com/khorum08/SimThing/pull/1418
- Accepted first landing (governance only): `cbf3b81f5659bbfdefafa1001276713178f54dab`
- Remand base reviewed: `38c0185da63faa90812bcd6b8a3b550728c1d3ba`
- Implementation / tested_code_sha: `25083f0d32209e00f65e140293e9972e34e1f207`
- Evidence bind commit: *(next)*
- Final head: *(after evidence bind)*

## Load-bearing proof

`cargo test -p simthing-workshop --test tp_field_session_elevate_0 -- --test-threads=1`

Representative paired print (local Windows GPU, post-remand):

```
TP12_10_PAIRED ticks=8 path=simthing_driver::SimSession::open_from_spec + step_once
  productive={production:318 disruption:70 suppression:2 crossings:1}
  tension={production:280 disruption:32 suppression:40 crossings:0}
  divergence={production:38 suppression:38 crossings:1}
TP12_10_COUPLING_NEGATIVE … production=320 disruption=72 suppression=0
TP12_10_COEFFICIENT_NEGATIVE … production_open=0 production_after=0 crossings=0
```

Distinct hosts proven per run: `shipyard != outpost != session_root`.

Predeclared minima: production divergence ≥ 6, suppression divergence ≥ 6, crossing divergence ≥ 1.

## Remand corrections

1. Construction need = `terran_manufacturing_need` on `terran_shipyard_hulls_quantity` / `event_kind = 92` (threshold 5.0). RF-5 minerals/expansion (`event_kind = 91`) kept via Clause-source variants in the RF-5 tests.
2. Negatives are Clause-source only (`coefficient = 0.0`; omit `flow_coupling` block). No post-hydration recipe surgery. Coupling is optional when production exists; present malformed couplings still fail closed.
3. Studio gauges use exact property keys; separate disrupted_hulls row; cumulative construction crossings are event_kind-filtered. Headless falsifier: `studio_macro_gauges_use_exact_property_and_construction_event_kind`.
4. `compose_recursive_rf_profile` grafts authored Location hosts under GameSession (no root collapse).

## Surfaces

- Generic coefficient + optional `flow_coupling` authoring/hydration
- Spec/kernel admit `output_coefficient` / `output_scale` ≥ 0 (0 = neutralization)
- Canonical clause + Unbounded Amount layout
- Studio_ops Telemetry exact-identity macro gauges
- Location-host graft in recursive RF compose

## Regressions

- RF-5 high/low need-event pair (minerals source variant): green
- RF-4 `15/10` marginal `5` + governed-Balance disconnect: green
- `field_economy_grammar_0` (10): green
- `tp_clause_economy_author_0`: green
- agent_scan / inventory drift: green

## Owner OVL

**Pending** — paired Studio_ops Telemetry screenshots under the two policy-weight authorings remain Owner-closed after orch code acceptance. Do not self-merge or mark ready.
