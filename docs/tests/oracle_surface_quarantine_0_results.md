# ORACLE-SURFACE-QUARANTINE-0 results

Status: **PROBATION / proof-present / DA-review-pending / UNMERGED**.
Coding has not merged, graduated the rung, moved the pointer, started 15.5,
15.6, or 15.7, begun response compression, rewritten canon, or closed the
track.

## Provenance and disposition

- Board handoff: `5531304048`
- exact branch base: `8ee45b14e844894ad46894d5b85e2d9630fdb9b8`
- branch: `codex/oracle-surface-quarantine-0`
- `HD-RECEIPT: 2a1a98c9fcca`
- `ORIENT-RECEIPT: c05b799e70ab`
- orientation rule stamp: `ce36bb354377823a`
- orientation digest:
  `23559f6d8d29e1d4e2977675c1a301efc6368df0993298245e13371de54579ba`
- expected return route: `DA-RESERVE(binding)`

Archaeology supports a mixed, minimal disposition. The recursive Vendor Door
witness is the sole embedder consumer of
`clear_constrained_claims_at_generation`, so that item remains reachable only
through an explicitly named, conversion-free
`run::cpu_filter_oracle` alias. `clear_stamped_owner_channels` has no embedder
caller, example, or document consumer and is removed from the embedder surface
entirely. Neither owning spec function changes.

## Symbol-keyed archaeology

| Symbol | Embedder before | Embedder consumers | Disposition | Embedder after |
|---|---|---|---|---|
| `clear_constrained_claims_at_generation` | unqualified `run` re-export | two calls in the frozen recursive non-residency Vendor Door witness; no production/example caller | rehome the same function item, with no wrapper | `run::cpu_filter_oracle::clear_constrained_claims_at_generation` only |
| `clear_stamped_owner_channels` | unqualified `run` re-export | none | remove the unused re-export | absent |

The repository-wide census also finds the unchanged owning definitions and
their spec/driver/proof corpus. The host-clearer source census remains five
definitions. The driver constitutional caller census remains the same two
authorized identities: growth entitlement's generalized CPU reference call
and runtime-RF tick compilation's temporal reference call. The embedder adds
no production caller.

The historical host-door census now records exact live re-export truth:
`clear_constrained_claims_at_generation` is re-exported by spec and the
explicit embedder oracle namespace; `clear_stamped_owner_channels` is
re-exported by spec only. Related current evidence tables carry the same
correction.

## Public-path and implementation proof

| Surface | Before | After | Mechanical proof |
|---|---:|---:|---|
| unqualified `run::clear_constrained_claims_at_generation` | resolves | absent | compile-fail `E0432` seal |
| unqualified `run::clear_stamped_owner_channels` | resolves | absent | compile-fail `E0432` seal |
| explicit `run::cpu_filter_oracle::clear_constrained_claims_at_generation` | absent | resolves | exact function-pointer type identity plus real Vendor Door calls |
| explicit embedder stamped-door alias | absent | absent | source census and constitutional row |

The explicit module contains one `pub use` item and no function body. It is a
Rust item alias to the existing spec function: no wrapper, adapter, newtype,
conversion, arithmetic, state, or authority exists in the facade. The
ordinary `run` lifecycle remains initialize/start/tick/serialize plus its
existing leaf delegations.

## Quarantine and preservation

`CPU-CLEARING-ORACLE-EMBEDDER-REEXPORTS=1` pins the one allowed explicit alias.
The focused referee independently reconstructs all five spec definitions and
checks the unchanged peer-authority residue. Together with the existing
constitutional guards, the structural counts remain:

- `CPU-CLEARING-ORACLE-DOORS=5`;
- `CPU-CLEARING-ORACLE-CALL-SITES=2`;
- `CPU-CLEARING-ORACLE-EMBEDDER-REEXPORTS=1`;
- `RECURSIVE-FILTER-PEER-RUNTIME-AUTHORITY-RESIDUE=2`;
- resident duplicate-settlement, economic-adapter, private-field-solver, and
  global-coupling forbidden shapes all `0`.

No exact-Q source, canonical `T_s` carrier, resident R/Q runtime, 15.2
persistence deformation, 15.3 consequence ingress, CPU door implementation,
or production caller changes.

## Verification ledger

| Command / proof | Result |
|---|---|
| focused `oracle_surface_quarantine_0` | PASS — 2/2 |
| embedder compile-fail docs | PASS — two old-path `E0432` seals |
| real-GPU `vendor_door_triad_surface_0` | PASS — 3/3 |
| frozen 15.1/15.2/15.3/14.5/14.6 batteries | PASS |
| touched-package checks and embedder battery | PASS |
| test inventory / drift | PASS — 1407/1407, missing 0, unledgered 0, stale 0 |
| constitutional census / selftest | PASS — doors 5, driver callers 2, embedder aliases 1, peer residue 2, planted 12 |
| anchors/lifecycle, sanctioned-surface freshness, detachability, Agent Scan | PASS |
| full structural certificate | PASS — ZERO RED |
| hosted Doctrine Scan/Exec, exact-head clearance, relay-lint | recorded in the immutable Board return packet |

## Changed-file census and fences

The sole production change is `simthing-embedder/src/run.rs`: two unqualified
re-exports leave the ordinary surface and the one required function item is
re-exposed under the explicit oracle namespace. Proof changes update the
existing Vendor Door call spelling, add the focused embedder referee and two
compile-fail seals, correct the symbol-keyed host-door re-export census and
current evidence prose, and register the constitutional/test/reach ledgers
plus regenerated sanctioned-surface digest.

No workflow, CI shell/Python, spec/driver/kernel/GPU/sim/ClauseThing/Studio
production code, shader, door implementation, workplan pointer, handoff
pointer, or canonical doctrine changes. Final exact head, PR, hosted run IDs,
clearance, and relay-lint are recorded on the Board because those identities
exist only after this report is committed.
