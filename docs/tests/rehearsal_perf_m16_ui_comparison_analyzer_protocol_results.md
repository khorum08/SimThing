# M16 B1 — original analyzer and protocol versions

PROBATION / proof-present data / OPEN / UNMERGED. Rung: 0088-UI-PROTOTYPE-0.
Authority: Board 5654699942 and disposition 5671634736.
Retires with the rung; this is evidence, not a permanent instrument or authority.

Exact UTF-8 source bytes represented as JSON strings; SHA-256 binds the original bytes, including line endings/BOM. The replay uses only the original per-run versions. No historical result is re-adjudicated with a later protocol. Source is preserved as evidence; it adds no production module.

<!-- m16-block:versions -->
```jsonl
{"kind":"analyzer","sha256":"77a5a12ecd8d844099e47bc9002e01f25b7674aad0c7e11f43301a8a4bb9bcd7","utf8_text":"\"\"\"External B1 evidence analysis; standard library only; no app/render authority.\"\"\"\nimport argparse\nimport bisect\nfrom collections import defaultdict\nimport hashlib\nimport json\nimport math\nfrom pathlib import Path\nimport statistics\n\n\ndef load(path):\n    return json.loads(Path(path).read_text(encoding='utf-8-sig'))\n\n\ndef digest(path):\n    return hashlib.sha256(Path(path).read_bytes()).hexdigest()\n\n\ndef stats(values):\n    if not values:\n        return {'count': 0, 'status': 'UNAVAILABLE'}\n    ordered = sorted(values)\n    return {'count': len(values), 'min': ordered[0], 'max': ordered[-1],\n            'mean': statistics.mean(values),\n            **{f'p{p}': ordered[math.ceil(p * len(ordered) / 100) - 1] for p in (50, 95, 99)}}\n\n\ndef unique_changes(values):\n    result = []\n    for value in values:\n        if not result or value != result[-1]:\n            result.append(value)\n    return result\n\n\ndef stamp_key(stamp):\n    return tuple(stamp[key] for key in ('scene', 'resident_epoch', 'generation', 'published_ns'))\n\n\ndef analyze(capture, protocol):\n    failures = []\n    meta = capture['config']['metadata']\n    group = meta['condition']\n    expected_client = meta['client_condition']\n    start, stop = capture['started_ns'], capture['stopped_ns']\n    if capture.get('instrument') != 'm16-b0a-v1':\n        failures.append('Wrong capture instrument')\n    if meta.get('code_revision') != protocol['code_revision']:\n        failures.append('Wrong code revision')\n    if capture.get('stop_reason') != 'operator stop' or stop is None or stop <= start:\n        failures.append('Capture did not finish with a valid operator stop')\n        return {'valid': False, 'failures': failures}\n    duration = (stop - start) / 1e9\n    if duration < protocol['minimum_capture_seconds']:\n        failures.append(f'Capture was only {duration:.2f}s; at least 10s is required')\n    facts = [capture['initial_facts']] + [s['facts'] for s in capture['samples'] if s['kind'] == 'Runtime']\n    for fact in facts:\n        for key, value in [('source_identity', protocol['expected_source_identity']),\n                           ('profile_identity', protocol['expected_profile_identity']),\n                           ('dependencies', protocol['expected_dependencies']),\n                           ('systems', 7), ('reset_pending', False), ('panels_hidden', False),\n                           ('focused', True), ('native_enabled', expected_client == 'Native')]:\n            if fact.get(key) != value:\n                failures.append(f'Unexpected {key}; check source/client/focus/setup')\n        if fact.get('scene') != facts[0].get('scene') or fact.get('resident_epoch') != facts[0].get('resident_epoch'):\n            failures.append('Scene or resident changed while recording')\n        if fact.get('paused') != (group != 'running'):\n            failures.append('Unexpected pause/run state while recording')\n        if fact.get('rate') not in ('1\u00d7', '1x'):\n            failures.append('Clock rate must be 1x')\n        if group != 'numeric_edit' and fact.get('max_tps') != 10:\n            failures.append('Max TPS must remain 10 for this group')\n        if group != 'resize' and fact.get('window_pixels') != facts[0].get('window_pixels'):\n            failures.append('Window changed outside the resize group')\n        if fact.get('scale_factor') != facts[0].get('scale_factor'):\n            failures.append('DPI changed during this fixed-scale capture')\n    tps_sequence = unique_changes([f['max_tps'] for f in facts])\n    size_sequence = unique_changes([f['window_pixels'] for f in facts])\n    selection_sequence = unique_changes([f['selected_system'] for f in facts])\n    if group == 'numeric_edit' and tps_sequence != [10, 12.5, 10, 12.5, 10, 12.5, 10]:\n        failures.append('Numeric edits must record exactly three 12.5 -> 10 pairs')\n    if group == 'resize':\n        initial = size_sequence[0]\n        if len(size_sequence) != 7 or any(size_sequence[i] != initial for i in (2, 4, 6)):\n            failures.append('Resize must record three maximize/restore cycles ending at the initial size')\n        elif not (size_sequence[1] == size_sequence[3] == size_sequence[5] and size_sequence[1] != initial):\n            failures.append('Resize cycles did not reach the same second size')\n    if group == 'list_interaction' and selection_sequence != [None, 1, 2, 3, 4, 5, 6, 7]:\n        failures.append('Selection must start clear and visit systems 1 through 7 in order')\n    if group not in [g['id'] for g in protocol['groups']]:\n        failures.append('Unknown condition')\n\n    frames = [s for s in capture['samples'] if s['kind'] == 'Frame']\n    if not frames:\n        failures.append('No raw frame cadence samples were recorded')\n    cpu = [s for s in capture['samples'] if s['kind'] == 'Cpu']\n    cpus = defaultdict(list)\n    for frame in frames:\n        if (frame['time_strategy'] != 'Automatic' or frame['end_ns'] - frame['start_ns'] != frame['delta_ns']\n                or frame['delta_ns'] <= 0 or frame['start_ns'] < start or frame['end_ns'] > stop):\n            failures.append('Invalid/synthetic/reversed frame interval')\n    if any(a['end_ns'] >= b['end_ns'] for a, b in zip(frames, frames[1:])):\n        failures.append('Non-monotonic/duplicate frame records')\n    for sample in cpu:\n        if sample['end_ns'] < sample['start_ns'] or not start <= sample['start_ns'] <= sample['end_ns'] <= stop:\n            failures.append('CPU interval outside capture or reversed')\n        cpus[f\"{sample['client']}:{sample['scope']}\"] .append(sample['end_ns'] - sample['start_ns'])\n    expected_scope = f'{expected_client}:shared_clock_and_observation_projection'\n    if not cpus.get(expected_scope):\n        failures.append('Expected client projection was not measured; keep its pane visible')\n    if expected_client == 'Egui' and cpus.get('Native:shared_clock_and_observation_projection'):\n        failures.append('Native projection appeared in the native-disabled baseline')\n    publications = {stamp_key(s['stamp']): s['stamp'] for s in capture['samples'] if s['kind'] == 'Publication'}\n    all_stamps = dict(publications)\n    consumptions = [s for s in capture['samples'] if s['kind'] == 'Consumption']\n    fresh = defaultdict(list)\n    pre_capture = defaultdict(list)\n    seen = set()\n    by_publication = defaultdict(dict)\n    for sample in consumptions:\n        stamp = sample['stamp']\n        key = stamp_key(stamp)\n        all_stamps[key] = stamp\n        token = (sample['client'], key)\n        age = sample['consumed_ns'] - stamp['published_ns']\n        if (age < 0 or stamp['scene'] != facts[0]['scene']\n                or stamp['resident_epoch'] != facts[0]['resident_epoch'] or token in seen\n                or not start <= sample['consumed_ns'] <= stop):\n            failures.append('Stale/reversed/duplicate consumption stamp')\n        seen.add(token)\n        if stamp['published_ns'] < start:\n            pre_capture[sample['client']].append({'stamp': stamp, 'age_ns': age, 'consumed_ns': sample['consumed_ns']})\n        else:\n            if key not in publications:\n                failures.append('In-capture consumption lacks matching Publication record')\n            fresh[sample['client']].append({'stamp': stamp, 'age_ns': age, 'consumed_ns': sample['consumed_ns']})\n            by_publication[key][sample['client']] = sample\n    fresh_pairs = [{'stamp': publications[key],\n                    'native_minus_egui_ns': pair['Native']['consumed_ns'] - pair['Egui']['consumed_ns']}\n                   for key, pair in by_publication.items() if 'Native' in pair and 'Egui' in pair]\n\n    # CPU intervals are paired only inside an actual recorded Frame interval,\n    # with one call per client and the same unchanged publication identity.\n    # Unmatched boundary calls are retained in the raw file and counted below.\n    stamped = sorted(all_stamps.values(), key=lambda s: s['published_ns'])\n    stamp_times = [s['published_ns'] for s in stamped]\n    frame_ends = [f['end_ns'] for f in frames]\n    projected = defaultdict(lambda: defaultdict(list))\n    unpaired_projection_calls = 0\n    for sample in cpu:\n        if sample['scope'] != 'shared_clock_and_observation_projection':\n            continue\n        index = bisect.bisect_left(frame_ends, sample['end_ns'])\n        before = bisect.bisect_right(stamp_times, sample['start_ns']) - 1\n        after = bisect.bisect_right(stamp_times, sample['end_ns']) - 1\n        if (index >= len(frames) or frames[index]['start_ns'] > sample['start_ns']\n                or before < 0 or before != after):\n            unpaired_projection_calls += 1\n            continue\n        projected[(index, stamp_key(stamped[before]))][sample['client']].append(sample)\n    cpu_pairs = []\n    runtime_times = sorted(s['at_ns'] for s in capture['samples'] if s['kind'] == 'Runtime')\n    for (index, key), pair in projected.items():\n        runtime_index = bisect.bisect_left(runtime_times, frames[index]['start_ns'])\n        runtime_changed = runtime_index < len(runtime_times) and runtime_times[runtime_index] <= frames[index]['end_ns']\n        if len(pair.get('Egui', [])) == len(pair.get('Native', [])) == 1 and not runtime_changed:\n            egui, native = pair['Egui'][0], pair['Native'][0]\n            cpu_pairs.append({'frame_index': index, 'frame_start_ns': frames[index]['start_ns'],\n                              'frame_end_ns': frames[index]['end_ns'], 'publication_key': list(key),\n                              'egui_start_ns': egui['start_ns'], 'egui_end_ns': egui['end_ns'],\n                              'native_start_ns': native['start_ns'], 'native_end_ns': native['end_ns'],\n                              'native_minus_egui_ns': (native['end_ns'] - native['start_ns']) - (egui['end_ns'] - egui['start_ns'])})\n        else:\n            unpaired_projection_calls += sum(map(len, pair.values()))\n    rejected = [s for s in capture['samples'] if s['kind'] == 'Rejected']\n    if rejected:\n        failures.append('App rejected stale/reset/unmatched observations; raw rejections retained')\n    if group == 'running' and len(fresh.get(expected_client, [])) < 3:\n        failures.append('Running capture did not publish/consume at least three generations')\n    frame_stats = stats([s['delta_ns'] / 1e6 for s in frames])\n    return {'valid': not failures, 'failures': sorted(set(failures)), 'condition': group,\n            'client': expected_client, 'repetition': int(meta['repetition']),\n            'duration_seconds': duration, 'initial_facts': facts[0], 'final_observed_facts': facts[-1],\n            'tps_sequence': tps_sequence, 'window_size_sequence': size_sequence,\n            'selection_sequence': selection_sequence, 'frame_ms': frame_stats,\n            'p95_frame_16_7_ms_met': frame_stats.get('p95', math.inf) <= 16.7,\n            'cpu_scopes_ns': {key: stats(values) for key, values in sorted(cpus.items())},\n            'freshness_within_capture': dict(fresh), 'pre_capture_publication_ages': dict(pre_capture),\n            'freshness_ns': {client: stats([s['age_ns'] for s in values]) for client, values in fresh.items()},\n            'same_publication_freshness_pairs': fresh_pairs,\n            'same_frame_same_publication_projection_pairs': cpu_pairs,\n            'unpaired_projection_calls': unpaired_projection_calls,\n            'retained_rejections': rejected,\n            'input_latency': 'INSTRUMENT-INVALID / UNAVAILABLE; p95 <=100 ms UNEVALUATED',\n            'first_display': 'UNAVAILABLE', 'direct_render_gpu_cost': 'UNAVAILABLE',\n            'simulation_vs_presentation_gpu_memory': 'UNAVAILABLE'}\n\n\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('raw')\n    parser.add_argument('report')\n    parser.add_argument('--protocol', default=str(Path(__file__).with_name('protocol.json')))\n    args = parser.parse_args()\n    report = analyze(load(args.raw), load(args.protocol))\n    report['raw_sha256'] = digest(args.raw)\n    report['analyzer_sha256'] = digest(__file__)\n    report['protocol_sha256'] = digest(args.protocol)\n    with Path(args.report).open('x', encoding='utf-8', newline='\\n') as output:\n        json.dump(report, output, indent=2, allow_nan=False)\n        output.write('\\n')\n    if not report['valid']:\n        print('CAPTURE CHECK FAILED: ' + '; '.join(report['failures']))\n        return 1\n    print(f\"SAVED: {report['condition']} / {report['client']} / repetition {report['repetition']}; \"\n          f\"{report['frame_ms']['count']} frames; {report['duration_seconds']:.1f}s\")\n    return 0\n\n\nif __name__ == '__main__':\n    raise SystemExit(main())\n"}
{"kind":"protocol","sha256":"a44567fe3b3dc949fd78092fd3870edcbaf47fe2a5a19f68434e7f4e0e64a4f3","utf8_text":"{\n  \"protocol\": \"m16-b1-ovl-v1\",\n  \"authority\": \"Board 5654699942; OVL addendum 5653843173; DA 5651557467\",\n  \"code_revision\": \"a8c117313dbe3796aa160ba12cad1cd677bdc5e6\",\n  \"instrument\": \"m16-b0a-v1\",\n  \"expected_source_identity\": \"fnv1a64:ee4e4df9e8c9fbd9:5798\",\n  \"expected_profile_identity\": \"fnv1a64:bfcbc44323b304bf:23530\",\n  \"expected_dependencies\": {\n    \"stellaristhing_base.base.json\": \"fnv1a64:c49f9ca3c8c75e77:20370\",\n    \"stellaristhing_base.dependencies.json\": \"fnv1a64:2f064bfb3e043aa0:72\"\n  },\n  \"source_path\": \"C:/Users/mvorm/SimThing-0088-m16-ui-comparison/scenarios/stellaristhing_base.clause\",\n  \"workload_cardinalities\": \"Unchanged Meridian: 7 systems, 6 links, 2 owners, 2 sites, 4 cohorts at each site, 13 energy RF participants, 2 recipes, 4 material loci. No stress variant.\",\n  \"repetitions\": 3,\n  \"pair_order\": [[\"Egui\", \"Native\"], [\"Native\", \"Egui\"], [\"Egui\", \"Native\"]],\n  \"target_capture_seconds\": 15,\n  \"minimum_capture_seconds\": 10,\n  \"warmup\": \"Owner waits at least five seconds after load/client/condition setup, before F9; operator-qualified, not independently timestamped. The B0a collector excludes the first interval straddling F9.\",\n  \"window\": \"Existing Windowed setting, 1600x900 logical startup size, copied presentation settings. OS maximize/restore is used only during resize captures. The runtime records physical size and scale; paired geometry must be checked.\",\n  \"groups\": [\n    {\n      \"id\": \"paused\", \"title\": \"Paused steady pane\",\n      \"prepare\": \"Load the source once. Leave the simulation paused at generation 0, rate 1x, Max TPS 10. Keep Sim clock transport expanded. Select the requested client with F8. Wait five seconds.\",\n      \"action\": \"Press F9, leave the map and pane visible for at least 15 seconds without interaction, then press F10.\",\n      \"sequence\": \"Pinned Meridian G0; paused/1x/TPS10; no input during capture.\"\n    },\n    {\n      \"id\": \"numeric_edit\", \"title\": \"Active numeric editing\",\n      \"prepare\": \"Load the source once. Stay paused at G0, rate 1x, Max TPS 10. Select the requested client with F8. Use that client's Max TPS field. Wait five seconds.\",\n      \"action\": \"Press F9. In Max TPS, Ctrl+A, type 12.5, Enter; then Ctrl+A, type 10, Enter. Repeat this pair three times, pausing at least one second after each Enter. After at least 15 seconds total, press F10. End at TPS 10.\",\n      \"sequence\": \"Paused G0; three exact shared-owner Apply TPS pairs 12.5 -> 10. Bounded numeric editing only; not general text accessibility.\"\n    },\n    {\n      \"id\": \"resize\", \"title\": \"Window maximize and restore\",\n      \"prepare\": \"Load the source once and stay paused at G0/TPS10. Select the requested client. The window must be at its normal startup size, not fullscreen. Release numeric/native focus with Escape. Wait five seconds.\",\n      \"action\": \"Press F9. Press Win+Up to maximize, wait two seconds, then Win+Down once to restore, wait two seconds. Repeat three times. Keep Studio visible; after at least 15 seconds total press F10. End at the starting size.\",\n      \"sequence\": \"Paused G0; three existing OS maximize -> restore cycles. Full raw resize frames retained; qualification is this physical cycle only, not arbitrary drag or multi-monitor DPI.\",\n      \"reference\": \"https://support.microsoft.com/en-us/windows/hardware/input-devices/windows-keyboard-tips-and-tricks\"\n    },\n    {\n      \"id\": \"list_interaction\", \"title\": \"Seven-system selection and native list\",\n      \"prepare\": \"Load the source once and stay paused at G0/TPS10. Use the Overhead (O) button so system labels can be located; keep the same camera for both clients. Select the requested client, release field focus and clear selection with separate Escape taps. Wait five seconds.\",\n      \"action\": \"Press F9. Select systems A through G (IDs 1 through 7) in order, with about two seconds between selections. Egui: click the corresponding map systems. Native: click row A, then press Down six times, allowing the three-row list to scroll. After at least 15 seconds total press F10. If a system cannot be identified or Down does not work, stop and report FAIL; do not improvise a different sequence.\",\n      \"sequence\": \"Same shared selection sequence 1,2,3,4,5,6,7 on the same seven-system workload. Map picking and native list are different adapters; no equal-widget-cost claim. Seven-row interaction is not long-list scaling proof.\"\n    },\n    {\n      \"id\": \"running\", \"title\": \"Running steady pane\",\n      \"prepare\": \"Before EACH capture, reload the same Clause source through Library to get a fresh G0 resident. Select the requested client. Set rate 1x and TPS10. Click that client's Play, wait five seconds, then capture.\",\n      \"action\": \"Press F9. Leave it running with no other input for at least 15 seconds. Press F10, wait for the saved confirmation, then Pause. Reload the source before the next capture. Do not change TPS, selection or window while recording.\",\n      \"sequence\": \"Each repetition: same source re-admission at G0, Play at 1x/TPS10, five-second operator warmup, at least 15 seconds observed running, F10, Pause. Actual resident generations and capture duration are retained; manual start phase is a limitation.\"\n    }\n  ],\n  \"statistics\": {\n    \"quantile\": \"Nearest rank: sorted[ceil(p*N)-1], predeclared p50/p95/p99 plus min/max/mean/count; no outlier removal or clipping.\",\n    \"frame\": \"B0a Time<Real> unsmoothed main-loop cadence; not scanout or compositor delivery. p95 characterized at 16.7 ms.\",\n    \"cpu\": \"shared_clock_and_observation_projection is the same computation. Each other adapter scope is reported separately. Nested scopes are not summed; coexistence includes the egui pane when Native is enabled.\",\n    \"freshness\": \"Publication stamp to first matching consumption, with full scene/resident/generation identity. Pre-capture publication ages retained separately; they are not steady publication-delivery samples. Same-publication Egui/Native differences retain negative values.\",\n    \"memory\": \"One read-only PID private-bytes/working-set snapshot immediately after each completed raw export, before analysis; UTC/start time/lag retained. Process total after instrumentation/export, not GPU or UI-only memory.\",\n    \"paired_residuals\": \"Native minus Egui by condition/repetition for capture statistics and memory snapshots. Same-publication freshness and, where provable, same-frame shared projection use direct sample pairs. Chronological frame-ordinal residuals are descriptive cross-run pairs only, with all unmatched tails retained; never a latency or causal frame-identity join.\"\n  },\n  \"unavailable\": {\n    \"input_latency\": \"INSTRUMENT-INVALID / UNAVAILABLE; p95 <=100 ms UNEVALUATED; B0b containment and R1 successor-window failure packets carried forward unchanged.\",\n    \"first_display\": \"UNAVAILABLE: no qualified matching DISPLAYED endpoint.\",\n    \"direct_render_gpu_cost\": \"UNAVAILABLE: no directly admitted qualified per-client timing instrument.\",\n    \"simulation_vs_presentation_gpu_memory\": \"UNAVAILABLE: no direct separately measured allocation pair.\",\n    \"long_list_and_multi_monitor_dpi\": \"UNQUALIFIED by this seven-system, single-monitor protocol; retain Leaf A limits.\"\n  },\n  \"decision_constraint\": \"Input latency is required and unavailable. No positive migration recommendation is permitted on this packet; RETAIN-EGUI will cite the measured results and remaining uncertainty after collection.\"\n}\n"}
{"kind":"protocol","sha256":"eca99546e881053c518e12161f005408e0a92a1e461acf512b608a5b3d062bd8","utf8_text":"{\n  \"protocol\": \"m16-b1-ovl-v2\",\n  \"revision_note\": \"Instructions only: use the explicit checkbox state, identify F9 as silent and keep Studio focused until F10. Frozen executable, analyzer, quantities, operation sequences and all acceptance rules unchanged. v1 Owner failures remain rejected and preserved.\",\n  \"authority\": \"Board 5654699942; OVL addendum 5653843173; DA 5651557467\",\n  \"code_revision\": \"a8c117313dbe3796aa160ba12cad1cd677bdc5e6\",\n  \"instrument\": \"m16-b0a-v1\",\n  \"expected_source_identity\": \"fnv1a64:ee4e4df9e8c9fbd9:5798\",\n  \"expected_profile_identity\": \"fnv1a64:bfcbc44323b304bf:23530\",\n  \"expected_dependencies\": {\n    \"stellaristhing_base.base.json\": \"fnv1a64:c49f9ca3c8c75e77:20370\",\n    \"stellaristhing_base.dependencies.json\": \"fnv1a64:2f064bfb3e043aa0:72\"\n  },\n  \"source_path\": \"C:/Users/mvorm/SimThing-0088-m16-ui-comparison/scenarios/stellaristhing_base.clause\",\n  \"workload_cardinalities\": \"Unchanged Meridian: 7 systems, 6 links, 2 owners, 2 sites, 4 cohorts at each site, 13 energy RF participants, 2 recipes, 4 material loci. No stress variant.\",\n  \"repetitions\": 3,\n  \"pair_order\": [[\"Egui\", \"Native\"], [\"Native\", \"Egui\"], [\"Egui\", \"Native\"]],\n  \"target_capture_seconds\": 15,\n  \"minimum_capture_seconds\": 10,\n  \"warmup\": \"Owner waits at least five seconds after load/client/condition setup, before F9; operator-qualified, not independently timestamped. The B0a collector excludes the first interval straddling F9.\",\n  \"window\": \"Existing Windowed setting, 1600x900 logical startup size, copied presentation settings. OS maximize/restore is used only during resize captures. The runtime records physical size and scale; paired geometry must be checked.\",\n  \"groups\": [\n    {\n      \"id\": \"paused\", \"title\": \"Paused steady pane\",\n      \"prepare\": \"Load the source once. Leave the simulation paused at generation 0, rate 1x, Max TPS 10. Keep Sim clock transport expanded. Set Native checkbox to the card's explicit state with the mouse; leave it alone if already correct. Keep Studio focused and wait five seconds.\",\n      \"action\": \"Press F9, leave the map and pane visible for at least 15 seconds without interaction, then press F10.\",\n      \"sequence\": \"Pinned Meridian G0; paused/1x/TPS10; no input during capture.\"\n    },\n    {\n      \"id\": \"numeric_edit\", \"title\": \"Active numeric editing\",\n      \"prepare\": \"Load the source once. Stay paused at G0, rate 1x, Max TPS 10. Set Native checkbox to the card's explicit state with the mouse; leave it alone if already correct. Use that client's Max TPS field. Keep Studio focused and wait five seconds.\",\n      \"action\": \"Press F9. In Max TPS, Ctrl+A, type 12.5, Enter; then Ctrl+A, type 10, Enter. Repeat this pair three times, pausing at least one second after each Enter. After at least 15 seconds total, press F10. End at TPS 10.\",\n      \"sequence\": \"Paused G0; three exact shared-owner Apply TPS pairs 12.5 -> 10. Bounded numeric editing only; not general text accessibility.\"\n    },\n    {\n      \"id\": \"resize\", \"title\": \"Window maximize and restore\",\n      \"prepare\": \"Load the source once and stay paused at G0/TPS10. Select the requested client. The window must be at its normal startup size, not fullscreen. Release numeric/native focus with Escape. Wait five seconds.\",\n      \"action\": \"Press F9. Press Win+Up to maximize, wait two seconds, then Win+Down once to restore, wait two seconds. Repeat three times. Keep Studio visible; after at least 15 seconds total press F10. End at the starting size.\",\n      \"sequence\": \"Paused G0; three existing OS maximize -> restore cycles. Full raw resize frames retained; qualification is this physical cycle only, not arbitrary drag or multi-monitor DPI.\",\n      \"reference\": \"https://support.microsoft.com/en-us/windows/hardware/input-devices/windows-keyboard-tips-and-tricks\"\n    },\n    {\n      \"id\": \"list_interaction\", \"title\": \"Seven-system selection and native list\",\n      \"prepare\": \"Load the source once and stay paused at G0/TPS10. Use the Overhead (O) button so system labels can be located; keep the same camera for both clients. Select the requested client, release field focus and clear selection with separate Escape taps. Wait five seconds.\",\n      \"action\": \"Press F9. Select systems A through G (IDs 1 through 7) in order, with about two seconds between selections. Egui: click the corresponding map systems. Native: click row A, then press Down six times, allowing the three-row list to scroll. After at least 15 seconds total press F10. If a system cannot be identified or Down does not work, stop and report FAIL; do not improvise a different sequence.\",\n      \"sequence\": \"Same shared selection sequence 1,2,3,4,5,6,7 on the same seven-system workload. Map picking and native list are different adapters; no equal-widget-cost claim. Seven-row interaction is not long-list scaling proof.\"\n    },\n    {\n      \"id\": \"running\", \"title\": \"Running steady pane\",\n      \"prepare\": \"Before EACH capture, reload the same Clause source through Library to get a fresh G0 resident. Select the requested client. Set rate 1x and TPS10. Click that client's Play, wait five seconds, then capture.\",\n      \"action\": \"Press F9. Leave it running with no other input for at least 15 seconds. Press F10, wait for the saved confirmation, then Pause. Reload the source before the next capture. Do not change TPS, selection or window while recording.\",\n      \"sequence\": \"Each repetition: same source re-admission at G0, Play at 1x/TPS10, five-second operator warmup, at least 15 seconds observed running, F10, Pause. Actual resident generations and capture duration are retained; manual start phase is a limitation.\"\n    }\n  ],\n  \"statistics\": {\n    \"quantile\": \"Nearest rank: sorted[ceil(p*N)-1], predeclared p50/p95/p99 plus min/max/mean/count; no outlier removal or clipping.\",\n    \"frame\": \"B0a Time<Real> unsmoothed main-loop cadence; not scanout or compositor delivery. p95 characterized at 16.7 ms.\",\n    \"cpu\": \"shared_clock_and_observation_projection is the same computation. Each other adapter scope is reported separately. Nested scopes are not summed; coexistence includes the egui pane when Native is enabled.\",\n    \"freshness\": \"Publication stamp to first matching consumption, with full scene/resident/generation identity. Pre-capture publication ages retained separately; they are not steady publication-delivery samples. Same-publication Egui/Native differences retain negative values.\",\n    \"memory\": \"One read-only PID private-bytes/working-set snapshot immediately after each completed raw export, before analysis; UTC/start time/lag retained. Process total after instrumentation/export, not GPU or UI-only memory.\",\n    \"paired_residuals\": \"Native minus Egui by condition/repetition for capture statistics and memory snapshots. Same-publication freshness and, where provable, same-frame shared projection use direct sample pairs. Chronological frame-ordinal residuals are descriptive cross-run pairs only, with all unmatched tails retained; never a latency or causal frame-identity join.\"\n  },\n  \"unavailable\": {\n    \"input_latency\": \"INSTRUMENT-INVALID / UNAVAILABLE; p95 <=100 ms UNEVALUATED; B0b containment and R1 successor-window failure packets carried forward unchanged.\",\n    \"first_display\": \"UNAVAILABLE: no qualified matching DISPLAYED endpoint.\",\n    \"direct_render_gpu_cost\": \"UNAVAILABLE: no directly admitted qualified per-client timing instrument.\",\n    \"simulation_vs_presentation_gpu_memory\": \"UNAVAILABLE: no direct separately measured allocation pair.\",\n    \"long_list_and_multi_monitor_dpi\": \"UNQUALIFIED by this seven-system, single-monitor protocol; retain Leaf A limits.\"\n  },\n  \"decision_constraint\": \"Input latency is required and unavailable. No positive migration recommendation is permitted on this packet; RETAIN-EGUI will cite the measured results and remaining uncertainty after collection.\"\n}\n"}
{"kind":"protocol","sha256":"d724c3ee3ca8e022bb76f2a9508220f059536aeb473b6c51cec4e13f30518e3d","utf8_text":"{\n  \"protocol\": \"m16-b1-ovl-v3\",\n  \"revision_note\": \"Instructions only: explicitly refocus the numeric field before each of the six existing edits, verify the applied readout after Enter, and load once per group except Running. Frozen executable, analyzer, quantities, semantic operation sequences and acceptance rules unchanged. Prior captures retain their original protocol and qualification.\",\n  \"authority\": \"Board 5654699942; OVL addendum 5653843173; DA 5651557467\",\n  \"code_revision\": \"a8c117313dbe3796aa160ba12cad1cd677bdc5e6\",\n  \"instrument\": \"m16-b0a-v1\",\n  \"expected_source_identity\": \"fnv1a64:ee4e4df9e8c9fbd9:5798\",\n  \"expected_profile_identity\": \"fnv1a64:bfcbc44323b304bf:23530\",\n  \"expected_dependencies\": {\n    \"stellaristhing_base.base.json\": \"fnv1a64:c49f9ca3c8c75e77:20370\",\n    \"stellaristhing_base.dependencies.json\": \"fnv1a64:2f064bfb3e043aa0:72\"\n  },\n  \"source_path\": \"C:/Users/mvorm/SimThing-0088-m16-ui-comparison/scenarios/stellaristhing_base.clause\",\n  \"workload_cardinalities\": \"Unchanged Meridian: 7 systems, 6 links, 2 owners, 2 sites, 4 cohorts at each site, 13 energy RF participants, 2 recipes, 4 material loci. No stress variant.\",\n  \"repetitions\": 3,\n  \"pair_order\": [[\"Egui\", \"Native\"], [\"Native\", \"Egui\"], [\"Egui\", \"Native\"]],\n  \"target_capture_seconds\": 15,\n  \"minimum_capture_seconds\": 10,\n  \"warmup\": \"Owner waits at least five seconds after load/client/condition setup, before F9; operator-qualified, not independently timestamped. The B0a collector excludes the first interval straddling F9.\",\n  \"window\": \"Existing Windowed setting, 1600x900 logical startup size, copied presentation settings. OS maximize/restore is used only during resize captures. The runtime records physical size and scale; paired geometry must be checked.\",\n  \"groups\": [\n    {\n      \"id\": \"paused\", \"title\": \"Paused steady pane\",\n      \"prepare\": \"Load the source once at group start; keep it loaded for all six captures. Leave the simulation paused at generation 0, rate 1x, Max TPS 10. Keep Sim clock transport expanded. Set Native checkbox to the card's explicit state with the mouse; leave it alone if already correct. Keep Studio focused and wait five seconds.\",\n      \"action\": \"Press F9, leave the map and pane visible for at least 15 seconds without interaction, then press F10.\",\n      \"sequence\": \"Pinned Meridian G0; paused/1x/TPS10; no input during capture.\"\n    },\n    {\n      \"id\": \"numeric_edit\", \"title\": \"Active numeric editing\",\n      \"prepare\": \"Load the source once at group start; keep it loaded for all six captures. Stay paused at G0, rate 1x, applied Max TPS 10. Set Native checkbox to the card's explicit state with the mouse; leave it alone if already correct. Use that client's editable Max TPS box. Keep Studio focused and wait five seconds.\",\n      \"action\": \"Press F9 ONCE, then do exactly SIX edits: 12.5, 10, 12.5, 10, 12.5, 10. For EACH value: click INSIDE the editable Max TPS box, Ctrl+A, type the value, check the box, press Enter. Check the STATUS BELOW shows applied TPS 12.500 or 10.000, then wait at least one second. Click back into the box before EVERY value: Egui Enter releases field focus. If the draft or status does not change, F10 and take a Studio screenshot showing both; report FAIL. After edit six and at least 15 seconds since F9, press F10. Keep Studio focused; do not add extra edits.\",\n      \"sequence\": \"Paused G0; three exact shared-owner Apply TPS pairs 12.5 -> 10. Bounded numeric editing only; not general text accessibility.\"\n    },\n    {\n      \"id\": \"resize\", \"title\": \"Window maximize and restore\",\n      \"prepare\": \"Load the source once at group start; keep it loaded for all six captures and stay paused at G0/TPS10. Select the requested client. The window must be at its normal startup size, not fullscreen. Release numeric/native focus with Escape. Wait five seconds.\",\n      \"action\": \"Press F9. Press Win+Up to maximize, wait two seconds, then Win+Down once to restore, wait two seconds. Repeat three times. Keep Studio visible; after at least 15 seconds total press F10. End at the starting size.\",\n      \"sequence\": \"Paused G0; three existing OS maximize -> restore cycles. Full raw resize frames retained; qualification is this physical cycle only, not arbitrary drag or multi-monitor DPI.\",\n      \"reference\": \"https://support.microsoft.com/en-us/windows/hardware/input-devices/windows-keyboard-tips-and-tricks\"\n    },\n    {\n      \"id\": \"list_interaction\", \"title\": \"Seven-system selection and native list\",\n      \"prepare\": \"Load the source once at group start; keep it loaded for all six captures and stay paused at G0/TPS10. Use the Overhead (O) button so system labels can be located; keep the same camera for both clients. Select the requested client, release field focus and clear selection with separate Escape taps. Wait five seconds.\",\n      \"action\": \"Press F9. Select systems A through G (IDs 1 through 7) in order, with about two seconds between selections. Egui: click the corresponding map systems. Native: click row A, then press Down six times, allowing the three-row list to scroll. After at least 15 seconds total press F10. If a system cannot be identified or Down does not work, stop and report FAIL; do not improvise a different sequence.\",\n      \"sequence\": \"Same shared selection sequence 1,2,3,4,5,6,7 on the same seven-system workload. Map picking and native list are different adapters; no equal-widget-cost claim. Seven-row interaction is not long-list scaling proof.\"\n    },\n    {\n      \"id\": \"running\", \"title\": \"Running steady pane\",\n      \"prepare\": \"Before EACH capture, reload the same Clause source through Library to get a fresh G0 resident. Select the requested client. Set rate 1x and TPS10. Click that client's Play, wait five seconds, then capture.\",\n      \"action\": \"Press F9. Leave it running with no other input for at least 15 seconds. Press F10, wait for the saved confirmation, then Pause. Reload the source before the next capture. Do not change TPS, selection or window while recording.\",\n      \"sequence\": \"Each repetition: same source re-admission at G0, Play at 1x/TPS10, five-second operator warmup, at least 15 seconds observed running, F10, Pause. Actual resident generations and capture duration are retained; manual start phase is a limitation.\"\n    }\n  ],\n  \"statistics\": {\n    \"quantile\": \"Nearest rank: sorted[ceil(p*N)-1], predeclared p50/p95/p99 plus min/max/mean/count; no outlier removal or clipping.\",\n    \"frame\": \"B0a Time<Real> unsmoothed main-loop cadence; not scanout or compositor delivery. p95 characterized at 16.7 ms.\",\n    \"cpu\": \"shared_clock_and_observation_projection is the same computation. Each other adapter scope is reported separately. Nested scopes are not summed; coexistence includes the egui pane when Native is enabled.\",\n    \"freshness\": \"Publication stamp to first matching consumption, with full scene/resident/generation identity. Pre-capture publication ages retained separately; they are not steady publication-delivery samples. Same-publication Egui/Native differences retain negative values.\",\n    \"memory\": \"One read-only PID private-bytes/working-set snapshot immediately after each completed raw export, before analysis; UTC/start time/lag retained. Process total after instrumentation/export, not GPU or UI-only memory.\",\n    \"paired_residuals\": \"Native minus Egui by condition/repetition for capture statistics and memory snapshots. Same-publication freshness and, where provable, same-frame shared projection use direct sample pairs. Chronological frame-ordinal residuals are descriptive cross-run pairs only, with all unmatched tails retained; never a latency or causal frame-identity join.\"\n  },\n  \"unavailable\": {\n    \"input_latency\": \"INSTRUMENT-INVALID / UNAVAILABLE; p95 <=100 ms UNEVALUATED; B0b containment and R1 successor-window failure packets carried forward unchanged.\",\n    \"first_display\": \"UNAVAILABLE: no qualified matching DISPLAYED endpoint.\",\n    \"direct_render_gpu_cost\": \"UNAVAILABLE: no directly admitted qualified per-client timing instrument.\",\n    \"simulation_vs_presentation_gpu_memory\": \"UNAVAILABLE: no direct separately measured allocation pair.\",\n    \"long_list_and_multi_monitor_dpi\": \"UNQUALIFIED by this seven-system, single-monitor protocol; retain Leaf A limits.\"\n  },\n  \"decision_constraint\": \"Input latency is required and unavailable. No positive migration recommendation is permitted on this packet; RETAIN-EGUI will cite the measured results and remaining uncertainty after collection.\"\n}\n"}
{"kind":"protocol","sha256":"e9fe47fdc20804cdff2028241e98f085164a02c64cfcc7f8890ad940f251b81d","utf8_text":"{\r\n  \"protocol\": \"m16-b1-ovl-v4\",\r\n  \"revision_note\": \"Instructions only: resize requires exactly three cycles TOTAL, counting restores 1,2,3, then idle at the initial size until F10. Clarifies the ambiguous Repeat three times wording. Frozen executable, launcher, analyzer, quantities, semantic sequences and acceptance rules unchanged; all prior captures retain their original results and qualification.\",\r\n  \"authority\": \"Board 5654699942; OVL addendum 5653843173; DA 5651557467\",\r\n  \"code_revision\": \"a8c117313dbe3796aa160ba12cad1cd677bdc5e6\",\r\n  \"instrument\": \"m16-b0a-v1\",\r\n  \"expected_source_identity\": \"fnv1a64:ee4e4df9e8c9fbd9:5798\",\r\n  \"expected_profile_identity\": \"fnv1a64:bfcbc44323b304bf:23530\",\r\n  \"expected_dependencies\": {\r\n    \"stellaristhing_base.base.json\": \"fnv1a64:c49f9ca3c8c75e77:20370\",\r\n    \"stellaristhing_base.dependencies.json\": \"fnv1a64:2f064bfb3e043aa0:72\"\r\n  },\r\n  \"source_path\": \"C:/Users/mvorm/SimThing-0088-m16-ui-comparison/scenarios/stellaristhing_base.clause\",\r\n  \"workload_cardinalities\": \"Unchanged Meridian: 7 systems, 6 links, 2 owners, 2 sites, 4 cohorts at each site, 13 energy RF participants, 2 recipes, 4 material loci. No stress variant.\",\r\n  \"repetitions\": 3,\r\n  \"pair_order\": [\r\n    [\r\n      \"Egui\",\r\n      \"Native\"\r\n    ],\r\n    [\r\n      \"Native\",\r\n      \"Egui\"\r\n    ],\r\n    [\r\n      \"Egui\",\r\n      \"Native\"\r\n    ]\r\n  ],\r\n  \"target_capture_seconds\": 15,\r\n  \"minimum_capture_seconds\": 10,\r\n  \"warmup\": \"Owner waits at least five seconds after load/client/condition setup, before F9; operator-qualified, not independently timestamped. The B0a collector excludes the first interval straddling F9.\",\r\n  \"window\": \"Existing Windowed setting, 1600x900 logical startup size, copied presentation settings. OS maximize/restore is used only during resize captures. The runtime records physical size and scale; paired geometry must be checked.\",\r\n  \"groups\": [\r\n    {\r\n      \"id\": \"paused\",\r\n      \"title\": \"Paused steady pane\",\r\n      \"prepare\": \"Load the source once at group start; keep it loaded for all six captures. Leave the simulation paused at generation 0, rate 1x, Max TPS 10. Keep Sim clock transport expanded. Set Native checkbox to the card's explicit state with the mouse; leave it alone if already correct. Keep Studio focused and wait five seconds.\",\r\n      \"action\": \"Press F9, leave the map and pane visible for at least 15 seconds without interaction, then press F10.\",\r\n      \"sequence\": \"Pinned Meridian G0; paused/1x/TPS10; no input during capture.\"\r\n    },\r\n    {\r\n      \"id\": \"numeric_edit\",\r\n      \"title\": \"Active numeric editing\",\r\n      \"prepare\": \"Load the source once at group start; keep it loaded for all six captures. Stay paused at G0, rate 1x, applied Max TPS 10. Set Native checkbox to the card's explicit state with the mouse; leave it alone if already correct. Use that client's editable Max TPS box. Keep Studio focused and wait five seconds.\",\r\n      \"action\": \"Press F9 ONCE, then do exactly SIX edits: 12.5, 10, 12.5, 10, 12.5, 10. For EACH value: click INSIDE the editable Max TPS box, Ctrl+A, type the value, check the box, press Enter. Check the STATUS BELOW shows applied TPS 12.500 or 10.000, then wait at least one second. Click back into the box before EVERY value: Egui Enter releases field focus. If the draft or status does not change, F10 and take a Studio screenshot showing both; report FAIL. After edit six and at least 15 seconds since F9, press F10. Keep Studio focused; do not add extra edits.\",\r\n      \"sequence\": \"Paused G0; three exact shared-owner Apply TPS pairs 12.5 -> 10. Bounded numeric editing only; not general text accessibility.\"\r\n    },\r\n    {\r\n      \"id\": \"resize\",\r\n      \"title\": \"Window maximize and restore\",\r\n      \"prepare\": \"Load the source once at group start; keep it loaded for all six captures and stay paused at G0/TPS10. Select the requested client. The window must be at its normal startup size, not fullscreen. Release numeric/native focus with Escape. Wait five seconds.\",\r\n      \"action\": \"Press F9 ONCE. Do exactly THREE maximize/restore cycles TOTAL. For each cycle: Win+Up once, wait two seconds; Win+Down once, wait two seconds. Count the restores: 1, 2, 3. After the THIRD restore, STOP resizing and remain at the starting size. If less than 15 seconds has elapsed since F9, wait idle at that size. Then press F10 ONCE. Keep Studio focused throughout.\",\r\n      \"sequence\": \"Paused G0; three existing OS maximize -> restore cycles. Full raw resize frames retained; qualification is this physical cycle only, not arbitrary drag or multi-monitor DPI.\",\r\n      \"reference\": \"https://support.microsoft.com/en-us/windows/hardware/input-devices/windows-keyboard-tips-and-tricks\"\r\n    },\r\n    {\r\n      \"id\": \"list_interaction\",\r\n      \"title\": \"Seven-system selection and native list\",\r\n      \"prepare\": \"Load the source once at group start; keep it loaded for all six captures and stay paused at G0/TPS10. Use the Overhead (O) button so system labels can be located; keep the same camera for both clients. Select the requested client, release field focus and clear selection with separate Escape taps. Wait five seconds.\",\r\n      \"action\": \"Press F9. Select systems A through G (IDs 1 through 7) in order, with about two seconds between selections. Egui: click the corresponding map systems. Native: click row A, then press Down six times, allowing the three-row list to scroll. After at least 15 seconds total press F10. If a system cannot be identified or Down does not work, stop and report FAIL; do not improvise a different sequence.\",\r\n      \"sequence\": \"Same shared selection sequence 1,2,3,4,5,6,7 on the same seven-system workload. Map picking and native list are different adapters; no equal-widget-cost claim. Seven-row interaction is not long-list scaling proof.\"\r\n    },\r\n    {\r\n      \"id\": \"running\",\r\n      \"title\": \"Running steady pane\",\r\n      \"prepare\": \"Before EACH capture, reload the same Clause source through Library to get a fresh G0 resident. Select the requested client. Set rate 1x and TPS10. Click that client's Play, wait five seconds, then capture.\",\r\n      \"action\": \"Press F9. Leave it running with no other input for at least 15 seconds. Press F10, wait for the saved confirmation, then Pause. Reload the source before the next capture. Do not change TPS, selection or window while recording.\",\r\n      \"sequence\": \"Each repetition: same source re-admission at G0, Play at 1x/TPS10, five-second operator warmup, at least 15 seconds observed running, F10, Pause. Actual resident generations and capture duration are retained; manual start phase is a limitation.\"\r\n    }\r\n  ],\r\n  \"statistics\": {\r\n    \"quantile\": \"Nearest rank: sorted[ceil(p*N)-1], predeclared p50/p95/p99 plus min/max/mean/count; no outlier removal or clipping.\",\r\n    \"frame\": \"B0a Time<Real> unsmoothed main-loop cadence; not scanout or compositor delivery. p95 characterized at 16.7 ms.\",\r\n    \"cpu\": \"shared_clock_and_observation_projection is the same computation. Each other adapter scope is reported separately. Nested scopes are not summed; coexistence includes the egui pane when Native is enabled.\",\r\n    \"freshness\": \"Publication stamp to first matching consumption, with full scene/resident/generation identity. Pre-capture publication ages retained separately; they are not steady publication-delivery samples. Same-publication Egui/Native differences retain negative values.\",\r\n    \"memory\": \"One read-only PID private-bytes/working-set snapshot immediately after each completed raw export, before analysis; UTC/start time/lag retained. Process total after instrumentation/export, not GPU or UI-only memory.\",\r\n    \"paired_residuals\": \"Native minus Egui by condition/repetition for capture statistics and memory snapshots. Same-publication freshness and, where provable, same-frame shared projection use direct sample pairs. Chronological frame-ordinal residuals are descriptive cross-run pairs only, with all unmatched tails retained; never a latency or causal frame-identity join.\"\r\n  },\r\n  \"unavailable\": {\r\n    \"input_latency\": \"INSTRUMENT-INVALID / UNAVAILABLE; p95 <=100 ms UNEVALUATED; B0b containment and R1 successor-window failure packets carried forward unchanged.\",\r\n    \"first_display\": \"UNAVAILABLE: no qualified matching DISPLAYED endpoint.\",\r\n    \"direct_render_gpu_cost\": \"UNAVAILABLE: no directly admitted qualified per-client timing instrument.\",\r\n    \"simulation_vs_presentation_gpu_memory\": \"UNAVAILABLE: no direct separately measured allocation pair.\",\r\n    \"long_list_and_multi_monitor_dpi\": \"UNQUALIFIED by this seven-system, single-monitor protocol; retain Leaf A limits.\"\r\n  },\r\n  \"decision_constraint\": \"Input latency is required and unavailable. No positive migration recommendation is permitted on this packet; RETAIN-EGUI will cite the measured results and remaining uncertainty after collection.\"\r\n}\r\n"}
{"kind":"analyzer","sha256":"e829eb2d3ac954fa24d9b7789f4ccd62a60a287fe79b29af03674ebabf03402a","utf8_text":"\"\"\"External B1 evidence analysis; standard library only; no app/render authority.\"\"\"\r\nimport argparse\r\nimport bisect\r\nfrom collections import defaultdict\r\nimport hashlib\r\nimport json\r\nimport math\r\nfrom pathlib import Path\r\nimport statistics\r\n\r\n\r\ndef load(path):\r\n    return json.loads(Path(path).read_text(encoding='utf-8-sig'))\r\n\r\n\r\ndef digest(path):\r\n    return hashlib.sha256(Path(path).read_bytes()).hexdigest()\r\n\r\n\r\ndef stats(values):\r\n    if not values:\r\n        return {'count': 0, 'status': 'UNAVAILABLE'}\r\n    ordered = sorted(values)\r\n    return {'count': len(values), 'min': ordered[0], 'max': ordered[-1],\r\n            'mean': statistics.mean(values),\r\n            **{f'p{p}': ordered[math.ceil(p * len(ordered) / 100) - 1] for p in (50, 95, 99)}}\r\n\r\n\r\ndef unique_changes(values):\r\n    result = []\r\n    for value in values:\r\n        if not result or value != result[-1]:\r\n            result.append(value)\r\n    return result\r\n\r\n\r\ndef stamp_key(stamp):\r\n    return tuple(stamp[key] for key in ('scene', 'resident_epoch', 'generation', 'published_ns'))\r\n\r\n\r\ndef analyze(capture, protocol):\r\n    failures = []\r\n    meta = capture['config']['metadata']\r\n    group = meta['condition']\r\n    expected_client = meta['client_condition']\r\n    start, stop = capture['started_ns'], capture['stopped_ns']\r\n    if capture.get('instrument') != 'm16-b0a-v1':\r\n        failures.append('Wrong capture instrument')\r\n    if meta.get('code_revision') != protocol['code_revision']:\r\n        failures.append('Wrong code revision')\r\n    if capture.get('stop_reason') != 'operator stop' or stop is None or stop <= start:\r\n        failures.append('Capture did not finish with a valid operator stop')\r\n        return {'valid': False, 'failures': failures}\r\n    duration = (stop - start) / 1e9\r\n    if duration < protocol['minimum_capture_seconds']:\r\n        failures.append(f'Capture was only {duration:.2f}s; at least 10s is required')\r\n    facts = [capture['initial_facts']] + [s['facts'] for s in capture['samples'] if s['kind'] == 'Runtime']\r\n    for fact in facts:\r\n        for key, value in [('source_identity', protocol['expected_source_identity']),\r\n                           ('profile_identity', protocol['expected_profile_identity']),\r\n                           ('dependencies', protocol['expected_dependencies']),\r\n                           ('systems', 7), ('reset_pending', False), ('panels_hidden', False),\r\n                           ('focused', True), ('native_enabled', expected_client == 'Native')]:\r\n            if fact.get(key) != value:\r\n                failures.append(f'Unexpected {key}; check source/client/focus/setup')\r\n        if fact.get('scene') != facts[0].get('scene') or fact.get('resident_epoch') != facts[0].get('resident_epoch'):\r\n            failures.append('Scene or resident changed while recording')\r\n        if fact.get('paused') != (group != 'running'):\r\n            failures.append('Unexpected pause/run state while recording')\r\n        if fact.get('rate') not in ('1\u00d7', '1x'):\r\n            failures.append('Clock rate must be 1x')\r\n        if group != 'numeric_edit' and fact.get('max_tps') != 10:\r\n            failures.append('Max TPS must remain 10 for this group')\r\n        if group != 'resize' and fact.get('window_pixels') != facts[0].get('window_pixels'):\r\n            failures.append('Window changed outside the resize group')\r\n        if fact.get('scale_factor') != facts[0].get('scale_factor'):\r\n            failures.append('DPI changed during this fixed-scale capture')\r\n    tps_sequence = unique_changes([f['max_tps'] for f in facts])\r\n    size_sequence = unique_changes([f['window_pixels'] for f in facts])\r\n    selection_sequence = unique_changes([f['selected_system'] for f in facts])\r\n    if group == 'numeric_edit' and tps_sequence != [10, 12.5, 10, 12.5, 10, 12.5, 10]:\r\n        failures.append('Numeric edits must record exactly three 12.5 -> 10 pairs')\r\n    if group == 'resize':\r\n        initial = size_sequence[0]\r\n        if len(size_sequence) != 7 or any(size_sequence[i] != initial for i in (2, 4, 6)):\r\n            failures.append('Resize must record three maximize/restore cycles ending at the initial size')\r\n        elif not (size_sequence[1] == size_sequence[3] == size_sequence[5] and size_sequence[1] != initial):\r\n            failures.append('Resize cycles did not reach the same second size')\r\n    if group == 'list_interaction':\r\n        list_protocol = next(g for g in protocol['groups'] if g['id'] == group)\r\n        if (protocol['protocol'] != 'm16-b1-ovl-v5'\r\n                or meta.get('protocol_revision') != protocol['protocol']\r\n                or meta.get('operation_sequence') != list_protocol['sequence']):\r\n            failures.append('List protocol mismatch; use the original run analyzer/protocol for old captures')\r\n        if selection_sequence != list_protocol.get('selection_ids'):\r\n            failures.append('Selection must start clear and visit IDs 1,2,3,4,5,7,6 in order')\r\n    if group not in [g['id'] for g in protocol['groups']]:\r\n        failures.append('Unknown condition')\r\n\r\n    frames = [s for s in capture['samples'] if s['kind'] == 'Frame']\r\n    if not frames:\r\n        failures.append('No raw frame cadence samples were recorded')\r\n    cpu = [s for s in capture['samples'] if s['kind'] == 'Cpu']\r\n    cpus = defaultdict(list)\r\n    for frame in frames:\r\n        if (frame['time_strategy'] != 'Automatic' or frame['end_ns'] - frame['start_ns'] != frame['delta_ns']\r\n                or frame['delta_ns'] <= 0 or frame['start_ns'] < start or frame['end_ns'] > stop):\r\n            failures.append('Invalid/synthetic/reversed frame interval')\r\n    if any(a['end_ns'] >= b['end_ns'] for a, b in zip(frames, frames[1:])):\r\n        failures.append('Non-monotonic/duplicate frame records')\r\n    for sample in cpu:\r\n        if sample['end_ns'] < sample['start_ns'] or not start <= sample['start_ns'] <= sample['end_ns'] <= stop:\r\n            failures.append('CPU interval outside capture or reversed')\r\n        cpus[f\"{sample['client']}:{sample['scope']}\"] .append(sample['end_ns'] - sample['start_ns'])\r\n    expected_scope = f'{expected_client}:shared_clock_and_observation_projection'\r\n    if not cpus.get(expected_scope):\r\n        failures.append('Expected client projection was not measured; keep its pane visible')\r\n    if expected_client == 'Egui' and cpus.get('Native:shared_clock_and_observation_projection'):\r\n        failures.append('Native projection appeared in the native-disabled baseline')\r\n    publications = {stamp_key(s['stamp']): s['stamp'] for s in capture['samples'] if s['kind'] == 'Publication'}\r\n    all_stamps = dict(publications)\r\n    consumptions = [s for s in capture['samples'] if s['kind'] == 'Consumption']\r\n    fresh = defaultdict(list)\r\n    pre_capture = defaultdict(list)\r\n    seen = set()\r\n    by_publication = defaultdict(dict)\r\n    for sample in consumptions:\r\n        stamp = sample['stamp']\r\n        key = stamp_key(stamp)\r\n        all_stamps[key] = stamp\r\n        token = (sample['client'], key)\r\n        age = sample['consumed_ns'] - stamp['published_ns']\r\n        if (age < 0 or stamp['scene'] != facts[0]['scene']\r\n                or stamp['resident_epoch'] != facts[0]['resident_epoch'] or token in seen\r\n                or not start <= sample['consumed_ns'] <= stop):\r\n            failures.append('Stale/reversed/duplicate consumption stamp')\r\n        seen.add(token)\r\n        if stamp['published_ns'] < start:\r\n            pre_capture[sample['client']].append({'stamp': stamp, 'age_ns': age, 'consumed_ns': sample['consumed_ns']})\r\n        else:\r\n            if key not in publications:\r\n                failures.append('In-capture consumption lacks matching Publication record')\r\n            fresh[sample['client']].append({'stamp': stamp, 'age_ns': age, 'consumed_ns': sample['consumed_ns']})\r\n            by_publication[key][sample['client']] = sample\r\n    fresh_pairs = [{'stamp': publications[key],\r\n                    'native_minus_egui_ns': pair['Native']['consumed_ns'] - pair['Egui']['consumed_ns']}\r\n                   for key, pair in by_publication.items() if 'Native' in pair and 'Egui' in pair]\r\n\r\n    # CPU intervals are paired only inside an actual recorded Frame interval,\r\n    # with one call per client and the same unchanged publication identity.\r\n    # Unmatched boundary calls are retained in the raw file and counted below.\r\n    stamped = sorted(all_stamps.values(), key=lambda s: s['published_ns'])\r\n    stamp_times = [s['published_ns'] for s in stamped]\r\n    frame_ends = [f['end_ns'] for f in frames]\r\n    projected = defaultdict(lambda: defaultdict(list))\r\n    unpaired_projection_calls = 0\r\n    for sample in cpu:\r\n        if sample['scope'] != 'shared_clock_and_observation_projection':\r\n            continue\r\n        index = bisect.bisect_left(frame_ends, sample['end_ns'])\r\n        before = bisect.bisect_right(stamp_times, sample['start_ns']) - 1\r\n        after = bisect.bisect_right(stamp_times, sample['end_ns']) - 1\r\n        if (index >= len(frames) or frames[index]['start_ns'] > sample['start_ns']\r\n                or before < 0 or before != after):\r\n            unpaired_projection_calls += 1\r\n            continue\r\n        projected[(index, stamp_key(stamped[before]))][sample['client']].append(sample)\r\n    cpu_pairs = []\r\n    runtime_times = sorted(s['at_ns'] for s in capture['samples'] if s['kind'] == 'Runtime')\r\n    for (index, key), pair in projected.items():\r\n        runtime_index = bisect.bisect_left(runtime_times, frames[index]['start_ns'])\r\n        runtime_changed = runtime_index < len(runtime_times) and runtime_times[runtime_index] <= frames[index]['end_ns']\r\n        if len(pair.get('Egui', [])) == len(pair.get('Native', [])) == 1 and not runtime_changed:\r\n            egui, native = pair['Egui'][0], pair['Native'][0]\r\n            cpu_pairs.append({'frame_index': index, 'frame_start_ns': frames[index]['start_ns'],\r\n                              'frame_end_ns': frames[index]['end_ns'], 'publication_key': list(key),\r\n                              'egui_start_ns': egui['start_ns'], 'egui_end_ns': egui['end_ns'],\r\n                              'native_start_ns': native['start_ns'], 'native_end_ns': native['end_ns'],\r\n                              'native_minus_egui_ns': (native['end_ns'] - native['start_ns']) - (egui['end_ns'] - egui['start_ns'])})\r\n        else:\r\n            unpaired_projection_calls += sum(map(len, pair.values()))\r\n    rejected = [s for s in capture['samples'] if s['kind'] == 'Rejected']\r\n    if rejected:\r\n        failures.append('App rejected stale/reset/unmatched observations; raw rejections retained')\r\n    if group == 'running' and len(fresh.get(expected_client, [])) < 3:\r\n        failures.append('Running capture did not publish/consume at least three generations')\r\n    frame_stats = stats([s['delta_ns'] / 1e6 for s in frames])\r\n    return {'valid': not failures, 'failures': sorted(set(failures)), 'condition': group,\r\n            'client': expected_client, 'repetition': int(meta['repetition']),\r\n            'duration_seconds': duration, 'initial_facts': facts[0], 'final_observed_facts': facts[-1],\r\n            'tps_sequence': tps_sequence, 'window_size_sequence': size_sequence,\r\n            'selection_sequence': selection_sequence, 'frame_ms': frame_stats,\r\n            'p95_frame_16_7_ms_met': frame_stats.get('p95', math.inf) <= 16.7,\r\n            'cpu_scopes_ns': {key: stats(values) for key, values in sorted(cpus.items())},\r\n            'freshness_within_capture': dict(fresh), 'pre_capture_publication_ages': dict(pre_capture),\r\n            'freshness_ns': {client: stats([s['age_ns'] for s in values]) for client, values in fresh.items()},\r\n            'same_publication_freshness_pairs': fresh_pairs,\r\n            'same_frame_same_publication_projection_pairs': cpu_pairs,\r\n            'unpaired_projection_calls': unpaired_projection_calls,\r\n            'retained_rejections': rejected,\r\n            'input_latency': 'INSTRUMENT-INVALID / UNAVAILABLE; p95 <=100 ms UNEVALUATED',\r\n            'first_display': 'UNAVAILABLE', 'direct_render_gpu_cost': 'UNAVAILABLE',\r\n            'simulation_vs_presentation_gpu_memory': 'UNAVAILABLE'}\r\n\r\n\r\ndef main():\r\n    parser = argparse.ArgumentParser()\r\n    parser.add_argument('raw')\r\n    parser.add_argument('report')\r\n    parser.add_argument('--protocol', default=str(Path(__file__).with_name('protocol.json')))\r\n    args = parser.parse_args()\r\n    report = analyze(load(args.raw), load(args.protocol))\r\n    report['raw_sha256'] = digest(args.raw)\r\n    report['analyzer_sha256'] = digest(__file__)\r\n    report['protocol_sha256'] = digest(args.protocol)\r\n    with Path(args.report).open('x', encoding='utf-8', newline='\\n') as output:\r\n        json.dump(report, output, indent=2, allow_nan=False)\r\n        output.write('\\n')\r\n    if not report['valid']:\r\n        print('CAPTURE CHECK FAILED: ' + '; '.join(report['failures']))\r\n        return 1\r\n    print(f\"SAVED: {report['condition']} / {report['client']} / repetition {report['repetition']}; \"\r\n          f\"{report['frame_ms']['count']} frames; {report['duration_seconds']:.1f}s\")\r\n    return 0\r\n\r\n\r\nif __name__ == '__main__':\r\n    raise SystemExit(main())\r\n"}
{"kind":"protocol","sha256":"0e00ec2031a3e8878fc2008de250f37138bdb6b94930c70b31bb2ef234c8b375","utf8_text":"{\r\n  \"protocol\": \"m16-b1-ovl-v5\",\r\n  \"revision_note\": \"Future Group 4 only: both clients select actual ClauseScript row order A,B,C,D,E,G,F (IDs 1,2,3,4,5,7,6), seven selections on unchanged pinned workload. Replaces the erroneous ascending-ID assumption with a newly frozen common sequence before fresh captures. Prior data retain original protocol/results; no retrospective acceptance. All other groups, quantities, thresholds, launcher and Studio unchanged.\",\r\n  \"authority\": \"Board 5654699942; OVL addendum 5653843173; DA 5651557467\",\r\n  \"code_revision\": \"a8c117313dbe3796aa160ba12cad1cd677bdc5e6\",\r\n  \"instrument\": \"m16-b0a-v1\",\r\n  \"expected_source_identity\": \"fnv1a64:ee4e4df9e8c9fbd9:5798\",\r\n  \"expected_profile_identity\": \"fnv1a64:bfcbc44323b304bf:23530\",\r\n  \"expected_dependencies\": {\r\n    \"stellaristhing_base.base.json\": \"fnv1a64:c49f9ca3c8c75e77:20370\",\r\n    \"stellaristhing_base.dependencies.json\": \"fnv1a64:2f064bfb3e043aa0:72\"\r\n  },\r\n  \"source_path\": \"C:/Users/mvorm/SimThing-0088-m16-ui-comparison/scenarios/stellaristhing_base.clause\",\r\n  \"workload_cardinalities\": \"Unchanged Meridian: 7 systems, 6 links, 2 owners, 2 sites, 4 cohorts at each site, 13 energy RF participants, 2 recipes, 4 material loci. No stress variant.\",\r\n  \"repetitions\": 3,\r\n  \"pair_order\": [\r\n    [\r\n      \"Egui\",\r\n      \"Native\"\r\n    ],\r\n    [\r\n      \"Native\",\r\n      \"Egui\"\r\n    ],\r\n    [\r\n      \"Egui\",\r\n      \"Native\"\r\n    ]\r\n  ],\r\n  \"target_capture_seconds\": 15,\r\n  \"minimum_capture_seconds\": 10,\r\n  \"warmup\": \"Owner waits at least five seconds after load/client/condition setup, before F9; operator-qualified, not independently timestamped. The B0a collector excludes the first interval straddling F9.\",\r\n  \"window\": \"Existing Windowed setting, 1600x900 logical startup size, copied presentation settings. OS maximize/restore is used only during resize captures. The runtime records physical size and scale; paired geometry must be checked.\",\r\n  \"groups\": [\r\n    {\r\n      \"id\": \"paused\",\r\n      \"title\": \"Paused steady pane\",\r\n      \"prepare\": \"Load the source once at group start; keep it loaded for all six captures. Leave the simulation paused at generation 0, rate 1x, Max TPS 10. Keep Sim clock transport expanded. Set Native checkbox to the card's explicit state with the mouse; leave it alone if already correct. Keep Studio focused and wait five seconds.\",\r\n      \"action\": \"Press F9, leave the map and pane visible for at least 15 seconds without interaction, then press F10.\",\r\n      \"sequence\": \"Pinned Meridian G0; paused/1x/TPS10; no input during capture.\"\r\n    },\r\n    {\r\n      \"id\": \"numeric_edit\",\r\n      \"title\": \"Active numeric editing\",\r\n      \"prepare\": \"Load the source once at group start; keep it loaded for all six captures. Stay paused at G0, rate 1x, applied Max TPS 10. Set Native checkbox to the card's explicit state with the mouse; leave it alone if already correct. Use that client's editable Max TPS box. Keep Studio focused and wait five seconds.\",\r\n      \"action\": \"Press F9 ONCE, then do exactly SIX edits: 12.5, 10, 12.5, 10, 12.5, 10. For EACH value: click INSIDE the editable Max TPS box, Ctrl+A, type the value, check the box, press Enter. Check the STATUS BELOW shows applied TPS 12.500 or 10.000, then wait at least one second. Click back into the box before EVERY value: Egui Enter releases field focus. If the draft or status does not change, F10 and take a Studio screenshot showing both; report FAIL. After edit six and at least 15 seconds since F9, press F10. Keep Studio focused; do not add extra edits.\",\r\n      \"sequence\": \"Paused G0; three exact shared-owner Apply TPS pairs 12.5 -> 10. Bounded numeric editing only; not general text accessibility.\"\r\n    },\r\n    {\r\n      \"id\": \"resize\",\r\n      \"title\": \"Window maximize and restore\",\r\n      \"prepare\": \"Load the source once at group start; keep it loaded for all six captures and stay paused at G0/TPS10. Select the requested client. The window must be at its normal startup size, not fullscreen. Release numeric/native focus with Escape. Wait five seconds.\",\r\n      \"action\": \"Press F9 ONCE. Do exactly THREE maximize/restore cycles TOTAL. For each cycle: Win+Up once, wait two seconds; Win+Down once, wait two seconds. Count the restores: 1, 2, 3. After the THIRD restore, STOP resizing and remain at the starting size. If less than 15 seconds has elapsed since F9, wait idle at that size. Then press F10 ONCE. Keep Studio focused throughout.\",\r\n      \"sequence\": \"Paused G0; three existing OS maximize -> restore cycles. Full raw resize frames retained; qualification is this physical cycle only, not arbitrary drag or multi-monitor DPI.\",\r\n      \"reference\": \"https://support.microsoft.com/en-us/windows/hardware/input-devices/windows-keyboard-tips-and-tricks\"\r\n    },\r\n    {\r\n      \"id\": \"list_interaction\",\r\n      \"title\": \"Seven-system selection and native list\",\r\n      \"prepare\": \"Load the source once at group start; keep it loaded for all six captures and stay paused at G0/TPS10. Use the Overhead (O) button so system labels can be located; keep the same camera for both clients. Select the requested client, release field focus and clear selection with separate Escape taps. Wait five seconds.\",\r\n      \"action\": \"Press F9 ONCE. Select A, B, C, D, E, G, F (IDs 1,2,3,4,5,7,6) in that exact order, about two seconds apart. G comes BEFORE F in this map's Native row order. Egui: click those map systems in that order. Native: click row A, then tap Down six separate times, releasing between taps, and check the selected ID after each. End at F (ID 6). Stop selecting; wait idle if needed until at least 15 seconds since F9, then F10 ONCE. If a label or key does not work, F10 and report FAIL; do not add corrective selections.\",\r\n      \"sequence\": \"Same shared selection sequence 1,2,3,4,5,7,6 on BOTH clients and unchanged seven-system workload, following actual ClauseScript structural row order. Clear selection before F9. Egui map picks A,B,C,D,E,G,F; Native clicks A then six Down taps. Seven selections per capture. Different adapters; no equal-widget-cost or long-list scaling claim. Fresh captures only; earlier ascending-ID captures retain their original results.\",\r\n      \"selection_ids\": [\r\n        null,\r\n        1,\r\n        2,\r\n        3,\r\n        4,\r\n        5,\r\n        7,\r\n        6\r\n      ]\r\n    },\r\n    {\r\n      \"id\": \"running\",\r\n      \"title\": \"Running steady pane\",\r\n      \"prepare\": \"Before EACH capture, reload the same Clause source through Library to get a fresh G0 resident. Select the requested client. Set rate 1x and TPS10. Click that client's Play, wait five seconds, then capture.\",\r\n      \"action\": \"Press F9. Leave it running with no other input for at least 15 seconds. Press F10, wait for the saved confirmation, then Pause. Reload the source before the next capture. Do not change TPS, selection or window while recording.\",\r\n      \"sequence\": \"Each repetition: same source re-admission at G0, Play at 1x/TPS10, five-second operator warmup, at least 15 seconds observed running, F10, Pause. Actual resident generations and capture duration are retained; manual start phase is a limitation.\"\r\n    }\r\n  ],\r\n  \"statistics\": {\r\n    \"quantile\": \"Nearest rank: sorted[ceil(p*N)-1], predeclared p50/p95/p99 plus min/max/mean/count; no outlier removal or clipping.\",\r\n    \"frame\": \"B0a Time<Real> unsmoothed main-loop cadence; not scanout or compositor delivery. p95 characterized at 16.7 ms.\",\r\n    \"cpu\": \"shared_clock_and_observation_projection is the same computation. Each other adapter scope is reported separately. Nested scopes are not summed; coexistence includes the egui pane when Native is enabled.\",\r\n    \"freshness\": \"Publication stamp to first matching consumption, with full scene/resident/generation identity. Pre-capture publication ages retained separately; they are not steady publication-delivery samples. Same-publication Egui/Native differences retain negative values.\",\r\n    \"memory\": \"One read-only PID private-bytes/working-set snapshot immediately after each completed raw export, before analysis; UTC/start time/lag retained. Process total after instrumentation/export, not GPU or UI-only memory.\",\r\n    \"paired_residuals\": \"Native minus Egui by condition/repetition for capture statistics and memory snapshots. Same-publication freshness and, where provable, same-frame shared projection use direct sample pairs. Chronological frame-ordinal residuals are descriptive cross-run pairs only, with all unmatched tails retained; never a latency or causal frame-identity join.\"\r\n  },\r\n  \"unavailable\": {\r\n    \"input_latency\": \"INSTRUMENT-INVALID / UNAVAILABLE; p95 <=100 ms UNEVALUATED; B0b containment and R1 successor-window failure packets carried forward unchanged.\",\r\n    \"first_display\": \"UNAVAILABLE: no qualified matching DISPLAYED endpoint.\",\r\n    \"direct_render_gpu_cost\": \"UNAVAILABLE: no directly admitted qualified per-client timing instrument.\",\r\n    \"simulation_vs_presentation_gpu_memory\": \"UNAVAILABLE: no direct separately measured allocation pair.\",\r\n    \"long_list_and_multi_monitor_dpi\": \"UNQUALIFIED by this seven-system, single-monitor protocol; retain Leaf A limits.\"\r\n  },\r\n  \"decision_constraint\": \"Input latency is required and unavailable. No positive migration recommendation is permitted on this packet; RETAIN-EGUI will cite the measured results and remaining uncertainty after collection.\"\r\n}\r\n"}
```
