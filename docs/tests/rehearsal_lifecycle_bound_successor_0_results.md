# Bound-session structural lifecycle successor + TWELFTH E8 roll

DA increment per Orchestration relay `5704549307` (Astra return `5704350506` accepted).
**Model 1 remains UNDECIDED; Model 2 remains CLOSED.** Sixth admitted substrate gap of the
track: the tick-zero whole-table binding freeze predated lawful in-session structural
growth, so the installed ActionBand's OWN pre-admitted AddChild consequence
self-invalidated the binding table at its first funded birth.

## The admitted law (relay's preferred shape, all ten rules)

- **Tick-zero installation singularity kept** — no late install, no reinstall, no rebuild
  (rule 1). Install additionally derives the **bound footprint**: the identities of the
  admitted active instances resolved through the tick-zero snapshot — existing admitted
  artifacts only, no shadow registry (rule 3).
- **`ensure_action_band_ingress_current` is untouched in meaning**: whole-shape equality
  against the ACCEPTED baseline before every hot cycle and between boundaries — foreign
  drift still refuses exactly as before (rules 2, 8).
- **Acceptance happens in exactly one place**: `dispatch_action_band_boundary`, called
  synchronously immediately after the canonical boundary with its `BoundaryOutcome`, so
  the observed delta IS that transition's work (rule 7). The pure law
  `adjudicate_action_band_boundary_binding_table` admits the current table as the new
  baseline ONLY when: registry/dimension/resident fences are exactly unchanged; every
  pre-existing row is present identically (bound-footprint rows refuse with the NEW typed
  `BoundIdentityRemapped`, any other pre-existing change with `BindingTableStale` — rule
  5); and additions are present ONLY when the outcome carries accepted growth evidence
  (`Accepted` growth-residency facts or fission pairs) (rule 4).
- **Newly born children acquire no enrollment** — instances, consequences, and the sealed
  dispatch are untouched by acceptance (rule 6). Touched-generation fail-stop untouched
  (rule 9). One execution owner, sealed provenance, no fifth leg (rule 10).

## Proof floor executed (reference machine)

- **Pure-law battery 5/5** (ledgered, invariant-required): additive+growth lawful /
  additive−growth stale; bound-identity remap AND removal typed-refused even during
  growth; unbound pre-existing change stale even during growth; registry/dimension drift
  typed-refused even during growth; identical shape always lawful.
- **#2062's UNCHANGED suites at exact tested head `07238c0e`, post-repair: 7/7 GREEN** —
  the funded birth matrix completes in BOTH orders: G1 WIP settles (no birth), G2 consumes
  1A+1B and the sealed consequence queues (facility generation 0→1), **G3
  `born=true component=true placed=true`, and the session CONTINUES through G4/G5** with
  the child attached and boundaries running — no reinstall, no retry semantics, repeated
  post-birth generations included. Insufficient-input control: five generations, zero
  birth, zero placement, facility generation pinned at 0. Joint lifecycle, WIP,
  provenance-negative suites all green.
- **Existing planted negatives intact:** embedder `actionband_execution_ingress_0` green,
  including its late-registry-growth `RegistryStale` plant and its ordinary structural
  consequence.
- Prior law green: driver lib 21/21 (all earlier rehearsal witnesses included).

## TWELFTH E8 roll (`session.rs` is a bundle component)

- **Old-pin refusal (RED, required first), repaired source, reference tuple:** ordinary
  ingress `UnqualifiedAdapter { required: 17752583138607907014, observed:
  7493919273263963470 }`; parity referee FAILED with
  `RESIDENT-CLEARING-QUALIFICATION-FINGERPRINT: 67ffbfdfe25a1d4e` — the same observed
  value derived independently twice.
- **Roll:** both literals atomically `0xf65d_d84e_ae50_40c6` → `0x67ff_bfdf_e25a_1d4e`;
  no third site; no comparator/component-list/`build.rs`/Cargo change.
- Pin chain: `…0xd5fc…` → `0xf65d…` → `0x67ff_bfdf_e25a_1d4e`.
- **Battery at floor:** parity 1/1 + mutant matrix, score-and-bands 3/3, runtime 4/4.

## Clean-checkout proof

CLEANCHECKOUT_PLACEHOLDER
