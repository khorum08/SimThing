# PORTABILITY-PROOF-0 results

- Track: 0.0.8.7 RF arena modernization (12.1)
- Status: **PROBATION / proof-present / DA-review-pending**
- Branch: `codex/portability-proof-0`
- Dispatch/base: orchestrator resume `5459581170` / `ff44bf597f063b62807304a68db0b974cb45fa68`
- DA A1 authority: `5459514102`
- HD-RECEIPT: `745248a69bcd`
- Final ORIENT-RECEIPT: `8907d5c4426e`
- Orientation rule stamp: `c931947f76cd5944`
- Orientation digest: `61fb867db1f4ba9fca46e67c0bc9222a56b0b44b1c15287f4428be1620046dac`
- Pointer: 12.1, unchanged

## Result

One standing witness promotes the existing network-saturation seed into an
unrelated-domain five-verb portability proof. Its imports are limited to
`std` and `simthing_embedder`; no shipped scenario, ClauseThing input, game
adapter, or lower-crate escape hatch participates.

- **Derive** admits a carrier specialization and the existing PowerLaw gadget.
- **Populate** authors and admits the resource-flow tree and its live values.
- **Overlay** attaches an attributable two-tick domain modifier.
- **Bind** admits PALMA and Gu-Yang sweeps and reads the resulting STEAD/Triad
  surface without writing a decision. The observed net-flux, gross-flux, and
  stall bits equal the ordinary mapping's canonical live columns.
- **Run** opens the ordinary admitted-field-sweep GPU session, records one
  authoritative boundary, reads that artifact through the Run facade, restores
  its existing spec snapshot/deltas, and applies its canonical replay frame.
  The participant CPU-shadow row promised by the existing replay law is
  nontrivial and bit-exact after replay.

The generic existing `open_replay_with_spec` entry and its required products
also cross `run` verbatim. The specialized admitted-field-sweep session is
reopened by composing the same existing reader, spec apply entries, and replay
driver through `run`; no facade wrapper or alternate replay semantic was
created.

## A1 facade proof

`simthing_embedder::run` adds only public `use` re-exports of the existing
driver reader/opener/apply functions and their exact driver/sim product and
error types. The compile-level falsifier assigns both the lower entries and the
Run entries to identical function-pointer signatures, then uses same-type
identity functions for `LoadedReplay`, `ReplayOpenError`, `SpecDelta`,
`ReplayDriver`, and `ReplayFrame`. A facade-local shadow type or wrapper cannot
compile against this proof. The existing facade-law battery remains unchanged
and green, including leaf direction, zero owned state, closed five-verb shape,
and read-only observation.

## Exact verification

| Command / proof | Result |
|---|---|
| `cargo test -p simthing-embedder --tests` | PASS — 15/15 |
| focused portability witness | PASS — 1/1 on the ordinary GPU/session path |
| replay export/type identity falsifier | PASS — 1/1 |
| finance/network/vendor-door regression battery | PASS — unchanged |
| `cargo test --workspace --no-fail-fast -j 1 --quiet` | Structural certificate: 123 suites / 468 passed / 3 failed / 14 ignored |

The three certificate reds are byte-identical in identity to the inherited
ClauseThing baseline and outside this rung: `gpu_category_micro_economy_matches_arena_allocation_oracle`,
`gpu_scatter_projection_matches_cpu_oracle_through_commitment`, and
`star_naming_canonical_tp_all_systems_have_display_names`. The initial parallel
certificate attempt exhausted the Windows paging file; the reported
certificate is the complete single-job, no-fail-fast rerun. DA retains final
certificate-scope adjudication because the bounded facade re-export is a
production-surface change.

Inventory/drift, doctrine scan/selftest, orientation freshness, handoff lint,
doc budget, agent delta scan, diff check, exact tested/final heads, and hosted
workflow IDs are bound in the PR relay and Board return. No merge or
self-graduation is claimed.
