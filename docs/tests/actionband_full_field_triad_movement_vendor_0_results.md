# ACTIONBAND-FULL-FIELD-TRIAD-MOVEMENT-VENDOR-0 results

- Track: 0.0.8.7 RF arena modernization (rung 7.5c)
- Status: **PROBATION / proof-present / DA-review-pending**
- Branch: `codex/actionband-full-field-triad-movement-vendor-0`
- Dispatch base: `2f6aa44916ddd56fbc4f4bf391393ac3f44556e2`
- tested_code_sha: `beb86f8b`
- HD-RECEIPT: `c0685b41c7c4`
- ORIENT-RECEIPT: `c10d6012f34b`
- orientation_rule_stamp: `9a869600268812a2`
- Dispatch: board comment `5262167206`
- Remand repaired: board comment `5266968417`
- DA pre-dispatch: `5262109628`
- 7.5b graduation state: PR #1732 @ `2ef325b7`; exit stamp #1734 @ `686d31e0`

> **Born-mortal.** This is a workshop-only positive vendor proof. `cargo tree -i
> simthing-workshop` has no reverse dependency, so reaping the module removes no
> production ActionBand, FieldSweep, Gu-Yang, RF, CostBand, overlay, or structural capability.

## Scope ledger

| Path | Kind |
|---|---|
| `crates/simthing-workshop/src/actionband_full_field_triad_movement_vendor_0.rs` | assertion-only workshop consumer |
| `crates/simthing-workshop/src/lib.rs` | module export |
| `crates/simthing-workshop/tests/actionband_full_field_triad_movement_vendor_0.rs` | four real-GPU integration proofs |
| `docs/tests/actionband_full_field_triad_movement_vendor_0_results.md` | evidence |
| `docs/tests/current_evidence_index.md` | evidence index |
| `scripts/ci/inspect_justifications.tsv` | four-test TEST-BUDGET justification |
| `scripts/ci/test_inventory.tsv` | four permanent-residue rows |

**Production engine `src` diff: none.** The graduated 7.4 and 7.5b witnesses are unchanged.
DA stamp/workplan/orientation surfaces are unchanged. No CI gate implementation or workflow is changed.

## Proofs and TEST-BUDGET

The focused budget is four integration tests; each admits a distinct load-bearing failure mode.

| Test | Why admitted / regression caught |
|---|---|
| `full_vendor_capacity_overlay_costband_and_arrival_chain_is_native_bounded` | Exercises the real GPU location/target → STEAD/overlay → PALMA → Gu-Yang/RF → one native bound → ActionBand/EML → ordinary structural/lifecycle chain. It binds the real Phase-5 generation for both `[1.0, 0.25, 1.0]` capacity/restoration and fixed-capacity ordinary-condition N/N+1/N+2 series. Catches route drift, non-native physical progress, same-generation convergence/history, CostBand-before-throughput ordering, non-exact restoration, or retained arrival state. |
| `semantic_rename_delete_is_blind_at_shadow_to_positional_template_boundary` | A1 authority-boundary proof. Renames/replaces authored IDs, renames/deletes labels, updates conserved-progress textual references, and keeps authored vector order fixed. All variants dispatch on the real GPU; the proof compares positional index, plan fingerprint, compiled numeric binding, target/descent, bound source, actual structural destination, and the sealed commitment. Catches any name map, sort, or textual dispatch authority. |
| `costband_selector_is_unrepresentable_and_workshop_is_structurally_reapable` | A2 closed-set/type and detachability proof. Catches a CostBand selector entering `None|RfGrant|GuYangAvailable|GuYangRealized`, a constructible sink-price mutant scaling physical movement, or any production dependency on the workshop witness. |
| `opposed_demand_requires_native_contest_and_abs_gross_mutant_reds` | Runs the opposed pair through real GPU PALMA/Gu-Yang/Phase-5/ActionBand/RF. Catches treating net≈0 absence as contest or advancing both actors from gross/absolute flux while native signed stall/contest authority says stall. |

## Results

- Focused 7.5c: **4 passed, 0 failed, 0 ignored** on NVIDIA Vulkan.
- Inherited 7.5b/7.5a/7.5/7.4/7.3/7.2/7.1: **46 passed, 0 failed**.
- FieldSweep N4 + Gu-Yang comparative: **8 passed, 0 failed**.
- CostBand unit/integration/production batteries: **26 passed, 0 failed**.
- Full `simthing-workshop` crate: PASS. Its pre-existing certification-only ignored tests are unrelated; the 7.5c target has zero skips.
- `cargo check -p simthing-workshop`: PASS.
- `TEST-INVENTORY-DRIFT-CHECK`: PASS (`rows=1241`, `discovered=1241`, `unledgered=0`, `stale=0`).
- `DOCTRINE-SCAN-VERDICT`: **INSPECT `failures=0 inspect=1`**; sole finding is the admitted four-test TEST-BUDGET row with an existing justification.
- `WORKSHOP-HOMING-DETECTION`: PASS; `DETACHABILITY-VERDICT`: PASS (`production_coupling=0`, `proof_coupling=0`, `ceiling=0`).
- Type/dependency audit: zero production-source diff; no `MovementPlanner`, `FluxThing`, path/predecessor, movement saturation/listener/crossing/congestion/executor peer; workshop is a leaf.

## Authority findings

- A1 holds at `ActionBandSemanticShadow → ActionBandTemplateIndex`: the authored IDs demonstrably change in the semantic shadow and in their conserved-progress textual references, while authored vector order, positional template index, numeric plan/binding, target/descent, bound source, sealed GPU commitment, and actual structural consequence remain identical. The association-only frozen-admission hash intentionally includes authored identity and is not treated as numerical authority; no name map, sort, or index derivation is introduced.
- A2 holds by exhaustive match over the admitted three nonzero native sources (with wire `None=0`); CostBand is only downstream draw quantization. Price changes draw while physical progress remains native; the CostBand-scaled movement mutant REDs.
- Capacity `[1.0, 0.25, 1.0]` at bound generations 75/76/77 preserves target/descent/template/plan identity, lowers physical advance exactly with native flux, and restores bit-exactly. A separate fixed-capacity 90/91/92 series changes only one ordinary Gu-Yang source condition at N+1: the generic field/throughput result changes only in the next-generation dispatch and restores bit-exactly at N+2, with PALMA descent and the admitted plan unchanged and no route/search/convergence/history object.
- Opposed demand requires positive native stall/contest; net≈0 alone is rejected, and gross/absolute-flux advance mutants RED on the real path.
- Arrival uses ordinary structural and overlay lifecycle collapse; no task/movement executor record survives.

Coding does not `/clearance`, merge, move the pointer, or begin 8.1/8.2/Vector CostBand/StemThing-B.
