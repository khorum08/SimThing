# TEST-PARE-CONSERVATIVE-SURVIVOR-DELETE-0 Results

## Status
DA-executed deletion wave (executive DA acting under the owner's §17 direct authority). PROBATION until
the §17 merge conditions are verified on the PR, then DA-merged.

## #1104 merge commit
`be1bd0761ef9` (verified: head `4a727e9b14` exact, scan PASS, default exec PASS, targeted exec PASS
SHA-bound to head via `DOCTRINE-EXEC-VERDICT ... head_sha=4a727e9b...`; master inventory 5234 confirmed).

## Input candidate set
112 `B-T6-PROTECTED-ORACLE-PROMOTION-REVIEW` / `promotion-target:protected-oracle-review` rows
(101 integration, 11 unit), verified disjoint from all 270 TRUE_MEMBER coverage survivors.

## Inventory before / after / delta
5234 → **5147** (−87 rows). Rows considered: 112 (+2 file-level entries).

## Rows deleted / kept / blocked
- **DELETED_TEST: 87** — fossil capability proofs (closed-rung c0 protocol gates, dress-rehearsal/no-CPU-planner
  era assertions now type/scan-owned), upload/recompile-cache hygiene batteries, combined-flag ladder relics,
  superseded 1e-5 legacy comparisons, CPU-fixture expectation variants whose surfaces are owned by TRUE_MEMBER
  survivors, and 2 private-twin duplicates of the public sealed-door tests.
- **DELETED_EMPTY_FILE: 2** — `palma_path_4_benchmark.rs` (benchmark relic), `c1_threshold_scan_parity.rs`
  (threshold surface owned by kernel-internal TRUE members in `session.rs`/`passes.rs` + gpu choke tests).
- **KEPT (25, reclassified with judgment notes):** 6 sanctioned-door contract units on the kernel CPU-oracle
  twin (public door + private-twin uniques); SoftAggregateGuard suite (4, invariants.md-named); child_share
  zero-weight (invariants RF row); admission-boundary representatives (nonfinite transfer, exact conservation,
  MinAcrossInputs recipe, emission overflow contract, EvalEML whitelist/tree-id, soft-formula gate); band-order
  (`runs_after_velocity_before_overlay`, invariants Evaluation row); overlay lifecycle + fission inheritance
  (core §6/§2.1); atlas default-off (invariants mapping row); PALMA field-not-route (STEAD §6); registry
  tombstone scope. 23 → `permanent-residue:behavior-regression` with `catches:` notes; 2 →
  `permanent-residue:determinism` (three-run bit-identity).
- **Rows blocked: 0.**

## Promotion rows retired / retained
All 112 `protected-oracle-review` promotion rows retired (87 deleted, 25 resolved to permanent residue).
Remaining plan rows: 24 (tier2-collapse boundaries, unchanged).

## Coverage map post-check
No TRUE_MEMBER, dependency-floor, STEAD §8, doc-named, seal-proof, or golden-byte row deleted (candidate set
verified disjoint from the 270-surface coverage map before deletion; threshold surface re-verified owned by
kernel TRUE members before the c1 file removal). STEAD §8 suites byte-untouched.

## Judgment-note rule
All 23 behavior-regression keeps carry specific `catches:` notes; `--prove-judgment-note-rule` PASS.

## Ledger/gate repairs made in-wave (DA authority, recorded)
- `determinism` added to `allowed_class` in `test_inventory_check.sh` — the class taxonomy omitted a
  never-pare class (same enum-gap family as the #1088 0R).
- 25 resolved keeps removed from the `test_pare_audit.tsv` candidate queue; boundary keep rows mirror the
  new inventory classes; deleted boundary rows adopt the #1104 usecase-superseded owner convention.
- Ledger defect noted: none (no duplicate rows found; the two same-named kernel tests live in two real files —
  public sealed-door module and private twin; the private twin's duplicates were deleted).

## Scope rows and re-seal
All 8 stale `test-pare-protected-pare-delete` scope rows removed (re-sealed). 6 temporary rows added for this
rung only (`authorized_profile: test-pare-conservative-survivor-delete`, retirement: delete when this rung
closes), with the Owner/DA-approved marker; src rows authorize cfg(test) removal exclusively.

## GHA proof-seal compliance / no-full-battery proof
Profile `test-pare-conservative-survivor-delete` (targeted; cargo check floors only; gpu_required=no).
`doctrine_exec_profile_lint.sh` PASS + `--prove-gha-proof-seal` PASS. No bare full-crate or workspace cargo
test anywhere; no Atlas/Bevy/GPU/desktop proof in GHA.

## GitHub gates (local pre-push run)
inventory check PASS · boundary check PASS · drift check PASS (+ --prove PASS) · edit-scope check PASS
(+ --prove PASS) · profile lint PASS · doctrine scan PASS 0/0 · digest PASS.

## Owner-deep/local proof
Cargo check floors (kernel/sim/driver/spec/workshop lib) PASS. Kernel `cpu_oracle` unit suite: **15/15 PASS**
on the owner's machine (covers all six door-contract keeps). Driver/spec/workshop edited binaries run locally
(results below/PR thread).

## MAJOR FINDING — pre-existing broken TRUE_MEMBER binaries (recorded loudly, out of this rung's authority)
The `simthing-sim` parity binaries `c3/c4/c5/c6` and `forked_four_slot` **do not compile on clean master**
(`c8b/c8c/c8d` compile and their suites pass locally, including this wave's admission-boundary keeps) — verified by stashing this wave's diff and checking `c5` at the merge base (AS-era seal
adoption drift: private `PropertyValue.data`, `RoleOffset` arithmetic, `SlotIndex` casts, sealed
`ThresholdEvent` fields). Every prior wave's proof was a lib-only `cargo check` floor, so this was invisible.
Consequence: the coverage map's **sim-crate TRUE_MEMBER oracle-parity survivors are currently inert gates**
(§0.6.6) — the live parity enforcement that actually runs is kernel-internal (`session.rs`/`passes.rs`
parity tests + the kernel cpu_oracle units, all green). This wave's deletions inside those files remain valid
(deleting fossil fns from a broken binary neither causes nor worsens the breakage; the errors predate the
diff and reference surviving code). **Named follow-on (next wave, DA-gated):**
`TEST-REPAIR-OR-REPLACE-TRUE-ORACLE-BINARIES-0` — either repair the test-only seal-adoption drift in the
TRUE-member binaries, or re-key the coverage map to the kernel-internal representatives and delete the inert
sim binaries; a TRUE_MEMBER that cannot compile is a false coverage claim either way.

The finding extends to **driver** fossil binaries, all verified pre-existing on master: `owner_silo_*` and
adjacent fixtures never adopted the TP-BASE-DISC provenance fields (`SimThingScenarioProvenance` E0063);
`palma_path_min_plus_oracle` has 30 seal-drift errors (its TRUE members and this wave's PALMA keep are
currently inert); `resource_economy_burn_in` E-drift; and `runtime_0080_0_r0` **compiles but fails 14/15 at
runtime on unmodified master** (stash-verified). This wave neither caused nor worsened any of it; the wave's
deletions inside those files stand. The scope of the follow-on wave is therefore repair-or-replace across
sim + driver fossil binaries, with coverage re-keyed to compiling kernel-internal representatives wherever
repair is not warranted.

## Scope Ledger
Test fns + 2 test files + ledger data + scope/profile data + this doc + review table + evidence line + track
row only. No product logic, no workflows, no scanner allowlists, no never-pare definitions, no §8 sub-paring.

## DA review note
Review conducted directly by the executive DA under §17 authority; every touched row is in
`docs/tests/test_pare_conservative_survivor_delete_0_review.tsv` (114 rows) with per-row disposition and
reason. Keep reasons name owners (doc rows / sanctioned door / STEAD contract); no generic keeps.

## Graduation routing
- CI verdict: pending PR run (expected PASS-RELIABLE)
- Triage entries: none
- Risk class: conservative-survivor deletion + promotion-target retirement + protected coverage preservation + gate-state scope edits
- Falsification check: every deleted row came from the reviewed candidate set (review table is the exact
  manifest); coverage map surfaces still owned; scope re-seal verified; §17 condition list.
- Recommended posture: DA-merge on §17 conditions.

## Known gaps / next
Remaining corpus after this wave: 5147 rows (4250-class behavior-regression AUDIT mass remains the next
material target). The `determinism` class-enum fix should propagate to the other two checkers' vocab if they
ever validate class (they validate tokens today). DF cadence decision remains open pending further reduction.
