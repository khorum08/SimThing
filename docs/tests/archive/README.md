# Test results archive

Archived files are retained historical evidence from sandbox/probe/revert cycles.
They are no longer active guidance unless explicitly cited by an active ADR or design doc.

When changing mapping classifications, consult the active ADR first, then cited evidence.

| Directory | Contents |
|---|---|
| [`mapping/`](mapping/) | Mapping optimization sandbox full logs |
| [`sead/`](sead/) | SEAD probe full logs |
| [`reverts/`](reverts/) | Revert verification reports and revert full logs |
| [`v7_6/`](v7_6/) | V7.6 promotion probe artifacts superseded by guardrail/parked results |
| [`resource_flow/`](resource_flow/) | Reserved for Resource Flow historical logs |

Active load-bearing summaries remain in [`../`](../) (parent `docs/tests/`).
