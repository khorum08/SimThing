# 0088 historical records and runnable successors

PROBATION / proof-present / clearance-pending / OPEN / UNMERGED.
Dispatch Board 5627721196; HD-RECEIPT 70df16e1babd;
ORIENT-RECEIPT 28f56884d309; 48-anchor ACK Board 5612017867.
Base 9d2f5d9538442c011cd1f3663c1400502e42307a. Exact tested head and hosted
results are in the PR and Board return. This independent leaf does not require
the pending plain-JSON adapter PR #2037.

The two owning tests replace no production authority. They establish the
successor evidence required before the DA's coupled code/census retirements.
The literal-install helper, ct_1a referee and vertical-seed constructor/export/
example remain unchanged here.

## Historical contract and modifier successor

`rehearsal_ingress_literal_record_retains_hydration_parity` reads the unchanged
CT-1a ClauseScript and RON fixtures. It compares the entire hydrated DomainPack,
the 40 seed and the complete seed list, and refuses promotion of that entity
record through the scenario hydrator. It also checks the native successor's
property identity, explicit quantity layout, overlay id/target/kind/source/
lifecycle/Amount multiplier and hydrated seed against the historical record.

The separate native specimen declares an unbounded, ungoverned Amount field.
This is intentional: CT-1a's CPU proof only applied the modifier, whereas its
implicit standard Amount layout clamps to [0,1] during ordinary velocity
integration. A runnable quantity must author its layout. The historical fixture
and its stage assertion are unchanged; no kernel law is changed or bypassed.

`rehearsal_ingress_literal_successor_preserves_born_modifier_parity` loads that
specimen through the production Studio native loader and reproducible cache,
opens ordinary resident sessions through StudioLiveSessionBridge, verifies one
installed overlay and the exact 40 seed/open observation, then executes a tick.

| Authored multiplier | Native born Amount | Cache born Amount |
| --- | ---: | ---: |
| 1.25 | 50 | 50 |
| 1.5 | 60 | 60 |
| 1 | 40 | 40 |

Thus the baseline CPU result 50 has an installed native/cache successor, with
both a changed-program discriminator and a neutral multiplier control. Expected
answers are never written into the opened session. No test-side numerical
executor or duplicate live-profile adapter is introduced.

## Portable vertical specimen

The successor's declared JSON lattice dependency is a byte copy of the existing
runtime_vertical_seed.simthing-scenario.json record, identified by relative path
and content identity. Its native envelope supplies canonical Scenario/Owner
authority and the modifier declaration. Both live routes verify the historical
8x8 frame, two occupied cells at (row=2,col=3)/(row=2,col=4), system ids 1/2 and
the 1-to-2 link; STEAD and links validate, Studio reconstructs both cells with
payload children and the link, and the document is canonical rather than legacy.
The old producer constructor has no live consumer: remaining Rust references are
its definition, lib reexports and the one-shot write_vertical_seed_fixture example.

## Checks and readiness

Owning commands: `cargo test -p simthing-clausething --test rehearsal_ingress_records
-- --test-threads=1` and `cargo test -p simthing-mapeditor
--test rehearsal_ingress_literal_successor -- --test-threads=1 --nocapture`.
Full package results and hosted verdicts are recorded at the exact final head in
the PR/Board return. Existing historical stage, golden and negative tests remain
in the full ClauseThing run. The unrelated historical Board-pointer referee
remains Orchestration-owned and unchanged.

CENSUS-TRANSITION READY, conditional on this leaf landing:

- AUTHORING-INGRESS-LITERAL-INSTALL: the helper's sole active referee can be
  retired with it after these successors are accepted. Keep the historical
  ClauseScript/RON fixture files consumed by the new owning test. Delete the
  helper, exports and obsolete referee/inventory row with the DA census increment.
- LEGACY-STUDIO-RUNTIME-VERTICAL-SEED: the portable asset now covers the seed's
  exact geometry, load/projection and actual resident execution. Delete the
  constructor/module export and its obsolete one-shot writer example with the
  DA census increment; no live consumer remains to migrate.

No deletion or census edit is split off here. These are readiness signals to
Orchestration, not self-graduation. Hydration/generation/preset convergence and
the remaining B1/final stamp work continue under the same contract.
