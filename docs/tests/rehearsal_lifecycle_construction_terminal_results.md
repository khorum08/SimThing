# Construction Leaf A — terminal crossing provenance STOP

**PROBATION / proof-present / BLOCKED. Model 1 UNDECIDED; Model 2 CLOSED.**
This is a concrete production/precondition STOP, not FAIL-MODEL-1 or graduation.

Authority: Board handoff [5721382287](https://github.com/khorum08/SimThing/issues/1332#issuecomment-5721382287),
release [5721385463](https://github.com/khorum08/SimThing/issues/1332#issuecomment-5721385463),
DA source-generation ruling [5721333880](https://github.com/khorum08/SimThing/issues/1332#issuecomment-5721333880).
Same #2062 / `codex/0088-construction-leaf-a`, synchronized ONCE onto exact master
`05e65c218a8364569472122bd608919f2357364e` (#2071). Backup
`codex/0088-construction-pre-2071` retains historical head
`89221121fac260ea98d1527fa8a0bfa389e5b86a`. One coding lane; no subagent.

## Result

All 13 retained Leaf A tests pass on the released tree, including the previously
RED 14-generation continuation matrix. Their sources/assertions and historical
RED evidence are preserved. One appended terminal table test adds 46 integrated
sessions: 32 independent ordering cases, eight healthy identity-refusal cases,
two unchanged-alert controls, and four mismatched-crossing cases. The last four
are RED: ordinary execution accepts changed threshold provenance and can create
the second product before its frozen funded threshold is reached.

The final assertion requires every negative to refuse. No ignored test, expected
panic, caught failure treated as success, or production repair masks this result.
The full emitted diagnostics are in [the raw packet](rehearsal_lifecycle_construction_terminal_raw_results.md).
The PR body/validation return binds final head, exact-head commands, hosted
artifacts, INSPECT accounting and fresh final-body clearance.

## Concrete failure and control

At tick zero, two distinct structural products are admitted once, against one
project's product balance thresholds **0.5 and 1.5**. Source supply and recipes
are the ordinary two-project/two-resource scenario. After commitments are
installed, the negative uses only the public alert configuration door:
`velocity_alerts().to_vec()`, `clear_velocity_alerts()`, and
`register_velocity_alert()`. It changes registration 1's threshold to **0.75**;
the ActionBand plan/consequence table stays frozen. No packet, key, seal, raw
GPU event, generation stamp or structural request is fabricated by the test.

- G1 allocates P=(3A,1B,0), Q=(1A,3B,0).
- G2 consumes exactly one complete pair per project, leaving P=(2,0,1), Q=(0,2,1).
  The production Phase-5 GPU mint emits source-generation-1 crossings at 0.5
  and **0.75**, both with post-value 1. The second crossing is not the admitted
  1.5 threshold, and the project's funded receipt has not reached 1.5.
- G3 adds **both** distinct product identities under that one project. The raw
  packet records their structural deltas, allocator rows, parent relations and
  committed placements. Its owned receipt remains **1**, through G4.
- Neither the preflight nor the sealed dispatch reports a typed refusal. The
  facility has advanced once for the two-crossing batch. No retry or clock
  pumping causes the second birth.
- The identical clear/re-register control preserving **1.5** creates only the
  first product, as required. Both control and negative execute at P and Q.

A second pair of negatives changes 1.5 to **1.25 after G3**. The fixed authored
G4/G5 source pulse produces the next ordinary receipt at G6. Its real sealed
crossing carries source generation 5 and threshold 1.25, yet the frozen 1.5
consequence is accepted and creates the second product at G7. This isolates
post-start provenance drift as well as the early-birth consequence.

## Source diagnosis and requested routing

`session.rs::current_action_band_ingress_shape` records binding table,
registry activation/columns and resident dimensions, but no threshold-definition
identity. `BoundaryProto` rebuilds changed alert registrations through its normal
threshold-dirty path. `ActionBandExecutionPlan::crossings_from_sealed` joins on
registration index, active-instance slot and template band range; it does not
compare the sealed threshold identity with the frozen binding. The depth-1
emission path uses that crossing and produces the existing structural request.

The negative demonstrates an admission/provenance hole, not failure of ordinary
owned stock or conjunctive recipe semantics. Return through Orchestration/DA for
the generic frozen-binding/stale-threshold law. Preserve canonical Phase-5 mint,
authored threshold identity, exactly-once source generation, typed refusal and
touched-generation fail-stop. This leaf does not choose or implement a new
production invalidation policy or edit the E8 kernel/pin.

## Contract accounting

| Terminal obligation | Executed evidence and disposition |
|---|---|
| Materials / reservation / disposition | GREEN. Owned incomplete stock remains recoverable until recipe consumption. Success, cancellation and refusal account for residual A/B plus consumed-pair receipts; supply is 4, then 7, then 10. Refusal and funded cancellation explicitly scrap consumed inputs, with no refund/reservation inferred from the receipt or construction ledger. |
| Authored restart | GREEN. Fixed source overlays at G4/G5 and G9/G10 drive new paid receipts after refusal/cancellation; no readback-dependent policy or stale candidate replay. |
| Actual freed extent reuse | GREEN. The unchanged matrix's second successful funded product at G12 consumes the exact extent released at G9, with a new placement identity. The added ordering cases also exercise real integrated reuse. |
| Two fresh completions | GREEN. G7 and G12 complete distinct product/component/overlay identities in each 14-generation session; refused candidate 0 never appears. |
| Complete product profile | GREEN. Tree membership, parent/child relations, property and overlay identities, stable host bindings, new resident rows and GPU observations 18/19 are asserted. No frozen-template counter substitutes for structural birth. |
| Independent determinism | GREEN. P/Q × independent child, arena, recipe and physical spacer placement reversals: 32 sessions. All eight-generation stock/live-count/facility-ordinal traces are identical; host rows actually differ between {1,2} and {5,6}. Each session consumes the freed extent for its fresh second subtree. |
| Integrated negatives / provenance | PARTIAL / RED. Foreign and stale binding plants, canonical removal of the bound participant, and duplicate resident product identities refuse with provenance. Four real mismatched-threshold cases are accepted; two create a premature second product. Full negative closure is blocked. Supplemental batteries are not substituted. |
| Exactly-once / fail-stop | GREEN for healthy and faulted identity cases: sealed sources [1,5], quiet boundaries do not advance the facility; 12 preflight refusals leave values/clocks/schedule unchanged; 12 post-touch retries remain faulted and preserve values/clocks/schedule/bindings/facility ordinal. The crossing hole itself produces no fault to stop. |

## Proof identity and scope

The unchanged recipe prefix SHA-256 is
`883ac82f7f02956763f0ab16e785093dde73a04d38e2eb97e7238f4b2a8cab8d`;
it is byte-preserved. Full appended recipe source SHA-256 is
`5ab7655fd70d335906bb7000442278fa4b5d1b68df433289532157361539a805`.
The source-frozen diagnostic uses this full source; final exact-head validation
repeats the complete four-suite Leaf A matrix before the Board return.

Reference GPU: NVIDIA GeForce RTX 4080 Laptop / Vulkan / NVIDIA 595.79;
rustc 1.95.0 / LLVM 22.1.2; `simthing-gpu/eml-resource-profiling` enabled.
E8 required=observed `ec5a2a30afaee795`; semantic bundle `9c2091194366b23b`.

Only the existing driver integration-test file is appended, with one ordinary
AUDIT / delete-at-closeout inventory row and these two evidence documents.
No production source, scenario, Cargo, gate code, class, anchor, workplan,
orientation or triage/justification change. No other worktree is changed.
No merge; return #2062 OPEN / DRAFT / UNMERGED.

ORIENT-RECEIPT: 28f56884d309
role: coding
orientation_rule_stamp: 73e54d6b56b7266b
orientation_digest_sha: e5b941f898d53f86a8be2e19afba51d52a41dda06f69dff4d3b52817ffed89c6
ANCHOR-ACK: rehearsal-0088-construction-contract@bdd51c7c4ae2
ANCHOR-ACK: actionband-crossing-surface@623db585f145
ANCHOR-ACK: actionband-determinism-lifecycle@6306c484732c
