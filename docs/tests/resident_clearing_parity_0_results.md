# RESIDENT-CLEARING-PARITY-0 results

Status: **PROBATION / archaeology-complete / implementation-in-progress /
DA-review-pending / UNMERGED / no 14.6**

- exact dispatch/master base: `8f8e1f2e8626253876862771ee1f553327a27c8b`
- coding ORIENT receipt: `d50e2f5d3aaf`
- orientation rule stamp: `7369084da1f79d79`
- HD receipt: `4521aa9c142b`
- authority: Board handoff `5513827005`, repository handoff
  `handoffs/RESIDENT-CLEARING-PARITY-0.hd.md`

The tables below were completed before any executable 14.5 edit. They reopen
the graduated CPU oracle, 13.6/13.9 seals, 14.2 resident plan and multi-tree
ownership, 14.3 direct allocation/pressure/recurrence path, 14.4 Q149 exact
settlement and `T_s`, and the existing adapter-qualification pattern.

## Required-anchor acknowledgement

All 63 anchors rendered by the handoff were queried through
`scripts/ci/anchor_query.sh` under the coding role and are recorded in the
append-only reach ledger. The binding subset for this referee is the exact
numeric/conservation law, Field-Triad/ActionBand authority, recursive RF germ
and self-consumption law, tree-local identity, physical-order prohibition,
admission ladder, cross-crate seal residue, and workshop-as-referee boundary.

## Pre-edit parity archaeology

| Positive item | Reopened authority | Dispatch-base classification | 14.5 action |
|---|---|---|---|
| EML score bits | `AuthoredClearingProgram::score` and 13.6 matrix | **already proved** for finite non-negative output and canonical signed zero | compose the same admitted program into the neutral transcript; do not add a resident score lane |
| equality bands | `clear_constrained_claims_at_generation` score-bit grouping; 14.3 ordering audit | **already proved** | expose the canonical band transcript in the referee |
| scope grouping | `OwnerChannelScopeKey` CPU `BTreeMap`; resident owner/resource/scope ordinals | **already proved** | compare multi-scope canonical products under permutations |
| requested totals | frozen checked-`u64` oracle; 14.4 software-wide pair/Q149 proof | **already proved** | exercise zero/full/short/max request sets |
| integer base grants | frozen CPU oracle and 14.4 CPU/GPU exact division | **already proved** | compare neutral products to the frozen grants |
| exact fractional remainder order | frozen descending exact remainder then claimant id; 14.4 exact limbs | **already proved** | retain unequal-remainder and planted arrival-order RED cases |
| generation-rotated exact ties | `ClearingRemainderAuthority`; 14.4 successive-generation witness | **already proved** | compare consecutive generations through CPU and resident execution |
| grants plus unresolved `U` | frozen result/grant construction; canonical `ResidentConstrainedProduct` | **already proved** | compare `(granted, unresolved)` exactly and carry `U` into N+1 once |
| replay | 13.6 stamped replay and 14.2 canonical plan/replay digest | **needs composed witness** | replay identical neutral inputs and compare exact transcript/products |
| pressure-informed lawful drift | 14.3 born Gu-Yang `F`, raw `P`, direct `AllocatedFlow`; 14.4 exact numerator basis | **needs composed witness** | prove both pressure sources alter exact grants while neutral bases retain parity |

No positive item is semantically missing. In particular, parity is a neutral
degeneration only: replacing live `AllocatedFlow` with request proportions in
all cases would contradict the graduated 14.3/14.4 authority.

## Pre-edit negative/error archaeology

| Row | CPU/origin authority | Resident classification | Dispatch-base classification | 14.5 action |
|---|---|---|---|---|
| duplicate supply | `DuplicateSupply` | one `available` value per claim/scope; inconsistent repeats are `InconsistentSupply` | **already typed / needs equivalence witness** | assert the two fail-closed admission shapes |
| missing supply | `MissingSupply` | unrepresentable after resident claim construction because `available` is required | **already sealed / needs witness** | prove CPU refusal and resident constructor shape |
| duplicate claim | `DuplicateClaim` | `DuplicateSemanticTarget` or `DuplicateLogicalClaim` | **already typed / needs equivalence witness** | cover both resident duplicate axes |
| missing claim | a supply with no claim is a lawful empty scope | an empty claim vector produces no `T_s` rows | **already lawful / needs witness** | prove zero semantic products and no invented claim |
| invalid score | `InvalidScore` for non-finite or negative EML output | score cannot enter the resident plan; only admitted precedence remains | **sealed-unreachable / needs witness** | run NaN/+inf/negative CPU rows and show no resident score field/door |
| invalid allocation | n/a before resident live values | `InvalidContinuousAllocation` on CPU mirror and GPU readback | **already typed / needs full matrix** | cover NaN/+inf/-inf/negative and recovery with no exposed partial vector |
| duplicate authored data | `DuplicateAuthoredData` | upstream of the sealed resident product | **already typed / needs witness** | exercise the ordinary reduced-channel door |
| missing authored data/source | `MissingAuthoredData` / `MissingDemandSource` | upstream of resident admission | **already typed / needs witness** | exercise both ordinary door refusals |
| demand mismatch | `DemandDoesNotMatchReducedClaim` | upstream of resident admission | **already typed / needs witness** | exercise scope/quantity mismatch |
| arithmetic overflow | `ArithmeticOverflow` | `ArithmeticOverflow` | **already typed / needs matrix witness** | compare unresolved-total overflow and index/dispatch admission refusal |
| zero request | silently omitted by the frozen CPU law | omitted by resident plan construction | **already equivalent** | prove no zero-grant row is materialized |
| canonical signed zero | EML score canonicalizes `-0.0` to `+0.0` | Q149 allocation decodes both zero signs to exact zero | **already lawful / needs witness** | compare `+0.0` and `-0.0` transcripts/products |
| canonical result order | scope key then claimant id | resident scope key then claimant id | **already proved** | compare exact vectors under logical/physical shuffles |
| no partial writes | CPU returns `Err` before a result vector | resident readback returns `Err`; scratch remains private | **needs composed witness** | fail after mixed valid/invalid rows, then rerun valid input on the same buffers/session |
| provenance/seal equivalence | private `ConstrainedGrantSeal` | private `T_s` fields plus plan digest and per-tree buffer owner | **already sealed / needs witness** | prove foreign plan/buffer rejection and unforgeable/adapter REDs |

No error row requires a new error ontology, normalization, clamp, fallback, or
partial semantic result. “Missing claim” is not silently repaired: it is the
frozen lawful empty-scope case on both sides.

## Recursive, multi-tree, and qualification archaeology

| Obligation | Existing substrate | Classification | 14.5 proof shape |
|---|---|---|---|
| at least three recursive edges | 14.3 direct `AllocatedFlow(N)` self-consumption; 14.4 one canonical `T_s`; 14.3 production N→N+1 demand door | **needs composed witness** | a four-level harness invokes the real plan/settlement substrates per edge, passes the same alias type downward without conversion, and invokes the production demand recurrence exactly once |
| adapter conversion RED | existing `compile_fail,E0308` role-type mutant | **already proved** | retain and make the From/Into settlement mutant explicit |
| seam payload translation RED | type-identity law; no current translator | **needs compile-fail witness** | add a second pinned `E0308` payload-translation mutant at the intake port |
| asymmetric pressure | 14.3 immediate-flow `F` and entitlement-first raw `P`; 14.4 `AllocatedFlow` numerator | **needs composed witness** | feed both graduated pressure cases into exact settlement and show different grants |
| scale/physical invariance | 13.6 matrix; 14.3 scheduling mutant; 14.4 WG32/WG64/partition proof | **already proved / needs expanded corpus** | sweep scopes, bands, ties, supplies, max values, Q149 subnormals, shuffles, both workgroups, and partitions |
| multi-tree independence | 14.2 simultaneous overlapping-id plans/buffers; 14.1 divergent-generation/overlap fixture | **already proved / needs end-to-end settlement** | execute independent resident plans/buffers at divergent generations with overlapping local raw ids and interleaved schedules |
| exact adapter qualification | EXP/LN exact live tuple pattern; `GpuContext` exposes full adapter identity/features | **needs 14.5 certificate** | bind backend, vendor/device/type, driver/runtime, exact features, rustc/wgpu/naga identity, shader hash, WG/subgroup assumptions, and resident ABI into one equality/fingerprint record; mutate every field to invalidate |

The absent live recursive cutover is not a semantic MISSING: 14.6 exclusively
owns production schedule/replay-head cutover and exact seam egress. The 14.5
composition remains a referee over the real graduated substrates and cannot
become an alternate production bridge.

## Implementation and verification

Pending. This section will be replaced with exact-head artifacts, commands,
qualification record, hosted workflow IDs, and routing evidence after the
additive referee is complete.
