# Phase 15 charter — recursive resource filter unification

> **Status: OWNER-APPROVED PHASE CHARTER (2026-09-03).** Owner authorization reopens the
> 0.0.8.7 pointer from NONE into successor Phase 15 of the still-unclosed track. No Phase-14
> graduation fact is altered. Authority: the Owner post-14 handoff as corrected by engineering's
> four dispositions (accept: numbering, temporal recursion, executable oracle-side R,
> root/interior/leaf falsifier, harness sequencing, no-fusion prohibition; amend: (P,F)-per-band
> is a presumption, not a theorem; change: deletion metric = symbol-keyed peer-authority census;
> reject: any modification of the frozen 14.1 comparator). The ladder rows 15.0/15.1 in
> [design_0_0_8_7_rf_arena_modernization.md](design_0_0_8_7_rf_arena_modernization.md) are the
> binding scope; this charter is their rationale and reference formulation. Lifecycle: anchored
> `until:RECURSIVE-RESOURCE-FILTER-UNIFICATION-0` — at 15.1 graduation the surviving accepted
> statement is homed into constitutional prose and these anchors repoint canonical.

## 1. The operator

For every StemThing node v, the candidate operator with two projections:

    R_v : (S_v, Phi_v, {P_c, F_c, U_c, ...}) -> (P_up, {x_{v->c}}, U_v)

with the exact possession boundary kept separate and unchanged:

    T_s = Q(R_v.downward)     — Q is the graduated exact quantizer, frozen.

The target is recognition, not construction: Phase 14 already landed and cut over the
identity instance. `exact market = Q compose R`. There is no peer clearer/disburser.

## 2. The two recursion axes (both are REQUIRED identities)

SPATIAL — the parent's outgoing edge resource IS the child's incoming resource, at both strata:

    continuous:  x_{p->v} = S_v          (14.3 live-column rebind — no propagated copies)
    exact:       Q(x_{p->v}) = T_s(p->v) (14.6 dispatch(None) recursive intake)

TEMPORAL — unresolved quantity re-enters demand exactly once, through the ordinary
Current->Next authority, never same-generation:

    d_effective(v, N+1) = d_authored(v, N+1) + U(v, N)

One operator, two recursion axes: supply recurses downward in space; demand recurses
forward in time.

## 3. Sufficient statistic — presumption, not theorem

PRESUMPTIVE: (P, F) within each already-authoritative precedence class suffices to
reproduce graduated semantics. 15.0 must prove or falsify this BEFORE any richer
representation is admitted. A within-band continuous marginal-response law remains a live
mathematical possibility — notation `A_v(lambda)` (never `D`, which is the graduated demand
product), candidate recursion `A_v(lambda) = a_v(lambda) + sum_u min(C_vu, A_u(lambda + Z_vu))`
— to be adjudicated by archaeology, not pre-eliminated and not pre-adopted. lambda is a proof
variable: presumptively implicit in the graduated normalized share (`x_i = S w_i / sum w`);
no ShadowPrice/LambdaPlane/ScarcityColumn absent proven necessity.

## 4. Standing prohibitions (both rungs)

- Triad exactly once: PALMA impedance and Gu-Yang serviceability appear inside the born
  eligible state OR explicitly in the research objective — NEVER both.
- Hard precedence is orthogonal; no float curvature becomes exact winner-take-all ordering.
- Generation law: sealed Current N -> feed-forward filter -> Q -> N+1. No same-generation
  lambda convergence, reweight, or re-clear.
- No physical fusion work: the existing band pipeline already IS the two projections of one
  dispatch sequence.
- The graduated 14.1 comparator is frozen byte-and-procedure; a composite T_R metric, if
  wanted, derives from existing recorded legs or a NEW Phase-15 artifact.
- No host descendant walk, no economic query engine; subtree questions are answered by
  projection of born state (impairment P - F; later, if admitted, A-differences).
- Root/interior/leaf are the SAME operator under degenerate parameters; dispatch
  Some(rows)/None is a binding distinction, never a second economic operator.

## 5. Executable oracle-side R

15.0's formal statement lands as the CpuVendorizedOracle's reference implementation of R
over the generalized share-vector oracle, driven through the frozen 14.5 parity corpus with
BIT-FOR-BIT identity demanded against the graduated resident path — the spec executing IS
the proof. 15.1 then exposes filter-vocabulary aliases/views (conversion-free, canon sec
1.5.1) over the oracle; the five censused compatibility doors remain quarantined oracle
vocabulary until the existing retirement mechanism fires — they cease to define the
architecture, they are not prematurely deleted.

## 6. The deletion metric

15.1's exit binds to a SYMBOL-KEYED peer-authority census over public/runtime symbols
classified as clearer/disburser/feedback/adapter authorities:

    N_peer_runtime_authorities(after) < N_peer_runtime_authorities(before)
    N_new_economic_authorities = 0        (absent a formally admitted 15.0 MISSING)

Raw prose, allow-file rows, and documentation nouns are NOT the metric — aliases, fences,
and census-protection rows may lawfully increase while architecture shrinks.

## 7. Horizon (explicitly unminted)

RESOURCE-RESPONSE-COMPRESSION-0 exists only behind its four-fact gate: (1) identity response
reproduces AllocatedFlow and exact g bit-for-bit; (2) a second admitted program demonstrates
capability unexpressible by scalar/tuple state without a second market; (3) O(1) resident
state per node per frozen family; (4) a real consumer or measured workload. Absent all four,
the horizon stays deleted. Its absence is a decision, not an omission.

## 8. The accepted end-state statement (15.1 exit)

> A StemThing evaluates one recursively composable constrained-resource filter. Its upward
> projection is the sufficient response of its subtree to scarcity. Its downward projection
> is the resource flow allocated over each child edge. The child consumes that same edge
> resource as its own supply. Exact settlement is the deterministic quantization Q of that
> flow into canonical identity-bearing possession; it is not a second market.

Sequencing: the held harness fix session (positional identity x2, dispatch-window
handoff-status, lifecycle grammar in two parsers) is recommended between 15.0 and 15.1.

## 9. Addendum (Owner-approved 2026-09-03) — the persistence deformation port

The row-11 demotion preserved the authored EML/CostBand/Overlay chain but installed no
deformation hook on the native carry: authored persistence POLICY (decay, expiry,
saturation) became inexpressible. Remedy rung `PERSISTENCE-DEFORMATION-PORT-0` (15.2):
the Current->Next door admits an optional sealed EML deformation program applied to the
carried u inside the single once-mint — absent program = identity, bit-exact; bounded and
typed; authored through ordinary EML so ClauseScript modifier chains regain persistence
policy via the same lowering vehicle. 15.0 gains the authored-persistence census
(consequence-only vs demand-re-injection); re-injection programs migrate to the port.
The law is unchanged: authored deforms; the substrate creates.
