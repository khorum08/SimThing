# 0.0.8.4.8.4.2 — RF Harness (SUPERSEDED — folded into 0.0.8.7)

> **Status: SUPERSEDED / DISSOLVED (Owner ruling 2026-07-26).** This track never opened.
> Its interventions are **[HARNESS]-tagged phases** of
> [`design_0_0_8_7_rf_arena_modernization.md`](design_0_0_8_7_rf_arena_modernization.md)
> — harness/guard work, explicitly NOT SimThing core simulation work:
>
> - **RFH-1 `RF-COLUMN-ADMISSION-GATE-0`** → 0.0.8.7 **Phase 0** (guard, early, non-breaking:
>   `ColumnIndex` constructor taxonomy + scan retarget + DA-gated exclusion edits).
> - **RFH-2 `RF-COLUMN-MINT-MIGRATE-0`** → 0.0.8.7 **Phase 8** (sweep, late: runs once onto
>   the final object-semantic doors after Phase 4; deletes the exclusion list, retires
>   `COLUMN-INDEX-MINT`).
> - Further RF drift interventions the Owner adds slot per-item at commitment:
>   **guards early, sweeps late.**
>
> **The interim standing order remains IN FORCE** (carried in the 0.0.8.7 Phase 0 row):
> `COLUMN-INDEX-MINT` exclusion additions are FROZEN — DA sign-off required; peer-citation
> is not a valid justification.
>
> Root-cause history (exclusion-accumulation erosion, the 18-row list, RF-5's attempted
> peer-citation #19) is preserved in the git history of this file (pre-2026-07-26) and in
> 0.0.8.7 §1's evidence base. Do not dispatch from this doc.
