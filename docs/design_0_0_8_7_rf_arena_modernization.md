# 0.0.8.7 — RF Arena Modernization (the SimThing unification)

> **Status: COMMITTED / OPEN (Owner commitment 2026-07-26).** Track opened via
> `gen_orientation.sh --open`; the active pointer is on this doc. Rung ladder = §3b
> (dispatch from §3b ONLY; §3 phase rows carry the design detail each rung references).
> Absorbs the former 0.0.8.4.8.4.2 RF-harness track ([HARNESS] phases; §5.2).
> Next-Rung pointer governs; ONE rung in flight at a time.
>
> **CORE track.** At completion this is **built INTO THE CORE**: the canonized object model
> lands as constitutional sections of `simthing_core_design.md`, and the **SimThing object is
> complete and distinct** — one concrete, working, GPU-resident simulation kernel.

## 1. North star

Unify the SimThing principle: **a SimThing with resource properties, parented into a tree,
simulates — intrinsically, recursively, on the GPU — with zero scenario-side wiring.** The
recursive RF arena regulating resource economics and the **Field Triad** — Wei's STEAD automata
as spatial heatmaps, PALMA potentials, Gu-Yang saturating flux (P5: one sweep mechanism, three
conservation classes) — ARE the Sim in SimThings; this track makes that a property of the *object*, not of
per-session configuration. Future scenarios — and other games — script onto SimThing trees and
**work**.

**The prize is emergence (Owner, 2026-07-20).** The unification is real and tonal — STEAD is
control economics: the same allocation kernel run over a non-conserved signal commodity, where
stocks are the plant, the field is the controller, the arena-pressure projection is the sensor,
banded commitments are the actuator, and authored weights are the prices. But the unification is
the *rail*, not the prize. **The value this track exists to capture is Wei's emergent behavior**:
fronts, borders, escalations, and strategy arising UNSCRIPTED from one shared local rule plus
authored prices (attractor dynamics — 12.10's `38/38/1` divergence from two weight scalars was an
attractor-relocation proof, the first captured instance). Every phase privileges that emergence
while aligning strictly to the SimThing recursive emission-band RF topology: mechanisms serve the
dynamics and must never script the outcomes they exist to let emerge.

Evidence base: the 0.0.8.6 RF sub-track (RF-1…RF-5A). Every expensive failure was the same
gap — the RF kernel living as session wiring instead of object semantics (engine disengaged
for three eras behind opt-in flags; RF-5A's manual plumbing; the cosplay era only possible
because there was a seam to fake). The substrate ADR's original intent ("one execution model;
allocation is intrinsic") is completed at the object level, now falsifiable because the
executed recursive default (RF-2), the conservation oracle (RF-1), and repaired kernel
contracts (RF-2A) exist.

## 2. Pillars (Owner requirements, binding at commitment)

- **P0 — The Root Contract (Owner constitutional restoration, 2026-07-26).** SimThing is the
  atomic root from which every specialist derives (SessionThing, GridcellThing, PopCohort,
  FleetObject, PlanetaryBuilding, …) — an **interface in the hard-contour sense**, not an
  abstract class: the contract is SHAPE (a dense-matrix row; columns reached only through the
  role pathway; emission bands/thresholds on cells; a parent slot + child-row allocation) plus
  PROTOCOL (the recursive cycle below). The implementation exists **exactly once — the unified
  kernel** — and descendants cannot supply their own: an interface whose every method the
  substrate itself implements. By derivation alone — no wiring, enrollment, or registry — every
  SimThing possesses the root functions: (a) RF arena resolution when it carries resource
  properties + a parent edge; (b) tree-wide effects queryable as GPU sweep reductions (including the P5 Field Triad — influence, potentials, flux — whose outputs are anchored columns);
  (c) EML emission-band threshold branching pulled from the core; (d) dense-matrix residency +
  slotting; (e) **STEAD anchor semantics (Owner fulcrum ruling, 2026-07-26 — the
  un-sidesteppable Consumer Law).** Every resource-bearing property value store is **born a
  STEAD anchor** (default-Anchored: falloff profile + band ladder auto-derived from the
  property class, authoring-overridable); opting out is an authored, diff-visible
  `Unobserved { reason }`, and the dark-cell set is itself a standing board/Studio surface —
  observation is free, deferral is loud ("waiting for consumer" can never again close a turn
  silently; born from the 12.3 walled-disruption failure). Impact is enforced at the two choke
  points derivation cannot avoid: the unified write door **derives** band-crossing deltas in
  the same fused pass (nothing for derived code to remember), and structural ops —
  fission/fusion, table reallocation — **cannot encode** without an anchor-remap section
  (anchor identity minted at admission, stable across slot moves; the field survives topology
  change by construction). All observation reads ONE derived GPU-resident anchor table (band,
  last-crossing generation, urgency, falloff params) maintained solely by admission + the write door
  — data-shaped, never a listener framework. Band ladders are **value-domain-generic**:
  boolean, integer, and float/complex ladder variability is anticipated and exposed as coding
  paths (per-domain EML evaluation paths), never intrinsic scalar assumptions — anything the
  SimThing GPU matrix stack holds gets free auditability, STEAD actionability, and parallel
  evaluation. **Two fences keep the rider lightweight (Owner-affirmed 2026-07-26):
  (i) the matrix stays HOMOGENEOUS lanes — domains are admission-tag interpretations, never
  storage types; wider values (complex, 64-bit) are multi-lane properties via the existing
  layout width machinery; tagged-union / templated / heterogeneous cell storage is a
  constitutional violation (it forfeits the dense-sweep economics the lightweight guarantee
  rests on). (ii) band ladders bind ONLY to ordered scalars — non-ordered domains reach the
  ladder through sealed EML projection stacks (magnitude / phase / component: one comparator,
  N projections; "new domain" = new projection stack at library speed, zero object or kernel
  change). Complex-magnitude projections needing deterministic sqrt route through the
  sanctioned Candidate-F exact-magnitude door (bit-exact; cf. the workshop sqrt-candidate
  track) rather than minting a new float path. Agents reaching for cell-storage polymorphism
  are CAUGHT, not argued with: a heuristic tripwire (Phase 0 ride-along) flags the reach and
  logs it to the reach log — accumulated reaches are the evidence base for any future
  expansion of this ruling.** The STEAD model rides the recursive architecture for free: anchored emission
  reduces upward in the same passes as amounts, ladders are the same threshold registrations,
  urgency the same EML stacks — behavior and observation are one synchronous update (Wei).
  **Specialization is additive-only**: extend, never opt out of, reimplement, or
  shadow a root function (`DefaultDisabled` is the single sanctioned opt-out, authored, never
  kind-implied). **Lightweight guarantee:** a SimThing at rest is a row; core functions are
  shared dense sweeps — cost scales with `slots × columns`, never object-count × dispatch.
  **The recursive cycle (the core protocol):** each SimThing's resource property values reduce
  UPWARD to its parent as a velocity (the GovernedPair rate cell); the parent applies overlay
  modifications; final values disburse back DOWN — and it does the same to all its children.
  One shared rule at every level: the constitutional cycle and Wei's P2 emergence prerequisite
  are the same clause. The cycle resolves **within a single generation as ordered passes**
  (OrderBand), never pipelined across generations. It already executes as
  `RecursiveArenaResourceFlow` — this track binds it to the *object*.
  **The atomic SimThing tick (Owner ruling, 2026-07-26):** the constitutional time unit is
  the **generation** — exactly one run of the reduce → overlay → disburse → STEAD-update
  cycle. Calendar and day semantics, pacing, and scheduling are **front-end cadence
  bindings**, never constitutional: the Stellaris/Clausewitz day-tick is clausething's
  binding (`day := generation` + pacing/calendar), coexisting for now but not locked in.
  The kernel is already calendar-free (gpu: 0 day-refs; kernel: 1 comment); the core's
  residual calendar vocabulary (`evaluate.rs` `day` field, spawn-day) is renamed to
  generation terms as a Phase 1 ride-along. Two **execution postures** over ONE kernel:
  **paced** (front-end-scheduled generation barriers — the game model, default) and
  **continuous** (free-running batched generations; the CPU semantic side is a submission
  pump + observer). Posture is a scheduling policy, never a kernel fork.
  Falsifier: when P1 lands, the synthetic arena-participation wrapper kind is
  **eliminated**, not retained as a compatibility fossil — the wrapper existing was the
  symptom of participation-by-wiring; its absence is the proof of
  participation-by-derivation.
- **P1 — Intrinsic RF kernel at the object.** Resource properties + a parent edge ⇒ arena
  participation **derived at admission** (typed, inspectable, spanned hard-errors;
  `DefaultDisabled` opt-out retained). Behavior when parented and as a parent is intrinsic,
  natural, recursively automatic. Collapse the config seams (ResourceFlowSpec / ArenaRegistry /
  execution profiles / registrations) into the derivation.
- **P2 — Overlays as THE data/settings/directives layer (Owner re-grounding, 2026-07-26).**
  Canonize overlays with a full, **living API surface** — one modification law from authored
  data to operator input, per core design §6 ("every modifier is an overlay; there is no other
  modification mechanism"). The canonized taxonomy, so agents cannot re-interpret it:
  **(1) Capability bestowals** — the Capability Tree contract per `capability_tree_v1.md`:
  a tree is ONE SimThing child of its owner (never a tree of SimThings); entries are
  progress/rate sub-field pairs (GPU-tracked, threshold-detected, **anchored per P0(e)** —
  unlock proximity is observable) plus one **suspended overlay** per entry; unlock = threshold
  crossing → prerequisite gate → `ActivateOverlay` at the generation boundary; **tiered AND
  mutual prerequisites are admission-validated data** (typed DAG — cycles/dangling refs are
  spanned admission errors; the runtime gate check stays boundary work), with mutual
  exclusivity (`max_active`) enforced by same-barrier sibling suspension — atomic at the
  generation. **The effect-target discipline is load-bearing** (ADR
  `capability_effect_target_scope.md`): overlay-prep applies by TREE POSITION, so every effect
  overlay must live on its resolved host (`EffectTarget` Owner / tree / root via
  `overlay_hosts`) — the documented v0 silent-misdelivery trap is CLOSED by canonization, not
  re-documented. Bestowed values with `EffectTarget::Owner` write the **owner SimThing's
  policy/weight columns** — a bestowal IS a standing policy delta at the operator's seat.
  Fission inheritance rides `capability_container_kinds` / `clone_capability_children`.
  **(2) Standing policies** — overlays writing weight columns read by the allocation sweep:
  "a policy IS its numeric pressure on the flow" — never a policy enum, never a branch.
  **(3) Lifecycled transient effects** — `Transient{DissolveConditions}` overlays (property
  thresholds, generation timers, override) over the tree RF evaluation; activation/dissolution
  is boundary-protocol work at generation barriers (under continuous posture, via the Phase 6
  async command queue with logged injection generations), never mid-generation mutation.
  **(4) Operator directives** — `OverlaySource::Player`, structurally IDENTICAL to AI policy:
  **orders are price injections, never command channels.** The canonical example is binding:
  a user-selected destination = a dominant weight overlay (e.g. +10000) on the fleet's need
  columns — the fleet still behaves entirely by STEAD banded commitments, but the price
  dominance makes the outcome undeniable, so it FEELS like a responsive order while remaining
  lawful emergence. Dominance magnitudes are an **authored order-weight class** (sanctioned
  band that dominates ambient prices; finite — never infinities, preserving RF-1 envelopes +
  replay). Directive latency = decision-ingress latency (band crossing at the next
  generation), giving orders their responsive feel by construction.
- **P3 — Specialization protocol.** Specialized SimThings (spatial, owner, session, …) get a
  protocol **richer than a kind enumeration** — but it **must not break existing kind code**.
  Promotion, not rewrite; every rung keeps the existing corpus green (compatibility falsifier:
  full existing test suite passes unmodified at every rung).
- **P4 — GPU-residency intrinsic to the object.** SimThings ARE dense-matrix rows: the
  row/column setup is as intrinsic as RF flow, property-value emission bands, and thresholds.
  **A SimThing knows how to slot into a parent, and how child rows are allocated relative to
  it.** Slot/column identity flows from the object model through the role pathway — never
  minted beside it (consumes the Phase 0 column admission-gate; the Phase 9 sweep then
  migrates legacy sites onto the object-semantic doors this phase defines).
- **P5 — Intrinsic field influence: the Field Triad (Owner PALMA + Gu-Yang elevations,
  2026-07-26).** Field propagation over the lattice is ONE sweep mechanism parameterized by
  semiring — three canonical instances spanning the conservation classes: **STEAD**
  (non-conserved signal, superposition/accumulate — the heatmaps), **PALMA** (selection,
  `(min,+)` relaxation — potential field `D` from impedance `W`; lineage: the PALMA
  tropical-algebra paper, algebra borrowed, ARM implementation explicitly not), and **Gu-Yang**
  (conserved **saturating flux** — per `stead_spatial_contract.md` §7 a conservative-flux
  stencil, never a border/frontline semantic service). Boolean reachability (`(OR,AND)`) rides
  the P0(e) domain rider; future domains = a fourth semiring tag at library speed.
  Resource accumulation on spatial SimThings emits influence falloff as *object behavior* (not
  authored emitters); `W` is a **sealed EML cost projection over observables** (STEAD bands,
  overlay-modified values, RF pressure) — "arbitrary value pathing" = new projection stack +
  semiring tag, zero kernel change. All field outputs are ORDINARY property columns
  (homogeneous lanes) and therefore **born STEAD anchors** per P0(e) — triad outputs are STEAD
  inputs, recursively; STEAD commitments bind to their bands like any emission band (this IS
  the guaranteed STEAD access — no separate access mechanism exists or is needed).
  **Border/chokepoint semantics are DERIVED OBSERVABLES, never services (the Gu-Yang border
  door — positive complement to the withdrawn doctrine):** Gu-Yang's saturation IS the border
  mechanism — opposing conservative fluxes meet and stall, and the stall locus is the front.
  The sanctioned derivations are sealed EML comparative projections over co-located field
  columns: **dominance** (argmax over competing emitter classes + margin top1−top2 — 2-3
  columns regardless of N owners; deterministic authored tie-break), **border** (sign-flip
  band of the margin), **contest/border-pressure** (flux-stall magnitude; both-strong at small
  margin), **chokepoint** (band conjunction: contested border ∧ PALMA low-`D` corridor). Each
  is an anchored column — front-formed/hardened/chokepoint-emerged arrive as sealed band
  crossings for free. Border/contour/frontline SERVICES (tracers, polyline engines, border
  objects) are constitutional violations — polyline extraction is presentation-side
  projection/cache only (§10); a HEURISTIC border-service tripwire rides Phase 0.
  **Default-derived guarantee (the first-order elevation):** spatial specialists ASSUME the
  triad — admitted stencil topology ⇒ PALMA potentials derived; ≥2 admitted competing emitter
  classes ⇒ dominance/margin/contest projections **born, not wired** (default-on like anchors;
  authored opt-out; dark-cell visibility). Movement descends PALMA potentials; fronts are
  Gu-Yang-derived observables — movers are field samplers, never planner clients; all triad
  fields are eventually-consistent (Wei P1 finite information speed), never instant-query
  services (CPU oracles retained for tooling/proof only).
  **The PALMA pathing door (whitelist law):** any quantitative pathing between nodes derives
  through the PALMA semiring field sweep over admitted stencil topology — the SOLE sanctioned
  door, operationalizing §7's "no CPU map planner"; `D` is a field, not a route (no
  predecessors, no path objects). Bespoke graph-search in production crates
  (A*/Dijkstra/priority-queue search) is a constitutional violation; a HEURISTIC tripwire
  (Phase 0 ride-along) flags such reaches to the reach log.
- **P6 — Portability proof.** A domain deliberately unrelated to any shipped scenario (ADR's own arena examples: food /
  research / colony) scripted **purely onto SimThing trees, zero engine code** — and it works.
  The 12.6 second-synthetic-scenario discipline scaled to a whole game.

## 3. Phase ladder (stub; decomposed at commitment)

Phases tagged **[HARNESS]** are guard/CI/lifecycle work — **not SimThing core simulation work**
(Owner ruling 2026-07-26, absorbing the former 0.0.8.4.8.4.2 RF-harness track). They protect and
de-cruft the substrate the core phases are built on, but they are NOT part of the object model
and do NOT land in `simthing_core_design.md` at canonization. Split rule for any further RF drift
interventions the Owner adds at commitment: **guards early, sweeps late.**

| Phase | Theme | Note |
|---|---|---|
| 0 | **[HARNESS]** RFH-1 `RF-COLUMN-ADMISSION-GATE-0` + hygiene prerequisites | **Type boundary (= OC-K2.1a; kernel lane, Owner-gated K2 lineage) — lands NON-BREAKING before core work begins.** `ColumnIndex` constructor taxonomy in `simthing-core`: `new` remains a deprecated public compatibility alias delegating to the fenced raw door; legal doors = layout-derived paths (`PropertyLayout::offset_of` / `col_for_role` / arena-layout ranges) + two doc-fenced choke points (GPU round-trip constructor; fenced raw door for oracle/rehearsal code — existing ~84 call sites keep compiling untouched, only NEW code is steered). Retarget `COLUMN-INDEX-MINT` to the choke-point tokens; DA-gate exclusion edits on the scan. Rides along: execution-status taxonomy `executed / oracle / rehearsal / compile-plan`, board-surfaced; **cell-storage-polymorphism tripwire (P0(e) fence i)** — HEURISTIC scan flagging tagged-union/templated/heterogeneous matrix-cell reaches, logged to the reach log as the evidence base for future ruling expansion; **bespoke-pathfinder tripwire (P5 PALMA door)** — HEURISTIC scan flagging A*/Dijkstra/priority-queue graph-search reaches in production crates, logged to the reach log; **border-service tripwire (P5 Gu-Yang door)** — HEURISTIC scan flagging border/contour/frontline-service reaches (tracers, polyline engines, border objects) in production crates, logged to the reach log. Falsifier: post-rung, the scan proves every remaining `::new` is inside the fence. **Interim standing order (in force NOW, until this phase lands): `COLUMN-INDEX-MINT` exclusion additions are FROZEN — DA sign-off required; peer-citation is not a valid justification.** |
| 1 | Intrinsic RF kernel (P1) | Derived-at-admission participation; config-seam collapse (incl. `min_plus_traversal_field` enable/disable — same session-wiring family); RF-1 oracle + determinism judge every rung; core calendar-vocabulary rename ride-along (P0 generation ruling). |
| 2 | Overlay API canonization (P2: the four-family overlay law) | Canonizes the P2 taxonomy — capability bestowals / standing policies / lifecycled transients / operator directives — as ONE living surface. Concrete obligations: **close the effect-target v0 trap** (ADR `capability_effect_target_scope.md` — host placement + `overlay_hosts` become the admission-checked canonical path; silent misdelivery becomes a spanned error); **admission-validate prerequisite DAGs** (tiered + mutual; cycles/dangling = spanned errors; `max_active` same-barrier atomicity); **generation-align the unlock/activation boundary protocol** (day-boundary language retired per the P0 generation ruling; continuous posture routes through Phase 6's logged command queue); **land the authored order-weight class** (finite dominance band for operator directives — the "+10000 feels like an order" mechanism); capability progress sub-fields anchored per P0(e). Exit-proof: a capability unlock, a policy shift, a timed crisis, and a user destination-order all express as overlays through the one surface — zero bespoke channels — and the misdelivery fixture (effect authored against a host that lacks the property) hard-errors at admission instead of silently landing. |
| 3 | Specialization protocol (P3) | Richer-than-kind, kind-compatible; spatial/owner/session first citizens. |
| 4 | Intrinsic GPU residency (P4) | Row/col + parent-slotting + child-row allocation as object semantics. |
| 5 | Intrinsic field influence (P5: the Field Triad — STEAD + PALMA + Gu-Yang) | Falloff emission + heatmap interaction + Gu-Yang/PALMA consumption; 5.4-5.8 carry the Owner-approved FIELD-SWEEP map/fold remodel (adjacency axis, semiring rung withdrawn — full P5 pillar rewrite lands with 5.5). **STEAD-as-emission-bands (Owner correction 2026-07-20):** STEAD influence on a parent SimThing is READ as **emission-band thresholds** — a banded ladder of threshold registrations on the accumulated cell (existing machinery composed: N band edges = N threshold registrations, each with its own event kind/band id; the RF-5A append-scan proved multi-scan sequencing). Bands bind **1:1 to the falloff influence bands in the heatmap**; intensity (band), velocity (banded thresholds on the GovernedPair rate cell), and magnitude (value) are all observable/measurable through existing property mechanisms. **Quantize the READING, never the FIELD**: falloff superposition + conservation math stay continuous; bands are the observation/decision/coupling surface only — band edges must not enter accumulation math (replay determinism + RF-1 envelope). **Implements the P0(e) STEAD fulcrums (Owner ruling 2026-07-26):** (1) anchor disposition at admission — default-Anchored, authored `Unobserved{reason}` opt-out, dark-cell board surface; (2) write-door impact — band-crossing deltas derived in the fused mutation pass; fission/fusion + table-reallocation ops refuse to encode without anchor remaps; (3) the derived anchor table as the SOLE observation surface for heatmap/Gu-Yang/PALMA/Studio/telemetry; (4) domain-generic ladders — bool/int/float-complex band evaluation as per-domain EML coding paths. **Implements the P5 PALMA elevation (Owner ruling 2026-07-26):** semiring tag on field registrations (accumulate / min-plus / Boolean over ONE sweep mechanism); `W` = sealed EML cost projection over observables; `W`/`D` land as ordinary anchored property columns (D-field bands emit corridor events for free); promotes `min_plus_traversal_field` from its enable/disable session-wiring posture to derived-at-admission (the P1 kill-list pattern) — the existing GPU-resident dispatch + CPU oracle become the executed instance + judge. **Implements the P5 Gu-Yang elevation (Owner ruling 2026-07-26):** dominance/margin/contest/border-band/chokepoint as sealed EML comparative projections over co-located triad columns (all anchored -> free front events); default-derived for spatial specialists with >=2 admitted competing emitter classes (born, not wired; authored opt-out); deterministic authored tie-break at margin==0; `stead_spatial_contract_guards` + mapgen batteries stay green every rung (contract section 8). |
| 6 | **Generation spine & async transport** (Owner-added 2026-07-26) | Implements the P0 atomic-tick ruling and pre-empts the async hazards of continuous posture. (a) Every emission/threshold event carries an explicit **generation stamp** — today's transport timestamps implicitly via the per-tick CPU readback cadence (a hidden day-tick dependence; `EmissionRecord` carries `reg_idx`+`emit_count` and no time at all); (b) event egress = a generation-stamped **ring/stack with an admission-time backpressure policy** (coalesce-per-band / overwrite-oldest / throttle) — observer lag must NEVER stall or perturb the sim; (c) CPU semantic actions become an **async command queue** landing at the next generation barrier, injection generations LOGGED (bit-exact replay under asynchrony); (d) shadow trees and telemetry read **generation-consistent snapshots** (double-buffered, no torn reads); (e) Field-Triad band events (need, corridor, front, chokepoint) ride the SAME stamped egress — one transport for every observable family. On-device decision machinery (threshold/band crossings, EvalEML warp branching, sealed appends) needs NO redesign — it is already generation-local and CPU-free; this phase modernizes only the egress/ingress transport. Per-subtree multi-rate stepping = `HORIZON-ENTRY(2026-07-26)` — breaks Wei P2 synchrony + replay; observation recursion (per-level anchors disposing into one stamped stream: crossing density per subtree per generation window) is free and encouraged. Exit-proof: N-generation **continuous soak** with forced observer lag — zero sim perturbation, declared backpressure observed, replay bit-exact from the injection log. |
| 7 | **Movement-Front execution** (Owner-added 2026-07-20; 7.6-7.9 carry the Owner-adjudicated intrinsic-overlay semantic closure — anchor `stemthing_intrinsic_overlay_capability.md`; the §22.B field-solver performance program is explicitly NOT minted and remains workshop research) | Fleet movement AUTHORITY: spatial SimThings move along STEAD need/falloff gradients (core §7 automaton; the FIRST full Triad consumer: driven by STEAD need bands, descends PALMA potentials, respects Gu-Yang fronts/chokepoints — all via sealed band crossings, never field reads; legacy swapped labels corrected 2026-07-26) under the ordinary tick — decisions stay threshold crossings, movement state is sim authority. **Decision ingress = sealed band-crossing events from Phase 5's emission bands (OC-K-DECISION-INGRESS-0 pattern) — never CPU branching on raw field/heatmap reads** (the naive-Phase-6 temptation is pre-empted by design). Fills the 12.4 `InTransit` readback seam ("test-private until authoritative movement readback exists"); the 12.5 icon descriptors start expressing real transit with zero icon-layer change (the renderer-seam design anticipates this). |
| 8 | **Combat as RF arena** (Owner-added 2026-07-20) | The axiom made mechanical: combat = RF arena resolution (HP/damage as resource flows). Promote the R6/R6B/R6C dress rehearsals from falsification-oracle to executed default via the proven RF promotion pattern (oracle-first → executed → legacy/rehearsal re-anchored); RF-1-style conservation judging; rehearsals retain oracle role against the executed path. Combat context (where fronts harden, which chokepoints contest) arrives as Triad band observables — consumed via bands, never via border/path services (Triad Doors law). |
| 9 | **[HARNESS]** RFH-2 RF-COLUMN-MINT-MIGRATE-0 (row 9.2) | **Migration sweep (= OC-K2.1b) — deliberately LATE (Owner ruling 2026-07-26): Phase 4 makes row/col identity object semantics, so the sweep runs ONCE onto the FINAL object-semantic doors.** Scope per the DA exclusion audit (2026-07-26, all 18 exclusions dispositioned): **A** law-itself (`column_index`/core `registry`/`accumulator_op*` — the role pathway + fused-kernel plumbing; exclusions dissolve with the Phase 0 taxonomy) · **B** GPU round-trips (~8 files: `arena_allocation_plan`, `resource_economy_compile`, `transfer/emission/intensity_accumulator`, silo/link compiles, `region_field_admission`) → **type the plan structs** (`ColumnIndex` end-to-end; raw u32 only at the single WGSL encode/decode boundary) · **C** genuine violations: `gated_rates` gate evaluation → **authored EML library stack** + role-pathway columns; `first_slice_mapping_runtime` (hardcoded `eml_resource_col=1`/`eml_output_col=4` magic numbers; a hand-rolled P0 recursive cycle) → **deleted, superseded by P0 derivation** · **D** oracles/rehearsals (`cpu_oracle` ×2, R6 family) → fenced raw door (judging independence requires raw mint) · **E** era-0080 (~86 mints) → **already EXCISED pre-track 2026-07-26** (Owner-directed: 18 src + 5 test files deleted, exclusions removed, inventory reconciled). Endgame unchanged: **exclusion list deleted; tripwire retired; full build + RF batteries green.** |
| 10 | **[HARNESS]** Doctrine-CI reconciliation (Owner-added 2026-07-26) | **The rustification CI must reflect the doctrine as it emerged, not as it was.** Standing rule from day one: **doctrine-CI co-evolution** — any rung that changes doctrine carries its scan/anchor/digest/selftest-fixture updates IN THE SAME PR (the graduation-stamp philosophy: reconcile at merge, never batch; the #1434 stale-digest red is the tripwire proving CI notices). This phase is then the residue sweep, not a migration: (a) retire/demote detectors whose watched violation the new type boundaries made impossible (beyond RFH-2's `COLUMN-INDEX-MINT`: audit every scans.tsv row + doctrine anchor against the landed object model); (b) mechanize the NEW gates the constitution created — EML library additions (opcode/stack) route DA-RESERVE like gate-wiring, telemetry-lifecycle binding check, Root-Contract admission-error family coverage; (c) re-point anchors/digests/orientation at the canonized §-numbers; (d) selftest fixtures prove the updated gates fire; (e) **allowlist re-baseline (Owner-added 2026-07-26):** the `scripts/ci/allow/*.txt` door-class records are reconciled to the emerged doctrine — their standing promotion-blockers ("retire when … closed by type-boundary admission") come DUE as each phase lands its boundary, and rows retire in the same PR (co-evolution rule); end-state falsifier: `kernel_surface.txt` (227 authority-exports at track-open) shrinks to the closed post-admission set. **Standing allowlist admission standard (in force from track-open, ahead of this phase):** new entries admit ONLY along the emerged SimThing paths — RF/simulation/resource-evaluation symbols must conform to the Root-Contract RF paths (recursive cycle + EML library doors, never bespoke evaluation exports); row/col symbols only via the Phase 0 constructor-taxonomy standards; event emission listeners / boundary events only via the sealed threshold/band-crossing machinery (threshold registrations, sealed post-RF appends, band-crossing event kinds — the decision-ingress pattern); pathing/border/edge/dominance symbols only via the P5 Triad Doors (PALMA potential sweep; Gu-Yang comparative projections). Non-conformant additions route DA-RESERVE and are refused absent DA sign-off. Falsifier: zero scans watching impossible states; zero new doctrine surfaces unwatched; selftest battery green on the reconciled set. |
| 11 | **Embedder's Interface — the Vendor Door** (Owner-added 2026-07-26) | **Short phase.** Formalize the onboarding/entry surface for vendoring the SimThing simulation kernel into ANY domain (network saturation / load-balancing, finance, population models, …) — an easily **human-readable interface inheriting the stem-cell simplicity: five verbs mirroring the object's anatomy, and nothing else.** (1) **Derive** — declare specialists as data (P3 specialization spec: kind/custom + properties; no engine code); (2) **Populate** — build the tree + RF property values; admission does the rest (arena participation derived per P1, anchors born per P0(e)); (3) **Overlay** — the P2 authored data/settings/directives layer; (4) **Bind** — boundary event trees as band→commitment bindings + threshold registrations (incl. Triad bands: need/corridor/front/chokepoint — declaring competing emitter classes in (1) makes dominance/border observables BORN per P5, and admitted stencil topology makes PALMA potentials born), and **CPU shadow tree objects** = the canonical consumer read seam formalized as an embedder-facing handle (coherent per-generation snapshots; strictly read-only observation — decisions stay on-device, never a second authority); (5) **Run** — one standard initialize / start / tick / serialize lifecycle (bit-exact replay included) with an **execution-posture choice: paced | continuous** (P0 atomic-tick ruling; same kernel, scheduling policy only). **Scale-invariant by construction:** the same five verbs for one SimThing or a tree of thousands — a tree is a node with children; any scale-dependent special case in the interface is a defect. **`simthing-clausething` is repositioned as ONE front-end** that lowers to this interface; other domains write specs directly or bring their own front-ends — the interface is the layer clause authoring already lowers into, formalized and documented, not a new subsystem. Deliverables: (a) the thin API surface (existing admission/hydrate/session mechanisms, formalized); (b) a DOC-BUDGET-capped **Embedder's Guide** (cold-start human-readable, owner_authoring_guide lineage); (c) two micro-exemplar specs from non-game domains (e.g. 3-node finance toy; N-node network-saturation toy — which exercises the full Triad natively: congestion impedance → PALMA potentials, saturating link flux → Gu-Yang fronts, load bands → STEAD) that are admission-checked in CI and double as Phase 12's portability seeds; (d) the vendoring crate boundary (which crates an embedder takes, feature-gated). Exit-proof: a cold reader stands up a running, serialized, STEAD-observed tree from the guide alone — zero engine edits, zero scenario-side wiring. |
| 12 | Portability proof + CORE canonization (P6) | A domain unrelated to any shipped scenario works untouched **— scripted through the Phase 11 Embedder's Interface, which this proof exercises end-to-end**; object model — **P0 Root Contract, recursive cycle, and EML-ISA laws included** — written into `simthing_core_design.md` (HARNESS phases excluded — they canonize nothing); SimThing complete and distinct. |

### Phase 5.5 binding amendment — generic field sweep

The earlier P5 phrase "semiring tag on field registrations" is **WITHDRAWN**. Rung 5.5 binds
field propagation to one proof-admitted `FieldSweepRegistration` over authored EML map /
fixed-linear-fold / post programs and an admitted adjacency. "Semiring" is authoring shorthand only,
never a field tag, algebra enum, or operator-identity dispatch. PALMA and Gu-Yang are authored
instances over this mechanism, with canonical order and conservative symmetry consumed at admission.

Rung `FIELD-SWEEP-N4-PARITY-0` is **PROBATION / proof-present / DA-review-pending**. The track pointer
remains on 5.5 until DA clearance; no authority or graduation is claimed by this implementation.

## 3b. Committed PR ladder (Owner commitment, 2026-07-26)

**Cadence and protocol (binding):** ONE rung in flight at a time; handoffs via
`handoff_dispatch.sh` (HD-RECEIPT, projection/ingress caps); the orchestrator owns the
verification tier (exact-head clearance, remands); the DA rules once per relay and stamps
graduation AT MERGE (pointer is machine truth); scenario-born candidate code homes in
`simthing-workshop` (§12); default-delete at closeout stands. Corpus-green law: hosted green ≠
corpus green — GPU/cargo batteries run at the head before graduation.

**PHASE 5 IS REOPENED (Owner ruling, 2026-08-04).** 5.9c and 5.12 each stamped *"PHASE 5
COMPLETE"* and each was superseded — 5.10, 5.11 and 5.12 all graduated after 5.9c's. Those stamps
are historical truth as of their merges and are NOT edited; **the ladder's TODO rows are the live
state, and a completion phrase inside a graduation stamp seals nothing.** Phase 5 reopens for
`5.13 EXACT-CONSUMER-OBLIGATION-0`, which lands the half of the exact-primitive door that 5.10–5.12
left unstated: what a CONSUMER of an exact primitive owes. StemThing-A (6.4/6.5) and everything
fenced behind it — StemThing-B's minting, Phase 7 — **wait on it by Owner ruling**, on the ground
that mechanically guaranteeing `EXP`/`LN` every time is worth the delay.

**Greenfield discretion charter (Owner, 2026-07-26):** much of this ladder reconfigures known
SimThing concepts, but where a rung builds **greenfield surfaces** — the anchor table, the EML
core library, the generation transport spine, semiring tags, comparative projections, the
embedder API — the DA is directed to **seize the opportunity for a stronger, more elegant,
more performant core**. Canonical-simplicity choices within the binding laws need no Owner
ping; anything that would alter a §2/§4 law escalates. The projected anchor library
(`core-0087` domain) pre-seeds this guidance to every coder and orchestrator.

**Coder-lane column (Owner directive 2026-07-26; assignments are DATA per the roles-are-slots
doctrine, revisable without ceremony):** `Frontier — Codex 5.6` (Fable may implement at DA
discretion); `Std — Grok CLI` (`grok-4.5` pinned); `Fable` = DA-implemented harness work.

| Rung | ID | Scope (details in the phase row) | Exit proof | Tier — Coder | Status |
|---|---|---|---|---|---|
| 0.1 | `RF-COLUMN-ADMISSION-GATE-0` | Phase 0: `ColumnIndex` constructor taxonomy, NON-BREAKING (fenced raw door); retarget `COLUMN-INDEX-MINT` to choke-point tokens; mechanize DA-gated exclusion edits. | **DA-GRADUATED / merged [#1447](https://github.com/khorum08/SimThing/pull/1447) @ `4db8ac14`** — ColumnIndex admission doors; scan proves remaining `::new` inside fence; corpus green. | DA-reserve · Frontier — Codex 5.6/Fable | **DA-GRADUATED / merged #1447** |
| 0.2 | `EXECUTION-STATUS-TAXONOMY-0` | Phase 0: `executed / oracle / rehearsal / compile-plan` classification as data; board-surfaced. | **DA-GRADUATED / merged [#1448](https://github.com/khorum08/SimThing/pull/1448) @ `407850e2`** — export-module census; DA mixed-posture primaries (reduction=`compile-plan`, world_state=`executed`; relay `5085410594`). | Std — Grok | **DA-GRADUATED / merged #1448** |
| 0.3 | `CONSTITUTION-TRIPWIRES-0` | Phase 0: the three HEURISTIC tripwires (cell-storage polymorphism; bespoke pathfinder; border service) + reach-log wiring; DA 0.2 primary-class fold + Board/orient pointer repair. | **DA-GRADUATED / merged [#1450](https://github.com/khorum08/SimThing/pull/1450) @ `1b7e2432`** — fixtures fire; INSPECT-only; reach-log lands; `mixed_ruled=2`. | Std — Grok | **DA-GRADUATED / merged #1450** — ruling `5086131411`; `HD-RECEIPT: bf9ece38aea8`. |
| 1.1 | `ROOT-DERIVE-PARTICIPATION-0` | Phase 1: admission derives arena participation from resource properties + parent edge (typed, spanned; `DefaultDisabled` retained); collapse the default-path config seams. | **DA-GRADUATED / merged [#1453](https://github.com/khorum08/SimThing/pull/1453) @ `488ad2c9`** — zero explicit arena wiring; RF-1 + replay referees unedited, battery 108/0. | DA-reserve · Frontier — Codex 5.6/Fable | **DA-GRADUATED / merged #1453** — corpus battery 108/0 at `8333ec56`; `HD-RECEIPT: d0110f30ed12` |
| 1.2 | `SESSION-WIRING-KILL-SWEEP-0` | Phase 1: kill remaining opt-in toggles incl. `min_plus_traversal_field` enable/disable → derived-at-admission; core calendar-vocabulary rename (`evaluate.rs day`, spawn-day → generation). | **DA-GRADUATED / merged [#1456](https://github.com/khorum08/SimThing/pull/1456) @ HEAD `dc53dfa8`** — toggles dead (constructor census 0 tree-wide); referee `mapgen_palma` unedited 19/0; serde aliases exactly `FieldSnapshot.day` + `SimThing.spawned_day` w/ round-trip tests; core day-refs = generation vocabulary | Std — Grok | **DA-GRADUATED / merged #1456** — corpus battery 108/0 reproduced at `dc53dfa8`; `HD-RECEIPT: b2a5237b0aa7`; ingress-cap sticky waived (wrapper-overhead defect → Phase 10) |
| 1.3 | `ARENA-PARTICIPANT-DEPRECATION-0` | Phase 1: eliminate the synthetic participation wrapper, its scaffold, and reserved-gap pools; admitted SimThings host flow properties on their own rows and carry resource-parent membership in the registry. | **DA-GRADUATED / merged [#1459](https://github.com/khorum08/SimThing/pull/1459) @ HEAD `cc29b588`** — ZERO ArenaParticipant refs tree-wide; ledger 53 sites ALL fixed-now (0 deferred); participants on own rows; sparse single-writer INPUT_LIST lowering (§10-conformant, no kernel edits) | DA-reserve · Frontier — Codex 5.6/Fable | **DA-GRADUATED / merged #1459** — battery 112/0 reproduced at `cc29b588` incl. both live-GPU referees; economics fingerprint diff empty; 5-file surface amendment ADMITTED; `HD-RECEIPT: 28d2e8e276f2` |
| 2.1 | `OVERLAY-EFFECT-HOST-ADMISSION-0` | Phase 2: close the effect-target v0 trap — host placement + `overlay_hosts` admission-checked; misdelivery = spanned error. | **DA-GRADUATED / merged [#1461](https://github.com/khorum08/SimThing/pull/1461) @ HEAD `1d09fede`** — misdelivery = spanned admission error (overlay/host/property/scalar-token span); overlay_hosts==affects==physical placement proven; production GPU execution proof (Owner cell bit-exact 6.0); corpus census 7/7 admitted 0 mis-hosted | DA-reserve · Frontier — Codex 5.6/Fable | **DA-GRADUATED / merged #1461** — battery 114/0 reproduced w/ exact adapter-pinned command; remedial referee 3/3; `HD-RECEIPT: dbace3440530` |
| 2.2 | `CAPABILITY-PREREQ-DAG-ADMISSION-0` | Phase 2: tiered + mutual prerequisites as admission-validated typed DAG; `max_active` same-barrier atomicity; generation-aligned boundary protocol. | **DA-GRADUATED / merged [#1463](https://github.com/khorum08/SimThing/pull/1463) @ HEAD `efcb1619`** — prereq DAGs admission-validated w/ spans; max_active same-barrier atomicity proven on the AUTHORITATIVE runtime tree; ship-size hydration fail-closed; existing corpus admits unchanged | Std — Grok | **DA-GRADUATED / merged #1463** — battery 114/0 + atomicity 2/2 reproduced at `efcb1619`; sim observation surface ADMITTED per Consumer Law; `HD-RECEIPT: 739c562571c0` |
| 2.3 | `ORDER-WEIGHT-CLASS-0` | Phase 2: authored finite order-weight class + `OverlaySource::Player` directive path (orders = price injections). | **DA-GRADUATED / merged [#1465](https://github.com/khorum08/SimThing/pull/1465) @ HEAD `c1e2bede`** — typed finite order-weight class; Player directives = ordinary overlays via the single feeder drain gate; canonical TP exemplar: dominance (2.5,27.5)→dissolve→(15,15), causal lifecycle, bit-exact reversibility twin, generation-stamped replay reinjection | Std — Grok | **DA-GRADUATED / merged #1465** — battery 119/0 + order referee 5/5 reproduced adapter-pinned at `c1e2bede`; dominance contract RULED install-envelope-at-admission (orders may be lawfully outbid — emergence, not defect); commitment-state oracle bound to 7.1 exit-proof; `HD-RECEIPT: 1b74667c3200` |
| 3.1 | `SPECIALIZATION-PROTOCOL-0` | Phase 3: the richer-than-kind protocol, kind-compatible (promotion not rewrite). | **DA-GRADUATED / merged [#1467](https://github.com/khorum08/SimThing/pull/1467) @ HEAD `da0dbe4f`** — profiles = typed admission data (KindIdentity; artifact-bound contracts: structural col+row stamps; field-economy policy/weight authority stamp; strict sole-direct-child root); ONE installed canonical report derives all three populations vs independent oracles; authored span provenance both error classes; pre-3.1 wire fixture; full suite UNMODIFIED | DA-reserve · Frontier — **Fable** (Owner routing 2026-07-27: Codex rate-limited) | **DA-GRADUATED / merged #1467** — 3 orch remands discharged; referee 8/8 + driver 119/0 + core 36/0 reproduced; surfaces hydrate_scenario.rs + spec/src ADMITTED; one-tree + §7-placement rulings CONFIRMED; `HD-RECEIPT: 691d281978da` |
| 3.2 | `FIRST-CITIZEN-SPECIALISTS-0` | Phase 3: spatial / owner / session as first citizens on the protocol. | **DA-GRADUATED / merged [#1470](https://github.com/khorum08/SimThing/pull/1470) @ HEAD `156f1570`** — authored `specialization=` on location+entity blocks w/ span capture; OWNER_POLICY_WEIGHT_AUTHORITY out-of-band mint guard (HEURISTIC, both authored formats, reach-log wired); citizen counts via generator (spatial=1500 owner-seat=2 session-root=1) | Std — Grok | **DA-GRADUATED / merged #1470** — new referee 5/0 + 3.1 referee 8/8 UNMODIFIED reproduced; battery 119/0 adapter-pinned (evidence-only delta verified); zero new requirement variants; `HD-RECEIPT: 11ddf57fcac0` |
| 4.1 | `ROW-SLOT-OBJECT-SEMANTICS-0` | Phase 4: parent-slotting + child-row allocation as object semantics through the role pathway. | **DA-GRADUATED / merged #1475 @ 272f82e2** — typed object-issued residency is the sole production structural row door; allocator-derived layouts match the legacy canonical + uneven fixtures exactly; install/AddChild/fission-clone/reparent/tombstone and sidecar rejection referees green; structural file splits landed with `mixed_ruled=0`; DA reproduced full corpus green (core 36/0, kernel 78/0, sim 35/0, driver 119/0/13 adapter-pinned RTX 4080/Vulkan). | DA-reserve · Frontier — Codex 5.6/Fable | **DA-GRADUATED / merged #1475 @ 272f82e2** |
| 4.2 | `PLAN-STRUCT-TYPING-0` | Phase 4: exclusion-audit Family B — `ColumnIndex` end-to-end in plan structs; raw u32 only at the single WGSL encode/decode boundary. | **DA-GRADUATED / merged #1480 @ 6e7fa2b3** — Family B plan/compile intermediates typed end-to-end; production `from_gpu_round_trip` collapsed to `wgsl_encode`; typed-plan + wire-parity referees + seven-arm census green; DA reproduced corpus green (core 37/0, kernel 81/0, sim 35/0, driver 123/0/13 across 64 harnesses, RTX 4080/Vulkan; `mixed_ruled=0`) | Std — Grok | **DA-GRADUATED / merged #1480 @ 6e7fa2b3** |
| 5.1 | `ANCHOR-DISPOSITION-ADMISSION-0` | Phase 5: P0(e) fulcrum 1 — default-Anchored disposition; authored `Unobserved{reason}`; dark-cell board surface. | **DA-GRADUATED / merged #1485 @ 06103edf** — ordinary hydrate/compile/install assigns exactly one typed disposition to every resource property; canonical TP inventory generated from live registry state (`anchored=25`, `unobserved=0`, dark-cell set fixture-only per DA expectation); dark-property reason/span + blank-reason referees green; DA reproduced corpus green (core 37/0, spec 75/0, sim 35/0, referee 6/0, driver 123/0/13 on RTX 4080/Vulkan; censuses PASS, `mixed_ruled=0`). | DA-reserve · Frontier — Codex 5.6/Fable | **DA-GRADUATED / merged #1485 @ 06103edf** |
| 5.2 | `WRITE-DOOR-BAND-DELTA-0` | Phase 5: fulcrum 2 — in-pass band-crossing derivation; structural ops refuse to encode without anchor remaps. | **DA-GRADUATED / merged #1488 @ d41a079b** — typed BandCrossingDelta from the fused pass; refuse-to-encode law exact (fabricated remap_not_required spans vs independently derived keys; omitted/extra/duplicate/wrong-endpoint reject pre-encode); same-cell two-edge GPU referee replays bit-exact; cfg(test)-aware census + selftest landed (remand 4); DA verified census/selftest/write-door true-exit 0, referee 10/0, driver 133/0/13 across 65 harnesses on RTX 4080/Vulkan | Std — Grok | **DA-GRADUATED / merged #1488 @ d41a079b** |
| 5.3 | `ANCHOR-TABLE-SURFACE-0` | Phase 5: fulcrum 3 — derived GPU anchor table as the SOLE observation surface; consumers repointed (substrate scope; canonical live-host cardinality deferred to 5.3b). | Studio/telemetry read only the table; no second observation path (grep-proven); populated-fixture + production-sequence referees green; canonical install inventory exactly `25 Anchored / 0 Unobserved` with exactly `0` live loci (honest baseline for 5.3b); **DA-GRADUATED / merged #1491 @ d9544c52** — DA verified censuses true-exit 0, referee 13/0, studio 10/0, driver 146/0/13 across 66 harnesses on RTX 4080/Vulkan; two pre-existing legacy 0.0.8.3 doc-guard failures verified reproducing at base (not chargeable; routed to corpus debt) | Std — Grok | **DA-GRADUATED / merged #1491 @ d9544c52** |
| 5.3b | `CANONICAL-ANCHOR-MATERIALIZATION-0` | Phase 5: admission governs existence — host-materialization from value-placing relations only; hostless admitted properties declare `Unobserved{reason}`; no fixed Anchored/Unobserved count target; ordinary unmutated install proves TOTALITY | **DA-GRADUATED / merged #1500 @ 1294cc87** — admission governs existence; totality proven on the ORDINARY unmutated install (packs + overlays enabled): every admitted Anchored property has >=1 live host, derivation set empty, no repeated (thing, property), lawful multi-host preserved; derived inventory published (Anchored=18 / Unobserved=7) as result not target; DA reproduced core 47/0, sim 27/0 (one PRE-EXISTING failure carved out to 5.3c), 5.3b referee 4/0, unmodified 5.3 table 13/0, Studio 10/0, driver 150/0/13 on RTX 4080/Vulkan, three censuses true-exit 0 | Std — Grok | **DA-GRADUATED / merged #1500 @ 1294cc87** |
| 5.3c | `THRESHOLD-EVENT-REGRESSION-REPAIR-0` | Phase 5: repair the GPU threshold-event regression introduced by 5.3 (DA bisect: PASS at `d41a079b`, FAIL at `d9544c52`) — `s6_threshold_events_match_cpu_golden` reads back ZERO events where the golden expects one. Determine whether the emission path regressed or the direct-drive fixture's preconditions changed under 5.3, then repair the cause (never the golden). Evidence MUST include the full crate matrix: core, kernel, sim, driver, mapeditor. | **DA-GRADUATED / merged #1507 @ 462cc794** — root cause was the 5.3 convenience-dispatch dropping the `prepare_threshold_scan`/`finish_threshold_scan` lifecycle around the fused encode/submit; repair is that two-call restoration, golden and event payload unedited. DA reproduced at exact head: s6 green, write-door 10/0, anchor-table 13/0, materialization 4/0, sim suite green, detachability PASS (production=0), expiry PASS (expired=0), and the `--rungclose` gate itself verified intact through the reconstruction. | Std — Grok | **DA-GRADUATED / merged #1507 @ 462cc794** |
| 5.4 | `FIELD-SWEEP-IR-PROBE-0` | Phase 5: workshop-leaf disposable probe — minimal target/neighbor-ctx EML map/fold interpreter over a gather; publishes the measurement surface (adapter, adjacency kind, theater size, degree distribution, node count, actual stack depth, column reads/edge, time/sweep, edges/s, resource class) and re-derives current EML cap facts as ground truth; the N8 case runs on a THROWAWAY workshop gather (engine N8 stays 5.6 — accepted scope, lane held Std with eyes open); fallback pre-named (MIN × INPUT_LIST, PRODUCT × INPUT_LIST + banded flux). | **DA-GRADUATED / merged #1526 @ a895c989** — HONEST INDETERMINATE PROBE. Parity ABSOLUTE and met: N4 CPU+GPU bit-exact vs the existing PALMA and Gu-Yang paths on inline synthetic state. Cap facts corrected and published: `MAX_EML_TREE_NODES=32` with observed per-tree max 9; `EML_STACK_MAX=32` with **observed peak operand stack 3** (a 10x oversized ceiling — direct input to 5.7 resource classes). Threshold NOT adjudicated: occupancy/stall counters are unreachable through admitted public doors, so the tiered gate as authored was unsatisfiable — a DA gate-design defect, not a coder failure; the coder's counter-surface STOP and refusal to infer memory-shadow from timing were correct. Diagnostic dispatch-median ratios published: PALMA-shaped ~2.271x, Gu-Yang-shaped ~12.666x (generic 2 dispatches vs bespoke 1 — structural, not an occupancy artifact). Threshold adjudication and the counter requirement REASSIGNED to 5.7. Born mortal as directed: workshop `tests/support` only, no library export, no engine reverse dependency, no corpus read; rows AUDIT/ledger-only, birth track 0.0.8.7, dsu=0; detachability 0/0/0. | Std — Grok | **DA-GRADUATED / merged #1526 @ a895c989** |
| 5.5 | `FIELD-SWEEP-N4-PARITY-0` | Phase 5: engine landing — EML edge context {target_slot, neighbor_slot, accumulator, edge_scalar, dt} + `TARGET_VALUE`/`NEIGHBOR_VALUE` opcodes under the EML growth law; `FieldSweepRegistration {adjacency, map_program, fold_program, identity_bits, post_program, FieldLawProof, CanonicalOrderProof, resource_class}`; `FieldLawProof` for conservative folds REQUIRES an undirected-symmetry certificate of the adjacency (conservation is a contract, not a compiler accident); `resource_class` defaults to the single legacy class (fixed 32-stack interpreter) and admission accepts ONLY that default until 5.7 defines classes; fixed linear fold order; grid offset-table order pinned as canonical authored data; `SEMIRING-FIELD-TAGS-0` WITHDRAWN in this diff (replacement registration with sealed proof metadata); P5 pillar + STEAD contract §10 amendment + `stead_spatial_contract_guards` co-evolve in the same PR.; land the permanent `FIELD-SWEEP-SINGLE-PATH` doctrine tripwire (grep-class, Track-A shape), two arms: (a) no algebra enum, field-kind tag, or operator-identity `match` in the sweep path; (b) new field-kernel `.wgsl` admitted only against the retiring seven-shader allowlist, which EMPTIES at 10.1 so any later bespoke field kernel FAILS the scan | **DA-GRADUATED / merged #1529 @ 4ab19613** — THE REMODEL'S CENTREPIECE LANDS. PALMA and two-stage Gu-Yang run through the ONE generic Grid-N4 EML map/fixed-linear-fold/post interpreter and are **bit-exact CPU and GPU** against the retained bespoke oracles, which are unmodified. DA-verified independently: zero algebra enum/tag/operator dispatch anywhere in the sweep path (drift-detector 7 satisfied); `SEMIRING-FIELD-TAGS-0` withdrawn in-diff; sealed `FieldLawProof`, `CanonicalOrderProof`, and `UndirectedSymmetryCertificate` present; `resource_class` pinned to the single legacy fixed-32 class until 5.7; typed `ColumnIndex`/`SlotIndex` through registration with raw encoding only at the WGSL frontier; session binding sealed (WENS→NSEW mismatch rejects pre-dispatch). **The permanent `FIELD-SWEEP-SINGLE-PATH` law is FALSIFICATION-PROVEN, not asserted**: DA planted an eighth bespoke field shader and the SHADERS arm flipped PASS→FAIL naming the exact file; the ALGEBRA arm stays green. P5 + STEAD §10 + guards co-evolved in the same PR per the co-evolution rule. Reproduced on RTX 4080/Vulkan: parity 3/0 (incl. `palma_and_gu_yang_are_bit_exact_cpu_and_gpu`), STEAD guards 3/0, full driver 48/0/1, detachability 0/0/0, expiry clean, drift PASS. | DA-reserve · Frontier — Codex 5.6/Fable | **DA-GRADUATED / merged #1529 @ 4ab19613** |
| 5.6 | `FIELD-ADJACENCY-GENERATORS-0` | Phase 5: adjacency as a registration axis — weighted `GridOffsets [(dx,dy,w)]` with N4/N8/radius-r presets (N8 diagonal weights AUTHORED, not free) + LinkGraph over the existing INPUT_LIST; per-node weighted-degree/conductance χ certificate at admission (χ_i · Σ_j abs(c_ij) ≤ admitted bound); LinkGraph CanonicalOrderProof basis = the link compiler's existing sorted+deduped undirected neighbor lists; scheduling buckets are degree-homogeneous but NEVER author the physics and NEVER reorder a node's own neighbor list. | **DA-GRADUATED / merged #1532 @ f95eb261** — THE ADJACENCY GAP IS CLOSED. One authored map/fold/post EML program now runs over weighted `GridOffsets` (N4, explicitly-weighted N8, radius-r) AND `LinkGraph`, through one kernel-owned generic interpreter. EMERGENCE FALSIFIER MET, not waived: `same_authored_field_law_emerges_as_diamond_octagon_and_link_topology` proves N4 Manhattan diamond vs authored-half-weight-diagonal octagon vs remote link topology, and a **planted N8→N4 alias flips the verdict red** — the axis is live, not cosmetic. Scheduling never authors physics, PROVEN: `all_adjacencies_are_full_buffer_bit_exact_across_natural_bucketed_and_gpu_execution` plus `link_graph_admission_and_conductance_ignore_degree_schedule_as_physics`; the per-node conductance certificate is bound to the exact authored adjacency. DA-dispatched additions landed and BITE: a **1,025-slot LinkGraph** executes (asserted `> 1_024`), and the permanent `FIELD-SWEEP-DENSE-CAP-CROSSING` arm — DA planted `REGION_FIELD_MAX_CELL_COUNT` on the field-sweep path and it flipped PASS→FAIL naming file, line and constant. Dense-theater caps can no longer reach sparse adjacency. `FIELD-SWEEP-LEGACY-CALLERS` arm green with its own planted red: zero compiled production callers of the seven legacy operators, which survive as test-only referees. Corpus Boundary restored in-rung — the link-compiler basis proof is minimal inline synthetic, zero game-scenario `include_str!`. Remand 2 closed a CPU/GPU authority asymmetry: transient reads now bind the actual sealed producer certificate, not mere shape. Reproduced RTX 4080/Vulkan: adjacency 7/0, parity 3/0, STEAD guards 3/0, driver 55/0/1, gates 0/0/0. | DA-reserve · Frontier — Codex 5.6/Fable | **DA-GRADUATED / merged #1532 @ f95eb261** |
| 5.7 | `EML-RESOURCE-CLASS-ADMISSION-0` | Phase 5: specialized interpreter resource classes (pipeline-constant stack sizes) and/or JIT-backed classes; universal node cap replaced by a measured resource-class budget ONLY once classes are measurable; `ExactPrimitiveAdmission` door — sovereign determinism key (specified bit semantics, domain/special-value policy, exhaustive reference artifact, supported-backend replay artifact) AND conjunctive cost key (resource class); at most one proven primitive iff a candidate with a concrete consumer exists (none promised; sqrt rides the Candidate-F track through this door when it lands).; the door's `domain_policy` admits BOUNDED-DOMAIN primitives — a smooth gate proven exhaustively over a fixed f32 range is tractable where general-domain exp/ln (range reduction, special values, cross-backend proof) is not, and is the cheapest first customer for the door | **DA-GRADUATED / merged #1537 @ a7c05715 — substrate accepted; Gu-Yang recorded as MEASURED PERFORMANCE DEBT with a named cause and remedy, NOT a failed branch.** Landed: one closed `EmlResourceClass` authority (`CompactStack4`, `LegacyFixed32`) with deterministic smallest-fit; canonical postfix-IR-to-WGSL SSA JIT + cache keyed by sealed class and complete program identity; CPU-oracle / interpreted-GPU / JIT bit identity across the admitted census; sealed `ExactPrimitiveAdmission` with ZERO admitted primitives; no algebra/operator/scenario compiler branch; LinkGraph still sparse and unbounded. Measured RTX 4080 / Vulkan 1.4.329: **PALMA PASSES 1.0891x median and 0.7996x worst — the generated kernel is FASTER than bespoke at worst case**, which is the proof that the JIT approach works. Gu-Yang fused generated 140.4us vs bespoke 29.5us = 4.7593x median / 5.4648x worst, FAILING the inherited <=1.25x / <=1.5x gate. **The threshold is NOT weakened and no case was averaged away.** DA verified the cause first-hand: the generated WGSL performs naive per-edge GLOBAL gathers — zero `var<workgroup>` / `workgroupBarrier` anywhere in the JIT — while a bespoke 4-neighbour stencil tiles into workgroup memory and reuses each cell ~4x. That factor is almost exactly the observed gap. **Remedy is named and safe:** the JIT knows the offset table at compile time, so it can emit the same tiled loads; tiling changes WHERE values load from, never the ORDER they fold, so canonical per-node fold order and bit-exactness survive. **Nothing is un-shipped by accepting:** `FIELD-SWEEP-LEGACY-CALLERS` is green — production already has ZERO compiled callers of the seven, so the generic path is what ships today and the bespoke Gu-Yang is a retained oracle. Debt routes to `FIELD-SWEEP-TILED-GATHER-0` with the unweakened gate as its exit. Reproduced: resource-class 2/0, adjacency 7/0, parity 3/0, driver 57/0/1, gates 0/0/0. | DA-reserve · Frontier — Codex 5.6/Fable | **DA-GRADUATED / merged #1537 @ a7c05715** |
| 5.8 | `GUYANG-COMPARATIVE-PROJECTIONS-0` | Phase 5: dominance/margin/contest/border-band/chokepoint projections over generic field-sweep outputs; **border = winner-identity change** across canonical adjacency (NOT a sign flip — `top1-top2` is a non-negative MAGNITUDE); stall = gross minus abs(net) via a second authored EML registration; durable authored-order tie-break. **TP witness VOID (Owner-directed purge reconciliation): proof is SCENARIO-NEUTRAL, no corpus load of any kind.** Comparative birth is EXPLICIT and fail-closed here; default-derived birth moves to 5.8b. | **DA-GRADUATED / merged #1540 @ 70119766 — comparative substrate accepted on scenario-neutral fixtures; focused suite 5/0 (RTX 4080/Vulkan) with Grid AND LinkGraph CPU/oracle/GPU parity; exact `top1-top2` margin, border by winner-identity change over canonical adjacency, gross-minus-abs(net) stall from a second generic EML sweep, bounded 3-column census, authored-order tie-break. TP witness RATIFIED VOID — deep-tree pass confirms 0 TP tokens in `.rs`, 0 domain-activity words, 0 allowlist widening, 0 `.clause` reads, 0 engine→authoring deps. Birth EXPLICIT and fail-closed; default-derived birth deferred to 5.8b.** | Std — Grok | **DA-GRADUATED / merged #1540 @ 70119766** |
| 5.8b | `COMPARATIVE-DEFAULT-BIRTH-0` | Phase 5: the two canonical seams 5.8 correctly refused to invent. (A) carry the ALREADY-ADMITTED `FieldAdjacency` / field-plan registration binding from `compile_and_install` into `SpecSessionState` — plumbing an admitted artifact to a consumer, precedented by 5.1's `PropertyAdmissionReport` delivery; NO second topology authority, no inference. (B) derive emitter/PALMA-D/Gu-Yang-U/Gu-Yang-C roles FROM THE ADMITTED REGISTRATIONS themselves — the registration already names its programs and column bindings — with emitter identity and order taken from AUTHORED ORDER. **No new string namespace, no property-name grammar, no role enum.** If the registrations genuinely cannot express a role, STOP to DA; do not invent grammar under remand pressure. **DA AMENDMENT 2026-08-01 (ruling `5154348081`) — SCOPE NARROWED after three correct coder STOPs disproved the DA's own seam rulings on live surface.** (B) as originally written is UNIMPLEMENTABLE and is withdrawn: `output() == Matrix(target_col)` recovers NONE of the triad. **PALMA-D is not in the authored field set at all** (`compile_min_plus_field_sweep` is a separate producer, not a `RegionFieldOperatorSpec`); **Gu-Yang value writes `Matrix(source_col)`, not `target_col`**; and **Gu-Yang conductance writes `FieldSweepOutput::Transient`** while settled 5.8 stall consumes a Matrix column — a SUBSTRATE gap no lookup can close. Deeper reason it is undefaultable: which three fields form a comparative triad is a CONSUMER question, not an authoring fact — a scenario may author ten fields and nothing in the authoring says which three are compared. **IN SCOPE:** the (A) install product, and default EMITTER birth — emitter set from `GameModeSpec.region_fields`, `authored_order` = designer list position, and **`class_id = authored_order as f32`** (the settled struct already documents `authored_order` as the durable key independent of vec/registration order; `RegionFieldSpec::name` is human-facing identity, NOT the dominance-column value — the DA conflated them and was wrong). **OUT OF SCOPE:** triad column identity. `palma_d_col`, `guyang_value_col`, `guyang_conductance_col` REMAIN EXPLICIT as 5.8 graduated them. Emitters scale with authoring and default naturally; the triad is three columns a consumer names once. Forbidden throughout: producer discriminant, operator→role mapping, role enum/tag, string or property-name grammar, hashed or column-derived identity. **RECORDED FOR A SUCCESSOR RUNG, NOT WAIVED:** the Gu-Yang conductance `Transient`-vs-`Matrix` mismatch is the real prerequisite for any future triad defaulting, and it is a lowering change with its own parity obligations. | **DA-GRADUATED / merged #1567 @ 6dfc5aa1 — default emitter birth landed on the DA-narrowed scope.** Ordinary `compile_and_install` mints `FieldPlanAdmissionReport` on `SpecSessionState` from `GameModeSpec.region_fields` (install.rs:437) — no parallel door, no `Some(plan)` enrollment. `class_id = authored_order as f32` exactly as ruled; name is diagnostic only. Triad columns remain EXPLICIT. **DA-verified independently at head `06de05a6`, not accepted on the relay's evidence:** focused referee 11/0, 5.8 Gu-Yang suite preserved 5/0, core 49/0, spec 19/0, workspace lib errors 0, inventory drift PASS, scenario-residue `scenario=0 domain=0`, detachability `0/0/0`. Fences measured on the diff: 0 parallel-install doors, 0 role enums/tags/producer discriminants, 0 defaulted triad columns. **Falsifiability re-demonstrated by the DA planting directly:** perturbing `class_id` off `authored_order` turns the referee RED 3/11 and restoring returns 11/11. Value-column selection computes BOTH forward and reverse iteration, making incidental vector-order independence structural rather than asserted. **RECORDED, NOT WAIVED:** the DA planted the named-forbidden first-Matrix fallback and the referee stayed GREEN. This is NOT a defect — spec admission makes the ambiguous case UNREACHABLE for every admitted variant (`region_field_admission.rs:264` rejects Gradient unless `target_col == output_col`; `:317` rejects SaturatingFlux unless `source_col == target_col`), so a referee for it would test an admission-guaranteed condition, which the handoff template fences as kabuki. **FORWARD HAZARD:** any future operator variant that relaxes either constraint makes that branch reachable with NO referee defending it, and must land one in the same PR. | Std — Grok | **DA-GRADUATED / merged #1567 @ 6dfc5aa1** |
| 5.9 | `TP-PURGE-0` | **Phase 5 hygiene — INTENT NOTED, sequencing Owner-scheduled and MAY preempt 5.4.** Terran-Pirate was authored as a ClauseScript hydration test and has become canonical infrastructure by accretion: production hydration branches on a faction string (`hydrate_scenario.rs` owner == pirate posture select) and carries TP-named production fields (`hydrate_combat_arena.rs` terran/pirate weapon damage); 20 referees plus 5.1's generated `property_admission_inventory.tsv` bind engine admission law to the TP corpus, so retiring TP would today break the engine's own proofs. **This is a Corpus Boundary Law repair, not hygiene.** Purge scope: (a) de-name the production breaches VALUE-PRESERVING with the prescribed shape — `terran_weapon_damage`/`pirate_weapon_damage` (public serde-bound fields on `HydratedCombatArenaPayload`, mirrored in `ParsedCombatArenaPayload`, and authored keys in the corpus) become owner-keyed authored data admitting N sides; the `owner == pirate` posture select becomes a per-owner authored posture with a default; hydrated output for TP stays identical; (b) re-base admission proofs onto a minimal synthetic corpus so TP is one witness among several and never the definition — no engine law stated in terms of one corpus's contents, and no corpus edited to make engine law pass; (c) reap the unleased workshop survivor `crates/simthing-workshop/src/tp_rf_reduce_up_golden.rs` (zero rows in `closeout_artifacts.tsv` — no clock will ever reap it) and the two TP handoff objects still leased under the closed 0.0.8.6 track. **Fix-then-ratchet:** the `WORKSHOP-HOMING-DETECTION` promotion from HEURISTIC to hard-fail is the LOCK-IN and lands at 10.1, never before (a) and (b) — a gate promoted ahead of the debt fails against the debt it exists to prevent. Root cause on record: the escaped code was authored directly into engine crates, so it never entered the workshop leaf and was therefore never subject to the default-delete sweep or to an elevation ceremony; the 0.0.8.5 closeout correctly reaped what WAS contained (six post_hydration modules, #1284). | **DA-GRADUATED / merged #1520 @ 153ba40c** — DA independently reproduced at exact head `9ccd800c`: **THE FALSIFIABLE TEST PASSES — engine 181/0/1 across all six crates with `terran_pirate_galaxy.clause` DELETED**, corpus restored clean. Stage-B arithmetic verified from the map itself: 222 rows = 145 INLINE + 73 REAP + 4 SURVIVOR, 0 CONFLICT; exactly 2 parametrized harnesses, exactly 10 approved cases, each with a demonstrated planted-defect red. JIT determinism case verified live-vs-live in the green posture (`compile_eml_gadget` both sides), mutant only on side B — the assertion is wired to the live compiler, not a replica. Detachability `production=0 proof=0 ceiling=0`; inventory drift PASS; expiry PASS `expired=0`; `hydrate_combat_arena.rs` and the unleased workshop golden confirmed gone. RESIDUE NAMED, NOT WAIVED: 26 `simthing-driver/src` files (14 dress-rehearsal/demo, mostly `*_0080_*`-era) still carry scenario vocabulary, and `simthing-spec/src` carries one `//!` doc-comment mention. Neither couples the engine to the corpus (proven by the corpus-absent run); both are assigned to 10.1 as the PRECONDITION of its `WORKSHOP-HOMING-DETECTION` hard-fail promotion — the ratchet cannot land until they clear. | Std — Grok | **DA-GRADUATED / merged #1520 @ 153ba40c** |
| 5.9b | `SCENARIO-RESIDUE-PURGE-0` | **Phase 5 hygiene — Owner-directed 2026-08-01, DA-implemented, preempts 5.8b.** Completes what 5.9 named and deferred. 5.9 proved the engine is not COUPLED to the corpus (corpus-absent run green); this rung removes the residue that coupling-proof left standing. Measured at `70119766`: `simthing-driver/src` carries **26 TP-bearing files / 23,437 lines = 47.6% of the crate**, with **0 external consumers, 0 references from `main.rs` (the shipped `simthing` binary), 0 rows in `test_inventory.tsv`, 0 imports from `driver/tests/`, 0 entries in `allow/*.txt`**. The internal references form a CLOSED CHAIN (R1 ← R2, R3, R4, R5, R6, R6b, R6c) that feeds only itself. **This is dead scaffolding, not architecture**, and it survived because it is production `src`: only TESTS receive inventory rows, so no clock could ever reach it — structural invisibility, not a scheduling oversight. Scope: (a) delete 25 files and strip 23 `pub mod` lines plus three re-export blocks from `simthing-driver/src/lib.rs`; (b) R6/R6c (5,515 lines) carry ZERO inventory rows, gate nothing, and are already barred as an oracle basis by 8.1 — **no replacement fixture is owed**, because the invariant they gesture at (contested claims against bounded supply conserve) IS `CONTENTION-CONSERVATION-JUDGE-0` over inline synthetic contention; before deletion, grep both for numeric conservation expectations not derivable from the Invariant Set and, if any exist, land them as CASES in 8.1's table, never as a resurrected rehearsal; (c) de-name the single `simthing-spec/src/spec/scenario.rs` doc comment, bringing the whole engine to ZERO; (d) move `simthing-mapeditor/src/terran_pirate_skeleton.rs` to the `simthing-workshop` default-delete leaf and de-name the three remaining editor references to a neutral sample-scenario label — authoring-layer TP is legal under the Detachability Law but is still a disposable rehearsal; (e) `.clause` assets STAY (Owner ruling: external assets like meshes; they influence the corpus only if a proof reads them, already false). **Anti-cruft mechanism (the durable half):** `SCENARIO-RESIDUE` promotes `SEMANTIC-WORDS` from HEURISTIC to hard-fail once (a)–(d) pay the debt — fix, then ratchet — and `DEAD-EXPORT` flags any engine module `pub mod`-exported with zero consumers outside its own crate above a line threshold, catching the SHAPE of this cruft rather than the word "pirate", so the next one is caught whatever it is named. Also repairs the immortality vocabulary this rung exposed: `test_inventory_check.sh` MANDATES that a KEEP row declare a `permanent-residue:*` promotion target (1,020 of 1,038 rows carry one) and `test_lifecycle_expiry_check.sh` carves out `permanent-residue:escaped-bug` — both predate 5.8 and both reconstitute the permanence the Owner abolished. Per the Purge Reconciliation Law this rung enumerates what it invalidates and reconciles every ladder row naming it IN THE SAME PR. Success signal is arithmetic, as in `track_closeout`: **net line count must DECREASE by ~23.4k.** | **DA-GRADUATED / merged #1544 @ 682e9d0e — engine at ABSOLUTE ZERO TP across `src` AND `tests` for all seven crates, and zero domain-activity vocabulary. Net 26,298 deletions.** Deleted 25 driver files (23,437 lines, 47.6% of the crate) after measuring 0 external consumers, 0 `main.rs` references, 0 inventory rows, 0 `driver/tests` imports, 0 `allow/*.txt` entries — and, across all 23,437 lines, **8 asserts and 0 `#[test]`**, those 8 sitting in `pub fn`s nothing calls so they never executed. 124 exported functions, zero external callers: dead scaffolding, not architecture. R6/R6c (5,515 lines) carried zero assertions, so **no replacement fixture was owed** — 8.1 already owns their invariant over inline contention. Deleted 3 TP-born test targets and 7 support fixtures whose only consumers were other support fixtures. **One real coupling found and repaired:** `simthing-spec/tests/scenario_ingestion_admission.rs` read a shipped TP JSON via a pointer file, stating engine admission law in terms of one corpus's contents — it survived 5.9's corpus-absent falsification because 5.9 deleted the `.clause`, not the JSON; re-based onto inline input with falsifiability demonstrated (`World`→`Scenario` red, restored green). `SCENARIO-RESIDUE` hard-fail and `DEAD-EXPORT` advisory both land with a 6-fixture selftest carrying 2 planted defects; a first live run exposed a systematic false positive in the detector itself (module-name matching flags every healthy re-exported module as dead), fixed by following re-exports, 51→21, pinned by a fixture. NOT DONE, carried forward: the permanence vocabulary (`test_inventory_check.sh` still MANDATES a `permanent-residue:*` target on KEEP rows; `test_lifecycle_expiry_check.sh` still exempts `permanent-residue:escaped-bug` on a 40-character note) — measured but not repaired here, and `0.0.8.5-terran-pirate` remains a closed track carrying 65 authoring-layer rows whose reap is due 2026-08-11. | DA-implemented — Opus 5 | **DA-GRADUATED / merged #1544 @ 682e9d0e** |
| 5.9c | `ARTIFACT-PROVENANCE-CONTAINMENT-0` | **Phase 5 hygiene — Owner-directed 2026-08-01.** Enforces the Vendor Containment Law's mechanical corollary and finishes the second of the two normalization pumps. (a) Re-base `FIRST-CITIZEN-SPECIALISTS-0` off `scenarios/terran_pirate_galaxy.clause` (hardcoded at `first_citizen_specialists_0.rs:40`) and off `../simthing-mapeditor/tests/fixtures/tp_base_disc_1500...json`, so `specialization_citizen_counts.tsv` stops publishing "Terran + Pirate policy/weight authorities" and `spatial=1500` into orientation. **BLOCKER DIAGNOSED 2026-08-01 (root cause, not symptom).** A `format!`-built clause reaches `spatial=2 / session-root=1` against the synthetic `scenarios/corpus/minimal_scenario_galaxymap...json` (placements `row0_col0` / `row0_col1`), but `owner-seat` stays 0 against the `owner-seat >= 2` assertion. **`owner-seat` is DERIVED, not authored** — neither TP nor any fixture declares `specialization = owner-seat`, so authoring it is not the fix. `seed_profiles()` requires all three of `Kind(Owner)`, `ParentKind(GameSession)`, and **`HostsAdmittedPolicyWeightLocus`**. `build_authority_root` already satisfies the first two for any scenario-level `owner = {}` block; **the third is what a bare owner block lacks** — the owner must host an admitted policy/weight (owner-silo) locus, and `terran_pirate_galaxy.clause` is the ONLY file in the repository that builds one, which is precisely why TP survives here. **`owner-seat` is genuine core law, not a TP artifact** (the owner-silo locus is platform contract), so the assertion must NOT be weakened and `>= 2` is a legitimate plurality guard. The fix is therefore bounded and unblocked: give the fixture two owners that each host an admitted policy/weight locus, using the SIX synthetic owner-silo witnesses already in `scenarios/corpus/` (`owner_silo_balanced_flow`, `owner_silo_disburse_down_scoped`, …) as the shape reference. Recorded because the earlier note misread this as an unreproducible install path; it is a missing capability witness, and under the Vendor Containment Law the absence of a synthetic owner-silo witness IS the defect. (b) Land the **artifact provenance gate**: every generated artifact consumed by `gen_orientation.sh` or `handoff_dispatch.sh` (19 TSVs today) must be witnessed by a fixture, hard-FAIL if its generator reads `scenarios/`; planted-defect fixture required. This is the gate `SCENARIO-RESIDUE` structurally cannot be — the coupling lives in the GENERATOR while the artifact sits in `scripts/ci/`. (c) Extend `DEAD-EXPORT` to the two shapes it currently misses, both found by hand during 5.9b: a `tests/` target declaring ZERO test functions (`simthing-spec/tests/planet_child_location_admission.rs`, 240 lines, runs `0 passed`) and a `tests/support/` module with no consumer outside `support/` (`simthing-driver/tests/support/resource_economy_session.rs`, 311 lines, reads `scenarios/rebellion_demo.ron`); delete both. (d) Repair the two pre-existing FALSE-REDs on master that CI does not catch: `first_citizen_specialists_0.rs:448` asserts an orientation heading `## Specialization citizens` that `gen_orientation.sh` has NEVER emitted, and 19 `simthing-clausething` test compile errors stand on master while doctrine-scan is green. **DA CORRECTION 2026-08-02 (ruling on orchestrator hold `5157085970`): (a) IS ALREADY DISCHARGED — do not redo it.** It landed as normalization-pump-2 at **`57f89407`**: `first_citizen_specialists_0.rs` now hydrates the crate-local `specialist_citizens_minimal.clause` (two owners each referenced by an `owner_policy_overlay`, which is what stamps `HostsAdmittedPolicyWeightLocus`), the base disc is re-homed to `tests/fixtures/citizens_base_disc.simthing-scenario.json`, and the only surviving mention of the TP clause in that file is a DOC COMMENT recording what it used to read. Verified on master: `specialization_citizen_counts.tsv` publishes `spatial=2 owner-seat=2 session-root=1` from a fixture authority install, with no scenario vocabulary. **REMAINING SCOPE IS (b) AND (c) ONLY.** (b) is the load-bearing half: it is the gate that would have caught BOTH normalization pumps automatically, and neither was caught by a gate — both were found because the Owner asked the right question. `SCENARIO-RESIDUE` structurally cannot see them because the coupling lives in the GENERATOR while the artifact sits in `scripts/ci/`. (c) closes the two `DEAD-EXPORT` blind spots the DA found BY HAND (a `tests/` target declaring zero test functions; a `tests/support/` module with no consumer outside `support/`) — the fact that both required manual discovery is the evidence the gate needs extending. | **DA-GRADUATED / merged #1574 @ 2e3afacb — PHASE 5 COMPLETE.** (b) `artifact_provenance_check.py` follows consumer -> generated TSV -> regenerator -> Cargo proof -> local `mod` closure, and hard-FAILs on any `scenarios/` read, failing closed on unresolved shapes. (c) `DEAD-EXPORT` extended to both ruled blind shapes (integration target with zero test functions; `tests/support/` module unreachable from outside `support/`), advisory census widening 20 -> 58; the two HD-authorized dead files deleted with no replacement ceremony. **DA-verified independently at head `fbd70bea`:** provenance `PASS generated=2 consumers=2 errors=0`, its selftest 3 checks/1 planted defect, residue selftest 10 checks/4 planted defects, `scenario=0 domain=0`, drift PASS, detachability `0/0/0`, lifecycle `expired=0`, core 49/0, spec 19/0, workspace lib errors 0. **The decisive proof, planted by the DA rather than accepted on report: restoring pump-2's ACTUAL `scenarios/terran_pirate_galaxy.clause` read into the generator's proof turns the gate FAIL with a diagnostic naming artifact, source file and line; restoring returns PASS.** The gate demonstrably catches the exact failure mode that produced both normalization pumps — which were found only because the Owner asked, never by a gate. **CENSUS CORRECTION, ACCEPTED:** the rung text said `19 TSVs`; the live census is `generated=2`. The DA verified this is correct SCOPING, not under-coverage — 20 TSVs are referenced but 18 are hand-authored authority tables with no generator and therefore no provenance chain to check. `generated_artifact()` is an OR of the GENERATED header or a `# Regenerate:` line, so a regenerator without a header is still caught. **RESIDUAL HAZARDS, NAMED NOT WAIVED:** a generated TSV carrying NEITHER marker escapes the census, and a generator reading `scenarios/` transitively through another CRATE is outside the local `mod` closure. Both are follow-up surface, not 5.9c defects. The 58 advisory dead-export findings are discovery output and carry NO deletion authority. | Frontier — coder | **DA-GRADUATED / merged #1574 @ 2e3afacb** |
| 5.9d | `TP-EXPORT-ERADICATION-0` | **Phase 5 hygiene — Owner-directed 2026-08-01, DA-implemented.** Eliminates TP's EXPORTED influence under the Vendor Containment Law. Owner framing: the mapeditor surfaces were always meant as **dev TELEMETRY** to observe TP hydration; a future rehearsal will be rebuilt and must never export beyond its own crate. Stage A — mapeditor `src`: delete `tp_base_disc.rs` (256 lines of production code parameterizing the generic `simthing-mapgenerator` profile with one scenario's constants, orphan-exported from `lib.rs` and consumed only across crate lines) and `terran_pirate_skeleton.rs`; **genericize the Studio's per-owner macro gauges**, which named three literal property keys from one shipped scenario (`tp_economy::terran_shipyard_*`, `pirate_outpost_*`) and therefore only worked for that scenario — now driven entirely by whatever keys the loaded install admits, naming none. Stage B — sever the cross-crate fixture reach: 7 ClauseThing tests pathed into `../simthing-mapeditor/tests/fixtures/`, so the fixture is re-homed inside ClauseThing; the two remaining mapeditor proofs are re-based onto synthetic witnesses (`legacy_world_root_minimal.simthing-scenario.json`) because the law they carry — a legacy World-root file loads as a LegacyWorldRoot authority document — holds for ANY such file. Stage C — reap the two workshop TP proofs: both carry GENERIC laws (byte-identical regeneration; need-binding fail-closed) already witnessed scenario-neutrally by `cpu_gpu_parity_matrix_0` and `determinism_matrix_0`, so the laws survive and only the disposable witnesses die. **ELEVATION CANDIDATES — none owed.** Each surviving capability was already generic or already elevated: the disc `GenerationProfile` lives in `simthing-mapgenerator` (the TP constants were pure instance data); the macro gauges generalized in place; both workshop laws are covered by the two harnesses. The ONE forward candidate is Owner-channel semantics — **owners and user seats must become authored SimThings as generic, recursive notions**, since owner channels are how adversarial resource contention resolves; that is 8.1/8.2 `CONTENTION-*` work, now unblocked because `specialist_citizens_minimal.clause` gives the policy/weight locus a synthetic witness for the first time. **Assets untouched**: `terran_pirate_galaxy.clause` and its siblings remain, as `.png`-equivalents that ClauseThing must keep ingesting natively. | ZERO files matching scenario vocabulary anywhere outside `simthing-clausething`; mapeditor `src` and `tests` build and pass with no shipped-scenario read; no engine or authoring crate reaches across a crate boundary for a scenario fixture; inventory drift PASS with every deleted proof's rows reaped; no assertion weakened. | DA-implemented — Opus 5 | **DA-GRADUATED / merged #1550 @ 24df4166** |
| 5.10 | `EML-PRIMITIVE-DOMAIN-0` | **Phase 5 completion — Owner-approved 2026-08-03, anchored by `docs/full_eml_unification.md` (DESIGN-ADMITTED after two decorrelated DA reviews; §10 is the integration plan and governs).** Generic door machinery only — NO primitive admitted by this rung: sealed `PrimitiveDomain { min_bits, max_bits, special_value_policy }` on `ExactPrimitiveAdmission` (the current two-variant `ExactPrimitiveDomainPolicy` cannot express an interval); call-site obligation discharged by exactly one of TWO ADMISSION SHAPES with the distinction enforced as semantics — (1) **range-certified input** (existing sealed property/sub-field range guarantee; zero runtime cost; certificate verified) or (2) **explicitly guarded semantics** (the author's `CLAMP_BOUNDED`/`MAX`/`SELECT` IS the authored law — saturated exponential — never a validity proof of the unguarded value; the `LN(min_normal)` masking hazard is the reason the shapes are distinct). No interval analyzer — EML inputs are runtime columns; a full prover is heavy, conservative, unnecessary. | **DA-GRADUATED / merged #1618 @ 35dcf43a — the exact-primitive door lands admitting ZERO primitives.** Sealed type lands; an unguarded, uncertified call site is a spanned admission error with a planted-defect red; a clamp recorded as shape-2 semantics never satisfies a shape-1 requirement (planted-defect red); zero runtime cost on shape-1 paths demonstrated; no primitive admitted. | DA-reserve · Frontier — Codex 5.6/Fable | DA-GRADUATED / merged #1618 @ 35dcf43a |
| 5.11 | `EML-EXP-PRIMITIVE-0` | Phase 5 completion: **full-domain `EXP` `[−87.33, +88.72]`** through the 5.10 door — the first admitted exact primitive, ending the transcendental deferral C-8 left conditional. Pinned algorithm is the reference semantics (algorithm-as-spec; semantics append-only — any future change is a NEW primitive name, never a mutation); CPU twin implements the identical sequence; **exhaustive 2³² digest per certified (compiler, backend, driver) toolchain with automatic invalidation/requalification on trust-chain change** — certification is a phase-boundary LOCAL act with pinned artifacts (Candidate-F pattern), never CI re-execution; scans verify artifact presence/freshness only. Anti-reassociation devices (bitcast fences) are implementation aids retained only where measured necessary and free — the observable invariant is the law. `div` excluded from internals; interpreter arm + JIT straight-line lowering arm; `CLOSED_OPCODES` widening is the DA-scoped vocabulary change riding this landing; scan/allowlist co-evolution in the same PR. Consumers: CostBand smooth steering (6.1b, landed) + authored exponential STEAD falloff law. | **DA-GRADUATED / merged #1632 @ d664a866 — the first admitted exact primitive.** **Cost gate unweakened: the primitive must beat its own ~20-node `ADD`/`MUL`/`FLOOR` gadget encoding under the existing exact-primitive cost key (no regression + ≥1 strict improvement)**; exhaustive digest green per supported toolchain; CPU/interpreted-GPU/JIT bit identity; stabilized gadget forms land (`Logistic` sign-stable, `SoftmaxWeight` as `EXP(β·(zᵢ − max z))` — naive form rejected); a planted fused-multiply mutant reds the digest. | Frontier — coder | DA-GRADUATED / merged #1632 @ d664a866 |
| 5.12 | `EML-LN-PRIMITIVE-0` | Phase 5 completion: `LN` over **positive finite normals `[2⁻¹²⁶, f32::MAX]`** by the 5.11 template — same artifact set, same gates, same append-only semantics law. Extended-real conventions (`ln(0) = −∞`) REJECTED, not emulated; call sites carry shape-1 certificates or shape-2 guards per 5.10, with the shape distinction load-bearing here (a silent clamp masking `x ≤ 0` is exactly the hazard shape-2 recording exists to surface). Completes the generating pair: `POW(x,a) = EXP(a·LN(x))` lands as a gadget (no third opcode) and **`eml(x,y) = SUB(EXP(x), LN(y))` lands as a three-node library entry — Anchor B becomes literal**. Consumers: `LogAccumulate` (a NEW authored numerical law riding the Sum lane's existing discipline — never a semantics-preserving Product optimization; no fold reordering anywhere), `PowerLaw`, entropy diagnostics. | **DA-GRADUATED / merged #1637 @ 97b5b399 — PHASE 5 COMPLETE; the second admitted exact primitive.** Digest + parity + cost gate per the 5.11 template; `POW` and `eml()` library entries admitted with domain guards; `LogAccumulate` documented as its own law with a planted-defect red on any use as a Product substitute on a conservation path. **GRADUATION RULING REVISED (Owner ruling 2026-08-04) — the graduation STANDS, the completion claim does NOT.** `LN` is admitted, exact, and four-arm bit-identical; that finding is unchanged and was independently verified. What the phrase *"PHASE 5 COMPLETE"* asserted beyond it was wrong: **the door 5.10 opened has two sides, and 5.10–5.12 built only the producing side.** `ExactPrimitiveConsumerEvidence` carries a cost measurement and zero exactness evidence, so a consumer may lose the bit-exactness these three rungs proved while admission stays green — and 5.11's own STEAD falloff consumer ALREADY DOES, at ≤1 ULP on the SSA-JIT arm, which this ruling recorded as triaged residue rather than recognising as the first instance of a class. **The DA under-read it: a residue that is EXP-free and one call downstream of a bit-exact primitive is evidence about the DOOR, not about the primitive.** Phase 5 is reopened for `5.13 EXACT-CONSUMER-OBLIGATION-0`, which mints the consuming side and repairs the falloff seam in the same rung; 5.13's stamp carries the completion phrase, and this one is superseded. | Std — Grok | DA-GRADUATED / merged #1637 @ 97b5b399 |
| 5.13 | `EXACT-CONSUMER-OBLIGATION-0` | **PHASE 5 REOPENED (Owner ruling 2026-08-04) — the exact-primitive door's missing half.** 5.10 mints the door; 5.11 and 5.12 walk exact values through it; **nothing states what the RECEIVING side owes.** `ExactPrimitiveConsumerEvidence` (`crates/simthing-kernel/src/eml_opcode_gate.rs`) carries exactly one field — `measured_threshold_excess_bps`, a COST measurement — and **zero exactness evidence**, so a consumer may silently lose the bit-exactness its primitive proved while admission stays green. Mint the obligation per §4's Exact-Value Provenance Law: **a consumer of an exact primitive INHERITS NOTHING** — it DECLARES whether it is exact-bearing, and an exact-bearing declaration carries its own cross-arm digest over its own admitted domain; declaring without that evidence is an **admission hard-error**, never a review finding. Enumeration of consumers is **TREE-DERIVED**, never curated, and publishes a reconciliation line. **DA review 2026-08-05 — the two halves of that enumeration sweep DIFFERENT surfaces and conflating them makes it circular.** The type-walk over `ExactPrimitiveConsumer` enumerates only consumers that ALREADY REGISTERED (a 2-variant enum today); **the defect class is a consumer that never registered at all**, so the grep cross-check MUST sweep `EXP`/`LN` USAGE — `EML_OP_EXP`/`EML_OP_LN`, `eml_exp`/`eml_ln`, `EmlOp::Exp`/`::Ln`, the `POW` and `eml()` library entries — a **24-file** surface at this base, and NOT the registration type, which would return the same closed set and reconcile to nothing. Same shape as the StemThing census's raw-`u32` evader: a type-walk sees only what was already typed. **The one known non-conforming consumer is REPAIRED IN THIS RUNG, not exempted:** 5.11's STEAD falloff SSA-JIT arm drifts ≤1 ULP at the generic map/fold seam, where a MUL-ending map contracts into an FMA — the drift is **EXP-free**, so it is a SEAM defect and not a primitive defect, with a live witness probe already standing at `crates/simthing-workshop/tests/eml_exp_primitive_0_qualification.rs`. **Minting an exception, waiver, or grandfather mechanism to carry it is FORBIDDEN BY NAME**: an affordance whose only purpose is to hold one violating row is the permanent-escape-hatch shape the Definable Horizon Law exists to prevent, and it would defeat the obligation on the day it lands. Two routes are known-viable and neither is mandated — pin the seam's `fma`/rounding intrinsics so contraction cannot occur, or make the fold's constants grid-exact so contraction cannot change the result; **if seam exactness appears to require a kernel widening, a new opcode, or an Invariant Set change, STOP and report the measurement** rather than inventing an exception. Downstream binding lands in this PR per the Capability Binding Law (9.1's row). | A consumer declaring exact-bearing WITHOUT a cross-arm digest **hard-errors at admission**, planted-defect proven in PRODUCTION admission and never in a `#[cfg(test)]` seam. Every exact-primitive consumer is enumerated **tree-derived** with a published reconciliation line (type-walk count vs grep residue; every hit is either a row or recorded provably-non-consuming **with reason**); the count is a RESULT, not a target. Every exact-bearing consumer's digest reproduces **BIT-IDENTICALLY across every arm that consumer actually EXECUTES ON**, with the arm set derived from the tree and justified per consumer — never asserted as a count. **DA review 2026-08-05 — "all four arms" was a DA drift and is corrected here.** Four is the PRIMITIVE's arm count and it includes the standalone frozen-candidate artifact, which is a qualification surface and not an execution path; the consumer probe is explicitly **three-way** (`CPU twin / interpreted GPU / SSA-JIT`, `eml_exp_primitive_0_qualification.rs:283`) and the word `standalone` appears **zero** times in it. A consumer whose stack the lowerer recognizes as a fused kernel has that path as a genuine additional arm and must prove it. Hedging this as *"where applicable"* is forbidden — it is an unbounded out; the arm set is enumerated, not excused. The STEAD falloff consumer's four arms are BIT-IDENTICAL over its admitted domain, and the existing ≤1 ULP witness probe is **TIGHTENED to bit-identity** with a planted red proving the tightened probe FAILS against the pre-repair seam. **No exception/waiver/grandfather mechanism exists anywhere in the tree** (grep-proven). No second evidence channel beside `ExactPrimitiveConsumerEvidence`, no 5.10 door widening, no new opcode, no allowlist widening. §4's Exact-Value Provenance Law (2026-08-04) landed with `anchor_check.sh --resync` deliberately re-stamped. Synthetic-only focused + residue/detachability/lifecycle/inventory/orientation/anchor gates green, plus crate batteries at the head. **DA-GRADUATED / merged #1644 @ 339e4f8f — PHASE 5 COMPLETE; the exact-primitive door now has both sides.** `ExactPrimitiveConsumerEvidence` is the sole channel and `ExactPrimitiveAdmissionDoor::verify_consumer` hard-errors missing/zero/mismatched/incomplete exact-bearing evidence; seven planted hard-error shapes. **DA deep-tree verified independently rather than on report:** `FieldConsumerShapeProof` has ONE constructor, `pub(crate) from_admitted_field_registration`, whose sole production mint is `FieldSweepRegistration::exact_consumer_shape_proof()` reading typed registration state — unforgeable outside the kernel crate, and the battery exercises that production door rather than a test constructor. Consumer arm sets DERIVE from execution shape (`derive_consumer_arms`), closing orchestration's third remand where `FieldSweepEvalEml` could claim the AO shape and shed SSA-JIT. All five AO consumers execute their own probe domains through the real AO EvalEML GPU interpreter; falloff `0x3d60d7448fca13f8` and LogAccumulate `0x84cb4316b67aeedb` CPU/interpreted/JIT identical; no primitive digest used as consumer evidence. Census publishes the type-walk and the raw `EXP`/`LN` usage sweep SEPARATELY per #1642. **SEAM LAW RATIFIED** — see §4; witness tightened ≤1 ULP → bit-identity, EXP-free seam 0/32 (pre-repair 3/32), falloff 0/32 (pre-repair 2/32). **The Gu-Yang conservation question the DA raised is answered by an INDEPENDENT pre-existing witness the rung did not author:** `crates/simthing-driver/tests/field_sweep_n4_parity_0.rs` is UNMODIFIED by this rung and asserts `legacy_gu_yang_gpu == generic_gu_yang_gpu` bit-exactly — the legacy bespoke kernel was already fused, so the law brought the generic path INTO agreement with the incumbent and closed a latent legacy/generic divergence rather than introducing one. EXP/LN pinned digests unchanged (`0x7875a45ba919d588` / `0x196aced82d03f378`). **RESIDUAL, NAMED NOT WAIVED:** the seam law is an EML *evaluation-semantics* rule and shipped declared only in a results doc, code comments and tests — no author-facing ISA declaration. That is a DA debt, not a coder defect, and is discharged in this same stamp (§4 EML ISA block + Library growth law extended to evaluation rules + Provenance Law fence/pinning refinement). | Frontier — coder | **DA-GRADUATED / merged #1644 @ 339e4f8f** |
| 5.14 | `EML-ARITHMETIC-SEMANTICS-0` | **Owner-directed 2026-08-05 — finish the language instead of policing it. THE DELIVERABLE IS A DELETION.** 5.13 shipped two architectures and did not notice they were alternatives. The SEAM LAW **dissolved** a drift class by defining what a composition MEANS — one sentence, no evidence, nothing left to check. The consumer-evidence machinery **handles** the same class case by case, and its cost grows with every consumer forever: 7 types / **147** refs / `derive_consumer_arms` / a **602**-line battery / **+242** lines of gate. The second was unnecessary the moment the first existed. **Specify the arithmetic.** Only **six** of the 24 opcodes carry rounding — `ADD`, `SUB`, `MUL`, `DIV`, `EXP`, `LN`; `MIN`/`MAX`/`CLAMP` are exact selections and that is stated so it is never re-litigated. Give each ONE arm-independent meaning, and state the contraction rule **GENERALLY** — the SEAM LAW is an instance of it, not the rule. Each arm (CPU twin, interpreted WGSL, SSA-JIT) becomes a **faithful lowering**, proven per-opcode at a cost **bounded by the opcode set** rather than per-consumer. **DELETE** the per-consumer evidence machinery and the census-as-mechanism; enumeration becomes a query over admitted programs. **KEEP** the per-opcode exhaustive qualification — those digests are claims about the TOOLCHAIN, are fixed-cost, and stay pinned. **Owner standing directive: `EXP`/`LN` must emerge performant, elegant and flawlessly error-free.** Error-freedom is already evidenced (2.13B patterns, all arms, pinned digests) and §11 of the expansion doc records 5.11's `EXP` already beating its ordinary-gadget baseline on resource cost — **elegance is the open item, and (b) is what took it.** **FORWARD-FIT (binding, from `docs/EML_exp_ln_unification_expansion.md` §8.2 — a tile is a physical container, NOT a semantic opcode):** a future tiled backend must land as **one more faithful lowering**, never a new evidence axis. Under the deleted design a new physical arm would have multiplied every per-consumer digest by a new axis and made the tile probe combinatorially expensive to even attempt; this rung is a precondition for that exploration, not merely tidier. **This rung precedes the expansion amendment.** **STD-LANE CONFIGURATION (Owner-directed 2026-08-05 — Frontier capacity constrained).** Grok has graduated 18 rungs on this track including 5.12 `LN`; the one observed failure mode is DISCOVERY of a novel numerical route, never execution against a specified one. This rung is therefore configured so the design is FIXED HERE and the lane implements it. **THE SEMANTICS ARE SPECIFIED BY THE DA, NOT DISCOVERED BY THE CODER:** (i) `ADD`, `SUB`, `MUL`, `DIV` are IEEE-754 single-rounding with **no reassociation** — three arms, one meaning, no latitude; (ii) `MIN`, `MAX`, `CLAMP_BOUNDED`, `CLAMP_FLOORED` are **exact selections** carrying no rounding, recorded so the question never returns; (iii) `EXP`/`LN` are ALREADY fully specified by their pinned exhaustive digests on the certified tuple and are **NOT reopened** by this rung; (iv) **the contraction rule is the only open semantic content**, and it is bounded as follows. A `MUL` whose result flows into exactly one `ADD`, with no other `MUL` feeding that same `ADD`, **IS FUSED** — the SEAM LAW is precisely this case at a map/fold boundary and must be re-expressed as an instance rather than restated. **THE AMBIGUOUS CASE IS FENCED, NOT GUESSED:** where two or more `MUL`s feed one `ADD` (e.g. `(a*b) + (c*d)`) the licensed contraction is genuinely ambiguous and **inventing a tie-break is FORBIDDEN**. The rung's FIRST step is enumeration, not design — sweep every admitted program for contraction-ambiguous shapes and publish the set. **If it is empty, fail-closed is free and the rung proceeds; if it is NON-EMPTY, STOP and return the list — the tie-break is a DA decision, not a coder one.** Fail-closed is the standing default everywhere: an unrecognized or ambiguous shape classifies as NOT exactness-proven and is refused, never assumed fine. | Every rounding-bearing opcode and every contraction-exposed composition has ONE specified arm-independent meaning, with `MIN`/`MAX`/`CLAMP` recorded as exact selections; the contraction rule is stated GENERALLY and the SEAM LAW re-expressed as an instance, not restated as a special case. Each of the three arms is proven a faithful lowering per-opcode, planted-defect proven per arm in PRODUCTION lowering code, never in a `#[cfg(test)]` seam. **THE FALSIFIER (Owner-directed 2026-08-05): a NEW `EXP` consumer is authored in this rung and needs ZERO new evidence, ZERO declaration and ZERO census row** — its determinism following from admission alone. If that costs anything, the specification did not land and the rung is not done. The 7 evidence types, `derive_consumer_arms`, the 602-line battery and the census-as-mechanism are **DELETED**, not deprecated; grep-proven absent, with total line count in `crates/simthing-kernel/src` DECREASING. Consumer enumeration is a query over admitted programs. `EXP`/`LN` pinned qualification digests (`0x7875a45ba919d588` / `0x196aced82d03f378`) UNCHANGED — a specification that moves them has changed the primitives and is wrong. Falloff and all 5.13 consumers stay bit-identical across their arms with no per-consumer evidence carrying that claim. No new opcode, no 5.10 door widening, no allowlist widening. **STOP, do not paper over:** if any composition cannot take a single arm-independent meaning without a real measured performance cost, return the MEASUREMENT — reintroducing per-consumer evidence to cover it is the failure this rung exists to end. Synthetic-only focused + residue/detachability/lifecycle/inventory/orientation/anchor gates green, plus crate batteries at the head. **STD-LANE EXIT ADDITION (2026-08-05):** the contraction-ambiguity sweep is PUBLISHED before any lowering lands — empty set proceeds, non-empty set STOPS with the list. **DA-GRADUATED / merged #1650 @ fc291ce0 — PHASE 5 COMPLETE; the language is specified and the policing is gone.** **THE UNIQUENESS RULE** landed as one law over the `ADD`/`SUB` family: exactly one `MUL` feeding a consuming `ADD` or `SUB` is **FUSED** (one rounding); two or more feeding the same consumer are **UNFUSED** (`U`). **No tie-break exists or ever will** — ambiguity resolves to unfused by construction, so the question cannot recur for a new shape. SEAM LAW re-expressed as an instance. **The rung worked exactly as designed at its hardest moment: the coder STOPPED on the first-step census rather than guessing**, returning 5 two-`MUL`→`ADD` programs with per-arm measurements sufficient for a one-pass ruling. **The DA's `SUB` catch was load-bearing:** the census had declared *"`SUB` carries only standalone IEEE-754 single-rounding"* without sweeping it; the sweep found **4 `MUL`→`SUB` dataflows** (all unique → FUSED), so the specification had been asserting something FALSE about a reachable surface, and the pre-authored one-word extension corrected it with zero further rulings. **SSA-JIT planted-defect obligation DISCHARGED BY UNREACHABILITY (DA ruling `5193244394`):** the truthful mutant — a lawful separated `MUL`/`ADD` — is re-contracted by the certified toolchain to identical bits, so the wrong value is **not constructible on that arm**, which is stronger evidence than a caught mutation. Recorded as an unreachability witness, **not** a planted-defect pass, and **toolchain-scoped** — the kept per-opcode qualification re-opens it if the tuple changes. The coder's refusal to force separation with an optimizer fence was correct: that is the dead construction, measured dead four times now. **DELETION VERIFIED INDEPENDENTLY, AND THE DA'S OWN CRITERION WAS THE DEFECT:** the 5.13 per-consumer machinery is grep-absent except as string literals inside its own absence-referee; measured `crates/simthing-kernel/src` is **`.rs` 22,134→22,065 (−69)** while **`.wgsl` 2,043→2,222 (+179)**, total **+110**. The row demanded total line count DECREASE; that is a bad proxy and the failure is the DA's, not the lane's — the growth is `field_sweep.wgsl` +135 and `accumulator_op.wgsl` +44, i.e. **the arm lowerings, which are the deliverable**; a rung cannot specify semantics on three arms without writing the GPU arm. **The correct measure for a delete-and-specify rung is grep-proven architecture absence plus net change in the NON-deliverable surface, never gross total** — `.rs` fell 69 lines while simultaneously absorbing ~200 lines of new uniqueness specification, which is the real evidence the deletion was substantial. Recorded so the next deletion rung does not inherit the bad proxy. `EXP`/`LN` pinned digests unchanged (`0x7875a45ba919d588` / `0x196aced82d03f378`; DA verified they were never in `crates/` at base, so nothing was lost). Focused semantics witness 7/7, language witnesses 3/3, two-`MUL`→`ADD` census 5, hosted Doctrine Scan/Exec PASS at exact head. | Std — Grok | **DA-GRADUATED / merged #1650 @ fc291ce0** |
| 6.0 | `OWNER-CHANNEL-INTRINSIC-0` | **Phase 6 foundation — Owner-directed 2026-08-01. Makes ownership ATOMIC AND INTRINSIC to the stem-cell SimThing.** Owner channels are how resource contention resolves — collaborative or attritional — so if 8.x lands with no intrinsic owner, every rung re-invents per-participant stamping and the next corpus becomes load-bearing exactly as TP did. **Measured baseline 2026-08-01:** core `SimThing` has NO owner field; ownership is TWO unrelated notions — `resource_parent_edges` (per-property, non-spatial, recursive, span-tracked) and `OWNER_FLOW_OWNER_REF_PROPERTY_ID` (a flat string stamped individually onto each participant from ~10 call sites). **No ownership resolution exists anywhere** — nothing answers "who owns this?" by walking up. `owner_flow_owner_ref` returns `Option`, and `unowned` exists ONLY in the mapeditor render layer, whose own comment refuses to "invent a 'None owner' group". **(a) OWNER AS A SPARSE PROPERTY DIMENSION, INERT BY DEFAULT.** The stem cell already affords this: `properties` is documented as "only properties that are currently meaningful for this entity" and "adding a new property dimension never changes this struct". Owner is therefore additive with **zero struct change and zero bytes for the inert majority** — absence MEANS inherit, so most SimThings store nothing and still resolve correctly. **(b) RESOLUTION IS TOTAL, PURE, AND NEVER MATERIALIZED.** `resolve_owner(thing)` walks explicit owner property -> parent's resolved owner -> session default, returning exactly one owner ALWAYS. **`unowned` becomes a real neutral owner, not `Option::None`**, which deletes a partial function from the RF path instead of adding one, makes ownership flip on movement an ordinary rebind rather than a nothing-to-something transition, and lets neutral ground participate in contention as a real party. Resolution is a pure function over the tree and MUST NOT be stamped onto nodes — materializing it would recreate the flat-stamping defect with 1500 redundant properties. Fission is therefore free: a subtree root rebinds one property and its whole descendant ownership re-parents. **(c) OWNERSHIP IS SINGLE; PARTICIPATION IS MULTI-OWNER.** These are different questions and conflating them is a trap the DA walked into and the Owner caught: forcing one owner per channel would make a star hosting two owners' mineral flows illegal, and **adversarial contention structurally impossible**. Every SimThing resolves to exactly ONE owner; every container admits AS MANY owners as it has participants. Admission checks **conservation across the container, never ownership uniformity**. Alliance is NOT ownership — it is a relation BETWEEN owners, and folding it in would reproduce the domain-word failure; ally vs enemy in one system is the SAME structure under a different AUTHORED resolution rule (8.1/8.2). **(d) OWNER IN THE BUCKET KEY — NO CONDITIONAL.** The Owner's rule (merge same-owner child flow with own, pass different-owner flow up segregated) is EMERGENT from keying by owner and must not be written as an if/else: a key is DATA, a branch is code that drifts. `PlanetChildRfScopeKey` already proves it — `{owner_ref, resource_key, local_scope_id, planet_id, star_system_gridcell_id_raw}` — but only at ONE named seam. Generalize it to every node, and **DELETE `planet_id` / `star_system_gridcell_id_raw`**: those are DOMAIN NOUNS in an engine `simthing-spec` struct, invisible to `SCENARIO-RESIDUE`, and the generic `ScopeId` that replaces them already exists. This rung therefore REMOVES domain shape from engine spec rather than only adding capability. **(e) BOUNDED PER-NODE STEAD RECORDING.** Reduce-up is currently a TRANSIENT report terminating at the owner silo — no STEAD write exists in the path, so "what flowed through this star?" is unanswerable today. Record **crossing flows only** (child owner != node owner) plus the node's own aggregate: the full worldstate RF map is reconstructible from that, at O(crossings) rather than O(nodes x owners x resources). This makes **conservation checkable LOCALLY at every node**, a strictly stronger invariant than 8.1 currently plans. **(f) OWNER BOUNDARY = EXECUTION BOUNDARY = REDUCE-UP SEAM.** One concept, three jobs. A subtree whose root resolves ownership internally is independently schedulable by construction — only the reduce-up crosses — which is precisely the self-executing-subtree / network-node telemetry model, obtained with NO new scheduler concept. Holds regardless of how many owners' flows transit the subtree. **(g) THE CARROT — A SANCTIONED-MECHANISM REGISTRY (Owner-directed).** CI is the stick; this is the carrot. Ship `scripts/ci/allow/contention_mechanisms.txt` in the SAME rung that mints the mechanisms, never ahead of them (a registry naming symbols that do not exist yet is a forward-declaration that goes stale). Unlike the existing export allowlists it is a **need -> ready-mechanism map**, so an agent reaching for a clever bespoke conflict resolver meets the sanctioned one first: contested claim over bounded supply -> AUTHORED resolution rule (proportional / priority / price-clearing / **attrition**); ownership divergence within a subtree -> **ownership fission** by single-property rebind (b); "who owns this?" -> `resolve_owner`, never a new stamp; "whose flows cross here?" -> the owner-keyed bucket, never an equality branch (d). Wire it into `gen_digest.sh` `GLOBAL_SOURCES` and `TRACK_ALLOW_FILES` so it reaches the sanctioned-surface digest agents actually read, and register it in `clearance_check.sh` `GATE_WIRING_PATHS`. Rows carry the existing four-column shape and an explicit promotion-blocker, so a mechanism that stops being the ready answer is retired rather than accumulating. Attrition rows land at 8.1/8.2; fission and resolution rows land here. **Witness is synthetic**: extend `specialist_citizens_minimal.clause`; no corpus, per the Transient Fixture Law. | **DA-GRADUATED / merged #1561 @ 8cbd7e3d — ownership is now INTRINSIC to the stem cell.** Owner is a sparse property dimension: an unbound tree stores ZERO owner bytes and binding a 3-node tree costs exactly ONE property. `resolve_owner` is pure and NEVER materialized. **The coder corrected the DA's own design**: the DA's version returned the neutral owner for a target absent from the tree, ALIASING an invariant violation to a legitimate value, so a mistyped id resolved silently to neutral; it now fails membership closed, and `try_new_authored` additionally rejects an authored Owner named `unowned` that would collide with the substrate's neutral identity. Totality is preserved where it matters — every valid admitted member yields exactly one owner, and no `Option` effective owner exists. (d) `OwnerChannelScopeKey {owner_ref, resource_key, scope_id}` replaces the domain-shaped key; `planet_id` and `star_system_gridcell_id_raw` are GONE from engine `src` (DA-verified by grep), with `PlanetChildRfScopeKey` surviving only as a type alias so 9 call sites did not churn. Aggregation has ZERO owner-equality comparisons in the diff. (e) Retained owner state is exactly crossings plus own aggregates, and the full RF map reconstructs from it. **DA-verified independently, not accepted on the relay's evidence:** 18 proofs green across core/spec/driver; core 49 and spec 19 green; inventory drift PASS; scenario-residue `scenario=0 domain=0`; detachability `0/0/0`. **Falsifiability re-demonstrated by the DA planting defects directly** — dropping a crossing and duplicating a crossing each turn the driver referee RED (3 of 4), restoring returns 4/4. The single materialization grep hit was a doc comment FORBIDDING materialization. RESIDUE NAMED, NOT WAIVED: the module/type NAMES `planet_child_rf.rs`, `PlanetChildRfScopeKey`, `PlanetChildRfReduceUpBucket` remain domain-shaped. The exit-proof was field deletion and is met; the names are Mechanisms-Not-Domains residue for a follow-up. | DA-implemented — Opus 5 | **DA-GRADUATED / merged #1561 @ 8cbd7e3d** |
| 6.0b | `SIMTHING-AUTOMATON-INTRINSIC-0` | **Phase 6: Owner-directed 2026-08-02 — the minimum viable Wei automaton, intrinsic to the stem cell.** Anchored by `docs/stead_simthing_automata.md`, which is normative for this design and records the rejected shapes. A SimThing today can EMIT, ACT and FLOW but **cannot LISTEN** — a cell with an outbox and no inbox — and that is the only reason Phase 6 cannot close. **EVENTS ARE RF:** reduce-up carries PERCEPTION, disburse-down carries DIRECTIVE. Both channels are built, so this rung needs NO NEW TRANSPORT. **(a) RECEIVE** via three delivery modes on existing homes: deficit-driven -> `owner_silo_disburse_down` / `runtime_local_allocation_from_disbursement` (free, rides the sweep); standing -> the `resolve_owner` inheritance walk (zero storage for inheritors, absence means inherit); predicate broadcast -> ONE tree walk paid by the initiator, the only mode that does NOT fall out of RF and must be named as its own. **(b) ORIGINATE:** `Overlay.origin: SimThingId`, REQUIRED. This is not new provenance — **60 construction sites already hold their originating node in scope and discard it** (`event_handler` resolves targets to `SimThingId`s; `capability_handler` has the instance; `feeder/work.rs` has `target`/`child`/`new_parent`). Scenario-authored overlays originate from the ScenarioThing, which is itself useful corpus. `OverlaySource` is RETAINED and complementary: origin says WHICH NODE, source says WHAT KIND OF WILL — `Player` vs `Ai` on the same OwnerThing is the intervention signal. Spatial location is DERIVED (origin -> `PlacedParticipant` -> `StructuralCoord`), never stamped. **(c) ROUTE** up to the common ancestor then down, **so the overlay stack filters along the path** — direct `affects` targeting bypasses every intermediate policy layer and is what would BREAK the model, not a cheaper version of it. **(d) INERT BY DEFAULT:** `SimThing.overlays` is a `Vec` starting empty, so an empty gridcell that never originates stores nothing — the same zero-cost property ownership has. **CONSERVATION IS NOT WEAKENED:** a consumed directive is a resource and conserves; a standing directive is an INHERITED PROPERTY, read not moved, and carries no conservation claim. Modelling a non-consumed value as a resource would put a non-conserving quantity inside the conservation judge. **(e) PREDICATE COMPOSITION IS NOT INHERITED BY ASSUMPTION:** (c) reuses the `TransformStack` chain that `evaluate_node` already composes ancestors-first (`crates/simthing-core/src/evaluate.rs`), but that stack is a `Vec<PropertyTransformDelta>` applied SEQUENTIALLY per matching `property_id`, where a later delta may overwrite an earlier one (`Set` after `Multiply`). Value composition is therefore LAST-WINS-CAPABLE, and routing a predicate through it does NOT automatically yield the conjunctive semantics a policy chain requires — a descendant overlay could LOOSEN an ancestor's restriction, which is precisely the asymmetric gate doctrine inverted (tightening free, loosening reserved). This rung must STATE the composition rule for predicates rather than inherit it from `TransformOp`, and restrictions must compose conjunctively. | **DA-GRADUATED / merged #1591 @ af886019 — the stem cell can now LISTEN.** Reception is intrinsic: no new field, the overlay Vec is the inbox, and arrival rides the existing RF seams. A SimThing receives events from its own subtree in all three modes with a planted-defect red for each; every `Overlay` carries a required `origin` and an unattributable overlay is UNCONSTRUCTIBLE; a routed directive is demonstrably filtered by an intermediate policy overlay that direct targeting would bypass; an inert SimThing stores ZERO bytes for reception and origination; event spatial location is derived from origin with no stamped coordinate; standing directives make no conservation claim and the judge is unchanged; the composition rule for routed PREDICATES is stated explicitly and proven, not inherited from `TransformOp` value composition — two nested policy overlays on one path compose CONJUNCTIVELY, with a planted-defect red proving a descendant overlay CANNOT loosen an ancestor's restriction. | Frontier — coder | DA-GRADUATED / merged #1591 @ af886019 |
| 6.1 | `EVENT-GENERATION-STAMP-0` | Phase 6: generation stamps on emission/threshold events; stamped ring egress with admission-time backpressure. **DA AMENDMENT 2026-08-02 (Owner design session) — 6.1 IS THE SEAM PRIMITIVE, not bookkeeping.** It is the prerequisite for independently-executing subtrees, so its scope extends: **the generation stamp gains a SECOND CARRIER — reduce-up products, not only events.** A parent integrating a child's product must know which generation it came from or it is summing state from mixed times. **ASYNC IS ADMITTED AS ORDINARY:** a parent at generation N+3 integrating a child's gen-N product is the NORMAL case, never an admission error — requiring the parent to wait is synchronous execution by another name and defeats the whole capability. **DETERMINISM IS PRESERVED BY RECORDING, NOT BY WAITING:** determinism holds RELATIVE TO A RECORDED INTEGRATION SCHEDULE (which stamped products were integrated at which parent generation), which is authored/recorded data, not ambient timing. Replay the schedule, reproduce exactly. This is the same move made everywhere in this track — what varies becomes DATA. **The stamp is what makes staleness VISIBLE rather than silent**, and the recorded schedule is what makes the resulting heatmap a REPRODUCIBLE training corpus rather than an artifact of which host happened to be slow. That is the argument for doing it properly here rather than deferring. **BINDS `OVERLAY-LIFECYCLE` (Definable Horizon Law):** decision dispatch attaches overlays (`effect = { attach_overlay }`), so every overlay a dispatch mints carries `UntilDissolved` with an authored condition. A runtime-generated overlay with no dissolve condition is the accretion this law exists to stop — it would accrete in the hottest loop in the system rather than in a TSV. Do NOT reintroduce a permanence variant, and do NOT treat `AtSessionEnd` as "never". **BINDS 6.0b `SIMTHING-AUTOMATON-INTRINSIC-0` (preservation, not new work).** 6.0b already converted `BoundaryRequest::AttachOverlay` to route through `deliver_routed_overlay`, so decision dispatch ALREADY arrives via the generic routed primitive with a required `origin`. This rung must PRESERVE that: a dispatch-minted overlay may not reintroduce direct `affects` attachment, may not carry a synthesized or borrowed `origin` to dodge attribution, and may not add a dispatch-local delivery path beside the routed one. The stamp rides the existing arrival, it does not get its own. | **DA-GRADUATED / merged #1596 @ df117bcc — generation identity is now a real async seam primitive.** Events carry stamps; forced observer lag honors declared backpressure without perturbing the sim. **SECOND CARRIER:** reduce-up products carry generation stamps too, not only events — a parent integrating an unstamped product is a hard error, with a planted-defect red. **ASYNC IS ORDINARY:** a parent at generation N+3 integrating a child's gen-N product completes with NO error and NO wait; a referee proves the parent does not block on a lagging child, and a mutant that makes the parent wait REDs. **DETERMINISM BY RECORDING:** the integration schedule (which stamped products were integrated at which parent generation) is RECORDED, and replaying it reproduces the run bit-exactly; a mutant that drops the schedule and relies on ambient timing REDs. **STALENESS IS VISIBLE:** a stale integration is observable and attributable from the stamp alone, never silent. **DISSOLVE DISCIPLINE:** every dispatch-minted overlay carries `UntilDissolved` with an authored condition — a runtime overlay with no dissolve condition is UNCONSTRUCTIBLE or hard-errors at admission, proven by planted defect; no permanence variant and no `AtSessionEnd`-as-never. **ROUTED ARRIVAL PRESERVED (6.0b):** dispatch still arrives via `deliver_routed_overlay` with a required real `origin`; zero direct `affects` attachment and zero dispatch-local delivery paths. | Std — Grok | DA-GRADUATED / merged #1596 @ df117bcc |
| 6.1b | `BAND-QUANTIZED-DRAW-0` | **Phase 7 foundation — Owner-directed 2026-08-02. RESTORES the original band-crossing queued-production capability and REUNIFIES the two halves it drifted into.** **Archaeology (measured):** emission bands were authored as `num_to_produce * cost_to_produce` with the threshold AS the unit cost, resolving `incoming / cost = produced`. That single mechanism SPLIT: thresholds kept only EDGE DETECTION (`ThresholdEmission {reg_idx, slot, col, value}`; `BandCrossingDelta` carries `threshold` and `post_value` but NO count), while the quantity half migrated to the recipe/transfer planner — its fingerprint survives verbatim in `output_coefficient`'s doc, *"target units credited per exact affordable recipe unit ... consumes the existing transfer planner's `output_scale`"*. The queue-depth cap `throttle_hint_max_per_tick` was ORPHANED in the split: authored, hydrated by two paths, stored, and its own comment says *"Not an enforced GPU or CPU cap."* **THE MECHANISM (not a domain):** a band whose threshold is a unit cost yields `N = floor(V/C)` and `R = V - N*C`, so **`V = N*C + R` EXACTLY**. It is a CONSERVATIVE QUANTIZATION — it splits a continuous value into a discrete count and a continuous remainder losing nothing, which is why it belongs to RF rather than beside it: it inherits the conservation invariant instead of inventing one. Pure algebra, no branching. **ZERO WGSL, ZERO BUFFER-LAYOUT CHANGE:** the MULTIPLY is authored and UPSTREAM (`num_to_build * cost_per_build` is the participant's deficit, which `owner_flow_deficit` already models as *generic unmet demand*), and the DIVIDE is DOWNSTREAM of allocation (`allocated / cost`). The arena between them is untouched. Both operands are already sealed on `BandCrossingDelta` (`threshold()`, `post_value()`), so the quotient is derivable with no kernel widening. **AUTHORED MARKER, NEVER INFERRED:** the quotient is MEANINGLESS for `FissionTrigger`, `VelocityAlert`, `PropertyExpiry`, `AggregateAlert`, and `CapabilityUnlock` — so unit-cost intent is authored PER REGISTRATION (necessary) and MAY additionally be declared per resource; per-registration WINS, and ambiguity is an admission error, never a silent default. Marker rides the existing CPU-side semantic table indexed by `event_kind`, not the GPU POD. **CLAUSESCRIPT SURFACE:** the queue's EXISTENCE AND SHAPE are authored; its DEPTH is runtime state. A player queueing builds mutates a property value and MUST NOT require re-hydration. **ANTICIPATED ISSUES, ruled in advance:** (1) **Conservation is PER-RESOURCE-CHANNEL, not across the transformation** — `N*C` of the input is consumed exactly, `N*coefficient` of the output is MINTED; 8.1 must not judge global conservation across a recipe or every build will read as a violation. (2) **Unbounded remainder is the starvation failure** — a queue perpetually under one unit's cost accumulates forever; under the Definable Horizon Law the remainder needs an authored horizon, never implicit immortality. (3) **A "permanent" population-growth queue is `UntilDissolved` + `AtSessionEnd`**, never a permanence variant — the law admits no exception for queues that happen to outlive everything else. (4) **"Which cohorts grow first" is CONTENTION** and resolves by an AUTHORED rule (8.2), never by vector iteration order — the same discipline that made 5.8b's `authored_order` durable. (5) **Restores the orphan**: `min(floor(V/C), throttle_hint_max_per_tick)` finally gives that field a consumer. **NON-BREAKING IS THE PRIMARY EXIT PROOF.** **STAGE 2, NOT THIS RUNG:** re-expressing recipes on this primitive to retire the `output_scale` duplication touches a working system and is deliberately deferred — restore first, unify second. **DA AMENDMENT 2026-08-02 (Owner) — CANONICAL TERM AND SINK DEFINITION.** The mechanism is named **`CostBand`** (one word, camel-humped). That spelling is canonical and is the term used in all prose, code, and authoring surfaces from here forward. **A CostBand is not a MODE a threshold may opt into — it is the DEFINITION of a resource sink.** The DA first authored the marker as "authored per registration, default off", then corrected it to a sink/observation TAXONOMY naming six semantics. **Both were wrong, the second more subtly (Owner correction 2026-08-02).** A taxonomy enumerates and therefore invites "which category is my new event?"; the truth defines and needs no list. **OBSERVATION IS THE BASE CASE — it is what a crossing IS.** A threshold detecting a crossing costs nothing and consumes nothing; that is already observation. **ACTION IS OBSERVATION WITH A CostBand ATTACHED.** So there are not two kinds of event to tell apart: there is ONE mechanism and an OPTIONAL CONSUMER. `VelocityAlert` is not a different species — it is a crossing nobody hung a CostBand on. It follows that **everything is observable by construction**, because observation is what crossings are, and **conservation enters only where something is consumed** — so nothing is ever forced to conserve that does not. The authored question is therefore **"is this a sink?"**, never "is this a CostBand?": **every sink IS a CostBand, and there is no second sink mechanism.** Stating it as an opt-in flag invites a rival sink to appear later for something that "didn't need the quotient", which is exactly how this capability fragmented the first time. **BOOLEANS ARE A CostBand OF DEPTH 1.** This REMOVES a branch rather than adding a case: there is no separate did-it-fire path, only how-many, with boolean as the degenerate `N` in `{0,1}` — the same move that made `unowned` a real owner rather than `None`, where the special case stops existing instead of being handled. **DOC OBLIGATION (Owner-directed):** this rung DEFINES `CostBand` in `docs/stead_spatial_contract.md` section 5 (RF/Accumulator relationship to STEAD), which is the contract that already governs RF over Locations. Canonization into the core design doc is bound at 12.2. **DA AMENDMENT 2026-08-02 (Owner) — MOVED FROM 7.0 TO PHASE 6; CostBand IS THE PRIMARY EVENT STEERING SURFACE.** Separating the action SEMANTIC (this rung) from the action TRANSPORT (6.2 `ASYNC-COMMAND-QUEUE-0`) across a phase boundary was the DA's placement error. Phase 6 is the event phase END TO END: ingress -> transport -> action -> resolution site. **EML-COMPUTED STEERING (Owner design):** `TransformOp` is ALREADY a degenerate EML — `Add` is `EML_OP_ADD`, `Multiply` is `EML_OP_MUL`, `Set` is `LITERAL_F32` ignoring the current value. Overlays therefore already carry a three-instruction language with no opcodes beyond the three and no way to reference anything. This rung GIVES THAT EXISTING LANGUAGE ITS FULL INSTRUCTION SET rather than adding a mechanism: a **static overlay value becomes the ONE-NODE degenerate program** `LITERAL_F32(v)`, magnitude bands become `SELECT(CMP_GE(N,t2), hi, SELECT(CMP_GE(N,t1), mid, lo))`, and a continuous response becomes ordinary arithmetic over `N`. **There is no branch between "static" and "computed" overlays — the special case stops existing**, exactly as boolean became depth 1 and `unowned` became a real owner. `N` arrives via `EML_OP_PARAM`. Because EML is INTERPRETED in-shader from a storage buffer (`accumulator_op.wgsl` binds nodes `var<storage, read>`), banded steering **compiles nothing** — it is an authored node array, so per-call instantiation is already free and the JIT is an optimisation for hot paths rather than the primary path. **BOUND:** overlay application becomes O(program length) instead of O(1); a one-node program stays static-priced, but an unbounded authored program applied per-slot-per-overlay in the hot loop is the failure mode and must be capped. **BINDS 6.0b (shared-surface ledger):** the `V` for every CostBand comes from `reduce_owner_channel_rf` — the sweep ALREADY summed `surplus_total`/`deficit_total`, so a CostBand evaluated during reduce-up is DECLINING TO DISCARD a quantity rather than computing a new one. CostBand emits into the existing sealed `BoundaryRequest` vocabulary (7 verbs), never a new door; `AttachOverlay` is already the decision-overlay path end to end. Both quotient operands are already sealed on `BandCrossingDelta` (`threshold()`, `post_value()`). **COMMAND DEFICITS ARE THE DEPTH-1 CostBand (DA review 2026-08-03).** 6.0b landed `CommandDeficit` with `requested: 1` -- that IS "boolean is a CostBand of depth 1", but left implicit it invites a fork: the command path drifting into a parallel depth-1-only sink beside the quantized resource draw, which is a SECOND SINK MECHANISM and forbidden by this row's own law. Binding: a command deficit is a CostBand draw of depth N with N=1 as the degenerate case -- same algebra, same `V = N*C + R`, no separate code path. | **DA-GRADUATED / merged #1602 @ 63c8ed51 — CostBand restored and EML is the singular overlay value path.** Every existing registration without the marker produces BIT-IDENTICAL crossings, demonstrated by a planted defect; `V = N*C + R` holds exactly over randomized inline inputs with a planted-defect red on both a lost remainder and an off-by-one count; ambiguous per-registration/per-resource markers hard-error at admission; `throttle_hint_max_per_tick` is enforced and its cap demonstrated; runtime depth mutation changes output with NO re-hydration; zero WGSL diff, zero `ThresholdRegistration` layout change, no allowlist widening. `CostBand` is DEFINED in `docs/stead_spatial_contract.md` section 5 by that exact spelling; every admitted sink registration is a CostBand and no rival sink path exists; a boolean sink executes as depth 1 through the SAME code path as depth N, demonstrated by a planted defect that a separate boolean branch would survive. **EML STEERING COVERAGE (DA 2026-08-03 — the prior proof covered only the CostBand half).** The opcodes this rung names ALREADY EXIST (`LITERAL_F32`, `SELECT`, `CMP_GE` are live in `cpu_oracle`, `eml_resource_class`, `field_sweep`), so this is a RE-EXPRESSION, not a language design: `TransformOp` and EML are today still SEPARATE paths and this rung joins them. Proof obligations: a static overlay value executes as the one-node degenerate program `LITERAL_F32(v)` and produces **BIT-IDENTICAL** results to today's `TransformOp`, demonstrated by a planted defect; there is **NO branch** between static and computed overlays, demonstrated by a planted defect that a separate static fast-path would survive; `N` arrives via `EML_OP_PARAM` from the CostBand quotient with no second parameter channel. **PROGRAM-LENGTH CAP — the row's own named failure mode, previously unproven.** `DEFAULT_EML_TREE_CAPACITY = 64` and `DEFAULT_EML_NODE_CAPACITY = 2048` are TABLE budgets across all trees, NOT a per-program bound: one authored overlay can today reach ~2048 nodes and blow the hot loop while staying inside table capacity. This rung admits a PER-PROGRAM node cap, ADMITTED not hardcoded, exceeding it is an admission error with a planted-defect red, and no second cap mechanism is minted beside the existing table capacities. **STATIC-PRICED IS MEASURED, NOT ASSERTED:** the one-node degenerate program must show no measurable per-overlay regression against the pre-rung path, recorded as evidence — 'a one-node program stays static-priced' is a claim this rung must pay for, since the re-expression puts an interpreter on the hottest path in the system. **EML IS THE ONLY ELIGIBLE VALUE PATH — AT THE TYPE BOUNDARY (DA review 2026-08-03, Owner-directed exit-proof drift correction).** Owner lean, encoded as a TYPE not a law: prose is the weakest admission tier and a law would be redundant beside a type that admits one form. The overlay value form is SINGULAR — there is no `Static`/`Computed` discriminant and a second form is a COMPILE ERROR, proven by a pinned `compile_fail` doctest in the manner of `Overlay.origin`'s `E0063`. This also discharges the 2026-08-02 phase-move and CostBand-canonization amendments, whose obligations are the CostBand clauses above. **CARROT LANDS WITH THE MECHANISM:** this rung ADDS a `scripts/ci/allow/contention_mechanisms.txt` row answering the need 'compute an overlay value from runtime state' with the EML program surface, so an agent reaching for a bespoke computed-overlay path meets the sanctioned answer first. Row lands in this rung because it mints the mechanism, never ahead of it.| Frontier — coder | DA-GRADUATED / merged #1602 @ 63c8ed51 |
| 6.2 | `ASYNC-COMMAND-QUEUE-0` | Phase 6: async CPU action queue landing at generation barriers; injection generations logged; generation-consistent snapshots. **DA AMENDMENT 2026-08-02 (Owner design session) — the queue is where async survives contact with a fast child.** **A SLOW CHILD MUST NEVER BLOCK THE TREE.** **COALESCE, NEVER DROP:** every field of `OwnerChannelRfBucket` (`participant_count`, `surplus_total`, `deficit_total`, `net_surplus`, `net_deficit`) is a SUM, so N pending products for the same `{owner, resource, scope}` bucket MERGE into one with no information lost and conservation preserved exactly — because addition is already what those fields are. That bounds the queue by the number of DISTINCT BUCKETS, not by tick-rate ratio: a child running 100x faster queues no more than one running 2x faster. Backpressure therefore stops being a rate problem and becomes a cardinality problem, bounded by authoring. Dropping is forbidden — it breaks conservation; coalescing is safe for exactly the reason the bucket key made same-owner aggregation emergent in 6.0. **CONSERVATION ACROSS THE SEAM:** the seam is a HOLDING ACCOUNT, not transport. Resources that have left the child and not yet landed are held BY THE BOUNDARY, so `child + seam + parent` is exact at every instant. Conservation is not relaxed to eventual — it stays instantaneous over three balances instead of two. **STALENESS TOLERANCE IS AUTHORED PER SEAM** (a remote host on a slow link and an in-process subtree have different tolerances); exceeding it is an ADMISSION ERROR, never a silent stall. **BINDS 6.0b (shared-surface ledger):** the async queue IS the directive channel — `owner_silo_disburse_down` and `runtime_local_allocation_from_disbursement` carry command deficits exactly as they carry resource deficits, with NO second transport. "Closest automaton" is `order_band` / `max_disbursement_band` — **authored, never inferred from tree distance** — so directive routing inherits RF's existing determinism instead of needing its own tie-break. **STAMP SEMANTICS UNDER COALESCING (DA review 2026-08-03) -- values sum; stamps do NOT.** "Coalesce, never drop" is proven for bucket fields because they are sums; 6.1's generation stamps are not. The coalesced product carries the NEWEST stamp (staleness is `parent_gen - latest_integrated_child_stamp`, so `latest` = max), and the recorded integration schedule records the FULL COALESCED GENERATION SET per product -- schedule rows are PER-PRODUCT, never per-bucket-latest, or replay loses which generations merged and bit-exactness fails. **ONE REPLAY RECORDER:** this rung's injection log IS 6.1's recorded integration schedule -- one mechanism, a second row kind, never a second log with its own replay path. Two replay codepaths is the CostBand fragmentation shape applied to time. **THE SEAM IS BIDIRECTIONAL (DA review 2026-08-03).** The holding account holds BOTH directions: upward stamped products, and DOWNWARD the ancestor standing/policy view a child integrates, stamped and refreshed at barriers. 6.0b's live-route ruling holds WITHIN a resolution site; across an async seam "live" is not well-defined, so the rule is **live within a site, stamped across a seam** -- the child records which ancestor-view generation it observed, or standing reads replay differently and determinism-by-recording fails on the down direction while being proven on the up. **INTERNED IDS NEVER CROSS A SEAM:** 9.2's interned owner ids are session-local; seam transport carries canonical `OwnerRef` identity. | **DA-GRADUATED / merged #1616 @ af4f9dbb — the async seam is a holding account, not a transport.** Replay bit-exact from the injection log; shadow-tree reads torn-free (double-buffer proof). **SCOPE COVERAGE (DA review 2026-08-03, Owner-directed exit-proof drift correction).** Coalescing preserves conservation EXACTLY -- N pending products for one `{owner, resource, scope}` bucket merge with no information lost, with a planted red on a dropped product; dropping is forbidden and proven forbidden. The seam is a HOLDING ACCOUNT: `child + seam + parent` is exact at EVERY instant, not eventually, with a planted red on an in-flight product escaping all three balances. Authored staleness tolerance exceeded is an ADMISSION ERROR, never a silent stall, planted-defect proven. **NO SECOND TRANSPORT:** command deficits ride `owner_silo_disburse_down` / `runtime_local_allocation_from_disbursement`, and directive routing derives closeness from authored `order_band` / `max_disbursement_band`, never inferred tree distance. **STAMPS ARE NOT SUMS:** a coalesced product carries the NEWEST stamp and the schedule preserves the FULL coalesced generation set per product, with a planted red on bucket-latest collapse. **ONE RECORDER:** this rung's injection log IS 6.1's integration schedule extended with a row kind -- a second log with its own replay path REDs. **BIDIRECTIONAL SEAM:** the downward ancestor-view a child integrates is stamped and refreshed at barriers, and replay reproduces standing reads bit-exactly, not only upward products. Interned owner ids never serialize past their session. **DISCHARGES amendments dated 2026-08-02** — the clauses above are the proof obligations those amendments create; this citation exists so the parity gate can see the proof was widened with the scope. | DA-reserve · Frontier — Codex 5.6/Fable | DA-GRADUATED / merged #1616 @ af4f9dbb |
| 6.2b | `RESOLUTION-SITE-SPLIT-0` | **Phase 6: Owner-approved 2026-08-02 — the closed loop becomes the DEFAULT SimThing execution model, and the CPU-authoritative system we run today becomes a VENDORIZED BUILD derived from it.** The inversion is the point: built the other way round, the CPU path stays load-bearing forever and the closed loop is permanently the special case, so every new capability gets built CPU-first and ported. **These are NOT two systems — they are ONE MODEL WITH TWO RESOLUTION SITES.** The model: SimThings resolve state via field arithmetic; crossings are observations; CostBands are actions; allocation happens at a barrier. Closed-loop resolves IN-SHADER IN SLOT SPACE. CPU-authoritative resolves AT THE BARRIER with identity re-attached. Same math, same vocabulary, different placement — which is why indistinguishability is PROVABLE rather than aspirational. **MEASURED FINDING THAT MAKES THIS SMALL:** the current dispatch is mostly IDENTITY RE-ATTACHMENT, not decision logic. `VelocityAlert`'s entire arm is `registry.get(event_kind())` -> pull `sim_thing_id`/`property_id`/`sub_field` -> build a struct; there is no decision in it. It exists only because `ThresholdEvent {slot, col, value, event_kind}` is flat and the CPU registry puts SimThing identity back. **A closed loop stays in slot space and never crosses that bridge, so those arms EVAPORATE rather than port.** `FissionTrigger` is the genuine exception — it reads `DimensionRegistry` and `fission_templates` to size a pre-grow, which is ALLOCATION and stays at the barrier in BOTH modes. Of the 13 boundary stages, seven (pre-grow, fission/fusion, lineage, AddChild pre-grow, structural mutations, dimension rebuild, capacity) are allocation machinery that survives untouched; only three sites match on `ThresholdSemantic` at all. **The CPU accommodation is therefore NOT new code** — the identity re-attachment the dispatch performs today IS the semantic-shadow-maintainer job, relocated and demoted from AUTHORITY to MIRROR. **RISKIEST ASSUMPTION, TEST FIRST:** slot-space identity must be sufficient for everything the closed loop does. The moment something in-shader needs a `SimThingId`, the bridge returns and the mode difference stops being mere placement. **PREREQUISITE — MEASURE BEFORE BUILDING:** per-stage telemetry ALREADY EXISTS (`tick_event_readback_ms`, `boundary_value_readback_ms`, `boundary_alert_collect_ms`, `boundary_pregrow_fission_ms`, `boundary_structural_ms`, `tick_total_ms`). Run a representative soak first: readback over total is the UPPER BOUND on what closing the loop can save, and the allocation stages are the floor it can never save. If allocation dominates, the architectural case must carry this rung alone — which is legitimate, but must be known rather than assumed. **BINDS 6.0b:** reception must work at BOTH resolution sites. An automaton that can only perceive at the barrier is not autonomous, and a closed loop whose cells cannot listen is just a faster version of the same one-way pipeline. **ORIGIN IN SLOT SPACE (DA review 2026-08-03) -- the riskiest-assumption section was missing its most likely instance.** 6.0b made `Overlay.origin: SimThingId` REQUIRED and unconstructible without; this rung's closed loop stays in slot space and never crosses the identity bridge -- yet the Wei automaton originates overlays intrinsically, so the closed loop mints overlays with NO `SimThingId` in scope. A coder hitting that compile error will reach for a synthesized placeholder origin, the exact forbidden move. Ruling in this row's own vocabulary: **in-loop, origin is carried in SLOT SPACE; `SimThingId` re-attachment happens at the barrier exactly like every other identity re-attachment.** The required-origin type boundary holds at the CPU representation; slot->id resolution must be TOTAL at the barrier and fail closed -- a slot with no admitted SimThing is an admission-integrity failure, never a default origin. | **DA-GRADUATED / merged #1621 @ 1a520eee — the closed loop is the default; CPU-authoritative is a vendorized placement of the same model.** Both modes produce BIT-IDENTICAL `BoundaryRequest` streams from identical crossings **ORIGIN IN SLOT SPACE (DA review 2026-08-03, Owner-directed exit-proof drift correction):** the closed loop originates overlays carrying origin in SLOT SPACE with `SimThingId` re-attached at the barrier like every other identity re-attachment; slot->id resolution is TOTAL at the barrier and FAILS CLOSED, with a planted red proving a slot with no admitted SimThing cannot become a default or synthesized origin., demonstrated per converted semantic with a planted defect; the 13-stage boundary pipeline is unmodified; allocation remains barrier-only in both modes; no in-shader path requires a `SimThingId`; conversion is incremental per semantic with no flag day; the soak measurement is landed as evidence BEFORE any resolution-site code. **DISCHARGES amendments dated 2026-08-02** — the clauses above are the proof obligations those amendments create; this citation exists so the parity gate can see the proof was widened with the scope. | Frontier — coder | DA-GRADUATED / merged #1621 @ 1a520eee |
| 6.3 | `CONTINUOUS-POSTURE-SOAK-0` | Phase 6: continuous posture (batched generations) + N-generation soak under forced lag. **BINDS 6.0 `OWNER-CHANNEL-INTRINSIC-0` (refreshed against the landed surfaces at 8cbd7e3d):** the soak must drive `simthing_spec::reduce_owner_channel_rf` and `reconstruct_owner_channel_rf_map` over many generations and hold the `OwnerChannelRfSteadSurface` bound: retained rows are exactly ownership crossings plus own aggregates, NEVER O(nodes x owners x resources). That surface is new retained state, and the soak is the only rung positioned to falsify its growth curve rather than assert it. A soak that never reads the STEAD surface will report green while the memory profile drifts. **DA AMENDMENT 2026-08-02 (Owner design session) — the soak is also where STALENESS OBSERVABILITY is proven.** This row already soaks UNDER FORCED LAG, which is exactly the condition staleness exists to express. **STALENESS IS DERIVED, NEVER STORED:** it is `parent_generation - latest_integrated_child_stamp`, pure arithmetic over operands that must already exist (6.1's stamp and the parent's own counter). **ITS ONLY HOME IS THE STEAD FIELD COLUMN** — one f32 lane per slot (`slot * n_dims + col`). NO per-node staleness property (O(nodes), stale-able — the exact flat-stamping defect 6.0 deleted) and NO history log (O(nodes x generations), unbounded — the accretion that put 1,683 rows in the inventory). The field column is the REPRESENTATION, not a second copy beside a truth. **WHY IT IS ADMISSIBLE ON STEAD AT ALL:** ownership is an IDENTITY (no magnitude, no gradient, does not diffuse) and correctly became an intrinsic property in 6.0; staleness is a DIFFUSING MAGNITUDE and is therefore a scalar field by type, not by convenience. Identities become intrinsic stem-cell properties; diffusing magnitudes become STEAD columns. **THE SWEEP IS SEEDED, NOT GLOBAL:** seed at the `boundary_simthing_id` carried by each retained `OwnerChannelRfCrossingFlow` — already computed every generation by 6.0(e) and already described as living on the ordinary STEAD tree surface — and bound it by horizon. Cost scales with `crossings x horizon-neighbourhood`, NOT lattice size. A naive whole-lattice registration works and is silently ~100x more expensive at 1500 stars; that is the same shape as the recorded Gu-Yang tiling debt and must not be repeated. **INERT BY DEFAULT:** sweeps are driven by registrations, so a world with no async seam pays exactly zero — no registration, no column, no sweep. **MECHANISM, NOT SERVICE:** staleness is a scalar field with authored diffusion; it is never "the health monitor" (stead_spatial_contract section 9 discipline — the same fence that forbids "Gu-Yang is a border/frontline semantic service"). **DA AMENDMENT 2026-08-02 (Owner) — THE SOAK IS ALSO THE DUAL-MODE EQUIVALENCE PROOF.** 6.2b makes resolution site a choice, so this soak must run BOTH modes and demonstrate they are indistinguishable: identical crossings produce bit-identical `BoundaryRequest` streams over N generations, under forced lag, with the 13-stage boundary pipeline unmodified. This is the rung that makes the vendorized build TRUSTWORTHY rather than merely available — without it, "indistinguishable" is a claim rather than a measurement. It also carries the PREREQUISITE SOAK for 6.2b: the per-stage telemetry already exists, so the readback-over-total ratio and the allocation floor are recorded as evidence BEFORE any resolution-site code is written. **BINDS 6.0b — THE SOAK IS THE AUTOMATON'S FALSIFIER.** It must drive the CLOSED CAUSAL CYCLE over many generations: receive -> CostBand -> EML -> originate -> route -> receive. **The cascade needs NO authored depth bound and must not acquire one:** neither `owner_silo_disburse_down` nor `runtime_local_allocation` contains a convergence loop (both single-pass), and disbursement is depth-banded via `max_disbursement_band` -> `band_layout.disburse_band(max_depth-2, max_depth)`. Each direction runs EXACTLY ONCE per generation, so a full cycle takes one generation and is bounded by O(tree depth) — the cycle is PACED BY GENERATIONS, which is how a CA paces itself. The soak proves that pacing holds under load rather than asserting it. | **DA-GRADUATED / merged #1624 @ fdc11efb — Phase 6 COMPLETE; the StemThing census trigger fires.** Soak exit-proof per the phase row; paced posture unchanged (scenario-neutral posture regression green). **SCOPE COVERAGE (DA review 2026-08-03, Owner-directed exit-proof drift correction).** The soak DRIVES `reduce_owner_channel_rf` and `reconstruct_owner_channel_rf_map` over many generations and MEASURES the `OwnerChannelRfSteadSurface` growth curve: retained rows are exactly ownership crossings plus own aggregates and NEVER O(nodes x owners x resources), with a planted red on a growth curve that drifts to the product form -- asserting the bound instead of measuring it is the failure this row exists to prevent. **STALENESS IS DERIVED, NEVER STORED:** it is `parent_generation - latest_integrated_child_stamp` living ONLY in a STEAD field column (one f32 lane per slot); a planted red proves neither a per-node staleness property nor a history log is admitted. **THE SWEEP IS SEEDED, NOT GLOBAL:** seeded at each retained crossing's `boundary_simthing_id` and horizon-bounded, with cost scaling by `crossings x horizon-neighbourhood` and a planted red on whole-lattice registration. **INERT BY DEFAULT:** a world with no async seam registers nothing, allocates no column, and runs no sweep -- ZERO bytes and ZERO dispatches, proven. **DUAL-MODE EQUIVALENCE:** both resolution sites produce bit-identical `BoundaryRequest` streams over N generations under forced lag with the 13-stage pipeline unmodified. The 6.2b prerequisite telemetry (readback-over-total ratio, allocation floor) is RECORDED as evidence before any resolution-site code exists. **DISCHARGES amendments dated 2026-08-02 and BINDS `OWNER-CHANNEL-INTRINSIC-0`** — the clauses above are the proof obligations those amendments create; this citation exists so the parity gate can see the proof was widened with the scope. | Std — Grok | DA-GRADUATED / merged #1624 @ fdc11efb |
| 6.4 | `SLOT-LOGICAL-IDENTITY-0` | **Phase 6→7 gap: StemThing-A (anchor: `docs/stead_stemthing_unification.md`; Tier-2 slot-identity law landed with this row's mint — core design §3, shape (a) ruled from `scripts/ci/stemthing_slot_census.tsv` — tree-derived, fully dispositioned, zero BLOCKER; reproducible via `stemthing_slot_census_check.sh`).** Implement the logical/physical split the law states: `SlotIndex` = stable logical identity; physical row binding minted at boundary upload through the id→slot binding table (`world_state` map — census row C1-id-slot-map); epoch rebind lands as an `AnchorRemapOperation::EpochRebind` variant on the EXISTING `AnchorLocusRemap` door (5.2) — naming per DA `5194703997` (compaction is a motive; rebind is the event); typed `RemapSubject::{PropertyLocus,ObjectRow}` on the same record (FIRST-STEP STOP `5194589941` accepted: locus granularity alone cannot represent object-row rebinds); never a second record; `SpecDelta::ScriptedInstanceSlotChanged` retyped from raw `u32` to the typed boundary (census P1 REPLAY-RECORD row); the two index spaces (matrix-row vs dense-cell) named as distinct types or documented type aliases so conflation is visible (census K3 row). Zero per-access indirection between epochs. **STOP CONDITION (Codex, 2026-08-04 — remap granularity):** before implementing, demonstrate `AnchorLocusRemap`/`AnchorRemapOperation` can represent every object-row rebind EXACTLY ONCE — including SimThings with zero anchored property loci, without N-loci duplication — inside the one canonical history; if it cannot, STOP and escalate (extend the existing history authority, never mint a second log). | A forced epoch remap (scrambled physical rows, unchanged logical ids) reproduces a multi-generation run **bit-exactly** — CPU and GPU, planted-defect red on an order derived from physical row; a pre-remap replay replays bit-exactly against post-remap state via the remap chain; `AnchorLocusRemap` endpoint validation extended to the new variant with its existing witness discipline; zero new history mechanisms (grep-proven). **DA-GRADUATED / merged #1655 @ 1d0e1ab9 — StemThing-A's identity half is landed.** **The FIRST STEP STOP is the reason this rung is correct.** Fable proved the existing `AnchorLocusRemap` door COULD NOT carry the record: keyed `(sim_thing_id, property_id)` on a flat struct with four `Option` endpoints and eight non-row-level operations, so zero-anchor objects were invisible to demand and a k-loci object duplicated k times. The decisive corroboration was that `SpecDelta::ScriptedInstanceSlotChanged { current_slot: u32 }` **already persisted a row-level slot change OUTSIDE the typed history** — census row `P1-spec-replay-slot-delta` had been pointing at the missing granularity since the census ran. **DA-verified independently at the exact head, constraint by constraint:** (1) `RemapSubject::{PropertyLocus{property_id, from_col, to_col}, ObjectRow}` — `ObjectRow` is a **unit variant, structurally incapable of carrying a column**, and the `Option<SimPropertyId>` absence-encoding was refused as the `None`-means-something-else shape; slot endpoints stayed record-level, which revealed that **slot was always a property of the record and column always a property of the locus subject** — the flat struct simply could not say so. (2) Demand derives from `slot.rs:343` *"Snapshot THE binding table (id → slot) — the demand source"*, with zero-anchor objects present by construction, and **both** over-emission directions RED in one production referee (`epoch_rebind_duplicate_object_row_and_unchanged_column_locus_red`). (3) `EpochRebind` 20 refs, **`EpochCompaction` 0** — the record names the event, not one motive for it. **Both `5193918971` amendments verified:** 9.2 carries `BINDS 6.4` naming the forbidden alternative in the Capability Binding Law's required form — *"NEVER on physical row position or allocation order"* — and the row-order mutant is planted in **production** `TopologyState` (`crates/simthing-kernel/src/reduction.rs:156`, the RF reduce-up fold), REDing on CPU **and** the real OrderBand GPU path, not a test-only executor. Forced physical-row scramble with fixed logical ids is multi-generation bit-identical CPU==GPU against the never-scrambled run; pre-remap replay resolves bit-exactly through the single chain. `CellSpaceIndex(u32)` is a distinct newtype, closing K3 so the conflation is no longer expressible by silence. Census PASS `rows=25 universe=51 assigned=51 dup=0 missing=0 blockers=0`; one history confirmed by type-grep. **Ceremony audit accepted:** orchestration caught five accidental future-rung status stamps (6.5, 7.1, 8.2, 11.1, 12.2) and reverted them to `TODO` in a docs-only successor commit touching no proof — DA re-verified that 6.4 was the only non-`TODO` unstamped cell. | DA-reserve · Frontier — Codex 5.6/Fable | **DA-GRADUATED / merged #1655 @ 1d0e1ab9** |
| 6.5 | `RESIDENCY-TIER-VOCABULARY-0` | **Phase 6→7 gap: StemThing-A.** The residency lane's authored vocabulary, per anchor §2/§5 under the Sol-amended laws: conserved capacity as an ordinary lane (`free + in_flight + occupied = capacity` exact, hard-currency discrete transfers only); tier as a sealed price vector `{lane set, residency_class, adjacency participation, churn class, unit cost}` — open across authoring, **frozen at session admission**, closed engine vocabulary beneath; census lanes only on granting-active nodes (sparse, inert-by-default); NO granting arena, NO allocator retirement, NO placement mechanics (all StemThing-B, post-8.2). Capacity is the conserved quantity; extents remain kernel physics behind the placement boundary (anchor §1 corrected identity). | **GRADUATION OBLIGATION (recorded 2026-08-04; clauses landed early in #1628): at this rung's stamp the DA verifies the 8.1/8.2 `BINDS 6.5` clauses are present, unweakened, and correct against the GRADUATED 6.5 vocabulary, carrying any final alignment edits in the stamp PR — verification is a stamp-time DA act, not an automatic gate.** Tier vocabulary admits and freezes at session build with a planted-defect red on a mid-session mint; partition invariant judged exactly over inline synthetic grants incl. in-flight; a `match tier` or domain noun in any tier row is a spanned admission error (planted red); zero engine branching on tier (grep-proven); census columns absent on non-granting nodes (memory profile witness). **DA-GRADUATED / merged #1659 @ 8486ede9 — STEMTHING-A IS COMPLETE (6.4 identity + 6.5 residency vocabulary).** **DA-verified independently at the exact head, and the central claim is stronger than the exit-proof asked for:** "engine never branches on authored tier identity" was to be **grep-proven**, but it landed as a **TYPE BOUNDARY** — `ResidencyTierRow` carries `name` while `ResidencyDrawShape` carries `{rows, lanes, shape, adjacency, churn}` and **no name or tier id at all**, so consumption cannot branch on identity because the identity is not in the type it receives. Unconstructible beats grep-proven. `unit_cost_rows` is documented as CostBand `C` and `rows` as `N·C`, so **residency pricing IS a CostBand rather than a rival pricing mechanism** — 6.1b's algebra reused, not paralleled. Session vocabulary freezes through the one production door `SpecSessionState::admit_session_residency_tiers`, second admission refusing `MidSessionTierMintRefused`; no dynamic tier door exists. `ResidencyCapacityPartition::verify_exact` uses `checked_add` so overflow cannot silently wrap. `materialize_granting_census` **filters before it collects**, so non-granting nodes are absent from the map rather than zero-filled — the distinction the exit-proof was written to force. All four DA-mandated mutants were planted in **PRODUCTION** paths per `5197291879` and RED: freeze refusal removed, `in_flight` credit omitted in `issue`, `tier.name` branched in `resolve_residency_draw`, granting filter removed in the census materializer. Envelope exactly 9 files, +1035/−2, no successor surface; 271 passed / 0 failed across core/driver/sim/spec. **8.1/8.2 `BINDS 6.5` VERIFIED AT THIS STAMP AS A DA ACT (not automated, not discharged by green CI) — both hold against the vocabulary that actually landed, and NO alignment edit is required.** 8.1: the landed partition is exactly `free + in_flight + occupied = capacity` over inline synthetic grants. **Referee independence is preserved by construction** — `verify_exact` is authored as *"the 8.1-class boundary oracle's **operand**"*, so 8.1's judge takes the partition as input rather than importing its verification; **8.1 must NOT reuse `verify_exact` as its own oracle**, and that is recorded here because it is the easiest thing to lose. 8.2: arena enrollment is expressible in the price-vector vocabulary, and *"no arena-local participant taxonomy"* is enforced by the same type boundary — an arena consuming residency cannot see a tier name. | DA-reserve · Frontier — Codex 5.6/Fable | **DA-GRADUATED / merged #1659 @ 8486ede9** |
| 7.1 | `ACTIONBAND-ADMISSION-DOOR-0` | Mint the one `simthing-spec` session-build ActionBand template admission door. Bind templates to the **existing Phase-5 threshold registrations/`BandCrossingDelta`** rather than a new crossing path. Land the closed target-form vocabulary, axis budget, bounded subordinate/dependency span, EML/emission-binding references, GPU-only/CPU-shadow type separation. No movement vocabulary. **CONSUMES the HORIZON-ENTRY markers** on `crates/simthing-kernel/src/decision_ingress.rs` and `crates/simthing-sim/src/overlay_lifecycle.rs` (dated 2026-08-08, expire ~2026-11-06): binding through the sealed crossing surface IS the consumer those markers were held for, so the markers come off in this rung's diff. | A depth-1 synthetic ActionBand is admitted and bound to an ordinary anchored threshold; changing the observed value produces the existing sealed crossing and resolves the opaque ActionBand binding. A planted rival crossing path is impossible/red. Predicate-only target, unretained velocity, over-axis-budget, over-subordination, mid-session template mint all fail admission. **DA-GRADUATED / merged #1694 @ 66182aeb — THE DOOR IS OPEN AND IT IS A TYPE BOUNDARY.** 17.19 discharged by UNCONSTRUCTIBILITY, not by grep or planted red: `ExistingThresholdRegistrationIndex` is a private-field newtype with no public constructor, whose sole construction site is inside `admit_band` after bounds-validating against the real `EmitOnThresholdRegistration` table and checking the registration's column against the template's declared channels. **A rival crossing authority cannot be built from outside the module — it is not forbidden, it is unspellable.** The seven §4.2 target forms landed as a closed `ActionBandTargetSpec` enum with no eighth variant and no predicate-only escape; both `HORIZON-ENTRY(2026-08-08)` markers are consumed (−6 lines each), which is the proof the door bound to the held crossing substrate rather than paralleling it. | Frontier | **DA-GRADUATED / merged #1694 @ 66182aeb** — `HD-RECEIPT: e2bb63c363f4`; every claimed battery reproduced by the DA at the exact head (focused 5/5, spec 44/44, kernel 79/79, sim 42/42) since CI runs no cargo tests; mid-session mint refused by the stateful session door itself |
| 7.2 | `ACTIONBAND-GPU-EXECUTION-0` | Land sparse GPU templates/instances, `StateCurrent/StateNext`, depth-1/2 fast shape, pure EML payload execution and fixed emission-binding lowering. All numerical authority remains GPU; CPU receives semantic/structural deltas only. Recurse-semantically/flatten-physically from birth. **CONSUMES the HORIZON-ENTRY markers** on `crates/simthing-kernel/src/decision_ingress.rs` and `crates/simthing-sim/src/overlay_lifecycle.rs` (dated 2026-08-08, expire ~2026-11-06): binding through the sealed crossing surface IS the consumer those markers were held for, so the markers come off in this rung's diff. | CPU/interpreted/JIT or applicable faithful-lowering parity under existing EML semantics; no CPU evaluator/scheduler/mirror; one-node/depth-1 path measures against existing crossing cost; program/binding bucketing demonstrated; structural request is GPU-authorized and CPU-applied without re-evaluation. **DA-GRADUATED / merged #1699 @ 6b2792b7** — §15.2 discharged by measurement, not assertion: **0 target-evaluator dispatches, 0 world re-gathers**, residual compute overhead within noise, so the attached delta IS the EML program plus its fixed emissions and nothing else. §15.7 is satisfied at the *binding type*: `action_state_current` is `var<storage, read>`, `action_state_next` is `read_write`, with whole-buffer swap as the sole generation boundary — per-row newer-buffer selection was removed as the constitutional defect it was. The StateCurrent→StateNext carry preserves non-crossing rows **without evaluation or re-gather**; measured at cardinality 1 it is `4,096 ns` to copy 32 bytes, i.e. **fixed command overhead, not bandwidth** — whether that stays flat or grows with active cardinality is 7.3's obligation, with §15.6 compact active masks/lists as the named remedy. The world-value write destination is gone: `@binding(7) values` is `var<storage, read>`, load-only. | Frontier | **DA-GRADUATED / merged #1699 @ 6b2792b7** — rulings `5232279126` (authority split), `5232410305` (performance revision + TEST-BUDGET JUSTIFIED), `5233247368` (carry accepted, measurement required), `5233469238` relay; `HD-RECEIPT: 52bfd6f2c34d`; batteries re-run by DA at the exact head (7.2 5/5, frozen 7.1 admission 5/5) |
| 7.3 | `ACTIONBAND-RECURSIVE-COMPOSITION-0` | Land pre-admitted subordinate activation, dependency spans, collapse, concurrent sibling evaluation, generation pacing, inline trivial requirements, and native RF/scalar-CostBand multisource binding. **Explicitly fence persistent cross-arena scarce holding/atomic common-depth semantics to post-8.x Vector CostBand.** | Parent sets child-next state from a real Phase-5 crossing at generation `t`; children evaluate from distinct Phase-5 crossings at `t+1`, emit into ordinary authoritative next-state lanes, and collapse inactive with no retained terminal record; a fresh consequence-caused Phase-5 crossing lets the parent resolve against ordinary current state at `t+2`. Siblings progress concurrently without physical-row semantics. The inline multisource proof writes a source-admitted ordinary RF claim and scalar CostBand progress, then consumes them through the existing generic accumulator and sealed CostBand resolver; the legacy structural-only door rejects that shape. Any template requiring unadmitted atomic multi-arena hold fails/defer-closes. No crossing replay, CPU scheduler, runtime pointer recursion, semantic append order, or ActionBand-local RF/sink authority. **DA-GRADUATED / merged #1705 @ 184689e5** — the carry curve required at 7.2 is DELIVERED and it REFUSES the §15.6 remedy on evidence: `1 row/32 B/4096 ns`, `64/2048 B/4096 ns`, `1024/32768 B/5120 ns`, `4096/131072 B/8192 ns`. Flat to 64 rows, marginal slope in the linear region exactly **1 ns/row**, cost-per-row falling 4096→2 ns across the range. Compact active masks/lists would trade the constitutional whole-buffer Current→Next property for a ~1 ns/row term — **not justified, and now provably so rather than presumably so.** Depth-2 rides the depth-1 entry as a CONSTRAINT, not an aspiration: dependencies present without the depth-1 fast path returns `RecursiveShapeDeferred` rather than minting a second path. The persistent `ACTIONBAND_STATE_TERMINAL` bit is GONE — parent completion is authorized by a fresh later crossing derived from ordinary next-state, not a retained completed-task bit or replayed batch. | Frontier | **DA-GRADUATED / merged #1705 @ 184689e5** — pre-dispatch ruling `5233617128` (A1 depth-2 ownership, A2 core-surface trigger — both discharged: depth-2 claimed, zero `simthing-core/src` diff); `HD-RECEIPT: f7dea5471359`; batteries re-run by DA at the exact head (7.3 5/5, inherited 7.2 5/5, inherited 7.1 5/5); `TEST-BUDGET` JUSTIFIED |
| 7.4 | `ACTIONBAND-SPATIAL-VENDORIZATION-0` | Born-mortal/scenario-neutral spatial witness proving the old useful movement substrate **through ActionBand**, not as a peer movement facility. Re-prove §13.1 fences: ActionBand target only, PALMA field not path object, adjacent local step, ambiguity fail-closed, sealed crossing provenance, CostBand for consumption, logical identity/placement law. Production core gains no movement action vocabulary. | Field/overlay-only changes redirect the spatial ActionBand target-seeking result with zero movement-specific destination authority. Adjacent step/ambiguity mutants red. Grep/type proof: no peer movement planner/path/crossing facility in core; witness can be reaped without removing ActionBand capability. **DA-GRADUATED / merged #1709 @ 2800853e** — the reap requirement is STRUCTURAL, not demonstrated: the witness is homed in `simthing-workshop`, whose Cargo graph shows **nothing depends on it**, so "reaping leaves ActionBand intact" is a fact about the dependency arrow rather than a claim. The entire crate diff is confined to `simthing-workshop/` — **zero production engine `src/`** — so ActionBand was consumed, never widened. Field-only redirect proven on the REAL GPU path with zero skips: same frozen template, same active-instance set, same thresholds; only uploaded field values change and the sealed commitment selects logical B then logical C, with candidate append order deliberately reversed to prove it non-semantic. Slot permutation `{A→10,B→20,C→30}` vs `{A→77,B→10,C→55}` chooses logical B under both while the physical key moves `20 → 10`. | Std — Grok | **DA-GRADUATED / merged #1709 @ 2800853e** — pre-dispatch ruling `5244437034` (A1 workshop homing — held exactly); `HD-RECEIPT: 8e91e677346a`; batteries re-run by DA at the exact head (7.4 9/9 with zero GPU skips, inherited 7.3 5/5, 7.2 5/5, 7.1 5/5); hosted `TEST-BUDGET PASS 0`, `WORKSHOP-HOMING-DETECTION PASS 0`, detachability `production_coupling=0` |
| 7.5 | `ACTIONBAND-SEMANTIC-SHADOW-0` | Bind generic stamped ActionBand crossings/terminal/structural deltas to existing CPU semantic shadow/readback surfaces. Human-readable designation is re-attached only after GPU authority, never used to dispatch. Retires any movement-specific readback obligation by making it an ordinary projection of ActionBand state. **CARRIES the 12.5 icon-descriptor obligation** retired from the invalidated `MOVEMENT-AUTHORITY-READBACK-0`: icon descriptors express real transit as an ordinary projection of ActionBand state, with ZERO icon-layer change. **BINDS 6.0 `OWNER-CHANNEL-INTRINSIC-0`:** readback reports the RESOLVED owner via `simthing_core::owner_channel::resolve_owner` (returns `Result<OwnerRef, OwnerResolutionError>`), PROPAGATES its failure rather than aliasing a foreign or malformed target to `unowned`, and is STAMP-QUALIFIED -- the generation stamp travels beside the resolved owner so authoritative transit is never silently stale under 6.1 async. | Same opaque GPU ActionBand run with semantic labels changed produces bit-identical numerical results; generic readback reports stamped identity/designation after the fact; no continuous numerical mirror and no movement-specific authoritative readback path. DISCHARGES BINDS `OWNER-CHANNEL-INTRINSIC-0`: a planted red proves a foreign/malformed target cannot alias to `unowned`, and readback reports the generation stamp beside the resolved owner. **FIELD-NEUTRALITY GATE (Owner-directed Gu-Yang remediation, 2026-08-10; DA-minted):** 7.5 may NOT graduate until its semantic-shadow/readback/public vocabulary is inspected for PALMA-only assumptions — *"PALMA is the only field execution input"*, *"movement progress == PALMA progress"*, *"CostBand affordability == physical channel throughput"*, *"no representation for flux/saturation/stall/choke provenance"*, *"human-readable movement semantics required to interpret numerical progress"*. **Two lawful outcomes: (i) already field-neutral** — the opaque numerical/readback schema reports ActionBand state and bound observable identities without encoding PALMA-only semantics; record that proof and graduate; **(ii) the incomplete story is frozen** — amend the in-flight handoff/implementation BEFORE graduation. **A compatibility shim in a successor rung is NOT an admissible outcome.** This inspects semantic/public vocabulary only; Gu-Yang and ActionBand numerical authority remain GPU-only and the CPU shadow computes neither. The coder must re-orient first: 7.5's handoff predates the Gu-Yang anchors landed at `3b93add1`, so a relay on the older stamp `FAIL(stale-orient-receipt)`s. **DA-GRADUATED / merged #1719 @ beccd380 — FIELD-NEUTRALITY GATE OUTCOME (i): FIELD-NEUTRAL, proven POSITIVELY.** The gate's two lawful outcomes are a typed enum and the chosen one is asserted in code (`FIELD_NEUTRALITY_OUTCOME == FieldNeutralityGate::FieldNeutral`), not recorded in prose. Per DA ruling `5246937280` A1 the proof is positive rather than negative: a synthetic **non-PALMA** bound observable (`synthetic-rf-grant-axis-v1`) round-trips through readback with no PALMA special-casing — **no Gu-Yang implementation and no 7.5a work required**, which is why its absence would have been unexplained. **Identity blindness is proven against ONE authority, not two agreeing runs:** one compile, one structural admission, one GPU dispatch, one sealed authority, then two post-authority semantic views differing only by label — same `authored_id`, same admission binding id, same generation/slot/col/event_kind/value bits/actor/owner/plan fingerprint. Comparing separate executions would prove determinism; comparing two views of one authority proves the label **cannot reach** the authority. Association is sealed by opaque `ActionBandSessionOrigin`: the free `dispatch_and_seal` door is gone, so execution A cannot be sealed under compile B; equal-fingerprint foreign logical admission REDs `FrozenCompileBindingMismatch` and equal-shape foreign structural session REDs `SessionOriginMismatch`. | Std — Grok | **DA-GRADUATED / merged #1719 @ beccd380** — rulings `5246828092` (Gu-Yang mint + gate), `5246937280` (A1 positive-proof amendment); `HD-RECEIPT: edeb59f58239`; batteries re-run by DA at the exact head with **zero GPU skips** (7.5 13/13, inherited 7.4 9/9, 7.3 5/5, 7.2 5/5, 7.1 5/5); detachability `production_coupling=0`; `TEST-BUDGET` INSPECT justified in-ledger |
| 7.5a | `ACTIONBAND-FIELD-TRIAD-PROGRESS-0` | **Gu-Yang remediation, rung 1 of 3 (Owner-directed 2026-08-10; Fable-approved re-derivation).** Bind generic ActionBand progress/band operands to existing STEAD/PALMA/**Gu-Yang**/RF outputs as native GPU inputs — **no new field solve, crossing surface, target form, or throughput model.** The omission was load-bearing: PALMA answers direction/potential and CostBand answers sink price, but **neither answers how much conserved quantity may actually traverse a constrained channel this generation, or where it saturates.** Gu-Yang already owns that answer, so ActionBand **consumes native Gu-Yang/RF outputs and never reconstructs a private throughput model.** Admit resident field outputs as ordinary anchored observables/bindings: available flux, realized flux, saturation/stall magnitude, sanctioned choke/contest projections. Add the **direct resident-output fast path** — where the authoritative observable already exists, bind to it rather than rerunning equivalent EML/field work (§15.2 *ride already-hot sweeps, including Gu-Yang*). Add the **closed per-binding conserved-progress bound-source declaration** (semantically `None`, `RfGrant`, `GuYangAvailable`, `GuYangRealized` — closed set of four; spelling rung-local). Apply the existing **bounded-feedback admission contract** to emissions feeding a field-seeded/RF recurrence — generation pacing bounds update RATE, not feedback GAIN. Gu-Yang stays instantaneous; **no stall-history state is minted implicitly.** Preserves §17.19 single crossing surface and §17.20 closed target forms. **Gu-Yang adds band/progress observables, NOT a target form.** No local-minimum/adversarial navigation. | A synthetic conserved-channel ActionBand consumes a REAL Gu-Yang / `FieldSweepRegistration` output with **zero ActionBand-local throughput solver**. Reducing admitted channel capacity reduces executable progress while target and ActionBand EML stay unchanged; restoring capacity restores progress. The same workload on resident exact field output performs **no duplicate field solve and no CPU numerical readback**. Each conserved progress leg has **exactly one** admitted bound source; planted **double-bound** and **no-bound** mutants fail admission or RED under a semantic-equivalence witness. A field-seeded positive **unbounded** feedback gadget fails bounded-feedback admission while a bounded/decaying equivalent admits. A planted private throughput/congestion/saturation implementation **REDs against the native result**. **DA-GRADUATED / merged #1723 @ b7881687 — Option B per adjudication `5248796666`.** The blanket `ConservedProgressEmlPayloadForbidden` is DELETED: it was stricter than §4.6, which permits computing `q_desired` and constrains only `q_exec`, and it would have made 7.5c's required chain unbuildable. The bound rides the **pre-existing reserved `auxiliary1`** on `ActionBandEmissionBindingGpu` — a field already declared in both the Rust and WGSL structs and already validity-checked — so no new plan structure, buffer, bind-group entry, reference kind, or authority was added. The existing `valid_shape` gate widened from `== 0` to the closed range `CONSERVED_BOUND_NONE..=CONSERVED_BOUND_GU_YANG_REALIZED`; wire code 4 REDs. **The shader evaluates lawful band EML FIRST, then clamps conserved execution to the SIGNED interval between zero and native `crossing.post_value`** — preserving canonical Gu-Yang orientation rather than `abs(flux)` — and `auxiliary1 == 0` bindings are untouched. **The `2*x` amplifier proof was RE-AIMED, not deleted, and is stronger for it:** the amplifier ADMITS, `q_desired` is bit-exactly `2.0 * flux`, and `q_exec` is bit-equal to native flux at high, lowered and restored capacity — proving the actual law rather than proving the composition inadmissible. | DA-reserve · Frontier | **DA-GRADUATED / merged #1723 @ b7881687** — adjudication `5248796666` (Option B; A and C rejected), pre-dispatch ruling `5248198350` (A1 producer/binding shape); `HD-RECEIPT: 1d744631ed8b`; batteries re-run by DA at the exact head with no adapter skip (7.5a 4/4, inherited 7.5 13/13, 7.4 9/9, 7.3 5/5, 7.2 5/5, 7.1 5/5 — including the door 7.5a extended and the shader it changed); `TEST-BUDGET` INSPECT justified, four distinct authority contracts, no new `#[test]`; **F15 did not fire** |
| 7.5b | `ACTIONBAND-SPATIAL-FLUX-WITNESS-0` | **Gu-Yang remediation, rung 2 of 3.** Prove the generic Field-Triad ActionBand binding over structural spatial adjacency **without making movement the source of the general mechanism.** Born-mortal, scenario-neutral, workshop-homed. STEAD/overlay state supplies local conditions; **PALMA** supplies authoritative local potential/impedance direction where routing applies; **Gu-Yang/RF** supplies **signed** realizable conserved throughput/saturation on the capacity-bearing channel; ActionBand binds those native values as progress operands; CostBand remains the sink/work quantizer **only where a sink is actually present.** No movement-local congestion model, planner, path/predecessor object, or saturation listener. **Saturation does NOT authorize rerouting or search** — the §18 local-minimum/adversarial-navigation fence stands. | Same target and PALMA potential, different Gu-Yang channel capacity ⇒ **same lawful descent identity, different per-generation executable progress**. A saturated capacity-bearing lane limits/stalls progress without changing target vocabulary or invoking route search. **OPPOSED-DEMAND WITNESS (mandatory):** equal opposed demand on one conservative channel produces **mutual stall/contest**, and an `abs(flux)` mutant that advances both ActionBands **REDs** — a capacity-monotonicity test proves magnitude but not direction. Canonical edge/order/sign authority comes from the admitted Gu-Yang registration; ActionBand cannot reorder or reinterpret it privately. Removing the witness after proof removes **no** production ActionBand/Field-Triad capability. **DA-GRADUATED / merged #1732 @ 2ef325b7.** A1 (ruling `5260442354`) discharged **without a production observation hook**, which was its STOP condition: the witness declares a **dual emission on the same band** — a conserved binding that passes through 7.5a's clamp, and a second `auxiliary1 == None` binding carrying the same payload that does not. The pre-clamp operand is therefore read through a seam that already existed. **The `abs(flux)` mutant is a REAL GPU dispatch** (`abs_eml` through `run_two_leg_opposed`), not a CPU-simulated one, and it loses opposition at the pre-clamp seam before the clamp can mask it. **Contest is distinguished from absence:** the 2×2 grid uses mixed-sign neighbours so gross flux exceeds the magnitude of net flux, making native stall (gross minus that magnitude) strictly positive — proving two opposed movers contesting rather than nobody moving, which a net-flux-only observation could not tell apart. | Std — Grok | **DA-GRADUATED / merged #1732 @ 2ef325b7** — pre-dispatch ruling `5260442354` (A1 pre-clamp), class-hardening `5261711347` (#1733 @ 104afc62); `HD-RECEIPT: c9f03eccc77a`; batteries re-run by DA at the exact head with **zero GPU skips** (7.5b 5/5, inherited 7.5a 4/4, 7.5 13/13, 7.4 9/9); workshop-only diff, zero production `src` |
| 7.5c | `ACTIONBAND-FULL-FIELD-TRIAD-MOVEMENT-VENDOR-0` | **Gu-Yang remediation, rung 3 of 3 — OWNER ADDITION.** **Do NOT reopen 7.4 to retrofit Gu-Yang.** Build a NEW vendorized movement model consuming the corrected complete intrinsic substrate, preserving 7.4 as historical evidence while making the corrected model a **new positive proof**. Required composition: location Property/coordinate → movement ActionBand target (`LocusRadius`/`PalmaReachableSet`, already admitted) → STEAD + overlays compose conditions/pressure/impedance → PALMA lawful local potential descent → **Gu-Yang/RF signed realizable lane throughput + saturation** → per-binding bound source selects the **one** native executable-progress authority → CostBand quantizes consumed movement resource where authored → ActionBand crossing/EML → intrinsic Overlay emission or already-sealed structural consequence → location value/velocity advances → next generation repeats until the target condition satisfies. The human-readable vendor may say `movement_points`, `fuel`, `lane_capacity`; **none of those names enter core numerical dispatch.** **Separate from 7.5b deliberately:** 7.5b proves the generic signed spatial flux binding; 7.5c proves a real vendorized domain composition end-to-end. **The separation keeps movement from defining the generic law while still demanding a production-shaped proof before 8.x.** | **DA-GRADUATED / merged #1738 @ 73b72e6c** — One movement ActionBand reaches the same target under identical PALMA potential at two admitted lane capacities: **route descent identical, per-generation physical movement differing exactly with native Gu-Yang/RF realizable throughput.** With symmetric opposed movers on a capacity-bearing lane, both do **not** advance merely because gross/absolute flux is high — the native signed/stall result governs. Movement-point/fuel consumption is CostBand quantization **downstream** of the selected native progress binding; **CostBand is never lane-capacity authority.** Changing an ordinary overlay that changes field/RF conditions alters movement on the next lawful generation **without invoking movement-specific path/congestion code.** Arrival dissolves/collapses the ActionBand/actuation state through ordinary lifecycle with **no permanent task/movement executor state.** **Delete/rename every human-readable movement designation in the CPU semantic shadow and the GPU numerical result stays bit-identical.** Grep/type proof: no peer `MovementPlanner`, path/predecessor object, congestion service, `FluxThing`, movement-specific saturation evaluator, or rival crossing machine becomes production authority. | DA-reserve · Frontier | **DA-GRADUATED / merged #1738 @ 73b72e6c** (Owner-relayed DA ruling; stamp records machine truth per the merge ritual) |
| 7.6 | `OVERLAY-GERM-ARCHAEOLOGY-0` | **Overlay closure, rung 1 of 4 — anchored by `docs/stemthing_intrinsic_overlay_capability.md` (Owner-adjudicated 2026-08-10; probes §23; laws §18). Probe A.** Enumerate EVERY overlay attach/activate/suspend/dissolve/apply/override/expire route across feeder, sim, kernel, driver, and spec (`work.rs`, `patcher.rs`, `overlay_lifecycle.rs`, `tree_mutation.rs`, `overlay_prep.rs`, `automaton_reception.rs`, `compile/overlay.rs` are the known starting set — the census derives the full set from the tree, never from this list). Classify each route: SEMANTIC-DUPLICATE (migrates to the intrinsic germ), GENUINELY-STRUCTURAL (stays boundary), or DEAD. Deliver the census as a reproducible TSV + checker script (sibling of `stemthing_slot_census_check.sh` — universe pinned, re-derivable, staleness reads STALE). Carries the anchor's keep/migrate/delete disposition obligation (promotion criterion 15). Docs/TSV/script only — zero engine diff. | **DA-GRADUATED / merged #1748 @ e52b5603 — 74 routes across four families (OVERLAY 52 / INHERIT 8 / CROSSING-WRITE 8 / EML-REGISTRY 6), classified 56 GENUINELY-STRUCTURAL / 16 SEMANTIC-DUPLICATE / 2 DEAD, zero unclassified.** Universe pinned at ROUTE level (71 entries) in `overlay_germ_archaeology_census_universe.txt`; `--check` PASS and now CI-wired (#1749 @ 619c15a9); planted unlisted route REDs for its exact reason `FAIL(unlisted-route:core:planted_unlisted_route.rs:dissolve_overlay)` while the same mutation reads `STALE` on `--harvest`. The census is archaeology, not transcription: `inherit_active_overlays` and `resolve_owner` were both found in files ABSENT from this row's starting set and split correctly — the hot inheritance walk migrates, the owner query stays as 7.8a's oracle. Crate scope is stated: `simthing-core` deliberately included beyond this row's list because the germ lives there (24 of 71 routes), `simthing-clausething` deliberately excluded as an authoring surface whose parse product enters already-classified routes. `AN-OVERRIDE` ruled `SEMANTIC-DUPLICATE / migrate` (DA `5289831443`): `DissolveCondition::OverrideReceived` is serde-authorable, admission checks only that the condition list is non-empty, evaluation returns false forever, and the documented attach-side handler does not exist — a bounded-looking overlay that is permanent in practice, the `Never` the type deliberately refuses. Binds 7.7. | Std — Grok | **DA-GRADUATED / merged #1748 @ e52b5603** — pre-dispatch `5289571616` (A1/A2/A3 all discharged), STOP ruling `5289831443`, remand `5290043139`; `HD-RECEIPT: e16ae62d9ce6` |
| 7.6a | `FACILITY-RESIDENT-PLANE-SUBSTRATE-0` | **Overlay closure, prerequisite rung — DA-minted 2026-08-14 on the genuine 7.7 substrate STOP (orchestrator escalation 5293848200, coding STOP 5293823074). Substrate only: ZERO lifecycle semantics.** 7.7 stopped lawfully because three seams do not exist, and each would otherwise force a forbidden shape. (a) **Owning-generation operand.** `AccumulatorTickParams` carries no generation, so the admitted Phase-5 threshold path has no operand for a `deadline_generation` comparison and a lifecycle-local comparator would be the forbidden second comparator. Admit the owning tree's generation through the RESERVED `_pad1` word already present in that uniform, the same vehicle 7.5a used for the conserved bound source in `auxiliary1` — no new buffer, no new binding, no second comparator. (b) **Routed generation provenance.** `DirectiveDeliveryReceipt` carries overlay/origin/target/route and no epoch stamp, so the Routed Lifecycle Epoch Law cannot be satisfied without transporting a foreign absolute deadline. Add the authored-duration plus provenance carrier so a destination can rebase against its OWN generation authority. (c) **Facility resident-plane primitive.** `ActionBandGpuSession` privately owns `state_current`, `state_next` and `generation`; the whole-buffer Current-to-Next swap discipline exists only INSIDE that graduated facility. Extract that discipline into a reusable primitive and have ActionBand consume it. **This is NOT a shared plane.** 7.8's `ResidentNextWrite` requires the destination to belong to the FACILITY-LOCAL resident plane and names `OverlayStateNext` as a legal destination, planting a direct foreign resident-plane write as a RED — so a plane shared between facilities is the defect, not the fix. One swap discipline and one generation boundary; N facility-local planes. No overlay-local epoch selector, per-row freshness bit, or second swap authority. Route drift is reconciled through the graduated 7.6 census by `--harvest` re-derivation, never hand-edit. | **DA-GRADUATED / merged #1758 @ cfc537b9 — the three seams are admitted and the graduated facility is provably unchanged.** (a) Owning generation rides the RESERVED `AccumulatorTickParams` word through the SOLE existing `threshold_crossed()` comparator: zero new buffers, bindings, comparators or crossing surfaces — the 7.5a `auxiliary1` precedent reused rather than re-argued. (b) `RoutedGenerationDuration` carries only private `authored_duration` + `provenance` under `serde(deny_unknown_fields)`, so a foreign absolute deadline is **unrepresentable at the deserialize boundary** — a type/admission proof, and the behavioural forced-skew mutant was correctly NOT pulled forward into a rung with zero lifecycle semantics. (c) The Current/Next whole-plane discipline is extracted into `FacilityResidentPlane` + `FacilityPlaneGenerationBoundary`, with ActionBand as first consumer; facility-locality is enforced by TYPED errors `ForeignPlaneWrite` and `ForeignSwapAuthority`, not by convention, and an N-plane witness advances distinct planes under one boundary. **Preservation is proven, not asserted:** a pinned `PRE_EXTRACTION_NUMERICAL_RECORD` byte oracle of real GPU readback — full `ActionBandStateGpu` structs, projection and emission bits, commitment tuple, satisfied vector — compares bit-for-bit after extraction. Carry unchanged at 1 row / 32 B / 4096 ns median. **DA finding, adjudicated:** ActionBand's pre-existing `generation` was never the swap counter — it advances unconditionally while the swap is conditional on the depth-1 fast path, so the extraction took swap authorities from 0 to 1, not 1 to 2; documented at the accessor. Zero lifecycle semantics, zero new `#[test]` rows, census unchanged at 74 routes. | DA-reserve · Frontier | **DA-GRADUATED / merged #1758 @ cfc537b9** — pre-dispatch `5294698585` (A1 type-proof + A2 pre-post capture, both discharged), remand `5295498578` (silent skip on the rung's own claim; dual-generation adjudicated benign); `HD-RECEIPT: ccec55ce2422` |
| 7.7 | `GPU-OVERLAY-LIFECYCLE-EXTRACTION-0` | **Overlay closure, rung 2 of 4 — the dedicated extraction rung the anchor's §23.1 mandates; never hidden in a composition rung. Probe B.** The largest remaining peer numerical authority is deleted: `resolve_overlay_lifecycle()` is retained as the CPU ORACLE while GPU takes ownership of overlay active/current/next state, dissolve-condition evaluation via EXISTING Phase-5 threshold registrations (field-output-driven lifecycle — blockade dissolves on a Gu-Yang crossing, rationing on an RF balance crossing), and `AfterTicks` WITHOUT a CPU decrement loop. `DissolveCondition` vocabulary maps to EML/threshold registrations at admission. Indistinguishability is the referee: same admitted state + recorded schedule → bit-identical lifecycle decisions, oracle vs GPU. Only after equivalence is proven does the CPU evaluator retire from the production path (oracle survives as referee residue). One-history: `OverlayDissolved`-class replay rides the existing stamped crossing/schedule surfaces — no `OverlayHistory` mechanism. | **DA-GRADUATED / merged #1761 @ 3fbdf56f — overlay lifecycle numerical authority is on the real Phase-5 GPU path and the CPU evaluator is a referee only.** Single crossing surface is a STRUCTURAL fact, not an assertion: DA verified in `accumulator_op.wgsl` that `project_overlay_lifecycle_crossing` has exactly ONE call site, inside `maybe_emit_threshold` after `threshold_crossed()` returns true — reachable only through the sole comparator, which is stronger than a planted mutant would be. Level predicates for `PropertyReaches`/`PropertyBelow` execute as lifecycle-only direction modes inside that same comparator; no second detector, dispatcher or CPU predicate landed. `AfterTicks` is compared not decremented, denominated in the owning tree's generation carried by 7.6a's reserved `_pad1` word, and overflow returns `DeadlineOverflow` with no saturation or sentinel arm. Routed epoch proven on the real `AttachOverlay` boundary: source provenance 900, destination activation 7, deadline 11 — foreign absolute 904 RED. Suspension snapshots remaining duration and reactivation rebases; full boundary witness activate 13, active 14/15, suspend 15, paused 16/17, reactivate 18, pending 19, dissolve 20. **The migration proof is rigorous:** `gpu_overlay_lifecycle_preextract_9593ca2e.ldjson` was emitted by `ReplayWriter` from the real pre-extraction CPU route in an isolated detached worktree at `9593ca2e`, is `include_bytes!`-embedded so it cannot drift, and DA confirmed it byte-identical across every branch commit (SHA-256 `b8039da1…`). Carry measured before any compaction. **The 7.6 `OverrideReceived` promotion blocker is DISCHARGED by admission rejection** at all three production doors — `deliver_routed_overlay`, `admit_dispatch_minted_overlay`, and spec compile — as typed `OverlayLifecycleAdmitError::OverrideReceivedForbidden`; it is no longer admitted-and-false. Census re-pinned through the sanctioned `--repin` for exactly the two new routes, zero removals. | DA-reserve · Frontier — Codex/Fable | **DA-GRADUATED / merged #1761 @ 3fbdf56f** — pre-dispatch `5290429128` (four amendments, all discharged), substrate STOP ruling `5293929841`, census-pin ruling `5298015175`; `HD-RECEIPT: 5c6218625bc9` |
| 7.8 | `ACTIONBAND-OVERLAY-ACTUATION-0` | **Overlay closure, rung 3 of 4 — probes C+D fused: the actuation door.** An ActionBand crossing resolves to a fixed admitted `OverlayStateNext` activation/parameterization (no direct world mutation, no domain executor); consequences realize NEXT generation through native substrate (Generation-Paced Actuation Law). One overlay parameterizes STEAD source/falloff, PALMA `W`/terminal value, and Gu-Yang conductance/capacity inputs strictly INSIDE their certified registration envelopes (Field Certificate Envelope Law — runtime variation never mutates adjacency, canonical order, conservation/symmetry, or χ certificates); field-seeded recurrent emissions pass bounded-feedback admission (gain, not just rate). **The landed 7.5c movement vendor RE-ANCHORS as the door's first production consumer**: its actuation path migrates to the intrinsic door with bit-identical GPU results — the oracle-first promotion pattern, movement proving the door rather than the door destabilizing movement. | **DA-GRADUATED / merged #1766 @ f26bba1c — one crossing-to-consequence substrate, three arms, and the two hardest obligations closed at TYPE boundaries rather than by assertion.** `StructuralAuthorization` is `{ request: BoundaryRequest }` admitting only AddChild/Remove/Reparent — no plane, destination, value or column field exists, so a same-facility state-plane write is UNCONSTRUCTIBLE, not merely fenced; `ResidentNextWrite` keys durable identity on `SimPropertyId` + `SubFieldRole` with no row, slot, buffer handle or owner capability representable, and foreign origin rejects as `ForeignResidentLaneAdmission` at the compile door; `RoutedOverlayDelivery` carries only target + overlay, so an overlay-local EML namespace/table/evaluator/cache has no field to occupy. Those discharge the open promotion blocker's cases (5) and (6) at the unrepresentability standard. The routed carrier is `GenerationStamped<RoutedOverlayProduct>` under `serde(deny_unknown_fields)` with NO absolute-deadline field, and its admit runs `admit_overlay_lifecycle`, so 7.7's admission governs the new seam instead of being bypassed by it. **Consume-once is structural:** the session is non-Clone and consumed by `bind_dispatch(self, ...)`, so a second same-generation dispatcher cannot be built; `GenerationBoundCrossingDedupe` clears at the actual `FacilityPlaneGenerationBoundary` advance, so no crossing state outlives its generation. Both rejection paths remain reachable — `DuplicateCrossingConsumption` within a generation, `CrossingGenerationMismatch` across one. The legacy private door is migrated: `submit_commitment_effects` delegates through the shared binding instead of minting `BoundaryRequest::AttachOverlay`. 7.5c parity preserved by real-GPU plan fingerprint; CostBand audit found only boundary reporting and correctly landed NO rewrite. | DA-reserve · Frontier — Codex/Fable | **DA-GRADUATED / merged #1766 @ f26bba1c** — pre-dispatch `5299264210` (A1 routed epoch, A2 structural arm; both discharged as type proofs), history ruling `5302829951`; `HD-RECEIPT: cf41c4ec2316` |
| 7.8a | `DERIVED-SPAN-PROJECTION-INVALIDATION-0` | **Unification sweep, the ONE new architectural rung (Sol/Fable v4, 2026-08-13) — deliberately BEFORE 7.9 so the fractal witness consumes this substrate instead of minting an overlay-local lowering.** One generic PHYSICAL subtree-span substrate over 6.4 logical identity + 6.5 range vocabulary: **(a) downward effective projection via effective-profile interning** — a homogeneous subtree resolves to span → `EffectiveProfile` id (already-composed parameters/program identities); local divergence narrows child spans; dense per-row materialization is a DERIVED CACHE, never semantic ownership, built only where a kernel measurably profits; **(b) downward dirty-span propagation + epoch invalidation**; **(c) source-blind `DerivedDependencyIndex`** — frozen per-session admission metadata mapping AUTHORITATIVE CHANGED LOCI (logical identity + PropertyId + role + optional binding/profile id) → dependent effective spans / STEAD / PALMA / Gu-Yang registrations / derived work, with invalidation keyed by locus NEVER by writer subsystem (a `match change_source` branch is a planted RED); never a runtime-mutable registry or service (planted RED). **Consumers keep their distinct semantic combine laws — no universal value resolver:** overlay inheritance (7.9) is the first GPU consumer; owner effective-view batch resolution adopts the substrate only where useful, `resolve_owner()` RETAINED as the query/oracle surface; capability/tier admission projection binds where the shape genuinely applies; **no owner GPU identity plane before 9.2's interning** (`OwnerRef(String)` stands). **StemThing-B is forward-bound to the span/invalidation substrate, not to any consumer's representation.** | **DA-GRADUATED / merged #1770 @ 563c91c5 — one generic physical span substrate, and the invalidation is source-blind at a TYPE boundary rather than by convention.** A `ChangedLocus` carrying an injected `change_source` field REDs as `unknown field` at the deserialize boundary, so the `match change_source` class is unrepresentable, not merely forbidden — mechanisms-not-domains drift cannot re-enter one layer down. Precision is measured, not argued: a million-row subtree with a 1-row divergence at 500,000 holds **10,001 spans / 3 profiles**, and one changed locus yields `dirty_span_ranges == 1`, `spans_examined == 1`, **`logical_member_rows_scanned == 0`** — no per-member walk at all, proven on the locally divergent fixture rather than only the homogeneous one. Dense materialization is derived cache: delete/rebuild and forced `EpochRebind` preserve bit-identical behaviour by logical identity, and standing plus routed projection stays bit-identical to the retained `build_overlay_deltas` CPU oracle. The `DerivedDependencyIndex` is session-frozen; shape mutation fails `FrozenDependencyShapeChanged` and requires fresh admission. **Public surface is minimized structurally:** `mod derived_span_projection` is PRIVATE with zero `pub` items and 20 candidate exports were reduced to the two genuine cross-crate seams `OverlayProjectionHostChange` and `OverlaySpanProjection`, DA-admitted to `allow/kernel_surface.txt` in the same commit because that gate is bidirectional. A1 satisfied: partial-reconciliation loci remain expressible through the consumer-neutral vocabulary with no StemThing-B widening, API, owner interning or physical-row identity. `resolve_owner()` survives as CPU query/oracle; no owner GPU identity plane before 9.2. | DA-reserve · Frontier — Codex/Fable | **DA-GRADUATED / merged #1770 @ 563c91c5** — pre-dispatch `5311512352` (A1 StemThing-B forward-bind, discharged); DA gate wiring + digest regen authored on-branch; `HD-RECEIPT: 713dc041018b` |
| 7.9 | `OVERLAY-FRACTAL-CLOSURE-WITNESS-0` | **Overlay closure, rung 4 of 4 — probes E+F plus the anchor's §23.2 ADVERSARIAL designer scenario (small and deliberately hard, never vocabulary breadth).** Ancestor residency at scale: one authored policy overlay at a high ancestor over a large synthetic subtree — zero leaf stamping (Ancestor Overlay Residency Law), zero per-generation `O(depth × descendants)` rewalk (Inheritance Hot-Path Law, instrumented not asserted); composition classes admission-explicit with a conjunctive-restriction-weakening mutant RED; cross-tree actuation crossing ONLY the stamped receive/product seam; one-history replay proof. The §23.2 scenario in full: ancestor-scoped standing modifier + descendant-local modifier + generated long key compiled to generic binding + gated/triggered + timed + has-modifier predicate + bounded subtree projection + RF/Field-Triad feedback + ONE lawful temporal cycle (Current→Next) + ONE illegal same-generation algebraic cycle that must REJECT at admission with a span (Temporal Feedback Law). | **DA-GRADUATED / merged #1774 @ c5896e19 — the overlay closure arc is CLOSED.** The anchor §23.2 ten-element adversarial scenario runs as ONE production composition, and the pair that carries the law is distinguished rather than conflated: the lawful temporal Current-to-Next cycle stays GREEN while the illegal same-generation algebraic cycle rejects at admission with a span. Ancestor residency consumes graduated 7.8a rather than lowering locally — **100,001 logical rows resolve to 2 profiles / 3 spans, with 1 rebuilt, 1 dirty, 1 candidate, 0 member rows scanned and 2 semantic overlay instances**: O(distinct profiles), not O(descendants), measured on the real construct. Five falsifiers RED at production boundaries for their exact reasons — conjunctive weakening, leaf-stamping unrepresentable, foreign-tree bypass, all-Current cycle admission, fine-telemetry readability. Telemetry is **three aggregate u64s / fixed +24 transient bytes, independent of template and population cardinality**, with no per-template hot table and nothing sim-readable without an authored observation Property. **One-History adjudicated (DA `5316943177`):** replay rides the pre-existing `ReplayFrame` — `replay.rs` has ZERO diff — with `OverlayActivated` and `BandCrossingDeltasApplied` stamped alongside the standard `shadow_values` checkpoint; the governing row says *existing history surfaces alone*, which the handoff had narrowed to *crossing/schedule* and the orchestrator correctly refused to collapse silently. Feedback bit-identical across generations (`3f4ccccd -> 3f6147ae`, then `3f4ccccd -> 3f6b851f`). Graduated only after the 7.8a regression it exposed was repaired at the source (#1775 @ c69ae5f1) and master returned green. | Frontier — coder | **DA-GRADUATED / merged #1774 @ c5896e19** — pre-dispatch `5315908359` (A1 census pin, A2 telemetry cost fence), One-History adjudication + HOLD `5316943177`, repair ruling `5323104852`; `HD-RECEIPT: edbc505dae18` |
| 8.1 | `CONTENTION-CONSERVATION-JUDGE-0` | **BINDS 6.5 (StemThing-A; clauses landed early in #1628, verified at 6.5 graduation):** the judge's invariant vocabulary extends to exact partitions of the form free + in_flight + occupied = capacity over inline synthetic grants; the judge stays OUTSIDE the StemThing regime (referee independence) — it learns B's invariant, it never emerges from B. Phase 8: RF-1-style conservation judge over CONTESTED FLOWS (oracle-first), **independent of the authored resolution rule**. Combat is a DOMAIN word; the mechanism is N claimants against bounded supply. **BINDS 6.0 `OWNER-CHANNEL-INTRINSIC-0` (refreshed against the landed surfaces at 8cbd7e3d):** the conservation judge operates over `simthing_spec::reduce_owner_channel_rf` buckets keyed by `OwnerChannelScopeKey {owner_ref, resource_key, scope_id}` and judges CONSERVATION ONLY — never ownership uniformity. A container with N distinct participant owners is the NORMAL case and is what makes contention possible at all; rejecting it would make adversarial resolution structurally impossible. The oracle may reconstruct via `reconstruct_owner_channel_rf_map` but must not re-derive ownership by any second path. **BINDS 6.1b `BAND-QUANTIZED-DRAW-0`:** the conservation judge must judge **PER-RESOURCE-CHANNEL**, never across a transformation. A quantized draw consumes `N*C` of the INPUT exactly and MINTS `N*coefficient` of the OUTPUT; a judge that sums both sides reads every legitimate build as a conservation violation and would force the rule to be weakened for the wrong reason. Judge `V = N*C + R` on the input channel; the output is creation, not conservation. **BINDS 6.2 (DA review 2026-08-03) -- the judge's universe includes the SEAM.** 6.2's holding account makes `child + seam + parent` exact at every instant; a judge that sums only child+parent reads EVERY in-flight product as a violation -- the per-resource-channel lesson, one seam over. The judge reads all three balances or it forces the rule to be weakened for the wrong reason a second time. **BINDS 7.3 `ACTIONBAND-RECURSIVE-COMPOSITION-0` (ActionBand constitutional amendment 2026-08-09, `multi-axis-ActionBand-STEAD.md` §21.2):** ActionBand-originated RF claims are ORDINARY claims and enter this judge's declared universe as declared inputs; the judge includes existing `in_flight`/seam balances exactly as already required. **No ActionBand-local conservation oracle is minted** -- referee independence means the judge never emerges from the facility it judges. | **DA-GRADUATED / merged #1779 @ 989ea914 — a conservation referee whose falsifiers bite the ORDINARY path, after a remand that removed three production defect switches.** The first return satisfied the letter of the anti-theatre rule and defeated its purpose: `ConservationSnapshot` shipped `owner_uniformity_required`, `fold_output_into_input` and `omit_seam`, so the production judge contained the three behaviours this rung exists to forbid, each one request away. The worst of them discarded `owner_count` outright and returned a hardcoded RED, and DA grep found the planted flag was the ONLY owner-uniformity logic in the judge — a defect invented in production so a test could kill it. **Repaired to the disagreement shape:** all three fields deleted; `fold_output_into_input_accountant` and `child_parent_only_accountant` now live test-side as rival wrong implementations, and each proof asserts that **production GREENs the very input the wrong accountant flags** — the lawful quantized draw and the in-flight seam. Owner uniformity is proven by ABSENCE: an `include_str!`-anchored scan of the production source over eleven needles, so the claim cannot drift from the file. Ordinary accounting was accepted unchanged throughout — per-channel granted summed from the sanctioned `reconstruct_owner_channel_rf_map`, `checked_add` overflow REDing rather than wrapping, over and under distinguished, and a genuine ActionBand-omission falsifier comparing real rows by scope, resource, surplus and deficit with no flag. Judge stays external to StemThing-A and authors no clearing rule; five binds (6.0, 6.1b, 6.2, 6.5, 7.3) exercised green. | Std — Grok | **DA-GRADUATED / merged #1779 @ 989ea914** — pre-dispatch `5323877029` (A1 orientation preamble, A2 stamp surfaces, A3 anti-theatre), proof-adequacy remand `5337253416`; `HD-RECEIPT: 6c58b7771d63` |
| 8.2 | `CONTENTION-ARENA-EXECUTED-0` | **BINDS 6.5 (StemThing-A; clauses landed early in #1628, verified at 6.5 graduation):** claims/deficits/resolution rules are lane data on the stem cell's existing legs; arena enrollment MUST be expressible in the 6.5 tier price-vector vocabulary so the residency lane joins at StemThing-B as data with zero arena rework; no arena-local participant taxonomy; priority consumes the existing 6.0b seam only. Phase 8: promote contention resolution to the EXECUTED RF arena — claims reduce up, the authored resolution rule allocates, results disburse down. **The rule is AUTHORED DATA, never a kind or enum branch**: proportional (cooperative), priority-ordered (queueing), price-clearing (2.3 order-weight already does this), and later-admitted numerical clearing laws. NOT ALL CONTENTION IS ADVERSARIAL and the engine must not assume it is. **ACTIONBAND AMENDMENT (Owner-committed 2026-08-05; determination at 7.1a):** the executed resolution surface is GENERIC CONSTRAINED CLEARING and may not require preclassification as 'contention' or 'adversarial' — contention is the OVERSUBSCRIBED OBSERVATION of that surface; trivial and contested clearing are one path. **ATTRITION MOVES TO TEMPORAL PERSISTENCE** (what was called combat): unresolved claim U → ordinary observation/field input → EML persistence valuation → CostBand-funded consequence → later-generation state/claims — UNLESS a real same-generation consumer proves a distinct clearing law necessary. **`U != R`:** U (requested constrained quantity not granted) and CostBand R (granted value below another executable quantum) are distinct, independently observable quantities. **GENERATION PACING (non-negotiable):** no persistence consequence may synchronously re-enter the same resolution site — clear→persist→re-clear convergence inside one generation is forbidden; action chains propagate at GENERATION SPEED, and implementations must not collapse multi-stage satisfaction into an intra-generation solver merely to reduce latency. **BINDS 6.0 `OWNER-CHANNEL-INTRINSIC-0` (refreshed against the landed surfaces at 8cbd7e3d):** attrition resolves over owner channels through the temporal-persistence path above, never a mechanism of its own. Claims arrive ALREADY segregated by `OwnerChannelScopeKey`, so the arena needs no owner-equality comparison anywhere in its diff — 6.0 landed with zero such comparisons and that is the standard to hold. Persistence-attrition rows are this rung's contribution to `scripts/ci/allow/contention_mechanisms.txt`, which already exists and is digest/clearance-wired: ADD rows, do not mint the file. **BINDS `OVERLAY-LIFECYCLE` (Definable Horizon Law):** persistence-funded attrition and every authored resolution rule expresses its outcome as overlays; each carries `UntilDissolved` with an authored condition. Contention outcomes are transient by nature — a permanent attrition overlay is a modelling error, not a lifecycle choice. **BINDS 6.1b `BAND-QUANTIZED-DRAW-0`:** authored resolution rules allocate against queued deficits (`num * cost` as unmet demand), and quantized draw converts the allocation into units. "Which claimant is served first" — including which cohorts grow first — is an AUTHORED rule, never vector iteration order. Attrition, price-clearing, proportional and priority all operate on the same deficit; none of them may special-case a queue. **PRIORITY DATA SEAM EXISTS (DA review 2026-08-03):** 6.0b landed `priority` on `CommandDeficit` -> `RuntimeOwnerSiloDemandBucket`; the priority-ordered rule CONSUMES that field, never mints a claim-priority mechanism beside it. **BINDS 7.3 `ACTIONBAND-RECURSIVE-COMPOSITION-0` (ActionBand constitutional amendment 2026-08-09, `multi-axis-ActionBand-STEAD.md` §21.2):** ActionBand claims clear through the GENERIC claim->clear->disburse path; trivial and contested clears remain ONE mechanism; unresolved claim `U` stays distinct from CostBand remainder `R`; generation pacing is non-negotiable and no same-generation re-clear exists. **No ActionBand-local clearing rule.** Coordinated atomic common-depth commitment and persistent provisional holding across independently contested scarce lanes belong to the `VECTOR-COSTBAND-PROBE-0` follow-on, which BINDS 7.3 + 8.1 + 8.2 -- during 7.* a template requiring them FAILS admission closed (§8.3). | **DA-GRADUATED / merged #1784 @ 202340a8 — the resolution rule is DATA, proven by inversion.** The emergence falsifier is the real thing: same `clear_reduced_owner_channels`, same claims (`assert_eq!(authored, authored_before)`), two authored EML programs — `100.0 - priority` versus `order_weight` — and the allocation **inverts 4/1 to 1/4**, both lawful and both conserving. `AuthoredClearingProgram` is a single `TransformOp`; band membership and band order come entirely from the authored score. **Proportional-within-band is not encoded policy** — within a band scores are BIT-IDENTICAL, so the author has declared indifference, and proportional share is the unique allocation that conserves, honours that equality and is order-independent; any winner-takes-all rule would select a winner the author never authored. Remainder rides `source_simthing_id`, a LOGICAL key, bounded by band size minus one — and production is proven order-independent under a reversed input while the test-side row-order mutant REDs, so physical order is defeated rather than merely avoided. Persistence is temporal: `fund_unresolved_persistence` hard-rejects `SameGenerationConsequence`, values U through authored EML with elapsed generations, quantizes through the existing scalar CostBand and emits via `dispatch_until_dissolved` — so a permanent outcome is UNCONSTRUCTIBLE, the file holding exactly one `OverlayLifecycle::` reference and it `UntilDissolvedWith`. U and CostBand R stay distinct types in distinct roles. **Zero owner-equality vocabulary in production**; scope grouping is the full `OwnerChannelScopeKey`. ActionBand demand is an ordinary row granted through the identical path, and the local-shortcut mutant is caught by the **graduated 8.1 referee used unweakened**, not a helper. | DA-reserve · Frontier — Codex 5.6/Fable | **DA-GRADUATED / merged #1784 @ 202340a8** — pre-dispatch `5337607437` (A1 rule-stamp pin; gate-wiring ruled option 2), ten reserved questions adjudicated in the graduation ruling; `HD-RECEIPT: 5adea81721be` |
| 9.1 | `GATED-RATES-EML-REWIRE-0` | Phase 9: exclusion-audit Family C — `gated_rates` gate evaluation → authored EML library stack; role-pathway columns; delete `first_slice_mapping_runtime` (superseded by P0 derivation). **BINDS 6.1b (DA review 2026-08-03):** the gate rewire consumes 6.1b's EXPANDED EML library and inherits its O(program-length) cap -- one library, one cap; a second EML expansion surface for gates is the fragmentation shape. **BINDS 5.10–5.12 (Owner-approved 2026-08-03): this rung MUST FOLLOW the EML primitive landings** — the rewire authors gate laws against the COMPLETED vocabulary (`EXP`/`LN`, smooth gates, stabilized forms) exactly once; authoring staircase forms first and re-authoring after the primitives land is the exact double-work the ordering exists to prevent. Gate formulas needing domains carry 5.10's shape-1 certificates or shape-2 guards. **BINDS 5.13 `EXACT-CONSUMER-OBLIGATION-0` (Owner ruling 2026-08-04):** this rung authors gate laws against the completed vocabulary EXACTLY ONCE, so every gate consuming `EXP`/`LN` DECLARES its exact-bearing status and carries its own cross-arm digest **at authoring time** — inheriting exactness from the primitive is forbidden, and a gate that consumes an exact primitive while carrying only cost evidence is the defect 5.13 mints admission against. Retrofitting the declarations after authoring is the double-work this ordering exists to prevent. | **DA-GRADUATED / merged #1789 @ 059b4896 — graduated through a substrate STOP that corrected the DA.** Registry-owned gate columns reach the role pathway via `col_for_role` / `resolve_node_columns_for_property`, remints removed, the one `EmlExpressionRegistry` and inherited `MAX_EML_TREE_NODES` cap consumed, no EXP/LN consumer and no third `ExactPrimitiveConsumer` variant — so #1787's 5.14 reconciliation holds and no exact-consumer obligation is triggered. `first_slice_mapping_runtime.rs` is DELETED with no alias or second mapping authority. **The successor's three gadget lanes are plan-local, not registry columns**, and the DA's first remand wrongly prescribed `col_for_role` for them; coding STOPped rather than manufacture registry roles for a plan-owned grid, and the DA withdrew it (`5351400933`). They are now named `StructuralScalarChannel` identities — `EML_RESOURCE`, `EML_WEIGHT_PRESSURE`, `EML_WEIGHT_RESOURCE` — crossing into `ColumnIndex` only through the sealed `into_plan_column()` STRUCTURAL-PLAN door, with `require_plan_channel` retaining the grid bound. The AUTHORED-ADMIT door survives only for the genuinely authored urgency fallback. **Both oracles execute production code:** the gate proof builds through `build_gated_rate_ops` and runs `eval_eml_cpu` against a retained pre-rewire formula oracle across below/equal/above-threshold, ungated-add and gated-mult; the successor proof runs `field_urgency_plan_channels` → `field_urgency_eml_nodes` → `eval_eml_cpu` bit-identical to the retained pre-delete positional program. Value mutants bite behaviourally — an always-on gate disagrees below threshold, and feeding the authored urgency column as the resource identity changes the result. | Std — Grok | **DA-GRADUATED / merged #1789 @ 059b4896** — reconciliation #1787, proof/derivation remand `5343274168`, substrate STOP ruling `5351400933`; `HD-RECEIPT: 30b58045c5ed` |
| 9.2 | `RF-COLUMN-MINT-MIGRATE-0` | Phase 9: the sweep — remaining sites onto doors; DELETE the exclusion list; retire `COLUMN-INDEX-MINT`. **BINDS 6.0 `OWNER-CHANNEL-INTRINSIC-0` (refreshed against the landed surfaces at 8cbd7e3d):** column mint/migrate carries `OwnerChannelScopeKey {owner_ref, resource_key, scope_id}`. `planet_id` and `star_system_gridcell_id_raw` are DELETED from engine `src`; `PlanetChildRfScopeKey` survives ONLY as `pub type PlanetChildRfScopeKey = OwnerChannelScopeKey`, a migration alias. Do not re-add a domain-shaped field to that alias and do not mint a column against it — new code names `OwnerChannelScopeKey` directly. **VRAM LAYOUT STABILITY — SOFT, NOT A BAN (Owner-directed 2026-08-01, refined same day):** an ownership change MUST NOT FORCE a reparent or table reallocation as an INCIDENTAL side effect; the flag-switch path must remain AVAILABLE and is the DEFAULT. Reparenting, reallocation, remapping, and slot-swapping REMAIN PERMITTED wherever a consumer deliberately chooses them — ownership grants RF continuity and flexibility, and some use cases will legitimately want the churn. **The constraint is on UNINTENDED cost, never on churn itself.** Reparenting is already safe and verified: `bind_owner` performs exactly one `add_property` and never touches `children`. The exposed risk is ORDERING, not size: `OwnerRef(String)` derives `Ord` and is the FIRST component of the bucket sort key, and `prove_owner_channel_rf_cpu_gpu_parity` enforces `pair[0].scope < pair[1].scope`. Flipping an owner from `alpha` to `zulu` therefore leaves bucket COUNT unchanged but PERMUTES bucket order, and with it `source_row_indices` and slot layout. That is free today only because plans are rebuilt every generation; it stops being free the moment this rung mints PERSISTENT columns, at which point a flag switch silently becomes a full VRAM re-index. **Therefore: persistent column layout must be keyed by a stable interned owner id rather than by `OwnerRef` string ordering — so that the cheap path EXISTS AT ALL.** Keep the string as IDENTITY (authoring, diagnostics) and intern it to a numeric id for LAYOUT. Interning is what makes reallocation a CHOICE: without it a flip permutes the table whether or not anyone wanted it; with it, layout is stable by default and a consumer that genuinely wants re-sorting, remapping, or reparenting on an ownership change may still do it deliberately and say so. No interning exists today (no `OwnerId`, no owner index); minting it is this rung's work. **BINDS 6.4 `SLOT-LOGICAL-IDENTITY-0` (DA `5193918971` amendment 1, landed with the 6.4 implementation):** persistent column layout keys on STABLE LOGICAL IDENTITY — `SlotIndex` under the Tier-2 epoch-rebind law plus the interned owner id below — and NEVER on physical row position or allocation order; an epoch rebind (`AnchorRemapOperation::EpochRebind`) must not invalidate or permute persistent column layout, and any layout artifact that would be perturbed by the 6.4 forced-scramble witness is inadmissible. **SEAM PORTABILITY (DA review 2026-08-03):** interned owner ids are SESSION-LOCAL -- two independent subtrees intern independently, so an interned id crossing a 6.2 seam is meaningless. Seam transport carries canonical `OwnerRef` identity; interned ids never serialize past their session. | **DA-GRADUATED / merged #1792 @ 987f7fd8 — the sweep is done and the mint is closed.** `ColumnIndex::new` is DELETED; comparative derived Amount reaches `col_for_role`; plan-local dummies are `StructuralScalarChannel` — the 9.1 lesson applied unprompted; GPU rematerialisation is confined to `wgsl_encode::column_from_wire`; the AUTHORED-ADMIT and oracle/rehearsal doors remain in their named roles. `COLUMN-INDEX-MINT`, its selftest arm and inspect justification are RETIRED per the promotion-blocker with the authorized deletion ledger; scope cleanup leaves `PlanetChildRfScopeKey` as the `OwnerChannelScopeKey` alias only. **The owner layout graduated through one remand (DA `5356888559`) that enforced the Owner directive — 6.* designed surfaces over coding convenience.** `PersistentRfLayoutKey` had minted a `logical_slot` from post-sort bucket enumeration — allocation order, the exact thing the module header forbade — typed as `SlotIndex`, which carries no logical identity in 6.4's model. Repaired: the key is now `{ OwnerLayoutId, SimThingId, ResourceKey, ScopeId }` with append-only index assignment, and the forced-`EpochRebind` witness now drives the PRODUCTION layout — held `SpecSessionState.persistent_rf_layout` compiled before and after a real `SlotAllocator::epoch_rebind` with rows verified moved: production keys bit-identical while the `slot_of`-keyed rival provably permutes on the same data. Persistence is real, not nominal: `PersistentRfLayout::new` has ZERO production callers; the layout lives on the session, `compile_owner_channel_rf_gpu_proof_plan` takes `&mut` and reuses it, and `sync_owners` gives unique-owner alpha→zulu a cheap rebind (with the `AlreadyInterned` merge case handled explicitly) while add/remove of a third owner renumbers nothing unrelated. Seam portability is type-unrepresentable: `OwnerLayoutId` and `PersistentRfLayout` derive no serde, and session-locality is declared on the type — no production-carrier witness owed. | Std — Grok | **DA-GRADUATED / merged #1792 @ 987f7fd8** — identity remand `5356888559` (Q1 persistence, Q2 6.4 identity; Q3 accepted at the type boundary), Fable deep review; `HD-RECEIPT: 11176c638ff7` |
| 10.1 | `DOCTRINE-CI-RECONCILE-0` | Phase 10: retire impossible-state detectors; mechanize new gates (EML-addition DA-RESERVE, telemetry-lifecycle, Root-Contract admission errors); promote `WORKSHOP-HOMING-DETECTION` from HEURISTIC to hard-fail for engine `src` (**RECONCILED by 5.9b:** the SCENARIO-VOCABULARY half of this obligation is DISCHARGED — `SCENARIO-RESIDUE` in `scripts/ci/scenario_residue_check.sh` is hard-fail over engine `src` AND `tests` for scenario and domain-activity vocabulary, with 2 planted defects in its selftest, and the de-naming it was gated behind has landed at engine-zero. What remains here is the narrower workshop-HOMING question: code that belongs in the disposable leaf but was authored into an engine crate — which is a PLACEMENT test, not a vocabulary test, and is exactly the gap that let the residue in); allowlist re-baseline (`kernel_surface` shrink) incl. the seven-shader bespoke stencil-family retirement (`min_plus_stencil`, `structured_field_stencil`, `min_plus_traversal_d_probe`, `saturating_flux_choke_threshold`, `structured_field_stencil_atlas_mask`, `w_impedance_compose`, `stress_compose` — each classed in/out-of-family, last-consumer-verified, superseded by 5.5-5.6 field-sweep instances); anchors re-pointed. **BINDS 5.11/5.12 (Owner-approved 2026-08-03):** the reconcile confirms the exact-primitive certification artifacts (exhaustive digests + per-toolchain replay records) are scan-watched for **presence and freshness only — never re-execution** (CI runs no cargo tests by standing Owner ruling), and that the certified-toolchain registry's invalidation-on-change discipline is wired; a certification artifact nothing watches is decoration. **BINDS 6.0 `OWNER-CHANNEL-INTRINSIC-0` (refreshed against the landed surfaces at 8cbd7e3d):** the allowlist re-baseline must include `scripts/ci/allow/contention_mechanisms.txt`, and doctrine-CI reconcile must confirm it stays wired into `gen_digest.sh` GLOBAL_SOURCES/TRACK_ALLOW_FILES and `clearance_check.sh` GATE_WIRING_PATHS — a sanctioned-mechanism registry nothing reads is decoration. ALSO carries 6.0's named residue: `planet_child_rf.rs`, `PlanetChildRfScopeKey`, and `PlanetChildRfReduceUpBucket` are domain-shaped NAMES that `scenario_residue_check.sh` is structurally blind to, because "planet" is not scenario vocabulary. Retire the names or extend the gate; do not let them become unmetered debt. **BINDS `OVERLAY-LIFECYCLE` (Definable Horizon Law):** the doctrine-CI reconcile carries the Owner-directed follow-on — retire the `permanent-residue:*` promotion targets in favour of `until-closeout:*`, updating `test_inventory_check.sh`'s allowed set and `test_lifecycle_expiry_check.sh`'s prefix matching. Closeout is an EVENT, so `until-closeout` is strictly shorter-lived than the current `closed_at + GRACE_DAYS` wall-clock lease. **AUTOMATA-REFACTOR ADDITIONS (DA review 2026-08-03):** (a) the impossible-state retirement sweep now includes conditions the refactor moved onto types -- unattributable overlays (`origin` required at the type boundary since 6.0b) and `Permanent` in authored assets (serde hard-errors at admission since the rename); any scan still watching those is decoration. (b) **PIN THE 91 UNPINNED `compile_fail` DOCTESTS** with error codes (only 2 of 93 are pinned; an unpinned block passes on ANY compile failure -- demonstrated vacuous-pass 2026-08-03). Pure tightening, free under the asymmetric gate doctrine, and this rung is its natural home. CI runs no cargo test by standing Owner ruling, which makes pinning MORE important, not less: the doctests only prove anything on the rare local runs, so each run must prove the right thing. **ACTIONBAND MECHANIZATION (added by the 2026-08-09 constitutional amendment; `multi-axis-ActionBand-STEAD.md` §17):** Phase 10 owns the CI reconciliation of the laws the ActionBand amendment made binding but did not yet mechanize -- (a) **17.19 single crossing surface**: a scan that REDs a second comparator pass, crossing record, listener framework, or CPU crossing evaluator beside the Phase-5 `BandCrossingDelta` machinery, which is the standing form of 7.1's one-shot planted-rival proof; (b) **17.20 closed target forms**: target-form additions outside the admitted §4.2 vocabulary route to DA-RESERVE like EML additions; (c) **17.17/17.18 admission-sealed caps**: subordination spans and axis/channel budgets are declared-and-frozen, so a runtime widening is detectable. Retire any of these whose condition becomes unrepresentable at a type boundary first -- the deliverable is DELETION where the compiler already refuses. | **DA-GRADUATED / merged #1796 @ 9da3f112 — the harness reconciled against the landed type boundaries, census-first, with two nominal promotions caught and made real at review.** Every scans.tsv row, anchor, allowlist row, compile_fail doctest and standard-workflow gate was dispositioned RETIRE/PROMOTE/KEEP from LIVE call-site and type-boundary evidence. Landed: the constitutional-surface census (EML opcode/stack libraries, ActionBand target/channel forms and caps, single-crossing symbols, telemetry-lifecycle binding, Root-Contract admission error) with seven planted defects; compile_fail doctests migrated from LINE-number to native rustdoc ERROR-CODE identity (104 fences, wrong-error selftest); `permanent-residue:*` retired for `until-closeout:*` with wall-clock grace removed — the 5.9b immortality vocabulary finally gone; rungclose resolves the ladder row by STABLE identity with duplicate fail-closed; plan-struct and write-door censuses wired into the standard scan; 90/93 render caps with all-role lint dry-run; kernel allowlist 297→288 by internalization only; seven bespoke stencil shaders KEEP on live-consumer proof; 6.0 domain names retired (`PlanetChildRfScopeKey` gone, `LegacyOwnerChannelReduceUpBucket`); the overlay-census harvester made vocabulary-independent (planted `renew_directive_germ` REDs as UNJUSTIFIED-BROAD); ~128 nullary `SpecError::ValidationFailed` sites replaced by `ValidationFailedAt { site }` and gated so the pattern cannot regrow. **Two remands made promotions real:** the homing gate had been a regex for the PHRASE 'homing boundary classification' — now a closed-vocabulary `Homing-Boundary:` declaration whose planted RED bites PATCH CONTENT (an engine-src delta declaring engine-native while adding `use simthing_workshop` / `include_str!…workshop`). And `OVERLAY-PEER-AUTHORITY` had been retired while peer actuation was still CONSTRUCTIBLE — `OverlayLifecycleStateGpu` had all-pub fields and `apply_gpu_overlay_lifecycle` is pub, so a peer could synthesize `dissolved: 1` rows with no crossing; 7.7's 'absent by grep/type proof' had been the grep doing the work. Retired lawfully only together with the SEAL: private fields, `pub(crate) from_gpu_readback` the sole constructor, zero external construction sites, `compile_fail,E0451` pin. Full workspace certificate at the tested head (Owner ruling 5365260676, kernel-type seal = structural): **107 suites / 425 passed / 3 failed / 14 ignored — identical to the Phase 9 baseline**, the three reds being the already-attributed clausething debts. | DA-reserve — Fable | **DA-GRADUATED / merged #1796 @ 9da3f112** — pre-dispatch `5365284584` (A1 ValidationFailed census, A2 harvester, A3 delta table), remand `5366507518` (Q2 homing, Q3 peer seal), Fable deep review; `HD-RECEIPT: 0d23f7c92251` |
| 11.1 | `EMBEDDER-INTERFACE-0` | Phase 11: the five-verb Vendor Door API (formalization of existing mechanisms) + vendoring crate boundary. **BINDS 6.0 `OWNER-CHANNEL-INTRINSIC-0` (refreshed against the landed surfaces at 8cbd7e3d):** ownership is part of the stem cell's anatomy, so the five verbs must express it as DATA — **Derive** declares owner seats (and any ship/planet-style specialist profile) as authored data; **Populate** binds owner via `bind_owner`, where ABSENCE MEANS INHERIT and stamping every node is forbidden. The embedder surface must expose `owner x specialization` as the sanctioned query so nobody reaches for `SimThingKind` to answer it — the struct doc forbids branching on kind, and `StarSystem`/`Station` are deprecated for exactly that reason. It must also surface that `try_new_authored` reserves `unowned`, so an embedder cannot author a party colliding with neutral ground. **BINDS `OVERLAY-LIFECYCLE` (Definable Horizon Law):** **Overlay** is one of the five Vendor Door verbs, so the embedder surface must expose `UntilDissolved` plus an authored `DissolveCondition` and must make a horizon REQUIRED rather than defaultable. An embedder that can mint an unbounded overlay exports the defect through the door. **BINDS 6.1b `BAND-QUANTIZED-DRAW-0`:** the Vendor Door must express a queued draw as DATA — **Populate** declares the queue's existence and shape, runtime mutates only its depth, and re-hydration must NOT be required to change it. An embedder that cannot author a unit-cost band without engine code has the containment law failing at the door. **BINDS 6.0b (DA review 2026-08-03):** the Overlay verb REQUIRES the embedder to supply the originating SimThing -- the door never synthesizes an origin; an embedder that cannot attribute an overlay has no business minting it, and a door-synthesized origin would launder unattributable overlays through the one surface vendors touch. | **DA-GRADUATED / merged #1800 @ 9929ac98 — the Vendor Door is five verbs over graduated machinery, and the crate that carries them owns nothing.** `simthing-embedder` is a verified LEAF (no engine crate depends on it) with ZERO owned state — no `static`, `Mutex`, `Arc`, `OnceCell` or `RefCell` in its src — so "thin facade" is a checked fact, not an adjective; every public entry delegates to a named graduated surface via the returned verb-to-mechanism table. **Derive** declares owner seats and specialist profiles as authored data and routes authored owners through `try_new_authored`, so `unowned` is REJECTED as reserved neutral ground; `owner_specializations` answers `owner x specialization` with NO `SimThingKind` parameter or read anywhere in its body — kind-branching is unrepresentable at the type boundary, not merely discouraged. **Populate** binds through `bind_owner` with absence=inherit. **Overlay** REQUIRES a caller-supplied origin plus authored `UntilDissolved` and `DissolveCondition`; missing/synthesized origin and default horizon RED on the production door. **Bind** exposes a per-generation read-only shadow view — `coord.shadow` is overwritten from GPU readback at every boundary (`boundary.rs:591`), so the handle observes real GPU state and decides nothing. **Run** is one lifecycle with `paced` / `continuous` as scheduling policy over the same `SimSession::step_once` and the existing replay history. Scale invariance witnessed: one SimThing and a multi-node tree through identical verbs. **Graduated through one narrow remand (DA `5380544437`) that removed a law the ladder never wrote:** the validator had REDed EVERY explicit binding equal to its inherited owner, a coder-authored strictness the row never imposed. Repaired to the row's actual shape — `BulkUniformStamp` fires only when a subtree of >=2 nodes has EVERY node carrying the same explicit stamp — with the falsifier proving both sides on the production door: a deliberately redundant lone binding passes Populate AND Run, while a fully-stamped 2-node tree REDs with `stamped_nodes: 2`. `RedundantBinding` is deleted, not left dead, and both doc comments were rewritten so no header states a law the code no longer enforces. Structural certificate at the tested head (Owner ruling 5365260676): **109 suites / 431 passed / 3 failed / 14 ignored**, run TWICE with identical results — exactly the 107/425 baseline plus the new crate's 2 suites and 6 tests; the three reds are the known clausething debts. | DA-reserve · Frontier — Codex 5.6/Fable | **DA-GRADUATED / merged #1800 @ 9929ac98** — pre-dispatch `5377813514` (A1 falsifier law, A2 sanctioned query, A3 gap-vs-STOP), review `5380544437` (Q1 shadow, Q2 registry authority, Q3 validator), Fable deep review; `HD-RECEIPT: 606500a6a787` |
| 11.1a | `FIELD-SWEEP-SESSION-SEAM-0` | **SUBSTRATE — minted by DA after a SECOND lawful STOP on the 11.2 arc, Owner-directed 2026-08-23 (build the seam rather than descope the exemplar).** The PALMA and Gu-Yang producers exist and return admitted `FieldSweepRegistration` products, but NOTHING in an ordinary session consumes them: `session.rs` has zero references to `FieldSweepRegistration`, PALMA, Gu-Yang or comparative; `SpecSessionState.comparative_projection` is DECLARED but never assigned in production; `install.rs` populates `field_plan_admission` only. So 11.1b re-export cannot make **Run** execute a Triad — you cannot re-export your way to execution. **The execution primitive already exists and is already used in production:** `FirstSliceMappingSession` builds `FieldSweepSession` instances and runs them, but compiles its OWN registrations from `gpu_config` and is gated on `formula_class == "field_urgency"`. This rung is therefore WIRING, not a new engine: let admitted PALMA and Gu-Yang registration products reach the existing `FieldSweepSession` execution through an ordinary production session, and let `admit_comparative_from_field_plan` reach the declared `comparative_projection` slot. **5.8b (DA `5154348081`) HOLDS:** Triad columns remain explicit consumer inputs — the seam ACCEPTS them from the consumer and must NOT default them at install. | **DA-GRADUATED / merged #1808 @ 1effef3f — the declared-but-dead field is live and the convergence held.** An ordinary `SimSession` now accepts already-admitted PALMA and Gu-Yang `FieldSweepRegistration` products plus explicit consumer Triad columns, and executes them through the EXISTING `FieldSweepSession` — the seam EXTENDS `compile_structured_field_sweeps` and reuses `FieldSweepSession::new` and `dispatch_chain` rather than authoring an executor, so stead section 10 convergence is proven by diff rather than asserted. `admit_comparative_from_field_plan` reaches a live production assignment at `session.rs:790`; the field had been DECLARED and never assigned, so the rung's central falsifier was that removing the assignment must RED, and `FIELD-SWEEP-SESSION-SEAM-ASSIGNMENT-REMOVAL` does exactly that. **5.8b (DA `5154348081`) intact by construction:** `install.rs` is not in the delta at all, and `FIELD-SWEEP-SESSION-SEAM-INSTALL-TRIAD-DEFAULT` guards it anyway — Triad columns remain explicit consumer inputs. `FIELD-SWEEP-SESSION-SEAM-SECOND-EXECUTION-PATH` additionally pins the constructor inside `mapping_runtime.rs` and keeps the CPU proof executor out of production source. **Shape test green on the correct reading:** the delta adds ZERO public surfaces to production src, so `chokepoint`, `corridor`, `front` and `dominance` remain born observables — a broad substring sweep reports hits only for pre-existing unrelated names (`frontier`, an atlas-mask error metric), none of them minted here. Witnessed on a real adapter: `step_once` executes admitted registrations and observes non-zero comparative output on RTX 4080 / Vulkan. Delta is six files, only two production-source. Structural certificate (Owner ruling `5365260676`): **110 suites / 433 passed / 3 failed / 14 ignored** — exactly the 109/431 baseline plus this rung's own witness file; the three reds are the known ClauseThing debts. | Frontier — Codex | **DA-GRADUATED / merged #1808 @ 1effef3f** — minted after the second lawful 11.2-arc STOP; Owner-directed 2026-08-23 to build the seam; unified Phase-11 authority `5385811865`; Fable deep review; `HD-RECEIPT: d08d00d27308` |
| 11.1b | `DIMENSION-FINALIZATION-SEAM-0` | **SUBSTRATE — DA-minted 2026-08-23 after a THIRD lawful STOP on this arc, and the third instance of one defect class: an exit proof narrower than what the downstream rung needs.** Production order on post-11.1a master is `install_atomic` compiling the ordinary field plan at the AUTHORED `RegionFieldSpec.n_dims` (`session.rs:753`), then `admit_comparative_from_field_plan` GROWING the live registry (`session.rs:779`), then `install_session_mapping` compiling the ordinary mapping from the ORIGINAL `GameModeSpec.region_fields` (`session.rs:802`). The admitted registrations therefore carry the post-growth width while the mapping carries the authored one, and attachment REDs with `AdmittedFieldSweepBindingMismatch { actual_dims: 57, expected_dims: 25 }`. **11.1a's own witness papers over this** at `field_sweep_session_seam_0.rs:236` with `field.n_dims = final_n_dims` — a caller-side rewrite of authored spec that a vendor cannot perform through the five verbs and that requires PREDICTING the final width. 11.1a is not falsified: its exit proof required execution through the ordinary path and got it. But it never required REACHABILITY WITHOUT SPEC MUTATION, which is exactly what the door needs, so 11.1c inherited an unusable seam. Land the missing object: a single-authority **dimension-finalization stage** in the ordinary admitted-sweep seam — after derived-column admission and before ordinary region-field and caller registration compilation — that binds both sides to the same final registry width with NO caller mutation of `GameModeSpec` and NO width prediction. | **DA-GRADUATED / merged #1813 @ 5d7000bd — the falsifier was a deletion and the deletion held.** `field.n_dims = final_n_dims` is GONE from the 11.1a witness, which today would RED with `AdmittedFieldSweepBindingMismatch`; the witness grew 2 to 3 cases rather than trading one away. The seam was redesigned rather than patched: it no longer takes pre-built registrations but a COMPILER CALLBACK the engine invokes AFTER it knows the final width, so the caller never predicts and never mutates. **Single authority verified:** one site computes `final_n_dims` from `registry.total_columns` and binds BOTH sides from it — `preview.stencil.n_dims` for the ordinary mapping and `compile_caller_field_sweeps(final_n_dims)` for the caller — and the two other `total_columns` reads in `session.rs` are pre-existing session-open and capacity-resize logic, not rival finalizers. **The banned rewrite was DELETED, not relocated:** the caller mutating AUTHORED spec at a PREDICTED width became the engine binding its own DERIVED preview at a COMPUTED width, and the authored `GameModeSpec` is untouched — the only `game_mode.` occurrence in the delta is a read. **The soundness assumption underneath it was checked, not assumed:** widening a stencil compiled at 25 to 57 is only correct because registry growth is APPEND-ONLY (`registry.rs` does `total_columns += stride` with `push` on ranges and owners), so prior columns keep their offsets and derived columns land at the tail; interleaving growth would have silently mis-mapped while passing every test. The old raw entry is gone and its replacement `open_with_finalized_field_sweeps` is `pub(crate)` with one call site. 5.8b intact by construction — `install.rs` is not in the delta. Convergence unchanged: no executor construction added. Structural certificate **110 suites / 434 passed / 3 failed / 14 ignored**, exactly the 110/433 baseline plus the one new falsifier case. | Frontier — Codex | **DA-GRADUATED / merged #1813 @ 5d7000bd** — minted after the third arc STOP; unified Phase-11 authority `5385811865`; substrate ruling `5387094239`; Fable deep review; `HD-RECEIPT: 4ed070f7ace4` |
| 11.1c | `VENDOR-DOOR-TRIAD-SURFACE-0` | **REMEDIAL — minted by DA ruling `5383361312`, RESEQUENCED to 11.1b behind the substrate rung 11.1a by DA ruling on the second STOP.** Re-export is only implementable once an ordinary session can EXECUTE the registrations; 11.1a lands that seam first. The Phase 11 parent row already contracts **Bind** to carry Triad bands (need/corridor/front/chokepoint), born from declaring competing emitter classes over admitted stencil topology per P5. The door graduated at 11.1 does NOT expose them: `bind.rs` carries only `velocity_threshold`, `aggregate_threshold`, `queued_draw` and `shadow`, and the crate contains no PALMA, Gu-Yang, chokepoint, corridor, contest or emitter surface at all — so the 11.2 full-Triad exemplar is unwritable through the five verbs. Root cause: 11.1's exit proof never mentioned Triad while its parent row's Bind contract does, and `EXIT-PROOF-COVERAGE` cannot see that class of gap because it compares a row against its OWN proof, never a parent phase contract against a child rung. Complete the contract by RE-EXPORT ONLY: **Derive** declares competing emitter classes (`ComparativeEmitterClass`); **Bind** registers the admitted PALMA and Gu-Yang N4 field-sweep topology (`PalmaN4FieldSweepSpec`, `PalmaOverlayParameterizedN4Spec`, `GuYangN4FieldSweepSpec`, `GuYangOverlayParameterizedN4Spec`) and observes comparative output (`GuYangStallOutputs`) read-only. Every one of those is a LANDED production surface in `simthing-driver`, so this rung adds no capability. **WIDENED by the Owner-directed single-ingress census 2026-08-23:** the door must surface EVERY unified ingress, not only the Triad. Census finding — a case-insensitive search of `crates/simthing-embedder/src/` for EML, opcode and program returns NOTHING, so there is NO EML authoring surface at the door; `overlay::authored` takes a `PropertyTransformDelta`, not an EML program. Re-export the authored EML surface (`EmlGadgetStackSpec`, `EmlGadgetInstanceSpec`) so a vendor can state a law as authored DATA — the type has real production consumers at `property.rs:359` and `intensity_accumulator.rs:103`, so this is re-export, not substrate. Also carry the ActionBand door question into this rung's archaeology and hand it to 11.1d. | **DA-GRADUATED / merged #1811 @ 36ffbc92 — the Vendor Door reaches the Triad through five verbs, and the facade law is now MECHANIZED rather than hand-checked.** The witness `vendor_door_triad_surface_0.rs` imports only `std` and `simthing_embedder` — the structural form of the five-verb claim and the strongest evidence this rung could carry — with ZERO engine-source edits in the delta. The projected-width helper and the `RegionFieldSpec.n_dims` rewrite are gone; Run delegates a deferred compiler to the 11.1b seam and the callback receives the live finalized registry width, so the vendor neither predicts a width nor mutates authored spec. **The facade proofs became standing tests:** the witness reads every `crates/*/Cargo.toml` to assert no reverse dependency (`VENDOR-DOOR-TRIAD-FACADE-LEAF`) and reads the embedder's own production sources to assert no `static`, `Mutex<`, `OnceCell<` or `RefCell<` — the two greps the DA had run by hand at every facade rung, now mechanized; intrinsic-over-prose applied to the facade law itself. Triad columns arrive through bounded authored admission with a planted raw-mint RED, so the door never re-opens the `ColumnIndex` mint Phase 9 closed. Authored EML (`EmlGadgetStackSpec`, `EmlGadgetInstanceSpec`) is re-exported at `derive.rs:12`, discharging the widened row and opening the door 11.1f's law-stating gadget arrives through. `simthing-gpu` joins the vendoring boundary, necessary because the seam signature names `FieldSweepRegistration`. Born-observable fence holds: zero public `chokepoint`/`corridor`/`dominance` surfaces added. **Graduated through a harness detour, not a substance remand:** a stale `parse-command` RED from a superseded workflow definition blocked merge; `REVIEW-EVENT-JSON-TRANSPORT-0` repaired the injection-prone inline `toJson` interpolation, and the red was retired by advancing the base rather than by waiving the never-merge-on-red rule — `crates/simthing-embedder` tree hash `7913f467` identical before and after, zero `crates/` delta. Structural certificate **111 suites / 436 passed / 3 failed / 14 ignored**, exactly the 110/434 baseline plus this rung's own witness. | Frontier — Codex | **DA-GRADUATED / merged #1811 @ 36ffbc92** — substance accepted `5391413241`; base-update ruling `5396985204`; incident correction `5397118514`; Fable deep review; `HD-RECEIPT: 622933c70c88` (retired `aff35cbd509a` never reused) |
| 11.1d | `ACCUMULATOR-CONVERGENCE-SEAL-0` | **CONVERGENCE SEAL — DA-minted after the Owner-directed single-ingress census 2026-08-23.** `PipelineFlags` in `simthing-sim/src/boundary.rs` stages the AccumulatorOp convergence behind TWELVE soft flags. Eight default ON, and two of those are already SEALED the right way — `use_accumulator_overlay_add` (legacy Pass 3 is GONE, disabling REJECTS the workload) and `use_accumulator_velocity` (disabling with governed pairs REJECTS). **Three default OFF, so their LEGACY path is what production actually runs:** `use_accumulator_transfer` (C-8c exact economic transfer), `use_accumulator_emission` (C-8d economic emission), and `use_accumulator_resource_flow` (E-11 arena resource-flow allocation). The last one is the Owner law failing at its most important point: RF allocation — the contention substrate itself — is opt-in, defaults to legacy, and carries a whole burn-in/soak/telemetry apparatus (`resource_flow_opt_in_burn_in.rs`, `resource_flow_dynamic_enrollment_soak.rs`, `ResourceFlowFlagSource`) precisely because it was never made mandatory. Complete each rollout, flip the default, then **DELETE the legacy path and the flag itself** so bypass becomes unrepresentable rather than merely non-default. A flag that can be set false is not a converged path; it is two paths with a preference. Per the admission ladder the endpoint is TYPE-level: the deliverable is DELETION. | **DA-GRADUATED / merged #1819 @ 7ee2350d — bypass is now unrepresentable, not non-default.** The three unconverged flags (`use_accumulator_transfer`, `use_accumulator_emission`, `use_accumulator_resource_flow`) are at ZERO occurrences tree-wide, and with them the legacy branches they selected and the entire selector apparatus: `ResourceEconomyOptInMode`, `apply_resource_economy_opt_in`, `resolve_resource_flow_execution`, `ResourceFlowFlagSource`, and both E-11 opt-in instrument files (`resource_flow_opt_in_telemetry`, `resource_flow_opt_in_burn_in`) — an instrument does not outlive the defect it measured; the still-load-bearing E-11 burn/dynamic/soak proof was RE-HOMED onto the converged path, witnessed on a real adapter. **A1 discharged in the strong form:** `deny_unknown_fields` on `ResourceEconomySpec`/`ResourceFlowSpec`/`GameModeSpec`, and four workload-specific referee cases prove an authored retired selector FAILS AT ADMISSION with a diagnostic naming the workload — never parse-and-ignore, so no authored surface claims to govern what it cannot. **The seal is self-guarding:** the referee carries a standing residue census over production and authored fixtures (the 11.1c mechanized pattern, reused as directed), so selector vocabulary cannot silently return. Deletion-heavy delta: 68 files, +496/-1896, deletion-guard PASS removed=0, inventory 1313/1313 clean. C-8c exactness 2/2, C-8d 3/3, E-11 arena 2/2 on RTX 4080/Vulkan, converged-route CT2A 1/1. The overlay-add and governed-velocity seals stand as regression witnesses. Structural certificate (owed — production paths deleted): **112 suites / 439 passed / 3 failed / 14 ignored**, exactly the 111/436 baseline plus this rung's own 3-case referee; the three reds are the known ClauseThing debts. With this rung the C-8c/C-8d/E-11 rollouts that began in 0.0.8.0 are CLOSED at the type: the AccumulatorOp path is the only path, per the Owner single-ingress law of 2026-08-23. | Frontier — Codex | **DA-GRADUATED / merged #1819 @ 7ee2350d** — pre-dispatch `5398723078` (A1 authored-selector surface), Fable deep review; `HD-RECEIPT: 28067af2cdab` |
| 11.1e | `ACTIONBAND-EXECUTION-INGRESS-0` | **INGRESS GAP — DA-minted after the Owner-directed single-ingress census 2026-08-23.** The Vendor Door already ADVERTISES ActionBand: `crates/simthing-embedder/src/bind.rs` re-exports `compile_crossing_consequence_session as action_band_commitments`. That function has **ZERO production callers** — it appears only in `lib.rs` as a re-export and at the door — and all eight witnesses of `compile_action_band_gpu_execution` are TESTS. `action_band` appears ZERO times in `simthing-sim/src/boundary.rs`, so ActionBand is not staged into the unified pipeline at all. The consequence arm IS wired (`session.rs:873-907`, CT-3b+4a closure, journaled crossings submitted through the admitted binding), so this is a partial, not a total, gap. **We are shipping the trap:** a vendor calls the door entry, receives a compiled session, and nothing executes it. Either wire compiled ActionBand execution into the ordinary session path, or withdraw the door entry and record a dated deferral — the door must not advertise a product nothing runs. | **DA-GRADUATED / merged #1822 @ 60654376 — the door stops advertising a product nothing runs; ActionBand execution now reaches the ordinary tick.** Outcome is the WIRING branch, not the withdrawal fallback, and no new mechanism was needed: DA pre-dispatch traced the chain and found it already complete — `compile_crossing_consequence_session` to `CrossingConsequenceSession::bind_dispatch` to a live `ActionBandGpuExecution::Active` — merely uninvoked, with every caller a test. There is now exactly ONE true production caller, `session.rs:485` binding through `&self.coord.shadow`, the buffer 11.1a established is refreshed from `state.read_values()` at every boundary; the Vendor Door compiles and installs atomically so no detachable compile-only product survives. **DA A1 discharged in the strong form.** `bind_dispatch` consumes `self` — one admission yields one dispatch — so the session holds the DISPATCH rather than the compiled session, in `SessionActionBandExecution` alongside an `ActionBandIngressShape` fingerprint over binding table, registry columns/properties/activation and coord/state slot-dimension shape. Staleness FAILS CLOSED with three typed refusals enforced at three sites: `BindingTableStale` (naming tree growth AND epoch rebind — the 9.2 hazard caught where it belongs), `RegistryStale`, `DimensionShapeStale`. A plan can no longer silently outlive its tree. The existing `session_origin` / `ForeignCompile` provenance seal is preserved, not weakened for wiring convenience. **The misleading tripwire is corrected honestly:** it now reads `ACTIONBAND-EXECUTION-INGRESS-DECLARATION: the graduated compiler declaration moved or multiplied` — naming a DECLARATION rather than falsely claiming a caller count — with a separate `DROPPED-PRODUCT` falsifier beside it. Real-adapter witness reaches the ordinary structural boundary; dropped-product and stale-registry falsifiers RED. Structural certificate (owed — execution entered the production tick): **113 suites / 440 passed / 3 failed / 14 ignored**, exactly the 112/439 baseline plus this rung's own witness; the three reds are the known ClauseThing debts. | Frontier — Codex | **DA-GRADUATED / merged #1822 @ 60654376** — pre-dispatch `5400536335` (A1 dispatch lifetime, resident-value point, staleness rule), Fable deep review; `HD-RECEIPT: 8d6560239f55` |
| 11.1f | `AUTHORED-LAW-GADGET-0` | **OWNER RULING 2026-08-23 — always trust intrinsic mechanisms over prose; the law-stating gadget is the more compliant option, so build it.** The alternative was to restate 11.2's authored-law obligation against what the data surface can express today — but that is PROSE: it leaves the staircase perfectly representable and merely asks the exemplar not to use it, the validate-conformance antipattern this track exists to refuse. The gadget DEFINES the semantics instead: add a law-stating member to `EmlGadgetInstanceSpec` that compiles a canonical domain law through the ALREADY-ADMITTED `EXP` and `LN` opcodes — a power law as `exp(k * ln x)`. With the gadget vocabulary closed, **stating a power law IS authoring the gadget**, and the staircase becomes unrepresentable as authored data rather than discouraged by documentation. **NO new opcode:** `POW` stays absent and the gadget COMPOSES from EXP and LN, so `EML-OPCODE-LIBRARY` is untouched. The nine existing variants cannot express this — `Decay` and `Ema` are exponential IN TIME, multiplicative recurrences, not a stated law. **This rung also CENSUSES `EmlGadgetInstanceSpec` as a constitutional surface in the same delta**, because extending a vocabulary and leaving the door open behind you is how the asymmetry arose: adding an opcode is DA-RESERVE-gated while adding the gadget vendors actually author against was ungated. | **DA-GRADUATED / merged #1825 @ 562f423a — a vendor can now STATE a domain law as authored data, and the staircase is unrepresentable rather than discouraged.** The Owner intrinsic-mechanism ruling landed exactly as intended: `PowerLaw` joins `EmlGadgetInstanceSpec` composing the ALREADY-ADMITTED primitives, and `EML-OPCODE-LIBRARY` census stays at **25** — **`POW` was never minted**, proven by an unchanged census rather than asserted. **DA A1 discharged beyond its terms:** the compile-side mirror `EmlGadgetKind` — which the handoff had not named and which was ungated — is now a SECOND censused constitutional surface, so the paired vocabularies carry identical 10-member lists and planted instance-only AND kind-only additions each RED. Censusing one of a mirrored pair is half a seal; both halves are sealed. **DA A2 discharged in the strong form:** domain safety became STRUCTURAL rather than asserted. `PowerLaw` requires a positive-finite-normal `input_floor`, and the canonical emission is `SLOT_VALUE, CLAMP_BOUNDED, LN, LITERAL_F32, MUL, CLAMP_BOUNDED, EXP, RETURN_TOP` — guarding BOTH transcendentals, the domain before `LN` and the range before `EXP`. An unsafe floor or non-finite exponent REDs at admission as `power_law_ln_domain_uncertified`; there is no runtime NaN repair, so a law that can be authored into a NaN is unrepresentable. **The falsifier bites shape, not spelling:** `validate_power_law_semantics` rejects a staircase node-set even when it is NAMED `PowerLaw`, while a correct composition under a misleading name passes — the inversion the DA specifically required. Authored RON reaches a real production consumer end to end: gadget compiler to exact-primitive registry to GPU program table to the ordinary `AccumulatorOpSession` EvalEML path on a live adapter. Structural certificate (owed — a lowering path now emits into production EML): **114 suites / 443 passed / 3 failed / 14 ignored**, exactly the 113/440 baseline plus this rung's own witnesses. **With this rung the Phase-11 substrate is COMPLETE.** | Frontier — Codex | **DA-GRADUATED / merged #1825 @ 562f423a** — Owner intrinsic-mechanism ruling; DA mint #1807; pre-dispatch `5402217538` (A1 mirrored vocabulary, A2 decidable domain safety), Fable deep review; `HD-RECEIPT: 0ac008a37a7e` |
| 11.2 | `EMBEDDER-GUIDE-EXEMPLARS-0` | Phase 11: DOC-BUDGET-capped Embedder's Guide + two non-game exemplars (finance toy; network-saturation full-Triad exercise), CI admission-checked. **BENEFITS FROM 5.11/5.12 (Owner-approved 2026-08-03):** with `EXP`/`LN`/`POW` in the vocabulary, exemplars may STATE their domain's canonical law rather than approximate it (volume-delay is a power law; backoff and utility are exp/log-shaped; growth is exponential) — the guide should demonstrate at least one authored-law gadget so vendors learn the completed vocabulary, not the staircase idioms it retired. **WIDENED by the Owner-directed single-ingress census 2026-08-23:** the guide must teach the unified surfaces as THE ONLY ingress — Overlay, CostBand, ActionBand and mapping EML `EXP` and `LN` into RF Triad resolution, RF Triad into all domain contention, and the SimThing object as the only execution path. A guide that documents the door without teaching its exclusivity invites the next vendor to reach around it. **BLOCKED FINDING:** the authored-law obligation is not satisfiable today — beyond the missing door surface (11.1b), `EmlGadgetInstanceSpec` admits only FieldSampler, SoftStep, WeightedAccumulator, VelocityMonitor, Decay, Ema, BoundedFeedback, Hysteresis and Acceleration. `Decay` and `Ema` are exponential IN TIME, not a stated law; nothing expresses `exp(k * ln x)`. **RULED 2026-08-23 (Owner): intrinsic mechanisms over prose — build the law-stating gadget.** 11.1e `AUTHORED-LAW-GADGET-0` lands it, and 11.2's authored-law arm dispatches against that gadget rather than against a weakened obligation. | **DA-GRADUATED / merged #1803 @ 9a883cc3 — the rung that opened the arc closes it: a cold reader can vendor SimThing into a non-game domain from the guide alone, and every capability the guide teaches actually exists.** 11.2 asked for one exemplar and uncovered SIX missing substrate rungs (11.1a–f), three lawful coding STOPs, and a harness repair; it graduates only now because the door finally does everything it advertises. **Born Triad, not threshold cosplay:** the network-saturation exemplar imports ONLY `std` and `simthing_embedder`, declares competing emitter classes, compiles PALMA and Gu-Yang through Bind, enters the graduated seam via `run::initialize_with_admitted_field_sweeps` (pure delegation — owner-boundary validation then `SimSession::open_from_spec_with_admitted_field_sweeps`), and OBSERVES born stall/contest/border/chokepoint columns from admission outputs — read, never written; the generic-threshold substitute rejected at the arc's first STOP is gone. **The authored law is the admitted gadget:** `EmlGadgetInstanceSpec::PowerLaw { exponent: 4.0, input_floor: 0.25 }`, asserted as the sole gadget kind; unsafe floor REDs; `POW` absent and the opcode census unchanged. **Single-ingress taught as executable anatomy:** unified authoring surfaces to RF Triad resolution to domain contention to unified SimThing execution, with every guide code block verbatim in a running exemplar — enforced by the new `embedder_guide_exemplars_check.sh` admission gate (248 lines, selftest, `known_bad_staircase` fixture) wired into the standard Doctrine Scan. **Freeze respected:** the accepted finance exemplar, guide core and checker architecture survived the resume with only the authorized narrow deltas; stale branch-side door re-exports were surrendered to the graduated master door. Guide 115/120 DOC-BUDGET. Doctrine Exec has no PR run for this surface class (path filter) — reported honestly, nothing claimed. Structural certificate: **116 suites / 446 passed / 3 failed / 14 ignored** — exactly the 114/443 baseline plus the two exemplar suites; the three reds are the known ClauseThing debts, explicitly out of 11.2 scope. | Std — Grok | **DA-GRADUATED / merged #1803 @ 9a883cc3** — re-authored resume handoff `99644421d2bd` (retired: `890b845b5655`); DA authorization `5403211098`; Fable deep review; graduated through the full 11.1a–f substrate arc |
| 11.2a | `STEMTHING-B-FLOW-MARKET-GERM-0` | **StemThing-B, rung 1 of 4 — Owner design session 2026-08-24, constitution at stead anchor SECTION 12 (merged #1829); the section is the law, this row is the pointer.** Prove the complete recursive conserved-resource MARKET GRAMMAR as the stem-cell germ, on already-intrinsic surfaces only: admitted conserved capacity, descendant claims through sealed Draw envelopes, recursive RF reduce-up, EML valuation and inherited effective clearing weight, authored generic constrained clearing (work-conserving largest-remainder, generation-rotated exact ties), CostBand quantization, disbursement, Gu-Yang throughput/saturation, PALMA potentials over admitted topology, STEAD anchored observation, ordinary ActionBand/OverlayThing response. Includes the ratified GRANT LIFECYCLE: detachment does NOT release; death/dissolution releases; fission/fusion partition or transfer exactly; termination is an explicit lifecycle fact. Includes the corrected release/reclaim law absent from the tree today. **RED: any new engine manager, allocator, clearing, or field mechanism.** | **DA-GRADUATED / merged #1832 @ 22d06383 — the germ exists: every SimThing now carries the recursive conserved-resource market, and both Owner fences held on first contact.** DA A2 was discharged map-first: eleven grammar links each named to a landed surface BEFORE any edit, and no missing-link STOP fired — the first rung of the 11.x era to complete its archaeology without surfacing a substrate gap, which is what six substrate rungs bought. DA A1 landed the SECTION-12 authored vocabulary sealed: `ConservedOfferingSpec`, `OfferingPriceVectorSpec`, `DrawEnvelopeTemplateSpec` and `admit_specialization_flow_market` with four `deny_unknown_fields` and a serde-level shadow-tier mutant (`per_type_delta` injection fails at parse). `ClearingRemainderAuthority { granter, generation }` landed INSIDE the existing `constrained_clearing.rs` home — rotation consumes the granter generation authority exactly as ruled, and exact ties witnessed rotating at generations 12 and 13. CostBand arithmetic witnessed: value 5.0 at unit cost 1.5 yields N=3, R=0.5. **The lifecycle law is asserted literally:** `retained_after_detachment() == initial` — detachment releases ZERO — with conservation exact across renewal/revocation/fission/terminal release (8 = 1 + 7) and death/dissolution each releasing in full. **The load-bearing subtree witness ran and the STOP did not fire:** a detached in-session child independently reduces its next RF generation while its identity-keyed cleared grant stays stable across the existing stamped seam. **F1 held:** the compute market reaches the REAL GPU chain — grant 4, PALMA bits 3f800000, Gu-Yang/STEAD and native-clamped ActionBand bits 3e48f5c2, ordinary Overlay response — never a second allocation lane. **F2 held by absence:** zero new history or telemetry types in the delta; replay reads the existing frame/writer/reader/driver path. Zero new authority-shaped types anywhere. TEST-BUDGET INSPECT adjudicated ACCEPTED: the four standing witnesses are separate named tests because each guards a distinct law with a distinct failure identity; collapsing them would conflate exactly what makes them falsifiers. Structural certificate: **117 suites / 450 passed / 3 failed / 14 ignored** — the 116/446 baseline plus exactly this rung's witness; the three reds remain the known ClauseThing debts. | Frontier — Codex | **DA-GRADUATED / merged #1832 @ 22d06383** — Owner design session 2026-08-24; stead SECTION 12; pre-dispatch `5405301542` (A1 vocabulary-is-deliverable, A2 map-first), Fable deep review; `HD-RECEIPT: e2a67ae4502e` |
| 11.2b | `STEMTHING-B-VRAM-RESIDENCY-0` | **StemThing-B, rung 2 of 4 — VRAM Residency as the DISTINCT engine-native market that first proves the germ (stead SECTION 12).** Not dissolved into generic vocabulary: extents, disjoint placement, relocation and physical residency constraints are kernel physics, named and separately witnessed. The ENTITLEMENT side consumes the 11.2a germ — residency claims clear through the same market grammar — while physical realization remains residency-specific behind the placement boundary. Lands the ratified TWO-STAGE placement law: a cleared entitlement is PROVISIONAL until the owning boundary's placement oracle proves a legal, disjoint, in-bounds realization; unchanged placements need no global per-generation re-proof (incremental or unrepresentable-by-construction). | **DA-GRADUATED / merged #1835 @ 12141dfa — the two-stage law is kernel physics, and both halves of the Owner sentence reproduce exactly.** `residency_placement.rs` lands the engine-native facility the A1 amendment named as deliverable: checked half-open `ResidencyExtent`, provisional physical entitlement, stable placement identity, typed outcomes, and a `pub(crate)` STATELESS placement oracle whose adjudication is LEVEL-LOCAL by construction — the placement book is indexed first by owning granter, every judgment reads exactly one inner map, and DA scrutiny confirmed the two apparent cross-level iterations both scope to a single granter (sibling judgment in `prepare`; the per-granter RED-2 `audit_level`). No global extent scan exists. **RED 1:** planted `[2,4)` against committed `[0,3)` returns typed `Overlap` — nothing commits, `retained_unmet_quantity()==2` stays ordinary U, `revalue_generation()` recorded, exactly ONE canonical refusal row, no same-generation retry, and a later generation places `[3,5)` successfully; the `Unchanged` disposition proves identical placements incur no re-proof. **RED 2:** planted committed overlap and out-of-bounds each record typed `ResidencyCommittedCorruption` evidence FIRST, then typed `SessionTerminated` naming the offending extents and owning granters; the boundary is unusable afterward — diagnosable termination, not U, not a warning, not a bare panic, and replay reaches the fault point through the one schedule. **Entitlement provenance holds:** the driver bridge `residency_market.rs` consumes a real 11.2a `MarketGrantRecord` and contains no clearing, ranking, retry, or geometry policy — new residency PHYSICS in scope, new market AUTHORITY absent, the A1 split exactly. **Relocation is transactional** on the existing epoch-rebind authority: allocator snapshot before the remap, restore on refusal, commit only on acceptance, logical identity preserved through the existing `AnchorRemapSection` chain — no second remap or history authority. The only history extension is four typed row kinds on the EXISTING 6.1 `IntegrationSchedule`. Free-list machinery untouched as policy (11.2c's deletion). Structural certificate: **118 suites / 452 passed / 3 failed / 14 ignored** — exactly the 117/450 baseline plus this rung's witness and the kernel corruption unit; the three reds are the known ClauseThing debts. | Frontier — Codex | **DA-GRADUATED / merged #1835 @ 12141dfa** — pre-dispatch `5406467103` (A1 physical vocabulary + hard-fault diagnosability), Fable deep review; `HD-RECEIPT: 0942e8fc2761` |
| 11.2c | `STEMTHING-B-GROWTH-ENTITLEMENT-SEAM-0` | **SUBSTRATE — DA-minted 2026-08-25 after the SIXTH lawful STOP of the track, fired exactly where the 11.2d(prev-c) A1 amendment aimed it.** Ordinary growth is grantless today: `fission.rs` attaches the child and calls `allocator.populate_subtree` with NO entitlement input (verified `fission.rs:298-300`), `BoundaryRequest::AddChild` carries no grant identity, and the graduated market/placement path REQUIRES the structural parent relation to already exist — a cycle only a PRE-ALLOCATION grant product crossing into the structural boundary can close. Land that seam on the graduated authorities ONLY: (1) one residency-market / standing-granter entitlement input installed into the session from the existing 11.2a Draw/clearing lifecycle — no second market, no manager; (2) before ANY ordinary fission/AddChild attach or row mint, the complete candidate batch converts to authored claims cleared by the EXISTING 11.2a authority into real accepted grant identities — oversubscription outcome independent of vector/free-list/event order; (3) the product feeds the 11.2b placement boundary with an ordering/transaction permitting placement before final row residency, level-local disjointness intact; (4) the same grant/placement fact rides the EXISTING canonical schedule/replay authority; infeasibility stays ordinary U, next-generation revaluation, no same-generation retry, and a refused claim attaches NOTHING; (5) population entry points NARROW so ordinary growth cannot compile without the entitlement product — the only lawful exceptions are the explicit install-only initial-bulk realization and the authoritative replay realization. **A scenario authoring no flow market runs under the root's implicit standing entitlement — a degenerate one-granter market cleared through the SAME 11.2a authority, producing the same grant products and schedule rows; implicit is not bypass.** | **COVERS the DA-ruled 2026-08-25 seam scope.** Ordinary fission and AddChild each witnessed end to end: candidate batch -> authored claims -> 11.2a clearing -> accepted grant identity -> 11.2b placement -> row residency, with the recorded fact on the one schedule. **The compile seal is the load-bearing falsifier:** a planted attempt to attach/populate ordinary growth WITHOUT an entitlement product fails to COMPILE or REDs at admission for its own named reason — the two lawful doors (install bulk, replay realization) are explicit, narrow, and named. **Replay realizes the RECORDED grant/placement fact and never re-clears** — a planted replay-side re-clearing REDs; replay of a recorded refusal reproduces the refusal. Oversubscribed ordinary growth witnessed: losers stay U with revaluation generations, winners place, and permuting candidate order changes NOTHING (order-independence witnessed, not assumed). Degenerate-market default witnessed: a scenario with no authored flow market grows through the implicit root standing entitlement with identical products and rows. No new clearing/entitlement/history/telemetry/generation authority; no retry loop; 11.2b two-stage laws untouched; existing epoch-rebind authority untouched. Structural certificate at graduation — the ordinary tick's growth path changes. | Frontier — Codex | **PROBATION / proof-present / DA-review-pending** — coding handoff `5417954669`; `HD-RECEIPT: 7d5052c34f5d` |
| 11.2d | `STEMTHING-B-ALLOCATOR-RETIREMENT-0` | **StemThing-B, rung 3 of 4 — the deletion rung, 11.1d shape (Owner R3 confinement, stead SECTION 12).** No allocator path may survive that is not a SimThing disbursing resources through the RF arena. Delete legacy residency allocation POLICY outside the market: free-list ORDER as grant policy becomes unrepresentable, per the core-design SECTION 4 amendment already merged (#1829). Physical free-range/index structures survive ONLY as implementation machinery DOWNSTREAM of market-decided entitlement. Scope boundary per Owner ruling: slots/extents and authored capacity lanes; registry column append and GPU buffer sizing remain infrastructure. | **COVERS the Owner-ratified 2026-08-24 scope.** The seal shape established by 11.1d: bypass is unrepresentable, not non-default — a planted attempt to grant by free-list order fails to compile or REDs at admission for its own named reason. Net-deletion success signal per the anchor: allocator-specific policy paths retire, the CPU boundary's allocation stages shrink to an ordinary lane, and NO new scan surface is minted (the phase retires allocation-shaped validation). Deletion-guard and inventory clean; structural certificate at graduation. | Frontier — Codex | TODO |
| 11.2e | `VENDOR-DOOR-GRANTING-SURFACE-0` | **StemThing-B, rung 4 of 4 — the Owner-sanctioned facade-only door completion (sanction with refinement, 2026-08-24).** Expose the generic flow-market authoring grammar through the EXISTING five verbs: Populate declares granter budgets on any authored conserved lane and the root arena; Derive declares offerings (strict reference, no per-type deltas) and sealed Draw envelopes on the existing specialization profile; ordinary Run executes granting through the graduated seam. Expose VRAM Residency's additional physical vocabulary where appropriate. **No sixth allocation verb. No new B semantics in the facade.** | **COVERS the Owner-ratified 2026-08-24 scope.** The witness includes at least ONE recursively granting child subtree — a granted child becomes a granter for its own descendants through the same authored surface. The 11.1 facade law re-proven unchanged by the mechanized standing tests: leaf by dependency arrow, zero owned state, every new public entry delegated. **If the door cannot expose graduated B without engine changes, STOP and reopen the substrate rather than burying new architecture in the facade** (Owner fence, verbatim). A vendor authors a non-residency market through the door with zero new engine mechanism — the genericity falsifier at the vendor surface. Structural certificate at graduation. | Frontier — Codex | TODO |
| 11.3 | `UNIFIED-FACILITY-CONVERGENCE-WITNESS-0` | **Unified-facility convergence witness — DA-minted 2026-08-17 on Owner direction.** Placed AFTER StemThing-B implements (the anchor sequences B's mint and implementation after 8.2) and BEFORE 12.2 canonizes, because **12.2 writes the one-stem-cell germ into `simthing_core_design.md` as §1-level anatomy and nothing executes all four facilities together**. Everything proven to date is pairwise at seams — 7.5c the Field Triad, 7.8 ActionBand-to-OverlayThing actuation, 7.8a the span substrate OverlayThing and StemThing-B both consume, 7.9 the overlay closure. Strong per seam; the composed whole is asserted, never observed. Canonizing an anatomy seen only in pieces is the audited-assertion shape this track remanded three times. Neither existing rung covers it: 12.1 is a Vendor Door portability proof in an unrelated domain, which need not touch all four facilities, and 11.2's second exemplar is a full-FIELD-Triad exercise, not a four-facility one. Scope: ONE scenario in which StemThing-A residency, StemThing-B granting, ActionBand crossing and OverlayThing consequence all carry load simultaneously through the graduated substrate. **No new facility, no new authority, no lowering** — this rung composes what exists and is a witness, not a mechanism. | **Emergence falsifier in 8.2's shape, not an enumeration.** ONE authored change — data only, **zero code change** — must be observable only by traversing all four facilities: StemThing-A residency to StemThing-B granting to ActionBand crossing to OverlayThing consequence. **Plant each facility as a passthrough in turn and require the observed outcome to stop moving**, four planted REDs for their exact reasons, on production constructs and never via inert helper refusals. Listing the four running side by side proves coexistence and is explicitly NOT sufficient; composition is the claim 12.2 makes and the claim this rung must earn. Replay bit-exact from existing history surfaces alone; no second history, dispatcher, generation authority or peer executor. Carry measured at admitted cardinalities before any compaction. 12.2 may not canonize the germ as §1 anatomy until this witness is green. | DA-reserve · Frontier | TODO |
| 11.4 | `UNIFIED-INGRESS-EXCLUSIVITY-0` | **THE OWNER LAW, MECHANIZED — DA-minted 2026-08-23. 11.3 proves the facilities COMPOSE; this rung proves nothing BYPASSES them.** Composition and exclusivity are different claims and 11.3 earns only the first. The law has three links: the unified surfaces (Overlay, CostBand, ActionBand, mapping EML `EXP`/`LN`) are the ONLY ingress to RF Triad resolution; RF Triad resolution is the ONLY ingress to any domain simulation's contention and resolution; and the unified SimThing object is the ONLY execution path for all simulation resolution. The census behind this rung found the chain broken at every link: RF allocation defaulting to a legacy path (11.1c), ActionBand advertised but unexecuted (11.1d), the Field Triad unreachable from an ordinary session (11.1a), and the door missing an EML authoring surface entirely (11.1b). Those rungs repair the instances; **this rung makes the law itself enforceable** so the next paradigm cannot land the same way. Deliverable is a CENSUS GATE, not prose: for every censused constitutional surface and every graduated producer, assert a NON-TEST production consumer exists, and assert no second execution path reaches contention resolution. Where the honest answer is no consumer yet, the row must carry a DATED DEFERRAL rather than silence — 5.8b did this correctly and 11.1 did not. | **COVERS the Owner-directed 2026-08-23 single-ingress law.** The gate FAILS a producer graduated without a non-test production consumer, FAILS a second execution path into contention resolution, and FAILS a censused surface reachable except through the unified ingress — each for its own named reason, with planted defects and a selftest. The falsifier bites the LAW, not a spelling: renaming a surface does not trip it, adding a bypass does. The two gap classes proven live on the 11.2 arc are covered by construction — parent-phase contract versus child-rung proof, and producer graduated without consumer — since `EXIT-PROOF-COVERAGE` compares a row only against its OWN proof and can see neither. `EmlGadgetInstanceSpec` is already censused by 11.1e, which extended it; this rung inherits that census rather than repeating it and asserts it is still closed. Structural certificate at graduation. | Frontier — Codex | TODO |
| 12.1 | `PORTABILITY-PROOF-0` | Phase 12: a domain unrelated to any shipped scenario through the Vendor Door end-to-end, zero engine edits. | Exemplar-seeded domain simulates + STEAD-observes + serializes untouched. | Std — Grok | TODO |
| 12.2 | `CORE-CANONIZATION-0` | Phase 12: object model (P0 Root Contract + cycle + EML-ISA + Triad Doors + overlay law) into `simthing_core_design.md`; HARNESS phases canonize nothing; track closeout follows. **OWNER SCOPE (approved 2026-08-13) — the closed-kernel rewrite:** home CostBand, ActionBand, OverlayThing (the intrinsic overlay closure), StemThing-A+B, the full RF/STEAD/PALMA/Gu-Yang Field Triad, and complete EML (`EXP`/`LN`, `ExactPrimitiveAdmission`, the grown gadget library) as facilities of ONE stem-cell germ; **the germ becomes the §1-level anatomy** — the SimThing presented as a closed simulation kernel from which everything fractally recurses, with current §§2–8 reorganized as expressions of that anatomy, never a subsystem list. DELETE superseded §4.1/§6/§7.2.1/§8 prose rather than annotating it (the dated correction notes accumulated between 5.5 and this rung are consumed and removed here). Written ONLY from graduated fact — zero anticipatory text. **BINDS 6.0 `OWNER-CHANNEL-INTRINSIC-0` (refreshed against the landed surfaces at 8cbd7e3d):** canonization carries owner as an INTRINSIC sparse property dimension (`OWNER_CHANNEL_PROPERTY_ID`, inert by default, absence means inherit) with `resolve_owner` in its LANDED `Result` shape — total for valid admitted members, failing closed on foreign/malformed input — plus the reserved neutral `unowned` identity. Canonizing the pre-6.0 model (no owner field, flat per-participant string stamps, no resolution) would freeze the defect into the 0.1 core permanently. **BINDS `OVERLAY-LIFECYCLE` (Definable Horizon Law):** canonization must carry `UntilDissolved` / `AtSessionEnd` and MUST NOT canonize a permanence variant or a `Never` condition into the 0.1 core. Freezing `permanent` into the canon would make the defect unfixable by definition. **BINDS 6.1b `CostBand` (Owner-directed canonization):** the core design doc must carry **`CostBand`** by that exact spelling, defined as **the** resource-sink mechanism — threshold as unit cost, `N = floor(V/C)`, remainder `R = V - N*C` carried, booleans as depth 1 — with NO rival sink mechanism admitted alongside it. Canonizing a threshold/sink split, or CostBand as an optional mode, would freeze the fragmentation this rung exists to repair. **EARLY DISCHARGE (2026-08-02, merge `7bec7a7c`) — three of this rung's BINDS obligations have already landed.** The amendment-timing rule (0.0.8.7 §4) requires a constitutional amendment to land WITH the rung that establishes it, so deferring these to Phase 12 was itself the defect: rung 6.0 and merge `10d9d639` had already made the pre-6.0 core text FALSE, and an unamended paradigm doc outranks the rung that superseded it. `simthing_core_design.md` now carries intrinsic ownership (§2), the `UntilDissolved` / `AtSessionEnd` lifecycle with no permanence variant (§6), and `CostBand` as THE sink definition (§5.4). **This rung therefore no longer AUTHORS those three — it VERIFIES them intact and unweakened.** Remaining authored scope is unchanged: P0 Root Contract, cycle, EML-ISA, Triad Doors, overlay four-family law. **SCOPE COVERAGE (DA review 2026-08-03, Owner-directed exit-proof drift correction).** The automata canonization set lands in its LANDED shapes and is verified against the code, not transcribed: events-are-RF with three delivery modes, required `Overlay.origin` with slot-space carriage, the CONJUNCTIVE predicate rule (selection conjunctive, value composition sequential -- two distinct algebras), generation stamps on both carriers with ONE recorded schedule and staleness as a STEAD field column, the singular EML overlay value form, and the Vendorized Build Principle. **ANCHOR DISSOLUTION IS PROVEN, NOT PROMISED:** `docs/stead_simthing_automata.md` is retired, its anchors re-pointed, and the ladder>anchor>memory precedence entry deleted -- `anchor_check --check` passes with zero orphans and no doc references the retired path. **AUTOMATA-REFACTOR CANONIZATION SET (Owner-approved 2026-08-03):** canonization additionally carries the automata deliverables in their LANDED shapes -- events-are-RF with the three delivery modes (deficit-driven / standing / predicate-broadcast), required `Overlay.origin` with slot-space carriage in the closed loop (6.2b), the CONJUNCTIVE predicate composition rule (selection conjunctive, value composition sequential -- two distinct algebras, proven not asserted), generation stamps on both carriers + the ONE recorded integration schedule + staleness as a STEAD field column, and the Vendorized Build Principle (closed loop default, CPU-authoritative as derived instance). **ANCHOR DISSOLUTION (Definable Horizon on the anchor itself):** `docs/stead_simthing_automata.md` was designated a TEMPORARY anchor; this rung is its definable horizon -- absorb its normative content into the core design doc, retire the file, re-point its anchors, and drop the ladder>anchor>memory precedence entry that exists only because the doc does. An anchor doc that outlives its canonization is the accretion the Definable Horizon Law names. **GATED by the Owner-directed single-ingress census 2026-08-23:** 12.2 may NOT canonize the germ as section 1 anatomy until 11.4 `UNIFIED-INGRESS-EXCLUSIVITY-0` is green, in addition to the existing 11.3 composition gate. Canonizing a closed kernel while a legacy bypass still reaches contention resolution would write a closure into the constitution that the tree does not have — the same audited-assertion shape 11.3 exists to prevent, one level up. Composition (11.3) and exclusivity (11.4) are separate claims and canonization needs both. | Canonized sections land; net-prose discipline; SimThing complete and distinct; the three early-discharged canonizations (intrinsic owner §2, `UntilDissolved`/`AtSessionEnd` §6, `CostBand` §5.4) are re-verified INTACT and unweakened at this rung rather than re-authored, with any drift repaired here; CLOSEOUT protocol run (Owner-gated). **DISCHARGES amendments dated 2026-08-03 and BINDS `OWNER-CHANNEL-INTRINSIC-0`** — the clauses above are the proof obligations those amendments create; this citation exists so the parity gate can see the proof was widened with the scope. **COVERS THE Owner-directed 2026-08-23 single-ingress scope:** canonization is blocked until 11.4 `UNIFIED-INGRESS-EXCLUSIVITY-0` is green as well as 11.3, so the constitution never claims a closure the tree does not hold. | DA-reserve · Frontier — Codex 5.6/Fable | TODO |

| Item | State |
|---|---|
| Active open rung | `UNIFIED-FACILITY-CONVERGENCE-WITNESS-0` |

## 4. Binding laws (carry from day one)

**The Native Ingestion Law (Owner mandate, 2026-07-30).** **An authored Clausewitz script must
always be natively ingestible through ClauseThing.** `scenarios/terran_pirate_galaxy.clause` is a
ClauseScript source written in the Clausewitz scripting language; it parses (jomini), hydrates, and
transpiles to a canonical `ScenarioSpec` **without engine changes**, and that must remain true
however radically the substrate is rebuilt. This is the deliberate complement to the Detachability
Law, not a contradiction of it: **the engine must never DEPEND on the corpus; the authoring layer
must ALWAYS be able to READ it.** Accordingly the Invariant Set governs the SUBSTRATE, and the
authoring layer carries exactly one invariant of its own — **native-ingestion** — witnessed by the
~40 MINIMAL authored fixtures in `crates/simthing-clausething/tests/fixtures/*.clause` (31B–3.4KB,
grammar-level), **never by the game corpus**. Proving the language on an 11.5KB galaxy proves that
one scenario transpiles, not that the language works. **`.clause` files are EXTERNAL ASSETS** —
like an image or a mesh. An asset may sit in the tree unread and unversioned by any proof; it
influences the corpus **only when a proof reads it**, and at that moment it becomes corpus
coupling subject to every rule above. So: **delete the proofs that read the game corpus; keep the
asset.** The engine's independence is proven by the corpus-absent run; the language's health is
proven by minimal fixtures; and the artefact itself persists as authored Clausewitz source that
must remain ingestible however the substrate is rebuilt.

**The Invariant Set (Owner mandate, 2026-07-30) — the COMPLETE proof surface of the substrate.**
Engine correctness is these laws and nothing else. Each holds for **any** input, so each is
provable over inline-constructed input: **a corpus, fixture, or generator is never a
prerequisite for proof.** (1) **Conservation** — flow in equals flow out, modulo authored
injection. (2) **Determinism** — identical input twice yields byte-identical output; fold and
neighbour order are authored data, never an accident of layout. (3) **CPU/GPU parity** — the
reference implementation and the kernel agree. (4) **Boundedness** — no NaN/inf escapes; clamps
hold. (5) **Admission totality** — every admitted Anchored property has a host or admission fails
closed. (6) **Residency/typing** — illegal states are unrepresentable at the type boundary, which
*deletes* tests rather than adding them. **A per-rung referee is INADMISSIBLE unless it names a
NEW invariant.** A rung is done when the invariant set still holds and the new capability is
exercised *by that set*; a rung that needs a bespoke referee has revealed a capability the
invariant language cannot express, which is a design signal, not a testing gap. Corollary:
**emergence checks are DEMONSTRATIONS, not gates** — they are what you look at to see the
mechanism is alive, and gating on them invites making-the-referee-pass pressure. Corollary:
generated input buys *coverage* (a fuzzer) and *measurement* (performance curves); it never
buys validity. This law is the standing answer to per-rung proof accretion: 13 rungs produced
123 lifecycle rows because rungs, not invariants, were the unit of proof.

**The Detachability Law (Owner mandate, 2026-07-30).** **SimThing exists without ClauseThing.**
The engine (`-core`, `-spec`, `-kernel`, `-sim`, `-gpu`, `-feeder`, `-driver`) may never depend on
the authoring/app layer (`-clausething`, `-mapeditor`, `-workshop`); the arrow points one way only.
Scenario vocabulary, where it must exist at all, lives in `-clausething` and is data the engine
reads back, never a literal the engine names. Mechanized by `scripts/ci/detachability_check.sh`:
production coupling is a hard FAIL (verified 0 at ruling time — the gate locks in a property the
repo already had), and proof coupling via dev-dependencies is metered against a ceiling that
**may only decrease**, each reduction being a rung that moved an engine proof onto the Invariant
Set instead of a hydrated scenario.

**The Per-Generator Admission Law (Owner mandate, 2026-07-31).** **A dense-execution budget may
never bind a sparse adjacency.** Adjacency admission is per-generator: `GridOffsets` carries the
dense-theater caps (`REGION_FIELD_STANDARD_MAX_GRID` 10, `REGION_FIELD_EXTENDED_MAX_GRID` 32,
`REGION_FIELD_MAX_CELL_COUNT` 1024 = 32x32), and `LinkGraph` carries **none of them** — its cost is
`O(E)` on bounded fanout, not `O(cells x neighbours)`, so a dense per-dispatch window is meaningless
there. The graph path never routes through `validate_grid` or `structural_n4_atlas_partition`.
**Scenario scale is never constrained by an execution window**: structural layout admission is
already unbounded by construction (`MapgenStructuralGridBudget` is four `Option` fields, `None` =
unbounded, `max_cells` checked in `u128`), and exceeding a dense window yields
`AtlasDeferralRequired` — tile it, never refuse it. A star count is an authoring number and appears
nowhere in the engine. **Absence of a cap is the requirement being met, not a gap**: no `LinkGraph`
budget type may be added without a live caller, since a budget with no consumer is speculative
capacity. Mechanized by the permanent `FIELD-SWEEP-DENSE-CAP-CROSSING` scan arm, whose planted
`REGION_FIELD_MAX_CELL_COUNT` fixture flips it red — the hazard this law exists to prevent is a
ceiling arriving **by inheritance rather than by decision**, when a future rung reaches for the
nearest available constant.

**The Purge Reconciliation Law (Owner mandate, 2026-08-01).** **A purge-class rung MUST enumerate
what it invalidates, and every ladder row naming that subject MUST be reconciled in the SAME PR.**
Resequencing a purge without reconciling downstream rows is how purged semantics return. Recorded
because it happened: `TP-PURGE-0` was Owner-directed to preempt the field-sweep ladder and graduated
as 5.9, but **no downstream row was re-read** — so 5.8's pre-purge exit criterion survived,
orchestration copied it into the canonical handoff, the DA re-issued it in ruling `5150877754`, and
the coder built a synthetic-TP fixture toward it. The lesson generalizes past TP: we had applied the
Corpus Boundary Law to CODE and to PROOFS but never to **the workplan**, which is the input that
generates all future code and proofs and was the one surface left unguarded. A purge that cleans the
tree while leaving the plan contaminated guarantees regrowth.

**EXTENSION — CONSTITUTIONAL-CHANGE CLASS (Owner-directed, 2026-08-03).** The same enumeration duty
binds a **constitutional-change-class** rung: any rung that renames a serde-visible variant, requires
a new field on an admitted type, retires an anchor id, or otherwise invalidates existing references.
A purge invalidates by REMOVING; a constitutional change invalidates by REDEFINING. The failure mode
is identical and so is the remedy. **The blast radius of such a change is STATICALLY COMPUTABLE and
MUST be enumerated in the same PR** — every serialized surface (`.ron`, `.clause`, `.json`, fixtures),
every doc example, every construction site, every anchor id. Recorded because it happened: the
`Permanent` -> `UntilDissolved` rename left **nine authored RON assets** naming a variant that no
longer existed, `existing_authored_ron_capability_trees_admit_unchanged` went RED, and it survived
several rungs before a DA review found it by accident.

**RECONCILIATION IS ENUMERATION, NOT TEST EXECUTION.** All nine assets were findable by grep with
zero test runs; the suite did not catch the defect, it merely reported it late. Running the workspace
suite is NOT the discharge of this duty and MUST NOT be proposed as one — CI runs no `cargo test` by
standing Owner ruling, and that ruling is not in tension with this law because the duty is static.
A sweep restricted to one file type is not an enumeration: the miss above happened because the sweep
used `--include=*.md` and never saw `.ron`.

**The duty lands with the rung that makes the change, never deferred to a reconciliation rung at the
end of the workplan.** Deferred reconciliation decays attribution — establishing whether a red
pre-dates a rung costs a base checkout per question, and that archaeology exceeds the fix cost long
before a phase closes.

**The Mechanisms-Not-Domains Law (Owner mandate, 2026-08-01).** **The engine names MECHANISMS; it
never names DOMAIN ACTIVITIES.** "Combat", "trade", "diplomacy", "siege" are domain words and are
inadmissible as mechanism, rung, type, or module names. The mechanism behind all of them is
**contested-claim resolution over the RF arena with an AUTHORED resolution rule** — proportional
(cooperative), priority-ordered (queueing), price-clearing (which the 2.3 order-weight class already
implements), or attrition (adversarial, the case previously called combat). **Not all contention is
adversarial and the engine must never assume it is.** This generalizes the Corpus Boundary Law from
scenario vocabulary to DOMAIN vocabulary, and it is drift-detector 7 once more: a domain enum where
authored data is the answer. Evidence it was already biting: `hydrate_combat_arena.rs` emitted real
`ResourceTransferSpec`s with `host_entity: None` on every one — hostless RF-shaped scaffolding
groping toward "combat IS resource flow" without committing, precisely because it had been named a
domain thing instead of a mechanism. Name the mechanism and the RF arena already implements it.

**The Vendor Containment Law (Owner mandate, 2026-08-01).** **`simthing-clausething`,
`simthing-testthing`, and every SimThing-derived or vendored domain crate must never impose its
semantics beyond its own crate.** A vendored crate performs **its own conversion to SimThing specs**
and lands on **exposed SimThing surfaces** from there; nothing downstream inherits its vocabulary,
its corpus, or its shape. This is the *containment direction* the Vendor Door (Phase 12) does not
cover: the Door governs how a domain ENTERS, this Law governs what it may PUSH BACK.

*Growth is permitted, and is expected.* `simthing-core` and `simthing-spec` **can and should grow
when a new capability enriches the platform.** What is forbidden is growing them to accommodate a
**scenario, a save file, or a specific domain crate** — unless the capability generalizes into the
**recursive SimThing principle**, in which case it is no longer an accommodation but a platform
capability and belongs in core on its own merits.

*The routing rule for every new domain.* Expanding a core surface is NORMAL and will be necessary
for every domain. It is not a licence to edit core directly. The expansion is **homed in
`simthing-workshop` first**, evaluated there into a **generic** capability, and then judged into
exactly one of: **(a) elevate to SimThing** — it generalized, so core grows; **(b) keep it bespoke**
in the domain crate, or in a new subsequent crate — it did not generalize, so it stays where its
semantics belong. Workshop is the evaluation chamber; core is never the draft.

*Mechanical corollary — the artifact provenance rule.* **No generated artifact that feeds the
orientation digest or a handoff projection may be derived from a shipped scenario.** This is the
rule that would have caught both normalization pumps found on 2026-08-01, which
[[SCENARIO-RESIDUE]] could not see because the coupling lived in the GENERATOR while the artifact
sat in `scripts/ci/*.tsv`: `ANCHOR-DISPOSITION-ADMISSION-0` hydrated the game corpus and published
`tp::hull` / `tp::upkeep` / `tp::weapon_damage` into `property_admission_inventory.tsv` under an
orientation heading reading "Canonical TP live inventories", annotated "Phase 8 (8.1-8.2)
successor" — a disposable rehearsal issuing requirements to future rungs; and
`FIRST-CITIZEN-SPECIALISTS-0` still hardcodes `scenarios/terran_pirate_galaxy.clause` and publishes
"Terran + Pirate policy/weight authorities" into `specialization_citizen_counts.tsv`. Nineteen
generated TSVs feed orientation; each must be witnessed by a fixture, never by a shipped scenario.
A referee that asserts `scenarios/` holds "exactly one canonical clause" — as
`ANCHOR-DISPOSITION-ADMISSION-0` did — has made a game asset a **structural requirement of the
build**, which is this Law inverted.

**Per-Tree Instantiation (verified finding, 2026-08-02 — NOT a new law, a measured fact that constrains future rungs).** Independently-executing subtrees require **NO new engine mechanism**. Every layer is already per-tree and instantiable, verified on master: `SimThingKind::GameSession` documents itself as *"authority marker only; NOT a runtime engine singleton"*; `SpecSessionState` is a plain owned struct; `SlotAllocator` has zero statics; the CPU shadow is GROWN FROM the allocator's slot space; and `sole_session_root` is computed as `ptr::eq(node, tree_root) && sole_direct_session_child` — **relative to whichever tree root is passed in**, so N subtree roots each satisfy it without conflict. There are zero session- or slot-related globals in driver, sim, or gpu. **Consequences that bind future rungs:** (1) do NOT build a multi-session manager, registry, or scheduler — a subtree IS a root SimThing tree and gets its own allocator, session state, shadow, and generation counter by instantiation; (2) CPU/GPU parity needs **no Invariant Set amendment** — parity was always a property of THE TREE BEING EXECUTED, and we simply never had more than one, so each tree proving parity at its own generation satisfies the existing invariant unchanged; (3) the flat `&mut [f32]` shadow is NOT a single-generation defect — it is flat WITHIN a tree, which is correct, and mixed generations arise only if one shadow is forced to serve independently ticking subtrees. What genuinely remains is the SEAM: per-tree parity composes to whole-system correctness only if the boundary transfer is verified, which is what 6.1's stamp on reduce-up products delivers.

**The STEAD SimThing Automata anchor (Owner-directed, 2026-08-02).** `docs/stead_simthing_automata.md` is the NORMATIVE anchor for the SimThing-as-automaton design and covers every change made after 5.9c graduated. It exists because the design was settled across many conversational exchanges and conversation does not survive. **Precedence: the LADDER wins over the anchor** (it is machine truth and carries graduation stamps); **the anchor wins over any memory or recollection.** Its section 7 records CORRECTIONS AND REJECTED SHAPES — opt-in-upward sensitivity, sink-vs-observation as a taxonomy, CostBand as an opt-in marker, negative coefficients for destruction, an authored cascade bound, and an Invariant Set amendment for per-tree parity were each proposed and found wrong. **Read section 7 before proposing**, or those cycles are paid again.

**The Doctrine Tiering Law (Owner-directed, 2026-08-02).** The doctrine corpus has **three tiers, and
a statement belongs to exactly one**: **PARADIGM** (`simthing_core_design.md`) — what a SimThing *is*,
true across every version; **CONSTITUTIONAL** (`design_0_0_8_*.md`, the STEAD contracts) — binding law
for the current era, including preservation invariants for a specific remodel; **RECORD** (digests,
ledgers, results docs, open questions) — provenance that explains *why*, binding on nothing. A
self-declared non-binding digest is never promoted into the paradigm doc merely because its subject
matter is important: `eml_n4_expansion_digest.md` is constitutional-tier, and only its §5 preservation
invariants is normative. **Duplicating one law across two tiers is the failure this law prevents** —
two reachable statements of one rule create a precedence question at the moment of conflict, which is
the worst moment to discover one.

**Anchors index law; they never hold it.** A doctrine anchor is `{doc, section, content_hash}`, so
adding law to the corpus is always *writing it into a doc*, and the `content_hash` makes the anchor
refactor **compelled rather than optional**: editing an anchored section fails `anchor_check` until
the row is re-stamped. There is therefore no separate "register the doctrine" step, and no way to
rewrite anchored law silently. Constitutional amendments land **with the rung that establishes
them** — never as a later cleanup, because an unamended paradigm doc outranks the rung that
superseded it and silently re-imposes the retired shape.

**The Vendorized Build Principle (Owner mandate, 2026-08-02).** **The general model is the DEFAULT; every specialisation is an INSTANCE derived from it — never the reverse.** Approved instance: the closed-loop GPU-resident simulation is the base SimThing execution model, and the CPU-authoritative boundary system in production today is a **vendorized build** derived from it. Built the other way round, the incumbent stays load-bearing indefinitely, the general case is permanently the exception, and every capability is authored against the specialisation first and ported second. This is the same move the track has made repeatedly at smaller scale and is now making structurally: `unowned` is a real owner rather than `None`; a boolean is a CostBand of depth 1 rather than a separate fire/don't-fire path; a static overlay value is a one-node EML program rather than a variant beside computed ones. **In each case the special case stops existing instead of being handled.** Three earlier decisions turn out to have been preconditions for this, none argued on these grounds at the time: ownership had to be RESOLVABLE rather than a side table, or the GPU could not answer who owns a node; CostBand had to be ARITHMETIC rather than a dispatch, or actions could not run in-shader; the action vocabulary had to be AUTHORED and finite, or the GPU could only select and never invent. Preconditions assembled independently are evidence the shape was right before it was named.

**The Definable Horizon Law (Owner mandate, 2026-08-02).** **No lifecycle, promotion target, or retention class may CLAIM PERMANENCE.** Every open-ended retention gets a **definable horizon deletion point** — one that may be effectively permanent in practice but is SURFACED AS CHANGEABLE. A condition governs **automatic** dissolution only; **explicit removal is always available and always ORDINARY, never exceptional.** The failure this prevents is linguistic before it is technical: a name that makes immortality feel normal produces immortality by omission. `permanent-residue` is the cautionary precedent — it was never a grant, only a promotion-target label, and it still accreted 1,020 rows because the word made permanence the unremarkable default. Landed instance: `OverlayLifecycle::Permanent` -> `UntilDissolved` and `DissolveCondition::Never` -> `AtSessionEnd`, with the dead `is_permanent()` accessor deleted (0 call sites, but an affordance inviting `if !is_permanent() { skip_cleanup() }`). Session closure is the FLOOR of the ladder, not an escape from it: bounded by a run is honest, `Never` is a claim about the future. Owner-directed follow-on: the CI `permanent-residue:*` targets become `until-closeout:*`, which makes most of them SHORTER-lived than the wall-clock lease they carry today.

**The Capability Binding Law (Owner mandate, 2026-08-01).** **A rung that introduces a new core capability MUST bind every downstream row that will consume it, in the SAME PR** — naming the mechanism and forbidding the old one by name. This is the Purge Reconciliation Law inverted, and it closes the same hole from the other side: a purge that does not reconcile downstream rows lets purged semantics return, and a capability that does not bind downstream rows lets **superseded semantics survive** — each consumer quietly re-invents what it should have inherited. The failure is identical and so is the remedy. Applied at 6.0: nine rows (6.3, 7.1, 7.2, 8.1, 8.2, 9.2, 10.1, 11.1, 12.2) carry a `BINDS 6.0` clause naming exactly what they must consume and what they may not fall back to. A binding clause is not a cross-reference — it must state the FORBIDDEN alternative, because "use the new thing" is advice while "never stamp owner per participant" is a rule.

**The Transient Fixture Law (Owner mandate, 2026-08-01).** **A scenario or rehearsal must NEVER
export its transient proofing fixtures beyond its own crate** — not by `pub` export, not by
`dev-dependency`, and not by filesystem path. A fixture that leaves its crate stops being
transient: downstream code binds to it and it can no longer be deleted when its rehearsal ends.
Terran-Pirate escaped by all three routes at once — `simthing-mapeditor/src/tp_base_disc.rs` was
`pub mod`-exported and consumed only across crate lines, and seven ClauseThing tests reached
through the filesystem into `../simthing-mapeditor/tests/fixtures/`.

*Corollary — every core capability must have a SYNTHETIC witness.* A capability whose only
witness is a shipped scenario keeps that scenario structurally alive forever, and no purge can
dislodge it, because deleting the asset reds a legitimate law. **This, not code coupling, is
what actually kept TP alive:** `owner-seat` requires `HostsAdmittedPolicyWeightLocus`, and
`terran_pirate_galaxy.clause` was the only file in the repository that built one. Freeing an
asset is done by authoring a synthetic witness, never by deletion.

*Assets are not code.* `terran_pirate_galaxy.clause` and its siblings are `.png`-equivalents:
never deleted for hygiene, and ClauseThing must always ingest them natively. **Legacy and future
authored assets will declare unorthodox owner SimThings with recursive parentage, and actioning
them with code is constitutionally forbidden — that is building the engine to satisfy a stale
savefile.** The engine may only ADMIT or REJECT by existing law. Where an asset cannot be
expressed, the answer is to BROADEN `simthing-spec` so authors can express it as data — a
narrower spec does not prevent contamination, it causes it, by forcing authors to reach into
engine crates for what they cannot author. Full inventory, coverage gaps, and the next-rehearsal
checklist: `docs/scenario_containment_handover.md`.

**The Corpus Boundary Law (Owner mandate, 2026-07-30).** **No scenario's vocabulary may be
hardcoded in engine crates.** No faction, owner, entity, or scenario name may appear in
production `src` of `simthing-core`, `-spec`, `-kernel`, `-sim`, `-gpu`, `-clausething`, or
`-driver` — not as a control-flow literal, not as a struct field, not as a serde/wire key, not as
an authored-grammar key the engine names rather than reads. Scenario identity enters ONLY as data
the engine reads back. Two corollaries, because both failures already happened: (a) **a corpus may
WITNESS engine law and may never DEFINE it** — engine law is never stated in terms of one corpus's
contents, and a corpus is never edited to make engine law pass; (b) **engine law must be provable
against at least two corpora, one of which is minimal and synthetic** — plurality, not abstinence,
is what stops a single corpus becoming definitional, and it preserves the emergence falsifiers that
require a rich witness. Hardcoded scenario vocabulary is ALSO a generality defect, not merely
hygiene: a two-field `terran_/pirate_` payload cannot express a third faction, and
`if owner == "pirate"` cannot express a non-pirate raider. Enforcement is `TP-PURGE-0` (de-name,
owner-keyed data, N-side capable) with the `WORKSHOP-HOMING-DETECTION` HEURISTIC to hard-fail
promotion at 10.1 as the lock-in — fix, then ratchet. Sealed-core status at ruling time: `-core`,
`-spec`, `-sim`, `-gpu` production `src` already CLEAN; the sole `-kernel` match is a `#[test]`
negative proof that scenario-tagged opcodes are inadmissible, which is this law working.

**The Consumer Law (Owner mandate, 2026-07-20).** STEAD, PALMA, Gu-Yang, and Studio observation
are STANDING CONSUMERS of all data accumulated at SimThings — accumulated data is never walled
off as hygiene ("waiting for a consumer" is not a state that exists; STEAD is the consumer).
Hygiene disciplines govern PROOF (source loci cannot close an OVL or prove a flow) and MUTATION
(no side-channel writes) — never READ ACCESS. CPU-side consumer reads route through the one
canonical generic read seam (hosted SimThingId + typed PropertyKey/role, role-pathway loci,
coherent per-generation snapshots); observation never becomes CPU decision branching — decisions stay
on-device threshold/band crossings. **The Consumer Law does NOT shield tests from lifecycle:**
it governs production read access, never test/evidence retention — almost no test outlives its
workplan/track (default-delete at closeout stands) unless deleting it breaks production code;
"STEAD might consume this someday" is not a lifecycle exemption. Born from the 12.3 stop: the 12.2→12.8 horizon seam was
dropped and the disruption map sat walled at 0.0 while macro disruption ran ~8446.
**THE TRIAD DOORS (allowlist law, Owner-ruled 2026-07-26).** The P5 Field Triad doors are the
ONLY sanctioned derivations for their semantic families, repo-wide: **pathfinding/routing →
the PALMA door** (semiring potential sweep; `D` is a field, not a route); **borders/edges/
dominance/contest/fronts → the Gu-Yang door** (sealed comparative projections + band
crossings over flux fields); **observation/urgency/actionability → STEAD anchors + bands**.
Any reach for these semantics outside the doors — CPU graph search, border/contour/frontline
services, listener frameworks — is a constitutional violation regardless of where it lands
(engine, studio, scenario, workshop-elevation): tripwires flag the reach (Phase 0
ride-alongs), the reach log accumulates the evidence, and allowlist/clearance additions in
these families route DA-RESERVE. PALMA and Gu-Yang are deliberately NOT first-order root
values — the VALUES are ordinary anchored columns; the DOORS are the constitution.
**EML is the SimThing ISA; the kernel is its microarchitecture (Owner-ruled 2026-07-26).**
Core derivable functions are canonically EXPRESSED as **sealed EML stacks** in a core library —
the queryable, predictable spec every descendant pulls (including emission-band threshold
branching). The lowerer recognizes well-known core stacks and EXECUTES them as the existing
fused kernels (reduction passes, accumulator ops): the spec is inspectable, the invariant hot
loop pays zero interpreter cost. Authored/variable logic — bands, urgency, allocation weights,
specialization behavior — stays interpreted EvalEML. **Library growth law:** the library grows
freely in posture, but **every addition — opcode or stack — is DA-approved**; core stacks are
sealed (composable, never shadowed); the consuming-stack requirement is WAIVED for
horizon-targeted additions, which must carry a dated `HORIZON-ENTRY(iso-date)` marker (undated
horizon seams were dropped twice in 0.0.8.6 — dated markers are what the tripwires can assess).
**EVALUATION RULES ARE THE THIRD DA-APPROVED CATEGORY (DA review 2026-08-05, from 5.13's ratification).** The growth law above named *opcode* and *stack*; a rule that changes what an EXISTING composition MEANS is neither, and 5.13's SEAM LAW went in with no ISA-level approval gate because the category did not exist. It does now: **an evaluation rule is DA-approved on the same terms as an opcode, and it is not admitted until it is DECLARED on an author-facing ISA surface.** An author cannot consent to semantics they cannot read; a rule living only in kernel comments, a results doc and a test battery is undeclared however correct its implementation. **THE UNIQUENESS RULE (DA `5192270934` / exit-proof `5193244394`, authored on the ISA surface by 5.14):** a `MUL` result is fused into its consuming **`ADD` or `SUB`** **iff that fusion is UNIQUE**. Exactly one `MUL` → `ADD`/`SUB` is **FUSED** (one rounding; `SUB` is fused-multiply-subtract); two or more `MUL`s → the same consumer is **UNFUSED** (`U`: each `MUL` rounds to f32 first). **No tie-break exists or will exist.** Admitted-program census measured **4** MUL→SUB dataflows (all unique → FUSED). **THE SEAM LAW is only an instance** of uniqueness at a map/fold boundary (ratified 2026-08-05, 5.13 @ `339e4f8f`; re-expressed 5.14): when a field map ends `[.., MUL, RETURN]` **and** the fold is the canonical Sum `[PARAM ACC, PARAM MAPPED, ADD, RETURN]`, exactly one `MUL` feeds that fold `ADD`, so the seam is **SPECIFIED FUSED on every execution arm** — `acc = fma(a, b, acc)`, one rounding — realized as CPU `mul_add`, interpreted WGSL `fma` behind a uniform registration-derived flag, and a generated fused fold on SSA-JIT. This is a **language definition, not an optimization**: the composition means the fused value. It is exact-by-construction and costs no opcode, no 5.10 widening, and no escape hatch.

**REFINEMENT to the Exact-Value Provenance Law (DA review 2026-08-05).** *"Never in an instruction sequence"* must not be read to forbid the repair that actually works. Three constructions, and only the first is dead:
- A **FENCE** attempts to PREVENT a licensed rewrite. Measured dead three times: 5.11's bitcast round-trips, 5.12's Knuth two-sum error lanes, and 5.13's own `fma(a,b,-0.0)` probe, which the certified tuple algebraically re-simplified and re-contracted.
- **INTRINSIC PINNING** SELECTS the operation the licensed rewrite would have produced, so no discretion remains. A conforming compiler MAY contract `a*b+c` into `fma`; it may NOT decontract an authored `fma`. This does not fight the rewrite — **it removes the freedom by choosing its outcome**, and it is lawful.
- **GRID EXACTNESS** makes every licensed rewrite land on identical bits (5.12's `EML_LN_LN2_HI`). Lawful.

The law's target was always the compiler's DISCRETION, never arithmetic as such. Pinning and grid-exactness both eliminate discretion; a fence merely objects to it.
**Call-tracking telemetry:** per-slot × per-stack invocation profiles are always-on **iff bound
to an existing checked/cleared lifecycle of matching scope** (bind candidate at implementation:
the session observation/snapshot lifecycle); if no such lifecycle exists, fall back to global
per-stack-id counters always-on + per-slot profiles as an opt-in observation channel. Telemetry
is **write-only with respect to the sim** — counters never feed any generation computation (bit-exact
replay; decisions stay on-device). The call profile is a standing Consumer-Law surface.
**The Exact-Value Provenance Law (Owner-directed, 2026-08-04).** **Exactness is a property of
ADMITTED DATA, never of an emitted instruction sequence.** An exact primitive's proof travels no
further than the last instruction a compiler is forbidden to rewrite. Every construction that
defends exactness by HOW THE ARITHMETIC IS WRITTEN — a bitcast fence, a two-sum error lane, an
operation-order convention — is defeated the moment a vendor backend contracts, reassociates or
elides it, and it fails SILENTLY, as drift rather than as an error. The lawful construction makes
the VALUE exactly representable, so that every rewrite a compiler may legally perform lands on the
same bits. 5.12 is the worked instance: the vendor route collapsed a Knuth two-sum error lane to
zero and defeated bitcast fences, and the repair was a 2⁻¹⁶-grid constant (`EML_LN_LN2_HI`) whose
product is exact with contraction or without it. **The enforceable corollary — a consumer of an
exact primitive INHERITS NOTHING from instruction-sequence shape.** Exactness travels with
**admitted arithmetic meanings** (5.14 uniqueness + pinned opcodes), not with per-consumer digest
policing. 5.13's per-consumer exactness evidence plumbing is **deleted** by 5.14; cross-arm
bit identity of consumers is a language-level witness following from those meanings. The
pre-existing witness is 5.11's STEAD falloff consumer: bit-exact across arms once the uniqueness
seam is specified. **This law is prose ON PURPOSE and knows the
tier it sits in.** It states the DESIGN INSTRUCTION — reach for the grid, never for a stronger
fence — which no rejection rule can teach; the obligation is enforced by the authored language
meanings and faithful arm lowerings (5.14), not by a growing per-consumer census.

**Bespoke parallel systems are constitutional violations, not conveniences.** Named
transitional debts with scheduled promotions: the R6/R6B/R6C combat rehearsals (Phase 8) and
the movement readback seam, now DISCHARGED GENERICALLY by the 2026-08-09 ActionBand amendment --
Phase 7 is the intrinsic ActionBand facility (`multi-axis-ActionBand-STEAD.md`, anchors
`actionband-*`), and movement is a born-mortal spatial vendorization at 7.4 whose readback is an
ordinary projection of ActionBand state at 7.5, never a peer seam. Anything new of that shape
fails admission.
**Emergence-first (the prize law).** Every phase carries a 12.10-style emergence falsifier:
authored-parameter changes must produce qualitatively different, UNSCRIPTED macro outcomes; a
phase that leaves the mechanism sound but the dynamics inert — over-quantized bands, over-
constrained admission, scripted outcomes, dead attractors — FAILS its exit-proof regardless of
green checks. P1 bounded-horizon and P2 one-shared-rule are preserved as the conditions under
which Wei’s emergence generalizes (per the paper); band ladders quantize the READING never the
FIELD, so the continuous dynamics that carry the emergence are never discretized away.
Promotion, not rewrite — existing code keeps working at every rung. Intrinsic ≠ implicit:
all automatic behavior is derived at admission, typed, spanned-erroring — never runtime magic.
Oracle-judged: RF-1 conservation + bit-exact replay at every phase. Deferred capabilities from
the RF-5A rulings (mid-session authored refresh; authored complete arena composition) land
here, not as scenario patches.

## 5. Open conditions

1. ~~0.0.8.6 TP chain closed~~ **SATISFIED** — 0.0.8.6 CLOSED 2026-07-25 (receipt `21f77abb55c9`, PR #1431).
2. ~~RF-harness (0.0.8.4.8.4.2) disposition~~ **RULED 2026-07-26** — folded into this track: RFH-1 = Phase 0 (guard, early, non-breaking), RFH-2 = Phase 9 (sweep, late, after Phase 4's object model). The separate track is dissolved; `design_0_0_8_4_8_4_2_RF_harness.md` reduced to a superseded pointer.
3. ~~Owner commits the full plan~~ **SATISFIED 2026-07-26** — §3b ladder committed (guards early, sweeps late honored: 0.x guards lead, 9.x sweeps trail); track opened via `gen_orientation.sh --open`.
4. **Vendorized-crate test validation — DEFERRED TO POST-CLOSEOUT (Owner-directed 2026-08-05).** A full-suite run at `6e4f2e08` measured the engine crates **green (282 passed / 0 failed, 100 doctests / 0 failed, GPU arms included)** and `simthing-clausething` **broken: 19 test compile errors**, root-caused to `E0063 missing field 'admission_disposition' in PropertySpec` — a **5.1-era** core change (`ANCHOR-DISPOSITION-ADMISSION-0`) never carried into the vendorized crate's tests. This is the accepted cost of the Vendor Containment Law, not a defect of it: uncoupling is what let the engine restructure `AnchorLocusRemap`, delete the 5.13 evidence machinery, and change `SlotIndex` semantics without a vendor veto. **0.0.8.7 sustains vendorized COMPATIBILITY and does not repair vendor tests.** After closeout, a substantial proportion of `simthing-kernel`, `simthing-clausething` and `simthing-workshop` test surface needs revalidation against the canonized object model, and each broken target gets a **repair-or-retire** ruling — a vendor test that is neither is the worst of both. **Do not schedule this as a 0.0.8.7 rung**; it is post-closeout scope and belongs to whatever track absorbs the vendor door. Recorded here because nothing else scheduled it and the breakage is invisible to CI by design (no `cargo test` in CI, Owner ruling).
