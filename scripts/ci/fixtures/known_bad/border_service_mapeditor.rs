// CI fixture: BORDER-SERVICE in mapeditor — semantic border service (not presentation cache).
// Must fire HEURISTIC INSPECT even when placed under simthing-mapeditor.
pub struct FrontlineTracer;

pub fn border_service_for_studio_semantics() -> FrontlineTracer {
    FrontlineTracer
}

pub fn contour_extraction_for_dominance_band() -> u32 {
    0
}
