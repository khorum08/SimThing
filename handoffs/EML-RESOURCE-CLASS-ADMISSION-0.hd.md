---
rung: EML-RESOURCE-CLASS-ADMISSION-0
kind: rung
track: 0.0.8.7
base_sha: 52cfba153742705a5db925025a1ca06717368079
audience: coding
model_tier: frontier
owner_approved: true
expected_route: DA-RESERVE(gate-wiring)
owner_notes: "Owner-approved 5.x amendment #1482; DA graduation and 5.7 ruling 5145263415. Carry 5.4 facts: configured stack/node caps 32/32, observed peak stack 3 and max tree 9, PALMA ~2.27x and Gu-Yang ~12.67x with 2 dispatches versus 1. Determinism is sovereign over cost; no primitive is promised. PR #1535 makes Per-Generator Admission Law binding: no dense cap or speculative budget may bind LinkGraph. 2026-08-11 remains sovereign."
surfaces: ["crates/simthing-core/src/eml_nodes.rs", "crates/simthing-core/src/eml_registry.rs", "crates/simthing-kernel/src/field_sweep.rs", "crates/simthing-kernel/src/shaders/field_sweep.wgsl", "crates/simthing-kernel/src/eml_opcode_gate.rs", "crates/simthing-kernel/src/exact_magnitude_gate.rs", "crates/simthing-gpu/src", "crates/simthing-driver", "scripts/ci", "docs"]
forbidden: ["LinkGraph node/edge/degree budget type without a live caller, any REGION_FIELD_* reach on sparse adjacency, validate_grid, or structural_n4_atlas_partition", "algebra/field tags, operator dispatch, second interpreter, hand-written field WGSL, or abandonment of the admitted IR", "primitive/opcode without both ExactPrimitiveAdmission keys and a concrete consumer, or more than one primitive", "5.8 comparative projections, Phase 6+, corpus/scenario edits, oracle deletion, deadline/lifecycle changes"]
required_checks: ["current-admission census plus 32/33-node and 32/33-stack boundary falsifiers", "adapter-pinned class/resource/occupancy measurements with matched PALMA and Gu-Yang envelopes", "stack-4/legacy-32 CPU/GPU bit parity and deterministic smallest-class selection", "ExactPrimitiveAdmission determinism-key and cost-key negatives; optional one-consumer positive only if proven", "FIELD-SWEEP-SINGLE-PATH, legacy-caller, dense-cap, STEAD, inventory/lifecycle/detachability, agent/doctrine/doc-budget, exact-head clearance"]
stop_conditions: ["stale-orient-receipt", "occupancy or backend resource effect cannot be measured truthfully through an admitted door", "cap-to-budget changes the currently admitted program set or class selection is caller-authored", "cost evidence is used to weaken determinism or a primitive lacks a live consumer", "LinkGraph acquires a speculative budget/dense cap or specialization requires a bespoke kernel"]
---
## BUILD
- Land one closed `EmlResourceClass` authority shared by ordinary EML registration and field sweeps; callers never author raw stack/node sizes. Admit at minimum a pipeline-constant stack-4 class covering every measured 5.4–5.6 program and the legacy fixed-32 compatibility class; select the smallest fitting class deterministically from validated peak stack and per-tree node facts.
- Replace `MAX_EML_TREE_NODES`/`EML_STACK_MAX` as universal admission decisions with per-class budgets across legacy metadata, formula registration/replacement, field program admission, node-buffer sizing, CPU evaluation, and GPU pipeline/session creation. Preserve the exact currently admitted set, with legacy compatibility mapping explicit and typed over-budget errors naming class, requested facts, and limits.
- Compile/cache one canonical interpreter form per admitted class using pipeline constants or IR-generated/JIT specialization; class selection is resource dispatch only, never algebra/field/operator dispatch. Any JIT output is generated solely from the admitted map/fold/post IR and remains under `FIELD-SWEEP-SINGLE-PATH`.
- Add a sanctioned supported-adapter measurement surface that reports class, compiled resource footprint, occupancy/active-wave evidence or equivalent backend counter, matched work, dispatch count, median/worst, and counter provenance. Compare stack-4 against legacy-32 on identical programs; unavailable required counters are a STOP, never timing-derived memory shadow.
- Re-run 5.4's matched PALMA and Gu-Yang cases and adjudicate the inherited gate at measured occupancy: generic median `<=1.25x` bespoke and supported-adapter worst `<=1.5x`. If pipeline classes miss, a JIT-backed class may be added from the same IR; failure never authorizes bespoke permanence or IR abandonment.
- Land a sealed `ExactPrimitiveAdmission` door with a sovereign determinism key (specified bit semantics, domain/special-value policy, exhaustive reference artifact, supported-backend replay artifact) and conjunctive cost key (measured resource class). Zero admitted primitives is valid; admit at most one only when a concrete consumer exists, with spanned rejection for every missing key. Bounded-domain policy is legal; Candidate-F is not auto-promoted.
- Add only load-bearing class/door proofs, one results doc/index line/inventory rows, and 5.7 PROBATION; coding does not move the pointer.
## FENCES
- Per-Generator Admission Law is absolute: `GridOffsets` retains dense execution windows and tiles on deferral; `LinkGraph` remains uncapped and gains no budget type absent a live caller. Preserve the permanent dense-cap-crossing planted red.
- Determinism keys are non-substitutable; cost/occupancy can only conjunct. No approximate primitive may serve conservation or hard-threshold consumers, and no second opcode/kernel path is implied by the door.
- Preserve full-buffer PALMA/Gu-Yang and N4/N8/radius-r/LinkGraph parity, canonical order, conductance/producer certificates, replay, homogeneous lanes, Native Ingestion, Corpus Boundary, and the Invariant Set. No 5.8 semantics or seven-oracle retirement.
## EXIT-PROOF
- A complete census maps every currently admitted EML/field program to exactly one deterministic smallest fitting class; the union admits the prior set exactly, while planted over-budget/class-forgery cases reject before upload/dispatch.
- Stack-4 versus legacy-32 has adapter-pinned bit parity and truthful resource/occupancy evidence; inherited PALMA/Gu-Yang thresholds are adjudicated, or the coder STOPs with the unavailable/failed measurement rather than inventing a verdict.
- `ExactPrimitiveAdmission` rejects each missing determinism/cost/consumer/backend/domain artifact; zero primitives is an accepted result, while any single admitted primitive has exhaustive CPU/reference and supported-backend replay bit identity plus a named consumer.
- Core/kernel/GPU/driver and field suites, permanent sweep/cap/caller gates with selftests, STEAD, lifecycle/inventory/detachability, anchors/orientation, evidence and settled-body exact-head clearance are green. Return PROBATION to DA; no 5.8 dispatch.
