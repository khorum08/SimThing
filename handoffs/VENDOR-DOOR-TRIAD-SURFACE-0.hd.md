---
rung: VENDOR-DOOR-TRIAD-SURFACE-0
kind: remedial
track: 0.0.8.7
base_sha: 9e81ef6aa12725d185372eed3ab8c7c8bffc9cb8
audience: coding
model_tier: frontier
owner_approved: true
expected_route: DA-RESERVE(gate-wiring)
owner_notes: "RE-AUTHORED by DA after 11.1a graduated. The previous object (HD-RECEIPT aff35cbd509a) is RETIRED and must not be reused: it predated the seam and carried no EML obligation, so dispatching it would have produced a third STOP at 11.2 for the same reason as the first two. This version is written against the tree 11.1a actually left behind. Run delegates to the real seam entry SimSession::open_from_spec_with_admitted_field_sweeps. Triad columns are three ColumnIndex values and a vendor may NOT mint them - they come through the admitted authored door. The load-bearing constraint is unchanged: chokepoint, corridor, front and dominance are BORN observables and authoring one as an API is a STOP."
surfaces: ["crates/simthing-embedder", "docs/tests", "scripts/ci/test_inventory.tsv"]
forbidden: ["new simulation mechanism, authority, evaluator, service, registry, scheduler, or second execution path behind the door", "minting a public chokepoint, corridor, front, dominance, or contest API; those are observables born from declaration over admitted topology, never surfaces the door creates", "CPU decisions derived from read-only observation; comparative output is observed, never used to steer on the CPU side", "raw ColumnIndex construction at the door; Triad columns reach the vendor only through the admitted authored door with its bounds check", "engine-crate edits; 11.1a landed the seam and this rung is re-export only, so a missing facility is a STOP and not a build", "owned state in simthing-embedder: no static, Mutex, Arc, OnceCell, RefCell, cache, or registry", "any engine crate depending on simthing-embedder; the door stays a leaf", "wiring or withdrawing the ActionBand door entry; that is 11.1d, record the finding and hand it over", "the 11.2 guide, exemplars, or admission gate; 11.1c, 11.1e, 11.3, 12.x, StemThing-B, Vector CostBand, ClauseThing debts"]
required_checks: ["before edits render/read this handoff; fresh Frontier coding ORIENT receipt at rule stamp 61818ff7d4adda84; ACK every rendered REQUIRED-ANCHOR", "FIRST STEP archaeology: read what 11.1a landed - SimSession::open_from_spec_with_admitted_field_sweeps and the seam witness at crates/simthing-driver/tests/field_sweep_session_seam_0.rs - and map every door entry to the exact landed surface it delegates to", "Derive exposes competing emitter-class declaration delegating to ComparativeEmitterClass", "Bind exposes admitted PALMA and Gu-Yang N4 field-sweep compilation and the comparative projection bands, delegating to compile_palma_n4_field_sweep, compile_gu_yang_n4_field_sweeps and ComparativeProjectionBands, plus read-only comparative observation delegating to GuYangStallOutputs", "Run reaches the seam: the door drives SimSession::open_from_spec_with_admitted_field_sweeps rather than re-implementing admission or execution", "Triad columns are obtained lawfully through the admitted authored ColumnIndex door with its bounds check; the door never constructs a raw ColumnIndex", "the door exposes the authored EML surface EmlGadgetStackSpec and EmlGadgetInstanceSpec so a vendor can state a law as authored DATA", "re-prove the 11.1 facade law unchanged: leaf by dependency arrow, zero owned state by grep, every new public entry delegated", "an embedder test declares competing emitter classes, compiles admitted topology, opens through the seam, runs, and observes Triad output through the five verbs with no engine edit and no scenario-side wiring", "planted defect proves the door cannot fabricate a Triad observable it did not observe, and REDs for its own named reason on the production path", "inventory rows for new tests; lifecycle, detachability, DOC-BUDGET, orientation, sanctioned digest, Agent and Doctrine green; inspect every hosted Doctrine Scan step; return exact base/head/tested_code_sha plus workflow ids"]
stop_conditions: ["a facility the exemplar needs is genuinely absent from production rather than merely unexported - name the exact missing surface and stop; 11.1a exists because that answer was correct twice", "exposing a facility appears to require new state, a new authority, or a CPU-side decision from observation", "the only way to satisfy the exemplar is to author a chokepoint, corridor, front, or dominance API on the door", "a vendor cannot obtain Triad columns without minting a raw ColumnIndex", "the facade law cannot be preserved: the door would need owned state or an engine crate would need to depend on it"]
---
## BUILD
- Complete the **Bind** and **Derive** contract the Phase 11 parent row names, by **re-export and delegation only**. 11.1a landed the execution seam, so every facility below is now reachable; this rung authors no capability.
- **Derive** — expose declaration of **competing emitter classes**, delegating to `ComparativeEmitterClass`. This is the declaration from which dominance and border observables are born per P5.
- **Bind** — expose admitted **PALMA** and **Gu-Yang** N4 field-sweep compilation (`compile_palma_n4_field_sweep`, `compile_gu_yang_n4_field_sweeps` and their specs), the comparative projection bands, and **read-only** observation of comparative output (`GuYangStallOutputs`), alongside the existing threshold, queued-draw and shadow entries.
- **Run** — reach the seam. The door drives `SimSession::open_from_spec_with_admitted_field_sweeps`, which 11.1a landed; it does not re-implement admission or execution.
- **Triad columns, lawfully.** The seam takes three `ColumnIndex` values. After Phase 9 a vendor may not mint one: expose acquisition through the **admitted authored door** with its bounds check. A door that hands out raw columns re-opens the mint Phase 9 closed.
- **Authored EML surface** — expose `EmlGadgetStackSpec` and `EmlGadgetInstanceSpec` so a vendor can state a law as authored DATA. The types already have production consumers, so this is re-export. 11.1e adds the law-stating gadget itself; this rung opens the door it arrives through.
## FENCES
- **The door exposes declaration and observation; it never mints an observable.** `chokepoint`, `corridor`, `front` and `dominance` have zero public production surfaces and must still have zero when this rung lands. Authoring one is a **STOP**, not a deliverable.
- Observation is strictly read-only. Comparative output may be read; no CPU-side decision may be derived from it. Decisions stay on-device.
- **Re-export, not re-implementation.** A facility genuinely absent from production is a substrate gap: name it and STOP. That answer was correct twice on this arc and produced 11.1a.
- **ActionBand is not this rung.** The door already advertises `action_band_commitments` over a function with no production caller. Record the finding in the archaeology and hand it to **11.1d** — do not wire or withdraw it here.
- The 11.1 facade law holds without exception: leaf, zero owned state, every public entry delegating to a named graduated surface.
## EXIT-PROOF
- The 11.2 network-saturation full-Triad exemplar is **writable through Derive/Populate/Overlay/Bind/Run alone** — witnessed by an embedder test that declares emitter classes, compiles admitted topology, opens through the seam, runs, and observes Triad output with zero engine edits and zero scenario-side wiring.
- **A vendor can state a law as authored data through the door**, proven by authoring an `EmlGadgetStackSpec` that reaches a production consumer.
- **Facade law re-proven unchanged:** leaf by dependency arrow, zero owned state by grep, every new public entry delegated — diff-proven, the same three proofs 11.1 and 11.1a carried.
- **Shape test green:** the delta adds **zero** public surfaces named for the four born observables, and the planted defect proves the door cannot fabricate a Triad observable it did not observe, REDing on the production path for its own named reason.
- **No raw column mint:** a planted attempt to construct a `ColumnIndex` at the door fails; Triad columns arrive only through the admitted authored door.
- Evidence: `docs/tests/vendor_door_triad_surface_0_results.md`, inventory rows for new tests, ladder row stamped at merge with `merged #<PR> @ <sha>`.
