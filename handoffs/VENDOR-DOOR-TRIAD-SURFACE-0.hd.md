---
rung: VENDOR-DOOR-TRIAD-SURFACE-0
kind: remedial
track: 0.0.8.7
base_sha: a716ca48a7864e802e324fe0f2ee7c9e62d0d12a
audience: coding
model_tier: frontier
owner_approved: true
expected_route: DA-RESERVE(gate-wiring)
owner_notes: "Minted by DA ruling 5383361312 after a lawful 11.2 STOP. The Phase 11 parent row already contracts Bind to carry Triad bands; the door graduated at 11.1 does not expose them, so the 11.2 full-Triad exemplar is unwritable. This rung completes that contract by RE-EXPORT ONLY. The load-bearing constraint: chokepoint, corridor, front and dominance have ZERO public production surfaces and must still have zero afterwards. They are BORN observables. Authoring a chokepoint-style API is a STOP, not a deliverable."
surfaces: ["crates/simthing-embedder", "docs/tests", "scripts/ci/test_inventory.tsv"]
forbidden: ["new simulation mechanism, authority, evaluator, service, registry, scheduler, or second execution path behind the door", "minting a public chokepoint, corridor, front, dominance, or contest API; those are observables born from declaration over admitted topology, never surfaces the door creates", "CPU decisions derived from read-only observation; comparative output is observed, never used to steer on the CPU side", "engine-crate edits to make the exemplar writable; if a facility is genuinely absent rather than merely unexported, STOP and name it", "owned state in simthing-embedder: no static, Mutex, Arc, OnceCell, RefCell, cache, or registry", "any engine crate depending on simthing-embedder; the door stays a leaf", "the 11.2 guide, exemplars, or admission gate; 11.3, 12.x, StemThing-B, Vector CostBand, ClauseThing debts"]
required_checks: ["before edits render/read this handoff; fresh Frontier coding ORIENT receipt at rule stamp 61818ff7d4adda84; ACK every rendered REQUIRED-ANCHOR", "FIRST STEP archaeology: map each Triad facility this rung exposes to the exact landed simthing-driver surface it delegates to, and prove by grep that no new production capability is authored", "Derive exposes competing emitter-class declaration delegating to ComparativeEmitterClass", "Bind exposes admitted PALMA and Gu-Yang N4 field-sweep registration delegating to PalmaN4FieldSweepSpec, PalmaOverlayParameterizedN4Spec, GuYangN4FieldSweepSpec, GuYangOverlayParameterizedN4Spec, and read-only comparative observation delegating to GuYangStallOutputs", "re-prove the 11.1 facade law unchanged: leaf by dependency arrow, zero owned state by grep, every new public entry delegated", "an embedder test declares competing emitter classes, registers admitted topology, runs, and observes Triad output through the five verbs with no engine edit and no scenario-side wiring", "planted defect proves the door cannot fabricate a Triad observable it did not observe, and REDs for its own named reason on the production path", "inventory rows for new tests; lifecycle, detachability, DOC-BUDGET, orientation, sanctioned digest, Agent and Doctrine green; inspect every hosted Doctrine Scan step; return exact base/head/tested_code_sha plus workflow ids"]
stop_conditions: ["a Triad facility the exemplar needs is genuinely absent from production rather than merely unexported - name the exact missing surface and stop", "exposing a facility appears to require new state, a new authority, or a CPU-side decision from observation", "the only way to satisfy the exemplar is to author a chokepoint, corridor, front, or dominance API on the door", "the facade law cannot be preserved: the door would need owned state or an engine crate would need to depend on it"]
---
## BUILD
- Complete the **Bind** and **Derive** contract the Phase 11 parent row already names, by **re-export and delegation only**. Every facility below is a landed production surface in `simthing-driver`; this rung authors no capability.
- **Derive** — expose declaration of **competing emitter classes**, delegating to `ComparativeEmitterClass`. This is the declaration from which dominance and border observables are born per P5.
- **Bind** — expose registration of the admitted **PALMA** and **Gu-Yang** N4 field-sweep topology (`PalmaN4FieldSweepSpec`, `PalmaOverlayParameterizedN4Spec`, `GuYangN4FieldSweepSpec`, `GuYangOverlayParameterizedN4Spec`), and **read-only** observation of comparative output (`GuYangStallOutputs`), alongside the existing threshold, queued-draw and shadow entries.
- Land the witness: an embedder test that declares competing emitter classes, registers admitted topology, runs, and observes Triad output **through the five verbs alone** — no engine edit, no scenario-side wiring. This is the capability 11.2 needs and could not reach.
## FENCES
- **The door exposes declaration and observation; it never mints an observable.** `chokepoint`, `corridor`, `front` and `dominance` have zero public production surfaces today and must still have zero when this rung lands. If the work turns into authoring one of those APIs, the shape is wrong — **STOP**.
- Observation is strictly read-only. Comparative output may be read; no CPU-side decision may be derived from it. Decisions stay on-device — the 11.1 Bind law, unchanged.
- **Re-export, not re-implementation.** If a needed facility is genuinely absent from production rather than merely unexported, that is a substrate gap: name the exact missing surface and STOP rather than building it here.
- The 11.1 facade law holds without exception: `simthing-embedder` stays a leaf with zero owned state, and every new public entry delegates to a named graduated surface.
- Out of scope: the 11.2 guide, its exemplars, and its admission gate. This rung only makes them writable.
## EXIT-PROOF
- The 11.2 network-saturation full-Triad exemplar is **writable through Derive/Populate/Overlay/Bind/Run alone**, witnessed by an embedder test that declares, registers, runs and observes with zero engine edits and zero scenario-side wiring.
- **Facade law re-proven unchanged:** leaf by dependency arrow, zero owned state by grep, every new public entry delegated — diff-proven, the same three proofs 11.1 carried.
- **Shape test green:** `chokepoint`, `corridor`, `front`, `dominance` still have zero public production surfaces after the rung; the planted defect proves the door cannot fabricate a Triad observable it did not observe, and REDs on the production path for its own named reason.
- Evidence: `docs/tests/vendor_door_triad_surface_0_results.md`, inventory rows for new tests, ladder row stamped at merge with `merged #<PR> @ <sha>`.
