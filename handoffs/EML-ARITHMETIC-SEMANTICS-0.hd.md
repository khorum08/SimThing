---
rung: EML-ARITHMETIC-SEMANTICS-0
kind: rung
track: 0.0.8.7
base_sha: 9ae4ea9c0bdea222923b8cb40d52192533dc3701
audience: coding
model_tier: std
owner_approved: true
expected_route: DA-RESERVE(gate-wiring)
owner_notes: "DA order 5191578752 is the live authority, revised by Owner and landed in #1647 @ 9ae4ea9c: Std — Grok; DA pre-dispatch ruling 5191860082 APPROVED this handoff with two bounded amendments and requires no further DA review. This supersedes the brief 6.4 sequencing in 5191250344/#1645; StemThing-A is deferred until 5.14 completes. The DA has already fixed the arithmetic semantics: ADD/SUB/MUL/DIV are IEEE-754 single-rounding with no reassociation; MIN/MAX/CLAMP_BOUNDED/CLAMP_FLOORED are exact selections; EXP/LN are NOT reopened and their pinned digests may not move; a MUL feeding exactly one ADD with no other MUL feeding that ADD IS fused. Two-or-more-MULs feeding one ADD is genuinely ambiguous and must STOP for DA rather than inventing a tie-break. The deliverable is deletion of the 5.13 per-consumer exactness-policing evidence plumbing while preserving every consumer cross-arm bit-identity assertion as a language-level witness."
surfaces: ["crates/simthing-kernel/src", "crates/simthing-kernel/src/shaders", "crates/simthing-workshop/tests", "docs/full_eml_unification.md", "docs/eml_gadget_library.md", "docs/tests", "docs/design_0_0_8_7_rf_arena_modernization.md", "scripts/ci/test_inventory.tsv", "scripts/ci/allow/kernel_surface.txt", "docs/sanctioned_surface.md", "docs/orchestrator_orientation.md"]
forbidden: ["inventing a contraction tie-break for two-or-more MULs feeding one ADD", "reopening, changing, or requalifying EXP/LN semantics, domains, algorithms, pinned exhaustive digests, or cost gates", "reintroducing per-consumer exactness evidence, declarations, digest axes, arm lists, census rows, waivers, exceptions, or compatibility shims", "new EML opcode, 5.10 primitive-door widening, new execution path, Invariant Set change, or allowlist widening to make arithmetic semantics pass", "treating compiler hopes, source ordering, a bitcast fence, or optimizer suppression as the language meaning; intrinsic pinning or exact values are lawful only when they remove compiler discretion", "editing docs/EML_exp_ln_unification_expansion.md in this rung; the expansion amendment follows 5.14", "6.4/StemThing-A, 6.5, Phase 7, 8.x bindings, movement/contention, or successor work"]
required_checks: ["fresh Std coding ORIENT-RECEIPT + current ANCHOR-ACKs + HD-RECEIPT before edits; read DA orders 5191578752 and 5191860082, canonical 5.14 row at #1647, full EML §4, and expansion §8.2 forward-fit", "FIRST STEP before lowering edits: enumerate every admitted EML program for contraction-ambiguous shapes where two or more MUL results feed one ADD; publish each program/gadget and node indices plus measured contraction behavior on CPU twin, interpreted WGSL, and SSA-JIT, including which MUL contracts on any arm; empty set proceeds, non-empty set STOPS and returns that full measurement packet for DA tie-break", "declare one author-facing arm-independent arithmetic meaning: ADD/SUB/MUL/DIV single-rounding with no reassociation; MIN/MAX/CLAMP exact selections; EXP/LN pinned existing meanings; general contraction rule one-MUL-to-one-ADD-with-no-other-MUL => fused; re-express SEAM LAW only as an instance", "fail closed: unrecognized or contraction-ambiguous composition is NOT exactness-proven and is refused, never assumed equivalent", "prove CPU twin, interpreted WGSL, and SSA-JIT are faithful lowerings per rounding-bearing opcode and contraction rule; plant production-lowering defects that RED each arm, not test-only alternate executors", "delete the 5.13 per-consumer exactness evidence plumbing rather than deprecating it: declarations, arm lists, digest axes, shape proofs, census rows, derive_consumer_arms-era machinery; preserve every assertion that a consumer is bit-identical across its arms by re-homing it as a language-level witness; grep-prove the evidence architecture absent and make total crates/simthing-kernel/src line count decrease", "author a NEW EXP consumer in this rung that requires zero new exactness evidence, zero exactness declaration, and zero census row; its determinism must follow from admitted arithmetic semantics alone", "preserve bit identity of falloff, LogAccumulate, and the other 5.13 consumers without per-consumer exactness evidence; keep EXP/LN exhaustive digests exactly 0x7875a45ba919d588 / 0x196aced82d03f378", "run synthetic focused semantics/deletion falsifiers plus EXP/LN qualification/freshness, field-sweep parity including Gu-Yang, resource/JIT, residue, detachability, lifecycle, inventory, orientation, anchors, agent/doctrine scans, hosted Doctrine Scan/Exec; report exact commands and counts"]
stop_conditions: ["the contraction-ambiguity sweep finds any admitted two-or-more-MULs-into-one-ADD shape; STOP before semantic/lowering work and return program/gadget, node indices, and measured contraction behavior for CPU twin, interpreted WGSL, and SSA-JIT, including which MUL contracts on any arm; report behavior only, never choose meaning", "any specified composition cannot have one arm-independent meaning without a measured performance cost; STOP and return the measurement", "any EXP/LN pinned digest, admitted domain, algorithm identity, cost gate, or primitive semantics would move", "removing per-consumer policing appears to require a replacement per-consumer evidence/declaration/census mechanism", "repair requires a new opcode, 5.10 door widening, Invariant Set change, new execution path, or allowlist widening", "completion requires 6.4/StemThing-A, expansion-amendment edits, Phase 7, 8.x, or successor work"]
---
## BUILD
- Finish the EML arithmetic language instead of policing each consumer. Implement exactly the DA-specified meanings from `5191578752` / #1647; do not rediscover numerical semantics.
- FIRST enumerate contraction ambiguity across admitted programs. Empty set proceeds. Any `ADD` fed by two or more `MUL` results STOPS the rung with program/gadget, node indices, and measured CPU/interpreted/JIT contraction behavior including which `MUL` contracts; the coder reports behavior and must not choose a tie-break.
- State and lower the general contraction law: a `MUL` whose result flows into exactly one `ADD`, with no other `MUL` feeding that `ADD`, IS fused. The existing SEAM LAW becomes one boundary instance of this general rule.
- Delete the 5.13 per-consumer exactness evidence plumbing while preserving all consumer cross-arm bit-identity assertions as language-level witnesses. Keep fixed-cost per-opcode/toolchain qualification. A new EXP consumer must inherit determinism from the language and need no new exactness evidence, declaration, arm digest, or census row.
## FENCES
- `EXP` / `LN` are frozen: pinned digests `0x7875a45ba919d588` / `0x196aced82d03f378`, domains, algorithm identities, and cost gates do not move.
- No tie-break invention, no per-consumer replacement mechanism, no new opcode, no 5.10 widening, no allowlist widening, no expansion-doc amendment.
- 6.4 / StemThing-A is deferred by the later Owner order until this rung graduates. 6.5, Phase 7 and 8.x remain fenced.
## EXIT-PROOF
- Published ambiguity sweep is empty; otherwise return STOP with program/gadget, node indices, and measured contraction behavior on all three arms, with no semantic guess.
- Each rounding-bearing opcode and licensed contraction has one arm-independent meaning and CPU/interpreted/JIT faithful lowerings with planted production REDs.
- New EXP consumer costs zero new exactness evidence/declaration/census; 5.13 evidence plumbing is deleted and grep-absent, consumer bit-identity assertions survive as language-level witnesses, and kernel source line count decreases.
- Existing consumers remain bit-identical, EXP/LN exhaustive digests are unchanged, inherited batteries and governance gates are green.
- Return PROBATION / proof-present with exact pushed/tested head. Coding does not `/clearance`, merge, move the pointer, start 6.4, or amend the expansion doc.
