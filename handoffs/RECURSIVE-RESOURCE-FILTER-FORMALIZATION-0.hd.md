---
rung: RECURSIVE-RESOURCE-FILTER-FORMALIZATION-0
kind: rung
track: 0.0.8.7
base_sha: 2266ee531556b3ed7420365faeb1f25bc4d3f630
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(binding)
owner_approved: true
owner_notes: "Phase 15 opened by DA/Owner mint 5519324403 and PR #1935 @ 2266ee53. Binding sources are the exact 15.0 ladder row plus docs/phase15_recursive_resource_filter_charter.md. Runtime invention is presumptively RED; recognition that the existing resident representation is already sufficient and no A(lambda) runtime representation is needed is explicitly a successful exit. Temporary anchors: phase15-filter-operator, phase15-recursion-axes, phase15-sufficient-statistic, phase15-deletion-census; resident-clearing-cutover-canon is canonical."
surfaces: ["crates/simthing-spec/src/**", "crates/simthing-driver/src/**", "crates/simthing-gpu/src/**", "crates/simthing-kernel/src/**", "crates/simthing-workshop/tests/**", "docs/phase15_recursive_resource_filter_charter.md", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/tests/*recursive_resource_filter*", "docs/tests/current_evidence_index.md", "scripts/ci/doctrine_anchors.tsv", "scripts/ci/constitutional_surfaces.tsv", "scripts/ci/test_inventory.tsv", "scripts/ci/anchor_reach_log.tsv"]
forbidden: ["runtime ShadowPrice, DemandCurve, ResponseProgram, LambdaPlane, ScarcityColumn, A(lambda) storage, or any richer resident response representation before 15.0 proves necessity", "second clear/disburse/feedback authority, peer market, alternate quantizer, changed graduated Q semantics, or changed canonical T_s ABI", "second PALMA/Gu-Yang solve, host descendant walk, private field/query solver, duplicate Triad application, or clearing-owned cache", "hard precedence encoded as continuous response curvature or same-generation lambda convergence/reweight/re-clear", "unbounded response state, CPU ordinary economic continuation, weakened Germ Self-Consumption, or economic adapter/role-newtype/seam translator", "physical fusion work; the existing ordered band pipeline is already the two projections and is to be recognized rather than rebuilt", "any modification or re-blessing of the frozen 14.1 comparator", "15.1 implementation, held harness-fix session, RESOURCE-RESPONSE-COMPRESSION-0, pointer movement, graduation, or track closeout"]
required_checks: ["before edits render/read this handoff and carry its HD-RECEIPT plus a fresh coding ORIENT receipt under the current rule stamp; ACK all rendered REQUIRED-ANCHORS including the four Phase-15 temporary anchors and resident-clearing-cutover-canon", "ARCHAEOLOGY FIRST: map the graduated Phase-14 resident market, CpuVendorizedOracle generalized share-vector oracle, 14.3 continuous allocation/pressure producer, 14.4 exact Q/T_s boundary, 14.5 parity corpus, 14.6 dispatch(None) causal self-consumption, Current->Next unresolved-demand recurrence, hard precedence bands, PALMA/Gu-Yang participation, and surviving clearer/disburser vocabulary", "formalize one candidate R_v:(S_v,Phi_v,{P_c,F_c,U_c,...})->(P_up,{x_v->c},U_v) with exact possession kept separate as T_s=Q(R_v.downward); no peer operator", "prove BOTH recursion identities: spatial x_{p->v}=S_v at continuous and exact strata, and temporal d_effective(v,N+1)=d_authored(v,N+1)+U(v,N) exactly once through Current->Next, never same-generation", "adjudicate the sufficient statistic BEFORE richer state: per-precedence-band (P,F) is a presumption only; prove or falsify it. A_v(lambda) is proof notation unless necessity is established; determine whether lambda is entirely implicit in normalized share", "prove Triad exactly-once: PALMA impedance and Gu-Yang serviceability are represented in born eligible state OR explicitly in the research objective, never both; identify exact causal location", "prove root/interior/leaf degeneracy: the same R under degenerate parameters; Some(rows)/None is only a binding distinction and an interior-only formalization is RED", "make the reference R EXECUTABLE ORACLE-SIDE over the generalized share-vector oracle and drive it BIT-FOR-BIT through the frozen 14.5 parity corpus; no approximate equivalence and no production reverse-fitting from workshop shapes", "rerun frozen 14.6 causal/self-consumption witnesses so executable R preserves produced AllocatedFlow, literal T_s self-consumption, generation pacing, physical-order/workgroup/partition/tree-realm invariance, and no host economic continuation", "identify real runtime variables versus proof-only variables exactly; if existing resident scalar/tuple state or per-band (P,F) is sufficient, explicitly conclude no A(lambda) runtime representation is needed", "produce a symbol-keyed deletion/rehome map of surviving clearer/disburser/feedback/adapter vocabulary for 15.1 preparation, but do not perform 15.1 rename/deletion unification", "if a composite Phase-15 R measurement is useful, derive it from existing recorded legs or a NEW Phase-15 artifact; never edit the frozen 14.1 comparator", "preserve Phase-14 canon and six canonical RF-market anchors; do not start 15.1, harness-fix work, compression horizon, or closeout", "run touched-package checks, focused 15.0 executable theorem/referee, frozen 14.5 parity and 14.6 causal suites, inventory/drift, doctrine-anchor validation, Agent Scan, hosted Doctrine Scan/Exec, then fresh exact-head /clearance and /relay-lint", "return PROBATION / proof-present / DA-review-pending with exact base/head, HD/ORIENT receipts, archaeology table, accepted R statement, real-vs-proof variable table, (P,F)/tuple/A(lambda) sufficiency verdict, recursion-axis proofs, Triad exactly-once proof, root/interior/leaf falsifier, executable oracle equivalence transcript, deletion/rehome map, changed-file census, structural certificate, and hosted workflow IDs"]
stop_conditions: ["the executable oracle-side R cannot be expressed without inventing new economic semantics or a second authority", "per-band (P,F) is falsified but the smallest counterexample/necessary statistic cannot be identified without runtime invention", "either spatial or temporal recursion identity cannot be proved on graduated production/oracle substrate", "root/interior/leaf require distinct economic operators rather than degenerate bindings", "Triad influence appears twice or requires a second PALMA/Gu-Yang solve", "bit-for-bit parity against the frozen 14.5 corpus or the frozen 14.6 causal/self-consumption witnesses becomes red", "a runtime A(lambda)/lambda/shadow-price/scarcity surface appears without formally proved necessity", "the zero-red structural certificate, doctrine anchors, inventory, Agent Scan, hosted checks, clearance, or relay lint become red"]
---
## BUILD
- Treat 15.0 as a theorem-and-executable-equivalence rung, not an implementation expansion. Begin from the already-running Phase-14 market and test whether its graduated state is already the minimal representation of one recursive resource filter.
- Candidate statement: `exact market = Q compose R`. R has upward subtree-response and downward edge-flow projections; Q is frozen. Spatial and temporal recursion are independently load-bearing and both must be executable facts.
- Per-band `(P,F)` is NOT granted as theorem. Attempt to prove it sufficient against the generalized oracle and parity corpus. If it fails, identify the smallest counterexample and STOP before creating richer runtime state. `A(lambda)` is mathematical notation only unless necessity is proven.
- The executable reference belongs on the CpuVendorizedOracle/reference side and must reproduce the graduated resident market bit-for-bit across the frozen 14.5 corpus while preserving 14.6 causal production facts.

## FENCES
- Phase 14 is immutable graduated substrate.
- One R, two projections, one Q, one T_s type, one Current->Next unresolved recurrence.
- Root/interior/leaf are degenerate bindings of the same operator.
- Triad influence appears exactly once.
- Hard precedence, continuous share, exact quantization, and generation pacing remain distinct authorities.
- No physical fusion and no new response storage absent demonstrated necessity.
- 15.1 is not in flight.

## EXIT-PROOF
- DA receives one executable formalization of R, a definitive sufficient-statistic verdict, exact proof-only/runtime quantity identification, both recursion-axis proofs, root/interior/leaf degeneracy falsification, Triad exactly-once accounting, and bit-for-bit oracle/resident equivalence over the frozen parity corpus with Phase-14 causal witnesses green.
- A result that the existing resident representation is sufficient and no A(lambda) runtime representation is needed is explicitly a PASS.
- Coding stops at PROBATION and does not merge, graduate, move the pointer, or begin 15.1.
