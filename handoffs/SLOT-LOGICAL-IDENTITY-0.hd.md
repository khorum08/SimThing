---
rung: SLOT-LOGICAL-IDENTITY-0
kind: rung
track: 0.0.8.7
base_sha: f39d36764d45dd466ee2ae73be5f1112d3848acd
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "DA ruling 5193918971 APPROVES 6.4 pre-dispatch with two bounded amendments and requires no further DA review after they land. Amendment 1: implementation PR binds downstream 9.2 RF-COLUMN-MINT-MIGRATE-0 to stable logical identity, explicitly forbidding persistent layout keying by physical row position or allocation order, and widens 9.2 exit-proof; bind any further certain consumer surfaced by FIRST STEP. Amendment 2: the physical-row-order planted defect lives in PRODUCTION order-bearing code, never a test-only alternate executor. Owner-directed ceremony ratchet: final PR body uses literal Rung: SLOT-LOGICAL-IDENTITY-0 and tested_code_sha: <exact final head> with current HD-RECEIPT; an otherwise-green return whose sole red is generated docs/orchestrator_orientation.md freshness is ADMIN-ONLY and must not trigger a semantic coding remand—report it for orchestration/GHA regeneration."
surfaces: ["crates/simthing-core/src/slot_index.rs", "crates/simthing-core/src/anchor_remap.rs", "crates/simthing-kernel/src/slot.rs", "crates/simthing-kernel/src/world_state.rs", "crates/simthing-sim/src/anchor_remap_encode.rs", "crates/simthing-sim/src/boundary.rs", "crates/simthing-driver/src/install.rs", "crates/simthing-driver/src/spec_session.rs", "crates/simthing-driver/src/spec_replay.rs", "crates/simthing-driver/src/arena_allocation_plan.rs", "crates/simthing-driver/src/field_sweep_compile.rs", "crates/simthing-gpu/src/field_sweep_instances.rs", "crates/simthing-core/tests", "crates/simthing-kernel/tests", "crates/simthing-sim/tests", "crates/simthing-driver/tests", "scripts/ci/stemthing_slot_census.tsv", "scripts/ci/test_inventory.tsv", "docs/tests", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md"]
forbidden: ["a second id-to-slot authority, second slot map, second remap/history log, sidecar epoch ledger, or any remap record beside AnchorLocusRemap", "per-access logical-to-physical indirection between epochs; epoch rebinding must be baked into boundary-uploaded artifacts", "physical row order becoming authored, semantic, replay, reduction, adjacency, or iteration order; any order-bearing path must remain logical/authored-key derived", "treating dense cell-space indices as matrix-row SlotIndex, or silently preserving the K3 cell-space conflation", "duplicating one object-row rebind once per anchored locus, or omitting objects with zero anchored property loci from the canonical remap history", "movement, residency-tier vocabulary/6.5, allocation-policy retirement, granting arena, placement mechanics, contention, Phase 7/8 work, or StemThing-B", "new slot semantics, new history authority, Invariant Set change, allowlist widening, scenario-specific witness, or weakening the landed Tier-2 logical/physical split"]
required_checks: ["fresh Frontier coding ORIENT-RECEIPT + current ANCHOR-ACKs + HD-RECEIPT before edits; resolve StemThing and slot-identity anchors rather than restating them", "FIRST STEP before implementation: prove the existing AnchorLocusRemap/AnchorRemapOperation history can encode every object-row epoch rebind exactly once, including objects with zero anchored property loci, without N-loci duplication; publish the mapping from object rebind to canonical history row", "run stemthing_slot_census_check.sh --check and preserve the sealed 51/51 accessor-consumer disposition, zero BLOCKER, zero ORDER-PIN; use census rows C1-id-slot-map, P1-anchor-remap-record, P1-spec-replay-slot-delta, and K3-cell-space-conflation as the minimum load-bearing worklist", "make SlotIndex the stable logical identity and keep physical row allocation/recycling behind the existing boundary binding table; epoch compaction rebuilds bindings only at the owning generation barrier", "extend AnchorRemapOperation with EpochCompaction on the existing AnchorLocusRemap door and existing endpoint-validation witness discipline; no second record or history path", "retype SpecDelta::ScriptedInstanceSlotChanged from raw u32 to the typed slot boundary and preserve replay-v3 identity resolution through the canonical remap chain", "name matrix-row and dense-cell index spaces distinctly by types or documented type aliases; prove dense-cell EML literals remain authored-coordinate identities, not physical matrix rows", "forced epoch remap: scramble physical rows while logical ids stay fixed; multi-generation CPU and GPU outputs remain bit-identical; plant the physical-row-order defect in a REAL PRODUCTION order-bearing path such as RF reduce-up or boundary upload, never in #[cfg(test)] or an alternate executor, and require it to RED", "pre-remap replay against post-remap state reproduces bit-exactly through the remap chain; zero per-access indirection between epochs and zero new history mechanisms are grep/tree-proven", "in the implementation PR, add BINDS 6.4 `SLOT-LOGICAL-IDENTITY-0` to 9.2 RF-COLUMN-MINT-MIGRATE-0: persistent column layout keys on stable logical identity and NEVER physical row position or allocation order; echo the binding in 9.2 exit-proof, and bind any additional certain consumer surfaced by FIRST STEP", "before return, PR body carries literal `Rung: SLOT-LOGICAL-IDENTITY-0`, `tested_code_sha: <exact final head>`, and current `HD-RECEIPT`; if the sole hosted red is generated orchestrator_orientation freshness, report ADMIN-ONLY and stop—do not spend a coding remediation turn on deterministic generated hygiene", "run focused slot/remap/replay/field/arena regressions plus census, residue, detachability, lifecycle, inventory, anchors/orientation, agent/doctrine scans, and hosted Doctrine Scan/Exec"]
stop_conditions: ["FIRST STEP fails: existing AnchorLocusRemap cannot represent every object-row rebind exactly once, including zero-anchor objects, without N-loci duplication; STOP and return the exact missing granularity/surface for DA amendment", "logical/physical split requires a second id-to-slot map, second remap log, persistent side table, or per-access indirection between epochs", "any production ordering or replay meaning depends on physical row order after the split, or a forced remap changes CPU/GPU results absent a planted defect", "dense cell-space and matrix-row identity cannot be separated without changing field semantics or authored-coordinate meaning", "replay cannot interpret pre-remap records against post-remap state using the single canonical remap chain", "completion requires 6.5 residency vocabulary, movement/contention, placement/allocation policy, new history authority, invariant change, or successor work"]
---
## BUILD
- Implement the ruled logical/physical split: `SlotIndex` is stable logical identity; physical rows are allocator/binding-table state rebuilt only at boundary epoch rebinds. Between epochs, uploaded plans/tables carry resolved physical bindings with zero per-access indirection.
- Extend the existing `AnchorLocusRemap` history with `AnchorRemapOperation::EpochCompaction`; represent each object-row rebind once in that one history, independent of anchored-locus count, and preserve existing endpoint validation.
- Retype the persisted scripted-instance slot delta to the typed slot boundary and make replay consume the canonical remap chain. Name dense cell-space separately from matrix-row identity so the K3 conflation is no longer expressible by silence.
## FENCES
- StemThing §3.1 shape (a) and the core §3 slot-identity law are fixed; this rung implements them, it does not choose a new identity model.
- One binding authority, one remap/history authority, no physical-row semantics, no per-access epoch indirection. Cell-space authored coordinates remain distinct from matrix placement.
- Scope ends at logical identity/rebinding. Residency vocabulary, granting/placement mechanics, movement, contention, and StemThing-B remain successor work.
## EXIT-PROOF
- FIRST STEP remap-granularity proof is green before implementation; zero-anchor and multi-anchor objects each yield exactly one canonical object-row epoch rebind.
- A forced physical-row scramble with unchanged logical ids is bit-identical across multi-generation CPU/GPU execution; the production physical-row-order mutant REDs; pre-remap replay reproduces against post-remap state through the single remap chain.
- Census remains 51/51 dispositioned with zero BLOCKER/ORDER-PIN; matrix-row vs cell-space distinction is explicit; grep/tree proof shows zero second history/map and zero per-access indirection between epochs; 9.2 carries and proves the 6.4 binding.
- Return PROBATION / proof-present / DA-review-pending with exact pushed/tested head and evidence. Coding does not `/clearance`, merge, move pointer, begin 6.5, movement/contention, or successor work.
