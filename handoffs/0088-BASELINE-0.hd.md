---
rung: 0088-BASELINE-0
kind: rung
track: 0.0.8.8
base_sha: af34aa7d471e68bd964d5e92a8946a779b89835c
audience: coding
model_tier: frontier
expected_route: ORCHESTRATOR-CLEARABLE(rehearsal-measurement)
owner_approved: true
owner_notes: "Owner dispatch order 2026-09-09: 'Dispatch 0.2 to Codex' under orchestration handoff 5594964935 and the 0.1 graduation relay 5595232123. The 0.0.8.8 track is substrate-subject law: the recursive StemThing RF Triad is the thing under test; this rung establishes the CURRENT measurement floor and discharges the inherited A1 FIRST-RUNG PRECONDITION. Measurement only — no optimization claim, no kernel change, no workload shaping. Cold-start law: bash scripts/ci/anchor_query.sh --domain rehearsal-0088."
surfaces: ["crates/simthing-workshop/src/rehearsal_perf_guyang_gather_0.rs","crates/simthing-workshop/src/rehearsal_perf_tr_composite_0.rs","crates/simthing-workshop/src/lib.rs","crates/simthing-workshop/tests/rehearsal_perf_guyang_gather_0.rs","crates/simthing-workshop/tests/rehearsal_perf_comparator_0.rs","crates/simthing-workshop/tests/rehearsal_perf_tr_composite_0.rs","docs/tests/rehearsal_perf_guyang_gather_0_results.md","docs/tests/rehearsal_perf_comparator_0_results.md","docs/tests/rehearsal_perf_tr_composite_0_results.md","docs/tests/rehearsal_perf_research_inventory_0_results.md","scripts/ci/closeout_artifacts.tsv","scripts/ci/test_inventory.tsv","scripts/ci/anchor_reach_log.tsv"]
forbidden: ["no engine/instrument-host src edits: crates/simthing-{core,kernel,gpu,sim,driver,spec,feeder,clausething,mapeditor}/src/** are OUT; a measurement that requires engine instrumentation is a STOP, not a patch","no optimization: no kernel/layout/scheduling/elision change, no candidate implementation, no performance claim beyond current-path characterization; 'measured constraint; optimization deferred' is the ONLY lawful optimization-adjacent statement","no workload shaping: no flattening, packing, capping, reordering, or scenario reshaping to flatter a number; adversarial/synthetic shapes are labeled synthetic","the archived A1 preserves under docs/workshop/archive/field_policy/** are READ-ONLY leased references: restore their comparison in a NEW contained rehearsal_perf_* harness; do not edit, move, delete, or resurrect the archived files as live code","the frozen Phase-14.1 comparator contract is byte-frozen: rerun it under its recorded contract; do not modify, rebase, or reinterpret it; current numbers are context, never a replacement","field-law fidelity is exact: canonical per-node fold order, conservative signed Gu-Yang flux, field-law parity between gather and tiled arms; no bespoke production shader, no forced dense LinkGraph, no arithmetic-order change","T_R is measurement only: timestamp existing seams; no global synchronization point, no new kernel counters, no summing overlapping intervals into a fictitious total; permit/barrier and N+1 execution measured separately","no E8 pin edits, no sealed-bundle changes, no qualification-mechanism edits of any kind","no gate/census/anchor/workflow edits: scripts/ci gate code, class tables, constitutional_surfaces.tsv, doctrine_anchors.tsv, anchor_triggers.tsv, .github/** are OUT (anchor_reach_log.tsv accrues only via lawful anchor_query use)","no Cargo.toml/Cargo.lock changes: dependency admission is D-scope; a needed dependency is a STOP","no reopening of market architecture, no second field solver, no CPU fallback route, no new economic or observation authority","lease renewals in closeout_artifacts.tsv only for rows this rung ACTUALLY consumes, fresh dates only, no immortality, no removal of the FIRST-RUNG PRECONDITION text from rows not fully discharged","no self-triage of scan findings; every scan-id-bearing INSPECT returns to orchestration"]
required_checks: ["render and read this handoff; obtain a coding ORIENT; run bash scripts/ci/anchor_query.sh --domain rehearsal-0088 and ACK every rendered REQUIRED-ANCHOR — the binding law for this rung is anchors rehearsal-0088-measurement (six status fields, packet binding set), rehearsal-0088-exposure-matrix, rehearsal-0088-charter (M1-M3 manifest rows), and rehearsal-0088-binding-laws (P6 measurement truth)","M1 RESTORE: build the Gu-Yang gather-vs-tiled comparison as a contained current harness under crates/simthing-workshop with the rehearsal_perf_ prefix, reproducing the archived A1 instrument's semantics faithfully against CURRENT head APIs (SlotIndex/ColumnIndex and StencilRefinementPrototype successors resolved to their landed forms); the comparison compares like-for-like admitted work with exact field-law parity between arms","M1 RUN: execute on the reference machine; the results packet binds code+instrument revisions, source/profile digests, seed, actual workload cardinalities, adapter/backend/driver/compiler tuple, release flags, cache state, warmup, repetitions, and the exact command; report medians, tail distributions, dispersion, memory, dispatches, bytes; paired samplewise residuals with negative residuals retained","M1 GATE: apply the inherited tiled-gather limits (<=1.25x median, <=1.5x worst) against the comparable retained reference; if the reference is no longer comparable, record the gate as EXPLICITLY UNRESOLVED with the exact reason and STOP short of re-adjudicating the reference yourself — a changed reference/envelope is a DA decision","M2: rerun the frozen Phase-14.1 comparator under its recorded byte-frozen contract on the same reference tuple; record current numbers as context in docs/tests/rehearsal_perf_comparator_0_results.md; any contract breakage is a STOP with the exact refusal","M3: define composite T_R for the current recursive filter — the enclosing resident causal interval from admitted generation intake through spatial settlement/consumption and next-generation demand preparation; measure permit/barrier and actual N+1 execution separately; report R, exact projection, spatial recursion, temporal preparation, launch, and egress as components; first capture recorded with the full binding set; per-tree intervals for independent trees","RESEARCH INVENTORY: enumerate every inherited research family (locality, materialization, settlement scale, E9a prepack, E9b validation, hot identities, elision, Current/Next carry, TreeVertical, multi-tree scheduling, inherited workshop pivots persistent_bench/multitarget_replay/transfer_contention/overlay_order/eml_phase5/weighted-mean, ClauseThing/Studio cost) in docs/tests/rehearsal_perf_research_inventory_0_results.md with per-family instrument validity, honest instrument-stale/unavailable rows, and the six status fields from workplan section 5.7 — a stale instrument is unresolved evidence, never a pass","REFERENCE TUPLE: record the exact adapter/driver/compiler/OS tuple of the reference machine in the M1 packet; this tuple is pinned for the track's later comparisons","LEASES: renew in scripts/ci/closeout_artifacts.tsv exactly the rows consumed (the A1 gather-vs-tiled instrument family and any pivot rows actually executed), fresh wall-clock dates, same PR","LEDGER: add test_inventory.tsv rows for every new test/results file; anchor acks recorded via lawful anchor_query use only","RUN: focused rehearsal_perf tests + touched-package (simthing-workshop) tests green locally; test-inventory drift check PASS; hosted Clearance + Doctrine Scan green on the PR head","RETURN: PROBATION / proof-present / clearance-pending / OPEN / UNMERGED on one final tested head with exact base/head SHAs, HD receipt, the four results packets, the lease-renewal diff, and every scan-id-bearing INSPECT listed for orchestration triage; no self-merge — the orchestrator completes the O* ritual (verify exit-proof, merge on green, stamp 'merged #N @ sha' in the ladder, advance pointer, regen orientation)"]
stop_conditions: ["faithful restoration of the A1 comparison is impossible without editing engine src, changing field law, or altering arithmetic order — return the exact incompatibility instead of approximating","the comparable retained reference for the tiled-gather gate is invalid or meaningless on current head — record UNRESOLVED with reasons; reference/envelope re-adjudication is DA scope","the frozen Phase-14.1 comparator cannot run under its byte-frozen contract on the reference tuple","T_R cannot be defined from existing seams without a global synchronization point or engine-src instrumentation","the reference machine's GPU adapter is unavailable or fails qualification for a required leg — record the typed refusal; skipped legs stay visible and cannot be papered over","any required check would need a forbidden surface (engine src, Cargo, gate files, archived preserves)","genuine novelty: any semantic gap, defect, or law question discovered in the substrate itself — measurement rungs report defects, they never fix semantics"]
---
## BUILD
Establish the 0.0.8.8 measurement floor. Three deliverables, all measurement-only, all under
the `rehearsal_perf_` prefix in `simthing-workshop` (the `rehearsal-measurement` O\* class):
**M1** — restore the stale A1 Gu-Yang gather-vs-tiled comparison as a contained CURRENT
harness (the archived preserves are read-only references; their 31 compile errors are
resolved by rebuilding against landed APIs, not by editing the archive) and discharge the
lease rows' FIRST-RUNG PRECONDITION with a real comparison packet under the inherited
≤1.25×/≤1.5× gate. **M2** — rerun the byte-frozen Phase-14.1 comparator for current context.
**M3** — define and first-capture the Phase-15 composite `T_R` (enclosing resident causal
interval; permit/barrier separate; components never summed into a fictitious total). Plus the
complete research-family inventory with honest instrument-validity rows. Charter law:
workplan §5.8.4 (manifest), §5.7 (packet binding set + six status fields), pillar P6.

## FENCES
- Measurement only: no optimization, no engine src, no workload shaping, no dependency
  changes, no gate/census/anchor edits, no E8 or sealed-bundle contact.
- Exact field-law parity between comparison arms; the frozen comparator stays byte-frozen;
  a changed tiled-gather reference is recorded UNRESOLVED, never re-adjudicated coder-side.
- Leases renew only for rows actually consumed, fresh wall-clock dates, same PR.
- A stale or unavailable instrument is unresolved evidence — recorded, never greened.

## EXIT-PROOF
Four committed results packets (`rehearsal_perf_guyang_gather_0`, `_comparator_0`,
`_tr_composite_0`, `_research_inventory_0`) each carrying the full §5.7 binding set on the
recorded reference tuple; the gather-vs-tiled gate applied or explicitly unresolved; the
FIRST-RUNG PRECONDITION discharged on the renewed A1 lease rows; the research inventory
covering every inherited family with six status fields and honest stale rows; no optimization
claim anywhere; workshop tests + drift check + hosted checks green; one PROBATION head
returned for exact-head orchestration verification and O\* graduation through the
`rehearsal-measurement` class.
