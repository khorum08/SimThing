---
rung: 0088-INGRESS-FIDELITY-0
kind: rung
track: 0.0.8.8
base_sha: c691ed215745f135f341100e008756437ced6cb3
audience: coding
model_tier: frontier
expected_route: ORCHESTRATOR-CLEARABLE(rehearsal-ingress-mapping)
owner_approved: true
owner_notes: "Owner lane selection 2026-09-10 (Board 5611740012): Astra / Frontier holds rung 1.1 on HD-receipt ACK. Owner standing directive: MINIMIZE DA RELAYS — ordinary leaf implementation PRs graduate O* through rehearsal-ingress-mapping via Orchestration ONLY; the DA is reached exclusively by a genuine reserve trigger or a census-transition-ready signal. This is a MULTI-PR rung under ONE admitted contract (this HD): split into bounded PRs freely, never mint new law at a split. B1 authority envelope already landed (#2028 @ c691ed21). Lifecycle law (Board 5610889723): NO proof is ever permanent; renew a penned referee ONLY at first true consumption. Cold-start law: bash scripts/ci/anchor_query.sh --domain rehearsal-0088."
surfaces: ["crates/simthing-clausething/src/**","crates/simthing-clausething/tests/**","crates/simthing-mapeditor/src/clause_scenario_ingest.rs","crates/simthing-mapeditor/src/clause_scenario_picker.rs","crates/simthing-mapeditor/src/studio_live_session_bridge.rs","crates/simthing-mapeditor/src/hydration.rs","crates/simthing-mapeditor/src/generation.rs","crates/simthing-mapeditor/tests/studio_live_session_bridge_0.rs","crates/simthing-mapeditor/tests/rehearsal_ingress_*.rs","scenarios/**","docs/tests/rehearsal_ingress_*_results.md","scripts/ci/test_inventory.tsv","scripts/ci/test_lifecycle_parked.tsv","scripts/ci/inspect_justifications.tsv","scripts/ci/triage_log.tsv","scripts/ci/anchor_reach_log.tsv","docs/tests/current_evidence_index.md"]
forbidden: ["no engine src: crates/simthing-{core,kernel,gpu,driver,sim,spec,feeder}/src/** are OUT; a convergence that requires spec/admission vocabulary change, a new lowering authority, or any kernel/driver edit is a STOP (genuine novelty), never a workaround","no protected surfaces: constitutional_surfaces.tsv, doctrine_anchors.tsv, anchor_triggers.tsv, class tables, clearance/gate scripts, gate_wiring_paths.txt, allow/**, .github/workflows/**, Cargo.toml, Cargo.lock — census-transition edits are DA increments; when a transition is READY (converged route proven, dead door deletable), SIGNAL Orchestration for the DA increment instead of touching the census","no second IR, registry, compiler framework, planner, or alternate scenario authority; converge INTO the canonical routes (parse/expand/hydrate front end -> admitted spec -> ordinary resident SimSession); reuse the existing raw model, hydrated types, admission vocabulary","the three-candidate profile substitution and the legacy hydrate-root branch are DELETED at convergence, never annotated, bypassed, feature-flagged, or left as inert scaffolding; removing the substitution alone does NOT close this rung","historical stage-only and negative ClauseThing fixtures retain their historical contracts as RECORDS; every LIVE executable path converges to canonical StemThing semantics; do not relabel stage-only fixtures fully executable and do not demand authors rewrite working files","preserve-as-compat surfaces (hydrator publics, session source branches, mapgen modes/output forms/strategies) conform THROUGH canonical semantics in place — they never shelter an alternate execution authority","the blocked literal-install ingress is pre-ruled DELETED and the runtime vertical seed pre-ruled REMOVED, but BOTH deletions ride their natural convergence points WITH their DA census increments; do not delete ct_1a's hydrate-parity witness before the rehearsal witness supersedes it at equal-or-greater strength (frozen-referee law)","failed load/admission leaves the prior valid document/session intact BEFORE any economic effect; after economic touch, graduated fail-stop law holds — no rollback or retry promises","unsupported executable declarations refuse BEFORE activation with source provenance (law/element/file/span); a modifier or recipe that parses but vanishes during lowering is a product failure, not a compatibility feature","pen renewals ONLY for referees a PR truly consumes: move the row back from test_lifecycle_parked.tsv to test_inventory.tsv with a structured 'downstream-utility: <named 1.1 consumer>' note and dsu_survivals incremented; no bulk renewals, no silent extension, no renewal without consumption","no optimization claims, no workload shaping, no E8/sealed-bundle contact, no numerical-law or field-law change","no self-triage of scan findings; every scan-id-bearing INSPECT returns to Orchestration"]
required_checks: ["render and read this HD; obtain a coding ORIENT; run bash scripts/ci/anchor_query.sh --domain rehearsal-0088 and ACK every rendered REQUIRED-ANCHOR BEFORE implementing — the binding law is anchors rehearsal-0088-legacy-census (worklist + batching), rehearsal-0088-acceptances (the discriminating witness), rehearsal-0088-charter (interchange ruling 5.8.2 + gap G4), rehearsal-0088-mission and rehearsal-0088-binding-laws","FALSIFIER FIRST (F1): preserve a semantic RED commit demonstrating the live defect — author a source with two owners, more than three positive participants, unequal depth, a zero-valued PRESENT participant, and two distinct authored policies; show the current bridge/projection installs a SUBSTITUTED economy (three-candidate profile, dropped unselected children/policies) diverging from authored intent","CONVERGE: canonical live-profile composition carries the FULL authored economics to the installed session — properties, programs, topology, owner relations, observation bindings each owned by an existing admitted representation; the legacy hydrate-root branch and the substitution are gone from every live path","BOTH-ROUTES WITNESS (GREEN): the same authored source reaches identical installed semantics through native .clause loading AND the supported canonical-JSON interchange (interchange per charter 5.8.2: generated JSON is reproducible cache, the Studio path adapter consumes the admitted loader); changing an UNSELECTED child's contribution and the SECOND owner's policy each land in installed economics and born outputs","TOPOLOGY/DEPENDENCY (G4): the projection's embedded-lattice rebind requirement becomes an explicit declared dependency of the source bundle or is converged away; a portable bundle opens from relative paths + content identities with no machine-specific resolver token; changing process working directory must not change source resolution","REFUSAL PROVENANCE: at least two unsupported-executable-declaration cases refuse before activation naming law and source element; stage-only and negative historical fixtures still pass AT THEIR STAGE as records","SESSION SAFETY: failed load/admission before effects leaves the prior valid session intact — witnessed, not asserted","witnesses live as rehearsal_ingress_*-prefixed owning tests + docs/tests/rehearsal_ingress_*_results.md packets (the class trigger convention); specimen sources may live under scenarios/**","PER PR: at least one rehearsal_ trigger file; pr body carries tested_code_sha, coverage_basis, ci_green, novelty_claim NO (or the reserve reason); test_inventory rows for every new test; pen renewals per the lifecycle law for referees actually consumed; touched-package tests green locally; hosted Clearance + Doctrine Scan green","CENSUS-READY SIGNALS: when the canonical route fully covers a worklist surface (projection adapters internalized; Studio hydration/presets converged; canonical-JSON on the admitted loader; literal-install deletable; vertical seed deletable), post the readiness with evidence to Orchestration for the DA census increment — do not wait on it to continue other leaves","RETURN per leaf PR: PROBATION / proof-present / clearance-pending / OPEN / UNMERGED to ORCHESTRATION with exact base/head, HD receipt, packet paths, and every scan-id INSPECT listed; Orchestration completes the O* ritual; the DA is contacted ONLY on a genuine reserve or census-ready signal (Owner MINIMIZE-DA-RELAYS directive)"]
stop_conditions: ["convergence requires a spec/admission vocabulary change, a new lowering authority, or any engine-src edit — return the exact gap as genuine novelty (a missing generic capability gets its own admitted D rung, landed with its first consumer)","the full authored-economics composition cannot be expressed through existing admitted representations without a second IR or a parallel scenario authority","the declared-dependency bundle contract (G4) cannot be implemented without spec-side changes","the F1 RED cannot be reproduced from a clean committed checkout, or the GREEN would require weakening a historical stage/negative contract instead of preserving it as a record","a preserve-as-compat surface cannot conform through canonical semantics without becoming an alternate authority","any required check needs a forbidden surface (census, gate files, Cargo, engine src)","a frozen referee (ct_1a hydrate-parity included) would be weakened or deleted before its rehearsal successor supersedes it at equal-or-greater strength"]
---
## BUILD
Full paradigm convergence of the ingress vertical — the 0.0.8.8 mission's Clause B executed
at its widest point. Every live route from authored source to installed resident session
(ClauseThing lowering, scenario projection/rebind, Studio bridge live-profile composition,
Studio hydration/generation, canonical-JSON adapter) converges to canonical StemThing
semantics or is retired through its pre-authorized census transition. The confirmed F1
defect is the anchor: `compose_recursive_rf_profile`'s three-candidate substitution and the
legacy hydrate-root branch install a Studio-shaped economy instead of the authored one.
Falsifier first, then one canonical composition, then the both-routes witness. Contract law:
workplan §5.2 (worklist, B1 batching), §5.8.2 (interchange/topology ruling), §5.6 (the
discriminating witness), pillar P2. This is ONE contract delivered across multiple bounded
PRs — leaf implementation graduates O\* through `rehearsal-ingress-mapping` (envelope
already covers the bridge/hydration/generation, #2028); census transitions are DA
increments triggered by your readiness signals.

## FENCES
- Converge INTO canonical routes; no second IR/registry/authority; engine src untouchable.
- Substitution and hydrate-root branch are deleted, not disabled; stage/negative fixtures
  stay lawful as records; preserve-as-compat conforms in place.
- Census/protected surfaces are DA-only; signal readiness, keep moving.
- Pen renewals only at true consumption, structured DSU note, dsu_survivals bumped.
- Refusal-before-activation with provenance; prior session intact pre-effect; fail-stop
  after economic touch.

## EXIT-PROOF
Preserved F1 semantic RED (the substituted economy) and GREEN (full authored economics
installed) on the §5.6 specimen shape; identical installed semantics through native and
interchange routes with unselected-child and second-owner-policy sensitivity; declared
dependency bundle resolving portably (G4); two refusal-with-provenance cases; session-safety
witness; historical stage contracts intact as records; every touched worklist surface either
converged with evidence or signaled census-ready; rehearsal_ingress_* tests + results
packets committed with inventory rows and lawful pen renewals; each leaf PR returned
PROBATION to Orchestration and graduated O\* under `rehearsal-ingress-mapping`; the rung's
own D increments (census transitions + final stamp) land DA-side on your evidence.
