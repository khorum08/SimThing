---
rung: EML-EXP-PRIMITIVE-0
kind: rung
track: 0.0.8.7
base_sha: 31c3e7d3
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "Owner direction 2026-08-04: close EML unification before StemThing-A implementation. 5.10 EML-PRIMITIVE-DOMAIN-0 is already DA-GRADUATED (#1618 @ 35dcf43a); full_eml_unification §10 now serializes 5.11 -> 5.12 -> Owner flip to 6.4. This handoff is projected from exact master 31c3e7d3 after the final StemThing review cleanup. 5.11 admits exactly one primitive: full-domain EXP. CLOSED_OPCODES widening and certification/freshness wiring are DA-reserve surfaces at landing, but no pre-dispatch DA ceremony is required absent a new design defect. Exhaustive qualification is a LOCAL phase-boundary act with pinned artifacts; standing CI may verify only presence/freshness and must never re-run the 2^32 sweep."
surfaces: ["crates/simthing-core/src/eml_nodes.rs", "crates/simthing-core/src/eml_registry.rs", "crates/simthing-kernel/src/eml_opcode_gate.rs", "crates/simthing-kernel/src/eml_resource_class.rs", "crates/simthing-kernel/src/accumulator_op/cpu_oracle.rs", "crates/simthing-kernel/src/accumulator_op/eml_program_table.rs", "crates/simthing-kernel/src/shaders", "crates/simthing-driver/src", "crates/simthing-driver/tests", "crates/simthing-workshop/tests", "docs/eml_gadget_library.md", "docs/full_eml_unification.md", "docs/tests", "scripts/ci", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md"]
forbidden: ["vendor-native exp/log, f64, DIV inside the primitive, implementation-defined transcendental semantics, CPU/interpreted-GPU/JIT sequences that differ from the pinned algorithm-as-spec, or unstabilized gadget forms such as naive EXP(beta*z_i) softmax", "a second primitive-admission door, second determinism/cost/consumer gate, new transcendental resource weight/class, weakened 5.10 admission distinction, or implicit primitive-side clamping that converts guarded semantics into hidden validity repair", "weakening the exact-primitive cost key, comparing against a toy baseline instead of the primitive's own pinned ~20-node ADD/MUL/FLOOR gadget encoding, or silently shrinking the certified toolchain set to make evidence green", "running the exhaustive 2^32 qualification in standing CI; CI may inspect pinned artifact presence/freshness only", "a bespoke EXP shader/kernel, semantic/scenario opcode, algebra-kind branch, second EML execution path, scenario/corpus witness, or FIELD-SWEEP-SINGLE-PATH weakening", "LN, POW, eml(x,y), LogAccumulate, entropy, StemThing-A implementation, movement/contention work, or any 5.12+ successor work", "mutating EXP semantics after admission under the same primitive name; a different semantics is a future new primitive name and is outside this rung"]
required_checks: ["fresh coding ORIENT-RECEIPT + current ANCHOR-ACKs + HD-RECEIPT before edits; prove the exact base admits zero exact primitives and the candidate admits exactly one named EXP, with CLOSED_OPCODES growing only by EXP", "bind EXP to one finite PrimitiveDomain covering canonical [-87.33,+88.72] endpoint bits; every production call is range-certified or explicitly guarded semantics through 5.10, while unguarded/uncertified calls remain spanned admission errors", "pin one f32-only algorithm-as-spec using add/sub/mul plus integer/bit ops as needed; CPU twin, interpreted GPU arm, and SSA-JIT lowering execute that same sequence with no vendor transcendental, f64, or div", "characterize the pinned sequence independently against a higher-precision mathematical exp reference across boundaries plus deterministic stratified/random samples; publish observed error/ULP envelope and monotonic/positive-finite sanity without claiming correctly-rounded semantics", "run the exhaustive 2^32 qualification LOCALLY for every currently certified compiler/backend/driver tuple; pin exact reference/replay digests plus trust-chain/algorithm identity, and wire freshness so algorithm/opcode/compiler/backend/driver drift invalidates the artifact while CI only checks presence/freshness", "exercise ExactPrimitiveCostEvidence against EXP's own pinned ordinary-EML gadget baseline (~20 ADD/MUL/FLOOR nodes): no compiled-resource regression and at least one strict improvement under the existing cost key, with no new cost dimension", "land/exercise the named authored-data consumers and stabilized EXP-only forms through existing EML surfaces: sign-stable Logistic CostBand steering, exponential STEAD falloff, and SoftmaxWeight as EXP(beta*(z_i-max z)); naive unstabilized softmax is not the canonical form", "plant a fusion/reassociation defect in the primitive or generated lowering and show the digest referee turns RED; retain any source anti-reassociation aid only if measured necessary and free", "run focused primitive/admission/JIT/interpreter/consumer batteries plus inherited EML resource-class and field-sweep parity referees; update inventory/results/evidence truthfully and regenerate orientation if ladder status changes"]
stop_conditions: ["any supported certified toolchain cannot match the pinned CPU semantics bit-for-bit, or the implementation requires f64/div/vendor transcendental/bespoke kernel/second EML mechanism; do not weaken determinism or silently drop the toolchain", "the candidate cannot beat its own gadget encoding under the landed exact-primitive cost key without weakening the gate or changing the baseline", "independent numerical characterization shows the sequence is grossly non-exponential, non-monotone, non-positive-finite inside the domain, or unsuitable for the named consumers; return the measured envelope for design ruling instead of admitting a misleading EXP", "CostBand steering or STEAD falloff requires changing their mechanisms rather than supplying authored EML data through existing doors", "completion requires LN/POW/eml/LogAccumulate/entropy, StemThing-A, movement/contention, invariant change, or gate loosening", "certification freshness cannot bind to compiler/backend/driver/algorithm trust-chain drift without standing exhaustive CI execution; return the exact gap instead of weakening the local-certification law"]
---
## BUILD
- Admit exactly one generic primitive, full-domain `EXP`, through the landed 5.10 `ExactPrimitiveAdmissionDoor`. Pin the algorithm-as-spec and add only the required closed-opcode, CPU-twin, interpreter, and SSA-JIT lowering surfaces.
- Keep semantics reproducible rather than pretending correct rounding: the pinned sequence is the bit law, but characterize it independently as an exponential so deterministic nonsense cannot pass merely by agreeing with itself.
- Produce the local exhaustive-certification artifacts and trust-chain freshness wiring; standing CI watches those artifacts but never re-executes the 2^32 qualification.
- Exercise the two named consumers as authored EML data: sign-stable CostBand steering and exponential STEAD falloff. Add the EXP-only stabilized gadget forms; leave LN-derived library work to 5.12.
## FENCES
- One primitive, one admission door, one interpreter/JIT vocabulary, one certified bit semantics. `CLOSED_OPCODES` widens only for `EXP`; no semantic opcode, bespoke kernel, second cost model, or second proof system.
- Domain admission remains exactly the 5.10 split: range certificate OR explicitly guarded semantics. Guarding changes authored meaning; it never proves the unguarded source was valid.
- Exhaustive qualification is local and phase-boundary; CI is freshness-only. Cost is judged by the existing exact-primitive resource-effect key against the real gadget baseline, unweakened.
- `EXP` semantics are append-only once admitted. No `LN`, `POW`, `eml()`, log-domain accumulation, StemThing, movement, or contention work rides this landing.
## EXIT-PROOF
- Exactly one `EXP` primitive is admitted over the full finite domain; CPU, interpreted GPU, and JIT are bit-identical to the pinned sequence, and a planted fusion/reassociation mutant reds the digest.
- Every certified toolchain has a pinned exhaustive 2^32 digest/replay artifact whose trust-chain freshness is mechanically watched without CI re-execution; independent numerical characterization records the approximation envelope without claiming correct rounding.
- The existing cost key proves no regression plus at least one strict compiled-resource improvement versus the pinned gadget encoding. Stabilized Logistic/Softmax and the CostBand/STEAD consumers execute through existing EML surfaces with no bespoke path.
- Return PROBATION / proof-present for orchestration review. Coder does not `/clearance`, merge, move the pointer, implement 5.12, or start StemThing-A.
