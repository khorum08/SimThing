---
rung: RESIDENT-SESSION-INTEGRATION-CONFORMANCE-0
kind: rung
track: 0.0.8.7
base_sha: 89bb6ef0c33135fa7941d5f0eb22afe705ce182d
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(binding)
owner_approved: true
owner_notes: "Owner ruling + DA mint 5548825933 accepted the engineering cross-rung remand, reopened the pointer to 15.8, and fixed cap-collision law for later 15.9. Binding scope for this STOP rung is the exact 15.8 ladder row, DA adjudication 5548783236, and truth-amended SimThing Unification v3 sec 8. The graduated 15.5-15.7 scoped results stand. 15.9 and 15.10 remain fenced."
surfaces: ["crates/simthing-driver/src/**", "crates/simthing-core/src/**", "crates/simthing-spec/src/**", "crates/simthing-gpu/src/**", "crates/simthing-kernel/src/**", "crates/simthing-sim/src/**", "crates/clausething/src/**", "crates/simthing-workshop/tests/**", "docs/tests/**", "docs/sanctioned_surface.md", "scripts/ci/allow/kernel_surface.txt", "scripts/ci/constitutional_surfaces.tsv", "scripts/ci/test_inventory.tsv", "scripts/ci/triage_log.tsv", "scripts/ci/anchor_reach_log.tsv", "scripts/ci/authorized_renames.tsv"]
forbidden: ["using a direct ResidentClearingRuntime workshop harness as the canonical 15.8 referee", "reconstructing or materializing U on the host to author temporal continuation", "forcing d_authored(N+1) to exist early merely to preserve a helper or test shape", "passing empty persistence deformation bindings through the ordinary session when authored persistence is present", "preferred_arena None selecting physical arena index 0 or any registry-order-dependent economic binding", "adding a new market, second persistence path, host continuation authority, score/router system, peer economic authority, second exact projection, or alternate resident executor", "changing graduated 15.5 recursion semantics, 15.6 real-arena homing/E5 exact-basis law/E7 rebind, 15.7 lease-permit law, E6 work-conserving commitment law, Q149 mechanics, exact residue law, or consequence-only law", "implementing the 15.9 cap-collision remedy, 15.10 abort safety remedy, canon rewrite/adoption, sec-8.4 marker deletion, compression, pointer movement, graduation, closeout, or fresh engineering re-review", "any coding edit to .github/workflows/**, scripts/ci/**/*.sh, or scripts/ci/**/*.py"]
required_checks: ["render/read this handoff first; carry machine HD-RECEIPT plus fresh coding ORIENT under the current rule stamp and ACK every rendered REQUIRED-ANCHOR before implementation", "ARCHAEOLOGY FIRST: map the ordinary SimSession path from authored generation-N demand through resident admission, market qualification, TreeGenerationPermit(N), exact T_s append, spatial consumption, temporal preparation, the N->N+1 boundary, authored N+1 demand creation/admission, persistence deformation admission, TreeGenerationPermit(N+1), and actual N+1 allocation; separately map market->RF-arena selection and prove every physical-order dependency", "AUTHORITATIVENESS ADJUDICATION FIRST: mechanically identify the existing ordinary-session source of d_authored(N+1), the exact moment it becomes authoritative, and which persisted/resident state is available then. If the source is authoritative only once Current N+1 exists, the bound law is U(N) remains resident and the once-mint d_effective(N+1)=d_authored(N+1)+f(U(N)) occurs at N+1 before allocation. If source archaeology instead proves an earlier authoritative datum, consume that exact authority without host U reconstruction. If timing is ambiguous, contradictory, or would require inventing a new authoring authority, STOP and return an Owner/DA adjudication packet before semantic implementation", "kill the ordinary-session deformation omission: authored persistence admission must reach ResidentClearingRuntime::admit_sealed_market_with_persistence_deformations through the real SimSession path; the current unconditional empty deformation slice is not lawful when authored deformation exists; absent authored deformation remains exact identity", "wire ordinary-session temporal continuation through the already-graduated resident temporal doors; no new temporal market or host recurrence authority. N+1 economics may execute only under TreeGenerationPermit(N+1), and temporal preparation/once-mint must preserve prepared-vs-executes law", "CANONICAL REFEREE = ACTUAL SimSession: generation N ordinary authored demand 10 clears to T_s(G4,U6) on the ordinary real RF arena; without host U reconstruction, ordinary authored N+1 demand 2 yields effective demand 8 under identity deformation and effective demand 5 under f(U)=U/2; both cases cross real N and N+1 permit boundaries and execute actual N+1 RF allocation", "prove no host U reconstruction/materialization participates in the temporal economic path: U remains resident from canonical T_s/live-head state until the lawful once-mint consumer; host materialize/readback may remain observation/proof only after economics", "ARENA RESOLUTION LAW: an authored market must resolve to exactly one semantically bound RF arena or admission typed-refuses. preferred_arena=None may not mean arena[0] in production. Falsifier: construct semantically equivalent registries with arenas ordered A,B versus B,A; the same authored market must produce identical semantic binding/results in both orders, or both must typed-refuse. A physical list position may never select economic meaning", "arena repair must use existing semantic identities/dictionaries/topology evidence; do not add a compatibility score, default-first fallback, host selector policy, or second admission authority", "rerun frozen 15.5 cross-product and 15.6/15.7 focused witnesses unchanged in addition to the new actual-SimSession cross-product; preserve E5 three-way exact-u32 production witness, E6 zero-basis work-conserving law, E7 identity-preserving topology rebind, lease/permit refusal matrix, qualification mutation matrix, and runtime-provenance zero-use proof", "prove authority census does not grow and all ordinary temporal continuation routes through the single existing resident runtime, one exact projection, one integration schedule/live head, one execution lease, and one whole-generation permit lifecycle", "run focused 15.8 referee, touched-package checks, full structural battery, inventory/drift, constitutional/census selftests, lifecycle/sanctioned-surface/anchor checks, detachability, Agent Scan, hosted Doctrine Scan/Exec, then fresh exact-head /clearance and /relay-lint", "return PROBATION / proof-present / DA-review-pending / UNMERGED with exact base/head, HD/ORIENT receipts, full ordinary-session archaeology, authored-demand authoritativeness table and adjudication outcome, actual persistence-admission call graph, actual-SimSession identity/half-deformation transcripts, permit-boundary transcript, host-U zero-use proof, arena semantic-resolution table and A,B/B,A falsifier transcript, unchanged authority census, frozen 15.5-15.7 reruns, changed-file census, FULL ZERO-RED certificate, hosted IDs, clearance and relay-lint"]
stop_conditions: ["ordinary SimSession cannot compose the graduated 15.5 temporal law, 15.6 real arena, and 15.7 permit lifecycle without a new peer authority or host U reconstruction", "d_authored(N+1) authoritativeness cannot be mechanically established from the existing ordinary authoring/session lifecycle, or implementation would need to invent/force future demand early", "authored persistence cannot reach the ordinary resident session without adding a second persistence path or changing the graduated once-mint semantics", "an authored market cannot resolve to one semantic RF arena independent of registry order without adding a new score/router/default-first authority", "the actual-SimSession 10->(G4,U6)->2->8/5 witness cannot execute through real N/N+1 permits and actual N+1 RF state", "any required frozen 15.5-15.7 witness, authority census, structural certificate, doctrine/anchor/inventory check, hosted gate, clearance, or relay lint is red"]
---

## BUILD

This STOP rung closes the cross-rung witness-substitution hole discovered after 15.7. The graduated rungs remain valid in their own scopes: 15.5 proved the spatial/temporal semantics, 15.6 homed them on the real RF arena, and 15.7 carried execution authority for the session lifetime. 15.8 must prove that one ordinary `SimSession` actually composes all three into temporal continuation.

The canonical referee is therefore the ordinary session itself. It must start from ordinary authored generation-N demand 10, produce canonical `T_s(G4,U6)`, preserve U resident without host reconstruction, combine that U exactly once with ordinary authored N+1 demand 2 through the admitted persistence port, and execute N+1 under the real N+1 permit. Identity deformation yields demand 8; `f(U)=U/2` yields demand 5. Direct runtime harnesses remain useful frozen witnesses but cannot discharge this rung.

Authoritativeness is discovered, not invented. Coding must first identify when ordinary `d_authored(N+1)` becomes authoritative. If it exists only once Current N+1 exists, keep U resident and perform the once-mint at N+1 before allocation. Never make future authored demand exist early merely to fit a helper API. Any ambiguity or need for a new authoring authority is an Owner/DA STOP.

Arena choice is semantic authority. An authored market either names/resolves one semantic RF arena through existing admitted identities or typed-refuses. Physical registry order is never economic meaning; `None -> arena[0]` is forbidden in production.

## FENCES

- one ordinary SimSession, one resident runtime/operator, one exact projection, one integration schedule/live head, one execution lease.
- one whole-generation permit for N and a distinct lawful N+1 permit; no per-edge or helper permit.
- U remains resident through the temporal handoff; no host reconstruction or host-authored recurrence.
- authored persistence uses the existing 15.2 admitted deformation port inside the existing once-mint.
- market-to-arena binding is semantic and order-independent or typed-refused.
- 15.9 cap projection, 15.10 abort safety, canon/adoption, compression, pointer movement, graduation, engineering re-review, and closeout are fenced.
- coding does not merge or infer the next rung.

## EXIT-PROOF

An actual ordinary `SimSession` executes generation-N demand 10 -> `T_s(G4,U6)` -> ordinary authored N+1 demand 2 -> effective demand 8 under identity and 5 under half-deformation, with no host U reconstruction, real N/N+1 permit boundaries, actual authored persistence admission, and real N+1 RF state. The authoritative timing of authored N+1 demand is mechanically established and recorded. Authored market arena binding is independent of registry order or typed-refuses symmetrically. All frozen 15.5-15.7 witnesses and authority censuses remain ZERO-RED. Coding stops unmerged for DA review.
