---
rung: MOVEMENT-DECISION-INGRESS-0
kind: rung
track: 0.0.8.7
base_sha: 6a70cb203d876e959f77afd5d656501c739e1870
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "Owner/DA progression 5198501412 directs Orchestration to author and self-clear the 7.1 handoff, then relay for pre-dispatch review. 6.5 graduated in DA ruling 5197745740; StemThing-A is complete. #1661 @ 6a70cb20 lands the Owner-committed ActionBand adoption plan: 7.1 remains a movement rung and canonical spatial witness, destination authority stays field/gradient-derived, and no production ActionBand type/planner/registry may be minted. Standing amendment: every planted defect must mutate the real production path it guards, never a #[cfg(test)] twin, alternate executor, or fixture authored to be broken."
surfaces: ["crates/simthing-core/src/owner_channel.rs", "crates/simthing-core/src/overlay.rs", "crates/simthing-core/src/residency_tier.rs", "crates/simthing-driver/src", "crates/simthing-sim/src", "crates/simthing-driver/tests", "crates/simthing-sim/tests", "docs/tests", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md"]
forbidden: ["CPU planner or CPU branching on raw field/heatmap values; movement decisions must enter only through sealed threshold/band-crossing events", "physical row relocation, compaction, placement/extents, allocator/grant machinery, or any StemThing-B mechanism", "per-participant ownership stamping, movement-side owner tables, materialized effective-owner copies, or any ownership path other than exactly one bind_owner at the moved subtree root", "permanent InTransit state, missing/synthesized overlay origin, lifecycle without UntilDissolved arrival semantics, or a movement-only overlay authority", "bespoke movement-cost accounting or direct resource decrement; consuming moves must use the existing CostBand hinge and free repositioning remains observation", "privileged Destination/path/predecessor object, path planner, hard-coded destination identity, generic ActionBand type/enum/registry/opcode/planner/match arm, or any 7.1a implementation", "7.2, 8.1/8.2 execution, contention clearing implementation, ActionBand determination, Vector CostBand, or successor-rung work", "new service/registry authority, new semantic runtime taxonomy, SimThingKind branching, allowlist widening, or weakening the landed StemThing-A identity/residency laws"]
required_checks: ["fresh Frontier coding ORIENT-RECEIPT + current ANCHOR-ACKs + HD-RECEIPT before edits; movement-front, field-policy, overlays, ownership, CostBand, StemThing-A, and ActionBand-adoption anchors must be read rather than paraphrased from the handoff", "consume movement decisions only from the existing sealed Phase-5 threshold/band-crossing emission surface; plant the raw-field/CPU-planner defect in the PRODUCTION movement decision-ingress path and require it to RED", "perform spatial movement as parent/column and arena-membership change only, with zero physical row relocation; plant the row-relocation/physical-slot-order defect in the PRODUCTION movement commit/reparent path and require it to RED while logical SlotIndex identity remains stable", "make the ownership flip exactly one simthing_core::owner_channel::bind_owner at the moved subtree root; plant the per-participant stamp or movement-side owner-table defect in that PRODUCTION movement commit path and require it to RED; preserve authored unowned rejection and legacy_owner_properties_remaining == 0", "construct InTransit and movement outcomes through the PRODUCTION overlay constructor/commit path with a real deciding-cell origin, slot-resolved at the barrier, and UntilDissolved arrival condition; plant missing/synthesized origin and permanent-lifecycle defects there and require them to RED", "route every resource-consuming move through the PRODUCTION CostBand crossing/action path; plant a direct/bespoke movement-cost bypass there and require it to RED; prove a free repositioning consumes nothing", "keep destination authority field/gradient-derived over admitted topology; waypoint/operator direction may only deform or weight potential via ordinary overlays; plant a hard-coded destination/path identity defect in the PRODUCTION destination-selection/weighting path and require it to RED without editing any destination identity", "preserve the ActionBand fence: 7.1 is the landed spatial witness only; no production ActionBand object or generic action abstraction may appear, and structural-comparison work belongs to sequential 7.1a after 7.1 graduation", "update one results doc + one evidence-index line + only the 7.1 status row to PROBATION; regenerate orientation deterministically and keep 7.1a/7.2/8.x TODO", "run focused movement ingress/reparent/owner/overlay/CostBand/gradient witnesses plus core/driver/sim suites as touched, agent/doctrine scans, anchor/orientation checks, exit-proof-coverage, and hosted Doctrine Scan/Exec"]
stop_conditions: ["movement requires reading raw fields on CPU or inventing a CPU planner instead of consuming sealed crossing events", "movement cannot be expressed as column/membership reparenting without physical row relocation or placement mechanics", "ownership transfer requires descendant stamping, a second owner authority, or any materialized effective-owner copy", "InTransit cannot be represented as an ordinary originated UntilDissolved overlay with arrival dissolution", "a consuming move requires a bespoke cost mechanism rather than CostBand, or free repositioning cannot remain pure observation", "destination authority requires a privileged Destination/path/predecessor object rather than field/gradient state plus overlay weighting", "completion requires ActionBand production machinery, 7.1a determination work, 7.2/8.x execution, StemThing-B, invariant weakening, new service authority, or successor work"]
---
## BUILD
- Land Phase 7 movement decision ingress only: sealed Phase-5 band crossings authorize movement under the ordinary generation; no raw-field CPU planner exists.
- Execute movement as spatial reparenting through column/membership state while preserving StemThing-A logical identity, one-root ownership rebinding, originated transient overlays, and CostBand consumption.
- Keep destination authority emergent from admitted field/gradient state; authored waypoints/directives may deform potential through overlays but never become destination/path authority.
## FENCES
- Core design §7 Movement-Front and StemThing §7.1 bind the rung: movement is the only spatial reparenting, but it never moves memory; logical/authored order survives and physical placement remains untouched.
- Owner-channel, overlay lifecycle, and CostBand laws are reused exactly: one root `bind_owner`, real overlay origin + `UntilDissolved` arrival, consuming move as CostBand, free repositioning as observation.
- #1661 ActionBand adoption is a fence, not an implementation request: 7.1 is the canonical spatial witness; no production ActionBand abstraction lands here, and 7.1a remains sequentially blocked.
## EXIT-PROOF
- A representative movement executes from sealed band-crossing ingress under the ordinary generation with zero CPU branching on field/heatmap reads; the production raw-field/planner mutant REDs.
- Reparenting changes parent/columns/memberships only and preserves logical SlotIndex; production row-relocation/order mutant REDs. Ownership flips by exactly one root `bind_owner`, with per-participant/table mutant RED and neutral-owner/legacy-owner guards still green.
- `InTransit` and movement outcomes carry real origin and `UntilDissolved` arrival; missing-origin/permanent mutants RED. Resource-consuming movement uses CostBand and bespoke/direct-cost mutant REDs; free repositioning consumes zero.
- Competing field attractors determine destination/progress; changing only field/potential state changes the chosen/weighted movement and changing only overlay weighting changes it again, while no destination identity is edited. Production hard-coded destination/path mutant REDs. Return PROBATION / proof-present / DA-review-pending; coding does not `/clearance`, merge, move pointer, start 7.1a, 7.2, 8.x, or StemThing-B.
