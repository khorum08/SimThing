---
rung: CONSEQUENCE-INGRESS-0
kind: rung
track: 0.0.8.7
base_sha: 40fbe6cae0b4a9ea1d3ff8356d7a0cab1eac568a
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(binding)
owner_approved: true
owner_notes: "DA falsehood-remediation dispatch 5528293654 commissioned orchestration after PR #1949 @ 40fbe6ca. Pointer reopened NONE -> 15.3. Binding scope is the exact 15.3 ladder row plus docs/0_0_8_7_SimThing_Unification.md sec 7 finding 2. Engineering review finds the consequence chain lawful but orphaned: fund_unresolved_persistence / AuthoredPersistenceValuation / PersistenceOverlayBinding have zero production or authoring ingress. PRESUMPTIVE disposition is a ClauseScript consequence-authoring ingress through the ordinary vehicle as the 15.2 port companion; down-classification is lawful only if archaeology proves there is no authoring future. 15.4 remains fenced until 15.3 graduation unless explicitly re-routed."
surfaces: ["crates/simthing-clausething/src/**", "crates/simthing-spec/src/**", "crates/simthing-driver/src/**", "crates/simthing-core/src/**", "crates/simthing-workshop/tests/**", "docs/tests/**", "scripts/ci/test_inventory.tsv", "scripts/ci/anchor_reach_log.tsv", "scripts/ci/constitutional_surfaces.tsv"]
forbidden: ["any consequence program that emits or reinjects demand, mutates the Current-to-Next carry, or creates a second persistence path", "any second market, clearer/disburser/feedback authority, response plane, economic adapter/newtype bridge, seam translator, host descendant walk, PALMA/Gu-Yang re-solve, field cache, or private solver", "any semantic alteration of exact Q, canonical T_s ABI, 15.0 R theorem/minimality, 15.1 filter canon, 15.2 persistence deformation law, 14.5 parity, 14.6 resident self-consumption, or frozen 14.1 comparator", "any scenario-specific kernel vocabulary or engine dependency on ClauseThing; authoring lowers one-way into admitted engine vocabulary", "any 15.4 oracle-surface-quarantine implementation, RESOURCE-RESPONSE-COMPRESSION-0 work, pointer movement, graduation, track closeout, or post-remediation work", "any coding edit to .github/workflows/**, scripts/ci/**/*.sh, or scripts/ci/**/*.py"]
required_checks: ["render/read this handoff first; carry machine HD-RECEIPT plus fresh coding ORIENT under the current rule stamp and ACK every rendered REQUIRED-ANCHOR", "MANDATORY FIRST STEP / engineering adjudication: reopen docs/0_0_8_7_SimThing_Unification.md sec 7 finding 2 and archaeology the entire consequence chain (fund_unresolved_persistence, AuthoredPersistenceValuation, CostBand funding, PersistenceOverlayBinding), all re-exports/callers, ordinary ClauseScript parse_script_value/modifier lowering, filter U outputs, Overlay admission/activation, and the 14.1 instrument. PRESUMPTIVE = real authoring ingress. If evidence proves no authoring future, STOP before implementation and return the exact dated down-classification case for DA ruling", "if ingress is supported, add exactly one ClauseScript consequence-authoring surface using the ordinary parse_script_value / modifier-chain lowering vehicle and existing admitted EML; do not invent a new opcode family, parser, market, persistence lane, or kernel vocabulary", "bind that authored surface to the EXISTING consequence chain as a lawful consumer of filter unresolved U: U observed -> AuthoredPersistenceValuation -> CostBand funding -> PersistenceOverlayBinding -> Overlay born; preserve existing chain semantics rather than replacing/reimplementing them", "fence CONSEQUENCE-ONLY mechanically: a consequence program cannot emit RuntimeOwnerSiloDemandBucket, enter produce_runtime_rf_next_generation_demands, author unresolved demand, or feed the 15.2 deformation port; prove by type/refusal/compile-fail or source-census falsifier rather than prose", "prove one end-to-end authored scenario through the real authoring door produces the intended Overlay consequence from unresolved U, including authored modifier-chain lowering and the generation/boundary at which the Overlay is born", "add/update a symbol-keyed constitutional or consumer census row that pins the production authoring/consumer path so fund_unresolved_persistence / AuthoredPersistenceValuation / PersistenceOverlayBinding can never silently re-orphan; raw prose or line numbers are not identity", "prove the ingress is detachably application-authored: ClauseThing lowers into admitted core/spec vocabulary; kernel/core do not depend on ClauseThing and no descendant/cache shortcut appears", "rerun frozen 15.0 consequence-only census/minimality theorem, frozen 15.2 persistence-port witnesses, 15.1 recursive-filter closure, 14.5 parity, 14.6 causal/self-consumption/tree isolation, generation pacing, and constitutional authority census", "run touched-package checks, focused 15.3 referee, inventory/drift, anchor/lifecycle checks, sanctioned-surface freshness, detachability, Agent Scan, hosted Doctrine Scan/Exec, then fresh exact-head /clearance and /relay-lint", "return PROBATION / proof-present / DA-review-pending / UNMERGED with exact base/head, HD/ORIENT receipts, engineering-adjudication result, archaeology map, authored lowering call graph, consequence production call graph, end-to-end transcript, consequence-only negative matrix, pinned-consumer census before/after, changed-file census, FULL ZERO-RED structural certificate, hosted IDs, clearance and relay-lint"]
stop_conditions: ["engineering archaeology contradicts the presumptive authoring future or shows the chain should be down-classified rather than wired", "the existing consequence chain cannot be reached from ordinary ClauseScript authoring without inventing a second parser/program family or violating kernel containment", "making the chain reachable requires demand reinjection, a second persistence lane, same-generation economic reclear, or alteration of 15.2 Current-to-Next semantics", "a consequence-only falsifier cannot mechanically exclude demand production/reinjection", "the consumer cannot be pinned symbol-keyedly without adding a new economic authority", "any required frozen witness, structural certificate, doctrine/anchor/inventory check, hosted gate, clearance, or relay lint is red"]
---

## BUILD

This is a falsehood-remediation rung, not a new subsystem. The capability already exists: authored persistence valuation, CostBand funding, and PersistenceOverlayBinding form a lawful CONSEQUENCE chain beside the recursive filter. The defect is that nothing in production or authoring reaches it.

First adjudicate the engineering finding. The commissioned presumption is that the chain has an authoring future and should receive one ordinary ClauseScript ingress, analogous to 15.2's authoring restoration but semantically separate: 15.2 deforms U inside the once-mint; 15.3 consumes U after the filter to produce consequences. If archaeology disproves that future, stop and return the down-classification case rather than wiring dead architecture.

If the ingress proceeds, reuse the ordinary ClauseScript script_value/modifier-chain vehicle and existing admitted EML. The full causal witness is authored scenario -> unresolved U -> valuation -> CostBand funding -> PersistenceOverlayBinding -> Overlay born. The consumer must be pinned by a symbol-keyed census row.

## FENCES

- consequence-only means consequence-only: never demand reinjection.
- authored consequence ingress is application-side lowering into existing engine vocabulary.
- one recursive filter R, one exact Q, one T_s, one temporal carry, one 15.2 deformation port.
- no new market authority, no second persistence path, no same-generation reclear.
- frozen 14.x / 15.0 / 15.1 / 15.2 semantics remain frozen.
- 15.4 and closeout are not in flight.

## EXIT-PROOF

Either engineering archaeology produces a DA-routable, dated down-classification case with no implementation, OR the real authored ClauseScript door reaches the existing consequence chain end-to-end and produces an Overlay from filter U; the consequence-only law is mechanically fenced against demand reinjection; the consumer is symbol-keyedly pinned so it cannot re-orphan; all frozen witnesses remain green; and the full structural certificate is ZERO-RED. Coding stops unmerged for DA review.
