---
rung: GATED-RATES-EML-REWIRE-0
kind: rung
track: 0.0.8.7
base_sha: 52053304e22540f320d4c126adeb08eca3d9b6ca
audience: coding
model_tier: std
owner_approved: true
expected_route: DA-RESERVE(gate-wiring)
owner_notes: "DA ruling 5342315576 reconciled 9.1 against graduated 5.14 and merged the governing row in #1787 @ 52053304; dispatch may proceed with no further DA pre-dispatch pass. This rung consumes ONE existing EML library and ONE existing per-program cap, rewires gated_rates onto ordinary admitted EML/role-pathway surfaces, and deletes first_slice_mapping_runtime. 5.14 deleted per-consumer cross-arm exactness declarations/digests/arm lists/shape proofs/census machinery, but ExactPrimitiveConsumerEvidence survives as a NECESSITY gate. Any gate reaching EXP/LN must use the live verify_consumer door with measured_threshold_excess_bps > 0; zero excess must RED ExactPrimitiveConsumerNotNecessary. ExactPrimitiveConsumer remains the closed two-variant enum. Corrected anti-theatre law from 5337253416: the ordinary production path must produce the right answer where a wrong implementation would not; wrong implementation may be test-side, mutated input, or excluded by type, never a caller-selectable bad branch inside production."
surfaces: ["crates/simthing-driver/src", "crates/simthing-driver/tests", "crates/simthing-core/src", "crates/simthing-spec/src", "crates/simthing-kernel/src", "docs/tests", "scripts/ci/test_inventory.tsv", "scripts/ci/triage_log.tsv"]
forbidden: ["new EML opcode, new EML library/registry, gate-local interpreter, gate-local per-program cap, second expansion surface, or widening the existing EML vocabulary to fit gated_rates", "third ExactPrimitiveConsumer variant, free-form gate consumer label, restored per-consumer exactness declaration/cross-arm digest/arm list/shape proof/census row, or any replacement for 5.14-deleted policing", "treating ExactPrimitiveConsumerEvidence as exactness evidence; it is necessity-only and zero measured excess must remain a production admission error", "hardcoded/raw physical column identity as gate policy, new column-mint mechanism, or compatibility shim preserving first_slice_mapping_runtime beside its P0-derived role-pathway successor", "production defect switch or caller-selectable mutant branch; CPU-only/helper-only/inert falsifier that never disagrees with the ordinary production path", "changing EXP/LN primitive semantics, domains, algorithms, qualification digests, arithmetic semantics, 5.10 admission shapes, or 5.14 contraction law", "StemThing-B, Vector CostBand, 9.2+, 11.3, pointer/ladder/workflow/gate/allowlist/rule-source changes, or OVERLAY-PEER-AUTHORITY retirement"]
required_checks: ["before edits: render/read the canonical handoff; obtain a fresh Std coding ORIENT-RECEIPT at literal rule stamp 61818ff7d4adda84; ACK every rendered REQUIRED-ANCHOR", "inventory the live gated_rates and first_slice_mapping_runtime consumers first; identify the P0-derived role-pathway successor for every live responsibility before deleting the legacy module; if any live responsibility lacks a lawful successor STOP rather than shim it", "rewire gated_rates gate evaluation through the ordinary admitted EML library stack and role-pathway columns; inherit the one existing per-program node cap with no gate-local cap/registry/interpreter or duplicate expansion surface", "prove gate behavior bit-identical to the retained pre-rewire/oracle behavior across the admitted gate domain; a wrong implementation must disagree while ordinary production remains right under the corrected anti-theatre law", "where a rewired gate reaches EXP/LN, use 5.10 shape-1 range certificate or shape-2 authored guard as required and pass ExactPrimitiveConsumerEvidence through the live verify_consumer door; plant zero measured excess and require ExactPrimitiveConsumerNotNecessary on production admission", "prove no 5.14-deleted per-consumer exactness machinery returns: no declarations, cross-arm digests, all-four-arms requirement, arm lists, shape proofs, or census rows; cross-arm bit identity remains a language-level witness inherited from admitted arithmetic semantics", "delete crates/simthing-driver/src/first_slice_mapping_runtime.rs and remove its module/export/references; prove no compatibility fossil, hardcoded-column fallback, or second mapping authority survives", "run the blast radius, not just focused tests: gated_rates/role-pathway/first-slice consumers, simthing-driver --lib and relevant driver tests, affected core/spec/kernel batteries, EXP/LN qualification freshness where reachable, inventory/lifecycle/detachability, Agent Scan/DOC-BUDGET/Doctrine, and every hosted Doctrine Scan workflow step; return exact commands/counts and hosted run identities"]
stop_conditions: ["the rewire requires a third ExactPrimitiveConsumer variant, free-form consumer identity, or any new EML opcode/library/cap/interpreter surface", "an EXP/LN gate cannot be expressed with existing 5.10 range-certified or explicitly-guarded call-site shapes, or necessity cannot be proven through existing ExactPrimitiveConsumerEvidence without widening the door", "deleting first_slice_mapping_runtime exposes a still-live responsibility that has no P0-derived role-pathway successor; STOP with exact consumer/substrate gap instead of preserving a compatibility shim", "lawful parity appears to require changing EXP/LN qualification, 5.14 arithmetic semantics, the existing per-program cap, or another graduated EML contract", "proof would require a production defect selector, helper-only refusal, CPU simulation of the production path, or a mutant that REDs before the targeted law", "completion requires StemThing-B, Vector CostBand, 9.2+, pointer/ladder/gate/workflow/allowlist/rule-source edits, or OVERLAY-PEER-AUTHORITY retirement"]
---
## BUILD
- Replace the legacy Family-C gate path with the already-graduated language: `gated_rates` evaluates authored gate laws through the ordinary admitted EML library stack and reaches values/targets through the role pathway. Consume the existing per-program cap; do not create a gate-local EML surface.
- Keep 5.10/5.14 exact-value law intact. EXP/LN exactness follows from the language and primitive qualification, not per-consumer digests. The one surviving per-consumer obligation is NECESSITY: an EXP/LN-reaching gate supplies `ExactPrimitiveConsumerEvidence { consumer, measured_threshold_excess_bps }` to the existing `verify_consumer` door, and zero excess is rejected.
- Delete `first_slice_mapping_runtime` once every live responsibility is proven to have a P0-derived role-pathway successor. Do not retain a shim, alias, fallback mapping service, or raw-column side door.
## FENCES
- `ExactPrimitiveConsumer` stays closed to `OrdinaryAccumulatorEvalEml` / `FieldSweepEvalEml`; no gate-specific third variant.
- No per-consumer exactness declaration, digest axis, execution-arm list, shape proof, or census mechanism returns. No EXP/LN or arithmetic-semantics reopening.
- No new EML opcode/library/cap/interpreter, no column-mint mechanism, no pointer/ladder/gate/workflow/allowlist change, and no successor-rung work.
## EXIT-PROOF
- The rewired production gate is bit-identical to the retained pre-rewire/oracle behavior on its admitted domain while consuming ordinary admitted EML and role-pathway columns; a test-side/mutated-input/type-boundary rival disagrees for the intended reason.
- Any EXP/LN-reaching gate proves necessity through the existing door; a zero-excess mutation REDs `ExactPrimitiveConsumerNotNecessary`. No deleted cross-arm per-consumer machinery is reintroduced.
- `first_slice_mapping_runtime.rs` is absent, all imports/exports/callers are migrated, and no legacy/raw-column compatibility path survives.
- Full blast-radius batteries and every relevant hosted workflow step are green. Return PROBATION / proof-present / DA-review-pending with exact base/head/tested_code_sha, live HD receipt, pinned-stamp coding receipt, all anchor ACKs, parity/falsifier evidence, deletion inventory, local battery counts and hosted identities. Coding does not `/clearance`, `/relay-lint`, merge, or move the pointer.
