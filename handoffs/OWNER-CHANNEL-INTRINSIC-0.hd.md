---
rung: OWNER-CHANNEL-INTRINSIC-0
kind: rung
track: 0.0.8.7
base_sha: 5a75f341
audience: coding
model_tier: frontier
owner_approved: true
expected_route: DA-RESERVE(gate-wiring)
owner_notes: "DA rulings 5151931092 and 5152409271 are authoritative. MANDATORY RE-ORIENTATION: every pre-5151931092 handoff/workplan/reading list is stale; before implementation run current orient.sh --role=coding, carry the fresh receipt, and read the current orientation digest plus docs/scenario_containment_handover.md. Pointer is 6.0 OWNER-CHANNEL-INTRINSIC-0; 5.8b is not next. Reap clock 2026-08-11 remains sovereign. PR #1558 landed the intended (a)(b)(g) substrate at 5a75f341, but restored orchestration review found bounded defects that MUST be remediated in this same 6.0 coder turn: missing-target and malformed-binding owner resolution can alias to neutral/inherit; live RF admission still reads legacy flat owner metadata instead of intrinsic resolution; the sanctioned-mechanism registry forward-declares unfinished (d)/(e); and gen_digest fingerprints but does not parse/render that registry. Treat #1558 as substrate, not settled exit proof. Implementer is Frontier coder, not DA."
surfaces: ["crates/simthing-core/src", "crates/simthing-core/tests", "crates/simthing-spec/src", "crates/simthing-sim/src", "crates/simthing-driver/src", "crates/simthing-driver/tests", "crates/simthing-clausething/tests/fixtures/specialist_citizens_minimal.clause", "scripts/ci/allow/contention_mechanisms.txt", "scripts/ci/gen_digest.sh", "scripts/ci/clearance_check.sh", "scripts/ci/test_inventory.tsv", "scripts/ci/triage_log.tsv", "docs/sanctioned_surface.md", "docs/tests", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md"]
forbidden: ["shipped-scenario/corpus reads or scenario/domain-activity vocabulary in engine src/tests", "cross-crate fixture export, dev-dependency, or filesystem reach", "new SimThing owner struct field or materialized/stamped/cached resolved owner; Option/None as the effective owner of an admitted member", "foreign/missing target or malformed/blank explicit owner binding silently resolving as neutral/unowned or inherited", "authored Owner identity colliding with the reserved neutral owner `unowned`", "RF admission/reduce-up/compile/writeback using OWNER_FLOW_OWNER_REF_PROPERTY_ID, PLANET_OWNER_REF_PROPERTY_ID, owner_flow_owner_ref, planet_owner_ref, or equivalent flat metadata as owner authority after intrinsic migration", "ownership-uniformity admission, owner-equality branch for aggregation, bespoke owner table/channel, or new scheduler concept", "planet_id/star_system_gridcell_id_raw retention in the generalized scope key or a new domain-shaped canonical scope key; O(nodes x owners x resources) retained recording", "sanctioned-mechanism rows whose named ready-surface is not live at the same head; manifest-only or silently ignored contention registry wiring", "predeclared attrition/8.x registry rows, assertion weakening for pre-existing reds, 5.8b/6.1+ implementation"]
required_checks: ["fresh coding ORIENT-RECEIPT and current ANCHOR-ACKs before edits; matching revised HD-RECEIPT in relay", "valid in-tree resolve_owner remains total with neutral-unowned, >=3-level inheritance, and single-property fission; a missing/foreign target cannot alias neutral; explicit malformed/blank owner binding fails closed; authored Owner id `unowned` rejects as reserved", "intrinsic owner binding is the RF authority: production participant/bucket owner derives from one deterministic tree/admission resolution pass, legacy flat owner metadata has zero RF authority consumers, and resolved non-neutral owners retain referential integrity to admitted Owner SimThings", "a real synthetic N-owner container runs the admitted reduce-up/conservation path and conserves with no ownership-uniformity/equality branch; generalized `{owner, resource, ScopeId}` bucket has retired domain fields absent", "bounded crossing-flow plus the ordinary node own-aggregate STEAD state reconstructs the RF map; incremental owner-crossing retained state is O(crossings), with CPU/GPU parity and canonical bucket ordering explicit", "contention_mechanisms is parsed and structurally validated by gen_digest, rendered as a dedicated sanctioned-surface table for low-context agents, included in global and track-addendum exactness, and contains only mechanisms live at the exact head", "crate-local synthetic fixture only; scenario-residue, detachability, digest/gate-wiring, inventory/lifecycle, agent-scan and focused batteries green at exact head"]
stop_conditions: ["stale/missing orientation receipt, stale anchor, reliance on deleted pre-intervention paths, or use of superseded HD-RECEIPT 8a5a9c8b0f14", "preserving total owner semantics for admitted members appears to require treating a missing tree member as the neutral owner rather than failing the membership lookup closed", "explicit owner binding cannot be distinguished from malformed binding without inventing a second owner authority", "migrating RF authority off legacy flat owner metadata appears to require materializing resolved owner, adding a SimThing owner field, or stamping every participant", "aggregation/admission appears to require owner equality control flow or one-owner-per-container", "ScopeId cannot replace domain-shaped keys, STEAD recording cannot remain bounded to crossing deltas plus ordinary own aggregate, or CPU/GPU canonical ordering cannot be preserved", "making contention_mechanisms visible/validated appears to require a second digest system rather than extending the existing gen_digest model", "completion appears to require gate loosening, cross-crate/shipped-scenario fixtures, future-mechanism predeclaration, or weakening a pre-existing red"]
---
## BUILD
- **Review remediation for landed (a)/(b): preserve the architecture, fix the fail-open edges.** `simthing_core::owner_channel` remains the canonical sparse-property mechanism; do not replace it with a struct field or side table. For an admitted member of the tree, effective owner remains exactly one `OwnerRef`: explicit binding -> nearest ancestor binding -> neutral `unowned`. But a target that is not a member of the supplied authority tree is a lookup/invariant failure, **not** the real neutral owner. Split checked membership from total member-resolution if needed; do not reintroduce `Option` as the effective owner of a valid member. Likewise, property absence alone means inherit: if the owner property is present but malformed, undecodable, blank, or otherwise invalid, fail closed at binding/admission rather than silently treating it as absence. Reserve the literal neutral identity `unowned`: an authored Owner entity may not alias it. Add planted-defect/falsifier coverage for all three cases.
- **Finish the ownership authority migration that #1558 did not perform.** The live RF path still derives participant owner through legacy `owner_flow_owner_ref` / planet-owner metadata. Replace that authority with intrinsic owner resolution over the SimThing tree. Existing serialized compatibility metadata may be read only at an explicit authoring/admission migration boundary if necessary, then converted to the minimum explicit owner-property bindings at ownership boundaries; it must not remain an RF admission/reduce-up/compile/writeback authority and must never be re-stamped per participant. Preserve the existing unknown-owner guard: every resolved non-neutral `OwnerRef` used by RF must correspond to an admitted Owner SimThing. Resolve deterministically once per tree/admission/compile view (or equivalent single traversal), not by repeated root DFS in a generation hot loop.
- **Complete (d): one generic key, no conditional.** Generalize the canonical reduce-up scope to `{owner, resource, ScopeId}` so same-owner merge and different-owner segregation emerge solely from key equality/order; no owner-equality branch may control aggregation. Retire `planet_id` and `star_system_gridcell_id_raw` from the canonical key and do not leave `PlanetChildRfScopeKey` as the platform-level owner bucket authority; a compatibility adapter may remain only if downstream legacy code still requires it and no new production path depends on the domain-shaped name. Keep `ScopeId` and its canonical comments/domain neutral. Owner boundary = reduce-up = execution boundary; do not mint a scheduler or owner side table.
- **Complete (e): bounded ordinary STEAD recording.** Record only ownership-crossing flow deltas plus each node's already-ordinary own aggregate/anchor state, sufficient to reconstruct the RF map without a second full-map sidecar. Incremental owner-crossing retained state must scale O(crossings), not O(nodes x owners x resources). `is_ownership_crossing` may detect a boundary; it must not become an owner-equality aggregation branch. Preserve canonical bucket ordering and prove CPU/GPU parity on both same-owner and different-owner paths.
- **Review remediation for landed (g): make the registry true and visible.** At `5a75f341`, `segregate-flows-by-owner` and `detect-ownership-crossing` describe unfinished (d)/(e) as already ready. At the final head, every `contention_mechanisms.txt` row must name a concrete live ready-surface at that same head; remove/replace any forward declaration before using the file as authority, then add the (d)/(e) rows only when their real implementation exists. `flip-ownership` / fission / resolution rows may remain only insofar as their named mechanism is genuinely live. Extend the existing `gen_digest.sh` model—not a second digest—to parse/validate the four-column contention rows, enforce unique keys, include track-addendum `contention_mechanisms.txt` rather than silently ignoring it, render a dedicated `Contention Mechanisms` table into `docs/sanctioned_surface.md`, and include that table in generated-exactness checks. The current Source Manifest hash alone is insufficient.
- Extend only crate-local/synthetic proof input. `specialist_citizens_minimal.clause` may witness authoring/admission, but no shipped scenario may define or prove the owner law. Land PROBATION evidence/inventory/status, not graduation.
## FENCES
- Hold current `simthing-0087-binding-laws`, `simthing-0087-pillars`, RF/typing/observation anchors, Vendor Containment, Transient Fixture, Capability Binding, Purge Reconciliation, and Mechanisms-Not-Domains; `scenario_residue_check.sh` is a hard fence, not a cleanup suggestion.
- Preserve #1558's valid substrate: sparse owner property, `OwnerRef` homed in core/re-exported from spec, neutral owner concept, one-property fission, and gate-wiring registration. Repair the defects above; do not redesign ownership into a new subsystem.
- Assets remain assets: no shipped scenario is a proof input; no transient fixture leaves its crate; do not repair baselined ClauseThing/test-inventory reds by weakening law or assertions.
- Preserve 5.8 graduation and 5.7 measured debt. Do not implement 5.8b, 6.1+, attrition rows, tiled gather, or any downstream `BINDS 6.0` consumer in this rung.
## EXIT-PROOF
- On an admitted tree, `resolve_owner` is total for neutral-unowned, deep inheritance, and one-property fission; inert SimThings add zero owner storage and no resolved owner is materialized. A foreign/missing target is demonstrably not neutral ownership; malformed/blank explicit binding demonstrably cannot inherit silently; authored Owner id `unowned` is rejected as a reserved identity.
- Production RF owner authority is intrinsic: a census/proof shows the admission -> reduce-up -> GPU compile/writeback chain obtains owner from the canonical intrinsic resolution path, with no authoritative read of `OWNER_FLOW_OWNER_REF_PROPERTY_ID`, `PLANET_OWNER_REF_PROPERTY_ID`, `owner_flow_owner_ref`, `planet_owner_ref`, or equivalent flat participant stamps. Non-neutral resolved owners remain referentially valid Owner SimThings.
- A synthetic N-owner container actually executes the admitted reduce-up/conservation path and conserves flow; same/different aggregation has zero owner-equality control branch. The generalized canonical scope key is `{owner, resource, ScopeId}` with `planet_id` and `star_system_gridcell_id_raw` absent and domain-shaped key authority retired.
- Crossing-flow recording plus ordinary own-aggregate state reconstructs the RF map with incremental retained owner-crossing state bounded O(crossings); same-owner and different-owner cases have explicit CPU/GPU parity and canonical ordering proofs.
- The sanctioned-mechanism registry contains only exact-head-ready 6.0 mechanisms; the premature (d)/(e) claims from #1558 are corrected; `gen_digest.sh` parses and validates the rows, track addenda cannot be silently ignored, and `docs/sanctioned_surface.md` visibly renders the registry contents rather than only its fingerprint. Generated exactness and gate-wiring remain green.
- Crate-local synthetic proof, scenario-residue/detachability/lifecycle/inventory/orientation gates are green at exact head. Return PROBATION / proof-present / DA-review-pending; coder does not invoke `/clearance` and does not merge.
