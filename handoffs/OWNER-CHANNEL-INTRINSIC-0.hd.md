---
rung: OWNER-CHANNEL-INTRINSIC-0
kind: rung
track: 0.0.8.7
base_sha: 5a75f341
audience: coding
model_tier: frontier
owner_approved: true
expected_route: DA-RESERVE(gate-wiring)
owner_notes: "DA rulings 5151931092 and 5152409271 are authoritative. MANDATORY RE-ORIENTATION: pre-5151931092 handoffs/workplans/readings are stale; run current orient.sh --role=coding before edits, carry its receipt, read current orientation plus docs/scenario_containment_handover.md, and never reuse deleted-path context. Pointer stays 6.0; 5.8b is not next; reap clock 2026-08-11 is sovereign. PR #1558 at 5a75f341 is valid substrate but not settled exit proof: restored orchestration found bounded (a)(b)(g) defects—foreign/malformed ownership can alias neutral/inherit, RF still uses flat owner metadata, and the registry forward-declares unfinished (d)/(e) while gen_digest does not render/validate it. Remediate these while finishing (d)/(e). Frontier coder, not DA."
surfaces: ["crates/simthing-core/src", "crates/simthing-core/tests", "crates/simthing-spec/src", "crates/simthing-sim/src", "crates/simthing-driver/src", "crates/simthing-driver/tests", "crates/simthing-clausething/tests/fixtures/specialist_citizens_minimal.clause", "scripts/ci/allow/contention_mechanisms.txt", "scripts/ci/gen_digest.sh", "scripts/ci/clearance_check.sh", "scripts/ci/test_inventory.tsv", "scripts/ci/triage_log.tsv", "docs/sanctioned_surface.md", "docs/tests", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md"]
forbidden: ["scenario/corpus proof inputs, scenario/domain vocabulary in engine src/tests, cross-crate fixture reach, or weakening inherited reds", "new SimThing owner field/side table/scheduler; materialized/cached resolved owner; Option/None as effective owner of an admitted member", "foreign target or malformed/blank explicit binding silently becoming neutral/inherited; authored Owner id colliding with reserved `unowned`", "legacy OWNER_FLOW/PLANET owner-ref metadata or equivalent flat participant stamp remaining RF authority after migration; ownership-uniformity or owner-equality aggregation control flow", "domain-shaped canonical scope keys/planet_id/star_system_gridcell_id_raw in generalized key; O(nodes x owners x resources) retained recording", "registry rows without exact-head live mechanisms; manifest-only/silently ignored registry wiring; future attrition/8.x rows; 5.8b/6.1+ or downstream BINDS-6.0 work"]
required_checks: ["fresh coding ORIENT-RECEIPT + current ANCHOR-ACKs + revised HD-RECEIPT before edits", "valid-member resolve is total with neutral/deep inheritance/fission; foreign target fails membership closed; malformed/blank explicit binding fails closed; authored `unowned` Owner rejects", "RF participant/bucket owner derives from deterministic intrinsic tree resolution; legacy flat owner metadata has zero RF-authority consumers; every non-neutral owner resolves to an admitted Owner", "synthetic N-owner admitted reduce-up/conservation executes with no owner-uniformity/equality branch; canonical `{owner,resource,ScopeId}` key retires domain fields; crossing+own-aggregate STEAD reconstructs RF at O(crossings) with CPU/GPU parity + canonical ordering", "contention registry contains only exact-head-ready mechanisms and gen_digest parses, validates, renders, track-addends, and exactness-checks it; synthetic-only scenario-residue/detachability/inventory/lifecycle/agent/focused gates green"]
stop_conditions: ["stale orientation/anchor/deleted-path context or superseded HD-RECEIPT 8a5a9c8b0f14", "fixing owner membership/binding validity or RF migration seems to require materialization, a second owner authority, per-participant restamping, or owner equality/uniformity control flow", "ScopeId cannot replace domain-shaped authority, crossing retention cannot remain O(crossings), or CPU/GPU canonical ordering cannot hold", "registry visibility/validation appears to need a second digest instead of extending gen_digest", "completion requires gate loosening, cross-crate/shipped fixtures, future-mechanism predeclaration, inherited-red weakening, or successor work"]
---
## BUILD
- Preserve #1558's architecture: sparse owner property, core `OwnerRef`, neutral `unowned`, one-property fission. Repair fail-open edges: valid in-tree members resolve explicit -> nearest ancestor -> neutral, but foreign targets are lookup/invariant failures; property ABSENCE alone means inherit, while present malformed/blank payload fails closed; reserve `unowned` against authored Owner identity. Add planted defects.
- Finish the authority migration #1558 did not: RF admission -> reduce-up -> GPU compile/writeback must derive owner from intrinsic tree resolution, not `OWNER_FLOW_OWNER_REF_PROPERTY_ID`, `PLANET_OWNER_REF_PROPERTY_ID`, `owner_flow_owner_ref`, `planet_owner_ref`, or equivalent flat stamps. Compatibility metadata may convert once at authoring/admission to minimum ownership-boundary bindings, never remain RF authority or restamp every participant. Preserve referential integrity to admitted Owners and resolve once per deterministic tree/admission/compile view, not repeated root DFS in the generation hot loop.
- Complete (d): canonical reduce-up key `{owner, resource, ScopeId}`; same/different owner behavior emerges from key order/equality, never an owner-equality branch. Retire `planet_id`/`star_system_gridcell_id_raw` and domain-shaped platform authority; `PlanetChildRfScopeKey` may survive only as a legacy adapter with no new production dependency. Owner boundary = reduce-up = execution boundary; no scheduler/side table.
- Complete (e): retain only ownership-crossing flow deltas plus each node's ordinary own aggregate/anchor state, enough to reconstruct RF with incremental owner state O(crossings). Crossing predicate may detect an edge but never control aggregation. Prove same/different-owner CPU/GPU parity and canonical ordering.
- Repair (g): every `contention_mechanisms.txt` row must name a concrete same-head ready surface; correct #1558's premature (d)/(e) claims and add them only when real. Extend existing `gen_digest.sh` to parse four-column registry rows, enforce unique keys, consume track addenda, render `Contention Mechanisms` in `docs/sanctioned_surface.md`, and exactness-check it. Source-manifest fingerprint alone is insufficient. Use crate-local/synthetic proof only; return PROBATION, not graduation.
## FENCES
- Hold current binding laws/pillars, RF/typing/observation anchors, Vendor Containment, Transient Fixture, Capability Binding, Purge Reconciliation, Mechanisms-Not-Domains, and hard `scenario_residue_check.sh`.
- Do not redesign valid #1558 substrate into a new owner subsystem; assets remain assets and no shipped scenario proves owner law.
- Preserve 5.8 graduation + 5.7 measured debt; no 5.8b, 6.1+, attrition/tiled-gather, or downstream `BINDS 6.0` implementation.
## EXIT-PROOF
- Valid members: total neutral/deep-inheritance/fission resolution, zero inert owner storage, zero materialization; foreign target != neutral ownership; malformed/blank explicit binding cannot inherit silently; authored Owner `unowned` rejects.
- Census/proof: RF admission -> reduce-up -> GPU compile/writeback takes owner from intrinsic resolution with zero authoritative legacy-flat reads; non-neutral owners are admitted Owners.
- Synthetic N-owner admitted reduce-up conserves with zero owner-equality control branch; canonical scope is `{owner,resource,ScopeId}` with retired domain fields absent. Crossing deltas + ordinary own aggregate reconstruct RF at O(crossings), with CPU/GPU parity + canonical ordering.
- Registry has only exact-head-ready 6.0 mechanisms; premature #1558 (d)/(e) claims corrected; gen_digest visibly parses/validates/renders global + track-addendum rows and generated exactness/gate wiring are green.
- Synthetic-only focused + scenario-residue/detachability/lifecycle/inventory/orientation gates green. Return PROBATION / proof-present / DA-review-pending; coder does not `/clearance` or merge.
