---
rung: ACTIONBAND-EXECUTION-INGRESS-0
kind: remedial
track: 0.0.8.7
base_sha: 02fadc8b20238011d2d1aa795c222ffd9dddd2e9
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "11.1d ACCUMULATOR-CONVERGENCE-SEAL-0 graduated under DA ruling 5400396198. This is the next lawful rung. Binding correction: the existing ActionBand tripwire pins the declaration at action_band_consequence.rs, not a production caller; true production-caller count is zero. The door currently advertises action_band_commitments. This rung must either wire compiled ActionBand execution into the ordinary session path through existing graduated machinery, or withdraw that advertised entry with a dated deferral if the required production seam is genuinely absent. Correct the tripwire wording either way. 11.1f, #1803/11.2, 11.3+, pointer movement, and new ActionBand vocabulary remain fenced."
surfaces: ["crates/simthing-driver", "crates/simthing-embedder", "crates/simthing-gpu", "docs/tests", "scripts/ci/test_inventory.tsv"]
forbidden: ["new ActionBand executor, dispatcher, crossing evaluator, listener, scheduler, history authority, or second execution path", "new ActionBand opcode, target form, registry, authored vocabulary, or EML gadget surface", "CPU-side decision authority derived from ActionBand observation/readback", "changes to 11.1f authored-law gadget, #1803/11.2 guide/exemplars, 11.3+, 12.x, or pointer state", "claiming the declaration tripwire or test-only compile_action_band_gpu_execution callers as proof of production execution", "keeping action_band_commitments advertised if ordinary production execution remains unreachable without a genuine new mechanism"]
required_checks: ["before edits render/read this handoff; carry a fresh Frontier coding ORIENT receipt at the current rule stamp and ACK every rendered REQUIRED-ANCHOR", "FIRST STEP archaeology: census all production and test callers of compile_crossing_consequence_session, compile_action_band_gpu_execution, CrossingConsequenceSession/Dispatch, and the embedder action_band_commitments export; distinguish declarations, tests, and true production callers", "map the existing ordinary SimSession hot-cycle/boundary path to the graduated ActionBand compile/dispatch machinery and identify the smallest lawful insertion seam; do not author a parallel executor", "if the existing seam is sufficient, wire the compiled ActionBand session into ordinary production lifecycle with one session-owned execution state and generation authority inherited from the existing session/boundary path", "if the existing seam is insufficient without a new mechanism, withdraw the Vendor Door action_band_commitments entry and record a dated deferral naming the exact missing production seam; do not leave a compile-only advertised product", "correct the existing tripwire/message so it states declaration-only and cannot be read as production-caller proof", "prove the advertised door entry, if retained, reaches real ordinary-session execution with a production-path witness and a planted dropped-product/bypass mutant; if withdrawn, prove the entry is absent and the dated deferral is present", "preserve the sealed Phase-5 crossing surface, existing consequence arms, generation dedupe, replay/history authority, and stead section 10 convergence", "run focused ActionBand driver/GPU batteries plus ordinary-session blast radius and real-adapter execution where the retained path requires it; structural certificate at graduation", "evidence follows handoff_template: one docs/tests/actionband_execution_ingress_0_results.md, one current_evidence_index.md line, one 11.1e status-row edit, plus mechanically required inventory/lifecycle updates; return exact base/head/tested_code_sha and hosted workflow IDs"]
stop_conditions: ["ordinary-session wiring requires a new executor, dispatcher, crossing evaluator, listener, scheduler, or second history/generation authority", "the retained Vendor Door entry cannot execute without expanding ActionBand target/opcode/authored vocabulary", "the only available path requires CPU-side decisions from readback rather than the existing GPU/feeder consequence machinery", "withdrawing the entry would require silently breaking shipped authored data rather than a bounded API surface change and dated deferral", "the change requires touching 11.1f, #1803/11.2, 11.3+, 12.x, or moving the pointer"]
---
## BUILD
- Reconcile the **advertised ActionBand door** with real execution. `simthing_embedder::bind::action_band_commitments` currently re-exports `compile_crossing_consequence_session`, while the ordinary `SimSession` has zero ActionBand execution ingress.
- Preferred lawful outcome: if the graduated compile/dispatch machinery is already sufficient, bind one compiled ActionBand execution state into the ordinary session lifecycle and drive it through the existing GPU/feeder consequence path. Reuse the existing session generation/boundary authority; author no rival executor or dispatcher.
- Lawful fallback: if a genuine missing production seam makes that impossible without new mechanism, withdraw `action_band_commitments` from the Vendor Door and record a dated deferral naming the exact missing seam. The door must advertise nothing it cannot run.
- Correct the existing ActionBand tripwire/message so it distinguishes a declaration from a production caller.
## FENCES
- Phase-5 crossing selection, ActionBand consequence vocabulary, generation dedupe, replay/history authority, and existing GPU execution semantics remain authoritative. This rung connects or withdraws; it does not redesign ActionBand.
- No CPU decision path, second executor, second dispatcher, new opcode/target form/registry, or successor-rung EML/gadget work.
- One rung only: 11.1f, #1803/11.2, 11.3+, 12.x and pointer movement stay untouched.
## EXIT-PROOF
- If the door remains advertised, an embedder-authored ActionBand compile product reaches **real ordinary `SimSession` execution** and observable consequence through the existing production machinery; a planted dropped-product/bypass defect REDs for its own named reason.
- If the door is withdrawn, the export is absent, the exact missing production seam is recorded with a dated deferral, and a compile/import witness proves vendors cannot obtain a product the runtime drops.
- The declaration/caller tripwire is corrected and separately proves **true production-caller count**, never treating the declaration or test callers as execution evidence.
- Existing ActionBand/consequence batteries, ordinary-session blast radius, replay/determinism invariants, and real-adapter proof (when execution is retained) are green at one exact head. Structural certificate runs at graduation.
- Evidence: `docs/tests/actionband_execution_ingress_0_results.md`, one current-evidence-index line, one 11.1e status-row edit, and only mechanically required inventory/lifecycle rows.
