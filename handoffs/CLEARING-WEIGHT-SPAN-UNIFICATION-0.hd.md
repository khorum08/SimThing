---
rung: CLEARING-WEIGHT-SPAN-UNIFICATION-0
kind: rung
track: 0.0.8.7
base_sha: 630e57d0d9747af3372d79310d4a49ccf5c976e5
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(binding)
owner_approved: true
owner_notes: "Phase-13 consolidated authority: Board 5462798074. 13.5 DA graduation and 13.6 opening authority: Board 5466915427. Owner hold after 13.6: Board 5466933355; graduation completes Phase 13 but closeout is forbidden until explicit Owner call after engineering review. Orchestrator orientation at authored base: ORIENT-RECEIPT 0600710656a3; rule stamp 164def77d171c853."
surfaces: ["crates/simthing-core/src", "crates/simthing-kernel/src", "crates/simthing-spec/src", "crates/simthing-sim/src", "crates/simthing-driver/src", "crates/simthing-embedder/src", "crates/simthing-core/tests", "crates/simthing-kernel/tests", "crates/simthing-spec/tests", "crates/simthing-sim/tests", "crates/simthing-driver/tests", "crates/simthing-embedder/tests", "scripts/ci/test_inventory.tsv", "scripts/ci/anchor_reach_log.tsv", "docs/tests", "docs/tests/current_evidence_index.md", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md"]
forbidden: ["any alternate, sibling, shadow, or compatibility participant-weight authority instead of the graduated 7.8a span/profile substrate", "retaining the whole-tree recursive clearing-weight walk or its materialized BTreeMap as a second executable path after convergence", "retaining or recreating the synthetic-generation clear_constrained_claims compatibility form after its four sanctioned test call sites migrate to clear_constrained_claims_at_generation", "any change to constrained-clearing arithmetic, integer apportionment, eligibility, epsilon treatment, tie-break ordering, DecimalField exactness, or deterministic replay semantics", "any authored-case or expected-value rewrite used to make clearing parity pass", "any weakening, ignoring, deletion, or semantic rewrite of the germ battery or the graduated 11.2f/11.3 witnesses", "Vector CostBand or any other banked performance-ladder implementation, benchmark-driven redesign, GPU/physical-model work, or post-closeout performance-track work", "any .github/workflows/** or scripts/ci/**/*.sh or scripts/ci/**/*.py gate-code edit", "any unrelated refactor, authoring-ingress work, ClauseThing/MapGen/Studio cleanup, or reopening of graduated 13.1-13.5 law", "pointer movement, Phase-13 closeout, closeout preparation, or engineering-review ceremony from the coding lane"]
required_checks: ["before edits render/read this handoff; perform fresh Frontier coding-role orientation on the exact merged handoff dispatch master named by the Board dispatch and ACK every machine-rendered REQUIRED-ANCHOR", "FIRST STEP mechanically enumerate every live call site and implementation body for resolve_effective_clearing_weights, the graduated 7.8a participant weight span/profile substrate it must consume, every constrained-clearing consumer, and the synthetic-generation clear_constrained_claims compatibility form; publish exact files/identifiers/counts before editing", "FIRST STEP mechanically verify the DA-authorized compatibility census: exactly one test consumer and exactly four call sites of the synthetic-generation clear_constrained_claims form; any mismatch is a STOP for orchestration/DA reconciliation, not permission to redefine the count", "record the dispatch-base outputs/weights for the complete germ battery authored cases plus the graduated 11.2f/11.3 clearing witnesses before edits so post-change bit identity is mechanically comparable", "replace resolve_effective_clearing_weights whole-tree traversal plus materialized BTreeMap resolution with consumption of the already-graduated 7.8a span/profile substrate; do not introduce a replacement map, cache, compatibility walk, or second weighting authority", "delete the old whole-tree implementation after all consumers converge; require net deletion and mechanically prove no executable recursive/tree-walk clearing-weight path remains", "delete the synthetic-generation clear_constrained_claims compatibility form; migrate its one test consumer/four call sites to clear_constrained_claims_at_generation with explicit truthful generations, with no replacement default-generation helper or implicit-generation shim", "preserve constrained-clearing semantics exactly: integer arithmetic/apportionment, participant eligibility, epsilon behavior, deterministic tie-breaks, exact DecimalField behavior, and replay determinism are immutable", "prove bit-identical effective weights across every authored germ-battery case and run the germ battery plus the graduated 11.2f/11.3 witnesses with substantive assertion bodies and expected values unmodified", "exercise the standing cross-domain clearing matrix covering ship, commodity, and freighter clearing; include order invariance, weighted ties, exact DecimalField apportionment, and deterministic replay under the canonical span/profile route", "ledger any new tests in test_inventory.tsv, preserve append-only anchor_reach_log discipline, and keep evidence/current evidence index truthful", "run focused clearing/span/profile batteries, doctrine/orientation/anchor/handoff/agent-delta/diff checks, hosted Doctrine Scan, and the procedure-of-record structural/full-workspace certificate; zero-red law is active and the expected certificate line is 0 failed", "return PROBATION / proof-present / DA-review-pending with exact dispatch base, tested/final SHA, changed-file census, archaeology inventory, old-to-new call-site map, deletion proof, bit-identity proof, regression-seal diff proof, cross-domain matrix, structural certificate, hosted run ids, HD receipt, and fresh body/head-bound /clearance plus /relay-lint; coding does not merge, graduate, move the pointer, start closeout, or invoke the engineering-review ceremony"]
stop_conditions: ["the live compatibility census is not exactly one test consumer / four call sites and cannot be reconciled to DA authority before edits", "the graduated 7.8a span/profile substrate cannot express the current effective clearing weights bit-identically without semantic expansion, a second path, or a compatibility shim", "any authored germ case, 11.2f/11.3 witness, ship/commodity/freighter case, DecimalField apportionment, weighted tie, order-invariance case, or replay witness changes observable semantics", "the old whole-tree/BTreeMap weighting implementation cannot be deleted after convergence", "the synthetic-generation clear_constrained_claims door cannot be deleted after the four explicit-generation migrations", "a proposed repair requires changing clearing arithmetic, eligibility, epsilon, tie-break, DecimalField semantics, authored expected values, or a graduated witness", "any new workspace failure, focused-test red, doctrine red, orientation red, or hosted Doctrine Scan red appears", "implementation pressure expands into Vector CostBand, the banked performance ladder, another Phase-13 rung, pointer movement, closeout, or post-phase engineering review"]
---
## BUILD
- Converge constrained-clearing weight resolution onto the **already-graduated 7.8a participant span/profile substrate**. The semantic target is deliberately boring: the same effective weights and the same clearing results, reached without a whole-tree recursive walk and without materializing a separate `BTreeMap` authority.
- Start with mechanical archaeology. Before editing, publish exact file/identifier inventories for: `resolve_effective_clearing_weights`; every caller/consumer that receives its effective weights; the canonical 7.8a span/profile producer and access path; the constrained-clearing entry points; and both `clear_constrained_claims` forms. Reconcile the Owner/DA A1 census to **one test consumer / four call sites** for the synthetic-generation compatibility form. A mismatch is a STOP.
- Capture dispatch-base truth before edits. For every authored case in the existing germ battery and every graduated 11.2f/11.3 witness, record the effective weight vector/profile and externally observed clearing result under the current implementation. This is the parity oracle; do not re-author it after seeing the new implementation.
- Reimplement `resolve_effective_clearing_weights` by consuming the graduated span/profile substrate directly. Preserve participant identity/order semantics and exact weight values. Do not rebuild the same tree walk under another helper, materialize a replacement map, or add a cache/adapter whose only purpose is to preserve the old route.
- Once all live consumers use the canonical profile/span, **delete the whole-tree implementation**. This rung requires net deletion and one executable weight authority. Dead code, compatibility wrappers, feature-gated copies, test-only copies, or dormant alternate traversal paths do not satisfy convergence.
- Apply the folded A1 exactly: **delete the synthetic-generation `clear_constrained_claims` compatibility form**. Its single test consumer / four calls migrate to `clear_constrained_claims_at_generation` and must pass explicit, truthful generation values. Do not substitute another default/current/synthetic generation helper.
- Preserve clearing semantics by law. The underlying constrained-clearing algorithm is not being redesigned: integer arithmetic and apportionment, eligibility, epsilon handling, deterministic tie-break ordering, exact `DecimalField` behavior, and replay determinism must remain bit/behavior identical.
- Regression proof is cross-domain, not a single happy path. Exercise ship, commodity, and freighter clearing through the canonical route, including order-invariance permutations, weighted ties, exact DecimalField apportionment, and deterministic replay. The existing germ battery plus 11.2f/11.3 witnesses are regression seals and pass **UNMODIFIED**.
- Return PROBATION / proof-present / DA-review-pending. Coding does not merge or self-graduate. 13.6 is the final implementation rung, but **Board 5466933355 holds closeout after graduation** pending Owner-directed engineering review; do not prepare, begin, or simulate closeout.

## FENCES
- **One weight authority.** The 7.8a span/profile substrate is the canonical source. No shadow `BTreeMap`, recursive compatibility resolver, lazily rebuilt map, secondary profile type, or fallback traversal may remain executable.
- **Semantic-neutral only.** Do not change clearing acceptance/eligibility, integer math, epsilon rules, tie-breaks, apportionment, `DecimalField` exactness, replay ordering, or participant identity semantics. Any apparent need is a STOP and a named design seam, not permission to broaden the rung.
- **Regression seals are immutable.** Germ-battery authored cases and graduated 11.2f/11.3 witness assertions/expected values remain substantively diff-identical. New coverage may be added; old truth may not be edited to bless drift.
- **Compatibility door disappears.** `clear_constrained_claims` synthetic-generation compatibility is deleted, not deprecated or hidden. The four sanctioned test calls use `_at_generation` explicitly. No new implicit-generation wrapper may replace it.
- **Performance ladder stays banked.** This is a topology/convergence cleanup with an already-authorized performance rationale, not a benchmark campaign or permission to implement Vector CostBand, GPU/physical changes, or the rest of `SimThing_Unification_Model.md`'s banked ladder.
- **Graduated law stays graduated.** Do not reopen 13.1-13.5 admission, ClauseThing, golden truth, workshop corpus, or legacy census decisions. Do not change authoring ingress to make this rung easier.
- Gate code remains fenced: no `.github/workflows/**`, `scripts/ci/**/*.sh`, or `scripts/ci/**/*.py`. Data ledgers/evidence may change only as required by normal proof discipline.
- **Owner hold is binding.** Even if implementation appears complete, coding does not advance the pointer, declare Phase 13 closed, invoke closeout, or begin engineering review. Graduation and the post-graduation Owner decision remain governance events.

## EXIT-PROOF
- **Archaeology proof:** exact pre-edit inventory of resolver body/callers, canonical 7.8a span/profile producer/access path, constrained-clearing consumers, both clearing entry forms, and a reconciled `1 consumer / 4 call sites` compatibility census.
- **Bit-identity proof:** for every germ-battery authored case, publish pre/post effective weights and demonstrate exact identity. No tolerance-based approximation is acceptable where the old result was exact.
- **Single-path/deletion proof:** show every constrained-clearing weight consumer reaches the canonical span/profile and prove the old whole-tree traversal + materialized-BTreeMap implementation is deleted with no executable equivalent left. Report relevant net line/file deletion and symbol/call-site searches.
- **Compatibility-removal proof:** `clear_constrained_claims` synthetic-generation form is absent; all four former calls are enumerated and now invoke `_at_generation` with explicit generation values. Prove there is no default/current/synthetic-generation replacement helper.
- **Regression-seal proof:** germ battery and graduated 11.2f/11.3 witnesses are GREEN with substantive assertion bodies and expected values diff-identical to dispatch base. If harness-only plumbing changes, report it separately and prove it does not alter oracle content.
- **Cross-domain proof:** ship, commodity, and freighter clearing GREEN through canonical weighting; order invariance, weighted ties, exact DecimalField apportionment, and deterministic replay each have named passing evidence.
- **Semantic-preservation proof:** report that integer arithmetic, eligibility, epsilon behavior, tie-break ordering, DecimalField exactness, and replay semantics are unchanged, with focused tests that would falsify each relevant dimension.
- **Structural proof:** run the current procedure-of-record workspace certificate and report the exact summary. The expected line is **0 failed**; any failure is STOP, not inherited-baseline prose. Focused clearing/profile tests plus doctrine/orientation/anchor/handoff/agent-delta/diff checks and hosted Doctrine Scan must also be green.
- **Scope proof:** exact changed-file census; no gate-code edits, no Vector CostBand/banked-performance implementation, no unrelated Phase-13 cleanup, no pointer/closeout/engineering-review work.
- **Hosted/routing proof:** return exact authored/dispatch base distinction, tested_code_sha/final head, HD receipt, all hosted run IDs, evidence artifact/index updates, and fresh body/head-bound `/clearance` plus `/relay-lint`. Remain PROBATION for orchestration/DA review; do not merge or graduate.
