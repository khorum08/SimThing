---
rung: DERIVED-SPAN-PROJECTION-INVALIDATION-0
kind: rung
track: 0.0.8.7
base_sha: 8fe0e4e3543538d30aa7e7e755c01b36d69fe490
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "DA ruling 5311422584 graduated 7.8, discharged its binding row, advanced the pointer to 7.8a, and authorized orchestration to author/self-clear this handoff and relay it for pre-dispatch review. DA pre-dispatch ruling 5311512352 APPROVED Frontier dispatch with A1 folded here before coding: StemThing-B's partial-reconciliation forward-bind is an analysis obligation only; the 7.8a span/profile descriptors and dependency-key vocabulary must remain consumer-agnostic and B's reconciliation loci must be expressible without B-specific widening, or the lane STOPs on the exact inexpressible locus. Fresh orchestrator ORIENT-RECEIPT 730e79491a77 at rule stamp 61818ff7d4adda84. 7.8a is deliberately before 7.9 so the fractal witness consumes this substrate instead of minting an overlay-local lowering. Invalidation is source-blind; materialization is derived cache only; resolve_owner() survives as query/oracle; no owner GPU identity plane before 9.2. OVERLAY-PEER-AUTHORITY remains live until 10.1."
surfaces: ["crates/simthing-core/src", "crates/simthing-spec/src", "crates/simthing-kernel/src", "crates/simthing-gpu/src", "crates/simthing-driver/src", "crates/simthing-sim/src", "crates/simthing-core/tests", "crates/simthing-spec/tests", "crates/simthing-kernel/tests", "crates/simthing-gpu/tests", "crates/simthing-driver/tests", "crates/simthing-sim/tests", "crates/simthing-workshop/tests", "scripts/ci/overlay_germ_archaeology_census.tsv", "scripts/ci/overlay_germ_archaeology_census_universe.txt", "scripts/ci/test_inventory.tsv", "scripts/ci/anchor_reach_log.tsv", "docs/tests"]
forbidden: ["per-generation O(depth x descendants) ancestor rewalk, per-descendant semantic EffectiveProfile instances for homogeneous spans, or leaf-stamped copies used as the physical lowering", "writer-subsystem/source-dependent invalidation such as match change_source; invalidation keys authoritative changed loci only", "runtime-mutable DerivedDependencyIndex/registry/service, dynamic dependency discovery in the hot path, hidden listener/manager, or runtime semantic/profile mint", "dense materialization becoming semantic ownership or truth; deleting/rebuilding the materializer may change performance but never semantics", "universal value resolver or collapse of distinct overlay/owner/capability/field combine laws into one semantics; this rung supplies physical spans/invalidation only", "owner GPU identity plane, OwnerRef interning, owner-column mint, or replacement/removal of resolve_owner() before 9.2", "overlay-local projection/invalidation lowering, new overlay manager, second generation/epoch/swap authority, direct world mutation, or bypass of 7.8 CrossingConsequenceBinding", "7.9 fractal closure semantics/telemetry/cycle scenario, 8.x, Vector CostBand, StemThing-B implementation, navigation research, speculative compaction beyond measured materializer evidence", "editing binding_conditions.tsv/orientation/ladder/pointer/workflows/scans/allowlists/gate code, retiring OVERLAY-PEER-AUTHORITY, or hand-editing the 7.6 census universe pin"]
required_checks: ["fresh Frontier coding ORIENT-RECEIPT at rule stamp 61818ff7d4adda84 + rendered HD-RECEIPT + every REQUIRED-ANCHOR ACK before edits", "before edits, cite the landed 6.4 logical-identity/remap substrate, 6.5 residency/range vocabulary, current resolve_owner() oracle/batch surfaces, graduated overlay inheritance routes, 7.6a facility generation discipline, 7.8 consequence door, and the StemThing-B forward-bind in docs/stead_stemthing_unification.md; return the exact production call graph and identify the current O(depth x descendants) path being replaced", "implement ONE generic physical subtree-span substrate over logical identity + admitted range vocabulary: homogeneous regions intern to shared EffectiveProfile descriptors, local semantic divergence splits/narrows child spans, and profile identity is independent of physical row/slot order", "implement a session-frozen DerivedDependencyIndex mapping authoritative changed loci (logical identity + PropertyId + role + optional binding/profile id) to dependent effective spans, STEAD/PALMA/Gu-Yang registrations, and derived work; invalidation must be source-blind and precise, never a global change scan; descriptors/key vocabulary remain consumer-agnostic, and the return states StemThing-B partial-reconciliation loci are expressible in that vocabulary or STOPs on the exact inexpressible locus without widening or B implementation", "downward dirty-span propagation and epoch invalidation must update only affected span/profile metadata; no per-generation full ancestor walk and no second epoch/generation authority", "dense per-row materialization is optional derived cache only where measured kernel economics justify it; prove delete/rebuild of the cache preserves bit-identical semantics and that authoritative descriptors can rematerialize after physical remap", "owner effective-view batch resolution may consume the substrate only where useful, but resolve_owner() remains the CPU query/oracle; capability/tier projection binds only where the same span shape genuinely applies; do not mint owner GPU identity or interning", "prove million-row homogeneous subtree scales as O(distinct profiles/spans) semantic descriptors and invalidation work rather than O(descendants); report profile/span counts and changed-span work for homogeneous and locally divergent fixtures", "production falsification must cover FIVE exact cases: descendant-scale profile/span explosion on a homogeneous subtree; O(depth x descendants) per-generation rewalk; writer-subsystem/source branch in invalidation; runtime-mutable dependency registry/service; dense materializer becoming semantic authority. Each must RED for its named reason on the production construct, not via inert refuse_* helpers", "run focused span/invalidation tests, forced physical-remap identity parity, relevant owner/oracle + overlay-inheritance regressions, inventory drift, agent scan, relay-lint, orchestrator-owned clearance and hosted Doctrine; if overlay census discovery changes, use sanctioned clean-tree --repin then --check, never hand-edit the universe pin"]
stop_conditions: ["effective semantics cannot be represented as O(distinct-profile) spans without per-descendant semantic instances or a per-generation ancestor rewalk", "precise invalidation requires branching on writer/source subsystem, runtime dependency discovery, a global scan, or a mutable registry/service", "materialization cannot remain a deletable/rebuildable derived cache and instead becomes semantic ownership or durable physical-row identity", "owner consumption would require an owner GPU identity plane/interning before 9.2 or removal of resolve_owner() as query/oracle; likewise STOP on the exact StemThing-B partial-reconciliation locus if it cannot be expressed in the consumer-agnostic admitted key vocabulary without B-specific widening", "implementation requires overlay-local lowering, a second epoch/swap/generation authority, direct world mutation, or reopening graduated 7.7/7.8 semantics", "completion requires 7.9/8.x/Vector CostBand/StemThing-B work, rule/gate edits, peer-authority retirement, unclassified census drift, or speculative optimization not required by the exit proof"]
---
## BUILD
- Implement only 7.8a: one generic physical subtree-span + source-blind invalidation substrate over graduated logical identity/range vocabulary.
- Intern already-composed `EffectiveProfile` descriptors for homogeneous spans; narrow/split only on real semantic divergence; map authoritative changed loci through a frozen `DerivedDependencyIndex` to precise dependent spans/field registrations/derived work.
- Keep dense row materialization optional and derived; preserve `resolve_owner()` as query/oracle and leave all consumer-specific semantic combine laws with their owners.
## FENCES
- Physical substrate, not a universal semantic resolver. One generation discipline, logical identity, source-blind invalidation, frozen admission metadata, no mutable manager/service.
- No per-descendant semantic stamping, no global/per-generation ancestor scan, no owner GPU identity plane, no overlay-local lowering, no successor-rung semantics.
- 7.9/8.x/Vector CostBand/StemThing-B remain fenced; `OVERLAY-PEER-AUTHORITY` remains live.
## EXIT-PROOF
- Million-row homogeneous subtree: O(distinct profiles/spans) descriptors and hot reads, never O(descendants) semantic instances or O(depth x descendants) rewalk; local divergence dirties only dependent spans.
- FIVE mandatory production falsifiers RED for exact reasons: span/profile explosion; ancestor rewalk; writer-source invalidation; mutable registry/service; semantic materializer. Forced physical remap preserves effective semantics, and deleting/rebuilding any dense cache changes performance only.
- Return PROBATION / proof-present / DA-review-pending with exact head/base, fresh coding orientation + HD receipt + ACKs, production call graph, profile/span/invalidation measurements, five falsifier reasons, remap/cache parity, StemThing-B expressibility statement (or exact-locus STOP without widening), focused/inherited batteries, census delta if any, inventory, relay-lint, clearance and Doctrine. Do not merge, move pointer, retire peer authority, or begin successors.
