---
rung: GENERATION-CRITICAL-PATH-BASELINE-0
kind: rung
track: 0.0.8.7
base_sha: 648fe0b418ec7e04a21502a103ea50ab4b25c022
audience: coding
model_tier: std
expected_route: DA-RESERVE(binding)
owner_approved: true
owner_notes: "Owner/DA authority Board 5472516590; canonical Phase-14 law Board 5471915320 rev 5; 14.0 DA-GRADUATED in #1904 @ 3cc86590 and stamped by #1905 onto live master 648fe0b418ec7e04a21502a103ea50ab4b25c022. This rung is measurement plus clearing-door census only: the baseline is a comparator and NEVER a go/no-go gate. The Owner GERM MANDATE and frozen-clearing-semantics law bind the phase but 14.1 implements no resident germ. Orchestrator ORIENT-RECEIPT e6bbcc1d103b; rule stamp 6e41e33143db01ef. Closeout hold 5466933355 remains behind Phase 14."
surfaces: ["crates/simthing-workshop/src/generation_critical_path_baseline.rs", "crates/simthing-workshop/src/generation_critical_path_baseline_report.rs", "crates/simthing-workshop/src/lib.rs", "crates/simthing-workshop/tests/generation_critical_path_baseline_0.rs", "crates/simthing-workshop/tests/generation_critical_path_baseline_reports.txt", "docs/tests/generation_critical_path_baseline_0_results.md", "docs/tests/current_evidence_index.md", "scripts/ci/test_inventory.tsv", "scripts/ci/closeout_artifacts.tsv", "scripts/ci/anchor_reach_log.tsv"]
forbidden: ["any production-source change in simthing-core, simthing-kernel, simthing-spec, simthing-gpu, simthing-driver, simthing-sim, simthing-feeder, simthing-clausething, or simthing-mapeditor; 14.1 observes the live path and may not repair, optimize, wrap, or relocate it", "any change to clearing semantics, score bits, score ordering, equality bands, scope grouping, requested totals, integer apportionment, fractional-remainder ordering, generation-rotated exact ties, grants, unresolved U, replay, generation authority, or structural-consequence law", "any resident-clearing implementation, GPU clearing kernel, compact-identity dictionary, TreeRealmId, ExecutionIncarnation, TreeExecutionContext, RealmQualified, SeamFactId, adapter qualification, cutover posture, or other 14.2-14.6 work", "any benchmark threshold, regression budget, pass/fail speed target, performance ratio, or wall-clock value used as a portable CI gate or as authority to cancel, narrow, or redirect the Owner-ruled Phase-14 placement", "any fabricated, extrapolated, estimated, or substituted timing in place of a measured leg; missing GPU access, unavailable counters, or inability to isolate the ordinary path is a STOP report, not permission to invent a proxy", "any raw-id injection, unsafe constructor, test-only identity wrapper, process-global uniqueness assumption, or synthetic overlapping-id fiction; the overlapping-local-id workload must use ordinary per-tree load/admission doors", "any new global mutable registry, host-wide clearing lock, all-tree synchronization, second IntegrationSchedule, second generation authority, scheduler, remote transport, network identity, or per-SimThing realm/host/device field", "FieldSweep tiling, plane carry, dispatch elision, subtree parallelism, corpus pipeline, Vector CostBand, remote networking, async-streaming default, Gu-Yang remeasurement, or any other fenced performance-track implementation", "any Cargo.toml, Cargo.lock, .github/workflows/**, scripts/ci/**/*.sh, scripts/ci/**/*.py, pointer, workplan graduation/stamp, orientation-guide, closeout apply/reap, or next-rung authoring change from coding"]
required_checks: ["before edits render/read this handoff; obtain a fresh coding-role ORIENT-RECEIPT against the exact merged dispatch master named by the Board dispatch, carry the HD-RECEIPT, and ACK every rendered REQUIRED-ANCHOR", "FIRST STEP publish a mechanical host-clearing-door census from the exact base before authoring the instrument: every public or re-exported clearing entry, path and symbol, generation-authority form, current callers, ordinary/oracle posture, and required 14.6 disposition; it MUST include generationless clear_reduced_owner_channels at crates/simthing-spec/src/spec/constrained_clearing.rs:420 and its re-export", "FIRST STEP trace the canonical ordinary generation critical path end to end and name exact measurement boundaries before collecting numbers; no leg may be hidden inside another, double-counted, or inferred by subtraction without the raw enclosing and component observations", "measure and report each leg separately: claim-production completion; GPU-to-host synchronization/readback; host conversion/grouping; EML scoring; score sorting/banding; integer apportionment; grant/result construction; host-to-GPU upload; N+1 launch delay; CPU schedule/replay recording; lawful structural-consequence work", "for every timing artifact record one complete reproducibility envelope: tested commit, UTC date, CPU, GPU, adapter/backend and driver, OS, compiler/toolchain and profile, workload cardinalities and deterministic seed, warm-up count, sample count, median, p95 and variance or dispersion, bytes read back, bytes uploaded, and exact command", "exercise realistic cardinalities and deterministic stress through at least 1,000, 10,000, 100,000, and 1,000,000 claims, with scopes, supplies, claimants, and granters stated; report allocation/setup separately from steady-state generation work", "measure all four multi-tree shapes independently: one large tree; many independent small trees; multiple trees at divergent generations; and two trees with overlapping local raw ids constructed through ordinary per-tree load/admission doors; report any global singleton, host-wide lock, all-tree synchronization, allocator, schedule, or generation-coupling observation", "prove the overlapping-id fixture is lawful rather than raw-id fiction by recording the exact ordinary construction/loading door and showing the same raw local value is interpreted only under distinct tree contexts; if the current tree has no lawful ordinary door, STOP and return that mechanically demonstrated blocker to orchestration and DA", "preserve measurement neutrality: the instrument consumes current public/runtime surfaces and may add only workshop/test observation code; compare instrumented and uninstrumented canonical outputs or seals to show the observation layer does not change clearing results", "publish raw per-sample records or a lossless report artifact plus summarized decomposition tables; reconcile the end-to-end interval with the named legs and explicitly expose instrumentation overhead or unattributed remainder instead of forcing arithmetic closure", "update the one closeout_artifacts.tsv ledger rather than minting another performance ledger: replace the constrained_clearing.rs owed-measurement marker with a dated measured:<date>@<tested-sha> entry naming the instrument and summary, and lease the new instrument/report artifacts with dated measured provenance; leave unrelated debt rows unchanged", "make structural assertions only about completeness, provenance, door enumeration, workload construction, and presence of all required legs/envelope fields; no timing number, ratio, percentile, or platform-specific result may determine a test or CI verdict", "run the focused workshop measurement/inventory tests, cargo check for simthing-workshop, existing constrained-clearing and replay seals needed to prove neutrality, test-inventory and ledger lifecycle checks, Agent Scan, doctrine/anchor/doc-budget/diff checks, hosted Doctrine Scan/Exec, then fresh exact-head /clearance and /relay-lint; any red is a STOP under the ZERO-RED LAW", "return PROBATION / proof-present / DA-review-pending with exact base/head, handoff and orientation receipts, anchor ACKs, complete door census, path diagram and leg definitions, environment envelopes, raw and summarized measurements, four multi-tree results, million-claim result, neutrality proof, measured ledger diff, exact changed-file census, commands and hosted run IDs; coding never merges, graduates, moves the pointer, or begins 14.2"]
stop_conditions: ["the ordinary generation path cannot be observed without changing production code, clearing semantics, scheduling authority, or generation authority", "any required leg cannot be isolated or bounded truthfully and would have to be guessed, extrapolated, or silently folded into another leg", "the benchmark would require a new runtime path, GPU kernel, resident clearing candidate, identity vocabulary, global registry, host-wide lock, or other 14.2+ implementation", "the two-tree overlapping-local-id workload cannot be built through an existing ordinary per-tree load/admission door without raw-id fiction", "the million-claim workload cannot be run on the measurement environment or the environment cannot supply a truthful GPU/driver/backend envelope; return the exact resource or instrumentation blocker rather than lowering the requirement", "instrumentation changes canonical results, replay, ordering, grants, U, schedule records, or structural products", "the host clearing-door census is incomplete, the generationless clear_reduced_owner_channels door or re-export cannot be accounted for, or a public door's current caller/posture is ambiguous", "any wall-clock result is proposed as a portable gate, go/no-go decision, semantic justification, or authority to narrow the ruled cutover", "any unrelated closeout ledger row, production path, manifest, lockfile, gate/workflow, pointer, closeout, fenced performance seed, or next-rung surface must change", "any focused, ledger, inventory, doctrine, orientation, hosted, clearance, or relay-lint check becomes red"]
---
## BUILD
- **Census before instrument.** Trace every host clearing entry and re-export on the exact dispatch base. The table must include `clear_constrained_claims_at_generation`, `clear_reduced_owner_channels_at_generation`, and the generationless `clear_reduced_owner_channels` compatibility door, plus any additional public door found mechanically. For each, record its exact callers and whether 14.6 must delete it or narrow it behind `CpuVendorizedOracle`; do not perform that disposition now.
- **Measure the actual brake, not a surrogate.** Build a workshop-homed instrument around the ordinary current generation path. It must time claim-production completion, readback/synchronization, host conversion/grouping, CPU EML score evaluation, sorting/banding, integer apportionment, result construction, upload, N+1 launch delay, schedule/replay append, and lawful structural consequence work as separate named legs. Keep setup/allocation and reporting overhead outside steady-state measurements.
- **Carry complete evidence envelopes.** Every report row identifies the exact tested commit and machine/software envelope, deterministic workload seed, warm-ups, samples, median, p95/dispersion, bytes in both transfer directions, and the exact invocation. Keep raw samples or a lossless machine-readable/text report so summaries remain auditable.
- **Scale and separate tree shapes.** Run realistic progression through one million claims, then run one large tree, many independent small trees, divergent-generation trees, and two trees sharing a raw local-id value through ordinary per-tree construction/loading. The multi-tree runs must reveal rather than hide process-global allocation, host-global locks, all-tree waits, shared schedule, or shared-generation assumptions.
- **Comparator only.** Present the current path's cost decomposition and likely dominant legs as dated evidence. Do not declare PASS/FAIL from speed, cancel the ruled cutover, create thresholds, or optimize the path. The Owner reviews the decomposition as information; emphasis within 14.2-14.5 may later adjust, while placement and cutover remain ruled.
- **Ledger the measurement.** Use `scripts/ci/closeout_artifacts.tsv` as the one ledger. Convert the existing CPU-host-constrained-clearing owed-measurement row to dated `measured:` evidence and lease the new instrument and report artifacts. Do not alter the Gu-Yang or unrelated performance debts.
- Return **PROBATION / proof-present / DA-review-pending**. Coding does not merge, graduate, move the pointer, author 14.2, or invoke closeout.

## FENCES
- **No production delta.** Workshop/test instrumentation may observe the current implementation; no production crate, manifest, lockfile, gate, workflow, or runtime authority changes.
- **Semantics are frozen.** This rung measures the exact graduated clearing law. It may not simplify, re-express, repair, specialize, or tune any result-affecting behavior.
- **No portable timing law.** Wall-clock values are dated facts about one reproducibility envelope. CI may test evidence completeness and deterministic outputs, never performance magnitude.
- **Tree-local truth, no identity theatre.** The multi-tree census must expose current assumptions without adding realm metadata to base rows, pretending process-global ids are realm-qualified, or manufacturing duplicate raw ids through test-only escape hatches.
- **Phase boundaries hold.** The resident constrained-clearing germ, realm/seam vocabulary, ordering-law audit, wide-integer resident apportionment, parity matrix, adapter qualification, and cutover all begin after this rung.
- **Owner fences hold.** Closeout and the unrelated performance seed remain held behind Phase 14; remote networking and async-streaming-default design remain future work.

## EXIT-PROOF
- **Door census:** a complete, mechanically sourced table of public/re-exported clearing doors, callers, authority posture, and 14.6 disposition, explicitly covering generationless `clear_reduced_owner_channels`.
- **Critical-path decomposition:** all eleven required legs have explicit boundaries, raw observations, summary statistics, and an end-to-end reconciliation with overhead/unattributed time stated honestly.
- **Reproducibility:** every timing artifact carries the full hardware/software/workload envelope, exact command, tested SHA, raw sample provenance, median, p95/dispersion, and transfer byte counts.
- **Scale and multi-tree:** realistic progression reaches 1,000,000 claims; one-large, many-small, divergent-generation, and lawful overlapping-local-id two-tree workloads are separately reported, including any global-coupling observations.
- **Neutrality:** canonical clearing outputs, replay/schedule records, and any exercised structural products are identical with and without observation; no production file is in the changed set.
- **Ledger:** the existing constrained-clearing debt is now dated `measured:` evidence and every new instrument/report is leased in the one closeout ledger; unrelated rows are byte-identical.
- **Routing:** only authorized workshop/test/docs/data-ledger surfaces changed; focused checks and hosted doctrine are green; exact-head `/clearance` and `/relay-lint` are fresh. Return PROBATION for orchestration and DA review; no merge or pointer movement by coding.
