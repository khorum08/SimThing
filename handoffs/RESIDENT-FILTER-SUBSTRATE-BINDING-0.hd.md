---
rung: RESIDENT-FILTER-SUBSTRATE-BINDING-0
kind: rung
track: 0.0.8.7
base_sha: 3b7d8bd91a81ad6c6de01d895e7d8037f2e96000
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(binding)
owner_approved: true
owner_notes: "DA graduation ruling 5535368453 graduated 15.5 and opened 15.6. Binding scope is the exact 15.6 ladder row, engineering remand E3/E5/E7, DA binding 5529092629 for ResidentMarketQualification, and the 15.5 graduated cross-product witness which must rerun on the real session RF arena. 15.7 remains fenced."
surfaces: ["crates/simthing-driver/src/**", "crates/simthing-gpu/src/**", "crates/simthing-kernel/src/**", "crates/simthing-core/src/**", "crates/simthing-spec/src/**", "crates/simthing-sim/src/**", "crates/simthing-workshop/tests/**", "docs/tests/**", "docs/sanctioned_surface.md", "scripts/ci/allow/kernel_surface.txt", "scripts/ci/constitutional_surfaces.tsv", "scripts/ci/test_inventory.tsv", "scripts/ci/triage_log.tsv", "scripts/ci/anchor_reach_log.tsv", "scripts/ci/authorized_renames.tsv"]
forbidden: ["retaining the synthetic one-resource flat-star as an ordinary production path", "private production continuous WorldGpuState, host continuous_values market vector, per-market-dispatch allocation-plan upload, or host-authored continuous_weight on the production path", "resident_qualified: bool or an enum/tag standing in for bound admission evidence", "silently narrowing the admitted u32 Draw/request domain to evade the E5 2^24 falsifier", "fresh market executor reconstruction during E7 rebind or loss of realm/incarnation/generation/live-head/semantic identity/persistence/provenance continuity", "new market, clearer, disburser, score system, host policy authority, second exact projection, or growth of the peer/economic authority census", "changing graduated 15.5 spatial/temporal semantics, Q149 mechanics, exact residue law, 15.2 persistence law, 15.3 consequence law, or 15.4 oracle quarantine", "implementing 15.7 authority lifetime, canon rewrite/adoption, RESOURCE-RESPONSE-COMPRESSION-0, pointer movement, graduation, or closeout", "any coding edit to .github/workflows/**, scripts/ci/**/*.sh, or scripts/ci/**/*.py"]
required_checks: ["render/read this handoff first; carry machine HD-RECEIPT plus fresh coding ORIENT under the current rule stamp and ACK every rendered REQUIRED-ANCHOR", "ARCHAEOLOGY FIRST: map every production resident market input from session RF arena state through born P/F, AllocatorWeight, live AllocatedFlow, precedence, exact projection, semantic rows, topology identity, registry/layout generation, and current plan compile/upload lifetime; census all synthetic flat-star/private-plane/host-vector/per-dispatch-plan/host-weight production uses", "kill the synthetic production substrate mechanically: prove production-use counts are exactly zero for private continuous WorldGpuState, host continuous_values market vector, per-market-dispatch allocation-plan upload, and host-authored continuous_weight; plan compilation is lawful only at admission/topology rebind, never per generation", "bind exact constrained projection to the ordinary session RF arena's actual admitted topology and live born state; falsifier: perturb live resident AllocatorWeight after any stale host binding exists and prove exact output follows the live arena cell", "BOUND ACCEPTANCE REQUIREMENT 5529092629 VERBATIM IN EFFECT: ResidentMarketQualification is sealed consumer-owned admission evidence, not an enum replacing a boolean; its seal/digest covers at minimum market semantic identity, resource/scope/Draw shape, RF arena/topology binding, precedence lowering, continuous-policy/EML identities, registry/layout generation or equivalent admitted shape, and exact-projection ABI; changing authored market, registry, or topology invalidates the token", "delete resident_qualified: bool; prove one genuinely authored NON-implicit market qualifies and executes resident; a market that cannot completely lower typed-refuses without growing a score system or alternate semantics", "E5 FIRST: plant r1=16777217, r2=16777216, S=1 against resident and exact oracle before remedy; if resident ties while oracle does not, STOP for explicit Owner/DA numeric-domain remedy choice; engineering may not narrow u32 Draw/request semantics for convenience", "E7: witness lawful growth above originally admitted capacity through identity-preserving rebind OR prove such growth unrepresentable at admission; no middle state; if rebind is required preserve TreeRealmId, ExecutionIncarnation absent true migration, GenerationStamp, live-head continuity, semantic owner/resource/scope/draw identity, persistence deformation bindings, and pending exact provenance", "rerun the full 15.5 canonical cross-product on the REAL session arena substrate: nonzero G + nonzero U + nonzero authored N+1 demand + changed child granter + changed semantic scope + production resident runtime; include identity demand8 and half-deformation demand5 CPU/resident parity", "prove zero new economic authority and no peer-authority census growth; preserve work-conserving E6 law with reservation only from real commitment/holding facts", "rerun frozen 14.5 parity, 14.6 cutover/causal/self-consumption/tree-isolation, 15.0 theorem/census, 15.1 filter closure, 15.2 persistence, 15.3 consequence ingress, 15.4 oracle quarantine, 15.5 recursion-axis/E6/cross-product witnesses, qualification tuple/fingerprint, generation pacing, and authority census", "run focused 15.6 referee, touched-package checks, full structural battery, inventory/drift, constitutional/census selftests, lifecycle/sanctioned-surface/anchor checks, detachability, Agent Scan, hosted Doctrine Scan/Exec, then fresh exact-head /clearance and /relay-lint", "return PROBATION / proof-present / DA-review-pending / UNMERGED with exact base/head, HD/ORIENT receipts, archaeology map, four absence-proof counts, real-arena call graph, qualification seal field table and invalidation mutants, authored-market resident witness, E5 falsifier transcript plus explicit STOP/ruling posture if it reproduces, E7 rebind transcript/preservation table, 15.5 cross-product rerun transcript, changed-file census, FULL ZERO-RED certificate, hosted IDs, clearance and relay-lint"]
stop_conditions: ["any of the four synthetic production shapes cannot be reduced to zero ordinary production uses", "binding to the real session RF arena requires a new market/score/adapter/host authority instead of consuming existing admitted state", "ResidentMarketQualification cannot seal the complete bound lowering or a token survives authored-market, registry, or topology mutation", "E5 falsifier reproduces the documented 2^24 divergence and coding attempts a numeric remedy before explicit Owner/DA ruling", "E7 growth is representable but cannot be handled by identity-preserving rebind, or a proposed rebind reconstructs a fresh economic executor", "the graduated 15.5 cross-product cannot rerun on the real session arena substrate", "any required frozen witness, structural certificate, doctrine/anchor/inventory check, hosted gate, clearance, or relay lint is red"]
---

## BUILD

This is the physical-homing rung. 15.5 corrected the semantics; 15.6 must make those semantics execute on the ordinary session RF arena rather than a synthetic resident flat-star beside it.

The production exact projection consumes the session's actual admitted topology, born P/F, live AllocatorWeight, live AllocatedFlow, precedence lowering, and canonical semantic rows. Private production field planes, host market vectors, per-generation plan recompilation/upload, and host-authored continuous weights must disappear as production authorities, not merely become unused by one witness.

Qualification is evidence of one exact lowering. `ResidentMarketQualification` is a sealed consumer-owned token over the authored market plus every semantic binding needed to prove the same resident operator will execute it. It must invalidate when authored market, registry/layout, or topology changes. A boolean, enum, compatibility tag, or score-style eligibility system is not a substitute.

E5 is a semantic decision gate. Plant the 16777217/16777216/S=1 resident-vs-oracle falsifier before changing numeric representation. If it reproduces the documented float-collapse divergence, STOP for Owner/DA ruling. Narrowing the admitted u32 Draw/request domain is forbidden absent that explicit ruling.

E7 is identity continuity, not executor replacement. If lawful post-open growth can exceed admitted resident shape, rebind the same execution identity and preserve realm/incarnation/generation/live-head/semantic ids/persistence/provenance; otherwise prove such growth is unrepresentable by admission law. There is no partially stale state.

The 15.5 canonical cross-product must rerun through this real substrate. Passing isolated real-arena allocation tests is insufficient if the combined spatial+temporal object no longer executes.

## FENCES

- one ordinary session RF arena; one resident operator; one exact projection.
- no private flat-star production authority survives behind aliases or convenience helpers.
- no silent numeric-domain narrowing; E5 waits for explicit ruling if RED.
- no fresh executor on E7 rebind; identity continuity is load-bearing.
- 15.7, canon adoption, compression, pointer movement, graduation, and closeout are out of scope.
- coding does not merge or infer the next rung.

## EXIT-PROOF

Four synthetic production-use counts are zero; exact output follows live resident arena state; `ResidentMarketQualification` is complete sealed bound evidence and invalidates on authored-market/registry/topology mutations; one non-implicit authored market resident-executes; E5 is falsified first and any resulting semantic remedy is explicitly ruled before implementation; E7 is either impossible-by-admission or identity-preserving rebind with the full preservation list; the graduated 15.5 cross-product reruns on the real session arena; authority census does not grow; all frozen witnesses and the full structural certificate are ZERO-RED. Coding stops unmerged for DA review.