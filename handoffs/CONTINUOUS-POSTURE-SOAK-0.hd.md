---
rung: CONTINUOUS-POSTURE-SOAK-0
kind: rung
track: 0.0.8.7
base_sha: ea18d190
audience: coding
model_tier: std
owner_approved: true
expected_route: DA-RESERVE(gate-wiring)
owner_notes: "DA ruling 5180385017 graduates 6.2b (#1621 @ 1a520eee; stamp #1622 @ ea18d190) and advances the pointer to 6.3, but explicitly says pointer movement is not dispatch. Standing delegation authorizes orchestration to author this handoff because exit-proof coverage does not flag 6.3 and #1601 already widened its proof. PRE-DISPATCH only: draft, self-clear, relay to DA; no coding until DA reviews/merges and orchestration explicitly dispatches. Design lane is Std — Grok. The 6.2b measure-first telemetry at 748a8222 is inherited historical evidence and is not re-paid. DA also corrected the prior shorthand: preserve the actual boundary stage ordering/structure; claim an exact stage count only if mechanically proven. The inherited TDR-shaped threshold-stress device loss at >=1000 slots is now a 6.3 soak finding to reproduce/characterize honestly, never hidden by silently lowering scale."
surfaces: ["crates/simthing-core/src", "crates/simthing-spec/src/spec/owner_channel_rf.rs", "crates/simthing-spec/src/spec/owner_silo_disburse_down.rs", "crates/simthing-spec/src/spec/runtime_local_allocation.rs", "crates/simthing-spec/src/spec/runtime_rf_tick.rs", "crates/simthing-spec/tests", "crates/simthing-sim/src/boundary.rs", "crates/simthing-sim/src/resolution_site.rs", "crates/simthing-sim/tests", "crates/simthing-driver/src", "crates/simthing-driver/tests", "crates/simthing-kernel/src", "crates/simthing-gpu/src", "scripts/ci/test_inventory.tsv", "docs/tests", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md"]
forbidden: ["staleness stored as a per-node property, retained history log, CPU cache, mirror truth, or semantic health-monitor service; its only runtime representation is the derived STEAD field column", "whole-lattice staleness registration/sweep, or any async-inert world allocating the staleness column, registration, dispatch, or retained side state", "a second execution model, resolution-site-specific meaning, second BoundaryRequest vocabulary, second transport/queue/replay recorder, or ambient-timing determinism", "an authored cascade-depth bound, convergence loop, extra recursive event transport, or more than the existing once-per-generation receive/CostBand/EML/originate/route/receive pacing", "moving allocation/structural mutation out of the barrier, deleting/reordering boundary stages, synthesized origin, direct affects attachment, or weakening 6.0/6.0b/6.1/6.1b/6.2/6.2b laws", "scenario/domain witnesses, shipped-scenario tuning, opportunistic movement/contention/5.11/census work, gate weakening, or an unrelated threshold-pipeline redesign solely to make the soak green"]
required_checks: ["fresh coding ORIENT-RECEIPT + current ANCHOR-ACKs + HD-RECEIPT quoted before edits", "run an N-generation synthetic continuous-posture soak under forced lag in BOTH ResolutionSite modes from identical seeds; BoundaryRequest streams are bit-identical and a planted mode-divergence defect REDs", "drive reduce_owner_channel_rf and reconstruct_owner_channel_rf_map over a scaling matrix; measure OwnerChannelRfSteadSurface retained rows as exactly own aggregates plus ownership crossings, with a planted product-form growth defect RED", "derive staleness as parent_generation - latest_integrated_child_stamp into ONE f32 STEAD lane per admitted slot; prove no per-node staleness property/history/mirror is admitted", "seed staleness sweeps only at each retained crossing boundary_simthing_id and bound by authored horizon; deterministic work/visit counts scale with crossings x horizon-neighbourhood and a whole-lattice mutant REDs", "world with no async seam has zero staleness registration, zero staleness column bytes, zero staleness dispatches, and no retained staleness side state", "soak the closed causal cycle receive -> CostBand -> EML -> originate -> route -> receive over many generations under load; generation pacing remains once per direction per generation with no authored cascade bound", "replay the forced-lag run from the existing integration schedule/injection log bit-exactly; paced posture remains behaviorally unchanged on scenario-neutral regression", "reproduce/characterize the inherited >=1000-slot threshold-stress device loss on untouched master and final head; do not misattribute it to 6.3 or silently shrink the claimed scale"]
stop_conditions: ["OwnerChannelRfSteadSurface retained state cannot stay crossing-bounded and drifts toward nodes x owners x resources", "staleness requires a second stored truth, per-node history, service object, or whole-lattice registration rather than a single derived seeded field column", "dual-mode equivalence requires changing semantics, vocabulary, or allocation behavior instead of scheduling/resolution placement", "the closed causal cycle requires a convergence loop, authored cascade-depth bound, second transport, or more than once-per-generation directional execution", "a no-async-seam world cannot remain zero-allocation/zero-dispatch for staleness", "the required forced-lag soak is blocked by the inherited device-loss defect and the only remedy is an unrelated algorithmic redesign; return the exact reproduction/profile instead of weakening the workload", "completion requires downstream movement/contention/5.11/census implementation, invariant change, or gate loosening"]
---
## BUILD
- Add the continuous execution posture as batched generations over the SAME kernel; paced posture remains the default scheduling policy and unchanged in semantics.
- Make async staleness visible only as the derived STEAD scalar `parent_generation - latest_integrated_child_stamp`. Seed at retained owner-channel crossing `boundary_simthing_id`s, horizon-bound the sweep, and allocate/register nothing when no async seam exists.
- Build the N-generation forced-lag soak around the landed 6.0/6.1/6.2/6.2b surfaces: measure retained owner-channel growth, exercise the closed automaton causal cycle, replay the recorded schedule, and run both resolution sites from identical input.
- Preserve the historical 6.2b telemetry evidence and separately reproduce/characterize the inherited threshold-stress device-loss finding without folding an unrelated optimization into this rung.
## FENCES
- One kernel, two scheduling postures; one model, two resolution sites. Staleness is a field magnitude, never an identity/property/service or second truth.
- Seeded/horizon-bounded work only; cost follows crossings x horizon-neighbourhood, never lattice size. No async seam means zero staleness state and work.
- Preserve CostBand/EML/routed-origin semantics, the single integration recorder, barrier-only allocation, and the actual boundary stage ordering/structure. No downstream rung work.
## EXIT-PROOF
- Continuous posture completes an N-generation forced-lag synthetic soak and replays bit-exactly from the existing schedule/log; paced posture remains unchanged.
- OwnerChannelRfSteadSurface growth is measured crossing-bounded, staleness has exactly one derived STEAD-column representation, seeded sweeps reject whole-lattice drift, and inert worlds pay zero.
- Closed-loop and CPU-authoritative resolution produce bit-identical BoundaryRequest streams across the soak, and the receive -> CostBand -> EML -> originate -> route -> receive cycle remains generation-paced under load.
- Inherited device-loss evidence is reproduced/characterized honestly. Return PROBATION / proof-present / DA-review-pending; coder does not `/clearance`, merge, move pointer, or begin successor work.
