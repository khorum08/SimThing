---
rung: GPU-OVERLAY-LIFECYCLE-EXTRACTION-0
kind: rung
track: 0.0.8.7
base_sha: 288a4bb36220a273f29138368f7239e28c30dd8b
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "DA ruling 5290337633 graduated 7.6 (#1748 @ e52b5603; gate #1749 @ 619c15a9; stamp #1750 @ 288a4bb3), advanced the pointer to 7.7, and authorized orchestration to author/self-clear this handoff. 7.6's census is now a durable CI input. The open binding condition from DA 5289831443 is promotion-blocking: OverrideReceived must either gain real override-replacement semantics in the germ or be rejected at admission; it may not remain admitted-and-false. 7.8/7.8a/7.9 remain fenced."
surfaces: ["crates/simthing-core/src", "crates/simthing-spec/src", "crates/simthing-kernel/src", "crates/simthing-gpu/src", "crates/simthing-driver/src", "crates/simthing-sim/src", "crates/simthing-core/tests", "crates/simthing-spec/tests", "crates/simthing-kernel/tests", "crates/simthing-gpu/tests", "crates/simthing-driver/tests", "crates/simthing-sim/tests", "scripts/ci/overlay_germ_archaeology_census.tsv", "scripts/ci/overlay_germ_archaeology_census_universe.txt", "docs/tests", "docs/design_0_0_8_7_rf_arena_modernization.md"]
forbidden: ["second lifecycle/crossing detector, post-crossing dispatcher, OverlayHistory, or peer OverlayManager/ActionThing authority", "OverlayThing-local EML namespace/table/evaluator/cache, duplicate arithmetic semantics, or cache identity outside (ProgramId, ArithSemanticsVersion, LoweringArm, ExecutionShape, BindingABI)", "CPU production lifecycle evaluator, AfterTicks decrement loop, per-generation countdown write, overlay-local epoch selector/freshness bit/swap authority, wall clock, global clock, or foreign-tree generation counter", "dynamic-capacity manager, mid-session semantic-template mint, generic everything-table, or speculative active-list/compaction machinery before admitted-cardinality carry is measured", "durable physical-row identity in semantic/persistence bindings; durable targets must remain logical identity + PropertyId + role/binding identity", "7.8 actuation migration/direct world mutation/foreign resident-plane writes; 7.8a span projection; 7.9 fractal witness; 8.x, Vector CostBand, StemThing-B, or owner GPU identity-plane work", "editing binding_conditions.tsv, orchestrator orientation, workflows/scans/allowlists/gate code, or retiring OVERLAY-PEER-AUTHORITY before 7.8 makes peer actuation uncompilable", "routine numerical transitions emitting CPU semantic deltas or telemetry; CPU-visible OverlaySemanticDelta egress exists only where semantic/persistence/structural visibility is actually required"]
required_checks: ["fresh Frontier coding ORIENT-RECEIPT + rendered HD-RECEIPT + all REQUIRED-ANCHOR ACKs before edits", "retain resolve_overlay_lifecycle() as CPU oracle while GPU becomes production authority for overlay active/current/next state and dissolution decisions; prove oracle/GPU bit identity with a planted production-path divergence RED", "map admitted DissolveCondition forms to canonical EML/existing Phase-5 threshold registrations; expiry fires through the existing crossing surface and a planted second crossing detector REDs for that exact reason", "replace AfterTicks countdown mutation with fail-closed deadline_generation = activation_generation + duration in the owning tree generation authority; compare rather than decrement; forced generation-lag global-clock mutant RED", "for routed duration lifecycle, transport authored duration + provenance only and establish the deadline against the destination tree generation authority; planted foreign-absolute-deadline capture RED under forced generation skew", "reuse canonical EML program identity/registry and physically compatible artifact cache key; planted overlay-local EML table RED; reuse 7.2/7.3 Current/Next/swap with no overlay-local epoch authority", "reuse session-frozen sparse facility residency/capacity accounting; no mid-session semantic-template mint; measure rows/bytes/carry at admitted cardinalities before any compaction and STOP on unexplained material regression", "durable bindings use logical identity + PropertyId + role/binding identity and physical rows only in upload/epoch binding state; planted durable-row-capture mutant RED", "discharge OverrideReceived promotion blocker by either implementing real germ override-replacement or rejecting the variant at admission; if existing authored content or required matching semantics makes that choice non-mechanical, STOP for DA rather than guessing", "prove one-history replay from existing stamped crossing/schedule surfaces, routine transitions require no CPU delta, CPU evaluator is absent from the production path, reconcile the 7.6 census/universe if route drift occurs, run focused+affected batteries and hosted Doctrine, and return 7.7 PROBATION with exact-reason falsifiers"]
stop_conditions: ["implementation requires a second crossing detector/dispatcher, private EML authority, local clock/countdown, durable-row semantic identity, dynamic manager, or CPU production numerical lifecycle authority", "existing Phase-5 crossings plus canonical Current/Next/swap cannot express lifecycle extraction without implementing 7.8 actuation semantics or arbitrary writes", "OverrideReceived cannot be implemented or admission-rejected without a new compatibility/matching/identity ruling; report the exact authored reach and stop", "Routed Lifecycle Epoch Law cannot be satisfied without transporting a foreign absolute deadline or consulting a non-owning generation clock", "performance repair requires compaction/active-list machinery before measurement, or admitted-cardinality carry shows a material unexplained regression", "completion requires 7.8/7.8a/7.9, gate-code/rule-source edits, scan retirement, successor work, or weakening a graduated 7.6 census/falsifier obligation"]
---
## BUILD
- Extract only overlay numerical lifecycle authority: GPU owns active/current/next and dissolve evaluation; `resolve_overlay_lifecycle()` remains the CPU oracle and leaves the production path only after parity.
- Compile lifecycle conditions onto canonical EML + existing Phase-5 crossings; `AfterTicks` becomes owning-tree deadline comparison, and routed durations rebase at destination residency.
- Reuse canonical EML identity/cache, frozen sparse residency, 7.2/7.3 Current/Next/swap, logical-identity bindings, and existing stamped egress. Close the `OverrideReceived` blocker without inventing a fourth lifecycle path.
## FENCES
- One crossing surface, one EML language/registry, one generation authority per owning tree, one Current/Next swap authority. No CPU countdown, private clock, dynamic manager, durable row capture, or OverlayHistory.
- 7.7 does not implement the 7.8 actuation door, 7.8a projection/invalidation, or 7.9 witness; `OVERLAY-PEER-AUTHORITY` remains live until 7.7+7.8 make peer actuation uncompilable.
## EXIT-PROOF
- Oracle/GPU lifecycle decisions are bit-identical; divergence, second-detector, overlay-local-EML, mid-session-template, durable-row, foreign-deadline, and global-clock mutants RED for their intended reasons.
- `AfterTicks` has zero CPU decrement writes; CPU lifecycle evaluation is proof-only, replay remains one-history, and routine GPU transitions produce no CPU semantic delta.
- Sparse carry is measured before compaction; route drift is reconciled through the graduated 7.6 census mechanism; the `OverrideReceived` promotion blocker is demonstrably discharged.
- Return PROBATION / proof-present / DA-review-pending with exact head, receipts/ACKs, parity and mutant evidence, carry measurements, focused+affected batteries, and hosted artifacts. Coding does not merge, close the binding row, move pointer, retire scans, or begin successors.
