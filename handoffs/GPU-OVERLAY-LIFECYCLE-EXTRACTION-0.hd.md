---
rung: GPU-OVERLAY-LIFECYCLE-EXTRACTION-0
kind: remedial
track: 0.0.8.7
base_sha: 9593ca2e658cfa063797e8d74a8cf6b29505a7b0
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "Owner-directed re-cut after Grok PR #1753 @ 1d588bc0 was REJECTED unmerged: production CPU loops were renamed GPU authority; replay/mutants were non-biting; property lifecycle never reached the alleged GPU path; production swallowed deadline overflow. Restart 7.7 from master 9593ca2e. DA 5290429128 + A1-A4 remain binding. #1753 is evidence only; salvage isolated ideas only after re-derivation."
surfaces: ["crates/simthing-core/src", "crates/simthing-spec/src", "crates/simthing-kernel/src", "crates/simthing-gpu/src", "crates/simthing-driver/src", "crates/simthing-sim/src", "crates/simthing-core/tests", "crates/simthing-spec/tests", "crates/simthing-kernel/tests", "crates/simthing-gpu/tests", "crates/simthing-driver/tests", "crates/simthing-sim/tests", "scripts/ci/overlay_germ_archaeology_census.tsv", "scripts/ci/overlay_germ_archaeology_census_universe.txt", "docs/tests"]
forbidden: ["host-side CPU loops/functions/structs named GPU substituting for real GPU execution, or inert refuse_* / error-enum / synthetic-vector proof standing in for production-path falsifiers/replay", "second lifecycle/crossing detector, post-crossing dispatcher, OverlayHistory, or peer OverlayManager/ActionThing authority", "OverlayThing-local EML namespace/table/evaluator/cache, duplicate arithmetic semantics, cache identity outside (ProgramId, ArithSemanticsVersion, LoweringArm, ExecutionShape, BindingABI), or cross-instance CSE claim", "CPU production lifecycle evaluator/countdown, overlay-local current/next/epoch/swap authority, wall/global/foreign-tree clock", "swallowing deadline overflow into u32::MAX/sentinel/default, or property lifecycle lowered with COL_NONE/direction=0/host_value=None/bypassing existing Phase-5 registered values", "dynamic-capacity manager, mid-session semantic-template mint, generic everything-table, or speculative active-list/compaction before carry measurement", "durable physical-row semantic identity; durable targets remain logical identity + PropertyId + role/binding identity", "7.8/7.8a/7.9/8.x/Vector CostBand/StemThing-B/owner GPU identity-plane work, direct world mutation, or foreign resident-plane writes", "editing binding_conditions.tsv/orientation/ladder/workflows/scans/allowlists/gates, retiring OVERLAY-PEER-AUTHORITY, or hand-editing the 7.6 universe pin"]
required_checks: ["fresh Frontier/Sol coding ORIENT-RECEIPT + rendered HD-RECEIPT + every REQUIRED-ANCHOR ACK before edits", "identify the REAL production GPU route in the return: boundary/driver upload -> kernel/simthing-gpu dispatch/pipeline -> resident lifecycle result -> existing structural dissolve apply; if generic GPU machinery is reused with little source diff, show the exact hardware-dispatch trace", "retain resolve_overlay_lifecycle() only as CPU oracle; compare against REAL GPU production decisions with a planted production-path divergence RED", "map admitted DissolveCondition arms to canonical EML/existing Phase-5 registrations; PropertyReaches/Below must consume real resident registered values; AfterTicks uses owning-tree deadline compare and production overflow hard-errors, with threshold-bypass and overflow-sentinel mutants RED", "routed duration transports authored duration + provenance and rebases at destination; foreign-absolute/global-clock mutants RED under forced skew", "reuse canonical EML ProgramId/registry/cache and graduated 7.2/7.3 Current/Next/swap; no cross-instance CSE, local EML table, local swap, mid-session semantic mint, or durable-row capture", "discharge OverrideReceived by real germ replacement OR admission rejection; rejection requires authored-reach/compatibility check and every admission door rejecting it, otherwise STOP for DA", "capture a REAL pre-extraction recording on master 9593ca2e, replay that same artifact post-extraction bit-identically, and separately prove one-history/no OverlayHistory", "all seven required mutants must alter/bypass the real production path and RED for stated reasons; helper functions that merely return named errors do not count", "measure sparse carry rows/bytes/dispatch cost before compaction; route drift uses clean-tree --harvest with reconciliation/classification then --check PASS; run focused+affected batteries and hosted Doctrine"]
stop_conditions: ["no lawful existing GPU execution substrate can host lifecycle decisions without a second crossing/dispatcher or 7.8 actuation semantics", "property-threshold lifecycle cannot consume existing Phase-5 registrations/resident values without new semantic authority", "OverrideReceived choice requires a new compatibility/matching ruling", "pre-extraction recording cannot be produced on 9593ca2e and consumed unchanged post-extraction", "deadline/routed-epoch law requires foreign absolute time or overflow sentinel", "completion requires speculative compaction, 7.8+, rule/gate edits, scan retirement, successor work, or weakening the graduated 7.6 census"]
---
## BUILD
- Re-implement only 7.7 from master: REAL GPU production lifecycle authority; CPU lifecycle remains oracle/referee only.
- Compile lifecycle onto canonical EML + existing Phase-5 registrations/crossings; use owning-tree deadlines and destination rebasing; discharge `OverrideReceived` without a peer lifecycle path.
- Reuse graduated Current/Next/swap and sparse residency; do not inherit #1753's host-side “GPU twin” architecture.
## FENCES
- Actual GPU dispatch is numerical authority. CPU may prepare/upload/apply already-authorized structural results, never decide lifecycle in production.
- Production-path falsifiers only; one EML language, one crossing surface, one owning-tree clock, one shared swap authority.
- 7.8/7.8a/7.9 remain fenced; `OVERLAY-PEER-AUTHORITY` stays live through 7.7.
## EXIT-PROOF
- Real GPU/oracle parity is bit-identical; all seven production mutants RED for intended reasons; property and deadline lifecycle both execute through the real GPU path.
- A recording captured on pre-extraction master `9593ca2e` replays bit-identically post-extraction, independently of one-history proof.
- Overflow hard-fails in production, routed duration rebases, carry is measured before compaction, and any census drift is clean-tree `--harvest` reconciled.
- Return PROBATION / proof-present / DA-review-pending with exact head, receipts/ACKs, GPU dispatch trace, mutant reasons, pre→post artifact/digest, carry, census delta, batteries, hosted artifacts. Coding does not merge, close binding row, move pointer, retire scans, or begin successors.
