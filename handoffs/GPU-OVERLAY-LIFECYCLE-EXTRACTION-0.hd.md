---
rung: GPU-OVERLAY-LIFECYCLE-EXTRACTION-0
kind: remedial
track: 0.0.8.7
base_sha: 9593ca2e658cfa063797e8d74a8cf6b29505a7b0
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "Owner directed post-landing triage after Grok received the Frontier handoff in error. Grok PR #1753 @ 1d588bc0 was audited and REJECTED unmerged: its production boundary renamed host-side CPU lifecycle loops as GPU authority, its replay/mutants were non-biting, property lifecycle never reached the alleged GPU path, and production swallowed deadline overflow. Restart 7.7 from merged master 9593ca2e. DA ruling 5290429128 and A1-A4 remain fully binding. PR #1753 is evidence only; isolated ideas may be salvaged only after re-derivation, never inherited wholesale."
surfaces: ["crates/simthing-core/src", "crates/simthing-spec/src", "crates/simthing-kernel/src", "crates/simthing-gpu/src", "crates/simthing-driver/src", "crates/simthing-sim/src", "crates/simthing-core/tests", "crates/simthing-spec/tests", "crates/simthing-kernel/tests", "crates/simthing-gpu/tests", "crates/simthing-driver/tests", "crates/simthing-sim/tests", "scripts/ci/overlay_germ_archaeology_census.tsv", "scripts/ci/overlay_germ_archaeology_census_universe.txt", "docs/tests"]
forbidden: ["host-side CPU loops/functions/structs named GPU substituting for a real GPU execution path; production lifecycle decisions must be produced by actual GPU dispatch through the existing kernel/simthing-gpu substrate", "inert refuse_* helpers, error-enum-only tests, synthetic expected vectors, or other kabuki standing in for production-path falsifiers/replay", "second lifecycle/crossing detector, post-crossing dispatcher, OverlayHistory, or peer OverlayManager/ActionThing authority", "OverlayThing-local EML namespace/table/evaluator/cache, duplicate arithmetic semantics, cache identity outside (ProgramId, ArithSemanticsVersion, LoweringArm, ExecutionShape, BindingABI), or any cross-instance CSE claim", "CPU production lifecycle evaluator, AfterTicks decrement loop, per-generation countdown write, overlay-local current/next vectors, epoch selector/freshness bit/swap authority, wall/global/foreign-tree clock", "swallowing deadline overflow into u32::MAX/sentinel/default; deadline construction/admission must propagate a hard fail-closed error", "property-threshold lifecycle lowered with COL_NONE/direction=0/host_value=None or otherwise bypassing the existing Phase-5 registration/value path", "dynamic-capacity manager, mid-session semantic-template mint, generic everything-table, or speculative active-list/compaction before admitted-cardinality carry is measured", "durable physical-row identity in semantic/persistence bindings; durable targets remain logical identity + PropertyId + role/binding identity", "7.8 actuation migration/direct world mutation/foreign resident-plane writes; 7.8a span projection; 7.9 fractal witness; 8.x, Vector CostBand, StemThing-B, owner GPU identity plane", "editing binding_conditions.tsv, orchestrator orientation, the 0.0.8.7 ladder/stamp doc, workflows/scans/allowlists/gate code, or retiring OVERLAY-PEER-AUTHORITY before 7.8", "hand-editing overlay_germ_archaeology_census_universe.txt; route drift must clean-tree --harvest re-derive the pin"]
required_checks: ["fresh Frontier/Sol coding ORIENT-RECEIPT + rendered HD-RECEIPT + every REQUIRED-ANCHOR ACK before edits", "identify the actual production GPU path in the return: boundary/driver upload -> kernel/simthing-gpu dispatch/pipeline -> resident lifecycle decision/result -> existing structural dissolve application; if an existing generic GPU executor is reused with little/no GPU-source diff, provide the exact dispatch trace proving hardware execution rather than a CPU twin", "retain resolve_overlay_lifecycle() only as CPU oracle/referee; compare its decisions against the REAL GPU production result with a planted production-path divergence that REDs for the intended mismatch", "map every admitted DissolveCondition arm owned by 7.7 to canonical EML/existing Phase-5 threshold registrations; PropertyReaches/PropertyBelow must consume real resident registered values and actually fire in the GPU witness", "AfterTicks uses fail-closed deadline_generation = activation_generation + duration in the owning tree generation authority, compare-not-decrement; production overflow must hard-error and a sentinel/default mutant must RED", "routed duration transports authored duration + provenance only and rebases against destination generation authority; foreign absolute deadline/global-clock mutants RED under forced generation skew", "reuse canonical EML ProgramId/registry/cache key; semantic dedup universal, artifact reuse only where physically compatible, NO cross-instance CSE claim; overlay-local-table mutant RED on the production path", "reuse the graduated 7.2/7.3 Current/Next/swap authority rather than minting overlay-local current/next/swap state; mid-session semantic-template mint and durable-row-capture mutants must be production-path defects, not helper-return tests", "discharge OverrideReceived by real germ replacement OR admission rejection; admission rejection is acceptable only after checking authored reach/compatibility and proving all admission doors reject it, otherwise STOP for DA", "capture a REAL pre-extraction recording on base/master 9593ca2e with the pre-extraction production path, then replay that same artifact post-extraction and compare bit-identically; separately prove one-history/no OverlayHistory", "all seven required mutants (oracle/GPU divergence, second detector, overlay-local EML, mid-session template, durable row, foreign deadline, global clock) must alter/bypass the real production path and RED for stated reasons; helper functions that merely return the named error do not count", "measure sparse carry rows/bytes/dispatch cost at admitted cardinalities before compaction; if route locations change, clean-tree --harvest, show reconciliation delta/classification, then --check PASS; run focused+affected batteries and hosted Doctrine"]
stop_conditions: ["no existing lawful GPU execution substrate can host lifecycle decisions without inventing a second crossing/dispatcher or 7.8 actuation semantics", "property-threshold lifecycle cannot consume existing Phase-5 registrations/resident values without a new semantic authority", "OverrideReceived admission rejection/implementation requires a new compatibility or matching ruling", "pre-extraction recording cannot be produced on 9593ca2e and consumed unchanged post-extraction through existing history surfaces", "deadline/routed-epoch law requires foreign absolute time or an overflow sentinel", "performance repair requires speculative compaction/active-list machinery before measurement", "completion requires 7.8/7.8a/7.9, rule/gate edits, scan retirement, successor work, or weakening the graduated 7.6 census"]
---
## BUILD
- Re-implement only 7.7 from master: REAL GPU production lifecycle authority, with CPU lifecycle retained solely as oracle/referee.
- Compile lifecycle onto canonical EML + existing Phase-5 registrations/crossings; use owning-tree deadlines and destination rebasing; discharge `OverrideReceived` without inventing a peer lifecycle path.
- Reuse the graduated Current/Next/swap and sparse residency substrates; do not inherit PR #1753's host-side “GPU twin” architecture.
## FENCES
- Actual GPU dispatch is the numerical authority. CPU may prepare/upload/apply already-authorized structural results, never decide lifecycle in production.
- Production-path falsifiers only: no inert refusal helpers or synthetic replay expectations. One EML language, one crossing surface, one owning-tree clock, one shared swap authority.
- 7.8/7.8a/7.9 remain fenced; `OVERLAY-PEER-AUTHORITY` stays live through 7.7.
## EXIT-PROOF
- Real GPU/oracle parity is bit-identical; all seven production mutants RED for their intended reasons; property and deadline lifecycles both execute through the real GPU path.
- A recording captured on pre-extraction master `9593ca2e` replays bit-identically post-extraction, independently of the one-history structural proof.
- Deadline overflow hard-fails in production, routed duration rebases at destination, sparse carry is measured before compaction, and any census drift is clean-tree `--harvest` reconciled.
- Return PROBATION / proof-present / DA-review-pending with exact head, receipts/ACKs, actual GPU dispatch trace, mutant reasons, pre→post replay artifact/digest, carry measurements, census delta, focused+affected batteries, and hosted artifacts. Coding does not merge, close the binding row, move pointer, retire scans, or begin successors.
