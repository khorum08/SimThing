---
rung: ACTIONBAND-ADMISSION-DOOR-0
kind: rung
track: 0.0.8.7
base_sha: 3f9ea9fe831de52d8f7ae745a1e9e795f0012b18
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "DA authorization Board 5229596923, re-issued after the ActionBand anchor expansion at 3f9ea9fe; pre-dispatch ruling 5229698142 APPROVED with one bounded amendment. Frontier coding lane. 7.1 is a constitutional door; DA-RESERVE is expected and is not router debt. The §4.2 delegation gap is closed by the anchor update. Coding must receive every projected actionband-* anchor and must STOP if the rendered set is incomplete. Movement fence means mint-versus-inherit: preserve existing movement-named admission invariants and Movement-Front law, but mint no new movement-specific peer facility."
surfaces: ["crates/simthing-spec/src", "crates/simthing-spec/tests", "crates/simthing-kernel/src/decision_ingress.rs", "crates/simthing-kernel/tests", "crates/simthing-sim/src/overlay_lifecycle.rs", "crates/simthing-sim/tests", "docs/tests", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md"]
forbidden: ["any crossing comparator, crossing record, listener framework, CPU crossing evaluator, or other crossing authority beside the existing Phase-5 sealed BandCrossingDelta path", "minting any new movement-specific action/destination/path/planner/crossing facility or reintroducing the reverted 7.1 movement-facility shape; explicitly preserve and do not scrub the pre-existing designer_admission/mobility_* family and its admission invariants, Movement-Front automaton naming, and the movement-front / movement-front-adjudications anchors", "predicate-only or open-ended target forms, runtime solvers, or target semantics outside the closed vocabulary projected by actionband-target-forms", "mid-session ActionBand template minting, runtime semantic construction, runtime widening of subordinate/dependency spans, axis/channel budgets, or hidden previous-state allocation", "ActionBand-local RF conservation judge, ActionBand-local clearing rule, persistent cross-arena scarce-grant holding, atomic common-depth transaction semantics, or any Vector CostBand work", "GPU ActionBand execution, CPU scheduler/evaluator/mirror, semantic-label dispatch, recursive runtime pointer scheduling, 7.2/7.3/7.4/7.5 implementation, 8.1/8.2 execution, StemThing-B, or successor work", "edits to GATE CODE -- scripts/ci/**/*.sh, scripts/ci/**/*.py, .github/workflows/** -- including workarounds for the known positional-test-identity or inner-loop prohibition defects; DATA LEDGERS are NOT gate code and remain writable for rows this rung earns: scripts/ci/test_inventory.tsv rows for tests this rung authors, and the append-only scripts/ci/anchor_reach_log.tsv. Adding a ledger row for a test you wrote is the ledger working, not a workaround; weakening or deleting existing rows is still forbidden", "#[cfg(test)] twins, alternate executors, intentionally broken fixtures, or mutants outside the real production path they claim to guard"]
required_checks: ["fresh Frontier coding ORIENT-RECEIPT at the current rule stamp plus exact HD-RECEIPT before edits; ACK every projected actionband-* anchor and simthing-0087 binding anchor, with actionband-crossing-surface, actionband-target-forms, actionband-axis-budget, and actionband-8x-sequencing mandatory; STOP if the ActionBand projection is incomplete", "mint exactly one session-build admission door in simthing-spec and freeze an immutable numeric ActionBand template product with stable indices/spans plus admitted subordinate/dependency, axis/channel, storage, EML-program, emission-binding, and crossing references; no runtime template-authoring door", "bind a depth-1 synthetic admitted ActionBand to an ordinary existing anchored threshold so changing the observed value produces the existing sealed BandCrossingDelta and resolves the opaque ActionBand binding; no second crossing identity is introduced", "make the rival-crossing defect unconstructible at the production admission/binding type boundary where possible; if the type system cannot make it impossible, plant the rival path in the REAL production ActionBand-to-threshold binding path and require it to RED; never use a test-only surrogate", "in the REAL production target/admission validation path, plant and RED the predicate-only target and unretained-velocity defects required by actionband-target-forms/actionband-binding-laws; velocity must be refused unless its previous-generation plane is explicitly admitted", "in the REAL production admission-cap path, plant and RED over-axis/channel-budget and over-subordination/dependency-span defects; cached/derived channels consume the admitted budget and runtime widening is refused", "in the REAL production session-freeze path, plant and RED a mid-session template-mint attempt; in the REAL production requirement-admission path, refuse any 7.* template requiring the atomic/persistent scarce-lane semantics fenced by actionband-8x-sequencing", "prove GPU numerical authority / CPU semantic-shadow separation at the admitted type boundary: human-readable labels may be retained only as authoring/diagnostic shadow and cannot become numerical dispatch keys; this rung admits references and bindings but does not implement 7.2 execution", "remove both live HORIZON-ENTRY(2026-08-08) markers from crates/simthing-kernel/src/decision_ingress.rs and crates/simthing-sim/src/overlay_lifecycle.rs in the implementation diff, and record their removal in relay evidence; a surviving marker is a review finding because binding to the sealed surface is its consumer", "run focused exact-head spec/kernel/sim admission, target-form, budget, freeze, sealed-crossing, and marker-consumption tests locally because hosted CI runs no cargo tests; report exact commands/results and rerun all existing affected-package tests without weakening tests or inventories", "update one results/evidence packet and only the 7.1 workplan status to PROBATION / proof-present / DA-review-pending; regenerate orientation deterministically; keep 7.2/7.3/7.4/7.5, 8.1/8.2, StemThing-B, and all successors TODO", "run handoff/agent/doctrine/anchor/orientation/exit-proof checks plus hosted Doctrine Scan and Doctrine Exec at the exact final head; read doctrine-scan-reports artifact and step conclusions, treating INSPECT with failures=0 as pass and any nonzero failures as failure"]
stop_conditions: ["the implementation requires a second crossing authority, comparator, record, listener, or CPU evaluator rather than binding to the existing sealed Phase-5 crossing surface", "the closed target vocabulary cannot supply total admitted satisfaction/projection semantics without predicate-only/open runtime solving", "correct admission requires runtime semantic minting, hidden history allocation, runtime axis/subordination widening, CPU ActionBand authority, or execution work belonging to 7.2+", "any template requires persistent cross-arena scarce holding or atomic common-depth commitment before the 8.x / Vector CostBand capability exists", "either HORIZON-ENTRY marker survives because the new door did not actually consume the held crossing substrate", "completion requires minting a new movement-specific peer facility, ActionBand-local RF judge/clearing, scripts/ci or workflow edits, known-harness-defect workarounds, invariant weakening, or successor-rung implementation; pre-existing mobility admission invariants and Movement-Front naming/law are inherited and must not be scrubbed"]
---
## BUILD
- Implement only 7.1 `ACTIONBAND-ADMISSION-DOOR-0`: one `simthing-spec` session-build admission door and frozen numeric template product, using the exact workplan row and the projected `actionband-*` anchors as authority.
- Bind admitted ActionBand crossings to the existing sealed Phase-5 threshold/`BandCrossingDelta` surface; admit the closed target vocabulary and frozen budgets/spans/references needed by later GPU execution without implementing that execution.
- Consume and remove the two held `HORIZON-ENTRY` markers as part of the same implementation because this rung supplies their declared consumer.
## FENCES
- Anchor IDs govern; do not substitute remembered prose. The coding projection must include the full ActionBand anchor set, especially `actionband-crossing-surface`, `actionband-target-forms`, `actionband-axis-budget`, and `actionband-8x-sequencing`.
- Mint no new movement-specific action/destination/path/planner/crossing facility and do not reintroduce the reverted 7.1 movement-facility shape. Preserve untouched the pre-existing `designer_admission/mobility_*` family and its admission invariants, Movement-Front automaton naming, and `movement-front` / `movement-front-adjudications` law. No second crossing machine, CPU numerical ActionBand authority, runtime template mint/widening, ActionBand-local RF/clearing/transaction subsystem, or 7.2+ work.
- Plant every defect in the real production path it guards and prefer unconstructible type boundaries over planted REDs. Do not edit CI/orientation machinery to accommodate line-only inventory drift; report the known harness defect instead.
## EXIT-PROOF
- A depth-1 synthetic ActionBand admitted through the single session-build door binds to an ordinary anchored threshold, and an observed-value change produces the existing sealed crossing and opaque ActionBand binding; rival crossing authority is unconstructible or production-path RED.
- Predicate-only target, unretained velocity, over-axis/channel budget, over-subordination/dependency span, mid-session mint, and pre-8.x atomic/persistent scarce-lane template each fail closed through the real production admission paths.
- Both held HORIZON-ENTRY markers are absent; no new movement-specific peer facility or second ActionBand/crossing/RF authority appears, while pre-existing mobility admission/Movement-Front law remains intact; focused exact-head local tests and hosted Doctrine evidence are green under the stated scan semantics.
- Return PROBATION / proof-present / DA-review-pending with exact base/head, HD-RECEIPT, fresh Frontier coding receipt/stamp, complete anchor ACK set, production-path mutant/impossibility evidence, marker-removal evidence, local test results, hosted scan/exec artifact evidence, and graduation routing. Coding does not `/clearance`, merge, graduate, move the pointer, or begin any successor.
