---
rung: CLEARING-WEIGHT-SEMANTIC-PARTITION-0
kind: rung
track: 0.0.8.7
base_sha: 861e098419328f362dee9a09e0536d4833ffb6cc
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(binding)
owner_approved: true
owner_notes: "Owner-approved remedial 13.9; authority Board 5470588161, minted by merged #1899. The DA-reproduced S1 and the reviewer 6.1 law are binding: current-value equality may compress representation but may never erase an admitted derivation boundary capable of producing a different future value. 13.8 graduation stands. After 13.9 graduation, Owner hold 5466933355 resumes: no closeout or next-track work without an explicit Owner Board call. Orchestrator ORIENT-RECEIPT 61ee1363d2df; rule stamp 3e76a60f12435301."
surfaces: ["crates/simthing-kernel/src/clearing_weight_projection.rs", "crates/simthing-driver/tests/clearing_weight_semantic_partition_0.rs", "scripts/ci/test_inventory.tsv", "scripts/ci/anchor_reach_log.tsv", "docs/tests/clearing_weight_semantic_partition_0_results.md", "docs/tests/current_evidence_index.md"]
forbidden: ["any production change outside crates/simthing-kernel/src/clearing_weight_projection.rs; a need to edit derived_span_projection.rs, flow_market.rs, lib.rs, any Cargo manifest, Cargo.lock, or another production surface is a STOP for DA rather than silent scope expansion", "any new registry, scheduler, cache, dirty map, writer/source discriminator, dependency vocabulary, runtime registration surface, secondary clearing path, or per-row materialized authority", "refresh logic that evaluates only current effective-span starts, scans logical member rows, or walks all spans in production; semantic windows must be reconstructed from the already-frozen override subtree ranges inside each affected range", "changes to 13.8 dependency binding shape, ChangedLocus semantics, constrained-clearing arithmetic, eligibility, epsilon, remainder/tie law, DecimalField exactness, generation authority, replay, or kernel-to-spec dependency direction", "any modification to the 13.6 matrix, germ battery, 13.8 lifecycle witness, integer-apportionment, tie-rotation, or replay seal files, including assertion, expected-value, fixture, or call-form edits", "projection caching, GPU clearing, profile interning, sparse-K optimization, benchmarking, or any other PERFORMANCE-TRACK implementation", "any .github/workflows/** or scripts/ci/**/*.sh or scripts/ci/**/*.py gate-code edit", "pointer movement, graduation/stamp work, closeout preparation/apply/reap, orientation-guide amendment, next-track authoring, or engineering-review ceremony from coding"]
required_checks: ["before edits render/read this handoff; fresh coding-role orientation must be bound to the exact merged handoff-dispatch master named by the Board dispatch, carrying the HD-RECEIPT and ACKing every rendered REQUIRED-ANCHOR", "FIRST STEP reproduce the DA S1 against the exact dispatch base before production edits: four-row preorder tree root -> child -> grandchild plus root sibling; default 1.0; child Set(1.0); spans-at-admission exactly 1; ancestor/default 1.0 -> 2.0; capture and quote the current failure left Some(2.0), right Some(1.0) for the child before repair", "FIRST STEP capture dispatch-base blob SHAs and assertion/expected-value counts for clearing_weight_span_unification_0.rs, stemthing_b_flow_market_germ_0.rs, and clearing_weight_deformation_lifecycle_0.rs; those seal files must remain byte-identical", "reconstruct semantic windows independently for each invalidated affected range from that range's start/end plus every intersecting already-admitted override subtree start/end; sort/deduplicate, evaluate one effective weight/profile per window, then remap only those windows through the existing DerivedSpanProjection::remap_range authority so adjacent-equal values coalesce after evaluation", "replace the production all-spans unaffected_profile_samples proof scan with affected-range-bounded probes or test-only proof; preserve the existing 13.8 public metrics and witness truth without iter_spans or whole-projection production work", "add a standing RED-before/GREEN-after witness containing the exact S1 as acceptance item 1: after default 1.0 -> 2.0, root and sibling become 2.0 while child and grandchild remain 1.0", "add nested preservation proof: a deeper child's distinct transform remains authoritative after an ancestor override changes; no latent derivation boundary is lost through an equal-valued compression", "add re-coalescence proof: when distinct semantic windows again evaluate to the same descriptor/profile, the stored projection returns to maximal adjacent-equal spans rather than preserving unnecessary physical partitions", "prove affected-only selectivity: zero logical member rows scanned; no production all-span walk; unrelated ranges retain effective values/profile identity; semantic work scales with affected semantic windows and not descendants or whole-projection span count", "preserve all prior seals unmodified and green: 13.6 matrix, germ battery, 13.8 lifecycle witness, integer apportionment, tie rotation, constrained clearing, and replay", "ledger the new test and results packet, append anchor-reach rows only as required, and add one current-evidence row; no closeout-artifact or performance-debt ledger mutation is authorized", "run focused 13.9, 13.8 lifecycle, 13.6 matrix, germ, derived-span, and constrained-clearing tests; then cargo check for touched crates, agent/doctrine/orientation/handoff/anchor/diff checks, hosted Doctrine Scan/Exec, and the procedure-of-record full structural certificate with 0 failed", "return PROBATION / proof-present / DA-review-pending with exact dispatch base/head, quoted RED-before output, GREEN-after acceptance matrix, semantic-window boundary inventory, affected-only metrics, production no-all-span proof, seal blob equality, changed-file census, structural certificate, hosted run IDs, HD receipt, coding orientation receipt/anchor ACKs, and fresh exact-head /clearance plus /relay-lint; coding never merges or graduates"]
stop_conditions: ["the exact DA S1 does not reproduce at dispatch base with one admitted span and child left Some(2.0), right Some(1.0)", "the repair cannot be expressed inside clearing_weight_projection.rs using the already-frozen override ranges plus existing remap_range/coalescing authority", "the repair appears to require a generic derived-span substrate edit, a new registry/cache/path, a writer discriminator, or any unlisted production surface", "preserving latent derivation boundaries requires a per-row materialization, logical-member scan, or production whole-span walk", "the exact S1, nested preservation, re-coalescence, or affected-only selectivity cannot all hold together while representation remains maximal", "any prior seal file must change, any clearing arithmetic/generation/replay semantic moves, or kernel-to-spec is introduced", "any focused/workspace/doctrine/orientation/hosted check becomes red under the zero-red law", "scope expands into performance work, pointer/graduation/closeout, next-track authoring, or any other Owner-held surface"]
---
## BUILD
- Reproduce the DA's S1 **before changing production code** and preserve the exact failure in the results packet: one effective span at admission; after the ancestor/default changes from 1.0 to 2.0, the compressed child `Set(1.0)` is wrongly overwritten to 2.0 on the dispatch base. This RED is acceptance item 1, not optional archaeology.
- Repair only `ClearingWeightSpanProjection::refresh` and its private clearing-weight helpers. For each affected logical range, derive the semantic partition from the range bounds plus every intersecting admitted override-subtree bound. Evaluate the composed weight/profile at every resulting window start. Apply each window through the existing `DerivedSpanProjection::remap_range`; let the graduated adjacent-equal merge restore maximal physical spans after semantic evaluation.
- Keep derivation semantics distinct from physical representation. Equal values may occupy one stored span, but refresh must be able to reconstitute every admitted default/override boundary from frozen authoring ranges when an upstream operand changes.
- Remove the production whole-projection proof scan. `unaffected_profile_samples` may become bounded to immediate unaffected neighbours or test-only proof, but `refresh` must not call `iter_spans` or inspect unrelated spans. Preserve the 13.8 metrics/witness without weakening its assertions.
- Add `clearing_weight_semantic_partition_0.rs` with the exact four-row S1, nested override preservation, re-coalescence to maximal spans, and affected-only selectivity. Do not edit any prior seal file.
- Return PROBATION / proof-present / DA-review-pending. Coding does not merge, graduate, move the pointer, invoke closeout, or open the performance track. Owner hold `5466933355` resumes after DA graduation.

## FENCES
- **Semantic partition is authoritative; effective spans are compressed representation.** Current equality never destroys the frozen derivation ranges needed for a future refresh.
- **Smaller repair only.** The existing override ranges, dependency index, invalidation result, `remap_range`, and adjacent-equal coalescer are sufficient. No generic substrate or second authority is admitted.
- **Affected-range-local means measurable.** No logical-member scans, dense row map, or whole-span production proof pass. Work is proportional to invalidated semantic windows.
- **Seals remain byte-identical.** The 13.6 and 13.8 proofs are additive predecessors, not editable scaffolding. The new witness extends the falsifier surface.
- **Performance remains banked.** This is a semantic repair, not a lowering, cache, GPU, interning, or benchmark rung.
- **Containment and governance.** `kernel -> spec` stays forbidden; no gate code, pointer/stamp, closeout, or next-track work from coding.

## EXIT-PROOF
- **RED-before / GREEN-after:** quote the exact dispatch-base S1 failure, then show the same test green: root/sibling 2.0; child/grandchild 1.0; admission started as one merged span.
- **Nested semantics:** a deeper transform survives an ancestor override change, proving every admitted derivation boundary can re-emerge after prior equal-value compression.
- **Re-coalescence:** distinct semantic windows that again resolve equal collapse back to the maximal stored span/profile representation.
- **Affected-only locality:** publish affected ranges, semantic-window count, dirty/rebuilt/examined spans, member rows scanned=0, bounded unaffected identity samples, and a production search/call-graph proof that no all-span walk remains in refresh.
- **Predecessor seals:** base/head blob equality for the 13.6 matrix, germ, and 13.8 lifecycle witness; all constrained-clearing, apportionment, tie, replay, and derived-span batteries green unmodified.
- **Structural/routing:** exact changed-file census within the six authorized surfaces, procedure-of-record structural certificate with 0 failed, focused + doctrine/orientation/anchor/handoff/agent checks green, hosted Doctrine Scan/Exec green, and fresh body/head-bound `/clearance` plus `/relay-lint`. Remain PROBATION for orchestration and DA review.
