---
rung: VENDOR-DOOR-GRANTING-SURFACE-0
kind: rung
track: 0.0.8.7
base_sha: e6b3677c147cfe3d95f0f12588bfa785661c86f5
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "DA ruling 5432021207 graduated 11.2d and explicitly unblocked this final StemThing-B rung with Orchestrator author/self-clear authority. This is FACADE-ONLY: expose the already-graduated generic conserved-market and residency vocabulary through the existing five Vendor Door operations bind/derive/overlay/populate/run. No sixth allocation/grant verb; no new B semantics; recursively granting child witness mandatory. If the facade cannot expose B without engine semantic change, STOP and reopen the substrate rather than burying architecture in simthing-embedder."
surfaces: ["crates/simthing-embedder", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/tests", "scripts/ci/test_inventory.tsv"]
forbidden: ["any semantic change in simthing-core, simthing-spec, simthing-sim, simthing-driver, simthing-kernel, simthing-gpu, WGSL, RF/CostBand/Triad/replay/history machinery merely to make the facade work", "a sixth Vendor Door verb/module/namespace or allocation/grant/market manager facade", "facade-owned state, cache, market registry, allocator, clearing, provenance, retry/convergence, history, telemetry, replay, generation, or observation authority", "semantic translation or duplicate vocabulary where direct delegation/re-export of graduated types/functions suffices", "per-type offering deltas, shadow offering tiers, free-list/order grant policy, direct allocation bypass, or direct engine write bypass", "a second replay/history/observation surface or ML/telemetry authority", "11.3 implementation, 11.4, 12.x, Vector CostBand, or the three ClauseThing baseline reds", "retired handoff receipts 890b845b5655 or aff35cbd509a", "retired/held allocator-retirement receipts 9d48b1b2c0e8 or 05eea0bd4c21", "adding cargo build/test/check to CI"]
required_checks: ["before edits render/read this handoff; perform fresh Frontier coding-role orientation on live dispatch master even if orientation_rule_stamp is unchanged, and ACK every rendered REQUIRED-ANCHOR", "FIRST STEP map the existing five facade modules bind/derive/overlay/populate/run and the exact current delegated engine calls; preserve the dependency arrow engine <- embedder and SimFacade zero-owned-state law", "FIRST STEP resolve the landed 11.2a authored grammar and exact legal construction/admission surfaces for conserved granter budgets, ConservedOfferingSpec / OfferingPriceVectorSpec strict references, sealed DrawEnvelopeTemplateSpec, and flow-market admission/clearing products; do not recreate them in the facade", "FIRST STEP resolve the landed 11.2b residency physical vocabulary appropriate to vendor authoring and the 11.2c session execution/grant path; every exposed entry must be a leaf delegation/re-export with no semantic translation", "if any required B authoring or execution capability cannot be exposed through the existing five verbs without changing engine semantics, STOP and name the missing substrate seam", "Populate exposes/delegates root and descendant granter conserved budgets for arbitrary authored conserved lanes through existing authored vocabulary; no allocation semantics", "Derive exposes/delegates offerings with strict reference semantics and sealed Draw envelopes on the existing specialization profile; per-type deltas or shadow tiers remain unrepresentable", "Run executes granting through the existing graduated session/11.2a+11.2c path; facade owns no clearing, placement, retry, generation, replay, or history loop", "VRAM Residency's additional authored physical vocabulary may be surfaced through existing verbs only where the engine already accepts it; no allocation verb and no facade placement oracle", "prove at least one non-residency conserved market is authored solely through the Vendor Door and exercises the graduated RF + CostBand + Field Triad + canonical observation/replay grammar with zero engine mechanism added", "prove recursive granting through the same facade: a child that receives an accepted grant becomes granter for its descendants using the same authored surface, with conservation/replay authority unchanged", "re-prove facade law: five verbs only, leaf by dependency arrow, zero owned state, every new public entry delegated; plant compile/API falsifiers against a sixth allocation/grant verb or direct engine bypass", "run focused Vendor Door B tests plus applicable facade standing tests, 11.2a market, 11.2b residency vocabulary, 11.2c growth/provenance/replay, 11.2d allocator-order seals, inventory/drift, Agent/Doctrine/orientation/lifecycle checks; return exact base/head/tested_code_sha and hosted workflow IDs", "structural certificate remains DA-side at graduation; inherited baseline is 120 suites / 460 passed / 3 failed / 14 ignored"]
stop_conditions: ["exposing generic B requires any new engine state, engine authority, semantic engine change, or new clearing/placement/replay path", "the five existing facade operations cannot express the required generic conserved-market authoring without a sixth verb", "recursive child-as-granter requires special-case facade semantics rather than the same graduated grammar", "a non-residency witness cannot exercise the existing full market/RF/CostBand/Triad/replay chain without creating a second mechanism", "VRAM physical authoring requires facade-owned placement/allocation policy", "completion requires 11.3, 11.4, 12.x, Vector CostBand, ClauseThing-red work, or a new telemetry/history authority"]
---
## BUILD
- Implement **11.2e `VENDOR-DOOR-GRANTING-SURFACE-0` only**. This is the final StemThing-B rung and a facade completion, not a substrate rung. Existing Vendor Door operations are exactly `bind`, `derive`, `overlay`, `populate`, `run`; preserve that five-operation architecture.
- Treat the existing five `SimFacade` methods/modules as the only top-level dispatch slots; helpers may be added only as module-private or directly delegated implementation detail and must not become a sixth operation.
- Add only the minimum public facade vocabulary/delegation needed for vendors to author the graduated B grammar. `Populate` owns declaration-shaped authoring (root/descendant granter budgets); `Derive` owns offering/strict-reference/sealed-Draw declaration; `Run` consumes the already-graduated session execution path. Bind/Overlay stay their existing meanings. Direct re-export/delegation is preferred over wrappers; wrappers may not acquire policy or owned state.
- Surface existing VRAM-residency authored physical vocabulary only where appropriate through those same operations. Never create an allocator/grant verb, facade placement oracle, facade clearing loop, or facade provenance type.
- Preserve `SimFacade` as zero-sized/stateless (`owns_state()==false`) and keep all engine crates independent of `simthing-embedder`. Every new public facade entry must terminate in an existing graduated engine/spec/driver entry, not a peer implementation.
- Update one focused results artifact, evidence/inventory as required, and mark the 11.2e row PROBATION in the coding diff; pointer remains 11.3.

## FENCES
- **STOP-AND-REOPEN is binding:** if the graduated B substrate lacks a needed authoring/execution seam, do not repair it inside the facade and do not edit engine semantics. Return STOP with the exact missing seam.
- The genericity witness must be **non-residency**. VRAM residency may be separately exposed/proven, but cannot substitute for the Owner F1 proof that the market germ is generic.
- No sixth verb/module/namespace, no facade-owned authority/state, no per-type offering deltas, no free-list/order grant policy, no second replay/history/observation surface, no new scan/gate invented solely for this facade.
- 11.2a/11.2b/11.2c/11.2d are frozen graduated substrates. 11.3 implementation remains blocked until this rung graduates; 11.4, 12.x, Vector CostBand and the three ClauseThing reds remain fenced.

## EXIT-PROOF
- **Generic non-residency witness:** through Vendor Door APIs only, author an arbitrary conserved lane/budget, descendant Draw claims and strict-reference offering, execute existing constrained clearing/CostBand/RF/Field-Triad response, and observe/replay through existing canonical surfaces. Zero engine semantic/source change is the primary facade-success signal.
- **Recursive-granter witness:** an accepted child grant must make that child a granter for a descendant through the same facade grammar; no special recursive API, manager, retry loop, or second authority is allowed. Conservation and replay remain exact.
- **Facade leaf seal:** dependency arrows remain embedder -> engine only; `SimFacade` owns zero state; every new public entry delegates; existing five operations remain exactly five. Plant a compile/API RED for a sixth allocation/grant verb or direct bypass, and retain the existing direct-engine-surface compile seal.
- **Strict vocabulary seal:** no per-type offering delta/shadow tier can be authored; sealed Draw/provenance identities are consumed from graduated vocabulary rather than recreated by the facade.
- **Residency coexistence:** appropriate 11.2b physical authoring can be reached without an allocation verb or facade placement policy, while the non-residency witness proves genericity independently.
- **Authority census:** zero new allocator/manager/clearing/provenance/retry/history/telemetry/observation authorities and zero engine semantic edits. DA structural certificate is owed at exit; inherited baseline **120/460/3/14**.
