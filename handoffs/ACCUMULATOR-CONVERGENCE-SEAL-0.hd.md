---
rung: ACCUMULATOR-CONVERGENCE-SEAL-0
kind: remedial
track: 0.0.8.7
base_sha: 13cd9c39311cdc4c043c620c75d5561a5c657ebe
audience: coding
model_tier: frontier
owner_approved: true
expected_route: DA-RESERVE(gate-wiring)
owner_notes: "11.1c VENDOR-DOOR-TRIAD-SURFACE-0 graduated under DA ruling 5397784934. This is the next lawful rung. The Owner single-ingress law requires deletion, not a default flip: C-8c transfer, C-8d emission, and E-11 arena resource flow must execute only through AccumulatorOp, with their three soft flags and legacy paths absent from the final tree. The existing overlay-add and governed-velocity seals are the precedent. 11.1e ActionBand, 11.1f authored-law gadget, #1803/11.2, and pointer movement remain fenced."
surfaces: ["crates/simthing-sim/src/boundary.rs", "crates/simthing-sim/src/gpu_sync.rs", "crates/simthing-sim/tests", "docs/tests", "scripts/ci/test_inventory.tsv"]
forbidden: ["new execution mechanism, second accumulator authority, or parallel contention/resolution path", "changes to ActionBand execution ingress, authored-law gadget/EML vocabulary, Vendor Door semantics, #1803/11.2, or pointer state", "retaining use_accumulator_transfer, use_accumulator_emission, or use_accumulator_resource_flow as compatibility flags, aliases, environment/config switches, or dead fields", "replacing deletion with default-true behavior, warning-only behavior, or telemetry that still describes a selectable legacy path", "weakening AccumulatorOp admission, conservation, ordering, or exactness to match legacy output", "new CPU-side resolution of C-8c, C-8d, or E-11"]
required_checks: ["before edits render/read this handoff; carry a fresh Frontier coding ORIENT receipt at the current rule stamp and ACK every rendered REQUIRED-ANCHOR", "FIRST STEP archaeology: census every production read/write of the three target flags, both sides of each branch, E-11 burn-in/soak/telemetry apparatus, and all callers/tests that select either path; return the map before deleting", "prove C-8c transfer and C-8d emission AccumulatorOp paths are exact against their existing production/oracle batteries before removing legacy selection", "for E-11 carry the existing burn-in and dynamic-enrollment/soak evidence before final deletion; do not infer readiness from the default alone", "delete the three target PipelineFlags fields and all legacy branches they controlled; final production tree has one path for each workload", "retire ResourceFlowFlagSource and opt-in telemetry/burn-in instruments that can describe only the deleted E-11 flag, preserving any still-load-bearing generic proof by moving it to the converged path rather than leaving opt-in vocabulary", "add a law-biting falsifier for each workload: an attempt to select/reconstruct the legacy route must fail to compile or RED at admission for its own named reason; do not use symbol-name grep as the only proof", "preserve the already-sealed overlay-add and governed-velocity behavior; do not widen this rung to other PipelineFlags", "run focused C-8c/C-8d/E-11 batteries plus blast-radius sim/driver batteries and real-adapter execution where those existing proofs require it; structural certificate at the exact tested head", "evidence follows handoff_template: one docs/tests/accumulator_convergence_seal_0_results.md, one current_evidence_index.md line, one 11.1d status-row edit, plus mechanically required inventory/lifecycle updates; return exact base/head/tested_code_sha and hosted workflow IDs"]
stop_conditions: ["any of C-8c, C-8d, or E-11 still requires a genuine legacy-only capability or cannot match the existing exact/conservation contract through AccumulatorOp", "removing a target flag requires inventing a new executor, CPU authority, second history, or alternate contention path", "E-11 burn-in/soak evidence is not sufficient to establish the converged path on the production workload", "a required generic proof can only be kept by retaining opt-in/legacy semantics instead of re-homing it on the converged path", "the change requires touching 11.1e ActionBand, 11.1f authored-law gadget, #1803/11.2, or moving the pointer"]
---
## BUILD
- Complete the C-8c transfer, C-8d emission, and E-11 arena-resource-flow AccumulatorOp rollouts on the ordinary production pipeline. Use the existing AccumulatorOp machinery and existing admission/ordering/conservation semantics; author no replacement executor.
- After the existing exactness and E-11 burn-in/soak evidence is green, DELETE `use_accumulator_transfer`, `use_accumulator_emission`, and `use_accumulator_resource_flow` from `PipelineFlags`, DELETE the legacy branches they selected, and remove call-site configuration of those flags. Final production code has no selectable bypass.
- Retire E-11 opt-in-only telemetry and flag-source apparatus with the flag it measured. If a proof remains load-bearing for the converged path, re-home that proof without preserving opt-in vocabulary or a second path.
## FENCES
- The endpoint is the same seal shape already established by accumulator overlay-add and governed velocity: disabling/selecting legacy is not a supported state because the legacy path no longer exists.
- Keep AccumulatorOp exactness, conservation, deterministic ordering, typed identities, and the existing single execution/history authorities unchanged. Never make the unified path imitate legacy by weakening an invariant.
- Scope is only the three default-OFF accumulator migrations. Do not alter other PipelineFlags, ActionBand, EML/gadget vocabulary, Vendor Door behavior, 11.2 artifacts, or pointer/governance state.
## EXIT-PROOF
- Tree census shows all three target flag fields, their legacy production branches, and legacy-only E-11 telemetry/source vocabulary absent; production has exactly one execution route for C-8c, C-8d, and E-11.
- Three planted legacy-route attempts bite the law: each fails to compile or REDs at admission for its own workload-specific reason. Overlay-add and velocity seal batteries remain green as regression witnesses.
- Existing C-8c/C-8d exactness plus E-11 burn-in/dynamic-enrollment/soak and blast-radius batteries are green at one exact head; run the structural certificate at graduation.
- Evidence: `docs/tests/accumulator_convergence_seal_0_results.md`, one `current_evidence_index.md` line, one 11.1d status-row edit, and only mechanically required inventory/lifecycle rows.
