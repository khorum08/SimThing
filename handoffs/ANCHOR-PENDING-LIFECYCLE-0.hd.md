---
rung: ANCHOR-PENDING-LIFECYCLE-0
kind: rung
track: 0.0.8.7
base_sha: d89bec30987804e7f7ca29caea5438bef538f621
audience: coding
model_tier: std
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "Owner notice 5453971653 minted 11.5; DA graduation ruling 5458588083 moved the pointer to 11.5 on master d89bec30; DA follow-up 5458831930 sanctions the complete four-site librarian command-transport extension inside the one existing authority, including owner-review routing for pending-anchors --confirm. Build explicit pending-anchor lifecycle, machine dispositions, confirmed reap with deletion provenance, and zero sibling authority."
surfaces: ["scripts/ci/doctrine_anchors.tsv", "scripts/ci/anchor_query.sh", "scripts/ci/anchor_check.sh", "scripts/ci/librarian.sh", "scripts/ci/doctrine_exec_commands.sh", ".github/workflows/doctrine-exec-commands.yml", "scripts/ci/track_closeout.sh", "scripts/ci/authorized_deletions.tsv", "scripts/ci/anchor_reach_log.tsv", "scripts/ci/fixtures", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md", "docs/tests"]
forbidden: ["a second anchor lifecycle registry/checker or sibling librarian authority", "using PR comments or Board comments as anchor targets instead of in-tree document sections", "deriving pending status heuristically instead of explicit lifecycle data", "making PENDING-HEALTHY a gate failure during an open track", "silently deleting or reaping an anchor without one explicit authorized_deletions.tsv provenance record", "overloading or widening authorized_deletions.tsv ambiguously if its current consumers cannot represent anchor deletion rows safely", "broadening pending-anchor trigger domains such that REQUIRED-ANCHOR ACK lists balloon beyond the rung vocabulary", "a new workflow or comment transport instead of extending the existing doctrine-exec librarian path", "production Rust, crate, kernel, GPU, WGSL, runtime, simulation, admission, replay, history, or telemetry changes", "12.1 portability implementation, 12.2 canonization/repoint prose, Vector CostBand work, or any of the three known ClauseThing baseline reds", "changing 11.4 UNIFIED-INGRESS-EXCLUSIVITY-0 semantics or its constitutional census", "adding cargo build/test/check to CI"]
required_checks: ["before edits render/read this handoff; perform fresh Std coding-role orientation on live dispatch master d89bec30987804e7f7ca29caea5438bef538f621 and ACK every rendered REQUIRED-ANCHOR", "FIRST STEP archaeology doctrine_anchors.tsv + anchor_query.sh + anchor_check.sh as the one anchor authority, librarian.sh + doctrine_exec_commands.sh + .github/workflows/doctrine-exec-commands.yml as the one stewardship/comment transport, track_closeout.sh as the closeout authority, and authorized_deletions.tsv plus every consumer of that ledger before choosing row shape", "FIRST STEP map the existing ladder machine-truth parser and prove how PENDING-HEALTHY, ORPHANED, and STALE-PENDING are derived without inventing a second rung-state parser", "widen doctrine_anchors.tsv by exactly one lifecycle column; existing rows become canonical except unified-ingress-exclusivity and stemthing-b-market-grammar, which become pending:UNIFIED-INGRESS-EXCLUSIVITY-0", "anchor_query and anchor_check must round-trip the widened schema and retain content-hash integrity, reach-log append behavior, and existing anchor reach semantics", "PENDING-HEALTHY is advisory-only and must not fail ordinary mid-track checks; ORPHANED is reap-eligible; STALE-PENDING is a repair failure and must not be auto-reaped", "extend librarian.sh with pending-anchors list and --confirm reap behavior", "extend ALL FOUR DA-verified doctrine-exec enumeration sites: doctrine_exec_commands.sh action parsing plus both FORMAT strings; workflow reject-malformed-librarian FORMAT string; workflow librarian-run action-to-flag case arm; workflow librarian-owner-review routing predicate", "pending-anchors --confirm inherits cull --confirm routing verbatim: non-owner requester plus confirm routes to owner review with zero mutation; unconfirmed invocation lists dispositions for anyone", "extend doctrine-exec parser/rejection selftests so a valid pending-anchors invocation preserves action+confirm and malformed invocation emits the updated truthful FORMAT surface", "confirmed ORPHANED reap deletes only targeted anchor rows and appends exactly one authorized_deletions.tsv provenance row per removed anchor; dry-run/list mode mutates nothing", "if authorized_deletions.tsv cannot represent anchor reap provenance unambiguously under its current consumers, STOP with the exact schema/consumer conflict before widening or overloading it", "track_closeout.sh must fail closeout when any pending:* row remains and pass only after every such row has been repointed to canonical or reaped with ledger provenance", "selftest a planted missing-rung ORPHANED, a planted canonization-live STALE-PENDING, and a healthy PENDING-HEALTHY advisory case; include confirmed-reap proof of one deletion row per removed anchor and refusal without --confirm", "prove the two live interim anchors retain their current narrow trigger domains and in-tree targets", "run anchor_query/anchor_check normal + selftests, librarian selftest and pending-anchors dry-run/confirm fixture, track-closeout fixture, doctrine_exec parser/workflow selftests, doctrine scans/selftests, orientation/digest freshness, doc budget, test inventory/drift if fixtures change, and diff check; return exact base, tested_code_sha, final head, and hosted workflow IDs", "no workspace structural certificate is owed because this rung is CI/scripts/data/docs-only under the Owner certificate ruling"]
stop_conditions: ["the lifecycle requires a second anchor registry/checker or second stewardship authority instead of extending the existing stack", "authorized_deletions.tsv cannot represent anchor reap provenance without an ambiguous or invalid schema change under its current consumers", "any of the four DA-enumerated doctrine-exec transport sites cannot be extended in place without minting a second workflow/command authority", "STALE-PENDING cannot be determined from existing ladder machine truth for 0.0.8.7 without inventing a new unapproved lifecycle registry or hardcoded cross-track policy", "implementing the lifecycle requires production/runtime or crate-source changes", "the two 11.4 anchors cannot be retro-tagged pending without weakening current anchor hash/reach integrity", "completion requires 12.1, 12.2 canonization content, Vector CostBand, or ClauseThing-red work"]
---
## BUILD
- Implement **11.5 `ANCHOR-PENDING-LIFECYCLE-0` only**. This is a harness-lifecycle rung for anchors minted at graduation before canonical prose catches up. Extend the existing anchor/librarian/closeout authorities; do not create a new anchor system.
- Add one `lifecycle` column to `scripts/ci/doctrine_anchors.tsv`: `canonical` or `pending:<RUNG-ID>`. Existing anchors are `canonical`; `unified-ingress-exclusivity` and `stemthing-b-market-grammar` are `pending:UNIFIED-INGRESS-EXCLUSIVITY-0`.
- Join explicit pending rows to existing ladder machine truth and emit exactly: `PENDING-HEALTHY` (graduated minting rung, canonization not yet closed; advisory only), `ORPHANED` (minting rung absent/reverted/remedial-superseded; reap-eligible), and `STALE-PENDING` (canonization consumer live/graduated but row not repointed; repair failure, not auto-delete).
- Extend `anchor_query.sh` / `anchor_check.sh` only as needed to parse, validate, report, selftest, and hash-check the widened schema while preserving existing canonical query/reach behavior.
- Extend `librarian.sh` with `--pending-anchors [--confirm]` (or exact existing CLI convention). Unconfirmed lists dispositions and mutates nothing. Confirmed invocation may reap **ORPHANED only**, writing exactly one deletion provenance record per removed row.
- Extend the existing `/librarian` transport at **all four DA-verified sites** from ruling `5458831930`: (1) `doctrine_exec_commands.sh` action enumeration plus both FORMAT strings; (2) workflow `reject-malformed-librarian` FORMAT string; (3) workflow `librarian-run` action case mapping; (4) workflow `librarian-owner-review` routing predicate. `pending-anchors --confirm` inherits `cull --confirm` owner-review semantics verbatim: non-owner + confirm routes to owner review with zero mutation; unconfirmed listing is available to ordinary authorized requesters.
- Extend the existing doctrine-exec selftest fixtures for valid parse/action+confirm and malformed-format rejection. No new workflow/job/mutation door.
- Archaeology `authorized_deletions.tsv` and every consumer before choosing anchor-reap row representation. If it cannot represent anchor deletion unambiguously under current consumers, STOP rather than widening/overloading it speculatively.
- Extend `track_closeout.sh` so closeout asserts **zero `pending:*` rows**: every interim anchor must be canonically repointed or explicitly reaped with ledger provenance.
- Preserve the current narrow trigger domains and in-tree targets of both live pending anchors. Lifecycle metadata must not broaden REQUIRED-ANCHOR burden.
- 12.2 later consumes the pending set as its canonization repoint worklist; do not author or anticipate 12.2 prose here.
- Keep pointer at 11.5 in coding. Return PROBATION / proof-present / DA-review-pending; no self-graduation or merge.

## FENCES
- Explicit lifecycle data, never heuristic inference.
- One anchor authority and one librarian/comment transport; no sibling lifecycle table/checker/workflow.
- `PENDING-HEALTHY` is lawful mid-track inventory, never an ordinary gate failure.
- Confirmed reap is explicit, ORPHANED-only, and ledgered one-for-one; healthy/stale rows are not reaped.
- `STALE-PENDING` is a repair failure, not an orphan synonym.
- PR/Board comments are evidence only, never anchor targets.
- No trigger-domain inflation.
- No crate/source/runtime changes. Do not touch 12.1, 12.2 canonization content, Vector CostBand, ClauseThing reds, or graduated 11.4 semantics.

## EXIT-PROOF
- **Schema round-trip:** all current anchors parse under the widened schema; canonical behavior and exact content hashes remain green.
- **Live pending rows:** both 11.4 stamp anchors are exactly `pending:UNIFIED-INGRESS-EXCLUSIVITY-0`, with current narrow domains and in-tree targets intact.
- **Disposition selftests:** missing minting rung -> `ORPHANED`; canonization-live unrepointed row -> `STALE-PENDING`; healthy pending -> `PENDING-HEALTHY` with ordinary checks green.
- **Reap safety:** direct librarian and `/librarian pending-anchors` are non-mutating without confirm; confirm reaps ORPHANED only, refuses healthy/stale, and writes exactly one authorized deletion provenance row per removed anchor.
- **Four-site transport seal:** a valid headless invocation parses, routes through the existing workflow/librarian job, preserves confirm state, and applies the owner-review predicate for non-owner confirm; malformed invocation is rejected by truthful updated FORMAT text at every rejection surface.
- **Closeout seal:** any remaining `pending:*` row makes closeout RED; canonical repoint or authorized reap clears it.
- **Reach preservation:** ordinary queries still append reach-log rows and anchor selection is unchanged except lifecycle reporting.
- **No structural certificate:** final diff remains CI/scripts/data/docs only under the Owner/DA no-certificate ruling.
