---
rung: ARTIFACT-PROVENANCE-CONTAINMENT-0
kind: rung
track: 0.0.8.7
base_sha: cfee6f2f
audience: coding
model_tier: frontier
owner_approved: true
expected_route: DA-RESERVE(gate-wiring)
owner_notes: "DA ruling 5157114036 supersedes the erroneous 6.1 pointer and authorizes Frontier coding on 5.9c. PR #1572 @ cfee6f2f restores the pointer to ARTIFACT-PROVENANCE-CONTAINMENT-0. Scope is (b) and (c) ONLY. (a) is already discharged by normalization-pump-2 @ 57f89407: specialist_citizens_minimal.clause now yields spatial=2 owner-seat=2 session-root=1 through crate-local fixture authority, and the base disc is crate-local. Do NOT rebuild or weaken that proof. 6.1 remains frozen until 5.9c graduates. Reap clock 2026-08-11 remains sovereign."
surfaces: ["scripts/ci/gen_orientation.sh", "scripts/ci/handoff_dispatch.sh", "scripts/ci/scenario_residue_check.sh", "scripts/ci/fixtures", "scripts/ci/test_inventory.tsv", "scripts/ci/triage_log.tsv", "crates/simthing-spec/tests/planet_child_location_admission.rs", "crates/simthing-driver/tests/support/resource_economy_session.rs", "docs/tests", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md"]
forbidden: ["redoing 5.9c(a), weakening specialization citizen counts, changing owner-seat derivation, or reintroducing TP/shipped-scenario inputs to orientation artifacts", "cross-crate fixture reach, scenario/corpus proof authority, domain vocabulary in engine code/tests, or treating shipped assets as semantic authority", "new domain/scenario-specific allowlist exception, hard-coded artifact-name bypass, or generator-specific exemption instead of the generic provenance rule", "promoting DEAD-EXPORT beyond its ruled posture, deleting a newly-live consumer surface, or replacing a deleted dead target with ceremonial tests", "reopening 5.9b/5.9d/6.0 semantics, owner-channel cleanup, Gu-Yang work, 6.1+ implementation, pointer advance, or gate weakening"]
required_checks: ["fresh coding ORIENT-RECEIPT + current ANCHOR-ACKs + matching HD-RECEIPT before edits", "artifact-provenance census covers every generated artifact consumed by gen_orientation.sh or handoff_dispatch.sh; each has fixture-backed provenance and any generator path that reads scenarios/ hard-fails", "artifact-provenance gate has a planted-defect fixture whose only defect is a generator read from scenarios/ and demonstrably turns red, then green when restored", "DEAD-EXPORT detects a Rust integration-test target with zero test functions and a tests/support module with no consumer outside support/; selftests plant both shapes and detect them without regressing existing re-export reachability logic", "delete crates/simthing-spec/tests/planet_child_location_admission.rs and crates/simthing-driver/tests/support/resource_economy_session.rs only after current-tree census confirms the ruled dead shapes; reconcile inventory/lifecycle truthfully with no replacement proof unless a real live invariant is discovered", "specialization_citizen_counts.tsv remains scenario-neutral at spatial=2 owner-seat=2 session-root=1; scenario-residue/detachability/inventory/lifecycle/orientation/agent/doctrine/doc-budget and exact-head clearance gates green"]
stop_conditions: ["any orientation/handoff artifact lacks an identifiable generator/fixture provenance path that can be checked without scenario semantics", "the provenance rule would require parsing scenario meaning rather than detecting generator dependency/read provenance, or would require a shipped scenario as a witness", "either ruled dead file now has a live consumer, executable test, or unique invariant not already covered; report instead of deleting blindly", "closing 5.9c requires weakening specialization counts, DEAD-EXPORT semantics, scenario containment, lifecycle/deletion guards, or touching 6.1+ scope"]
---
## BUILD
- Land the 5.9c(b) artifact-provenance gate over the generated artifacts consumed by `gen_orientation.sh` or `handoff_dispatch.sh`. Census the live set rather than trusting a stale count. Every consumed generated artifact must have fixture-backed provenance; if its generator reaches `scenarios/`, the gate HARD-FAILS. This is dependency/provenance containment, not scenario-content interpretation.
- Make the provenance gate falsifiable with a planted fixture where a generator reads `scenarios/`; the fixture must turn the gate red for that reason alone and return green when restored. Wire the gate into the existing CI/clearance surface so the two normalization-pump couplings would be mechanically caught rather than found by hand.
- Complete 5.9c(c) by extending existing `DEAD-EXPORT` detection to the two blind shapes the design names: a Rust integration-test target declaring zero test functions, and a `tests/support/` module with no consumer outside `support/`. Preserve the existing external-symbol/re-export reachability protection and current advisory posture unless an existing gate contract already says otherwise.
- After current-tree census confirms them still dead, delete `crates/simthing-spec/tests/planet_child_location_admission.rs` and `crates/simthing-driver/tests/support/resource_economy_session.rs`; reconcile inventory/lifecycle rows truthfully. Do not invent replacement tests for dead scaffolding. If either now carries a live consumer or unique executable invariant, STOP and report instead of deleting.
- Preserve the already-discharged normalization-pump-2 result: `specialization_citizen_counts.tsv` remains scenario-neutral at `spatial=2 owner-seat=2 session-root=1`; do not revisit owner-seat authoring/derivation or fixture topology. Return PROBATION only.
## FENCES
- Vendor Containment, Transient Fixture, Capability Binding, Purge Reconciliation, Mechanisms-Not-Domains, scenario-residue hard fence, and current orientation/HD rules remain binding.
- The provenance gate watches generator dependency provenance. It must not become a scenario parser, a filename allowlist, or a bypass registry. Shipped `.clause`/scenario assets remain assets and cannot prove engine/governance semantics.
- Scope is strictly 5.9c(b)+(c). The work that produced the synthetic specialist census is accepted substrate. Do not reopen 5.9b/5.9d/6.0 or start `EVENT-GENERATION-STAMP-0`.
## EXIT-PROOF
- All generated artifacts consumed by `gen_orientation.sh` or `handoff_dispatch.sh` are covered by fixture-backed provenance; a planted generator read from `scenarios/` turns the provenance gate red, restoring it returns green, and no orientation-feeding artifact fails on the clean tree.
- `DEAD-EXPORT` catches both newly ruled blind shapes with planted fixtures while preserving its existing module/re-export reachability behavior. The two named dead files are gone after a fresh consumer/invariant census, with lifecycle/inventory deletion truth reconciled and no ceremonial replacement.
- `specialization_citizen_counts.tsv` remains scenario-neutral and still reports `spatial=2 owner-seat=2 session-root=1`. Focused + provenance + DEAD-EXPORT selftests and scenario-residue/detachability/lifecycle/inventory/orientation/doctrine/doc-budget/clearance gates are green. Return PROBATION / proof-present / DA-review-pending; coder does not merge or advance the pointer.
