---
rung: STUDIO-FIELD-SESSION-ELEVATE-0
kind: rung
track: 0.0.8.6
base_sha: 456946421b662b0207b78405ba39b44c3c54fa5d
audience: coding
model_tier: frontier
owner_approved: true
expected_route: DA-RESERVE(unclassified-scope)
owner_notes: "Fully-automated: coder=Grok CLI. Orchestrator owns CI/clearance/tree-review/remands + delegated merge. Escalations→orchestrator not DA. [OVL] FAIL/OPEN (Owner plateau B=85/C=1817). No replacement exe until dedicated RF production repair accepted. No Studio-side RF arithmetic/rehearsal import."
surfaces: ["crates/simthing-mapeditor", "crates/simthing-workshop", "docs/design_0_0_8_6_studio_live_ops.md"]
forbidden: ["bespoke economy code in the tick", "CPU planner / AI-tick / decisions outside threshold crossings", "ScenarioSpec mutation from render/camera/UI", "new grammar/primitives in clausething or spec", "TP-specific code in a sealed crate (home to simthing-workshop)", "kernel/WGSL semantics", "Studio-side RF arithmetic / feeder patches / synthetic parent growth / RR rehearsal transplant"]
required_checks: ["cargo-check", "focused-tests", "doctrine-scan", "orientation-check", "doc-budget"]
stop_conditions: ["stale-orient-receipt", "scope-widening", "tick-needs-bespoke-economy-code", "decision-fires-outside-threshold-crossing", "missing-admitted-rf-execution-in-ordinary-step-once"]
---
## BUILD
- Field-bearing Studio path (`open_from_spec` + authored profile); structural-shell fallback. Elevate residue so **production/need** and policy overlays participate under ordinary `SimSession::step_once` via **admitted Arena RF** (child→ancestor reduce-up), not economy emission seeds alone. Decisions = FIELD_POLICY threshold crossings only.
- **[OVL]** session path + telemetry. Emission/`source_slot`/`source_col` rows are **diagnostic only** and **cannot close RF OVL**. RF OVL needs a named ancestor/Owner **sibling aggregate** (not a one-to-one child copy). No exe until dedicated RF repair lands.
## FENCES
- Generic RF/STEAD only — **no bespoke economy in the tick**. ScenarioSpec sole authority. No new grammar. §12 workshop-homing PASS 0. Missing admitted RF execution in ordinary `step_once` → **STOP / DA-route** (do not invent RF).
- Do not invent need/weight-profile install APIs or Studio arithmetic to manufacture ancestor growth.
## EXIT-PROOF
- **RF (load-bearing):** one named participating **child source** causally contributes to one named **ancestor/Owner sibling aggregate** through admitted Arena RF during ordinary `SimSession::step_once`. Paired control: remove/disable **only** that child (sibling participation + other inputs fixed); ancestor differential equals that child's admitted marginal contribution under declared mask/settle/disburse. Threshold fire under live ticks (zero without). Tests BITE.
- **BLOCKED (1) production child→ancestor RF seam** — no admitted non-rehearsal RF reduce-up/writeback in ordinary `step_once` (Remand 7 STOP; results). **BLOCKED (2) need/weight_profile install** — no GameMode/open seam for field-economy weight stacks.
- Emission-locus multi-tick deltas are **not** RF proof. **[OVL] FAIL/OPEN** until RF repair + approved screenshots. cargo/doctrine-scan/orient/doc-budget green when coding resumes.
