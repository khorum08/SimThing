---
rung: ACTIONBAND-FIELD-TRIAD-PROGRESS-0
kind: rung
track: 0.0.8.7
base_sha: 8da81c252e2ed9cb28f4a074b9462a13f9e82eb9
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "DA ruling 5248069220 graduated 7.5 ACTIONBAND-SEMANTIC-SHADOW-0: implementation #1719 @ beccd380, stamp #1720 @ 8da81c25, RUNGCLOSE PASS, pointer advanced to 7.5a. Field-neutrality gate outcome (i) is binding: the existing semantic/readback schema positively carries a synthetic non-PALMA bound-observable identity, so 7.5a must consume that generic schema rather than widen it. The Owner-directed Gu-Yang correction remains additive: 7.1-7.5 are closed. PALMA owns potential/direction; Gu-Yang owns conserved realizable flux/saturation; RF owns claims/clearing/disbursement; CostBand owns sink/work quantization; ActionBand consumes those native authorities and mints none of them. DA pre-dispatch ruling 5248198350 A1 verified that Gu-Yang is deliberately not a kernel/core primitive: compile_gu_yang_n4_field_sweeps in crates/simthing-driver/src/field_sweep_compile.rs produces two ordinary FieldSweepRegistration rows whose outputs land in ordinary conductance_col/value_col columns. Their unfamiliar shape is NOT an F15 trigger. The intended ActionBand binding is through an ordinary admitted threshold registration already referenced by ExistingThresholdRegistrationIndex; a new FieldSweepRegistration reference kind is STOP-and-report. DA execution-shape adjudication 5248796666 rejects a blanket conserved-progress EML ban and authorizes Option B only: carry the closed bound-source identity in the EXISTING reserved ActionBandEmissionBindingGpu auxiliary1 field, admit only the closed vocabulary in the EXISTING valid_shape check, and clamp emitted conserved quantity to the selected native bound AFTER EML evaluation so q_exec <= q_flux while canonical q_desired/EML composition remains lawful. Delete ConservedProgressEmlPayloadForbidden or equivalent blanket prohibition. The same ruling admits the narrow FieldSweepSession dispatch/export/readback counters as proof instrumentation. No new plan structure, buffer, binding, field solve, crossing surface, target form, authority, or core widening is authorized. F15 stands: if ordinary ActionBand execution genuinely needs throughput/congestion/saturation/lane-capacity/claimant-allocation authority outside Gu-Yang/RF, STOP to orchestration/DA rather than invent a private model. Sequence is 7.5a -> 7.5b -> 7.5c -> 8.1 -> 8.2, one rung in flight."
surfaces: ["crates/simthing-spec/src", "crates/simthing-gpu/src", "crates/simthing-driver/src", "crates/simthing-sim/src", "crates/simthing-kernel/src/accumulator_op/action_band_execution.rs", "crates/simthing-kernel/src/shaders/action_band_execution.wgsl", "crates/simthing-kernel/src/field_sweep.rs", "crates/simthing-spec/tests", "crates/simthing-gpu/tests", "crates/simthing-driver/tests", "crates/simthing-sim/tests", "docs/tests", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md"]
forbidden: ["new ActionBand-local throughput, congestion, saturation, flux, lane-capacity, claimant-allocation, conservation, or clearing authority; CPU numerical throughput/flux evaluation or raw GPU numerical readback used to decide progress", "new field solve, duplicate FieldSweepRegistration path, ActionBand-specific Gu-Yang pass, rerunning equivalent EML/field work when the authoritative resident output already exists, or any private reconstruction of Gu-Yang/RF realizable throughput", "new ActionBand target form, crossing/comparator/threshold authority, parallel admission door, vendor-local fifth bound source, implicit bound-source default, universal min(RfGrant, GuYangAvailable, GuYangRealized) recomposition, or a new ExistingFieldSweepRegistrationIndex/equivalent reference kind added merely to reach Gu-Yang output", "new ActionBand execution-plan structure/buffer/binding/authority for conserved bounds, any kernel/core change beyond the existing ActionBandEmissionBindingGpu auxiliary1 + existing valid_shape + existing emission-write clamp seam and the narrow FieldSweepSession proof counters, or any blanket prohibition on EML merely because a binding is conserved progress", "using PALMA potential as throughput, CostBand affordability as channel capacity, unsigned/abs(flux) reinterpretation, or changing canonical Gu-Yang sign/order authority; the opposed-demand spatial witness belongs to 7.5b and is not pulled forward", "hidden stall/saturation history, saturation listener, positive unbounded field/RF feedback, or treating generation pacing as a feedback-gain bound; Gu-Yang stays instantaneous and persistence must be ordinary admitted state", "widening the graduated 7.5 semantic-shadow/readback schema merely to carry Field-Triad identity, reopening 7.1-7.5, or adding movement/path/predecessor/rerouting/local-minimum/adversarial-navigation facilities", "7.5b, 7.5c, 8.1/8.2, Vector CostBand, StemThing-B, scenario-specific behavior, icon-layer work, gate-code edits, or re-derivation of held harness defects"]
required_checks: ["fresh Frontier coding ORIENT on the current post-#1720 rule source before edits; carry the rendered HD receipt and all required anchor ACKs, explicitly actionband-field-triad-authority@56cf5cdf2d2cb0f67b847a0ab3eb42157fd51c2b7ab37253dd37af412beab77f and actionband-native-authority-table@541a03cb00a13ee073bbb7a854226054888b9528b31560f5024071e44be6eb97", "FIRST STEP is producer/binding confirmation against the DA-verified native shape: compile_gu_yang_n4_field_sweeps in crates/simthing-driver/src/field_sweep_compile.rs -> [FieldSweepRegistration; 2] -> ordinary conductance_col/value_col outputs. The absence of a kernel/core Gu-Yang or SaturatingFlux primitive is BY DESIGN and is not F15. Reach the Gu-Yang output through an ordinary admitted threshold registration already referenced by ExistingThresholdRegistrationIndex. If the realized-flux output genuinely cannot be reached through that ordinary admitted-observable path, STOP-and-report rather than minting a new reference kind", "extend the ONE existing ActionBand admission product with a closed per-binding conserved-progress bound-source declaration semantically None|RfGrant|GuYangAvailable|GuYangRealized (exact Rust spelling rung-local); None is lawful only where no conserved transport bound applies, while every conserved-progress leg must admit exactly one non-None native source", "DA Option B: lower the selected closed bound-source identity through the EXISTING reserved ActionBandEmissionBindingGpu auxiliary1 field; widen only the EXISTING valid_shape check from auxiliary1==0 to membership in the closed vocabulary; no new plan structure, buffer, bind group entry, binding kind, authority, or FieldSweep reference is allowed", "DA Option B emission law: EML may compute q_desired on a conserved band, but the EXISTING emission write must clamp the emitted conserved quantity to the selected native bound AFTER EML evaluation so q_exec <= q_flux. Preserve canonical signed Gu-Yang orientation/order; do not use abs(flux) or unsigned reinterpretation. Delete ConservedProgressEmlPayloadForbidden/equivalent blanket conserved-EML ban. If this cannot be expressed inside auxiliary1 plus the existing validation/write seam, STOP-and-report", "prove admission/execution fails closed on zero-bound conserved progress, double-bounding/reapplication, any vendor-local fifth source/implicit fallback, and any attempt to bind an already-realized Gu-Yang result while also re-applying an RF grant that it already contains", "bind available flux, realized flux, saturation/stall magnitude, and sanctioned choke/contest projections only as already-admitted ordinary resident observables; preserve native canonical sign/order and do not normalize to abs(flux)", "prove the direct resident-output fast path using the DA-admitted FieldSweepSession call-graph counters at the REAL registration-dispatch/resident-export/host-readback seams: clean exact Gu-Yang path must show the native two-registration execution, one resident export, zero host field readbacks; a real duplicate-solve/readback rival must change those counts and RED. Source grep or test-owned bookkeeping counters are insufficient", "capacity + EML falsifier: with target and ActionBand EML unchanged, reducing admitted native channel capacity reduces executable progress and restoring capacity restores it; additionally an otherwise-valid conserved-band 2*x EML amplifier must ADMIT but the emitted executable quantity must not exceed the selected native q_flux bound. This re-aims the prior amplifier proof from admission rejection to actual bound enforcement", "bounded-feedback admission: a positive unbounded field-seeding/RF recurrence fails; a bounded/decaying equivalent admits under the existing feedback contract. Generation pacing may limit update rate but is not accepted as a gain bound; no implicit stall history is introduced", "preserve the graduated 7.5 field-neutral semantic projection: any bound-observable identity/provenance needed for readback uses the existing generic post-authority schema, and labels/designations remain unable to affect numerical dispatch or sealed authority", "run focused 7.5a plus inherited 7.5/7.4/7.3/7.2/7.1, relevant Gu-Yang/FieldSweep/RF batteries, full affected tests, inventory drift, and hosted Doctrine; only 7.5a becomes PROBATION / proof-present / DA-review-pending"]
stop_conditions: ["the DA-verified Gu-Yang authority exists as compile_gu_yang_n4_field_sweeps -> two ordinary FieldSweepRegistration rows -> ordinary columns; STOP only if those resident outputs cannot be consumed through an ordinary admitted threshold-registration/ExistingThresholdRegistrationIndex path and completion would require a new reference kind, new field solver, throughput authority, crossing surface, target form, or parallel admission door", "exactly-one conserved bound-source semantics cannot be expressed through the existing ActionBand admission product without inventing a second resource/field taxonomy or a competing feasibility rule", "DA Option B cannot be completed through the EXISTING ActionBandEmissionBindingGpu auxiliary1 + EXISTING valid_shape + EXISTING emission-write seam and would require a new execution-plan structure, GPU buffer/bind-group entry, crossing surface, or authority", "the resident fast path requires CPU numerical field readback/interposition, rerunning Gu-Yang/FieldSweep work for ActionBand, changing Gu-Yang/RF numerical authority, or proof instrumentation beyond the narrow existing FieldSweepSession dispatch/export/readback counters", "bounded-feedback compliance requires a new controller subsystem, hidden stall history, or same-generation convergence rather than the existing admission contract", "F15 triggers only on a genuinely missing authority: a real ordinary ActionBand case requires throughput/congestion/saturation/lane-capacity/claimant-allocation authority outside native Gu-Yang/RF; absence of a kernel/core Gu-Yang type alone is not F15", "completion requires any kernel/core widening beyond DA ruling 5248796666 Option B and the admitted FieldSweepSession counters, reopening 7.1-7.5, implementing 7.5b/7.5c/8.x/Vector CostBand/StemThing-B, movement navigation/rerouting, scenario behavior, or gate-code changes"]
---
## BUILD
- Implement only 7.5a. Bind generic ActionBand progress/band operands to already-authoritative resident STEAD/PALMA/Gu-Yang/RF outputs. For Gu-Yang, consume the ordinary columns produced by `compile_gu_yang_n4_field_sweeps` through an ordinary admitted threshold registration / `ExistingThresholdRegistrationIndex`; do not add a FieldSweep-specific reference kind. Gu-Yang/RF supplies conserved executable throughput; PALMA supplies potential/direction; CostBand remains downstream sink/work quantization.
- Add the closed per-binding conserved-progress bound-source descriptor to the existing ActionBand admission product and enforce exactly one native bound on every conserved-progress leg. Carry that identity only through the EXISTING `ActionBandEmissionBindingGpu::auxiliary1` reserved field and existing validity/write machinery. `None` is only the non-conserved/no-flux case; there is no implicit default and no fifth source.
- Preserve canonical conserved-band EML. EML computes desired progress; the existing emission write clamps the executable conserved quantity to the selected native bound after EML evaluation. Delete the blanket conserved-progress EML prohibition. Add no plan/buffer/binding authority.
- Keep the direct already-hot resident-output path, the DA-admitted narrow FieldSweepSession call-graph counters, and the existing bounded-feedback admission law for emissions that seed field/RF recurrence.
## FENCES
- No new field solve, crossing surface, target form, throughput/congestion model, CPU numerical mirror/readback decision, ActionBand-local conservation/clearing authority, FieldSweep-specific ActionBand reference kind, or new ActionBand plan/buffer/binding merely to carry the conserved bound. The only kernel ActionBand change authorized is the existing reserved `auxiliary1` identity + existing validity check + existing emission-write clamp; the only other kernel change authorized is the narrow FieldSweepSession proof instrumentation. Gu-Yang remains instantaneous; no hidden stall history.
- Preserve native Gu-Yang sign/order. Saturation limits progress but does not authorize search/rerouting. The full opposed-demand spatial witness is 7.5b, not 7.5a.
- F15 is a STOP fence, not design license: the DA-verified ordinary-field-sweep Gu-Yang shape is present and its lack of a kernel/core Gu-Yang type is not F15; if native Gu-Yang/RF authority is genuinely insufficient for an ordinary conserved-progress case, return that missing-authority fact to orchestration/DA.
- Preserve 7.1-7.5 as graduated and the 7.5 field-neutral semantic schema unchanged. Fence 7.5b/7.5c, 8.x, Vector CostBand, StemThing-B, movement vendorization, scenarios, icons, gates, and held harness defects.
## EXIT-PROOF
- A synthetic conserved-channel ActionBand consumes a REAL resident Gu-Yang output from the DA-verified `compile_gu_yang_n4_field_sweeps` -> two `FieldSweepRegistration`s -> ordinary-column chain through the existing ordinary admitted-observable/threshold-registration path, with zero ActionBand-local throughput solver and no new FieldSweep reference kind. Capacity down => executable progress down; capacity restored => progress restored, with target and ActionBand EML unchanged.
- A lawful conserved-band EML amplifier computes `2 * q_desired` but the actual emitted conserved quantity remains bounded by the selected native `q_flux`; the EML is admitted and the clamp, not an admission ban, kills the amplification defect.
- The resident exact-output case demonstrates no duplicate field solve/equivalent EML work and zero host field readback using the real FieldSweepSession dispatch/export/readback counters; a real duplicate-solve/readback rival changes the observed counts and REDs.
- Every conserved progress leg has exactly one admitted bound source; zero-bound and double-bound mutants RED/fail admission, including re-applying RF grant to already-realized Gu-Yang authority.
- Positive unbounded field-seeding feedback fails the existing bounded-feedback admission contract while a bounded/decaying equivalent admits. A planted private throughput/congestion/saturation implementation REDs against native authority.
- Return PROBATION / proof-present / DA-review-pending with focused+inherited+FieldSweep/RF batteries and hosted artifacts. Coding does not `/clearance`, merge, move pointer, or begin 7.5b.
