---
rung: COMPARATIVE-DEFAULT-BIRTH-0
kind: rung
track: 0.0.8.7
base_sha: 1ce3faba
audience: coding
model_tier: std
owner_approved: true
expected_route: DA-RESERVE(gate-wiring)
owner_notes: "DA ruling 5154348081 and merged amendment #1568 @ 1ce3faba supersede the prior 5.8b role-default clauses after three correct coder STOPs disproved them on live surface. Settled 5.8 remains authoritative for exact top1-top2 margin, winner-identity border, gross-|net| stall, authored-order tie-break, and explicit/fail-closed comparative admission. 5.8b now closes ONLY the ordinary-install topology/emitter-default seams: emitter set from GameModeSpec.region_fields, authored_order = designer list position, class_id = authored_order as f32. PALMA-D / Gu-Yang-U / Gu-Yang-C column identity remains EXPLICIT exactly as 5.8 graduated it; do not invent a default. MANDATORY fresh coding orientation after the #1568 design/orientation move. No 6.1+ or Gu-Yang conductance lowering/perf work. Reap clock 2026-08-11 unchanged."
surfaces: ["crates/simthing-spec/src", "crates/simthing-driver/src", "crates/simthing-driver/tests", "scripts/ci/test_inventory.tsv", "scripts/ci/triage_log.tsv", "docs/tests", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md"]
forbidden: ["second topology authority, topology inference, neighbor reconstruction from names, or parallel field-plan registry", "producer discriminant/operator-to-role mapping/new string namespace/property-name grammar/role enum/tag for comparative roles", "hashed, parsed, column-derived, property-derived, or incidental-vector-derived emitter class identity", "defaulting palma_d_col, guyang_value_col, or guyang_conductance_col; changing settled 5.8 explicit triad contract", "changing settled 5.8 comparative math, authored-order tie-break, 3-column comparative census, or threshold-event semantics", "scenario/corpus proof input, scenario/domain semantic branch, cross-crate fixture reach, border/frontline service, production pathfinder, CPU full-field decision scan", "new field kernel/WGSL/interpreter, Gu-Yang conductance Transient-to-Matrix lowering, Gu-Yang tiled-gather/performance work, owner-channel redesign/naming cleanup, 6.1+ or downstream BINDS-6.0 implementation"]
required_checks: ["fresh coding ORIENT-RECEIPT + current ANCHOR-ACKs + matching amended HD-RECEIPT before edits", "ordinary compile_and_install produces one typed install/admission product on SpecSessionState carrying the exact admitted FieldAdjacency plus same-authority captured neighbor rows and authored region-field/registration provenance, with no caller enrollment or second authority", "default emitter set derives ONLY from GameModeSpec.region_fields: authored_order = authored list position; class_id = authored_order as f32; value_col from the admitted registration binding for that authored field; incidental registration-vector reversal cannot change identity/order", "palma_d_col, guyang_value_col and guyang_conductance_col remain explicit settled-5.8 consumer inputs; no producer/operator/column heuristic attempts to default them", "with the same explicit triad inputs, default-emitter admission equals settled explicit 5.8 admission across 1/2/3/many emitters; one emitter remains InsufficientEmitters and authored opt-out remains visible; fixed comparative column count stays 3", "scenario-neutral GridOffsets + LinkGraph proofs cover exact topology capture/substitution falsifier, authored-order reversal invariance, CPU/oracle/GPU parity and ordinary threshold-plan compatibility", "single-path/border-pathfinder/scenario-residue/detachability/inventory/lifecycle/orientation/agent/doctrine/doc-budget and exact-head clearance gates green"]
stop_conditions: ["authored region_fields cannot be mapped to their admitted value registrations without inventing a producer/operator/name/column role heuristic", "admitted topology cannot be transported to SpecSessionState as one same-authority adjacency+neighbor artifact without a second topology authority, dependency inversion, or topology re-derivation", "default emitter derivation requires any identity other than authored_order as f32 or any order source other than authored GameModeSpec.region_fields position", "completion requires defaulting or rewriting the explicit 5.8 triad columns, Gu-Yang conductance lowering, scenario/owner-specific semantics, shipped corpus input, gate loosening, Gu-Yang performance work, owner-channel cleanup, or successor 6.1+ scope"]
---
## BUILD
- Close seam A by producing ONE typed install/admission product during ordinary `compile_and_install` and landing it on `SpecSessionState`, following the existing admitted-artifact delivery pattern. Carry the exact ALREADY-ADMITTED `FieldAdjacency` together with neighbor rows captured at the same lowering/construction site, so same-length LinkGraph substitution is unconstructible. No `compile_and_install_with_field_plan(...Some(plan))`, no caller enrollment, no topology accessor/reconstruction, no parallel registry.
- Close the surviving part of seam B ONLY for emitters. The emitter set comes from designer-authored `GameModeSpec.region_fields`. For each authored field admitted into the corresponding field-sweep registration, set `authored_order` to its position in that authored list, `class_id = authored_order as f32`, and `value_col` from the already-admitted registration binding. Preserve authored provenance across lowering; never infer authored order by enumerating an incidental registration vector.
- DO NOT default the comparative triad. `palma_d_col`, `guyang_value_col`, and `guyang_conductance_col` remain explicit settled-5.8 consumer inputs. DA `5154348081` withdrew the prior attempt because PALMA is a separate producer, Gu-Yang U writes `Matrix(source_col)`, and Gu-Yang conductance is `Transient`. Do not add a producer discriminant, operator-to-role mapping, alternate column convention, role enum/tag, or name grammar to manufacture a default.
- Consume the ordinary install product through the EXISTING 5.8 `admit_comparative_projections` path when a consumer supplies the explicit triad inputs. The defaulted portion is topology + emitter classes only: with identical explicit triad columns, callers must not have to enumerate/name emitter classes or duplicate topology. One emitter remains `InsufficientEmitters`; authored opt-out remains explicit and visible.
- Preserve every settled 5.8 semantic: margin exact `top1-top2`; border winner-identity change across canonical adjacency; stall gross minus abs(net) from the existing explicit Gu-Yang Matrix input; contest/chokepoint consume those existing outputs; comparative census 3; front/chokepoint events remain ordinary anchored threshold events. The Gu-Yang `Transient`-vs-`Matrix` substrate gap is successor work, not 5.8b.
- Prove default-emitter + explicit-triad admission is observationally identical to fully explicit 5.8 admission on scenario-neutral synthetic GridOffsets and LinkGraph cases, including 1/2/3/many emitters and registration-vector reversal with authored order fixed. Return PROBATION only.
## FENCES
- This rung is install-time transport plus emitter-default derivation over already-authored/admitted facts. It is NOT triad-role discovery and NOT a new field semantics layer.
- `class_id = authored_order as f32` is the one DA-granted default. `RegionFieldSpec::name` remains diagnostic/human-facing identity and is never converted into dominance payload. No hash, parse, column-derived id, property-name grammar, or incidental-vector identity.
- Triad columns remain explicit exactly as 5.8 graduated them. Do not weaken or silently rewrite that explicit contract to recover a zero-wiring claim. The zero-wiring claim in 5.8b is limited to emitter enumeration/identity/order and topology transport.
- Keep 6.0 graduated owner-channel semantics untouched. Preserve TP VOID, scenario containment, homogeneous lanes, Per-Generator Admission, Native Ingestion, Corpus Boundary, anchor-table sole observation, and unchanged Gu-Yang performance debt/thresholds.
## EXIT-PROOF
- Ordinary `compile_and_install` produces the typed `SpecSessionState` install/admission product without caller enrollment. That product carries the exact admitted topology as one same-authority adjacency+neighbor artifact plus emitter provenance from authored `region_fields`; a planted same-length LinkGraph substitution must be impossible/rejected.
- Default emitter derivation is exact and durable: emitter set from authored `GameModeSpec.region_fields`; `authored_order` = authored list position; `class_id = authored_order as f32`; `value_col` from its admitted registration. Reversing incidental registration/vector iteration with authored order fixed leaves winner and border unchanged; mutating authored order changes the tie-break as expected.
- Triad identity is NOT defaulted. A proof/census shows `palma_d_col`, `guyang_value_col`, and `guyang_conductance_col` still enter through the settled explicit 5.8 consumer contract, and zero new producer/operator/role/name grammar exists.
- Given the SAME explicit triad inputs, default-emitter/topology admission matches fully explicit 5.8 admission bit-for-bit across scenario-neutral GridOffsets + LinkGraph CPU/oracle/GPU proofs, with 1/2/3/many emitter cases, one-emitter `InsufficientEmitters`, authored opt-out, settled border/stall/tie-break/census/threshold behavior unchanged. Focused + containment/lifecycle/inventory/orientation/clearance gates green; no pointer move or 6.1+ dispatch by coder.
