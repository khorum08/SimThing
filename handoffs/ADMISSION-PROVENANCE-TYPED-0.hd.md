---
rung: ADMISSION-PROVENANCE-TYPED-0
kind: rung
track: 0.0.8.7
base_sha: a00215945020e1bc2cc44fbc3b42f0f552886e69
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(binding)
owner_approved: true
owner_notes: "Phase-13 authority: Board 5462798074. Kernel containment is binding: estate converges onto the domain-neutral kernel, never the reverse. 13.1 retires ROOT-CONTRACT-ADMISSION-ERROR by strengthening the existing spec/driver refusal path only. Orchestrator orientation at authoring base: ORIENT-RECEIPT 898cb5bbaed4; rule stamp cf4a20680136ef8f."
surfaces: ["crates/simthing-spec/src/error.rs", "crates/simthing-driver/src/install.rs", "crates/simthing-driver/src/session.rs", "crates/simthing-spec/tests", "crates/simthing-driver/tests", "crates/simthing-embedder/tests", "scripts/ci/constitutional_surfaces.tsv", "scripts/ci/test_inventory.tsv", "scripts/ci/anchor_reach_log.tsv", "docs/tests", "docs/tests/current_evidence_index.md", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md"]
forbidden: ["any ClauseThing baseline-red repair or oracle/assertion weakening", "any Scenario.field_plan_admission, FieldPlan, comparative-default-birth, triad-column, authored-order, class-id, or other 13.2 grammar work", "any kernel/core/gpu/sim semantic change motivated by application needs", "any new admission authority or second error taxonomy", "application-flavored SpecError variants or ClauseThing vocabulary in spec/driver admission errors", "acceptance-semantics changes: an accepted config becoming rejected or a rejected config becoming accepted", "budget changes or quieting known diagnostics", "Vector CostBand work", "any .github/workflows/** or scripts/ci/**/*.sh or scripts/ci/**/*.py gate-code edit", "track closeout or Phase-13 pointer movement from the coding lane"]
required_checks: ["before edits render/read this handoff; perform fresh Frontier coding-role orientation on the exact live dispatch master and ACK every rendered REQUIRED-ANCHOR", "FIRST STEP perform a tree-wide census of SpecError::ValidationFailedAt and any equivalent generic root-admission fallback; publish every caller before changing the type; the known baseline is five install.rs collapse sites and any additional production caller outside this handoff is a STOP, not silent scope growth", "replace the targeted generic root-admission collapse with one domain-neutral typed SpecError refusal shape carrying machine-readable law_id plus deterministic element_path; do not mint per-domain variants, a companion law enum, or a second error taxonomy", "at each promoted call site derive law_id from the existing rejection law and element_path from authored/admitted identity already in scope; never substitute the implementation site string for element identity and never invent Scenario/FieldPlan grammar to obtain provenance", "prove the promoted refusal survives InstallError -> SessionError through SimSession::open_from_spec without stringification or type erasure and is pattern-matchable by callers as exact law_id + element_path", "prove semantic equivalence with paired accepted/rejected fixtures: existing accepted configurations still admit; each targeted rejected configuration still rejects for the same semantic reason; mutate only provenance and error shape, not admission predicates", "update ROOT-CONTRACT-ADMISSION-ERROR in constitutional_surfaces.tsv from the ValidationFailedAt debt shape to the executed typed refusal witness; preserve the one constitutional census gate and do not add a sibling checker", "add focused spec/driver/embedder proof as needed and ledger every authored test in test_inventory.tsv; keep evidence current and report exact changed-file census", "because this rung changes production spec/driver code, return the structural/full-workspace certificate required by current governance; preserve the pre-13.2 baseline accounting honestly and do not repair ClauseThing reds inside this rung", "run focused crate tests plus doctrine/constitutional-surface/orientation/anchor/handoff/agent-delta/diff checks, hosted Doctrine Scan, then fresh body/head-bound /clearance and fresh /relay-lint on the exact final head"]
stop_conditions: ["the tree-wide census finds a generic admission caller whose truthful repair requires a surface outside spec/driver root admission or the named proof/data surfaces", "a law_id or deterministic element_path cannot be obtained from authority already present at the rejection site without adding new authored grammar or a second provenance registry", "preserving typed provenance through open_from_spec would require a new facade/session authority instead of the existing InstallError/SessionError chain", "any proposed fix changes whether a configuration is accepted or rejected rather than only how the rejection is represented", "any apparent need to change simthing-core, simthing-kernel, simthing-gpu, simthing-sim, ClauseThing hydration, Scenario/FieldPlan grammar, or comparative-default-birth; STOP and name the seam for DA/orchestration", "completion would alter budgets, suppress a known diagnostic, fix the ClauseThing baseline reds, or enter 13.2 scope"]
---
## BUILD
- Retire the censused `ROOT-CONTRACT-ADMISSION-ERROR` debt at the existing production root-admission seam. The current constitutional row names `SpecError::ValidationFailedAt`, and `crates/simthing-driver/src/install.rs` contains the known generic collapse sites. Strengthen that existing path; do not build a parallel diagnostic facility.
- Replace the targeted `ValidationFailedAt { site }` shape with **one domain-neutral typed `SpecError` refusal shape** whose payload is exactly the information Phase 13 requires: a machine-readable **law id** and a deterministic **element path**. The error remains part of the existing `SpecError -> InstallError::Spec -> SessionError::Install -> SimSession::open_from_spec` authority chain.
- Before editing, publish the complete tree-derived caller census. The known baseline at dispatch is five `install.rs` collapses: standalone-overlay compile diagnostics; two resource-economy property-placement lookup failures; admitted-tree host lookup failure; and base-flow-obligation participant-property failure. Treat that count as a baseline, never a target: an additional caller must be classified truthfully before proceeding.
- Give every promoted failure provenance from facts already present at its rejection site. `law_id` names the pre-existing admission law that rejected the value; `element_path` identifies the authored/admitted element that failed using stable ids/keys/indices already in scope. A filename/function/site string alone is not an element path.
- Preserve the outer API mechanically. `InstallError::Spec` and `SessionError::Install` may continue wrapping the typed `SpecError`; the proof must show that a caller of `SimSession::open_from_spec` can destructure through those wrappers and recover the exact law id + element path without parsing `Display`.
- Update the existing `ROOT-CONTRACT-ADMISSION-ERROR` constitutional-surface row so its admitted member/pattern witnesses the typed refusal and its posture is executed, with no dated deferral and no sibling census mechanism. The row must keep pointing at the real production consumer.
- Author focused falsifiers for the promoted cases. Each negative proves exact typed provenance; paired positive/legacy tests prove admission predicates did not change. Keep evidence and inventory ledgers current.
- Return PROBATION / proof-present / DA-review-pending. Coding does not merge, graduate, move the pointer, start 13.2, or close the track.

## FENCES
- **Kernel containment is absolute.** ClauseThing is an application layer; 13.1 is not permission to move application needs into the recursive kernel. A kernel/core/GPU/sim edit motivated by this rung is a STOP with the missing seam named.
- **Domain-neutral error vocabulary only.** No `ClauseThing*`, market/domain-specific, FieldPlan, comparative, star/fleet/planet, or other application-flavored `SpecError` variant. Do not create `AdmissionLaw`, `AdmissionReason`, or another companion taxonomy merely to type the same refusal; the law id is provenance carried by the existing error authority.
- Do not broaden “generic string” into a cleanup campaign over every `{ reason: String }` error in `SpecError` or `SessionError`. The target is the root-admission collapse censused as `ROOT-CONTRACT-ADMISSION-ERROR` and any mechanically equivalent caller discovered by the first-step census.
- 13.2 owns ClauseThing hydration convergence and consumes 13.1's named errors. Do not touch `hydrate_*`, `Scenario.field_plan_admission`, comparative-default-birth, triad target columns, authored-order/class identity, or any field-plan producer.
- Do not change acceptance semantics, budgets, invariant sets, execution paths, replay semantics, Vector CostBand, or the known ClauseThing baseline-red assertions. Do not make a red disappear by weakening an oracle or changing expected output.
- Gate code is fenced: no `.github/workflows/**`, `scripts/ci/**/*.sh`, or `scripts/ci/**/*.py` edits. Data ledgers such as `constitutional_surfaces.tsv`, `test_inventory.tsv`, and append-only `anchor_reach_log.tsv` remain lawful rung surfaces.
- One rung in flight. No 13.3-13.6 opportunistic cleanup and no closeout work.

## EXIT-PROOF
- **Census proof:** publish every pre-change `ValidationFailedAt` / equivalent root-admission fallback caller and reconcile the post-change tree to zero targeted generic fallbacks. No hidden caller is silently reclassified.
- **Typed-provenance proof:** for every promoted negative, the caller pattern-matches the existing wrapper chain down to one typed `SpecError` refusal and asserts the exact machine-readable `law_id` and deterministic `element_path`; no `Display` parsing.
- **Element-authority proof:** each element path is constructed from identity already authoritative at the failing site. A planted wrong law id or wrong element path must make the focused witness RED.
- **Open-from-spec proof:** `SimSession::open_from_spec` preserves the typed refusal through `InstallError`/`SessionError`; no string envelope, facade-owned result, or alternate admission entrypoint is introduced.
- **Semantic-equivalence proof:** targeted rejected configurations remain rejected for the same pre-existing semantic predicates and paired accepted configurations remain accepted. Report the before/after reason mapping explicitly.
- **Constitutional proof:** `ROOT-CONTRACT-ADMISSION-ERROR` is an executed production witness of the typed shape, with no dated debt/deferral; `constitutional_surface_check.sh` remains the one census authority and passes.
- **Containment proof:** no `simthing-core`, `simthing-kernel`, `simthing-gpu`, `simthing-sim`, ClauseThing hydration, Scenario/FieldPlan grammar, budget, Vector CostBand, or unrelated Phase-13 production delta.
- **Regression/certificate proof:** focused spec/driver/embedder tests and relevant existing admission batteries green; current doctrine/anchor/orientation/handoff/agent-delta/diff gates green; structural/full-workspace certificate reported for the production delta with ClauseThing baseline state reported, not repaired.
- **Hosted/routing proof:** hosted Doctrine Scan green at the exact final head; fresh body/head-bound `/clearance` and fresh `/relay-lint` are green/current. Return exact base, tested_code_sha/final head, changed-file census, pre/post generic-fallback census, law-id/element-path table, semantic-equivalence table, census-row before/after, certificate, and all hosted run ids.
