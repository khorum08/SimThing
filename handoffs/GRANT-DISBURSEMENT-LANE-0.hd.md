---
rung: GRANT-DISBURSEMENT-LANE-0
kind: rung
track: 0.0.8.7
base_sha: 1904fc1f4267a43270cc8d02b83d5a1068fa7896
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "DA ruling 5433789156 validated the seventh lawful STOP and minted 11.2f GRANT-DISBURSEMENT-LANE-0. The missing edge is the constitutional SECTION-12 grant/flow disbursement link: accepted grant lifecycle facts exist, but production publishes none of them into an ordinary ActionBand-bindable lane. Q1 YES: accepted-grant -> ordinary-lane publication is mandatory. Q2 YES: 11.3 will establish its load-bearing resident pre-open, so the frozen ActionBand binding law stands. Q3 NOT OPENED: no generic post-open ActionBand rebind mechanism. Orchestration is authorized to author/self-clear this handoff and relay it for pre-dispatch DA review."
surfaces: ["crates", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/tests", "scripts/ci/test_inventory.tsv"]
forbidden: ["any new market, clearing authority, allocator policy, history/replay authority, telemetry authority, generation authority, manager, dispatcher, peer executor, or second publication authority", "any direct runtime/test write helper that bypasses the existing ordinary boundary write machinery", "re-clearing grants during replay or deriving replay publication from fresh market evaluation instead of recorded facts", "same-generation grant-to-lane publication; publication must become authoritative at the next generation boundary", "a generic ActionBand binding refresh/rebind mechanism or weakening of BindingTableStale/LateInstall", "changes to graduated 11.2a-e market, lifecycle, placement, allocator-retirement, or Vendor Door semantics except the minimum consumption seam expressly required here", "weakening the two-stage placement law, opaque grant provenance, VerifiedGrowthResidencyCommit path, install-only door, or frozen ActionBand ingress-shape protections", "11.3 implementation before this rung graduates, 11.4 implementation, 12.x implementation/canonization, Vector CostBand work, or any of the three known ClauseThing baseline reds", "retired handoff receipts 890b845b5655 or aff35cbd509a", "adding cargo build/test/check to CI"]
required_checks: ["before edits render/read this handoff; perform fresh Frontier coding-role orientation on the live dispatch master even if orientation_rule_stamp is unchanged, and ACK every rendered REQUIRED-ANCHOR", "FIRST STEP map the exact landed grant lifecycle records and their existing canonical IntegrationSchedule rows for accept, renewal, revocation, partition/transfer, and release; map the existing conserved-capacity lane representation and the one ordinary boundary write path that may publish them", "FIRST STEP map one ordinary ActionBand-admitted destination class that can bind to the published conserved lane without changing the frozen ActionBand ingress/binding contract; if no such production lane/write seam exists, STOP and name the exact missing arrow rather than inventing a second writer", "make the schedule-owned publication generation-paced: a lifecycle fact recorded in generation N changes the authoritative published lane value only at the next generation boundary; no intra-generation causal shortcut", "publish the lifecycle-derived conserved capacity state through the existing ordinary write machinery only; production must have one publication authority and no direct runtime/test setter for the same lane", "prove exact lane arithmetic for the existing conservation partition free + in_flight + occupied = capacity on the published lanes, not merely in CPU accounting, across acceptance, renewal, revocation, partition/transfer, and release", "preserve grant identity/provenance and lifecycle semantics from 11.2a-c; publication consumes recorded lifecycle facts and must not become a new grant or clearing authority", "bind an ordinary ActionBand band to a published conserved lane with the resident participant established pre-open; prove a real accepted grant causes a generation-paced crossing on the graduated ActionBand execution path without any binding refresh", "replay the publication from the existing recorded schedule facts and reproduce the published lane values/crossing deterministically without any market re-clear or second history path", "plant a second-writer falsifier that attempts an independent authority over the published lane and REDs for its own named reason", "plant a replay-reclear falsifier that attempts to invoke fresh clearing during replay and REDs for its own named reason", "plant a direct-runtime-write falsifier that attempts to bypass the schedule-owned ordinary write path and REDs for its own named reason", "freeze 11.2a-e, the two-stage placement law, the frozen ActionBand binding shape, and the install-only door; do not broaden this substrate into 11.3 composition work", "run focused lifecycle/publication/ActionBand/replay tests plus relevant frozen B and ActionBand batteries, inventory/drift, Agent/Doctrine/orientation/lifecycle checks; return exact base/head/tested_code_sha and hosted workflow IDs", "structural certificate remains DA-side at graduation; inherited baseline is 120 suites / 461 passed / 3 failed / 14 ignored"]
stop_conditions: ["the canonical schedule does not contain enough authoritative lifecycle fact to derive publication without re-clearing or a new history source", "the existing ordinary boundary write machinery cannot publish the conserved lane without inventing a second writer/authority", "the proposed ActionBand witness requires post-open binding refresh, weakens BindingTableStale/LateInstall, or depends on ordinary growth after binding", "the partition invariant can be shown only in CPU bookkeeping and not on the authoritative published lanes", "accept/renew/revoke/partition/release require divergent publication authorities or non-generation-paced special cases", "replay correctness requires fresh clearing, test arithmetic, a direct write helper, or a second replay/history path", "completion requires changing frozen 11.2a-e semantics, implementing 11.3/11.4/12.x, Vector CostBand, or ClauseThing-red work"]
---
## BUILD
- Implement **11.2f `GRANT-DISBURSEMENT-LANE-0` only**. This is the missing production `grant/flow disbursement` edge exposed by 11.3's map-first STOP.
- Publish accepted grant **lifecycle facts** — acceptance, renewal, revocation, partition/transfer, release — into the existing conserved-capacity lanes as ordinary **generation-paced** lane values through the existing boundary write machinery.
- Consume the lifecycle facts already recorded on the canonical schedule. Publication is realization of recorded authority, not a new market/clearing decision.
- The authoritative lane update occurs at the **next generation boundary**, never intra-generation.
- Prove `free + in_flight + occupied = capacity` with exact arithmetic on the published lanes across the full lifecycle.
- Witness the arrow needed by 11.3 here: a real accepted grant changes a published lane and an ordinary ActionBand band bound to that lane observes the resulting crossing on the graduated path.
- Establish the ActionBand-observed resident pre-open. Do **not** add or weaken any binding refresh/rebind mechanism.
- Keep the workplan pointer at 11.3. 11.3 remains blocked until this substrate graduates.

## FENCES
- **One writer, one history, one generation authority.** The existing schedule-owned ordinary write path is the publication authority; no peer writer, telemetry state, shadow ledger, or helper setter.
- **Replay realizes; replay never re-clears.** Recorded lifecycle facts are sufficient input to reconstruct the lane values.
- **No direct write shortcut.** Test code may inspect/assert, but must not create the production causal arrow by manually writing a lane.
- **No ActionBand rebind.** `BindingTableStale`, `LateInstall`, the frozen ingress shape, and the pre-open installation model remain graduated protections.
- **No semantic reopening of B.** 11.2a-e market grammar, lifecycle law, provenance seals, two-stage placement, allocator retirement, and Vendor Door stay frozen except for consuming their already-authoritative products/facts.
- Do not start the 11.3 convergence scenario in this rung. This rung proves only the grant -> published lane -> ordinary ActionBand crossing edge that 11.3 may later compose.
- 11.4, 12.x, Vector CostBand and the three ClauseThing baseline reds remain fenced.

## EXIT-PROOF
- **Lifecycle publication seal:** accept, renewal, revocation, partition/transfer, and release each drive the authoritative conserved lane values through the one ordinary generation-boundary write path.
- **Lane conservation seal:** exact published-lane arithmetic proves `free + in_flight + occupied = capacity` at every lifecycle transition.
- **Generation pacing seal:** a generation-N fact cannot affect the published lane intra-generation and becomes visible at the next generation boundary.
- **ActionBand causality seal:** with the resident pre-open and binding shape unchanged, a real accepted B grant causes a crossing in an ordinary ActionBand band bound to the published lane.
- **Replay seal:** canonical replay reconstructs the same published lane values and crossing from recorded facts without re-clearing.
- **Second-writer RED:** a planted independent writer/authority for the same lane is rejected for its named one-writer violation.
- **Replay-reclear RED:** a planted replay-side fresh clear is rejected for its named violation.
- **Direct-write RED:** a planted runtime/test shortcut around the schedule-owned ordinary write path is rejected for its named violation.
- **Authority census:** zero new market, clearing, history, telemetry, generation, manager, dispatcher, peer executor, or rebind authority. DA structural certificate is owed at exit; inherited baseline **120/461/3/14**.
