---
rung: CONTENTION-ARENA-EXECUTED-0
kind: rung
track: 0.0.8.7
base_sha: ee3e20d61c84e005aff5aeff21c8b37087181f50
audience: coding
model_tier: frontier
owner_approved: true
expected_route: DA-RESERVE(gate-wiring)
owner_notes: "DA ruling 5337607437 APPROVED Frontier dispatch with bounded A1 and gate-wiring option 2. Implement GENERIC CONSTRAINED CLEARING, not a contention/combat service. Consume landed 6.0 owner-channel segregation/priority, 6.1b CostBand, 6.5 StemThing-A lane vocabulary, 7.3 ActionBand ordinary claims, 8.1 conservation, and overlay lifecycle. The corrected 8.1 falsifier law stands: wrong implementations belong test-side or are excluded by type; never ship caller-selectable defect switches. Coding MUST NOT edit scripts/ci/allow/contention_mechanisms.txt; DA will author companion attrition rows at graduation from the mechanisms actually landed. Return must list each landed persistence/attrition mechanism with ready-surface and promotion-blocker; if any gate requires a registry row before graduation, STOP rather than edit the file."
surfaces: ["crates/simthing-spec/src", "crates/simthing-core/src", "crates/simthing-kernel/src", "crates/simthing-sim/src", "crates/simthing-driver/src", "crates/simthing-driver/tests", "scripts/ci/test_inventory.tsv", "scripts/ci/triage_log.tsv", "docs/tests"]
forbidden: ["contention/combat/adversarial participant taxonomy, resolution enum, arena-local owner identity/equality, second ownership reconstruction, or non-OwnerChannelScopeKey grouping", "physical/vector iteration order as policy, queue special-case, or a new claim-priority mechanism beside landed 6.0b priority", "same-generation clear->persist->mutate->re-clear/convergence or any persistence/action consequence faster than generation pacing", "ActionBand-local clearing/oracle/claim kind/allocator/peer executor or bypass of generic claim->clear->disburse", "conflating unresolved U with CostBand R, cross-resource conservation, or weakening graduated 8.1", "Permanent/Never contention outcome or any outcome overlay without UntilDissolved plus authored dissolution condition", "Vector CostBand atomic scarce-lane holding, StemThing-B, Phase 9+, corpus rehearsal as oracle, pointer/ladder/workflow/gate widening, editing scripts/ci/allow/contention_mechanisms.txt, any rival contention allow file, or OVERLAY-PEER-AUTHORITY retirement"]
required_checks: ["fresh Frontier coding ORIENT-RECEIPT at rule stamp 61818ff7d4adda84 + rendered HD-RECEIPT + every REQUIRED-ANCHOR ACK before edits", "scenario-neutral ordinary claim->clear->disburse handles both trivial and oversubscribed cases; changing ONLY authored clearing data changes allocation with no code change", "priority consumes landed CommandDeficit/RuntimeOwnerSiloDemandBucket priority; owner buckets remain OwnerChannelScopeKey-segregated with zero owner comparisons/reconstruction; row-order mutant disagrees/REDs", "U=requested-not-granted and CostBand R=granted remainder are independently observed and distinct; unresolved U affects persistence/CostBand consequence only in a later generation with no same-generation re-clear", "every clearing/persistence outcome represented as an overlay is UntilDissolved with authored condition; Permanent/Never is unconstructible or admission-red", "ActionBand-originated claim clears through the same generic path; test-side/local-shortcut mutant disagrees/REDs", "graduated 8.1 judge remains unweakened and judges executed grants/disbursements green", "focused owner-channel/priority/CostBand/lifecycle/ActionBand/StemThing-A/driver/kernel batteries plus inventory/lifecycle/detachability, Agent Scan, Doctrine/doc-budget, exact-head clearance and relay-lint are green; return lists each landed persistence/attrition mechanism with ready-surface + promotion-blocker for DA companion registry rows"]
stop_conditions: ["generic clearing requires a new runtime authority, owner plane, participant taxonomy, second allocator/reconstruction, or priority cannot consume landed 6.0b priority without a parallel seam", "a real persistence consumer requires same-generation re-clear/convergence rather than later-generation consequence, or ActionBand requires a local clearing path", "proof requires persistent atomic common-depth holding across independently contested scarce lanes; that belongs to VECTOR-COSTBAND-PROBE-0", "a lawful outcome requires weakening 8.1 conservation, conflating U with R, or dropping UntilDissolved lifecycle law", "implementation needs a scripts/ci/allow/contention_mechanisms.txt row to pass a gate before DA graduation companion work; STOP and report rather than editing that file or minting another gate/registry"]
---
## BUILD
- Promote the existing RF path into one generic constrained-clearing surface: owner-channel claims reduce up, authored numerical data allocates bounded supply, grants disburse down; trivial and oversubscribed clears are one mechanism and contention is only the observed posture.
- Consume landed `OwnerChannelScopeKey` buckets, 6.0b priority and existing order-weight semantics directly. Proportional/priority/price-driven laws use one authored numerical surface; never re-derive owner identity or let engine kind/row order choose policy.
- Keep `U` distinct from CostBand `R`: clearing produces allocation plus unresolved demand; CostBand separately quantizes granted/held value into executable depth and remainder.
- Factor attrition/adversarial persistence temporally: unresolved `U` -> ordinary observation/field input -> authored EML valuation -> CostBand-funded consequence -> later-generation state/claims. No intra-generation re-clear.
- Express outcomes through ordinary overlays using `UntilDissolved` plus authored dissolution; route ActionBand claims through the identical generic path and defer atomic multi-lane common-depth holding to Vector CostBand.
- Keep graduated 8.1 independent and unchanged; it judges executed accounting but does not author the executor or weaken to fit it.
## FENCES
- Mechanisms, not domains: no combat/contention service, participant/queue taxonomy, resolution enum, owner comparison/reconstruction, row-order policy, parallel priority seam, or alternate sink.
- One generic clearing path for ordinary and ActionBand claims; no facility-local allocator/oracle or peer executor.
- Generation pacing is constitutional: `clear -> persist -> consequence -> new claim` crosses a generation boundary; never build an intra-generation solver.
- `U != R`; one channel/one seam conservation stays exact, and every outcome overlay is `UntilDissolved` with authored condition and ordinary explicit removal.
- Keep Vector CostBand atomic holding, StemThing-B, Phase 9+, corpus oracles, pointer/ladder/gate/workflow expansion, any `contention_mechanisms.txt` edit/rival allow file, and `OVERLAY-PEER-AUTHORITY` retirement out.
## EXIT-PROOF
- Inline claims exercise fit-capacity and oversubscribed execution; changing only authored clearing data changes a lawful allocation while code/claims remain identical, deterministic and 8.1-conserving.
- Prove priority uses landed data rather than physical order; owner segregation has no comparison/reconstruction; ActionBand takes the same path; all wrong-policy shortcuts live test-side or are unrepresentable.
- Expose distinct `U` and `R`; carry `U` only into a later-generation persistence/CostBand consequence; prove no same-generation re-clear and no permanent outcome shape.
- Re-run 8.1 plus affected owner-channel/priority/CostBand/lifecycle/ActionBand/StemThing-A/driver/kernel and governance batteries. Return PROBATION with exact mechanism list for DA companion registry rows; coding does not merge, move pointer, begin Vector CostBand/StemThing-B/Phase 9+, or retire `OVERLAY-PEER-AUTHORITY`.
