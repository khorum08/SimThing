---
rung: UNIFIED-INGRESS-EXCLUSIVITY-0
kind: rung
track: 0.0.8.7
base_sha: f501214ece2b23de3c4fd1fd60cf5d70810b9189
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "DA ruling 5447643638 closed 11.3, moved the pointer to 11.4, and explicitly authorized orchestration to author this handoff on fresh master f501214e. This rung mechanizes the Owner single-ingress law as a closed census gate. Extend the existing constitutional surface census/checker; do not create a parallel governance or runtime authority. The existing seven constitutional selftest plants become exactly nine under this rung. The paired EmlGadgetInstanceSpec/EmlGadgetKind census remains closed and 1:1."
surfaces: ["scripts/ci/constitutional_surface_check.sh", "scripts/ci/constitutional_surfaces.tsv", "scripts/ci/doctrine_selftest.sh", "scripts/ci/scans.tsv", "scripts/ci/test_inventory.tsv", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/tests"]
forbidden: ["a second constitutional census/checker authority parallel to scripts/ci/constitutional_surface_check.sh + constitutional_surfaces.tsv", "name-only or spelling-only exclusivity enforcement whose falsifier fires on rename instead of semantic bypass", "a second runtime execution, contention, resolution, admission, history, replay, or telemetry authority", "silently treating a graduated producer with no non-test production consumer as acceptable", "an undated or implicit consumer deferral", "weakening/removing any existing constitutional census member, including the paired EmlGadgetInstanceSpec and EmlGadgetKind vocabularies", "changing graduated 11.1-11.3 production semantics merely to make the census green", "12.1/12.2 implementation or canonization, Vector CostBand work, or any of the three known ClauseThing baseline reds", "retired handoff receipts 890b845b5655 or aff35cbd509a", "adding cargo build/test/check to CI"]
required_checks: ["before edits render/read this handoff; perform fresh Frontier coding-role orientation on the live dispatch master even if the orientation rule stamp is unchanged, and ACK every rendered REQUIRED-ANCHOR", "FIRST STEP archaeology the current scripts/ci/constitutional_surface_check.sh and constitutional_surfaces.tsv as the one census authority; map how doctrine_scan/scans.tsv and selftests invoke it, and extend that authority rather than introducing a sibling gate", "FIRST STEP enumerate every censused constitutional surface and every graduated producer in scope, then map each to its exact non-test production consumer or to an explicit dated deferral row; a test-only consumer does not discharge the obligation", "FIRST STEP map every production ingress that can reach RF Triad contention/resolution and every production execution path that can reach simulation resolution; identify the one lawful unified chain and enumerate any apparent peers before editing", "preserve the current closed EML gadget census exactly: EmlGadgetInstanceSpec and EmlGadgetKind remain paired 1:1 and closed; any vocabulary drift continues to require DA approval", "extend the existing constitutional registry with the minimum structured fields/rows needed to express producer, non-test production consumer, lawful unified ingress/path, and dated deferral evidence; do not create a second ledger with overlapping authority unless the existing TSV shape is technically incapable, in which case STOP and report the exact shape conflict", "mechanize three independent law failures with stable named reasons: a graduated producer without a non-test production consumer and without a lawful dated deferral; a second production execution path reaching contention/resolution; and a censused constitutional surface reachable outside the unified ingress", "the law must be semantic rather than spelling-based: a harmless rename that preserves the same admitted role must not itself trip exclusivity, while adding an actual bypass/peer path must trip it", "preserve all seven existing constitutional selftest mutations and add exactly two new law-level planted mutations so constitutional_surface_check.sh --selftest reports planted=9; the two new plants must collectively exercise the new 11.4 law and may assert multiple independently named failure reasons where one semantic bypass violates more than one clause", "prove the parent-phase-contract versus child-rung-proof gap is representable by the census and fails when a required production consumer is absent even if a child-rung proof/test exists", "prove the producer-graduated-without-consumer gap independently; test-only consumers, comments, and evidence prose do not count as production consumers", "dated deferrals are explicit structured census data with a concrete date and rationale/owner or equivalent existing governance provenance; missing/undated deferral is not silence-equivalent success", "run the constitutional surface checker in normal and selftest modes, doctrine selftests/scans, test inventory/drift, Agent/Doctrine/orientation/lifecycle checks, and the relevant frozen 11.1-11.3 batteries; return exact base, tested_code_sha, final head, and hosted workflow IDs", "structural certificate is DA-side at graduation; current inventory after 11.3 is 122 suites / 466 passed / 3 failed / 14 ignored, while the last full workspace structural certificate remains 121 / 465 / 3 / 14 from 11.2f"]
stop_conditions: ["the Owner single-ingress law cannot be enforced by extending the existing constitutional census/checker without creating a second overlapping governance authority", "a complete producer-to-consumer census requires inventing or changing production runtime semantics rather than observing existing production consumers or recording a dated deferral", "the only way to detect bypass is by symbol spelling/name matching such that a harmless rename is treated as a violation", "more than one lawful production execution path into RF Triad contention/resolution exists and closing it requires a design choice beyond this census rung", "the paired EmlGadgetInstanceSpec/EmlGadgetKind census cannot remain closed and 1:1 without reopening prior semantics", "completion requires 12.1/12.2, Vector CostBand, or ClauseThing-red work"]
---
## BUILD
- Implement **11.4 `UNIFIED-INGRESS-EXCLUSIVITY-0` only**. This is the Owner single-ingress law mechanized as a **closed census gate**, not a new runtime facility.
- Extend the existing authority at `scripts/ci/constitutional_surface_check.sh` + `scripts/ci/constitutional_surfaces.tsv`. Reuse its parsing/census/selftest model where technically fit. Do not create a parallel generic exclusivity checker merely to satisfy this rung.
- The gate must close three links:
  1. graduated/unified producer or constitutional surface -> an exact **non-test production consumer** (or explicit lawful dated deferral);
  2. unified ingress -> the **only** production ingress to RF Triad contention/resolution;
  3. unified SimThing execution -> the **only** production execution path for simulation resolution.
- Census facts are evidence about the production tree. They do not authorize a new runtime dispatcher, manager, executor, admission door, history surface, or telemetry surface.
- Keep pointer at 11.4 in coding. Do not implement/canonize 12.2.

### EXISTING CONSTITUTIONAL AUTHORITY TO INHERIT
- `constitutional_surfaces.tsv` is already a closed census consumed by `constitutional_surface_check.sh`; preserve every existing row unless a separately authorized prior-law correction is unavoidable.
- In particular, preserve the paired closed gadget vocabularies:
  - `EmlGadgetInstanceSpec`: FieldSampler, WeightedAccumulator, SoftStep, VelocityMonitor, Decay, Ema, BoundedFeedback, Hysteresis, Acceleration, PowerLaw.
  - `EmlGadgetKind`: the same ten members, 1:1 with instance vocabulary.
- Preserve the existing seven selftest plants:
  `eml-opcode`, `eml-stack`, `target-form`, `cap-widening`, `crossing-rival`, `telemetry-unbound`, `root-nullary`.
  Add exactly **two** law-level semantic plants; final constitutional selftest line must report **`planted=9`** and keep the existing valid telemetry-binding control green.

## FENCES
- **Consumer means production consumer.** A test, fixture, proof helper, comment, inventory row, or replay-only assertion cannot discharge a graduated producer's consumer obligation.
- **Deferral means explicit dated deferral.** If a lawful producer has no production consumer yet, represent that fact honestly with a concrete dated governance record rather than dropping the row or treating absence as success.
- **One gate, not two.** New columns/parsers/checks inside the existing constitutional census are expected if needed. A second overlapping registry/checker with its own truth is forbidden.
- **Law over spelling.** The checker may use source structure to locate declared/admitted roles, but its constitutional assertion must be about role/reachability/consumer relationships. A rename-only mutant is not a valid RED; a real alternate path or missing consumer is.
- Do not weaken prior EML, ActionBand, StemThing, OverlayThing, replay, admission, or execution laws to make the census close.
- 12.1 portability, 12.2 canonization, Vector CostBand, and the three ClauseThing reds remain fenced.

## EXIT-PROOF
- **Producer-consumer closure:** every censused graduated producer/unified surface has an exact non-test production consumer or explicit lawful dated deferral. Plant a graduated producer with neither and require the producer-consumer failure reason.
- **Single contention ingress:** census finds exactly the lawful unified ingress to RF Triad contention/resolution. Plant a real peer/bypass shape and require the second-ingress/execution-path failure reason.
- **Single simulation execution:** no production execution path reaches simulation resolution except through the unified SimThing path. An alternate production path must fail even if it is renamed or does not reuse the canonical symbol spelling.
- **Surface bypass seal:** a censused constitutional surface cannot reach resolution outside unified ingress; an actual bypass must fail with its own named surface-bypass reason.
- **Parent/child proof seal:** a parent-phase consumer obligation is not discharged merely because a child rung has a proof/test; remove the production consumer while retaining child proof surfaces and require RED.
- **Closed gadget census:** both ten-member gadget vocabularies remain closed and 1:1.
- **Constitutional selftest repair:** all prior seven plants continue to RED for their original reasons; exactly two new semantic plants bring the reported total to `planted=9`; valid-binding control remains green.
- **Rename control:** harmless symbol/name refactor preserving the same admitted role and consumer/path relation stays green; this demonstrates the gate bites law rather than spelling.
- **Authority census:** zero new runtime execution/contention/resolution/history/replay/telemetry authority. DA structural certificate is mandatory at graduation.
