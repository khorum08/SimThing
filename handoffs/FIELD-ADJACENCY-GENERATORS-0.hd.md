---
rung: FIELD-ADJACENCY-GENERATORS-0
kind: rung
track: 0.0.8.7
base_sha: bbe45411c611ffacb117966867b41f1f28ccd0b1
audience: coding
model_tier: frontier
owner_approved: true
expected_route: DA-RESERVE(gate-wiring)
owner_notes: "Owner-approved 5.x amendment #1482; DA graduation ruling 5143085476. 5.5 landed one bit-exact generic N4 interpreter. 5.6 owns adjacency; 5.7 owns resource/performance specialization. Emergence is the exit falsifier; 2026-08-11 remains sovereign."
surfaces: ["crates/simthing-core/src", "crates/simthing-kernel/src/field_sweep.rs", "crates/simthing-kernel/src/shaders/field_sweep.wgsl", "crates/simthing-gpu/src", "crates/simthing-driver", "crates/simthing-clausething/tests/stead_spatial_contract_guards.rs", "scripts/ci", "docs"]
forbidden: ["5.7 resource/performance work or 5.8/Phase 6+", "algebra tags, operator dispatch, second interpreter, or new bespoke WGSL", "implicit N8 diagonal metric or scheduler-authored chi/weights/order", "corpus/scenario/referee edits, oracle deletion, or lifecycle exemption"]
required_checks: ["workspace + core/kernel/gpu/driver suites", "adapter-pinned parity for N4/N8/radius-r/LinkGraph", "same-program emergence falsifier + planted alias", "conductance/order/bucket admission negatives", "zero-production-legacy-caller census + planted selftest", "FIELD-SWEEP-SINGLE-PATH + STEAD + inventory/lifecycle/detachability", "agent/doctrine/orientation/anchors/doc-budget/clearance"]
stop_conditions: ["stale-orient-receipt", "one interpreter or existing LinkGraph canonical-list basis is unavailable", "emergence is indistinguishable or N8 needs an implicit metric", "scheduler must author physics or reorder neighbors", "5.7 work, new kernel, corpus edit, oracle deletion, or scope widening is required"]
---
## BUILD
- Generalize sealed adjacency registration to weighted `GridOffsets [(dx,dy,w)]` plus LinkGraph lists over existing `INPUT_LIST`, preserving one interpreter and typed identities to the wire frontier. Admit N4, N8, and radius-r constructors with every weight explicit; no free diagonal metric.
- Admit LinkGraph only from the existing compiler's sorted+deduped undirected lists; mint order/symmetry proofs from that output, with no second sort, graph registry, or topology dispatch.
- Seal per-node metadata proving `chi_i * sum_j(abs(c_ij)) <= admitted_bound`; reject malformed/non-finite/over-bound input. Degree buckets may read admitted metadata only and may not choose chi, weights, or neighbor order; session binding includes lists, weights, proofs, and layout.
- Move all production callers of the seven bespoke shaders to the generic path while retaining those implementations unmodified as test-only oracles. Add a permanent zero-production-caller census with a planted production-call failure.
- Add only load-bearing parity/admission/order proofs, one results doc/index line, inventory/status rows, and 5.6 PROBATION. Coding does not move the pointer.
## FENCES
- Use the same authored program, coefficients, seed, and observation rule for N4, N8, and LinkGraph; only adjacency differs. Output-derived geometry must be diamond, authored-octagonal/non-Manhattan, and topology-following respectively; a planted adjacency alias must fail.
- No performance verdict, border/chokepoint projection, resource specialization, counter work, or oracle deletion. Preserve 5.5 bit-exactness, `FIELD-SWEEP-SINGLE-PATH`, replay determinism, homogeneous lanes, Native Ingestion, Corpus Boundary, and the Invariant Set.
## EXIT-PROOF
- N4/N8/radius-r/LinkGraph execute through one CPU/GPU path bit-exactly; bad certificates/order reject pre-dispatch; bucketed and unbucketed execution preserve authored order and bits.
- The same-program emergence falsifier distinguishes all three geometry classes and the planted alias flips red.
- Zero production callers of all seven bespoke shaders is grep/census-proven while oracle comparisons and planted caller/algebra/shader tests bite; all required suites, guards, lifecycle/scans, evidence, and exact-head clearance are green. Return PROBATION to DA; no 5.7 dispatch.
