---
rung: FIELD-ADJACENCY-GENERATORS-0
kind: rung
track: 0.0.8.7
base_sha: bbe45411c611ffacb117966867b41f1f28ccd0b1
audience: coding
model_tier: frontier
owner_approved: true
expected_route: DA-RESERVE(gate-wiring)
owner_notes: "Owner-approved 5.x amendment #1482; DA graduation 5143085476; consolidated Remand-1 revision 5143977793 supersedes all earlier 5.6 DA notes. 5.6 owns adjacency; 5.7 owns resource/performance specialization. Emergence is the exit falsifier; 2026-08-11 remains sovereign."
surfaces: ["crates/simthing-core/src", "crates/simthing-kernel/src/field_sweep.rs", "crates/simthing-kernel/src/shaders/field_sweep.wgsl", "crates/simthing-gpu/src", "crates/simthing-driver", "crates/simthing-clausething/tests/stead_spatial_contract_guards.rs", "scripts/ci", "docs"]
forbidden: ["5.7 resource/performance work or 5.8/Phase 6+", "algebra tags, operator dispatch, second interpreter, or new bespoke WGSL", "implicit N8 metric or scheduler-authored chi/weights/order", "LinkGraph budget type, ladder/digest edit, or >1024 sweep beyond one case", "game-corpus prerequisite, oracle deletion, or lifecycle exemption"]
required_checks: ["workspace + core/kernel/gpu/driver suites", "adapter-pinned full-buffer CPU/GPU parity for N4/N8/radius-r/LinkGraph, including one inline synthetic LinkGraph >1024 slots", "same-program emergence falsifier + planted alias", "conductance/order/bucket negatives and Gu-Yang unrelated-column preservation", "permanent legacy-caller and dense-cap-crossing census arms, each with planted red fixture", "FIELD-SWEEP-SINGLE-PATH + STEAD + inventory/lifecycle/detachability", "anchor resync, fresh coding orientation, agent/doctrine/doc-budget/exact-head clearance"]
stop_conditions: ["stale-orient-receipt", "one interpreter or existing LinkGraph canonical-list basis is unavailable", "emergence is indistinguishable or N8 needs an implicit metric", "scheduler must author physics, reorder neighbors, or borrow an unauthorised matrix column", "5.7 work, new kernel, game-corpus prerequisite, oracle deletion, or scope widening is required"]
---
## BUILD
- Generalize sealed adjacency registration to weighted `GridOffsets [(dx,dy,w)]` plus LinkGraph lists over existing `INPUT_LIST`, preserving one interpreter and typed identities to the wire frontier. Admit N4, N8, and radius-r constructors with every weight explicit; no free diagonal metric.
- Admit LinkGraph only from the existing compiler's sorted+deduped undirected lists; mint order/symmetry proofs from that output, with no second sort, graph registry, topology dispatch, dense-grid constant, or magnitude ceiling.
- Seal per-node metadata proving `chi_i * sum_j(abs(c_ij)) <= admitted_bound`; reject malformed/non-finite/over-bound input. Degree buckets may read admitted metadata only and may not choose chi, weights, or neighbor order; session binding includes lists, weights, proofs, and layout.
- Repair SaturatingFlux lowering so transient conductance never borrows an unrelated authored matrix column. Full-buffer parity must preserve every column except the target field and an explicitly authored choke output.
- Move all production callers of the seven bespoke shaders to the generic path while retaining those implementations unmodified as test-only oracles. In one permanent gate file add legacy-caller and dense-cap-crossing census arms; plant a known-bad production caller and a known-bad `REGION_FIELD_*` reach, and prove each arm flips red.
- Use minimal inline synthetic fixtures for substrate law, add only load-bearing proofs/results/index/inventory/status rows, and return 5.6 PROBATION without moving the pointer.
## FENCES
- Use the same authored program, coefficients, seed, and observation rule for N4, N8, and LinkGraph; only adjacency differs. Output-derived geometry must be diamond, authored-octagonal/non-Manhattan, and topology-following respectively; a planted adjacency alias must fail.
- Exactly one adapter-pinned LinkGraph parity case uses an inline synthetic graph with slot count greater than 1024; no broader scale sweep is owed.
- No LinkGraph budget type and no ladder/digest edit. Dense-theater constants remain absent from `field_sweep.rs` and driver adjacency lowering; pre-existing ClauseThing corpus-reader drift is disclosed, not charged.
- No performance verdict, border/chokepoint projection, resource specialization, counter work, or oracle deletion. Preserve 5.5 bit-exactness, replay determinism, homogeneous lanes, Native Ingestion, Corpus Boundary, and the Invariant Set.
## EXIT-PROOF
- N4/N8/radius-r/LinkGraph execute through one CPU/GPU path with full-buffer bit-exactness; one LinkGraph case exceeds 1024 slots; bad certificates/order reject pre-dispatch; bucketed and natural-order execution preserve authored order and bits.
- Gu-Yang matches the retained oracle across every column and cannot mutate an unrelated column when no choke output is authored.
- The same-program emergence falsifier distinguishes all three geometry classes and the planted alias flips red.
- Both permanent census arms and their planted violations bite; anchor resync is followed by fresh coding orientation, and all suites, guards, lifecycle/scans, evidence, and settled-body exact-head clearance are green. Return PROBATION to DA; no 5.7 dispatch.
