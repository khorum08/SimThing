---
rung: ACTIONBAND-OVERLAY-ACTUATION-0
kind: rung
track: 0.0.8.7
base_sha: 35ec31635c03d0a138533bfd4cbe36eae9fc19ef
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "DA ruling 5298449003 graduated 7.7 and authorized orchestration to author/self-clear 7.8 for pre-dispatch review. Fresh orchestrator ORIENT-RECEIPT a058e3dd0c1a at rule stamp ea1f69606c6929ea. 7.8 must implement the one crossing-to-consequence actuation door while preserving 7.3 as the reference instance, 7.7 lifecycle/EML/logical-identity fences, and the open 7.8 promotion blocker from binding_conditions.tsv. OVERLAY-PEER-AUTHORITY remains live through 7.8 and retires only at 10.1."
surfaces: ["crates/simthing-core/src", "crates/simthing-spec/src", "crates/simthing-kernel/src", "crates/simthing-gpu/src", "crates/simthing-driver/src", "crates/simthing-sim/src", "crates/simthing-core/tests", "crates/simthing-spec/tests", "crates/simthing-kernel/tests", "crates/simthing-gpu/tests", "crates/simthing-driver/tests", "crates/simthing-sim/tests", "crates/simthing-workshop/tests", "scripts/ci/overlay_germ_archaeology_census.tsv", "scripts/ci/overlay_germ_archaeology_census_universe.txt", "docs/tests"]
forbidden: ["second threshold/crossing detector, second post-crossing dispatcher, ActionThing/OverlayManager/peer executor, direct world mutation, or a new action/event scheduler", "shared resident plane across facilities, direct foreign resident-plane writes, foreign facility ownership, overlay-local swap/epoch/freshness authority, or owner GPU identity-plane work", "StructuralAuthorization encoded as or allowed to become a GPU state-plane write; it is authorization for the existing sealed BoundaryRequest egress only", "OverlayThing-local EML namespace/table/evaluator/cache, duplicate arithmetic semantics, cache identity outside (ProgramId, ArithSemanticsVersion, LoweringArm, ExecutionShape, BindingABI), or any cross-instance CSE claim", "durable semantic identity by physical row/slot/buffer offset; durable targets remain logical identity + PropertyId + role/binding identity and must survive physical remap", "runtime mutation of STEAD/PALMA/Gu-Yang adjacency, canonical order, conservation/symmetry proof, chi/stability certificate, field-law identity, or any parameter outside the admitted certificate envelope", "private flux/throughput/congestion/saturation solver or ActionBand-local replacement for Gu-Yang/RF authority", "unbounded positive feedback, same-generation emit-apply-recross recursion, hidden scheduler cadence, or a feedback loop admitted merely because its rate is generation-paced", "rewriting CostBand generally or moving CPU oracle/tooling implementations; absorb only a TRUE CPU numerical N=floor(V/C) authority leak that directly feeds production GPU actuation, if one still exists", "editing binding_conditions.tsv, orchestrator orientation, ladder/pointer, workflows/scans/allowlists/gates, retiring OVERLAY-PEER-AUTHORITY, or hand-editing the 7.6 census universe pin", "7.8a span/projection/invalidation substrate, 7.9 fractal/designer closure, 8.x, Vector CostBand, StemThing-B, local-minimum/navigation research, or speculative compaction/profile work"]
required_checks: ["fresh Frontier coding ORIENT-RECEIPT at rule stamp ea1f69606c6929ea + rendered HD-RECEIPT + every REQUIRED-ANCHOR ACK before edits", "before implementation, identify the exact landed reference surfaces for 7.3 subordinate activation, 7.5c movement-vendor actuation, 7.7 lifecycle/OverlayStateNext and routed receive seams, field certificate-envelope admission, bounded-feedback admission, and the production CostBand quotient path; return exact citations/trace", "implement ONE session-frozen CrossingConsequenceBinding substrate with exactly three admitted arms: ResidentNextWrite, RoutedOverlayDelivery, StructuralAuthorization; neither ActionBand nor Overlay may mint another dispatcher", "ResidentNextWrite must target a destination belonging to the facility-local resident plane already bound to the consequence (including ActionBandStateNext, OverlayStateNext, or another admitted parameter/state lane Next); ownership must be type/admission checked before buffer access; a planted direct foreign resident-plane write must RED for that exact reason", "RoutedOverlayDelivery must use ordinary attributable origin + target + admitted routing/filtering, cross-tree only through the existing stamped product/receive seam, and land through the ordinary destination receive ingress; positive witness proves origin attribution, filter traversal, generation pacing, and receive-leg arrival with no hidden global route", "StructuralAuthorization must reuse the existing sealed BoundaryRequest/structural authorization egress and remain authorization only; prove no numerical GPU plane write or direct world mutation can be constructed through this arm", "prove Generation-Paced Actuation: crossing at generation t authorizes/writes the lawful Next or sealed boundary destination; newly activated/parameterized overlay consequence participates no earlier than t+1; same-generation apply-recross-reemit mutant REDs", "full Field-Triad overlay binding witness: one admitted overlay parameterizes STEAD source/falloff, PALMA W/terminal value, and Gu-Yang conductance/capacity inputs strictly inside each existing certified registration envelope; certificate-envelope mutant (out-of-envelope parameter OR certificate/adjacency/order/conservation/chi mutation) hard-errors at admission", "field-seeded recurrent emission must pass existing bounded-feedback admission; plant an unbounded positive-feedback variant on the real production route and RED it for the exact admission reason", "re-anchor the graduated 7.5c movement vendor as the door's first production consumer with bit-identical real-GPU results; movement proves the door rather than defining it; if consolidation touches the 7.3 subordinate-activation reference path, use oracle-first parity and delete the superseded dispatcher in the same change", "run the engineering CostBand quotient audit on every production path feeding this actuation door; if any such path still resolves N=floor(V/C) numerically on CPU, migrate ONLY that authority leak into the existing GPU CostBand resolver and retain CPU oracle/tooling; if none exists, cite the audited production call graph and do not widen scope", "discharge the open 7.8 binding condition: for overlay-local EML-surface absence and durable-row-capture avoidance, plant each forbidden shape on the constructible 7.8 surface and RED it, OR prove the shape is unrepresentable at a type/admission boundary with exact citation; audited assertion alone is insufficient", "prove canonical EML identity/reuse remains shared with 7.7: no local program registry/cache/evaluator, no duplicate arithmetic semantics, artifact identity remains (ProgramId, ArithSemanticsVersion, LoweringArm, ExecutionShape, BindingABI), and no cross-instance CSE claim", "prove durable consequence bindings survive forced physical row/slot remap without semantic drift; physical rows may be upload/epoch state only, never durable target identity", "private-solver/throughput grep/type proof plus focused real-GPU actuation tests, inherited 7.3/7.5c/7.7 batteries, inventory drift, and hosted Doctrine; if overlay-route discovery drifts, use clean-tree --harvest to inspect, sanctioned --repin only after classifications are truthful, then --check PASS"]
stop_conditions: ["one CrossingConsequenceBinding cannot cover all three arms without introducing a second crossing/dispatcher, direct world mutation, or 7.8a projection/invalidation semantics", "ResidentNextWrite cannot enforce facility-local ownership without sharing another facility's plane, a foreign write, a second swap authority, or an owner GPU identity plane", "RoutedOverlayDelivery requires a hidden global route, foreign absolute deadline, or bypass of the ordinary stamped receive/product seam", "StructuralAuthorization requires numerical GPU state mutation rather than authorization through the existing structural boundary", "field parameterization cannot remain within the graduated certificate envelope without mutating adjacency/order/conservation/chi/field-law identity or adding a private solver", "the 7.5c movement path cannot migrate to the intrinsic door with bit-identical real-GPU behavior, or touching the 7.3 reference instance requires unproven semantic change", "the production CostBand audit finds an authority leak whose lawful repair necessarily widens beyond actuation-fed paths or reopens Vector CostBand", "either inherited promotion-blocker falsifier requires inventing a new semantic authority merely to make the proof bite; STOP for DA rather than theatrical forbidden-surface construction", "completion requires 7.8a/7.9/8.x, rule/gate edits, peer-authority retirement, successor work, or weakening graduated 7.7 lifecycle/census/replay proofs"]
---
## BUILD
- Implement only 7.8: one `CrossingConsequenceBinding` actuation door from the existing sealed crossing surface to exactly `ResidentNextWrite | RoutedOverlayDelivery | StructuralAuthorization`.
- Use the door to authorize fixed admitted OverlayStateNext/parameter updates and native next-generation Property/RF/Field-Triad consequences; full Field-Triad parameters stay inside existing certificate envelopes.
- Re-anchor the graduated 7.5c movement vendor through this door with bit-identical GPU behavior; preserve 7.3 as the reference instance unless genuine oracle-first consolidation is required.
## FENCES
- One crossing surface, one post-crossing consequence substrate, facility-local resident planes, logical durable identity, canonical EML, stamped cross-tree receive seam.
- `StructuralAuthorization` is authorization only. No peer executor, direct world write, shared/foreign plane write, private throughput solver, certificate mutation, same-generation feedback recursion, or dynamic semantic mint.
- 7.8a/7.9/8.x remain fenced. `OVERLAY-PEER-AUTHORITY` remains live through this rung.
## EXIT-PROOF
- Real crossing → `OverlayStateNext` → next-generation consequence is green; routed receive and structural-authorization arms are positive-witnessed through their existing lawful seams.
- Certificate-envelope, foreign resident-plane write, second post-crossing dispatcher, unbounded-positive-feedback, overlay-local-EML, and durable-row-capture falsifiers RED for exact reasons (or the last two are type-unrepresentable with exact citations as permitted by the binding condition).
- 7.5c real-GPU parity remains bit-identical after migration; private solver/throughput authority remains absent; CostBand quotient audit is explicit and bounded to actuation-fed production paths.
- Return PROBATION / proof-present / DA-review-pending with exact head/base, fresh coding orientation + HD receipt + ACKs, three-arm production traces, falsifier reasons, movement parity digest, CostBand audit, census delta, focused/inherited batteries, inventory drift, relay-lint, clearance and hosted Doctrine artifacts. Coding does not merge, close the binding row, move the pointer, retire `OVERLAY-PEER-AUTHORITY`, or begin successors.
