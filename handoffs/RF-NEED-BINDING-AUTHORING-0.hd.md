---
rung: RF-NEED-BINDING-AUTHORING-0
kind: rung
track: 0.0.8.6
base_sha: cecde41f7ed73287f309020eec6db8f57776feb9
audience: coding
model_tier: frontier
owner_approved: true
expected_route: DA-RESERVE(class-envelope)
owner_notes: "DA Option A at #1414 comment 5013802819; PropertyKey->one-row is permanently rejected. RF-5/#1414 remains paused until RF-5A merges. Recipient is Grok."
surfaces: ["crates/simthing-clausething/src", "crates/simthing-spec/src", "crates/simthing-driver/src", "crates/simthing-workshop/tests", "docs + scripts/ci"]
forbidden: ["raw slot/column Clause syntax or PropertyKey row uniqueness", "new kernel/WGSL primitive or accumulator role/combine/gate", "implicit participant property insertion or first-match owner lookup", "manual GPU reseed/reopen/test registration mutation", "TP tokens in clausething or canonical TP data amendment", "COLUMN-INDEX-MINT exclusion", "merge/readiness mutation"]
required_checks: ["grammar+hydrate+spanned admission errors", "ordinary open_from_spec+live refresh GPU proof", "second synthetic scenario+WORKSHOP-HOMING-DETECTION", "RF regressions+agent+doctrine", "orientation+inventory+budget"]
stop_conditions: ["named entity cannot resolve uniquely through existing identity model", "production authored refresh path is absent", "implementation requires a kernel primitive/new accumulator semantic"]
---
## BUILD
- First commit records RF-5/#1414 **BLOCKED-PAUSED / DA Option A @ `39b4b392`**, adds RF-5A, makes RF-5A active, and regenerates orientation.
- Add generic scenario-agnostic ClauseScript `need_binding`: profile-id join; participant named entity; input and weight sources as `(named entity, property, role)`; threshold and event kind.
- Lower semantic references through existing entity identity and role pathways. Before GPU planning, every source is a typed full-cell locus `(SimThingId/SlotIndex, ColumnIndex)`; absent/duplicate entity, absent property/role, or ambiguity is a spanned hard error.
- Require the named participant to be already admitted to the Arena and already own the flow/AllocatorWeight property. No install-time acquisition or fallback insertion.
- Use only existing EvalEML, AccumulatorOp and threshold-event machinery. Update/remove the 2026-07-16 horizon marker discharged by this seam.

## FENCES
- Clause authoring names semantic identity, never raw row/column numbers. Entity-name uniqueness supplies row authority; property-key uniqueness never does.
- No Studio work, canonical `terran_pirate_galaxy.clause` amendment, screenshot C, executable freeze, or RF-5 closure in RF-5A.
- No first DFS match, positional zip, naming guess, source relocation, participant mirror, CPU overlay arithmetic, direct dense-state write, or test-only authority.
- Grok does not mark ready, graduate, merge, or resume PR #1414.

## EXIT-PROOF
- Two scenario-agnostic Clause fixtures parse/hydrate the same generic form; clausething production code contains zero TP tokens and homing scan passes.
- Admission resolves every authored input/weight to the intended named SimThing property role and full live cell; duplicate/absent entity, missing property/role, ambiguous source, and non-admitted/missing-flow participant each fail closed with source spans.
- Ordinary `SimSession::open_from_spec + step_once` consumes the binding through existing GPU machinery; below/crossing controls produce sealed threshold events without UI/CPU comparison.
- Open once, mutate only an authored source through the existing production refresh path, step again, and observe live need change on-device without reopen, manual buffer reseed, registration mutation, or direct values installation.
- No new column-mint exclusion; all columns derive through `SubFieldRole -> offset_of -> col_for_role`. Focused/live-GPU, RF regression, doctrine, orientation, inventory and budget checks bind final implementation/test/evidence SHAs before DA relay.
