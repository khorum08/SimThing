---
rung: EML-LN-PRIMITIVE-0
kind: rung
track: 0.0.8.7
base_sha: 236f2ccf
audience: coding
model_tier: standard
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "Owner sequencing remains binding after DA graduation 5185796838: 5.11 EXP is DA-GRADUATED (#1632 @ d664a866; stamp #1633 @ 236f2ccf), so 5.12 LN may proceed now and must graduate before the Owner pointer flip to 6.4 / StemThing-A. This handoff is projected from exact master 236f2ccf. 5.12 admits exactly one additional primitive, LN, by the landed 5.11 template. CLOSED_OPCODES widening and certification/freshness wiring remain landing-route surfaces, but no pre-dispatch DA ceremony is required absent a new STOP or design defect. Exhaustive qualification is over the admitted LN domain and is a LOCAL phase-boundary certification act; standing CI verifies pinned artifact presence/freshness only."
surfaces: ["crates/simthing-core/src/eml_nodes.rs", "crates/simthing-core/src/eml_registry.rs", "crates/simthing-kernel/src/eml_opcode_gate.rs", "crates/simthing-kernel/src/eml_resource_class.rs", "crates/simthing-kernel/src/accumulator_op/cpu_oracle.rs", "crates/simthing-kernel/src/accumulator_op/eml_program_table.rs", "crates/simthing-kernel/src/shaders", "crates/simthing-driver/src", "crates/simthing-driver/tests", "crates/simthing-workshop/tests", "docs/eml_gadget_library.md", "docs/full_eml_unification.md", "docs/tests", "scripts/ci", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md"]
forbidden: ["vendor-native log/exp on the LN exact path, f64, DIV inside LN, implementation-defined transcendental semantics, or CPU/interpreted-GPU/JIT sequences that differ from the pinned LN algorithm-as-spec", "a second primitive-admission door, second determinism/cost/consumer gate, new transcendental resource weight/class, weakened 5.10 admission distinction, or implicit LN-side clamping that converts guarded semantics into hidden validity repair", "weakening the exact-primitive cost key, comparing against a toy baseline instead of LN's own pinned ordinary-EML gadget encoding, or silently shrinking the certified toolchain roster to make evidence green", "running exhaustive LN-domain qualification in standing CI; CI may inspect pinned artifact presence/freshness only", "a bespoke LN shader/kernel, semantic/scenario opcode, algebra-kind branch, second EML execution path, scenario/corpus witness, or FIELD-SWEEP-SINGLE-PATH weakening", "a POW opcode or any third transcendental opcode; trig; mutation of the already-admitted EXP semantics; StemThing-A implementation; movement/contention; 9.1 GATED-RATES-EML-REWIRE work; or any 5.12 successor work", "treating LogAccumulate as a semantics-preserving Product optimization, using it on a conservation path as a Product substitute, reordering a fold, or introducing an intra-fold tree reduction without its own separately admitted reduction contract", "extended-real LN conventions such as ln(0) = -infinity, infinity propagation, or silently accepting zero, subnormal, negative, NaN, or infinite LN inputs"]
required_checks: ["fresh coding ORIENT-RECEIPT + current ANCHOR-ACKs + HD-RECEIPT before edits; prove the exact base has EXP as the sole admitted exact primitive and the candidate adds exactly one named LN, with CLOSED_OPCODES growing only by LN", "bind LN to one PrimitiveDomain spanning positive finite normals exactly [2^-126, f32::MAX] (bits 0x00800000..=0x7f7fffff); every production call is shape-1 range-certified or shape-2 explicitly guarded semantics through 5.10, while unguarded/uncertified LN and out-of-domain inputs remain spanned admission errors; prove a guard never satisfies a shape-1 validity claim about its source", "pin one f32-only LN algorithm-as-spec and its algorithm identity; CPU twin, interpreted GPU arm, and SSA-JIT lowering execute the identical sequence with no vendor transcendental, f64, or div; the landed EXP sequence/identity is unchanged", "characterize the pinned LN sequence independently against a higher-precision mathematical ln reference across both endpoints, powers-of-two/exponent seams, neighborhoods of 1.0, and deterministic stratified/random samples; publish the observed error/ULP envelope plus finite/monotonic sanity without claiming correctly-rounded semantics", "run exhaustive admitted-domain qualification LOCALLY for every currently certified compiler/backend/driver tuple; pin exact CPU/interpreted/JIT digests plus algorithm/trust-chain identity; reuse the 5.11 live adapter/backend/driver certification discipline so an uncertified tuple hard-errors; standing CI remains freshness-only", "exercise ExactPrimitiveCostEvidence against LN's own pinned ordinary-EML gadget encoding: no compiled-resource regression and at least one strict improvement under the existing cost key, with no new cost dimension or weakened threshold", "land and exercise the LN-enabled library/consumer forms only through existing EML machinery: PowerLaw/POW(x,a)=EXP(a*LN(x)) with no POW opcode; eml(x,y)=SUB(EXP(x),LN(y)) as a three-node entry; entropy with authored p>0 handling; LogAccumulate as its own authored law on the existing Sum lane", "plant defects that prove the LN digest catches sequence/lowering drift, the 5.10 shape distinction catches validity-masking misuse, and LogAccumulate REDs if substituted for Product/conservation semantics; do not mutate 5.11 EXP to make any 5.12 proof pass", "run focused primitive/admission/JIT/interpreter/gadget/consumer batteries plus inherited 5.11 exact-primitive freshness/resource-class and field-sweep parity referees; update inventory/results/evidence truthfully and regenerate orientation if ladder status changes"]
stop_conditions: ["any supported certified toolchain cannot match the pinned LN CPU semantics bit-for-bit, or the implementation requires f64/div/vendor transcendental/bespoke kernel/second EML mechanism; do not weaken determinism or silently drop the toolchain", "the LN candidate cannot beat its own ordinary-EML gadget encoding under the landed exact-primitive cost key without weakening the gate or changing the baseline", "independent characterization shows the pinned sequence is grossly non-logarithmic, non-monotone, non-finite inside the admitted domain, or unsuitable for the named consumers; return the measured envelope for ruling instead of admitting a misleading LN", "POW/eml/entropy/LogAccumulate requires a new execution path, a third opcode, Product substitution, fold reordering, or any conservation-law weakening rather than ordinary authored EML over existing Sum/field-sweep machinery", "correct handling of x<=0 would require hidden primitive clamping or collapsing the 5.10 range-certified versus guarded-semantics distinction; preserve the distinction and return the exact gap", "completion requires StemThing-A, movement/contention, 9.1 rewire work, trig, a new infinity-propagation discipline, or any successor capability", "certification freshness cannot mechanically bind compiler/backend/driver/algorithm drift using the landed 5.11 trust-chain pattern without standing exhaustive CI execution; return the exact gap instead of weakening local certification"]
---
## BUILD
- Admit exactly one additional generic primitive, `LN`, through the landed 5.10 door and by the proven 5.11 exact-primitive template. Its domain is positive finite normals only; pin the algorithm-as-spec, CPU twin, interpreter arm, SSA-JIT arm, and the one required closed-opcode widening.
- Reuse the 5.11 certification/freshness architecture rather than inventing another proof system: exhaustive admitted-domain qualification is local, artifacts are pinned, live certified tuples hard-error on mismatch, and CI remains freshness-only.
- Complete the generating pair as authored library data: `POW(x,a)=EXP(a*LN(x))` with no POW opcode, and `eml(x,y)=SUB(EXP(x),LN(y))` as the three-node entry. Add entropy and PowerLaw consumers only through existing EML surfaces.
- Implement `LogAccumulate` as a distinct authored numerical law on the existing Sum lane. Preserve canonical fold order; prove it cannot masquerade as Product or a conservation-path optimization.
## FENCES
- One new primitive only. `CLOSED_OPCODES` widens only for `LN`; EXP semantics and identity are frozen; no third transcendental opcode, semantic opcode, bespoke shader, second interpreter/JIT path, second gate, or second cost model.
- LN validity is the 5.10 two-shape law made load-bearing: shape 1 certifies the source range; shape 2 deliberately guards authored semantics. A clamp may define saturated behavior but never proves the unguarded source was positive.
- Extended-real logarithm semantics are out. Zero, subnormal, negative, NaN, and infinity are rejected at admission rather than converted into infinities or silently repaired.
- Log-domain accumulation does not legalize Product substitution or fold reordering. No StemThing-A, movement/contention, 9.1 rewire, trig, infinity-propagation, or successor work rides this landing.
## EXIT-PROOF
- Exactly one `LN` primitive is newly admitted over `0x00800000..=0x7f7fffff`; CPU, interpreted GPU, and JIT are bit-identical to the pinned sequence across the entire admitted domain, and planted sequence/lowering mutants RED the digest.
- Every certified tuple has pinned qualification/replay artifacts and the live adapter/backend/driver comparator remains load-bearing; CI detects artifact/trust-chain freshness without re-executing exhaustive qualification. Independent characterization records LN's numerical envelope without claiming correct rounding.
- The existing exact-primitive cost key proves no regression plus at least one strict compiled-resource improvement versus LN's own pinned ordinary-EML gadget baseline. `POW`, `eml()`, PowerLaw, entropy, and LogAccumulate execute through existing EML/Sum machinery with no bespoke path.
- Return PROBATION / proof-present for orchestration review with exact tested head and evidence. Coding does not `/clearance`, merge, move the pointer, start StemThing-A, or begin any successor rung.
