---
rung: CLAUSETHING-ADMISSION-CONVERGENCE-0
kind: rung
track: 0.0.8.7
base_sha: 925855a3c908fde14ed8017258db79d85a8afdb5
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(binding)
owner_approved: true
owner_notes: "Phase-13 authority: Board 5462798074. 13.1 graduation: Board 5464215228. Kernel containment is binding and one-way: ClauseThing/application surfaces converge onto the domain-neutral kernel, never the reverse. Orchestrator orientation at authoring base: ORIENT-RECEIPT 026f3024763a; rule stamp 0316109428d6bbb0."
surfaces: ["crates/simthing-clausething/src/hydrate.rs", "crates/simthing-clausething/src/hydrate_category_economy.rs", "crates/simthing-clausething/src/hydrate_fleet_composition.rs", "crates/simthing-clausething/src/hydrate_overlay.rs", "crates/simthing-clausething/src/hydrate_resource_flow.rs", "crates/simthing-clausething/tests/ct_2c_category_economy.rs", "crates/simthing-clausething/tests/ct_3b_4a_gpu_projection.rs", "crates/simthing-clausething/tests/fixtures", "scripts/ci/test_inventory.tsv", "scripts/ci/anchor_reach_log.tsv", "docs/tests", "docs/tests/current_evidence_index.md", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md"]
forbidden: ["any edit under crates/simthing-core, crates/simthing-kernel, crates/simthing-gpu, crates/simthing-sim, crates/simthing-driver, crates/simthing-spec, or crates/simthing-embedder", "any ClauseScript grammar/syntax change or author-facing fixture rewrite used to make modern admission pass", "any weakening or semantic rewrite of ct_2c_category_economy or ct_3b_4a_gpu_projection assertion bodies", "any second admission path, test-only bypass, per-fixture compatibility shim, or application accommodation in the kernel", "any change to the 13.1 SpecError typed-provenance representation or its constitutional binding", "any negative fixture changing the semantic reason for which it refuses", "any 13.3 star-name golden adjudication or 13.4-13.6 implementation", "Vector CostBand or post-closeout performance work", "any .github/workflows/** or scripts/ci/**/*.sh or scripts/ci/**/*.py gate-code edit", "track closeout or pointer movement from the coding lane"]
required_checks: ["before edits render/read this handoff; perform fresh Frontier coding-role orientation on the exact merged handoff dispatch master and ACK every rendered REQUIRED-ANCHOR", "FIRST STEP census every hydrate_* production surface and identify the one existing ClauseScript-to-modern-spec lowering chain; publish the map before editing and STOP if admission requires an ingress outside the ClauseThing hydration layer", "FIRST STEP mechanically census the ct_2c pre-open adaptation glue under one reproducible counting rule; the Phase-13 mint counted five shims, so a result other than exactly five is a STOP for reconciliation before edits, not permission to redefine the count", "FIRST STEP tree-census the .clause corpus and reconcile the standing Owner witness of 41 fixtures plus canonical terran_pirate_galaxy.clause; publish exact paths/counts and STOP on material mismatch", "record the exact current typed 13.1 refusal identities for both ClauseThing reds before editing; the known admission failure is base-flow-obligation-participant-property-live with deterministic obligation/participant/property element paths", "repair version skew only in existing hydrate_* lowering code so ordinary SimSession::open_from_spec admits the hydrated modern spec; do not modify admission predicates or any engine/spec/driver/embedder surface", "make ct_2c_category_economy and ct_3b_4a_gpu_projection green with their assertion bodies unmodified; adaptation glue must strictly shrink from the censused five-shim baseline and no equivalent workaround may move elsewhere", "add one corpus-wide parse -> hydrate -> ordinary admit witness covering the full standing .clause corpus; positive inputs admit and existing negative inputs continue to refuse for the same named typed reasons", "preserve the Owner horizon that any ClauseScript input hydrates as before: no fixture rewrites, no author-facing syntax migration, no per-scenario repair, and no second lowering authority", "optional open_replay_with_spec debt 5460089932 may fold only if the same ClauseThing hydrate_* repair naturally discharges it with no forbidden lower-crate edit; otherwise carry the debt unchanged and do not chase it", "ledger every authored test in test_inventory.tsv, keep evidence current, and report exact changed-file census plus pre/post glue census", "run focused ClauseThing batteries plus doctrine/orientation/anchor/handoff/agent-delta/diff checks, hosted Doctrine Scan, and the structural/full-workspace certificate; with 13.3 still pending, any ClauseThing red after this rung is a STOP and the only expected inherited workspace red is the stale star-name golden", "return PROBATION / proof-present / DA-review-pending with hosted run ids; coding does not merge, graduate, move the pointer, start 13.3+, or close the track"]
stop_conditions: ["the hydrate_* census shows the truthful repair requires any non-ClauseThing engine/spec/driver/embedder accommodation", "the five-shim mint baseline cannot be reproduced under a clear mechanical census rule", "the live .clause corpus cannot be reconciled to the standing 41-fixture-plus-canonical witness without changing the rung contract", "a positive ClauseScript input can only be made to admit by changing authored syntax/fixture content rather than lowering", "either existing oracle requires assertion-body weakening or expected-output rewriting", "a negative fixture would refuse for a different semantic law/reason after the repair", "the proposed change adds compatibility glue, a second admission/lowering route, or application vocabulary below the ClauseThing layer", "any apparent need to edit the 13.1 AdmissionRefused shape/census binding or to change admission acceptance semantics", "the structural certificate retains either of the two ClauseThing reds or gains any new red"]
---
## BUILD
- Converge ClauseThing onto the graduated substrate **at the existing `hydrate_*` lowering boundary only**. ClauseThing is an application-layer authoring system; this rung translates its existing authored vocabulary into the modern generic admitted shape. It does not change the kernel to remember legacy application vocabulary.
- Start with archaeology, not edits. Publish the production hydration map across `hydrate.rs`, `hydrate_category_economy.rs`, `hydrate_resource_flow.rs`, `hydrate_overlay.rs`, `hydrate_fleet_composition.rs`, and any other `hydrate_*` module actually reached by the two target oracles. Name the single ordinary route from ClauseScript parse through hydration into `SimSession::open_from_spec`.
- Reproduce the Phase-13 mint's **five-shim** baseline in `ct_2c_category_economy` using one explicit counting rule. The current test contains pre-open adaptation work including scenario registry/property setup, participant materialization, balance-property/helper-property accommodation, and clearing hydrated properties before admission. Do not accept this prose as the count: derive and publish the exact five units from the live test. A different count is a STOP for orchestration/DA reconciliation.
- Record the two ClauseThing failures as typed by graduated 13.1 before changing hydration. Their current root refusal is `base-flow-obligation-participant-property-live`, with the element path identifying the base obligation, admitted participant, and missing property. The repair target is the stale lowering that produced that modern-spec mismatch, not the admission law that correctly rejected it.
- Change only the ClauseThing `hydrate_*` lowerer(s) needed to emit the already-canonical modern admitted representation. Version skew is absorbed exactly once at the language boundary. Authors do not rewrite `.clause` files; tests do not patch hydrated output into acceptability; engine admission does not learn ClauseThing compatibility rules.
- Both existing oracles, `ct_2c_category_economy` and `ct_3b_4a_gpu_projection`, must go GREEN with their **assertion bodies unmodified**. Delete obsolete pre-open adaptation glue as the lowerer becomes truthful. The post-change glue count must be strictly less than the exact five-shim baseline, with no equivalent shim relocated into a helper or new test utility.
- Add a standing corpus witness that discovers the complete `.clause` corpus rather than hard-coding a selective happy path. Reconcile the live tree against the Owner's standing witness: **41 fixtures plus canonical `terran_pirate_galaxy.clause`** (currently under the ClauseThing test fixtures tree). Every positive corpus member must parse, hydrate, and admit through the ordinary modern path. Existing negative fixtures must remain negative for their same named typed reason; do not weaken negative expectations merely because provenance is now machine-readable.
- Preserve parse and hydration compatibility: any ClauseScript input that hydrated before still hydrates under the same authored syntax. If the modern admitted target cannot be produced without changing ClauseScript grammar, authoring files, or lower crates, STOP and name the seam.
- The dated `open_replay_with_spec` admitted-field-sweep coverage debt (`5460089932`) is optional only in the narrow sense granted by the ladder: fold it if and only if this same `hydrate_*` change naturally discharges it without a forbidden driver/embedder/spec edit. Otherwise leave it explicitly outstanding.
- Return PROBATION / proof-present / DA-review-pending. Coding does not merge, graduate, advance the pointer, adjudicate the star-name golden, begin another Phase-13 rung, or close the track.

## FENCES
- **Kernel containment is absolute.** No edits under `simthing-core`, `simthing-kernel`, `simthing-gpu`, `simthing-sim`, `simthing-driver`, `simthing-spec`, or `simthing-embedder`. An apparent need for one is a STOP whose useful output is the named missing seam.
- **Application converges onto kernel, never reverse.** Do not add ClauseThing/ClauseScript names, compatibility enums, parking values, defaults, alternate constructors, or special admission cases to generic engine crates.
- **Hydration only.** This is not a parser/grammar redesign and not a fixture migration. Preserve author-facing ClauseScript syntax and the existing parse contract. Do not make the corpus green by editing scenario content one file at a time.
- **Oracles are seals.** The substantive assertion bodies of `ct_2c_category_economy` and `ct_3b_4a_gpu_projection` are immutable. Only remove/shrink the adaptation/setup glue made obsolete by correct lowering. No expected-value weakening, no ignored cases, no altered GPU/CPU parity criterion.
- **Compatibility glue must shrink.** Moving a shim into another helper, a corpus harness, or a hidden test utility is not convergence. Report the before/after mechanical census and explain every removed unit.
- **Negative compatibility is part of the contract.** A negative fixture may gain more precise 13.1 provenance, but it must still fail the same semantic law for the same authored defect.
- **No 13.1 reopening.** `SpecError::AdmissionRefused`, its law-id/element-path semantics, and `ROOT-CONTRACT-ADMISSION-ERROR` are graduated inputs to this rung, not implementation surfaces.
- No 13.3 star-name re-bless/fix, no workshop triage, no legacy-surface census campaign, no clearing-weight work, no Vector CostBand, and no closeout work.
- Gate code remains fenced: no `.github/workflows/**`, `scripts/ci/**/*.sh`, or `scripts/ci/**/*.py` edits. Data ledgers such as `test_inventory.tsv` and append-only `anchor_reach_log.tsv` remain lawful proof surfaces.

## EXIT-PROOF
- **Hydration-map proof:** publish the exact live `hydrate_*` call/producer map used by both target oracles and show the repair stayed inside that application-layer boundary.
- **Named-failure proof:** record the pre-change 13.1 `law_id` + `element_path` for both ClauseThing reds, then show ordinary `open_from_spec` no longer reaches those refusals because hydration now emits the already-admitted modern shape. Do not mutate the refusing engine law.
- **Oracle-seal proof:** both `ct_2c_category_economy` and `ct_3b_4a_gpu_projection` GREEN; their assertion bodies are diff-identical to the dispatch base. Report any setup/glue lines changed separately.
- **Glue-shrink proof:** publish the reproducible five-unit pre-change census, post-change count `< 5`, and a disposition for each removed shim. A helper relocation is a failure, not a removal.
- **Corpus-horizon proof:** tree-derived corpus inventory reconciles to the standing 41-fixture-plus-canonical witness; all positive `.clause` inputs parse -> hydrate -> ordinary admit, while every existing negative continues to refuse for its same named semantic reason. Report exact counts and negative reason table.
- **Compatibility proof:** no `.clause` fixture/content rewrite and no parser/grammar contract change was needed. The lowerer alone absorbs the version skew.
- **Containment proof:** zero diff under core/kernel/gpu/sim/driver/spec/embedder; no new admission authority, no second lowerer, no application vocabulary pushed downward, no 13.1 error/census change.
- **Structural proof:** focused ClauseThing batteries and current doctrine/orientation/anchor/handoff/agent-delta/diff checks green. Run the full workspace certificate. With 13.3 still pending, the two ClauseThing admission reds must be gone; the only expected inherited failure is the stale star-name golden. Any ClauseThing red or new failure is STOP, not baseline prose.
- **Debt proof:** state explicitly whether `5460089932` was naturally discharged inside the permitted `hydrate_*` seam; if not, carry it unchanged with provenance and no lower-crate chase.
- **Hosted/routing proof:** hosted Doctrine Scan green at exact final head; return exact base, tested_code_sha/final head, changed-file census, hydrate map, pre/post glue table, corpus inventory, negative-reason table, oracle assertion-diff proof, certificate, all hosted run ids, and fresh body/head-bound `/clearance` plus `/relay-lint` evidence for orchestration.
