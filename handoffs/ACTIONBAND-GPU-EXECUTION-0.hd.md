---
rung: ACTIONBAND-GPU-EXECUTION-0
kind: rung
track: 0.0.8.7
base_sha: 96794bfedcc8638aa9d45a3fcba0266340043922
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(gate-wiring)
owner_approved: true
owner_notes: "DA graduation ruling Board 5230049125 merged 7.1 #1694 @ 66182aeb, stamped it via #1697 @ 96794bfe, passed RUNGCLOSE, and advanced the pointer to ACTIONBAND-GPU-EXECUTION-0. The 7.1 admission door is frozen and does not move. The workplan's duplicated HORIZON-marker consumption sentence is already discharged by 7.1 graduation: both markers are absent and their continued absence is an inherited invariant, not a new 7.2 edit obligation. Frontier coding lane; 7.3+ remain fenced."
surfaces: ["crates/simthing-spec/src/compile/action_band_admission.rs", "crates/simthing-spec/src/spec/action_band.rs", "crates/simthing-spec/tests", "crates/simthing-kernel/src", "crates/simthing-kernel/tests", "crates/simthing-gpu/src", "crates/simthing-gpu/tests", "crates/simthing-sim/src", "crates/simthing-sim/tests", "docs/tests", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md"]
forbidden: ["moving, widening, bypassing, or duplicating the graduated 7.1 simthing-spec admission door, closed target vocabulary, ExistingThresholdRegistrationIndex binding, or existing Phase-5 BandCrossingDelta crossing authority", "any CPU ActionBand evaluator, planner, scheduler, numerical mirror, target solver, band-crossing evaluator, semantic decision loop, or CPU callback executing EML payload meaning", "runtime recursive function calls, pointer-linked ActionBand trees, GPU-side semantic authoring, unbounded GPU heap/object allocation, runtime template minting, or dynamic vocabulary/fanout growth", "subordinate activation/collapse semantics, dependency satisfaction, concurrent sibling semantics, generation-paced recursive composition, inline requirement semantics, RF/scalar-CostBand multisource composition, or any other 7.3 implementation", "movement/spatial vendorization, movement destination/path/planner authority, semantic readback/shadow completion, icon/transit work, 7.4/7.5 implementation, 8.1/8.2 execution, StemThing-B, Vector CostBand, or successor work", "ActionBand-local conservation judge, ActionBand-local clearing rule, persistent cross-arena scarce-grant holding, atomic common-depth commitment, same-generation convergence solver, local-minimum escape, or adversarial navigation policy", "human-readable labels, domain nouns, strings, or semantic names in hot GPU template/instance structures or as numerical dispatch keys", "imperative EML side effects or arbitrary destination writes; EML may compute bounded numeric results only and emission lowering must target pre-admitted fixed bindings", "reintroducing either consumed HORIZON-ENTRY(2026-08-08) marker or restoring a parallel decision-ingress/overlay-lifecycle seam", "edits to GATE CODE -- scripts/ci/**/*.sh, scripts/ci/**/*.py, .github/workflows/**; DATA LEDGERS such as scripts/ci/test_inventory.tsv and append-only evidence ledgers remain writable only for rows this rung legitimately earns, with no weakening/deleting existing rows", "#[cfg(test)] twins, alternate production executors, test-only semantic mirrors, intentionally broken fixtures, or mutants outside the real production path they claim to guard"]
required_checks: ["fresh Frontier coding ORIENT-RECEIPT and exact rendered HD-RECEIPT before edits; ACK the complete projected ActionBand anchor set and STOP if actionband-gpu-physical-model or actionband-eml-payload-purity is missing", "consume the graduated 7.1 admitted product without changing its authoring/admission authority: lower immutable admitted templates into flat domain-free GPU template descriptors and sparse active instance rows with stable opaque indices/spans", "land explicit StateCurrent/StateNext numerical state separation so one generation reads only current state and writes only next state/output buffers; no CPU scheduling or same-generation semantic recursion", "prove sparse registration: a SimThing with no active ActionBand owns zero hot ActionBand instance rows/bytes, while active instances materialize only admitted fixed-width state and parameter bindings", "land the depth-1/depth-2 fast physical shape without semantic special-casing: recursion remains flattened/indexed from birth and no pointer traversal or CPU child scheduler exists; this rung does not activate or resolve subordinate composition semantics", "execute admitted EML payloads on GPU through the existing admitted EML numerical semantics; any CPU/interpreted implementation used for parity is referee/oracle only and cannot become production authority", "lower EML result slots through pre-admitted fixed emission-binding tables into already-existing GPU-authoritative RF/CostBand/overlay-event/sealed structural-request surfaces; no imperative callback, arbitrary write target, or runtime destination minting", "prove program/binding bucketing: instances sharing admitted EML program/binding shape execute in common buckets/batches without branching on semantic labels, while different admitted program/binding shapes remain deterministically separated", "prove a structural consequence is authorized numerically on GPU and applied at the existing CPU structural boundary without CPU re-evaluation of target satisfaction, band crossing, EML result, or consequence choice", "measure the one-node/depth-1 ActionBand path against the existing sealed crossing cost using a meaningful repeated exact-head benchmark/probe; report methodology and numbers without inventing a waiver if the fast path materially regresses the intended crossing-scale economics", "plant production-path defects for current/next aliasing, dense all-SimThing instance materialization, CPU numerical re-evaluation, semantic-label dispatch, arbitrary emission destination, and structural-boundary re-evaluation; each must be unconstructible or demonstrably RED on the real path it guards", "verify inherited 7.1 type boundaries remain intact: ExistingThresholdRegistrationIndex still has no external constructor, the seven closed ActionBandTargetSpec variants remain closed, both prior HORIZON markers remain absent, and no second crossing authority appears", "run focused exact-head spec/kernel/gpu/sim tests locally because hosted CI runs no cargo tests; include CPU/reference-versus-GPU parity or another faithful-lowering parity under the existing EML semantics, plus full affected-package batteries without weakening tests or inventories", "update one results/evidence packet and only the 7.2 workplan status to PROBATION / proof-present / DA-review-pending; regenerate orientation deterministically; keep 7.3/7.4/7.5, 8.1/8.2, StemThing-B, Vector CostBand, and all successors TODO", "run handoff/agent/doctrine/anchor/orientation/exit-proof checks plus hosted Doctrine Scan and Doctrine Exec at the exact final head; read doctrine-scan-reports artifact and step conclusions, treating INSPECT with failures=0 as pass and any nonzero failures as failure"]
stop_conditions: ["the GPU executor cannot consume the graduated 7.1 admitted product without moving or duplicating the admission door or minting a second crossing authority", "correct execution requires CPU numerical ActionBand authority, CPU semantic scheduling, CPU callback evaluation, semantic labels as dispatch keys, or a continuous CPU numerical mirror", "EML payload execution cannot remain bounded/pure or fixed emission lowering cannot target pre-admitted existing surfaces without arbitrary writes or a new side-effect subsystem", "the physical model requires runtime pointer recursion, unbounded fanout/heap growth, runtime template authoring, or subordinate activation/composition semantics that belong to 7.3", "a structural consequence requires CPU re-evaluation of why the GPU authorized it rather than merely applying an existing sealed structural request", "the depth-1 fast path shows a material crossing-scale regression that cannot be explained by benchmark artifact or avoidable lowering/setup overhead; report measurement to orchestration/DA rather than redefining acceptable performance", "completion requires movement/vendorization, semantic-shadow/readback, ActionBand-local RF/clearing/transaction machinery, persistent scarce holding, 8.x semantics, gate-code edits, invariant weakening, or successor-rung implementation"]
---
## BUILD
- Implement only 7.2 `ACTIONBAND-GPU-EXECUTION-0`: lower the already-graduated 7.1 admitted ActionBand product into sparse flat GPU template/instance execution with explicit `StateCurrent` / `StateNext` numerical state.
- Execute bounded admitted EML payloads on GPU and lower their numeric result slots through fixed pre-admitted emission bindings into existing GPU-authoritative surfaces; CPU may apply an already-authorized structural request but may not re-evaluate the ActionBand decision.
- Provide the depth-1/depth-2 fast physical shape and deterministic program/binding bucketing while keeping the representation flattened/indexed and domain-nameless from birth.
## FENCES
- The 7.1 door is graduated and frozen: do not move it, widen it, duplicate it, or re-author target/crossing semantics. `ExistingThresholdRegistrationIndex` remains unforgeable outside admission; the seven target forms remain closed; both consumed HORIZON markers remain absent.
- GPU owns all ActionBand numerical authority. CPU reference/interpreted code is proof-only; no production evaluator, scheduler, planner, mirror, callback, semantic-label dispatch, or second crossing path.
- 7.2 establishes physical execution only. No subordinate activation/collapse, dependency/sibling semantics, recursive composition, RF multisource semantics, movement vendorization, semantic shadow/readback, contention/Vector CostBand, or other 7.3+ work.
- EML is a bounded numerical ISA, never an imperative side-effect language. Emission destinations are fixed at admission and lowered through existing surfaces only.
## EXIT-PROOF
- The same admitted ActionBand descriptor produces parity between the lawful reference/faithful lowering and GPU execution under existing EML semantics; production authority is GPU-only, with `StateCurrent` read and `StateNext` write separation proven by a biting alias/order mutant.
- Sparse inactive worlds allocate zero hot ActionBand instance rows; active instances use admitted fixed-width state. Depth-1/2 execution is flat/indexed, program/binding bucketing is demonstrated, and human-readable labels cannot influence numerical output.
- A GPU-authorized structural request reaches the existing CPU structural boundary and is applied without CPU re-evaluation; dead-coding GPU authorization or inserting CPU decision logic makes the referee RED.
- Existing 7.1 crossing/target type boundaries remain intact, both consumed HORIZON markers stay absent, no second crossing/ActionBand/RF authority appears, and the one-node/depth-1 benchmark is reported against existing crossing cost without an invented waiver.
- Return PROBATION / proof-present / DA-review-pending with exact base/head, HD-RECEIPT, fresh Frontier coding receipt/stamp, complete anchor ACK set, GPU/reference parity evidence, sparse/bucketing/current-next/structural-boundary falsifiers, local battery results, benchmark methodology/results, hosted scan/exec artifact evidence, and graduation routing. Coding does not `/clearance`, merge, graduate, move the pointer, or begin any successor.
