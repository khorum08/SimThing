---
rung: RF-COLUMN-MINT-MIGRATE-0
kind: rung
track: 0.0.8.7
base_sha: 255d9ddd60bfbf71945025b262efdf0a5443b6e8
audience: coding
model_tier: std
owner_approved: true
expected_route: DA-RESERVE(gate-wiring)
owner_notes: "DA 5356131544 graduated 9.1 and advanced the pointer to 9.2. This is the late RF column sweep committed at track open: migrate every remaining legacy ColumnIndex mint onto the final sanctioned identity doors, delete the exclusion residue, and retire COLUMN-INDEX-MINT only when its promotion blocker is genuinely discharged. 9.2 BINDS 6.0 owner-channel identity and 6.4 stable logical identity: persistent layout is keyed by stable logical identity plus session-local interned owner identity, NEVER OwnerRef lexical order, physical row position, or allocation order. OwnerRef remains canonical identity for authoring/diagnostics and seam transport; interned owner ids are layout-only, session-local, and never serialized across a 6.2 seam. Corrected anti-theatre law remains binding: ordinary production must be right where a wrong implementation would not; no production defect selector."
surfaces: ["crates/simthing-core/src", "crates/simthing-kernel/src", "crates/simthing-gpu/src", "crates/simthing-sim/src", "crates/simthing-driver/src", "crates/simthing-spec/src", "crates/simthing-clausething/src", "crates/simthing-feeder/src", "crates/simthing-mapeditor/src", "crates/simthing-mapgenerator/src", "crates/simthing-tools/src", "scripts/ci", "docs/tests"]
forbidden: ["new domain-shaped column identity, property-specific minting convention, second owner/layout authority, or alternate slot identity", "persistent layout keyed by OwnerRef string ordering, physical row position, allocation order, or transient vector iteration order", "serializing or transporting session-local interned owner ids across a 6.2 seam; seam identity remains canonical OwnerRef", "ownership flag switch forcing incidental reparent, table rebuild, remap, realloc, or slot swap; deliberate churn remains allowed only when explicitly chosen by the consumer", "re-adding planet_id or star_system_gridcell_id_raw to engine src, or adding domain-shaped fields to PlanetChildRfScopeKey; new production code names OwnerChannelScopeKey directly", "retaining ColumnIndex::new as production policy, adding COLUMN-INDEX-MINT exclusions, replacing the tripwire with a weaker scan, or retiring it before the live sweep proves zero forbidden reaches", "production defect switch, helper-only refusal, inert enum demo, source spelling grep as the sole proof, or a mutant that REDs before the targeted identity/layout law", "9.3+/10.1+, StemThing-B, Vector CostBand, 11.3, OVERLAY-PEER-AUTHORITY retirement, unrelated workflow/allowlist/ladder/pointer changes"]
required_checks: ["before edits: render/read the canonical handoff; obtain a fresh Std coding ORIENT-RECEIPT at literal rule stamp 61818ff7d4adda84; ACK every rendered REQUIRED-ANCHOR", "FIRST STEP archaeology: inventory the entire live production + oracle/rehearsal ColumnIndex mint universe from the tree, classify each remaining reach by lawful door and consumer role, and identify the actual current exclusion mechanism/residue; do not treat any historical 84/16 count or old handoff target list as authoritative", "prove the final constructor taxonomy by responsibility: registry/global property columns derive through PropertyColumnRange::col_for_role/layout ranges; plan-local structural channels use StructuralScalarChannel::into_plan_column; GPU wire rematerialization is confined to the sanctioned round-trip boundary; raw oracle/rehearsal construction remains only where judging independence genuinely requires it; migrate or delete every other reach", "remove legacy ColumnIndex::new production uses and retire the compatibility alias only if the live tree no longer needs it; if an existing public/serialized consumer genuinely requires the alias, STOP with exact consumer rather than preserving undocumented compatibility", "implement the row's session-local owner interning/layout authority without changing canonical OwnerRef identity: persistent RF column/layout keys must remain stable under alpha->zulu owner changes unless churn is explicitly requested, and must never derive ordering from OwnerRef lexical Ord; prove independent sessions may intern differently while seam transport remains OwnerRef", "preserve 6.4 logical/physical split: forced EpochRebind with logical ids fixed and physical rows scrambled must not invalidate or permute persistent column layout; no layout key may depend on physical row/allocation order", "scope cleanup: planet_id and star_system_gridcell_id_raw absent from engine src; PlanetChildRfScopeKey, if retained, is only the existing type alias to OwnerChannelScopeKey with no domain-shaped field or new production dependency", "delete the live COLUMN-INDEX-MINT exclusion residue only after every reach is dispositioned; retire the COLUMN-INDEX-MINT scan exactly at its documented 9.2 promotion blocker, with a proof that the retired detector cannot hide a surviving forbidden mint", "plant value/identity-biting rivals at the correct proof layer: OwnerRef lexical-sort rival must disagree with stable production layout on an owner-flip case; physical-row/allocation-order rival must disagree under forced EpochRebind; a seam mutant that transports an interned id must be rejected/unrepresentable. Do not invent tagged representation merely to force same-raw constructors to diverge", "run the blast radius: full affected crate libs and RF/owner-channel/slot-logical-identity/plan-struct typing/gated-rate consumers; inventory/lifecycle/detachability/census/Agent/DOC-BUDGET/Doctrine; inspect every hosted Doctrine Scan workflow step; return exact commands/counts and hosted run ids"]
stop_conditions: ["tree archaeology finds a live mint whose lawful identity source is genuinely absent from the graduated doors; STOP with exact call site and missing substrate instead of minting a new convention", "stable owner-layout behavior requires a process-global owner id, serialized owner id, cross-session shared interner, or seam transport of layout-local identity", "forced EpochRebind cannot preserve persistent layout without keying on physical row/allocation order or widening the 6.4 identity law", "retiring ColumnIndex::new or COLUMN-INDEX-MINT would break an unclassified external/public consumer not covered by a graduated door", "completion requires a new property/role taxonomy, second mapping service, owner side table with independent authority, 9.3+/10.1+, StemThing-B, Vector CostBand, or unrelated gate/workflow/allowlist changes"]
---
## BUILD
- Execute the late ColumnIndex sweep once against the FINAL identity model. Every remaining mint must either move onto an already-graduated door, be proven a legitimate oracle/rehearsal or GPU round-trip boundary, or be deleted. Do not migrate by regex substitution; derive the identity source for each caller.
- Mint the row-authorized session-local owner interning/layout mechanism only as layout metadata. `OwnerRef` remains semantic identity and seam currency; interned ids never serialize or cross session boundaries. Persistent column layout must remain stable by default across ownership changes and 6.4 epoch rebinds; deliberate churn is still legal when explicitly requested by a consumer.
- Empty the real exclusion residue, remove the production compatibility mint surface when no live caller needs it, and retire `COLUMN-INDEX-MINT` only when its documented promotion blocker is actually discharged.
## FENCES
- No new domain vocabulary or second identity authority. `OwnerChannelScopeKey` and 6.4 logical `SlotIndex` remain the canonical semantic keys; physical row and allocation order are never semantic layout keys.
- Cheap owner-flag switching must remain available/default, but this is NOT a ban on deliberate reparent/remap/realloc. Do not encode a global no-churn policy.
- No successor-rung work, no StemThing-B/Vector CostBand, no OVERLAY-PEER-AUTHORITY retirement, and no unrelated CI redesign.
## EXIT-PROOF
- Live mint census closes with every remaining constructor reach on a sanctioned door and zero unexplained production raw mints; `ColumnIndex::new` compatibility and the COLUMN-INDEX-MINT exclusion/tripwire are retired only if the tree proves their promotion blockers closed.
- Owner flip `alpha -> zulu` does not silently reorder/re-index persistent layout through lexical OwnerRef ordering; independent sessions may assign different interned ids yet canonical OwnerRef seam transport remains correct. A wrong lexical-order rival disagrees for the intended reason.
- Forced 6.4 EpochRebind scrambles physical rows with logical ids fixed and persistent layout remains bit/identity stable; a physical-row/allocation-order rival disagrees for the intended reason.
- `planet_id` and `star_system_gridcell_id_raw` are absent from engine src; `PlanetChildRfScopeKey` is at most the existing alias to `OwnerChannelScopeKey`. Full blast-radius + hosted workflow are green. Return PROBATION / proof-present / DA-review-pending with exact base/head/tested_code_sha, live HD receipt, coding receipt/stamp + ACKs, mint census/dispositions, owner-interner and seam evidence, forced-rebind evidence, retired-gate/exclusion proof, local battery counts and hosted identities. Coding does not `/clearance`, `/relay-lint`, merge, move the pointer, or begin 10.1.
