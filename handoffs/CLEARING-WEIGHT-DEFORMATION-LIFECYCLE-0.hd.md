---
rung: CLEARING-WEIGHT-DEFORMATION-LIFECYCLE-0
kind: rung
track: 0.0.8.7
base_sha: 8140206d66f3bfa6246227abd6c095a9d2ba22e3
audience: coding
model_tier: frontier
expected_route: DA-RESERVE(binding)
owner_approved: true
owner_notes: "Owner-approved final Phase-13 rung; authority Board 5469735502. Engineering-review absent-edge branch is binding: no production dynamic-deformation caller exists, so this rung integrates clearing weights with the existing 7.8a invalidation lifecycle and proves it end-to-end without adding runtime deformation wiring. Post-graduation Owner hold 5466933355 remains binding: no closeout or next-track work. Orchestrator ORIENT-RECEIPT 6b6defc57f42; rule stamp dd6572b07cbd2905."
surfaces: ["crates/simthing-kernel/src/clearing_weight_projection.rs", "crates/simthing-kernel/src/derived_span_projection.rs", "crates/simthing-kernel/src/overlay_prep.rs", "crates/simthing-kernel/src/lib.rs", "crates/simthing-spec/src/spec/flow_market.rs", "crates/simthing-spec/src/spec/mod.rs", "crates/simthing-spec/src/lib.rs", "crates/simthing-driver/tests/clearing_weight_deformation_lifecycle_0.rs", "crates/simthing-driver/tests/clearing_weight_span_unification_0.rs", "crates/simthing-driver/tests/stemthing_b_flow_market_germ_0.rs", "scripts/ci/test_inventory.tsv", "scripts/ci/anchor_reach_log.tsv", "scripts/ci/closeout_artifacts.tsv", "docs/tests/clearing_weight_deformation_lifecycle_0_results.md", "docs/tests/current_evidence_index.md"]
forbidden: ["any new dependency registry, changed-locus vocabulary, scheduler, cache, runtime registration surface, or second invalidation path", "projection caching, GPU clearing, profile interning work, sparse-K optimization, benchmarking campaign, or any other PERFORMANCE-TRACK implementation", "production runtime dynamic-deformation wiring or a new production caller; the 13.8 witness is the authorized authored-operand change", "retaining a panic-bearing Index<&SimThingId> production vocabulary after migration to effective_weight", "changes to constrained-clearing arithmetic, eligibility, epsilon, remainder/tie law, DecimalField exactness, generation authority, or replay semantics", "substantive rewrites of the 13.6 matrix or germ authored cases/assertions/expected values; only unavoidable call-form plumbing for Index removal is permitted", "kernel -> spec dependency or any new Cargo dependency/manifests/lockfile changes", "any .github/workflows/** or scripts/ci/**/*.sh or scripts/ci/**/*.py gate-code edit", "pointer movement, Phase-13 graduation/closeout, closeout prep/apply/reap, orientation-guide amendment, next-track authoring, or engineering-review ceremony from coding"]
required_checks: ["before edits render/read this handoff; fresh coding-role orientation must be bound to the exact merged handoff dispatch master named by the Board dispatch and ACK every rendered REQUIRED-ANCHOR", "FIRST STEP mechanically enumerate the current empty-index admission site, existing ChangedLocus/DerivedDependencyBinding/DerivedDependencyTarget/invalidate/remap_range lifecycle, every clearing-weight Index consumer, and every production dynamic-deformation caller; DA premise is zero production dynamic-deformation callers and zero production Index consumers, and any mismatch is STOP", "FIRST STEP capture dispatch-base blobs/results for the 13.6 matrix and germ and publish their authored assertion/expected-value census before edits", "bind default/override operand source loci into the existing DerivedDependencyIndex using existing 7.8a ChangedLocus vocabulary and existing dependency targets; no parallel locus/registry or writer-source discriminant", "document and implement the clearing-weight lifecycle as eager evaluation plus ChangedLocus -> existing index -> affected-range invalidation -> range-local rebuild; caching that does not consume the index is explicitly unlawful and must not be added", "add one end-to-end witness that changes an authored operand, emits the corresponding ChangedLocus, proves only affected spans/profiles rebuild while unaffected profile identity stays stable, then clears a next-generation claim through the existing constrained-clearing/germ path with the changed effective weight", "remove/narrow the panic-bearing Index<&SimThingId> production surface so effective_weight Option lookup is the sole production participant lookup; mechanically migrate only test call-form use needed for compilation and preserve seal oracle content", "append exactly one dated PERFORMANCE-TRACK owed-measurement lease row for clearing_weight_projection.rs sparse-K build complexity; record the debt and do not optimize it", "preserve 13.6 representation and constrained-clearing semantics exactly; no new production deformation caller, cache, GPU path, interning campaign, or secondary weighting authority", "ledger any new test, append anchor-reach rows only as required, add one results packet/current-evidence row, and run artifact-expiry plus decommission dry-run so the new lease row is proven non-reap cargo", "run focused lifecycle/invalidation, 13.6 matrix, germ, derived-span, and constrained-clearing seals; then agent/doctrine/orientation/handoff/anchor/diff checks, hosted Doctrine Scan/Exec, and the procedure-of-record full structural certificate with 0 failed", "return PROBATION / proof-present / DA-review-pending with exact dispatch base/head, archaeology counts, source-locus-to-target map, dependency-index proof, affected-only refresh metrics, next-generation claim proof, Index-removal search, seal diff proof, ledger row, structural certificate, hosted run IDs, HD receipt, and fresh body/head-bound /clearance plus /relay-lint; coding never merges or graduates"]
stop_conditions: ["the DA premise is false: a production dynamic-deformation caller or production Index consumer exists at dispatch master", "the clearing-weight source loci cannot be represented with existing 7.8a ChangedLocus/dependency-target vocabulary without minting a parallel registry or source discriminator", "the index cannot invalidate/rebuild only the affected clearing-weight ranges while keeping unaffected profile identity stable", "the changed authored operand cannot be observed by a next-generation claim without adding production runtime deformation wiring", "removing the panic-bearing Index form requires changing a seal's authored scenario, assertion, or expected value rather than call-form plumbing", "any proposed fix changes clearing arithmetic, eligibility, epsilon/tie/remainder law, DecimalField exactness, generation authority, replay, or introduces kernel -> spec", "any focused/workspace/doctrine/orientation/hosted test becomes red under the zero-red law", "scope expands into caching, GPU clearing, interning, sparse-K optimization, closeout, pointer movement, next-track work, or any other performance-track implementation"]
---
## BUILD
- Close the 13.6 lifecycle gap by making clearing-weight projection admission carry real source dependencies in the **existing** 7.8a `DerivedDependencyIndex`. Reuse `ChangedLocus`, `DerivedDependencyBinding`, `DerivedDependencyTarget`, `invalidate`, and `remap_range`; do not invent another invalidation vocabulary.
- First publish mechanical archaeology. Pin the empty-index site (`DerivedDependencyIndex::admit(Vec::new())`), the exact existing 7.8a invalidation/rebuild seam, all clearing-weight `Index` consumers, and all production dynamic-deformation callers. The DA-authorized branch expects zero production deformation callers and zero production Index consumers; a counterexample stops the rung.
- Admit dependencies from every clearing-weight default/override operand source locus. Source identity remains writer-blind `ChangedLocus(logical_id, property_id, role)` vocabulary; bind each locus to the existing logical member/subtree target actually affected by that operand. Default-source changes must cover the default's lawful scope; override-source changes must not dirty unrelated spans.
- Give `ClearingWeightSpanProjection` the narrow lifecycle seam needed to consume those loci: eager values at admission, then `ChangedLocus -> dependency_index -> affected_ranges -> remap_range` at the supplied generation. Rebuild only invalidated semantic spans and preserve maximal/profile semantics outside them. No cache or runtime dependency mutation surface is authorized.
- Witness the engineering-review chain **through the claim**. In a focused authored test, change one operand, form its existing `ChangedLocus`, refresh the affected clearing-weight spans, prove unaffected profile identity stays stable, and use the refreshed effective weight in an existing next-generation constrained clear so the grant/claim outcome reflects the changed operand.
- Narrow away the panic-bearing `Index<&SimThingId>` production API. `effective_weight(SimThingId) -> Option<f32>` is the sole production lookup vocabulary. If the 13.6 seal uses indexing, migrate only that call form; its authored cases, assertions, expected values, and semantic oracle remain unchanged.
- Append one lease-ledger rider on `crates/simthing-kernel/src/clearing_weight_projection.rs`: `consumer=PERFORMANCE-TRACK`, dated `owed-measurement:2026-08-30`, naming the sparse-K build-complexity debt. Record it; do not optimize or benchmark it here.
- Return PROBATION / proof-present / DA-review-pending. Coding does not merge, graduate, move the pointer, invoke closeout, or open the performance track. Owner hold `5466933355` resumes immediately after 13.8 graduation.

## FENCES
- **One lifecycle authority.** Clearing weights consume the existing 7.8a source-blind dependency index and range-local rebuild machinery. A clearing-specific registry, scheduler, cache, dirty map, source enum, or writer discriminator is a RED.
- **Absent-edge branch stays absent.** Do not wire dynamic EML/Overlay deformation into production merely to manufacture a caller. The rung's live operand change is a proof witness, not new runtime feature wiring.
- **Affected-only means falsifiable.** The witness must distinguish affected from unaffected spans/profile identities and show no unrelated semantic refresh; merely reporting a non-empty dependency count is insufficient.
- **Semantic-neutral.** 13.6 weight representation plus constrained-clearing integer math, eligibility, epsilon, ties/remainder, DecimalField exactness, generation pacing, and replay are frozen.
- **Seals stay seals.** The 13.6 matrix and germ may receive only mechanical lookup/plumbing edits forced by removal of `Index`; authored fixtures, assertions, expected values, and clearing outcomes stay unchanged.
- **Performance remains banked.** No caching, GPU clearing, interning, sparse-K optimization, benchmark campaign, or other performance-track ladder work.
- **Containment and governance.** `kernel -> spec` remains forbidden; no new dependency edges, gate code, pointer/closeout work, or next-track authoring.

## EXIT-PROOF
- **Archaeology:** exact source-locus inventory, empty-index site, existing 7.8a invalidation seam, Index consumer census, and production dynamic-deformation caller census. Any DA-premise mismatch is reported as STOP.
- **Dependency correctness:** publish the admitted locus->target rows and binding count; falsify wrong targeting by proving one operand change dirties/rebuilds exactly the expected ranges while an unaffected profile identity/weight remains bit-stable.
- **End-to-end lifecycle:** authored operand N changes -> existing `ChangedLocus` -> existing dependency index -> affected-only refresh at generation N+1 -> next-generation constrained claim consumes the refreshed weight and produces the expected changed clear. Repeating the same stamped inputs replays identically.
- **Index removal:** production `Index<&SimThingId>` implementation/usage is absent; `effective_weight` is the production lookup. Any seal-file delta is reported and mechanically shown to be call-form-only.
- **Regression seals:** 13.6 matrix and germ scenarios/assertions/expected values retain dispatch-base truth and pass; derived-span invalidation tests and constrained-clearing seals remain green.
- **Ledger:** one new valid lease row names sparse-K clearing-weight build complexity with dated owed-measurement and PERFORMANCE-TRACK consumer; artifact-expiry PASS and decommission dry-run do not classify it for reap.
- **Structural/routing:** exact changed-file census, procedure-of-record structural certificate with 0 failed, focused + doctrine/orientation/anchor/handoff/agent checks green, hosted Doctrine Scan/Exec green, and fresh head/body-bound `/clearance` plus `/relay-lint`. Remain PROBATION for orchestration/DA review.
