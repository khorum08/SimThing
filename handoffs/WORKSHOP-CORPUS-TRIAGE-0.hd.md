---
rung: WORKSHOP-CORPUS-TRIAGE-0
kind: rung
track: 0.0.8.7
base_sha: 3adca292fe5f0d678b34a5bfafeb4e0a531c9edd
audience: coding
model_tier: standard
expected_route: ORCHESTRATOR-CLEARABLE
owner_approved: true
owner_notes: "Phase-13 authority: Board 5462798074 rev 2; 13.3 graduation and 13.4 authorization: Board 5465885405. Fresh post-stamp orchestrator orientation: ORIENT-RECEIPT a8d8ecca1829; rule stamp 4660c85703ef2f33. Zero-red law is active; kernel containment binds."
surfaces: ["crates/simthing-workshop/**", "docs/workshop/archive/c1_perf_reframe_memo.md", "docs/workshop/archive/field_policy/**", "scripts/ci/closeout_artifacts.tsv", "scripts/ci/test_inventory.tsv", "scripts/ci/test_lifecycle_parked.tsv", "scripts/ci/test_residue_classes.tsv", "scripts/ci/anchor_reach_log.tsv", "docs/tests", "docs/tests/current_evidence_index.md", "docs/track_closeout_protocol.md", "docs/design_0_0_8_7_rf_arena_modernization.md", "docs/orchestrator_orientation.md"]
forbidden: ["default-delete or bulk reap without a per-asset Necessity-Test disposition", "a lease without a named consumer and date", "chartering or opening the post-closeout PERFORMANCE TRACK in this rung", "citing an inherited performance debt as motivation without a dated current-head measurement or dated owed-measurement marker and a live named instrument", "modernizing or repairing an archived Gu-Yang instrument merely to make the bounded re-measurement run; if stale, record instrument-stale instead", "any production engine/spec/kernel/gpu/sim/driver/embedder semantic change", "any .github/workflows/** or scripts/ci/**/*.sh or scripts/ci/**/*.py gate-code edit", "any 13.5/13.6 implementation, pointer movement, closeout apply, or track closure", "any workspace test failure after 13.3"]
required_checks: ["before edits render/read this handoff; perform fresh Standard coding-role orientation on the exact dispatch master and ACK every rendered REQUIRED-ANCHOR", "FIRST STEP run the live closeout discovery for track 0.0.8.7 and mechanically enumerate every reap-slated simthing-workshop asset; do not substitute a handpicked list of the named performance families", "for every reap-slated workshop asset record exactly one keep/delete/elevate/lease disposition with named consumer or dated rationale; deletions must name the higher-rung owner that makes the asset redundant", "pre-write the artifact-ledger rows required for closeout into scripts/ci/closeout_artifacts.tsv so the later closeout sweep executes decisions rather than makes them; TSV growth is limited to the disposition rows required by this rung", "evaluate persistent_bench plus its GPU WGSL, multitarget_replay plus WGSL, transfer_contention, overlay_order pivots, and eml_phase5 specifically for the named post-closeout PERFORMANCE TRACK lease consumer", "A1 cargo is BY NAME: docs/workshop/archive/c1_perf_reframe_memo.md plus the live archived field-policy tensor-stencil preservation set and crates/simthing-workshop/src/persistent_bench.rs with its GPU WGSL; reconcile the design-row wildcard to the actual tree rather than inventing a missing filename", "attempt one bounded gather-vs-tiled-stencil re-measurement at the current head using the archived instrument as-is; if it runs, record measured:<YYYY-MM-DD>@<full-commit> plus the fresh number in the lease row; if it no longer runs, record instrument-stale:<YYYY-MM-DD> and make re-measurement the performance track's FIRST-RUNG PRECONDITION", "do not repair the archived instrument during the bounded re-measurement; an instrument-stale result is a lawful outcome", "apply the same strong rule to every inherited performance debt: a dated current-head measurement or dated owed-measurement marker plus named live instrument must exist before a future performance-track opening ritual can cite the debt", "build/check the deterministic closeout disposition manifest and run a non-mutating closeout rehearsal/dry-run against the pre-written decisions; do not run --apply or close the track", "run focused workshop/ledger checks, track_closeout.sh --prove as applicable, doctrine/orientation/anchor/agent-delta checks, hosted Doctrine Scan, and the procedure-of-record workspace certificate; expected certificate has 0 failed and any red is STOP", "return PROBATION / proof-present / orchestration-review-pending with exact base/final SHA, changed-file census, full disposition counts, named lease rows, A1 measurement/stale result, dry-run result, workspace certificate, hosted run ids, HD-RECEIPT, and fresh body/head-bound /clearance plus /relay-lint evidence; coding does not merge, graduate, move the pointer, start 13.5+, apply closeout, or close the track"]
stop_conditions: ["the live discovery leaves any reap-slated workshop asset without a disposition row", "a proposed delete lacks a named Necessity-Test owner or a proposed lease lacks a named consumer/date", "the Gu-Yang debt lacks the named archived instrument plus either a dated current-head measurement or dated instrument-stale marker", "any inherited performance debt would be left as undated prose", "the work requires changing an archived instrument or production engine semantics to manufacture a measurement", "the closeout dry-run/check-eval is not clean against the pre-written decisions", "any test is red under the post-13.3 zero-red law", "the work would start 13.5/13.6, apply closeout, move the pointer, or close the track"]
---
## BUILD
- **Binding ladder row, verbatim:** **Rung 4: replace default-delete with Necessity-Test triage of the reap-slated workshop corpus against NAMED future consumers.** The post-closeout PERFORMANCE TRACK (Board-banked seed ladder from the 2026-08-29 engineering review) is a named lease consumer: `persistent_bench` (+GPU WGSL, the measurement harness), `multitarget_replay` (+WGSL), `transfer_contention`, `overlay_order` pivots, `eml_phase5` are evaluated FOR that consumer. Every workshop asset gets a keep/delete/elevate/lease disposition row with named consumer or dated rationale — the artifact-ledger rows are PRE-WRITTEN so the closeout sweep executes decisions instead of making them. **A1 (Owner-approved fold, 2026-08-29):** the Gu-Yang gather-vs-tiled-stencil debt gets the ledger treatment it never had. The debt's instrument survives ONLY as archived sandbox preserves (`docs/workshop/archive/field_policy/field_policy_tensor_stencil_*_sandbox_code_preserve.{rs,wgsl}` + `c1_perf_reframe_memo.md`, 5.7/C1-era) and its number was measured against a kernel two arcs stale (accumulator uniform, field-sweep seams, and EML consumers all moved since). (1) These artifacts are NAMED triage cargo leased to the performance track BY NAME as this debt's instrument. (2) BOUNDED RE-MEASUREMENT: attempt one gather-vs-tiled re-run at the current head and record the fresh number with date+commit in the lease row; if the archived instrument no longer runs against the current kernel, record THAT, dated, with re-measurement owed at perf-track opening. Either outcome upgrades the debt from folk number to ledger entry.
- **Exit law, verbatim:** Every reap-slated asset has its ledger row; leases carry the performance-track consumer and a date; deletions carry rationale; the closeout sweep dry-run is clean against the pre-written ledger; TSV growth bounded to the disposition rows themselves. **A1 ENFORCEMENT (Owner-directed, STRONG):** the performance track may NOT be chartered around an unmeasured or stale-measured debt. The gather-debt lease row must carry either `measured:<date>@<commit>` with the fresh number or `instrument-stale:<date>` with re-measurement as the perf track's FIRST-RUNG PRECONDITION — and this is LAW for the whole inherited class: every performance debt the track inherits requires a dated current-head measurement (or a dated owed-measurement marker) in the ledger before the track's opening ritual may cite it as motivation. A debt without a dated measurement and a live instrument is prose, and prose cannot charter a track. The closeout receipt restates this condition so the next track's opening verifies it mechanically.
- Live archaeology at dispatch: `scripts/ci/closeout_artifacts.tsv` is header-only, so 13.4 authors the first closeout artifact decisions rather than inheriting hidden dispositions.
- The named current workshop instruments exist: `persistent_bench{,_gpu.wgsl,_report.rs}`, `multitarget_replay{,_gpu.wgsl,_report.rs}`, `transfer_contention{,_gpu.wgsl,_report.rs}`, `overlay_order{,_current.wgsl,_pivot.wgsl,_report.rs}`, and `eml_phase5{,.wgsl,_hardcoded.wgsl}`; their report artifacts live under `crates/simthing-workshop/tests`.
- The archived memo is exactly `docs/workshop/archive/c1_perf_reframe_memo.md`. The live field-policy archive contains `field_policy_tensor_stencil_refinement_sandbox_code_preserve.rs` and `field_policy_tensor_stencil_wgsl_sandbox_code_preserve.rs` plus the stencil prototype/refinement WGSL family. Resolve the row's wildcard against this actual tree; do not fabricate a nonexistent preserve path.
- `track_closeout.sh` is the execution mechanism: discover/build/check-eval/prove are evidence here; `--apply` belongs to later closeout and is fenced.

## FENCES
- This is triage and ledger truth, not a workshop refactor and not the performance-track opening. Preserve the instruments you lease; do not improve their algorithms here.
- Necessity Test is per asset. No default-delete, no family-wide inference without enumerated rows, and no lease whose consumer is merely “future work.”
- PERFORMANCE TRACK is a named **future** lease consumer only. Its banked seed is descriptive; `simthing_core_design.md` remains law.
- Kernel containment remains absolute; application/workshop evidence converges onto the kernel and cannot motivate a lower-kernel accommodation.
- Data-ledger edits are allowed; gate-code edits are not. Do not blanket-fence `scripts/ci/**`.
- The post-13.3 zero-red law is active. Any red is a STOP rather than inherited-baseline prose.
- Phase 13 remains one rung in flight. 13.5, 13.6 and closeout stay unopened.

## EXIT-PROOF
- **Corpus proof:** deterministic discovery census + complete per-asset disposition table; no reap-slated workshop asset missing.
- **Necessity proof:** every delete names its owner; every elevate names its destination; every lease names consumer/date.
- **Performance-debt proof:** all inherited debts cited for the future performance track have a live named instrument plus dated measurement or dated owed marker.
- **A1 proof:** named Gu-Yang archived cargo + one bounded current-head attempt, producing either `measured:<date>@<commit>` + number or `instrument-stale:<date>` + first-rung remeasurement precondition.
- **Closeout proof:** pre-written artifact ledger and deterministic manifest pass check-eval/dry-run without applying closeout; report TSV before/after growth and ensure only disposition rows were added.
- **Containment proof:** no production semantic change, no gate-code edit, no 13.5+ work, no pointer movement, no track closure.
- **Certificate proof:** focused checks and procedure-of-record workspace certificate finish with 0 failed; any red is STOP.
- **Hosted/routing proof:** hosted Doctrine Scan green at exact final head; fresh `/clearance` then `/relay-lint`; return exact head, HD-RECEIPT, results/manifest paths, disposition counts, A1 result and workflow ids.
